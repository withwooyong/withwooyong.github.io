/**
 * 「개발 리더로서의 철학」 본문.
 *
 * 구 `pages/index.tsx` 의 `#about` 섹션(HEAD 기준 159~170줄)에 JSX 문단으로 박혀 있던
 * 것을 그대로 옮겼다. Task 10 의 전면 재작성으로 2·3문단이 추적 파일에서 사라질 뻔했다.
 *
 * **문자열은 한 글자도 바꾸지 않는다.** JSX 가 렌더하던 대로 연속 공백만 한 칸으로
 * 접었고(원본은 들여쓰기 때문에 줄바꿈이 들어 있었다) 그 밖의 수정은 없다.
 * 어투(「~습니다」)도 원문 그대로다 — 신규 섹션의 평서체와 다른 것은 의도가 아니라
 * **보존이기 때문**이며, 어투를 맞추는 것은 문구를 다시 쓰는 T12 의 판단이다.
 *
 * ⚠️ **소비자: T12 (`/about`).** 지금 이 데이터를 렌더하는 페이지는 하나도 없다.
 *    `components/home/section-now.tsx` 는 1문단의 **요지만 한 문장으로 압축**해 싣고 있고
 *    원문을 싣지 않는다. 철학 전문이 필요하면 그 페이지가 아니라 여기다.
 */
export const leadershipPhilosophy: string[] = [
  "개발의 실행은 이미 AI로 옮겨가고 있습니다. 리더가 할 일은 무엇을 만들지 정하는 판단과, AI가 안전하게 일할 수 있는 틀을 세우는 것이라고 믿습니다. 좋은 코드의 기준도 사람이 읽기 좋은 코드에서, AI가 정확히 읽고 안전하게 고칠 수 있는 구조로 옮겨가고 있습니다. 20년간 개발과 리딩을 함께 해 오며, 이 기준을 팀의 표준으로 세우는 일이 결국 팀의 속도와 서비스의 안정성을 만든다고 믿습니다.",
  "좋은 제품은 기획·디자인·개발이 같은 그림을 볼 때 나온다고 믿습니다. 기획, UI/UX부터 프론트, 백엔드, 앱, 데브옵스까지 전 직군을 총괄하며, 요구사항을 그대로 구현하기보다 문제 정의 단계부터 기획자·디자이너와 함께 고민해 사용자 경험과 기술 구조가 어긋나지 않는 접점을 찾아 왔습니다. 개발 리더가 제품과 사용자의 언어를 함께 쓸 때, 팀의 결과물은 기능을 넘어 제품이 됩니다.",
  "기술적 결정은 팀이 감당할 수 있는 복잡도 안에서 내리고, 위임과 코드 리뷰를 통해 동료의 성장이 곧 조직의 성장이 되도록 이끕니다. 리더의 일은 아키텍처 표준과 코드 리뷰·보안 게이트, 검증 파이프라인을 세워 AI가 안전하게 일하도록 만드는 것이라 보고, 야나두에서도 다양한 챗봇 형태의 AI 서비스를 직접 개발·런칭하며 이 방식을 실천해 왔습니다.",
];

/** 구 `#about` 섹션의 제목들. 문구를 다시 쓸 때 원본이 무엇이었는지 남긴다. */
export const aboutHeadings = {
  section: "소개",
  philosophy: "개발 리더로서의 철학",
} as const;

/**
 * 구 `#about` 섹션의 요약 카드 3장. HEAD 기준 176~204줄의 `CardTitle` 과 `CardContent` 다.
 *
 * ⚠️ **셋 중 `전문 분야` 는 이 파일이 유일한 사본이다.** 나머지 둘은 의미가 다른 곳에도
 *    남아 있다 — `직전 포지션` 은 `data/experience.ts` 의 첫 항목(`role`·`company`·`period`·
 *    `duration`)과, `팀 규모` 는 같은 항목의 `summary` 와 겹친다. 그러나 **의미가 남은 것과
 *    문자열이 남은 것은 다르고**, 어느 쪽을 쓸지는 문구를 다시 쓰는 T12 의 판단이다.
 *    그래서 셋을 전부 원문 그대로 둔다.
 *
 * 구 카드는 아이콘과 4색 액센트(`text-blue-600` 등)를 함께 갖고 있었으나 그것은 콘텐츠가
 * 아니라 구 디자인의 표현이라 옮기지 않았다 — `data/experience.ts` 의 `companyClass` 를
 * 신규 컴포넌트가 소비하지 않는 것과 같은 이유다.
 *
 * ⚠️ **소비자: T12 (`/about`).** 지금 이 데이터를 렌더하는 페이지는 하나도 없다.
 */
export const aboutFacts: { label: string; value: string }[] = [
  {
    label: "직전 포지션",
    value:
      "(주)야나두 a kakao company (구 카카오키즈) 커머스개발실장 (2022.02 ~ 2026.07 · 4년 6개월)",
  },
  { label: "전문 분야", value: "AI 서비스, N-Screen, OTT, STB, CMS 개발" },
  { label: "팀 규모", value: "20~30명 개발팀 총괄 경험" },
];
