/**
 * 축자 대조용 정규화 — `dup-scan` 과 `source-overlap` 의 **공용 정본**이다.
 *
 * 🔴 이 모듈이 생긴 이유. 2026-09-02 이전에는 두 검사기가 같은 정규화를 각자
 *    손으로 복사해 두고 있었고, `source-overlap` 의 자기 검사 ⑫-c 는 진짜
 *    `dup-scan.normalizeLine` 이 아니라 **같은 파일 안의 또 다른 손복사본**과
 *    비교했다. 그래서 `dup-scan` 쪽 문자군에서 물결표 하나를 빼 두 정규화를
 *    어긋나게 해도 자기 검사가 **21/21 을 그대로 냈다**. 「같은 정규화다」라는
 *    출력 라벨이 거짓이 되는데 검사는 아무 말도 하지 않는 상태였다.
 *
 *    ⇒ 검사로 드리프트를 잡으려 하지 말고 **드리프트가 생길 자리를 없앤다.**
 *    정의가 하나뿐이면 두 검사기가 갈라질 방법이 없다. 이것은 CLAUDE.md 의
 *    「금칙어 목록의 정본은 코드다 — 문서로 복사하지 마라」와 같은 원리다.
 *
 * ⚠️ 이 함수를 고치면 `dup-scan` 과 `source-overlap` 이 **함께** 바뀐다.
 *    양쪽 자기 검사(`dup-scan:verify` · `source-overlap:verify`)를 모두 돌려라.
 */

/**
 * 공백 제거 정규화. 마크다운 기호를 걷어내면 표 셀 경계를 넘는 일치까지 잡힌다 —
 * `| 셀A | 셀B |` 와 산문 `셀A 셀B` 가 같은 문자열로 수렴하기 때문이다.
 *
 * 기호를 지우는 것이 핵심이다. 지우지 않으면 원본 문장을 그대로 복사한 뒤
 * `**` 두 쌍만 씌워도 0건이 된다 (실측: 축자 27자 → 굵게를 씌우면 0자).
 */
export function normalizeStripSpace(raw) {
  return raw
    .replace(/\[([^\]]*)\]\([^)]*\)/g, "$1") // 링크는 제목만 — URL 은 겹쳐도 복제가 아니다
    .replace(/[|*_`#>~]/g, "")
    .replace(/\s+/g, "")
    .trim();
}
