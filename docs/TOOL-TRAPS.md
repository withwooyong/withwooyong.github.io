# 도구 함정 — 재현과 실측

> ⚠️ **2026-08-29 복원본이다. 이 문서는 롤백(`27ae83d`) 이전에 쓰였다.**
>
> 2026-08-19 이후 88커밋이 되돌아가면서 이 문서도 함께 삭제됐고,
> **함정 자체는 여전히 유효하므로** 태그 `pre-rollback-2026-08-29` 에서 그대로 되살렸다.
> 다만 아래 항목들은 **지금 저장소에 없는 코드를 재현 대상으로 지목한다** —
> 함정의 원리는 읽되, 재현 절차를 그대로 실행하려 하지 마라.
>
> | 사라진 대상 | 언급 | 해당 항목 |
> | --- | ---: | --- |
> | 아틀라스 (`pages/atlas/`·`lib/atlas/`) | 15회 | #35 등 |
> | Pagefind (검색 색인) | 7회 | #22 |
> | Playwright / E2E (`e2e/`) | 5·4회 | #19 · #20 · #27 · #31 |
>
> 되살리려면 `git checkout pre-rollback-2026-08-29 -- <경로>` 로 코드를 먼저 가져와야 한다.
> 자세한 경위는 `HANDOFF.md` §2·§3.


`CLAUDE.md` 의 「이 환경의 도구 함정」 표에 대한 근거 문서다.

| 문서 | 담는 것 | 로드 |
| --- | --- | --- |
| `CLAUDE.md` 표 | **공통 원리로 막을 수 없는 것만.** 미리 알지 못하면 반드시 당하는 개별 지뢰 | 매 세션 자동 |
| 이 문서 | **전수 목록과 어떻게 알아냈나.** 재현 절차·실측 수치·당시 경위 | 필요할 때 열람 |

나눈 이유는 하나다. 함정표가 커밋 19번에 걸쳐 0 → 31행으로 단조증가했고 한 번도 줄지 않았는데,
`CLAUDE.md` 는 매 세션 전량 로드되므로 그 증가가 **모든 세션의 고정 비용**이 됐다.
그다음 한 걸음 더 갔다 — 아래 「공통 원리」로 흡수되는 항목은 `CLAUDE.md` 에서 **원리 한 문장으로 대체**했다.
사례를 다 읽는 것보다 원리 하나를 아는 쪽이 상기율이 높고, 사례가 늘어도 고정 비용이 늘지 않는다.

**근거를 지우지 않고 옮긴 이유** — 근거 없는 규칙은 다음 사람이 지운다. 여기 남아 있으면
`CLAUDE.md` 의 한 줄이 왜 그렇게 쓰였는지 언제든 확인할 수 있다.

## 분류

| 갈래 | 번호 | 개수 | 성격 |
| --- | --- | --- | --- |
| 셸·grep 일반 | 1 · 2 · 3 · 4 · 5 · 6 · 36 · 38 · 43 · 44 · 48 | 11 | 어느 프로젝트에서나 참 |
| Windows·Git Bash 환경 | 8 · 9 · 10 · 14 · 16 · 28 · 33 · 35 · 37 · 39 · 40 · 41 | 12 | 이 개발 환경에서 참 |
| 도구·빌드체인·에이전트 | 11 · 21 · 29 · 32 · 42 · 45 · 46 · 47 · 49 | 9 | 이 하네스나 빌드 도구에서 참 |
| 이 리포 고유 | 7 · 12 · 13 · 15 · 17 · 18 · 19 · 20 · 22 · 23 · 24 · 25 · 26 · 27 · 30 · 31 · 34 | 17 | 이 코드베이스에서만 참 |

앞의 32건은 다른 프로젝트로 가져갈 수 있고, 뒤의 17건은 이 리포를 떠나면 의미가 없다.

⚠️ **39·40 은 이 표에서 빠져 있었다.** 41 을 넣으며 채웠다 — 항목을 추가하고 분류 표를
고치지 않으면 개수가 조용히 어긋난다.

⚠️ **같은 일이 `CLAUDE.md` 쪽에서 다시 일어났다.** 42 를 여기 넣으면서 `CLAUDE.md` 의
「전체 41건(809줄)」을 고치지 않아, 실제가 42건 870줄인 동안 그 문장이 계속 거짓이었다.
이 문서를 늘렸으면 **가리키는 쪽의 수치도 같은 커밋에서 고친다.**

## 공통 원리

**「없다」와 「읽을 수 없었다」가 같은 출력으로 나온다.** 거의 전부가 같은 모양이다.

- 0건이 나오면 검사기가 동작한 것인지 입력에 닿지도 못한 것인지 구분되지 않는다 (5 · 22 · 25 · 28 · 41 · 43)
- 종료 코드 0이 통과인지 아무것도 안 돌린 것인지 구분되지 않는다 (1 · 19 · 20 · 24)
- 초록이 규칙이 지켜진 것인지 애초에 후보가 아니었던 것인지 구분되지 않는다 (17 · 29 · 30)
- **빨강이 뮤턴트를 죽인 것인지 엉뚱한 것을 죽인 것인지 구분되지 않는다** (35) — 30번의 거울상이다

그래서 대응도 하나로 모인다. **확실히 존재하는 대조군을 먼저 측정하라.**

---

<a id="t1"></a>
### 1. 파이프 뒤 종료 코드

`$?` 는 **마지막** 명령의 것이다. `grep … | sort | uniq -c` 는 grep이 0건이어도 항상 0으로 끝난다.

**대응** — 종료 코드를 읽을 작정인 명령은 파이프 없이 단독으로 실행한다.

<a id="t2"></a>
### 2. 한글 1인칭

grep이 양방향으로 실패한다.

| 방향 | 예 |
| --- | --- |
| 오탐 | 「동시성 문**제가**」 · 「메이**저는**」 |
| 누락 | 「**내** 검색 커리어」 |
| 넓히면 홍수 | `내 [가-힣]{2,6}` → 「인덱스 **내** 문서」가 쏟아진다 |

**대응** — 매칭된 줄을 열어 읽는다. **1인칭은 검사기에 넣지 마라.** 자동화가 불가능한 항목이다.

<a id="t3"></a>
### 3. 한글 종결어미는 줄 끝에 오지 않는다

「…있습니다.」는 마침표로 끝나므로 `습니다$` 는 **아무것도** 맞히지 못하고 구조적 0을 보고한다.
브리프에 「~로 끝나는 줄」이라고 쓰면 에이전트가 정확히 그 앵커 패턴을 짠다.

**대응** — 앵커가 아니라 **부분 문자열**로 센다. 줄이 아니라 출현 횟수를 센다 (한 줄에 둘이 올 수 있다).
그다음 0이 아님이 확실한 절과 대조한다.

<a id="t4"></a>
### 4. 마크다운 강조가 단어를 쪼갠다

`**IDOL**을` 은 `IDOL을` 에 걸리지 않는다. 한글은 조사가 바로 붙으므로 구조적으로 발생한다 —
영어는 띄어쓰기가 있어서 같은 문제가 생기지 않는다.

**대응** — 강조를 벗긴 패턴도 같이 검색한다.

<a id="t5"></a>
### 5. 로케일이 한글과 이모지를 가린다

`LC_ALL=C` 없이는 이모지 grep이 거짓 0을 낸다. 그렇다고 `LC_ALL=C grep -P '[\x{1F300}-\x{1FAFF}]'`
를 쓰면 `range out of order` 로 죽는데, **출력이 없는 것은 발견이 없는 것과 똑같이 보인다.**

**대응** — `LC_ALL=C` 를 고정하고 이모지는 UTF-8 바이트 패턴으로 매칭한다.
`|| echo "0건"` 을 절대 붙이지 마라 — 「파일 없음」과 「0건」이 같은 문자열이 된다.

<a id="t6"></a>
### 6. 단어 안의 부분 문자열

`grep -o '비용'` 이 「대**비용**(對比用)」도 센다.

**대응** — 히트한 줄을 열어 의도한 단어인지 확인한다.

<a id="t7"></a>
### 7. 축자 중복 검사의 맹점

`scripts/dup-scan.mjs` 는 문자열만 본다. "같은 뜻, 다른 표현"은 보지 못한다.

**대응** — 문자열뿐 아니라 뜻으로도 정의를 대조한다. 검사기의 0은 축자 중복이 없다는 뜻일 뿐이다.

<a id="t8"></a>
### 8. 히어독이 백슬래시 한 겹을 먹는다

따옴표를 씌운 `<<'EOF'` 에서도 정규식의 `\\` 가 `\` 로 도착해 `SyntaxError` 를 낸다.
홑겹은 살아남고 겹겹만 벗겨진다. 히어독 안의 셸 따옴표가 히어독 자체를 깨뜨릴 수도 있다.

**2026-08-27 재확인** — `node -e '…[^\\]…'` 를 Bash 도구로 넘겼더니 `[^\]` 로 도착해
`Unterminated regexp literal` 로 죽었다. 이 문서를 만드는 작업 중에 다시 걸렸다.

**대응** — 그런 파일은 `Write`/`Edit` 도구로 쓰거나 `String.fromCharCode(92)` 로 문자를 만든다.
검증 스크립트처럼 정규식이 든 것은 파일로 써서 실행하는 편이 확실하다.

<a id="t9"></a>
### 9. grep은 CRLF를 감지하지 못한다

CR을 줄 끝으로 처리하므로, CR이 **393개**인 파일을 "LF 전용"으로 보고했다.

**대응** — `tr -cd '\r' | wc -c` 로 바이트를 센다.

<a id="t10"></a>
### 10. `core.autocrlf=true` 가 줄바꿈 변경을 숨긴다

git이 정규화한 뒤 비교하므로, 통째로 다시 쓴 파일도 깨끗한 diff로 보인다.
이 리포는 실제로 CRLF와 LF가 섞여 있다.

**대응** — 깨끗한 diff는 아무것도 바뀌지 않았다는 증거가 **아니다.**
대조 파일을 두고 CR/LF를 직접 센다.

<a id="t11"></a>
### 11. 서브에이전트가 보고 없이 유휴 상태가 된다

`reviewer` 와 `scout` 에는 `Write` 도구가 없다. `scout` 과 `verifier` 가 통지 3번에 걸쳐
아무것도 내놓지 않은 적이 있다.

**대응** — 메시지가 아니라 파일로 판단한다. 유휴 통지마다 `ls -la <스크래치패드>` 를 돌리고,
그다음 `SendMessage` 로 보고서를 회수한다. 이때 **원래 질문을 그대로** 다시 보낸다 —
「아까 물어본 대로」라고 하면 껄끄러운 항목부터 떨어져 나간다.

<a id="t12"></a>
### 12. `grep -r` 이 `out/` 과 `node_modules` 를 훑는다

120초 타임아웃.

**대응** — `Grep` 도구를 쓰거나 `--include` / 명시적 경로를 준다.

<a id="t13"></a>
### 13. Tailwind가 주석까지 훑는다

클래스 추출기가 원본 텍스트에 대한 정규식이라 주석을 파싱해 걸러내지 않는다.
**주석 안**에 쓴 임의값 클래스가 진짜 CSS로 방출된다.

주석에 예시로 남긴 `bg-[url('./${logo}')]` 가 규칙이 되어 PostCSS가 해석을 시도했고,
빌드는 **아무도 import하지 않은 파일 이름**을 가리키는 `Cannot find module './${logo}'` 로 죽었다.
(사이트 리뉴얼 T1에서 실제로 발생)

**대응** — 주석에 대괄호 임의값 클래스를 절대 넣지 마라. 예시는 산문으로 쓰거나 필드 이름만 적는다.

<a id="t14"></a>
### 14. Git Bash의 `sed -i` 가 CR을 전부 지운다

파일을 LF로 다시 쓴다. CRLF 파일이면 CR **531개**가 조용히 사라지는데,
`core.autocrlf=true` 가 정규화하므로 `git diff` 는 여전히 한 줄짜리 깨끗한 변경으로 보인다.
**git 기반 검사 전부에 이 손상이 보이지 않는다.** (사이트 리뉴얼 T4에서 실제로 발생)

**대응** — CRLF 파일에 `sed -i` 금지. `Edit` 도구(부분 치환이라 주변 CR이 보존된다)나 Node로 읽고 쓴다.
**`Write` 는 대안이 아니다 — [16번](#t16) 참조.**
검증은 바이트 수(`tr -cd '\r' | wc -c`)로 한다. `git diff` 나 `cat -A` 로 하지 마라 —
MSYS의 `sed | cat -A` 도 CR을 지워서 어느 쪽이든 LF 전용으로 보고한다.

<a id="t15"></a>
### 15. Tailwind variant 클래스는 빌드된 CSS에 백슬래시를 문자로 갖는다

`.focus-visible\:ring-signal{…}` 에서 `\` 는 이스케이프가 아니라 셀렉터 텍스트의 일부다.
잘못 이스케이프한 패턴으로 grep하면 **0과 종료 코드 1** 이 나오는데,
이는 "Tailwind가 그 클래스를 방출한 적 없다"와 똑같이 읽힌다.

**실측 (같은 파일)**

| 명령 | 결과 |
| --- | --- |
| `grep -c 'focus-visible\\:ring-signal'` | 0 |
| `grep -cF 'focus-visible\:ring-signal'` | 1 |

태스크 브리프가 구현자에게 이중 백슬래시 형태를 그대로 건네준 것이 발단이었다.

**대응** — 빌드된 CSS 셀렉터는 `-F` 와 홑 백슬래시로 grep한다.
증명하지 않은 0으로 "클래스가 방출되지 않았다"를 결론짓지 마라 — 존재가 확실한 클래스를
대조군으로 먼저 검색한다.

<a id="t16"></a>
### 16. `Write` 도구가 전체 교체 시 CRLF를 떨어뜨린다

받은 문자열을 그대로 쓰는데 그 문자열에 CR을 넣을 실질적 방법이 없어서,
전체 파일 쓰기는 항상 LF로 떨어진다.

**실측 (3줄 CRLF 표본)**

| 도구 | CR | LF |
| --- | --- | --- |
| `Edit` | 3 | 3 |
| `Write` | **0** | 3 |

이것이 나쁜 이유는 [14번](#t14)이 `sed -i` 의 대안으로 `Write` 를 지목했었기 때문이다 —
**권장 해법의 절반이 같은 결함을 갖고 있었다.** T6 수정 라운드에서 `components/theme-toggle.tsx`
의 CR 52개가 0이 되며 실제로 터졌다.

**대응** — 먼저 파일의 줄바꿈을 확인한다(`tr -cd '\r' | wc -c`).
LF 파일이면 `Write` 무방. CRLF면 `Edit`, 전체를 바꿔야 하면 Node로.
그리고 항상 CR을 다시 센다 — `git diff` 는 이것을 보여주지 못한다.

<a id="t17"></a>
### 17. `next/head` 가 하이드레이션 때 head 태그를 다시 넣는다

`out/blog/index.html` 에서 `<link rel="canonical">` 을 통째로 지워도 E2E 검사는 **초록**이었다.
Next의 head 매니저가 하이드레이션에서 되돌려 놓고, 로케이터는 복원된 DOM만 본다.

태그는 배포되는 바이트에 실제로 없었고, 그것이 Slack·카카오톡 언펄과 대부분의 크롤러가 읽는 것이다.
`check-forbidden:built` 와 같은 모양이다 — 깨끗한 소스 ≠ 깨끗한 산출물,
여기서는 깨끗한 *DOM* ≠ 깨끗한 산출물. (T8에서 실제로 발생)

**대응** — **원본 응답**에 대해 단언한다: `page.request.get(path)` + 텍스트 정규식.
DOM도 보되 두 번째의 **별개** 측정으로 본다. 헬퍼는 `e2e/raw-html.ts` 에 있다.

<a id="t18"></a>
### 18. React가 `#__next` 안에 손으로 넣은 마크업을 버린다

셀렉터를 시험하려고 `out/index.html` 의 `#__next` 아래에 탐침 `<div>` 를 넣었더니 **0건**이 나왔다.
하이드레이션이 트리를 조정하며 떨궜다. 검사기는 멀쩡한데 **실험 쪽이 "검사기가 고장났다"로 읽힌다.**
(T8에서 E2E 셸 게이트를 증명하다가 발생)

**대응** — React의 영역 밖을 건드린다: `<body>` 의 속성이나 `<head>` 노드.
`<body data-site-shell>` 로 같은 탐침을 다시 돌리니 설계대로 게이트가 뒤집혔다.

<a id="t19"></a>
### 19. Playwright의 종료 코드는 양방향으로 거짓말한다

| 상황 | 종료 코드 | 왜 헷갈리나 |
| --- | --- | --- |
| `-g` 가 아무것도 못 맞힘 | **1** + `No tests found` | 진짜 실패와 구분되지 않는다 (실제 사례: 어떤 제목에도 없던 `-g "다크 기본"`) |
| `retries: 1` 로 두 번째에 통과 | **0** + `1 flaky` | 실측: 같은 스펙에서 `CI` 미설정 → 1, `CI=true` → 0 |

별개로 `list` 리포터는 `test.skip` 사유를 찍지 않아서, 게이트된 스위트가 설명 없는 침묵으로 보인다.

**대응** — `$?` 만이 아니라 요약 줄을 읽는다. `retries: 0` 을 유지한다 (이유는 `playwright.config.ts` 에 있다).
게이트 상태를 `describe` 제목에 넣어 skip이 스스로 설명하게 한다.

<a id="t20"></a>
### 20. `-g` 필터가 스위트의 skip을 정당화하던 센티넬을 없앤다

이 리포는 게이트된 스위트마다 **별도** `describe` 에 센티넬을 짝지어 둔다.
그래서 게이트된 테스트를 고를 만큼 좁은 필터는 센티넬도 같이 떨군다.

**실측 2026-08-27**

```
npx playwright test e2e/search.spec.ts -g "검색 팔레트" --project=desktop
  -> 4 skipped, 종료 코드 0
```

완벽한 조용한 초록이다 — 의미 있는 것이 전부 skip되고,
그것을 말해 줄 유일한 테스트가 필터로 빠졌기 때문에 성공을 보고한다.

**대응** — 필터를 건 실행의 초록을 통과로 취급하지 마라.
게이트 상태는 **전체** 실행으로 판단하고, `$?` 가 아니라 skip 수를 읽는다.
센티넬과 그 게이트는 한 몸이고 필터는 그것을 가른다.

<a id="t21"></a>
### 21. `npm ci --dry-run` 이 실제로 `node_modules` 를 지운다

시뮬레이션이 아니다. npm은 멈출지 결정하기 **전에** 트리를 지우므로,
"드라이런"이 설치 패키지 **0개**를 남긴다 (실측 590 → 0).

이후 모든 도구가 npm이 아니라 자기 이야기를 하며 실패한다.
그 시점에 `npm test` 를 읽은 병렬 에이전트가 `'vitest' is not recognized` 를 받고
**"vitest가 설치되지 않았다"고 보고했다.** 리눅스 전용 의존성이 CI에서 설치될지 확인하려다 발생.

**대응** — 무엇이 설치될지 보려고 `npm ci --dry-run` 을 쓰지 마라.
대신 `package-lock.json` 을 읽는다 (`os`/`cpu`/`optional` 필드에 플랫폼 정보가 있다).
이미 돌렸다면 `npm ci` 로 복구하고, 이후의 어떤 실패든 믿기 전에 `ls node_modules | wc -l` 로 확인한다.

<a id="t22"></a>
### 22. Pagefind 조각은 gzip이라 `out/` grep이 구조적으로 눈멀어 있다

`npx pagefind --site out` 이 `\037\213\b` 로 시작하는 `.pf_fragment` **242개**를 쓴다.
확실히 안에 있는 문자열도 `grep -a` 가 0을 낸다 — **대조군(`url`)도 0이므로**
그 0은 "없다"가 아니라 "읽을 수 없다"는 뜻이다.

`check-forbidden:built` 는 `out/blog` 를 훑으므로 이 파일들을 아예 보지 않는다:
배포 바이트에 본문 사본이 들어 있는데 어떤 검사기도 읽지 않는다.
손으로 한 번 확인했다 — 242개를 전부 펼쳐 3,992,070바이트, 스캔 결과 **그날은 유출 없음.**

**대응**

```
for f in out/pagefind/fragment/*.pf_fragment; do gzip -dc "$f"; done > /tmp/frag.txt
```

확실히 있는 대조 문자열을 항상 먼저 grep한다 — 압축된 입력에 대한 0은 증거가 아니다.
펼친 바이트도 순수 JSON이 아니다: 각 조각이 `pagefind_dcd` 접두로 시작해서
`JSON.parse` 가 `Unexpected token 'p'` 로 죽는다. 첫 `{` 부터 잘라 쓴다.

<a id="t23"></a>
### 23. `/` 로 시작하는 지정자에는 `declare module` 이 적용될 수 없다

`import("/pagefind/pagefind.js")` 가 TS2307을 던진다.
`types/pagefind.d.ts` 에 `declare module "/pagefind/pagefind.js";` 를 넣어도 아무것도 달라지지 않는다.

문제는 그다음이다. `tsc --noEmit --listFiles | grep types/pagefind` 가
**그 d.ts가 프로그램에 포함되어 있음을 확인해 준다** — "선언은 로드됐으니 문제는 다른 데 있다"로 읽힌다.
아니다. TypeScript는 `/` 접두 지정자를 모듈 이름이 아니라 **루트 경로**로 해석하므로
어떤 앰비언트 선언도 맞을 수 없다. 이 확인 출력이 비용을 키운다 — `include` 패턴을 뒤지게 만든다. (T5)

**대응** — `tsconfig` 추적을 멈춘다. 번들러도 체커도 파싱할 수 없는 형태를 쓴다.

```js
new Function('return import("/path.js")')()
```

가정하지 말고 실제 브라우저에서 확인한다 — 여기서는 콘솔 에러 0에 결과 45건으로 실측됐다.
`unsafe-eval` 이 필요한데 GitHub Pages는 제한하지 않는다 (더 엄격한 호스트라면 문제가 된다).

<a id="t24"></a>
### 24. 모르는 `--reporter` 는 테스트 하나 돌기 전에 vitest를 죽인다

`--reporter=basic` 은 vitest 4에 존재하지 않는다. **커스텀 리포터 모듈**로 취급되어
해석에 실패하고, 아무것도 실행하지 않은 채 1로 종료한다.

그 출력에서 `Tests  N failed` 를 파싱하던 뮤테이션 테스트 하네스가
**뮤턴트 14개 중 14개를 "생존"으로 채점**했다. 완벽하고 그럴듯한 거짓 보고였고,
앞선 리뷰어의 발견과 일치해서 더 설득력 있어 보였다. (T8 리뷰 수정 검증 중 발생)

**대응** — 카운트가 없다고 "실패 없음"을 추론하지 마라.
파싱하기 전에 실행이 **시작됐는지**(출력에 `Test Files`) 확인한다.
반드시 잡히는 것을 아는 **대조 뮤턴트**를 항상 포함시킨다 —
대조가 죽지 않으면 코드가 아니라 하네스가 고장난 것이다.
여기서 동작이 확인된 것은 `--reporter=verbose` 다.

<a id="t25"></a>
### 25. 테스트의 `console.log` 가 파이프 아래에서 사라진다

vitest 기본 리포터는 stdout이 TTY가 아니면 테스트 stdout을 억제한다.
TTY 실행에서는 찍히는 줄이 `npm test | grep 노드` 에서 0이 된다.
그 0은 "찍히지 않았다"가 아니라 **"읽을 수 없었다"** — [22번](#t22)과 같은 부류다.

**대응** — 파일로 리다이렉트하고 `--reporter=verbose` 로 돌린 뒤 그 파일을 grep한다.
`×` 같은 비ASCII 표식은 `grep -a` / `LC_ALL=C` 주의가 필요하다.

<a id="t26"></a>
### 26. 코드 펜스 제거는 링크 쪽에는 맞고 헤딩 쪽에는 틀리다

`lib/atlas/links.ts` 의 `proseOnly` 는 펜스·인라인 코드·HTML 주석을 지운다.
산문 속 **예시** 링크가 진짜 엣지가 되지 않게 하고, 슬래시·앵커 검사기가
그것을 고칠 수 없는 위반으로 잡지 않게 하기 위해서다.

이것을 **대상** 쪽에도 적용했더니 앵커 2개가 깨졌다.
`headingIds` 는 인라인 코드를 **포함한** 헤딩 텍스트에서 id를 만들기 때문에,
`` `master`/`slave` `` 를 비우면 id가 바뀌고 `#master--slave-…` 가 해석되지 않는다.

**대응** — 링크를 **뽑아내는** 곳에서만 전처리하고, 헤딩을 **색인하는** 곳에서는 하지 않는다.
이 변환은 대칭이 아니다 — **"일관되게 어디에나 적용"이 여기서는 결함 그 자체다.**

<a id="t27"></a>
### 27. `locator('[role="dialog"]')` 는 숨은 다이얼로그를 센다

**실측 (같은 페이지)**

| 로케이터 | 개수 |
| --- | --- |
| `locator('[role="dialog"]')` | **2** — 이 리포의 모바일 드로어가 `hidden` 뒤에서 DOM에 남는다 |
| `getByRole('dialog')` | **1** |

"정확히 하나의 다이얼로그가 열려 있다"는 테스트가 **어느 로케이터를 쓰느냐만으로** 통과와 실패가 갈린다.
게다가 속성 셀렉터 쪽이 더 문자 그대로여서 손이 먼저 간다.

**대응** — 다이얼로그 개수는 `getByRole` 로 단언한다.
마크업이 아니라 **로케이터**가 무엇을 측정했는지를 결정한다. [31번](#t31)과 같은 부류다.

<a id="t28"></a>
### 28. 생 NUL 바이트가 ripgrep으로 하여금 파일 전체를 건너뛰게 만든다

`tests/atlas/build.test.ts` 가 오프셋 11273/11283에 진짜 NUL 2개를 갖고 있었다 —
이스케이프로 의도한 템플릿 리터럴 구분자가 제어문자 자체로 들어갔다.

리포 전체 `Grep` 으로 `정준|결정론` 을 찾으니 23개 파일이 나왔고 **이 파일만 아무 표시 없이 빠졌다.**
UTF-8로 읽으니 정준 ×2, 결정론 ×1, `sort` ×3이 들어 있었다.
「이 파일에 그것이 없다」와 「이 파일은 스캔된 적이 없다」가 **같은 침묵**이라,
"정준 순서 테스트가 없다"는 거짓 발견이 나올 뻔했다.

**기전 (재현으로 확인)** — 도구 인자가 JSON으로 도착한다. 그래서 `\uXXXX` 는
파일에 닿기 전에 그 코드포인트로 파싱되지만, 8진 `\0` 형태는 유효한 JSON 이스케이프가 아니라서
텍스트로 살아남는다. **같은 파일의 한쪽만 오염된 이유가 이것이다.**
한 세션에 **세 번** 당했고, 세 번째는 Bash 도구가 제어문자가 든 명령을 거부해서 겨우 드러났다.

**대응** — 그런 시퀀스를 문자 그대로 두려면 백슬래시를 겹치거나
`String.fromCharCode(92) + "u0000"` 으로 만든다. 이스케이프가 든 파일을 쓴 뒤에는 NUL을 센다.

```
node -e "const d=require('fs').readFileSync(F); console.log(d.filter(b=>b===0).length)"
```

파일이 실제로 읽혔음을 증명하지 않은 채, 리포 전체 grep의 결과로 "없다"를 결론짓지 마라.

<a id="t29"></a>
### 29. 부정 단언은 검사기가 동작해도 픽스처가 후보가 아니어도 초록이다

`expect(x).not.toContain(y)` 는 「면제가 동작한다」와 「이 픽스처는 후보였던 적이 없다」를
구분하지 못한다.

**실측** — 픽스처가 `graph.nodes[0]` (오늘의 정렬 순서 덕분에 우연히 artifact인 것)이던 자체 검사가,
첫 노드를 다른 타입으로 바꾸는 뮤테이션 아래에서도 **초록**을 유지했다.
반면 양성 형제 단언은 빨강이 됐다.
스위트는 한 번도 실행한 적 없는 면제 경로를 완전히 커버했다고 보고한다.

**대응** — 모든 부정 단언에 **차등 대조군**을 짝지운다.
면제 조건을 적용하기 **전에** 그것이 잡히는지 단언하고, 적용한 뒤에 잡히지 않는지 단언한다.
픽스처는 인덱스가 아니라 술어로 고른다: `filter(n => n.type === "artifact")[0]`.

<a id="t30"></a>
### 30. 살아남은 뮤턴트는 뮤테이션이 아무것도 안 했다는 뜻일 수 있다

"생존"은 서로 다른 두 상태를 덮는다.

| 상태 | 뜻 | 읽히는 방식 |
| --- | --- | --- |
| 테스트가 닿지 못하는 분기 | 테스트가 약하다 | 맞다 |
| 어떤 출력도 바꿀 수 없는 변경 | 상수가 무력하다 | **틀리게 읽힌다** |

여기서 두 번 측정됐다.

- `CLUSTER_R_MAX 14 -> 30` — 계산된 클러스터 반경(12.90)이 상한에 닿은 적이 없어서 생존했다.
  **그 상수는 무력했다.**
- `lib/atlas/layout.ts` 의 `clamp` — 오늘 순수 항등(`v => v`)으로 생존한다.
  `npx vitest run tests/atlas` 는 여전히 통과를 보고하는데,
  `clusterCap` 의 viewBox 항이 이미 모든 좌표를 범위 안에 붙들고 있기 때문이다.

첫 경우는 "이 테스트가 약하다"로 읽혀서 **멀쩡한 테스트를 다시 쓰게 만든다.**

**대응** — "생존"을 믿기 전에 그 뮤턴트가 **출력을 바꾸는지** 증명한다 (양쪽 버전의 값을 찍어 본다).
한계처럼 읽히는 상수가 아니라 실제로 **구속하는** 상수를 바꾼다.
정말 무력한 분기를 남겨 두는 것은 괜찮지만 주석으로 말한다 —
"여기서 항등은 빨강이 되지 않으며, 그 이유는".

<a id="t31"></a>
### 31. `getByRole` 개수는 Playwright 자신의 계산이지 접근성 트리가 아니다

Playwright는 `node_modules/playwright-core/lib/coreBundle.js` 안에
태그→role 표(`inputTypeToRole`)를 갖고 있다 — `injectedScript` 가 들어 있는 바로 그 번들(참조 50회).
그래서 role은 주입된 JS에서 DOM으로부터 유도된다.

따라서 `getByRole` 개수와 실제 브라우저 측정(탭 순서, 포커스 이동)이
**한 번의 도구 호출에서 똑같이 권위 있어 보이는데**, 엔진 자신의 답인 것은 두 번째뿐이다.

실제 사례로, 여기 코드 주석이 `getByRole("button")` 5 대 2 결과에 "Chromium 실측"이라고 적었고
리뷰어가 둘을 갈라내야 했다.

**대응** — 각 수치 뒤의 엔진을 밝힌다.
`getByRole` 은 스크린 리더가 노출하는 것의 강력한 근사이지만 **플랫폼 a11y 트리는 아니다** —
그 위에 실측이라고 쓰지 마라. [27번](#t27)과 같은 부류다.

---

<a id="t32"></a>
### 32. 이 리포의 vite 는 oxc 라 `esbuild.*` 설정 키가 조용히 무시된다

**증상.** vitest 테스트에서 페이지를 import 하면 파싱이 죽는다.

```
import { getStaticProps } from "@/pages/atlas/[...id]"
→ Failed to parse source for import analysis … contains invalid JS syntax
```

**함정.** 원인은 대괄호 경로가 아니라 `tsconfig.json` 의 `jsx: "preserve"` 다 — 변환기가 JSX 를 그대로 두고
vite 의 import-analysis 가 그걸 JS 로 읽으려다 죽는다. 그런데 상식적인 처방인 `esbuild: { jsx: "automatic" }` 을
넣어도 **오류 문구가 한 글자도 바뀌지 않는다.** 이 리포의 vite 는 8.2.1(rolldown/oxc)이라 esbuild 를 쓰지 않고,
모르는 최상위 키를 경고 없이 버리기 때문이다.

바뀌지 않은 오류는 「설정이 안 먹었다」가 아니라 **「원인이 딴 데 있다」로 읽힌다.** 그래서 `include` 패턴과
alias 와 대괄호 파일명을 차례로 의심하게 된다 — 정작 방금 쓴 처방이 맞았는데 키 이름만 틀렸다.

**대응.** `oxc: { jsx: { runtime: "automatic" } }`. `vitest.config.ts` 에 이유와 함께 들어가 있다(`d217827`).
일반화하면: **설정을 바꿨는데 오류가 한 글자도 안 바뀌면 먼저 그 키가 읽히는지를 의심한다.**
빌드 도구를 갈아끼운 리포(vite 8 = rolldown/oxc, `esbuild` 아님)에서 특히 그렇다.

*(다른 세션의 실측. 처방 코드는 커밋에 있고 이 항목은 그 경위 기록이다.)*

---

<a id="t33"></a>
### 33. Git Bash 의 `/tmp` 와 node 의 `/tmp` 는 다른 경로다

**재현.**

```bash
node -e "require('fs').writeFileSync('/tmp/probe.txt','x')"   # → C:\tmp\probe.txt 에 쓴다
grep -c x /tmp/probe.txt                                       # → No such file or directory
```

**함정.** Git Bash 는 `/tmp` 에 MSYS 경로 변환을 걸지만 node 는 그 변환을 받지 않아 드라이브 루트로 해석한다.
**같은 명령 블록 안에서 방금 쓴 파일을 못 읽는** 형태라 인코딩·권한·타이밍을 의심하게 된다 —
실패가 「쓰기가 안 됐다」로 보이지 「다른 데 썼다」로 보이지 않는다.

**대응.** 스크래치패드 절대경로를 쓴다. 셸과 node 가 같은 문자열을 같은 위치로 해석한다.
[9번](#t9)·[14번](#t14)과 같은 부류다 — **셸이 문자열을 손대는데 다른 프로세스는 안 손댄다.**

*(다른 세션의 실측. 스크래치패드로 바꾸자마자 해소됐다.)*

---

<a id="t34"></a>
### 34. 검사기의 빨강을 자기 스크립트로 진단하면 참인 관찰에서 틀린 인과가 나온다

셋 중 제일 값한다. 두 에이전트가 연달아 오진했고 **둘 다 관찰은 참이었다.**

**출발점.** `npm run build && npm run check-baseline` → exit 1 · 13건.

| | 진단 | 관찰 | 인과 |
| --- | --- | --- | --- |
| 오진 ① (리뷰어) | 「T11 의 `lg:grid-cols-[1fr_20rem]` 이 CSS 번들을 바꿨다」 | **참** — 번들에 그 클래스가 실제로 있다 | 틀림 |
| 오진 ② (검증 에이전트) | 「`buildId` 가 원인」 | **참** — 자기 비교 스크립트에서 남은 3곳이 전부 buildId 였다 | 틀림 |
| 실제 | CSS·JS **번들 파일명 해시** | | |

**함정.** `scripts/check-baseline.mjs` 의 `hashOf` 는 **buildId 를 이미 정규화하고 있었다.**
검증 에이전트의 비교 스크립트에는 그 정규화가 없었고, 자기 스크립트에서 본 차이를 검사기의 동작으로 일반화했다.
그대로 갔으면 **이미 있는 정규화를 한 번 더 넣고, 게이트는 여전히 빨간 채로 「고쳤다」고 보고**하게 된다.

번들 파일명이 진짜 원인인 이유는 공유 번들이기 때문이다 — 다른 페이지가 클래스를 하나 더 써도 파일명이 바뀌어
감시 대상이 전부 빨개진다. 그래서 새 화면을 만들 때마다 이 게이트가 통째로 빨갛고, 「원래 빨간 것」으로 학습된다.

**교정 절차.**

1. 자기 스크립트로 진단하기 **전에** `hashOf` 를 읽어 검사기가 **이미 정규화하는 축**을 확인한다
2. 정규화를 추가했으면 `--update` 로 기준선을 다시 만든다
3. 🔴 **반드시 대조군을 잰다.** 정규화 + `--update` 조합은 무엇을 하든 초록이 나온다 — 자기 자신과 비교하기 때문이다.
   `out/index.html` 의 `<body>` 에 속성 하나를 넣고 `check-baseline` 이 **exit 1 · 1건**인지 확인한다.
   실측: 번들만 바뀜 → exit 0, 마크업 한 글자 → exit 1. **이 둘이 갈려야 「고쳤다」이고, 안 갈리면 「검사를 껐다」다.**

**대응.** 에이전트 보고를 액면으로 받지 말고 검사기 소스를 직접 읽는다.
[24번](#t24)·[30번](#t30)과 같은 부류이되 한 겹 더 있다 — 저기서는 계측기가 안 돌았고, **여기서는 계측기가 돌았는데
다른 계측기였다.** 참인 관찰이 붙어 오므로 반증이 훨씬 어렵다.

*(다른 세션의 실측. 그 세션이 `hashOf` 소스를 직접 읽어 잡았다.)*

---

<a id="t35"></a>
### 35. Git Bash 가 명령 인자 안의 `/atlas/` 를 윈도 경로로 바꾼다

**증상.** 뮤테이션 테스트에서 `url.includes("/atlas/")` 를 다른 문자열로 바꿔 넣었더니
**테스트가 빨개졌다.** 「뮤턴트 사멸 — 검사가 살아 있다」로 읽었다. 그런데 실제로 적용된 것은
그 문자열이 아니었다.

**재현 (2026-08-27 실측).**

```
$ node -e 'console.log(process.argv[1])' '/atlas/'
C:/Program Files/Git/atlas/          ← 홑따옴표로 감싸도 바뀐다
$ node -e 'console.log(process.argv[1])' '//atlas/'
//atlas/                             ← 앞에 / 를 하나 더 붙이면 안 바뀐다
$ MSYS_NO_PATHCONV=1 node -e 'console.log(process.argv[1])' '/atlas/'
/atlas/                              ← 환경변수로 꺼도 된다
```

**함정.** MSYS2 의 경로 변환은 **셸 인용을 지나서** 동작한다. `/` 로 시작하는 인자를
유닉스 경로로 보고 `C:/Program Files/Git/…` 로 바꾼다. 이 리포의 라우트는 전부
`/atlas/`·`/blog/`·`/en/` 꼴이라 **인자로 넘기는 순간 전부 표적이다.**

가장 나쁜 결과는 침묵이 아니라 **거짓 빨강**이다. 뮤테이션 테스트에서
「죽었다」와 **「엉뚱한 걸 죽였다」가 같은 빨강**이라, 뮤턴트가 실제로는 주입되지 않았는데
계수기가 살아 있다는 증거로 기록된다. [30번](#t30)의 거울상이다 — 저기서는 생존이 거짓이었고
여기서는 사멸이 거짓이다.

**대응.**

1. **치환 문자열을 인자로 넘기지 말고 스크립트 파일에 넣는다**(`Write` 로 쓰고 `node file.mjs`)
2. 넘겨야 한다면 `MSYS_NO_PATHCONV=1` 또는 `//atlas/`
3. 🔴 **치환 결과 줄을 `grep` 으로 눈으로 본다.** 뮤턴트를 주입한 뒤 그 줄이 의도한 모습인지
   확인하기 전에는 빨강도 초록도 증거가 아니다

*(2026-08-27 실측. 위 세 줄은 이 문서를 쓰면서 다시 실행한 것이다.)*

---

<a id="t36"></a>
### 36. `\b` 를 문자열로 옮기려다 0x08 바이트가 파일에 박힌다

**증상.** 단어 경계 정규식 `/\bword\b/` 를 셸 경유로 파일에 쓴 뒤 실행하면 매칭이 안 된다.
`cat` 으로 보면 멀쩡해 보인다 — 0x08(백스페이스)은 터미널에서 **커서를 뒤로 움직일 뿐 보이지 않는다.**

**재현 (2026-08-27 실측). 범인은 히어독이 아니다.**

| 경로 | `\b` 가 도착하는 모습 |
| --- | --- |
| `cat > f <<'EOF'` (인용 히어독) | `\` `b` — **온전하다** |
| `cat > f <<EOF` (비인용 히어독) | `\` `b` — **온전하다** |
| `echo -e "a\bb"` | **0x08** |
| `printf 'x\by\n'` | **0x08** |

비인용 히어독이 먹는 것은 `\b` 가 아니라 **백슬래시 겹**이다 — 같은 실측에서
`"\\\\d"` 로 쓴 것이 파일에는 `"\d"` 로 도착했다(네 겹 → 한 겹). [8번](#t8)이 그것이다.

**함정.** 「히어독이 백슬래시를 먹는다」는 기억이 **`\b` 까지 히어독 탓으로 돌리게 만든다.**
그러면 히어독을 피하고 `printf` 로 우회하는데, 바로 그 `printf` 가 0x08 을 만든다.
**틀린 원인을 고치면 증상이 나빠진다.**

**대응.** `\` 가 들어가는 내용은 셸을 통과시키지 말고 **`Write`/`Edit` 로 파일에 직접 쓴다.**
셸을 써야 하면 인용 히어독(`<<'EOF'`)을 쓰고 `echo -e`·`printf` 의 포맷 문자열에는 넣지 않는다.
검증은 눈이 아니라 `od -c`(또는 `grep -P '\x08'`)다 — **`cat` 은 0x08 을 보여 주지 않는다.**

*(2026-08-27 실측. 위 표 네 줄을 직접 실행해 만든 것이다.)*

---

<a id="t37"></a>
### 37. 뮤턴트 원복이 파일을 LF 에서 CRLF 로 바꿔 놓는다

**증상.** 뮤테이션 테스트를 마치고 `git checkout -- <file>` 로 원복했는데,
그 뒤의 `Edit` 이 **앵커를 못 찾거나 엉뚱한 곳에 붙는다.** `git diff` 는 깨끗하다.

**재현 (2026-08-27 실측, 빈 리포).**

```
$ git config core.autocrlf     → true      (.gitattributes 없음)
$ printf 'a\nb\nc\n' > f.txt && git add f.txt && git commit -m x
  커밋 전 CR=0
$ printf 'a\nb\nZ\n' > f.txt   # 뮤턴트 주입 (LF)
  뮤턴트 CR=0
$ git checkout -- f.txt        # 원복
  원복 후 CR=3                 ← LF 파일이 CRLF 로 돌아왔다
```

**함정.** `core.autocrlf=true` + `.gitattributes` 없음이 이 리포의 설정이다.
인덱스는 LF 로 저장하고 **체크아웃할 때 CRLF 로 편다.** 그래서 원복은 「원래대로」가 아니라
**「인덱스대로」다** — 작업 트리에 LF 로 있던 파일은 CRLF 가 되어 돌아온다.

그리고 git 은 이 차이를 diff 로 보여 주지 않는다(같은 이유로 정규화하므로).
[9번](#t9)·[10번](#t10)·[14번](#t14)·[16번](#t16)과 같은 뿌리인데, **여기서는 내가 아무것도
안 썼는데 바뀐다** — 원복은 「되돌리는」 동작이라 의심의 대상이 되지 않는다.
2026-08-27 에 이렇게 3개 파일이 CRLF 가 됐고, 이후 `\n` 앵커 편집이 조용히 어긋났다.

**대응.**

1. 편집·주입 **전에** `tr -cd '\r' < f | wc -c` 로 CR 을 세고, **원복 후 다시 센다.** 두 값이 같아야 원복이다
2. 더 안전한 것은 git 을 거치지 않는 것이다 — `cp f f.bak` 으로 **바이트 백업**한 뒤 `cp f.bak f` 로 복원
3. `git checkout --` 로 원복했다면 그 다음 `Edit` 전에 CR 을 한 번 더 센다

*(2026-08-27 실측. 위 재현은 스크래치패드의 빈 리포에서 그대로 재현한 것이다.)*

---

<a id="t38"></a>
### 38. `grep -c` 로 빌드 산출물의 요소를 세면 답이 항상 0 아니면 1이다

**증상.** `out/index.html` 의 `<h2>` 를 세려고 `grep -c '<h2' out/index.html` 을 돌리면 **1** 이 나온다.
실제 `<h2>` 는 4개다. 오류도 경고도 없다.

**재현 (2026-08-28 실측).**

```
$ grep -c '<h2' out/index.html          → 1     ← 틀림
$ grep -o '<h2' out/index.html | wc -l  → 4     ← 맞음
$ awk 'END{print NR}' out/index.html    → 1     ← 파일 전체가 한 줄이다
```

**함정.** `grep -c` 는 **매치한 줄 수**를 세지 세는 대상의 개수를 세지 않는다.
Next 의 정적 export 산출물은 **개행이 없는 단일 라인**이므로, 그 안에 요소가 몇 개 있든
답은 언제나 0(없음) 또는 1(있음)이다.

**이것이 위험한 진짜 이유는 1 이 그럴듯한 기대값이라는 데 있다.**
`id="main"` 1개 · 스킵 링크 1개 · `<h1>` 1개는 정답이 전부 1이라
**틀린 도구로 재도 맞는 답이 나온다.** 그래서 검사가 여러 번 통과한 뒤,
정답이 2 이상인 항목에서 처음 어긋난다 — 그때는 이미 그 도구를 신뢰하고 있다.

[16번](#t16)·[35번](#t35)과 뿌리가 다르다. 저것들은 「없다」와 「읽을 수 없었다」가
같은 출력으로 나오는 문제지만, **이것은 도구가 정직하게 답하는데 묻는 단위가 틀린 것**이다.
중복 컨트롤(같은 접근명 버튼이 둘)을 찾는 검사가 정확히 이 단위로 실패한다 —
1 이 나와서 「하나뿐」이라고 읽지만 실제로는 「그 줄에 있다」는 뜻일 뿐이다.

**대응.**

1. 산출물의 **개수**를 셀 때는 `grep -c` 를 쓰지 않는다. `grep -o <패턴> <파일> | wc -l`
2. 세기 전에 `awk 'END{print NR}' <파일>` 로 **줄 수를 먼저 본다.** 1 이면 `-c` 는 무의미하다
3. 정답이 2 이상인 마커를 **대조군으로 하나 끼워 넣는다.** 전부 정답이 1인 마커만 재면
   도구가 틀렸다는 것이 드러나지 않는다
4. `</head>` 이후만 재는 것과는 **다른 문제다.** 그쪽은 `__NEXT_DATA__` 중복을 배제하는 것이고
   이쪽은 단위 자체가 틀린 것이라, 둘 다 해야 한다

*(2026-08-28 실측. T10 검증에서 검증자가 발견해 재계수했다. 경위는
`reports/2026-08-28-t10-main-sections.md` 의 Ruling R40.)*

---

### 39. `$TEMP` 를 POSIX 도구의 경로로 넘기면 「디렉터리가 없다」는 거짓말이 돌아온다

**증상.** 이전 커밋 상태를 재려고 `git archive <sha> content/blog | tar -x -C "$TEMP/before"` 를
돌리면 이렇게 죽는다.

```
tar: C\:\\Users\aeby\\AppData\\Local\\Temp/before: Cannot open: No such file or directory
tar: Error is not recoverable: exiting now
```

**함정.** 같은 명령의 바로 앞 `mkdir -p "$TEMP/before"` 는 **성공했다.** 디렉터리는 존재한다.
`$TEMP` 가 Windows 형식(`C:\Users\…`)이라 tar 가 백슬래시를 이스케이프 문자로 읽고,
경로가 뭉개진 채 열리지 않는 것이다. 메시지는 「없다」고 말하지만 실제 원인은 「경로가 틀렸다」다.

**이것이 위험한 이유는 진단이 엉뚱한 데로 간다는 데 있다.** 메시지를 곧이곧대로 읽으면
`mkdir` 이 실패했다고 보고 권한이나 디스크를 확인하게 되는데, 그쪽에는 아무 문제가 없다.

**대응.**

1. POSIX 도구(tar·rsync 등)에 넘길 임시 경로는 **`/tmp/<이름>` 을 직접 쓴다.** `$TEMP` 를 쓰지 않는다
2. `Cannot open: No such file or directory` 를 보면 **경로를 `echo` 로 먼저 찍어라.**
   백슬래시가 보이면 원인은 존재 여부가 아니다
3. [33번](#t33)과 뿌리가 같다 — 두 경로 세계가 한 명령 안에 섞이는 자리다

*(2026-08-29 실측. 알림센터 배치의 카테고리 링크 회고에서 당했다.)*

### 40. `python - <<'PY'` 의 한글 stdout 이 깨져도 파일 쓰기는 정상이다

**증상.** 문자열 치환 스크립트가 마지막에 `print('4건 교체')` 를 찍는데 콘솔에는
`4�� ��ü` 가 나온다.

**함정.** 깨진 것은 **stdout 인코딩뿐이고 파일은 정상으로 쓰였다.** 스크립트 안에서
`io.open(p, 'w', encoding='utf-8')` 로 쓴 내용은 온전하다. 그런데 출력이 깨져 있으면
「스크립트가 실패했다」로 읽고 되돌리거나 다시 돌리기 쉽다 —
**같은 치환을 두 번 돌리면 `assert a in s` 가 이번엔 걸려서 진짜로 실패한다.**

**대응.**

1. 출력이 깨져도 **실패로 읽지 마라.** 종료 코드를 보고, 결과는 파일을 다시 재서 판정한다
2. 확인은 `grep -c '<바뀐 문자열>' <파일>` 로 한다. 콘솔 메시지는 근거가 아니다
3. 굳이 찍어야 하면 **ASCII 로 찍어라** (`print('replaced 4')`)

*(2026-08-29 실측. 편5 원본 겹침 4건을 고치면서 당했다.)*

---

### 41. `io.open(p, "w")` 가 파일을 CRLF 로 바꾸고, 원본 대조가 거짓 0 을 낸다

**증상.** 축자 겹침 6곳을 파이썬 치환으로 고친 뒤 `source-overlap` 을 돌렸더니 세 원본
모두에서 **20자 0건**이 나왔다. 임계값을 **16자로 낮춰도 0건**이었다.

```
고치기 전   ① 11건 · ② 5건
CRLF 상태   ① 0건  · ② 0건      ← 거짓
CR 제거 후  ① 6건  · ② 3건      ← 참
```

**함정.** 파이썬은 텍스트 모드 쓰기에서 `\n` 을 플랫폼 개행(윈도에서는 `\r\n`)으로 **번역
한다.** `encoding="utf-8"` 만 주고 `newline` 을 비워 두면 파일 전체가 CRLF 가 되며, 실측으로
CR 이 **238개** 박혔다. `source-overlap` 은 빈 줄로 문단을 가르는데 `\r\n\r\n` 이 그 분할을
깨뜨려 **대조할 문단 자체를 잃는다.** 그런데 출력은 「0건」이라 **성공과 구별되지 않는다.**

37번(뮤턴트 원복이 LF 를 CRLF 로 바꾼다)과 원인이 다르다 — 그쪽은 git 체크아웃이고
이쪽은 파이썬 쓰기다. **증상이 같고 경로가 둘이라는 것이 요점이다.**

**왜 안 잡히나.** 여섯 곳을 고쳤으니 건수가 주는 것은 당연했고, 0 이 「다 고쳐졌다」로
읽혔다. 이상한 것은 **임계값을 낮춰도 0** 이라는 점 하나뿐이었다.

**대응.**

1. 발행본을 스크립트로 고쳤으면 검사기보다 **CR 계수를 먼저** 돌린다 —
   `tr -cd '\r' < <파일> | wc -c`. `grep` 은 CR 을 탐지하지 못한다(9번)
2. **대조군을 같이 센다.** 같은 배치의 앞 편이 CR 0 이면 이 파일만 오염된 것이다
3. 파이썬을 쓸 거면 **읽기·쓰기 양쪽에 `newline=""`** 를 준다
4. 되도록 `Edit` 도구로 고친다 — 개행을 건드리지 않는다
5. 🔴 **줄어든 0 을 통과로 세지 마라.** 「고쳤으니 줄었다」와 「검사기가 대상을 잃었다」는
   출력이 같다. 임계값을 낮췄는데도 0 이면 검사기 쪽을 의심한다

*(2026-09-02 실측. B 편1 원본 겹침을 고치면서 당했다. 설계서 §10-10-c 에 같은 기록이 있다.)*

---

### 42. 물결표 두 개가 강조를 가로질러 삼킨다 — 「줄 끝」은 조건이 아니었다

**증상.** `check-markup` 이 한 줄에서 위반 **2회**를 보고했다. 여는 별표와 닫는 별표를
각각 잡은 것인데, 그 줄만 떼어 읽으면 문법이 멀쩡하다.

```
둘이 정본과 다르다. 9,263 은 배정 범위(§0~§3, 11~172행)가 아니라 **1~172행이고**, 818 은
```

여는 `**` 는 앞이 공백이고 뒤가 숫자라 left-flanking 이며, 닫는 `**` 는 앞이 한글이고
뒤가 쉼표라 right-flanking 이다. **두 조건을 모두 만족하는데도 렌더되지 않는다.**

**함정.** 범인은 별표가 아니라 **물결표**다. GFM 취소선 확장이 홑겹 물결표도 짝으로 읽어서,
둘째와 셋째 물결표가 이룬 구간이 **여는 별표를 통째로 삼킨다.** 대조군으로 갈랐다.

| 무엇을 바꿨나 | 위반 |
| --- | ---: |
| 원문 (물결표 3개) | **2회** |
| 괄호 안 물결표만 남김 | 0회 |
| 강조 안 물결표만 남김 | 0회 |
| 물결표 전부 제거 | 0회 |

**하나만으로는 걸리지 않는다.** 괄호 안과 강조 안에 **둘 다** 있어야 짝이 강조를 가로지른다.
그래서 물결표를 쓰는 줄이 늘수록 확률이 오르는 것이 아니라, **강조와 같은 줄에 놓일 때만**
터진다. 한국어 문서는 범위를 물결표로 쓰므로 이 조합이 저절로 만들어진다.

**듣지 않는 교정.** 직관적인 두 가지가 모두 실패했다.

| 시도 | 결과 |
| --- | ---: |
| 물결표를 역슬래시로 이스케이프 | **2회 그대로** |
| 강조 범위를 줄여 조사를 밖으로 (`**1~172행**이고`) | **2회 그대로** |
| 강조 안을 인라인 코드로 감쌈 | 0회 |
| 강조 안의 물결표를 「부터」로 바꿈 | 0회 |

**왜 안 잡혔나.** 이 기전 자체는 직전 세션이 이미 알고 있었다. 문제는 **조건을 좁게
적어 둔 것**이다. 인수인계에는 「물결 둘 이상 **+ 닫는 별표가 줄 끝**」으로 기록돼 있었는데,
이번 사례는 닫는 별표 뒤가 **쉼표**였다. 「줄 끝」은 당시 실측된 3쌍의 우연한 공통점이었을
뿐 필요 조건이 아니었고, 그래서 같은 기전을 **처음 보는 것으로 착각했다.**

🔴 **실측 몇 건에서 뽑은 공통점을 조건으로 적지 마라.** 사례가 셋이면 우연한 공통점이
여럿 나온다. 조건으로 올리려면 **그 조건을 뺀 반례를 찾아봤는지**를 함께 적어야 한다.

교정기 `fix-markup` 은 조사 문제만 고치도록 만들어져 있어서 이 자리를 「뒤따르는 것이
알려진 조사가 아니다」로 **남겼다.** 자동 교정이 비껴간 것이 아니라 애초에 대상이 아니었다.

**대응.**

1. 강조 안에 물결표를 두지 않는다. 범위는 **「1행부터 172행」처럼 말로** 쓰거나
   인라인 코드로 감싼다
2. 🔴 **이스케이프로 해결하려 하지 마라.** 실측으로 듣지 않았다. 통한다고 가정하고
   넘어가면 화면에 별표가 그대로 남는다
3. 교정기가 「손볼 자리」로 남긴 것은 **조사 문제가 아닐 수 있다.** 그 줄의 물결표를 센다
4. 문서를 고쳤으면 `check-markup -- --files <경로...>` 로 되돌려 확인한다 —
   판정은 정규식이 아니라 페이지를 그리는 파서가 한다

*(2026-09-03 실측. 발행본 184편은 0회지만 리포 안의 문서 125개 중 **28개 파일 250회**가
남아 있다. 인수인계의 「다음 안건」 참조.)*

---

### 43. `git ls-files` 가 한글 경로를 따옴표로 감싸, 검사기가 일부를 조용히 버린다

**증상.** 문서 전량을 검사기에 넘겼는데 넘긴 수와 스캔한 수가 어긋났다.

```
대상 54개
스캔 50개 파일 · 위반 0개 파일 · 0회
렌더되지 않는 강조가 없다.
```

「0회」와 「검사기가 작동한다」가 함께 찍히므로 **전량이 깨끗하다고 읽힌다.** 4개가 빠진
것은 두 숫자를 나란히 놓고 볼 때에만 보이고, 검사기는 종료 코드 0 을 냈다.

**함정.** `core.quotePath` 의 기본값이 `true` 라, `ls-files` 가 비-ASCII 경로를 큰따옴표로
감싸고 바이트를 8진 이스케이프로 바꾼다.

```
pages/product-lead-loadmap/00_허브.md
  ↓ git ls-files 의 출력
"pages/product-lead-loadmap/00_\355\227\210\353\270\214.md"
```

끝이 `"` 라 **`.md` 로 끝나지 않는다.** 검사기는 `.md` 가 아닌 인자를 `continue` 로
건너뛰고 있었으므로 4개가 대상에서 빠졌다. `grep -v '\.md$'` 로 걸러도 같은 이유로 빠진다.

| 무엇을 | 결과 |
| --- | ---: |
| `git ls-files '*.md'` | 54줄 · 그중 **4줄이 `.md` 로 끝나지 않음** |
| `git -c core.quotePath=false ls-files '*.md'` | 54줄 · 전부 `.md` 로 끝남 |
| 앞의 것을 넘긴 스캔 | **50개** |
| 뒤의 것을 넘긴 스캔 | 54개 |

**왜 위험한가.** 이 리포의 검사기들은 「대상이 0개면 종료 코드 2」라는 가드를 갖고 있다.
그 가드는 **하나도 못 받았을 때**만 발동한다. 「받은 것 중 일부를 버렸다」는 통과하며,
부른 쪽은 전량을 검사했다고 믿는다. 🔴 **일부 누락은 대상 없음보다 다루기 어렵다** —
전자는 숫자가 그럴듯하게 나오기 때문이다.

**대응.**

1. 경로를 뽑을 때 `git -c core.quotePath=false ls-files` 를 쓴다. 전역 설정에 기대지
   않는다 — 다른 사람의 기계와 CI 에서 값이 다르다
2. 🔴 **수집을 호출부에 맡기지 않는다.** 훅과 CI 가 각자 파이프라인을 쓰면 같은 함정을
   두 곳에서 되풀이한다. 검사기 안에 `--docs` 같은 수집 모드를 두어 끄는 자리를 하나로 만든다
3. 🔴 **검사기가 인자를 조용히 버리지 못하게 한다.** 받은 것 중 대상이 되지 못한 것이
   있으면 종료 코드로 알린다. `check-markup` 은 이제 그렇게 하고, 케이스 ⓶ 와 뮤턴트
   **M6·M7** 이 그것을 지킨다
4. 넘긴 수와 스캔한 수를 **대조하라.** 검사기가 알려 주지 않는 상황에서는 이것이
   유일한 신호다

**곁가지 하나.** 같은 수집 경로에서 두 번째 함정이 나왔다. `ls-files` 는 **인덱스**를 보므로
파일을 지우고 아직 스테이징하지 않으면 없는 경로를 내놓고, `readFileSync` 가 ENOENT
**스택 트레이스**로 죽는다. 훅에서 그것을 본 사람은 검사기가 고장 났다고 읽고 `--no-verify`
로 넘어간다 — 게이트를 세우면서 게이트를 무너뜨리는 길을 함께 놓는 셈이다. 수집 단계에서
걸러 내되 몇 개를 왜 뺐는지 **말한다.**

*(2026-09-04 실측. 고치기 전 뮤턴트 M6 을 넣으면 케이스 ⓶ 가, M7 은 ⓹ 가, M8 은 ⓸ 가
FAIL 한다 — 세 케이스가 헛돌지 않음을 `npm run mutate` 로 확인했다.)*

---

### 44. 링크를 정규식으로 세면 **없는 문제**가 만들어진다 — 이 문서의 첫 거짓 양성

**증상.** 링크 실태를 파악하려고 일회성 스크립트를 짰다. 두 번 연달아 **틀린 위반**을
보고했고, 두 번 다 「고치러 가야 할 자리」로 읽혔다.

| 보고 | 실제 |
| --- | --- |
| 발행본 2곳이 없는 `.md` 를 가리킨다 | ```markdown 코드 블록 **안의 예시**였다 |
| 발행본 29곳이 없는 편을 가리킨다 | 앵커가 붙어 있었을 뿐 편은 전부 있었다 |
| 문서 44곳이 깨졌다 | 파서로 세면 **42곳** (코드 블록 예시 2곳이 빠진다) |

최종 실측은 **발행본 1,612개 링크 전부 온전 · 문서 42곳 깨짐**이었다. 처음 보고한
「발행본이 404 를 낸다」는 통째로 사실이 아니었다.

**함정.** 이 문서의 다른 43건은 거의 전부 **거짓 음성**이다 — 있는 것을 못 찾는다.
이번 것은 반대 방향이며, 그래서 더 위험하다.

```
 거짓 음성  0건이 나온다 → 「깨끗하다」 → 아무 일도 일어나지 않는다
 거짓 양성  N건이 나온다 → 「고치자」   → 멀쩡한 문서를 손대러 간다
```

🔴 **거짓 양성은 스스로 검증되는 것처럼 보인다.** 위반 목록이 파일명과 줄 번호까지
갖추고 나오므로 「검사기가 일했다」는 인상을 준다. 0건은 의심받지만 42건은 의심받지 않는다.

**왜 정규식이 지는가.** 링크 판정은 세 가지를 동시에 알아야 한다.

| 알아야 하는 것 | 정규식이 못 하는 이유 |
| --- | --- |
| 이 자리가 코드 블록 안인가 | 펜스의 열림과 닫힘은 상태다. 한 줄만 보면 알 수 없다 |
| 인라인 코드 안인가 | 백틱의 짝을 세어야 한다 |
| 앵커를 뗀 경로가 무엇인가 | 뗄 수는 있지만, **떼기로 결정한 사람**이 있어야 한다 |
| 참조식 링크의 목적지 | `[가][ref]` 와 `[ref]: /경로` 가 **떨어져 있다** |

앞의 둘은 파서를 쓰면 저절로 해결된다. 뒤의 둘은 파서를 써도 **의식적으로 다뤄야 한다** —
`mdast` 에서 참조식 링크의 URL 은 `link` 가 아니라 `definition` 노드에 있어서, `link` 만
순회하면 그 형식이 통째로 빠진다.

**대응.**

1. 마크다운의 **의미**를 묻는 검사는 파서로 짠다. `check-markup` 이 이미 그렇게 하고
   있었는데도 같은 실수를 되풀이했다 — **일회성 스크립트라는 이유로 규칙을 낮추지 마라**
2. 🔴 **위반이 나왔을 때가 검사기를 의심할 때다.** 0건일 때만 의심하는 습관은 절반이다.
   실측 수를 손으로 몇 건 열어 확인한 뒤에 고치러 간다
3. 앵커·끝 슬래시·참조식처럼 **정규화가 필요한 축**을 케이스로 고정한다. `check-links` 의
   ③⑤⑲ 와 뮤턴트 **CL2·CL4·CL3** 이 각각 그 자리를 지킨다

*(2026-09-04 실측. 이 세 실수를 케이스로 바꾼 뒤 뮤턴트로 되돌려 실제로 FAIL 하는지
확인했다 — 「고쳤다」는 그 자리가 다시 무너지지 않는다는 뜻이 아니다.)*

---

### 45. 브라우저용 라이브러리를 Node 에서 부르면 **판정에 닿지 못한 것이 통과로 집계**된다

**증상.** Mermaid 도식 649개를 `mermaid.parse()` 로 검사했다. 화면에는 이렇게 찍혔다.

```
파일 238 · 블록 648 · 통과 27 · 문법오류 0 · 환경오류 621
```

「문법오류 0」이다. 그런데 **96%가 파서에 도달하지 못했다.**

| 무엇을 | 블록 | 판정 도달 | 통과 | 문법 오류 | **환경 오류** |
| --- | ---: | ---: | ---: | ---: | ---: |
| DOM 없이 | 648 | 27 | 27 | 0 | **621** |
| `jsdom` 을 세운 뒤 | 649 | **649** | 649 | 0 | 0 |

**기전.** `mermaid` 는 브라우저용이라 DOM 을 쓴다. Node 에는 `window` 가 없어 `DOMPurify`
가 초기화되지 못하고, 파싱에 닿기도 전에 `TypeError: DOMPurify.addHook is not a function`
으로 죽는다. 통과한 27개는 sanitize 경로를 타지 않는 `sequenceDiagram` 계열이었다.

🔴 **`catch` 한 곳이 두 종류의 실패를 뭉갠다.**

```
 parse() 실패
   │
   ├─ Error          파서가 낸 것    → 위반이다
   └─ TypeError      환경이 깨진 것  → 위반이 아니다. 보지 못한 것이다
      ReferenceError
```

둘을 구분하지 않으면 어느 쪽으로 뭉개도 거짓이 된다. 「전부 위반」으로 접으면 649곳이
거짓 양성이 되고, 「전부 통과」로 접으면 649개를 보지 않은 초록이 나온다.

**분류의 방향이 중요하다.** 🔴 **환경 쪽을 열거하고 나머지를 위반으로 둔다.** 반대로 두면
— 위반의 모양을 열거하고 나머지를 환경으로 두면 — 라이브러리가 새 오류 형태를 도입한 날
**진짜 위반이 「환경 문제」로 분류되어 조용히 사라진다.** 위반을 놓치는 쪽보다 검사기가
시끄럽게 죽는 쪽이 낫다.

**대응.**

1. `jsdom` 으로 전역을 세운다. **`import` 보다 먼저**여야 한다 — `DOMPurify` 가 로드
   시점에 `window` 를 잡으므로, 순서가 뒤바뀌면 621건이 그대로 재현된다. 그래서 검사기는
   `loadMermaid()` 안에서 동적 `import` 를 쓴다
2. **수집한 수와 판정에 도달한 수를 나란히 출력해 대조한다.** 어긋나면 종료 코드 2 다.
   위반 0 은 전량을 판정한 뒤에만 결론이 된다
3. 🔴 스캔 앞에 **카나리**를 둔다 — 반드시 그려져야 할 도식 하나를 먼저 파싱하고, 그것이
   실패하면 스캔을 시작하지 않는다. 분류는 2차 방어이고 카나리가 1차다

⚠️ **Node 24 는 `globalThis.navigator` 를 getter 로만 노출한다.** 단순 대입은
`TypeError: Cannot set property navigator of #<Object> which has only a getter` 로
죽는다. `Object.defineProperty(globalThis, name, { value, configurable: true })` 를 쓴다.

⚠️ **스크래치패드에 둔 프로브는 리포의 `node_modules` 를 해석하지 못한다.**
`ERR_MODULE_NOT_FOUND` 가 난다. 프로브는 리포 루트에 두고 실행 직후 지운다.

*(2026-09-04 실측. 뮤턴트 `CM1`(DOM 설치 제거)·`CM2`(환경 오류 목록 비우기)·`CM3`(전부
환경으로 접기)·`CM5`(카나리 무시)·`CM6`(미도달 무시)이 각각 이 자리를 지킨다.)*

---

### 46. `npm install` 은 `engines` 를 강제하지 않는다 — 로컬 Node 에서만 도는 판이 조용히 커밋된다

**증상.** `jsdom` 을 devDependency 로 넣고 검사기를 만들었다. 로컬에서 전부 초록이었다.

| 무엇을 | 로컬 (Node 24) | CI (Node 20) |
| --- | --- | --- |
| `npm install jsdom` | 설치됨 (경고 없음) | — |
| `check-mermaid:verify` | **21/21** | 🔴 `TypeError: webidl.util.markAsUncloneable is not a function` |
| 종료 코드 | 0 | **2** |

푸시한 뒤에야 드러났다. **로컬 검증 전체가 CI 와 다른 런타임에서 돌고 있었다.**

**기전.** 설치된 `jsdom@30.0.1` 의 요구는 이랬다.

```
"engines": { "node": "^22.22.2 || ^24.15.0 || >=26.0.0" }
```

Node 20 은 어느 대역에도 없다. 🔴 **그런데 `npm install` 은 이 값을 강제하지 않는다** —
`engine-strict` 를 켜지 않는 한 경고조차 나오지 않는다. 로컬이 Node 24 라 만족했고,
CI 의 `actions/setup-node` 는 `node-version: "20"` 을 쓴다.

오류 메시지가 원인을 가리키지 않는다는 점이 더 나쁘다. `webidl.util.markAsUncloneable is
not a function` 만 보고 「Node 버전 불일치」에 닿으려면 그 API 가 언제 들어왔는지를 알아야 한다.

**왜 다른 함정과 다른가.** 이 문서의 대부분은 **한 환경 안에서** 도구가 거짓을 말하는
경우다. 이번 것은 **두 환경이 다르다**는 사실 자체가 숨는다 — 로컬 검증은 정직했고,
검사기도 설계대로 종료 코드 2 로 죽었다. 어느 쪽도 거짓말하지 않았는데 결과가 갈렸다.

```
 로컬 Node 24  →  jsdom 30 설치 · 21/21 · exit 0   →  「통과했다」
 CI   Node 20  →  jsdom 30 로드 실패 · exit 2      →  배포 실패
                 ↑
                 이 차이는 어느 로그에도 나오지 않는다
```

**대응.**

1. 🔴 **devDependency 의 `engines` 를 CI 의 Node 와 대조하는 케이스를 자기 검사에 둔다.**
   `check-mermaid` 의 ㉖ 이 그것이다 — `node_modules/jsdom/package.json` 의 `engines.node`
   와 `.github/workflows/deploy.yml` 의 `node-version` 을 실제로 읽어 맞춘다.
   **워크플로 파일이 정본이다.** 「CI 가 Node 20 이더라」를 기억에 두면 다음에 또 갈린다
2. 새 의존성을 넣었으면 `node -p "require('./node_modules/<pkg>/package.json').engines"`
   를 한 번 찍어 본다. 설치 성공은 호환의 증거가 아니다
3. 검사기 전용 의존성은 **낮은 요구를 가진 버전**을 고른다. 최신일 이유가 없다 —
   `jsdom@26.1.0` 은 `>=18` 이라 Node 20 과 24 양쪽에서 돈다

⚠️ **로컬에서 CI 의 Node 를 재현할 수 없다는 것이 근본 문제다.** 위 1번은 그 차이를
「검사할 수 있는 값」으로 바꾼 우회이지 재현이 아니다. 판정이 메이저 단위라 `^20.19.0`
같은 patch 요구는 통과시킨다 — `actions/setup-node` 가 그 대역의 최신을 주므로 실질적으로
맞지만, 워크플로가 patch 를 고정하면 그때는 이 케이스를 다시 봐야 한다.

*(2026-09-04 실측. 뮤턴트 `CM11`(범위를 무조건 허용)·`CM12`(CI Node 를 못 읽음)가 이
자리를 지킨다. `node_modules/jsdom/package.json` 의 `engines` 를 jsdom 30 의 값으로
되돌려 ㉖ 이 실제로 FAIL 하는지 확인했다 — 25/26 · 종료 코드 1.)*

---

### 47. 테마 변수가 닿지 않는 색이 있다 — 팔레트 계산은 전부 통과하고 화면만 틀린다

**증상.** `components/mermaid.tsx` 의 `themeVariables` 에 사이트 팔레트를 넣고,
색 변수 42개와 대비 짝 43개를 데이터로 두어 WCAG 대비를 전수 계산했다. 위반 0건이었다.

그 상태로 브라우저를 열어 렌더된 SVG 의 색을 `getComputedStyle` 로 전수 수집했더니
**팔레트에 없는 색이 하나 나왔다.**

| 무엇 | 값 |
| --- | --- |
| 요소 | 시퀀스 도식의 `stickTopArrowHead`·`stickBottomArrowHead` 마커 안의 `path` |
| 계산된 획 | `rgb(0, 0, 0)` |
| 다크 캔버스(`#0f172a`) 대비 | **1.18** |
| 팔레트 계산의 판정 | 위반 0건 — **이 색은 애초에 팔레트에 없으므로 잴 대상이 아니었다** |

**기전.** mermaid 11 의 화살촉은 두 갈래로 색을 얻는다.

| 화살촉 | 색이 오는 곳 | 테마 변수가 닿나 |
| --- | --- | :---: |
| `arrowhead` · `crosshead` | SVG 안의 `<style>` 블록이 `signalColor` 를 넣는다 | ✅ |
| `stickTopArrowHead` · `stickBottomArrowHead` | 렌더러가 `.attr("stroke", "black")` 으로 **속성에 직접 건다** | ❌ |

`node_modules/mermaid/dist/chunks/mermaid.core/sequenceDiagram-*.mjs` 의
`insertStickTopArrowHead` 를 열면 그대로 보인다. 대응하는 CSS 규칙이 없어서 테마를
무엇으로 두든 검정이 남는다.

**왜 계산으로는 잡히지 않는가.** 대비 감사는 「내가 지정한 색들」을 서로 짝지어 잰다.
지정하지 않은 색은 **짝의 양변 어디에도 나타나지 않으므로** 위반이 될 수 없다.
이것은 이 리포가 대비 1.06 을 배포했던 기전과 같다 — 그때도 `dark:` 짝 검사가
초록이었고, 이유는 색 클래스가 **아예 없는** 자리라 짝지을 대상이 없었기 때문이다.

⇒ **「내가 지정한 값」이 아니라 「화면에 그려진 값」을 열거하라.** 팔레트에 없는 색이
나오면 그것이 재지 않은 자리다. 판정 대상의 목록을 내가 쓰는 한, 그 목록의 누락은
그 목록으로 검사할 수 없다.

**부수 관찰 — 잠복한 결함은 실측에서도 보이지 않는다.** 이 화살촉을 부르는 문법(`-)` 계열)은
발행본과 리포 문서를 통틀어 **0건**이라, 위 색은 `<marker>` 정의 안에만 있고 화면에
그려지지 않는다. 화면 픽셀만 봤다면 못 찾았을 것이다 — **정의된 색까지 세는 열거**라야
잡힌다. 대신 「지금 안 보인다」를 이유로 넘기면, 문법 하나를 쓰는 날 되살아나고
그때는 아무도 팔레트를 다시 보지 않는다.

**측정 자체가 두 번 거짓을 말했다.** 둘 다 브라우저 쪽이다.

| 무엇 | 읽힌 값 | 실제 | 왜 |
| --- | --- | --- | --- |
| 카드 배경 | `rgb(145, 149, 158)` | `rgb(15, 23, 42)` | 흰색과 slate-900 사이 **전환 애니메이션의 보간값**이다. 탭이 포그라운드가 아니면 전환이 `running` 인 채 시계가 멈춰 **끝나지 않는다** |
| 스크롤·`scrollIntoView` | 좌표가 바뀌지 않음 | — | 같은 이유로 백그라운드 탭에서 무시된다. 스크린샷을 찍어 깨우거나 마우스 스크롤을 써야 한다 |

⇒ 재기 전에 `document.getAnimations()` 를 `finish()` 로 끝내라. 중간값을 결함으로
읽으면 없는 문제를 쫓게 된다.

---

### 48. `git ls-files` 로 대상을 모으는 검사기는 **방금 만든 파일을 보지 않는다**

**언제 겪었나** — 2026-09-06. `docs/superpowers/plans/2026-09-06-pm-practice-split-design.md`
를 새로 쓴 직후 `npm run check-markup:docs` 를 돌렸다.

```
스캔 59개 파일 · 위반 0개 파일 · 0회
렌더되지 않는 강조가 없다.
종료 코드 0
```

**59는 설계서를 쓰기 전의 수다.** `scripts/check-markup.mjs` 의 `collectDocs()` 가
`git -c core.quotePath=false ls-files -- '*.md'` 로 대상을 모으므로, **untracked 파일은 애초에
목록에 없다.** 파일이 존재하고 내용이 있는데도 검사기는 그것을 「없는 것」으로 본다.

| 무엇이 같아 보이나 | 실제 |
| --- | --- |
| 「위반 0」 | 스캔했으나 위반이 없었다 |
| 「위반 0」 | **스캔 대상에 들어가지도 않았다** |

**43번과 같은 도구·같은 결과이지만 원인이 다르다.** 43번은 한글 경로가 따옴표에 감싸여
`.md` 로 끝나지 않게 되는 문제였고(경로 **표기**), 이번은 파일이 색인에 없는 문제다
(버전 관리 **상태**). 두 원인 모두 「넘긴 수와 스캔한 수를 대조하라」로 막히지 않는다 —
호출부가 수를 넘기지 않고 검사기가 스스로 모으기 때문이다.

⇒ **대응은 둘이다.**

1. **문서를 새로 만들었으면 `git add` 뒤에 검사하라.** 훅은 커밋 시점에 돌아 결국 잡지만,
   커밋 전에 확인하려는 목적이면 스테이징이 먼저다.
2. **스캔 수를 기억해 두고 대조하라.** 직전 실행이 59였는데 파일을 하나 더했는데도 59라면
   그 0은 결론이 아니다. `HANDOFF.md` 가 이 수를 적어 두는 이유가 이것이다.

⚠️ **`--docs` 를 쓰는 세 검사가 모두 같은 수집 함수를 쓴다** — `check-markup` · `check-links` ·
`check-mermaid`. 하나에서 겪으면 셋 다 같은 상태다.

---

### 49. 라이브러리가 폭을 재는 자리와 그리는 자리가 다르면 **검사기 전부가 초록인 채로 글자가 잘린다**

**언제 겪었나** — 2026-09-06. 발행본을 열어 보다가 「순이익」이 「순이읙」으로, 「꺼내는 장치」가
「꺼내는 장」으로 보이는 것을 발견했다. 도식 검사기(`check-mermaid`)는 **문법만** 보므로
667개 전량에 대해 「위반 0」을 내고 있었고, 링크·마크업·대비·개수 검사도 전부 초록이었다.

**무엇이 일어나나** — mermaid 는 라벨의 폭을 재려고 임시 컨테이너를 만드는데, 그 자리가
**도식이 실제로 그려지는 자리가 아니다.**

```js
// node_modules/mermaid/dist/mermaid.core.mjs
root = select("body");
appendDivSvgG(root, id39, enclosingDivID);   // ← divStyle 인자가 없다
```

`mermaid.render(id, chart)` 처럼 세 번째 인자(`svgContainingElement`)를 주지 않으면 이 분기를
타고, 측정 컨테이너는 **`document.body` 바로 아래**에 붙는다. 이 경로에서는
`initialize({ fontFamily })` 값이 **인라인 스타일로도 걸리지 않는다** — 인자를 넘기는 쪽은
`svgContainingElement` 가 있는 분기뿐이다.

이 저장소는 `next/font` 를 쓰므로 `--font-inter` 변수와 `font-sans` 클래스가
`_app.tsx` 가 그리는 `<div>` 에만 있다. 그래서 두 자리의 폰트가 갈린다.

| 자리 | 계산된 `font-family` | 무엇에 쓰이나 |
| --- | --- | --- |
| `body` 직속 임시 컨테이너 | `"Noto Sans KR"` (브라우저 기본 한국어 글꼴) | **폭을 잰다** |
| `.mermaid-figure` 안 | `__Inter_…, __Inter_Fallback_…, Inter, system-ui, sans-serif` | **그린다** |

한글은 어느 쪽에서도 대체 글꼴로 떨어지는데 그 대체가 서로 다르다. 재는 글꼴이 더 좁으면
상자가 그만큼 좁게 만들어지고, `foreignObject` 는 **SVG 사양상 자기 폭 밖을 클리핑한다** —
`overflow: visible` 을 줘도 소용없다. 그래서 마지막 글자가 잘린다. 조사가 붙어 낱말이
길어지는 한국어에서 특히 잘 드러난다.

**실측** — 한 편의 라벨 34개 가운데 **17개**가 넘쳤다.

| 라벨 | `foreignObject.width` | 내용의 `scrollWidth` | 넘침 |
| --- | ---: | ---: | ---: |
| 「고객 층 — 행동의 수」 | 118.39 | 128 | **9.61** |
| 「사업 층 — 돈의 수」 | 105.51 | 114 | 8.49 |
| 「평균 판매가」 | 68.32 | 74 | 5.68 |
| 「순이익」 | 38.65 | 42 | 3.35 |
| 「기회 포착 Discovery」 | 63.23 | 66 | 2.77 |

같은 상태가 `search-system-overview`(35개 중 **17**) · `pm-practice-map`(16개 중 5)에서도
나왔고, 저장소의 도식 549개를 담은 채 **이미 배포되어 있었다.**

**어떻게 확정했나** — 폰트를 바꿔 보는 것으로는 판정할 수 없었다. 처음에 `document.body.style`
로 폰트를 맞추고 재렌더를 시켰는데 값이 소수점까지 그대로였다(재렌더 자체는 일어났음을 SVG 의
다크 팔레트 `fill` 로 확인했다). 결정적 증거는 **같은 문자열을 두 컨텍스트에서 직접 잰 것**이다.

```js
measure("순이익", { fontSize: "14px" })                       // body 기본 폰트 → 38.65
measure("순이익", { fontFamily: <figure 의 계산된 스택>, ... }) // Inter 체인   → 42
```

38.65 는 `foreignObject.width` 와 **소수점까지 일치**했고 42 는 실제 렌더 폭과 일치했다.
이어서 `.mermaid-figure` 만 body 폰트로 강제하니 넘침이 **17 → 0**, 되돌리니 다시 **17** 이었다.

⇒ **대응은 셋이다.**

1. **폰트를 상속에 맡기지 마라.** 도식이 그려질 자리에서 `getComputedStyle(...).fontFamily` 를
   읽어 그 값을 설정으로 넘긴다. 계산된 값은 `var()` 가 이미 풀린 뒤이므로 body 아래의 측정
   컨테이너에서도 똑같이 해석된다. `var()` 를 담은 문자열을 그대로 넘기면 **변수가 정의되지
   않은 자리에서 선언째로 무효가 되어** 문제가 되살아난다.
2. **포털도 같은 문제를 갖는다.** 확대 뷰어(`Dialog`)는 `body` 아래에 그려지므로 본문의 폰트를
   상속받지 못한다. 위 대응은 폰트 스택을 SVG 자체에 박으므로 이쪽도 함께 닫힌다 —
   47번의 색 문제와 같은 자리이며, **포털은 색과 글꼴을 스스로 지정해야 한다.**
3. **판정은 브라우저만 할 수 있다.** jsdom 에는 레이아웃이 없어 `scrollWidth` 가 언제나 0이므로
   단위 테스트로는 잘림을 재현하지 못한다. 그래서 고정할 수 있는 것은 잘림 자체가 아니라
   **상속 키워드가 다시 들어오는 길**이다 — `resolveDiagramFontFamily` 를 순수 함수로 뽑아
   케이스로 묶었고 뮤턴트 둘(TH19 · TH20)이 그 케이스가 실제로 지키는지를 확인한다.

⚠️ **「검사기 전부가 초록」이 이 함정의 얼굴이다.** 문법도 링크도 색도 개수도 멀쩡한데
화면만 틀렸고, 세 세션 동안 아무 검사도 이것을 보지 못했다. 화면의 **비례**를 보지 않는 것도
같은 결이다 — 같은 날 편2의 도식이 214 × 874(비 0.25)로 세로로 늘어져 있던 것을 검사기가
통과시켰다. **레이아웃이 계산된 뒤에야 드러나는 결함은 실물을 열어야만 보인다.**

---

## 새 함정을 추가할 때

1. 이 문서에 재현·실측을 적는다. **실측과 추측을 구분해 표기한다.** 여기가 기본 행선지다
2. 위 「분류」 표에 번호를 넣고 **개수를 함께 고친다** — 고치지 않으면 그 수치가 다음 사람에게 거짓말한다
3. **「공통 원리」로 흡수되는지 본다.** 흡수되면 `CLAUDE.md` 는 건드리지 않는다.
   흡수되지 않는 개별 지뢰일 때만 `CLAUDE.md` 표에 한 행을 넣고, 그때 **뺄 한 행을 함께 고른다**
   (`CLAUDE.md` 상한은 Claude Code 공식 지침인 200줄이다)
4. **해소된 항목이 있는지 하나만 확인한다.** 이 표는 한 번도 줄지 않았다 — 위 「분류」 표의
   합계가 그 기록이다. 넣는 절차만 있고 빼는 절차가 없으면 로그가 된다

   2026-08-27(T12b)에 35~37 을 넣으며 확인한 결과 **해소된 항목은 0건**이다. 가장 유력한
   후보 셋을 직접 봤다 — [32번](#t32)은 `vite` 가 여전히 rolldown/oxc 라 유효, [11번](#t11)은
   `reviewer` 에이전트의 도구 목록에 `Write` 가 아직 없어 유효, [28번](#t28)은 그 파일의 NUL 이
   지워졌을 뿐 **ripgrep 이 NUL 파일을 건너뛰는 성질 자체는 그대로**라 유효다.
   **「그 사례가 사라진 것」과 「함정이 해소된 것」은 다르다** — 그 구분이 이 표를 로그와 가른다
