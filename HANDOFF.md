# HANDOFF

> 마지막 갱신 2026-09-04 · `pm` 배치 **시리즈 A 9/9 · 시리즈 B 8/8 완결** ·
> **마지막 사각지대를 닫았다.** 도식·마크업·링크가 훅과 CI 양쪽에 있고, 발행본과 리포 문서가 모두 0 이다

## 지금 상태

발행본 **184편**이다. 배치는 편8로 끝났고, 그 뒤 여섯 세션이 **검사기의 사각지대**를 닫아 왔다.
직전 세션이 「남은 사각지대」 첫 줄에 **Mermaid 문법 검사**를 적고 「마지막 사각지대」라고
남겼다. 이번 세션이 그것을 닫았다.

| 항목 | 값 |
| --- | --- |
| 발행 완료 | **A 편1~9 + B 편1~편8 (184편)** · 시리즈 B 완결 |
| 검사기 | ✅ **아홉 종** · 뮤턴트 **48/48 잡힘 · 생존 0** |
| 훅 | ✅ 발행본 **9단** · 문서 **6단** · 코드만이면 즉시 통과 |
| CI | ✅ **22스텝** — 도식 3단이 새로 들어갔다 |
| 강조 렌더 실패 | ✅ 발행본 **0회 · 184편** · 리포 문서 **0회 · 54개** |
| 깨진 링크 | ✅ 발행본 **0곳**(1,612개 검사) · 리포 문서 **0곳** |
| 깨지는 도식 | ✅ 발행본 **0곳**(544개) · 리포 문서 **0곳**(105개) — **판정 도달 649/649** |
| 커밋 | `626a4ad`(도식 검사기) |
| 푸시 · 배포 | 🔴 **미푸시.** `git status -sb` 에 `[ahead]` 가 남아 있다 — 아래 「지금 해야 할 일」 |
| 다음 단계 | 아래 **「남은 사각지대」** — 이제 **앵커 유효성**이 첫 후보다 |

### 이번 세션이 한 일

| # | 무엇 | 어디에 남았나 |
| ---: | --- | --- |
| 1 | **도식 검사기를 만들어 훅·CI 에 걸었다** — 649개 전량 판정 도달, 위반 0 | `626a4ad` |
| 2 | 🔴 **거짓 0 의 세 번째 형태를 겪었다** — 판정에 닿지도 못한 것을 통과로 셌다 | 아래 1번 |
| 3 | 🔴 **자기 검사 하나가 아무것도 지키지 않고 있었다** — 뮤테이션이 잡았다 | 아래 2번 |
| 4 | 종료 코드 판정을 `decideExit()` 순수 함수로 뽑았다 | 아래 3번 |
| 5 | 뮤턴트 `CM1`~`CM10` 추가 — **38개 → 48개** | `scripts/mutate.mjs` |
| 6 | `CLAUDE.md`·`README.md` 의 낡은 뮤턴트 수(16·38) 정정 | 아래 「문서가 낡던 자리」 |

---

## 🔴 지금 해야 할 일

**`main` 이 origin 보다 1 앞서 있다.** 커밋 `626a4ad` 와 이 문서·`CHANGELOG.md` 갱신 커밋이
푸시되지 않으면 **CI 가 새 검사 세 단계를 한 번도 돌리지 않는다.**

```
git status -sb        # [ahead N] 을 눈으로 읽어라
git push origin main  # 🔴 보호 브랜치다 — 사용자 승인을 받고 실행한다
```

⚠️ **「푸시 완료」라고 적힌 문장은 근거가 아니다.** 이전 인수인계에서 실제로 거짓이었다.
배포는 푸시에만 걸리므로 `[ahead]` 를 직접 읽어라.

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 🔴 **브라우저용 라이브러리를 Node 에서 부르면 「보지 않은 것」이 「통과」가 된다**

`mermaid` 는 브라우저용이라 DOM 을 쓴다. DOM 없이 `parse()` 를 부르면 파싱에 닿기도 전에
`DOMPurify.addHook is not a function` 으로 죽는다.

| 무엇을 | 블록 | 판정 도달 | 문법 오류 | **환경 오류** |
| --- | ---: | ---: | ---: | ---: |
| DOM 없이 | 648 | 27 | 0 | **621** |
| `jsdom` 을 세운 뒤 | 649 | **649** | 0 | 0 |

윗줄의 「문법 오류 0」은 결론이 아니다. **96%가 파서에 도달하지 못했다.**

⇒ 지금 검사기는 오류를 둘로 가른다. 🔴 **환경 쪽을 열거하고 나머지를 위반으로 둔다** —
반대로 두면 라이브러리가 새 오류 형태를 도입한 날 **진짜 위반이 「환경 문제」로 분류되어
조용히 사라진다.** 그리고 스캔 앞에 **카나리**(반드시 그려져야 할 도식 하나)를 둔다.

⚠️ 전역을 세우는 일은 **`import` 보다 먼저**여야 한다. `DOMPurify` 가 로드 시점에 `window`
를 잡기 때문이다. 그래서 `loadMermaid()` 안에서 동적 `import` 를 쓴다.

⚠️ Node 24 는 `globalThis.navigator` 를 **getter 로만** 노출한다. 단순 대입은
「has only a getter」로 죽으므로 `Object.defineProperty` 로 덮어쓴다.

### 2. 🔴 **필터를 통과한 집합으로 그 필터를 검사할 수 없다**

자기 검사 **21/21** 안에서 한 케이스가 아무것도 지키지 않고 있었다. 뮤테이션이 잡았다 —
`core.quotePath` 를 되돌리는 뮤턴트 **CM10 만 생존**했다.

| 케이스 | ㉑ 「수집한 경로가 전부 `.md` 로 끝난다」 |
| --- | --- |
| 무엇을 지키려 했나 | quotePath 가 한글 경로를 따옴표로 감싸 대상에서 빠지는 것 |
| **왜 헛돌았나** | 감싸인 경로는 **있지도 않은 이름**이 되어 `existsSync` 에서 걸러진다. 남은 것은 전부 `.md` 로 끝나므로 케이스가 통과한다 |
| 실측 | 정상 54개(비-ASCII 4개) 대 뮤턴트 **50개(비-ASCII 0개)** |
| 고친 방법 | **대조할 비-ASCII 경로가 실제로 있는지를 먼저 센다.** 없으면 지키는 것이 없으므로 FAIL |

`check-links` 의 ㉓ 은 이미 이 조건(`notAscii.length > 0`)을 갖고 있었다. **그것을 참고하고도
옮겨 오지 못했다** — 조건이 왜 있는지 읽지 않고 형태만 봤기 때문이다.

⇒ 자기 검사를 쓸 때 **「이 케이스가 실패하는 세계가 실제로 있는가」를** 물어라.
대조군이 0개면 그 케이스는 영원히 통과한다.

### 3. 🔴 **가드를 `main` 안에 흩어 두면 자기 검사가 그것을 볼 수 없다**

가드가 `if (…) { console.error(…); process.exit(2); }` 형태로 `main` 에 있으면, 통째로
지워도 케이스가 전부 통과한다. 종료 코드 판정을 `decideExit({canary, rejected, scanned,
unreachable, violations})` 순수 함수로 뽑으니 뮤턴트 `CM5`~`CM9` 가 각 가드를 하나씩 짚는다.

⇒ **검사기가 「무엇을 하면 죽는가」는 순수 함수로.** 출력은 `main` 에 남겨도 된다.

### 4. ✅ **뮤턴트 하나만 격리해서 되돌려 보는 법**

`npm run mutate` 는 3~5분 걸린다. 한 자리만 확인하려면 사본을 만들어 치환하고 돌린다.

```
node -e 'const fs=require("fs");const s=fs.readFileSync("scripts/check-mermaid.mjs","utf8");
fs.writeFileSync("scripts/.tmp-x.mjs", s.replace(FROM, TO));'
node scripts/.tmp-x.mjs --self-test; rm -f scripts/.tmp-x.mjs
```

🔴 **고친 뒤에는 반드시 이것을 돌려라.** 통과만 보고는 케이스가 헛도는지 알 수 없다.
확인한 것은 「FAIL 이 나온다」이지 「PASS 가 나온다」가 아니다.

### 5. 🔴 **검사기는 「없는 위반」도 만들어 낸다 — 0건일 때만 의심하는 습관은 절반이다**

직전 세션의 교훈이며 여전히 유효하다. 정규식으로 짠 일회성 스크립트가 **31건을 전부 거짓
양성**으로 냈다. 거짓 양성은 파일명과 줄 번호를 갖추고 나오므로 스스로 검증되는 것처럼 보인다.

⇒ 마크다운의 **의미**를 묻는 검사는 일회성이어도 **파서로** 짜라. **위반이 나왔을 때도
몇 건을 열어 확인한 뒤에 고치러 가라.** 전체 경위는 `docs/TOOL-TRAPS.md` 44번에 있다.

### 6. ⚠️ 짧게 — 여전히 유효한 것들

| 무엇 | 요지 |
| --- | --- |
| **일부 누락 가드** | 「대상 없음」 가드는 하나도 못 받았을 때만 발동한다. 받은 것 중 일부를 버리면 숫자가 그럴듯하게 나온다. 대상이 되지 못한 경로가 있으면 **종료 코드 2** |
| **대상 수집은 검사기 안에서** | 호출부에서 `git ls-files` 로 뽑아 넘기지 마라. `core.quotePath` 를 끄는 자리를 한 곳으로 모았다 |
| **뮤턴트 ID 중복** | 새 검사기의 뮤턴트를 추가하기 전에 `grep -ao 'id: "[A-Z]*[0-9]*"' scripts/mutate.mjs \| sed 's/id: //' \| tr -d '"' \| sort \| uniq -d` |
| **`scripts/mutate.mjs` 는 grep 이 바이너리로 본다** | NUL 이 한 개 있다. 결함이 아니라 의도다 — `grep -a` 를 붙여라 |
| **훅만 있는 검사는 지켜지지 않는다** | `--no-verify` 로 지나가고 웹 UI 편집은 훅을 안 탄다. 새 검사기는 **훅과 CI 양쪽에** |
| **네 검사 모두 전량을 본다** | 스테이징된 것만 보면 깨뜨린 쪽이 아니라 **깨진 쪽**이 통과한다. 문서 전량 스캔은 도식까지 합쳐 실측 **약 8초** |
| **뮤테이션은 단독으로** | 러너가 파일을 심었다 되돌리는 동안이다. 다른 파일 작업과 병행하지 마라 |

---

## 검사기 아홉 종 — 순서는 「증명한 뒤 스캔」이다

| 검사기 | 무엇을 | 자기 검사 |
| --- | --- | ---: |
| `check-forbidden` | 금칙어 (소스 · 빌드 산출물) | 15건 |
| `dup-scan` | 발행본 사이의 축자 복제 | 7건 |
| `source-overlap` | 리포 밖 **원본**과의 겹침 | 28건 |
| `compose` | 성분별 분해 (분량 예산) | — |
| `check-counts` | README·CHANGELOG 의 발행본 수 | — |
| `check-markup` | 렌더되지 않는 강조 | 24건 |
| `fix-markup` | 위 위반의 교정 | 19건 |
| `check-links` | 깨진 링크 (R1·R2·R3) | 23건 |
| 🆕 `check-mermaid` | 그려지지 않는 도식 (R1) | **21건** |

**증명하기 전에는 0을 믿지 마라.** 그리고 **위반이 나왔을 때도 검사기를 의심하라.**
이번 세션이 더한 것 — **자기 검사가 통과해도 그 케이스가 무언가를 지킨다는 뜻은 아니다.**

`npm run mutate` 로 뮤턴트 **48개**를 되살려 자기 검사가 잡는지 본다. 검사기를 고쳤으면
반드시 돌린다. 실행에 3~5분 걸리므로 백그라운드로 돌리고 **다른 파일 작업과 병행하지 마라.**

---

## 남은 사각지대 — 다음 후보

| 후보 | 규모 | 비고 |
| --- | ---: | --- |
| **앵커 유효성** | 발행본 앵커 링크 **28개** | `check-links` 는 앵커를 **분리만** 하고 판정하지 않는다. 한글 제목의 슬러그 규칙을 렌더러에서 확인해야 한다 |
| **도식의 의미** | 도식 649개 | 문법은 지금 검사한다. 「화살표가 끊겼는가」·「라벨이 본문과 어긋나는가」는 여전히 사람의 판단이며 `PUBLISHING-CHECKLIST.md` 에 있다 |
| 분량 하한 SOFT **5편** | `ai-agent` 1 · `rag` 1 · `search-engineering` 3 | 이 배치 밖의 옛 편들이라 별도 판단이 필요하다 |
| 시리즈 A·B 밖의 새 배치 | — | 원본이 어디에 있는지부터 정해야 한다 |

---

## 다음 배치를 시작하면 다시 읽을 것

배치 작업이 끝나 지금은 쓰이지 않지만, 새 배치에서 **그대로 되풀이될** 것들이다.

- 🔴 **`compose` 는 「본문을 마지막으로 만진 뒤」 돌린다.** 복제 제거뿐 아니라 **리뷰 반영**도
  분량을 늘린다 — 편8에서 상한을 세 번 넘겼고 원인이 매번 달랐다. **표 예산이 총량보다 먼저
  터진다**(표 여유가 편6 296 → 편7 65 → 편8 **13** 으로 좁아져 왔다).
- 🔴 **인수인계에 적힌 수치도 대조군 없이는 근거가 아니다.** 편8 착수 때 원본 B 9,682 가
  틀렸다(실측 9,430). **경계는 줄 수가 아니라 「인용 줄이 끊기는 지점」으로 찾아라.**
- 🔴 **금칙어 때문에 뺀 블록은 낱말만 빼면 되는 것이 아니다.** 배정 밖 블록의 **판단**이
  본문으로 새는 일이 편6·편7·편8 **세 편 연속**이었다. 집필 전에 「이 블록에서 온 판단은
  쓰지 않는다」를 **명제 단위로** 적어라.
- 🔴 **`dup-scan` 0건은 원본 대조의 0건이 아니다.** 편8은 `dup-scan` 7건을 고친 **뒤에**
  `source-overlap` 이 편7의 4.6배를 냈다. **표를 전치할 때 칸이 증발한다** — 전치 뒤에는
  원본과 칸 수를 대조하라.
- 🔴 **산식의 상수 2,455 는 상수 절의 예산이 아니다.** 회귀 절편이며, 상수 절의 초과분은
  배율 안에 이미 흡수돼 있다. **산식의 항을 문서의 부위에 대응시키지 마라.**
- **초고가 어느 쪽으로 벗어나는지는 원본의 표 비중이 정한다.** 편8은 이것을 예측에 썼고
  「약 +23% 넘친다」가 실측 +22.6% 로 맞았다.

---

## 알려진 문제

- 🔴 **`compose` 통과는 배정 타당성이 아니다.** 성분 실측 **바로 다음에** 배정 원본의 금칙어를
  직접 세라. `LC_ALL=C` 필수다.
- 🔴 **표 배율을 `원본 표 B × 배율` 로 예측하지 마라.** 표는 **절대값 예산**으로 관리한다.
- SOFT 2건이 `agentic-coding/thirty-agent-catalog.md:195` 에 상시 남아 있다 — 정당하다.
- 분량 하한 SOFT 5편(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은 이 배치 밖의 옛 편들이다.
- 「강의」는 SOFT 금칙어다. 원본이 강의 노트라 **출처를 지칭하면 걸린다.**
- 배치 전체 `dup-scan` 20건은 전부 용어 정의 · 법률명이며 정당하다.
- ⬜ 강조 렌더 실패 **14회**가 `.superpowers/sdd/` 에 남아 있다. `*` 로 gitignore 된 도구
  부산물이라 커밋되지도 렌더되지도 않는다 — **검사 대상이 아니다.**
- ⚠️ `jsdom` 이 devDependency 로 들어왔다. `npm audit` 이 경고를 내지만 **검사기 전용**이며
  빌드 산출물에는 들어가지 않는다.

---

## README/문서 갱신 필요

이번 세션이 **고치지 않고 남긴** 것이다.

| 무엇 | 왜 남겼나 | 확인할 진실원 |
| --- | --- | --- |
| README 에 없는 npm 스크립트 `check-counts:verify` · `check-counts:print` · `map-terms:verify` · `compose:verify` · `source-overlap:verify` · `mutate:verify` | 전부 `:verify` 계열이고, README 는 본 명령 줄에 「`:verify` 는 자체 검사」로 함께 적는 방식을 쓴다. **일관된 의도로 보이므로** 세션 끝에 판단해 바꾸지 않았다 | `README.md` 의 서술 방식 |
| `check-forbidden:verify` 의 「15건」 | 두 세션 연속 건드리지 않았고 출력 형식이 달라 자동 대조가 안 됐다 | `node scripts/check-forbidden.mjs --self-test` |

⚠️ 수집 스크립트의 `ENV_KEYS_IN_CODE_BUT_NOT_IN_README`(`OPENAI_API_KEY` 등 5개)는
**오탐이다.** 코드에는 없고 **블로그 본문 3편의 코드 예시**에서 왔다. 이 프로젝트는 정적
포트폴리오라 그런 환경변수를 쓰지 않는다 — README 에 넣지 마라.

⚠️ `[LOW] SCRIPTS_DIR_MISSING_IN_README`(`check-mermaid.mjs` 등)도 **오탐이다.** README 는
파일명이 아니라 **npm 스크립트 이름**으로 문서화한다.

### 문서가 낡던 자리 — 이번에 고친 것

| 어디 | 적혀 있던 값 | 실제 |
| --- | ---: | ---: |
| `CLAUDE.md` 뮤턴트 수 | 16개 | **48개** |
| `README.md` 뮤턴트 수 | 38개 | **48개** |
| `CLAUDE.md` 훅 단수 | 7단 | **9단** |

`CLAUDE.md` 의 16개는 **세 세션 동안 낡아 있었다.** 뮤턴트를 추가할 때 문서를 함께 세는
습관이 없으면 반드시 어긋난다.

---

## 도구 함정 — 이번 세션에서 실제로 겪은 것

전체 **45건**은 [`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md) 에 있다. 이번에 더한 하나:

| # | 무엇 |
| ---: | --- |
| **45** | 브라우저용 라이브러리를 Node 에서 부르면 **판정에 닿지 못한 것이 통과로 집계**된다 |

되풀이해 겪은 것들:

- **스크래치패드에서는 리포의 `node_modules` 를 해석하지 못한다.** `ERR_MODULE_NOT_FOUND`
  가 난다. 프로브 스크립트는 리포 루트에 두고 실행 직후 지운다.
- **`$TMPDIR` 이 비어 있으면 경로가 루트로 붙는다.** `Permission denied` 로 죽는다.
  절대 경로를 직접 쓴다.
- **`git add` 뒤 `LF will be replaced by CRLF` 경고가 뜬다.** 작업 트리 이야기이며
  **커밋된 blob 은 LF 다** — `git cat-file blob HEAD:<경로> | tr -cd '\r' | wc -c` 로 확인하라.
- **`|` 뒤의 종료 코드는 마지막 명령의 것이다.** 종료 코드를 읽을 명령은 단독 실행.
