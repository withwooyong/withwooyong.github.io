# Session Handoff

> 갱신: 2026-08-30 (KST)
> 브랜치: `main` — 이 세션에서 **origin 으로 푸시했다** (사용자 승인: 「푸시까지」)
> 최신 커밋: 편3 발행(`ec125ef`)과 설계서 §10-5 갱신

## 현재 상태

원본 `java-backend/08-스프링부트-인증-쿠버네티스.md`(49,201 B 배정분)를 **6편**으로 나누는 배치가
진행 중이다. **편1·편2·편3 발행 완료**, 절차 ⑤(편3 실측으로 편4·편5 판정)까지 끝났다.
다음은 **편4 집필**이고, 목표가 14,080 에서 **15,900** 으로 올라갔으며 **밴드를 넓혔다.**

## 이번 세션에 한 일

| # | 작업 | 커밋 | 산출물 |
| ---: | --- | --- | --- |
| 1 | 편3 집필·검증·발행 | `ec125ef` | `content/blog/backend-engineering/requirements-spec-to-erd.md` (17,896 B) · 편2 링크 수정 · README · CHANGELOG |
| 2 | 편3 실측을 설계서 §10-5 에 반영하고 편4·편5 판정 | (후속 커밋) | `docs/superpowers/plans/2026-08-29-auth-k8s-split-design.md` |

`/ted-run` 파이프라인으로 돌렸다. 검증 결과: `check-forbidden` **HARD 0 · SOFT 2**(둘 다 이번 배치
무관), `dup-scan` 편3·편2 각 **0건**, 원본과의 축자 겹침 **20자 이상 0건**, `npm test` **54개 통과**,
`tsc --noEmit` 통과, `check-counts` **164편 일치**.

## 다음 할 일

| # | 작업 | 상태 | 메모 |
| ---: | --- | --- | --- |
| 1 | **편4 집필** (`session-vs-token-authentication`) | 다음 차례 | 원본 §7·§8. 목표 **15,900 B**, 밴드 13,900 ~ 18,000. 🔴 **반려선 13,300 을 브리프에 적어라** — 하단 여유가 600 B 뿐이다 |
| 2 | 편3 → 편4 링크(62 B) 채우기 | 편4 발행 커밋에 포함 | 편3은 밴드 상단까지 864 B 여유라 여유롭다 |
| 3 | 편5 집필 (`kubernetes-deployment-and-rollout`) | 대기 | 목표 **18,200 B**, 밴드 16,000 ~ 20,400. ⚠️ 원본 산문이 **37 B** 로 사실상 0이라 배율 모델이 외삽 구간이다 |
| 4 | 편6(Q&A) 집필 + **편1~5 링크 수정 5건** | 대기 | 규칙 12. 19,313 B · 42항목 |
| 5 | 타 카테고리 인바운드 링크 3건 | 대기 | §6-3 — `agentic-coding` 2편 · `ai-transformation` 1편 |
| 6 | 배치 마감 (§10-0 가설표) | 대기 | C-1 · C-2 · C-3 · **C-4(확인됨)** · **C-7(확인됨)** · **C-8(가설 수정 필요)** |

## 이번 세션의 판단과 근거

- **편4·편5의 목표를 다시 찍지 않고 밴드를 넓혔다.** 편2 비율과 편3 비율로 재환산하면 편4가
  14,080 과 17,669 로, 편5가 16,810 과 19,614 로 갈린다. 간격이 목표치의 20% 를 넘는다는 것은
  **표본 둘로 셋째 값을 정밀하게 찍을 수 없다**는 뜻이므로, 두 모델을 감싸는 밴드를 주고
  초고 `compose` 실측으로 확정하는 쪽을 택했다.
- **표 비율이 처음으로 1 을 밑돌았다(0.89).** 편2의 1.22 와 갈린 이유는 성분이 아니라
  §3-2-b 의 합치기 계획을 지켰는가다 — **C-4 가 참으로 확인됐다.**
- **표를 합치면 그만큼 산문이 는다는 것을 실측했다.** 표가 계획보다 1,354 B 줄었는데 본문 산문은
  2,553 B 늘었다. 표에서 뺀 열은 사라지지 않고 설명 문장으로 자리를 옮긴다. 두 항을 따로 세워 놓고
  한쪽만 줄이면 총량 예측이 어긋난다.
- **분해식에 헤딩 항이 없다는 것을 발견했다**(427 B). frontmatter 에 이은 두 번째 누락 항이고,
  절이 늘면 함께 느는 값이라 상수가 아니다 — **절 수 × 약 50 B** 로 잡아라(§10-5-a).
- **C-8 을 「상수인가」에서 「발행 순서로 줄어드는가」로 바꿔야 한다.** 고정 비용이 3,338 → 3,026 →
  2,715 로 폭까지 일정하게(−312 · −311) 감소한다. 편3은 밴드 하단을 385 밑돌았다.

## 🔴 리뷰가 잡은 것 — 검사기 전부 통과한 뒤에 나왔다

편4~편6에도 그대로 적용된다.

| 결함 | 왜 기계가 못 잡나 |
| --- | --- |
| `role` 테이블의 행을 「권한」 27회 · 「역할」 6회로 갈라 불렀다 | 편1 §0 용어표가 정한 규칙인데 검사기에 없다. **집필 브리프에 편1 용어표의 해당 행을 그대로 붙여라** |
| 「목록에 있는 명사는 빠질 수가 없다」가 자기 표에서 반증됐다 | 원본이 한 방향만 말한 주장을 양방향으로 **강화**한 것이 결함이다 |
| `/login`·`/logout` 이 「새 테이블을 부르지 않는다」가 편4와 충돌한다 | 뒤 편이 뒤집을 단정에는 「이 단계에서는」을 붙인다 |

⇒ **리뷰 축을 「경계·정합성」과 「원본 축자 겹침」 둘로 나눠 돌려라.** 한 축으로는 이 층이 안 보인다.
리뷰어는 `Write` 도구가 없으므로 브리프에 **보고 파일 경로와 heredoc 작성**을 반드시 명시한다.

## 알려진 문제

- **편2의 밴드 여유가 2 B 뿐이다.** 편3 링크를 넣어 21,318 이 됐고 상단이 21,320 이다.
  앞으로 편2 본문을 한 글자라도 늘리면 밴드를 벗어난다.
- **`grep -r` 로 리포를 훑으면 `out/` 과 `node_modules` 까지 스캔해 120초 타임아웃이 난다.**
  `Grep` 도구나 `--include` / 명시 경로를 써라.
- `check-forbidden` SOFT 2건은 `agentic-coding/thirty-agent-catalog.md:195` 의 「면접」·「이력서」로,
  채용 에이전트를 설명하는 정당한 문맥이다. 이번 배치와 무관하다.
- `npm test` 가 SOFT 로 보고하는 **분량 하한 미달 5편**(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은
  이번 배치 이전부터 있던 것이다. 빌드를 막지 않는다.

## 다음 세션을 위한 맥락

사용자의 목표는 사내 지식 문서(`yanadoo-exit/shared/knowledge/`)를 기술 블로그 발행본으로 옮기는
배치 작업이고, 이 리포의 주된 작업 방식이다. **핵심은 분량을 감으로 정하지 않는 것**이다 —
설계서가 원본을 성분(산문·표·코드·도식·불릿·인용)으로 분해해 편별 예산을 계산하고, 편마다 실측해
남은 편의 예산을 재환산한다. 그 계보가 `docs/superpowers/plans/*-split-design.md` 에 쌓여 있다.

**편4를 시작할 때 읽을 것**

| 순서 | 대상 | 왜 |
| ---: | --- | --- |
| 1 | 설계서 **§10-5-f** | 편4 목표 15,900 과 넓힌 밴드의 근거. 반려선 13,300 경고가 여기 있다 |
| 2 | 설계서 **§10-5-g** | 편3이 남긴 집필 관찰. 리뷰가 잡은 결함 셋이 편4에도 그대로 적용된다 |
| 3 | 설계서 **§3-2-b** · **§4-1** | 편4의 표 합칠 계획(§8-1 비교표에 §8-2 회전 정책의 「왜」 열을 붙인다)과 주제 경계 |
| 4 | 편1 **§0 용어표** · 편3 **맺음말** | 규칙 11. 편1 용어표는 「토큰」·「세션」 표기를 못 박아 두었고 편4가 그 두 낱말의 본편이다 |

🔴 **초고를 쓴 직후 `npm run compose` 를 먼저 돌려라.** 편3에서 이 절차를 지켰더니 초고가 목표를
**1.7%** 넘는 데 그쳤다. 편2는 이 절차 없이 산문부터 깎아 네 차례에 걸쳐 5,900 B 를 깎고도
밴드에 못 들었다.

발행 절차는 설계서 §8-1 의 일곱 검사를 순서대로 돌리고 §9-1 의 커밋 단위를 지킨다.
커밋은 `git commit -m "…" -- <경로>` 형태인데, **새 파일은 `git add` 를 먼저 해야 한다** —
경로를 명시해도 추적되지 않은 파일은 pathspec 에러로 죽는다.

**푸시 정책** — `main` 이 프로덕션(GitHub Pages)이므로 사용자가 명시적으로 요청할 때만 푸시한다.
이번 세션은 파이프라인 시작 시점에 「푸시까지」를 승인받았다.
