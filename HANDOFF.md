# HANDOFF

> 마지막 갱신 2026-09-03 · `pm` 배치 **시리즈 A 9/9 완결 · 시리즈 B 7/8 (편7 발행 완료)**

## 지금 상태

발행본 **183편**이다. 이번 세션은 **편5 · 편6 · 편7 세 편을 썼다.** 편7에서 드러난 것은 본문의
결함이 아니라 **검사가 초록인 이유를 묻지 않으면 초록이 정보가 아니라는 것**이다 — 산출물 검사가
편7을 한 번도 보지 않은 채 HARD 0 을 냈고, 금칙어 자기 검사는 18개 가운데 5개만 증명하고 있었다.

| 항목 | 값 |
| --- | --- |
| 발행 완료 | **A 편1~9 + B 편1~편7 (183편)** |
| 다음 단계 | **B 편8 `mydata-and-market-structure`** — 원본 `pm-po/04` §4·§5·§6 (**마지막 편**) |
| 편8 예산 | 🔴 **원본 B 실측 9,682** (설계서 9,419 대비 **+263**) · 목표 **22,303** · 밴드 **20,948 ~ 23,659** |
| 🔴 편8 상한 | 다음 편이 없으므로 링크 몫 0. 밴드 상한에서 **여유 600(리뷰 몫)** 만 뺀 **23,059** |
| 푸시 · 배포 | **편7까지 `origin/main` 반영됨** (2026-09-03). `main` 이 곧 프로덕션이라 푸시가 곧 Pages 배포다 |
| 커밋 | 편5 `71b40b5` · 편6 `3908175` · 편7 `6ee8c33` |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` · **§10-16 까지 있으면 편7이 끝난 것이다** |

### 이번 세션이 한 일

| # | 무엇 | 어디에 남았나 |
| ---: | --- | --- |
| 1 | **편5 발행** — `commerce-member-and-catalog` | 커밋 `71b40b5` · 설계서 §10-14 |
| 2 | **편6 발행** — `commerce-pricing-and-order` 18,535 B · k 2.169 | 커밋 `3908175` · 설계서 §10-15 |
| 3 | **편7 발행** — `fintech-regulation-and-open-banking` **17,916 B** · k **2.058** | 커밋 `6ee8c33` · 설계서 **§10-16** |
| 4 | 편6 리뷰 정확성 15 · 검사 유효성 9 / 편7 리뷰 정확성 **20** · 검사 유효성 **9** | 설계서 §10-15-g~h · §10-16-d~g |
| 5 | 🔴 **링크 몫 공식 정정** — 「슬러그 + 12 + 카테고리 길이」. 편7에서 실측 **+65** 로 검증됐다 | 설계서 §3-0-a · §10-15-b |
| 6 | 🔴 **§7-2 정정 2건** — 편6은 취소선, 편7은 지목을 **표 → 도식**으로 고쳐 살렸다 | 설계서 §7-2 · §10-15-c · §10-16-c |
| 7 | README 세 자리 · CHANGELOG 편6·편7 절 · 아카이브 이동 2회(**10절**) | `README.md` · `CHANGELOG.md` · `docs/changelog/2026-09.md` |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 0. 🔴 초록이 거짓이었던 자리가 편7에서 셋 나왔다 — **검사가 무엇을 읽었는지 확인하라**

| 초록 | 실제 |
| --- | --- |
| `check-forbidden:built` HARD 0 · 종료 코드 0 | 🔴 **편7을 한 번도 보지 않았다.** `out/` 이 본문보다 1시간 42분 오래됐고 `grep -rl <슬러그> out/` 이 **0개**였다 |
| `check-forbidden:verify` 15/15 | 🔴 HARD **18개 중 5개**만 증명한다. 미커버 13개에 **야나두·티빙·TVING·커머스개발·실장·아르고스**가 전부 들어 있다 |
| `source-overlap:verify` 28/28 | 🔴 `MIN_DEFAULT` 를 20 → 200 으로 바꿔도 **그대로 28/28** 이다. 본 스캔은 ① 0건 · ② 0건이 되는데도 그렇다 |

셋 다 **「검사가 통과했다」와 「검사가 그것을 봤다」 사이의 틈**이다.

⇒ 🔴 **편8 은 이 순서를 지켜라.**

```bash
npm run build
LC_ALL=C grep -rl 'mydata-and-market-structure' out/ | wc -l   # 0 이면 중단한다
npm run check-forbidden:built
```

⇒ 🔴 **금칙어는 손으로도 세라.** 자기 검사가 덮지 않는 13개 때문이다. 배정 원본 · 발행본 ·
산출물 세 곳에서 `LC_ALL=C` 로 18개를 전부 센다. 산출물의 **「허우용」 1건은 JSON-LD author
면제**(`BUILT_ALLOW`)이며 정당하다 — 그 문자열 밖에 나오면 위반이다.

### 1. 🔴 초고가 어느 쪽으로 벗어나는지는 **원본의 표 비중**이 정한다

편7 초고는 **−14.8%** 로 배치 처음 목표를 밑돌았다. 편2~편6 은 전부 넘쳤다.

| 배정 원본 표 비중 | 초고 | 편 |
| ---: | --- | :---: |
| 41% (3,657 / 8,880) | 🔴 **−14.8%** | 7 |
| 낮음 | +28.3% | 6 |

**표는 옮길 때 늘어날 여지가 거의 없으므로 표 비중이 높은 절일수록 배율이 1 에 수렴한다.**
편7의 용어 절(원본 표 100%)은 배율 **1.04** 였다.

⇒ 🔴 **편8 은 착수 시점에 배정 원본의 표 비중을 먼저 재라.** 40% 를 넘으면 초고가 모자랄 것을
예상하고 절별 목표를 배율이 아니라 **절대값**으로 잡는다.

⇒ **절별 배율표는 양방향으로 쓴다.**

| 초고가 | 어디부터 손대나 | 회차 |
| --- | --- | :---: |
| 넘쳤다 | **배율이 가장 높은 절부터 깎는다** | 편6: 고르게 깎다가 6회차 |
| 모자란다 | **배율이 가장 낮은 절부터 늘린다** | 편7: **3회차** |

### 2. 🔴 리뷰 반영의 **방향**은 분량 조정의 방향이 정한다

| 편 | 리뷰 반영 | 지적의 성격 |
| :---: | ---: | --- |
| 6 | 🔴 **+822** | 감축 6회차로 **증발한 원본 항목의 복원** |
| 7 | 🔴 **−361** | 초고를 채우며 넣은 **원본 근거 없는 단정의 제거** |

⇒ **여유 600 은 유지한다.** 다만 편8은 어느 쪽일지 미리 안다 — 초고가 넘치면 반영이 늘리는
쪽, 모자라면 줄이는 쪽이다.

### 3. 🔴 배정 밖 블록의 주장 유입이 **두 편 연속** — 막힌 것과 못 막은 것의 차이

편6 2건 · 편7 2건이고 `source-overlap` 은 **네 건 모두 잡지 못했다.** 낱말을 바꿔 옮기기 때문이다.

**그런데 편7의 세 블록 가운데 하나는 완전히 막혔다.**

| 목록에 적힌 명제 | 막혔나 |
| --- | :---: |
| 「도식에 여섯째 층(**기능**)을 만들지 마라」 — **본문에서 무엇을 하면 위반인지** 적혀 있다 | ✅ |
| 「리서치 과제를 받으면 대상 확정 기준부터 정의하라」 — 추상적이다 | ❌ |
| 「표준 인프라가 동등한 출발선을 만든다」 — 추상적이다 | ❌ |

⇒ 🔴 **집필 전에 주장 목록을 만드는 것만으로는 부족하다.** 명제를 그대로 옮겨 적지 말고
**「본문에서 이렇게 쓰면 위반」으로 번역**해서, 금지할 낱말과 금지할 구조를 함께 적어라.
그것이 유일하게 작동한 방식이다.

### 4. 🔴 링크 몫 공식이 편7에서 검증됐다

> 🔴 **링크 몫 = 슬러그 길이 + 12 + 카테고리 길이** (시리즈 B 는 +30)

12 는 마크업이다 — `[` 1 + `](/blog/` 8 + 카테고리 뒤 `/` 1 + 슬러그 뒤 `/)` 2.

| 편 | 슬러그 | 옛 값(+31) | **고친 값(+30)** | 실측 |
| :---: | --- | ---: | ---: | ---: |
| 5 → 6 | `commerce-pricing-and-order` | 57 | **56** | **56** ✅ |
| 6 → 7 | `fintech-regulation-and-open-banking` | 66 | **65** | **65** ✅ |
| 7 → 8 | `mydata-and-market-structure` | 58 | **57** | 편8에서 확인 |

⚠️ **편8은 다음 편이 없으므로 자기 여유에서 링크 몫을 뺄 필요가 없다.**

### 5. 🔴 링크 수는 `grep -c` 로 세지 마라 — **줄 수를 센다**

```bash
LC_ALL=C grep -o '](/blog/' <발행본> | wc -l    # 3 이상이어야 한다 (다음 편 미부착이면 2)
```

편7에서 링크가 3개인데 `-c` 가 **2** 를 냈다. 용어 절 뒤 문단에 앞 편과 지도편 링크가 **한 줄에
함께** 있었기 때문이다. 감축·증량 회차마다 이 명령으로 확인하라 — 편6에서 지도편 링크가 감축
중에 사라진 적이 있고, `npm test` 의 고립 검사는 **들어오는** 링크만 본다.

### 6. 🔴 표에는 절대값 예산, 도식에는 계수 — **서로 바꿔 쓰지 마라**

| 항목 | 정본 |
| --- | --- |
| 표 | **4,485 ± 335** (절대값) — §10-14-d |
| 도식 | **원본 도식 B × 1.263** (계수) — §3-3 |

편7 착수 노트가 도식 예산을 「실측 배율 1.42 ~ 1.89」로 잡았는데 그것은 편2·편3·편6 의 **사후
실측을 그대로 밴드로 삼은 것**이라 근거가 없었다. 계수 기준으로는 505 × 1.263 = 638 이고
실측 649 는 통과다. **실측 배율은 계수를 갱신할 근거일 뿐 그 자체가 예산이 아니다.**

### 7. 🔴 §7-2 의 지목이 **두 편 연속** 틀렸다

| 편 | 근거 낱말 | 자리 | 처리 |
| :---: | --- | --- | --- |
| 6 | `할인가` **0건** — 위험이 허구 | 틀림 | **취소선** |
| 7 | `시행령` **2건** — 위험은 실재 | 틀림 (표 → 도식) | **지목을 고쳐 살림** |

⇒ **원본의 자리를 지목할 때는 낱말을 `grep` 으로 실재 확인하고, 층(표 / 도식 / 산문)까지 적어라.**
설계 단계에서 원본을 「읽고」가 아니라 「훑고」 지목한 것이 두 번 연속 드러났다.

### 8. 검사기 사각지대 — **넷 다 다음 배치로 넘겼다**

| # | 무엇 | 상태 |
| ---: | --- | --- |
| 1 | `dup-scan:verify` 가 공용 정규화 뮤턴트를 통과시킨다 (파이프 제거 시 편7 판정이 0→1 인데 7/7) | 편6에서 넘김 · **미수정 확인** |
| 2 | `compose` 의 `wc -c` 라벨이 파일에서 재지 않는다 | 편6에서 넘김 · **미수정 확인** |
| 3 | 🔴 `source-overlap` 의 **임계값이 손복사**되어 있다 (케이스 8곳이 리터럴 `20`) | **신규** |
| 4 | 🔴 **링크 제목 추출 단계를 두 자기 검사 어느 쪽도 보지 않는다** (무력화 시 16→44건인데 7/7 · 28/28) | **신규** |

3번은 §10-13-h 의 M5 와 같은 형태의 **세 번째** 확인이다 — **정규화를 `normalize.mjs` 로 합쳐
없앤 손복사 드리프트가 임계값에서 되살아났다. 공용 모듈로 묶는 것은 묶은 것만 지킨다.**

⇒ 고치면 편1~편7 의 과거 수치를 다시 재야 하므로 넘긴다. **다음 배치 첫 안건이다.**

### 9. ✅ 지도편 배정 오류가 다섯 편 만에 0건이다

편3 3건 · 편4 1건 · 편5 2건 · 편6 2건 → **편7 0건.**

⚠️ 다만 브리프에 「배정 용어 7개」로 적었는데 실측 **6개**였다 — 지도편에서 「참가기관과 이용기관」이
**한 행**인 것을 둘로 셌다. ⇒ **배정 용어는 낱말이 아니라 지도편의 행으로 센다.**

### 10. 남은 작업

| 대상 | 상태 |
| --- | --- |
| 편7 말미 「다음 편」 링크 | **편8 발행 때 부착** (**57**) |
| 지도편 구성 표 형제편 링크 | 편1~편7 부착 완료. **편8 하나 남았다** |
| 배치 전체 `dup-scan` | 🔴 **편8 발행 뒤 시리즈 전체로 한 번 더** (§8-4 · 절차 ⑦) |
| 푸시 | ✅ **편7까지 완료.** 편8 발행 뒤 다시 **사용자의 명시적 요청**을 받는다 |

---

## 시리즈 B 사양 요약

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (편1~편7 발행) |
| 원본 | `C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/` |
| 편수 | 8편 (편1 지도 ✅ · 편2~편7 ✅ · **편8 남음**) |
| 산식 | **목표 B = 2,455 + 원본 B × 2.05 + 링크 몫** · 밴드 k ± 0.14 (1.91 ~ 2.19) |
| 역산 k | **(발행 B − 2,455 − 링크 몫) / 원본 B** (§10-14-b) |
| 원본 B 정본 | **배정 절의 행 추출 − 「💼 면접 활용」 블록의 인용 줄만**(§10-14-c). 설계서 표와 몇 B 어긋난다 — 편6 −5 · 편7 **−11** |
| 표 예산 | **4,485 ± 335** (절대값) |
| 도식 예산 | **원본 도식 B × 1.263** (계수) |
| 🔴 낱말 | 「기획」은 시리즈 A 와 뜻이 다르다. 편1이 갈라 뒀고 편2~편7 이 그 용법을 따랐다 |
| 🔴 편8 | 원본 `04` **155~302행** · 행 추출 11,053 − 블록 1,371 = **9,682** · 목표 **22,303** · 밴드 20,948 ~ 23,659 · **실질 상한 23,059** |
| 🔴 편8 경계 | 마이데이터 · 업권 경쟁 구도 · 스페셜 논점. **편7이 「데이터 3법」을 이름만 짚고 넘겼다** |
| 🔴 편8 배정 밖 | 「💼 면접 활용」 블록 **3개** — 원본 **211~213행 · 241~243행 · 298~300행** (각 3줄) |
| 🔴 편8 표 비중 | **29.7%** (3,288 / 11,053) — 40% 미만이므로 **초고가 넘칠 것을 예상하라**(위 1번) |

🔴 **편8 원본 B 는 설계서 값과 +263 어긋난다.** 설계서 §3-0 표가 뺀 값을 **1,635** 로 적었는데
「인용 줄만」 규칙(§10-14-c)으로 실측하면 **1,371** 이다. 착수 시 **§2-4-a 의 파일 합계와 대조**해
어느 쪽이 맞는지 판정하라 — 편5 814 + 편6 1,373 + 편7 1,369 + 편8 1,371 = **4,927** 이다.

### 편별 실측

발행 B 는 **현재 파일 크기**(`compose` 규약 = `wc -c` + 1)이며 리뷰 반영과 링크 부착이 누적된 값이다.

| 편 | 원본 B | 발행 B | 링크 몫 | 역산 k | 초고 이탈 | 감축·증량 회차 | 리뷰 반영 |
| :---: | ---: | ---: | ---: | ---: | ---: | :---: | ---: |
| 2 | 6,727 | 17,186 | 55 | 2.182 | — | — | +243 |
| 3 | 5,578 | 14,745 | 55 | 🔴 2.193 초과 | — | — | +551 |
| 4 | 8,118 | 19,409 | 57 | 2.081 | +13.9% | 2 | +344 |
| 5 | 7,834 | 19,500 | 56 | 2.169 | — | 3 | 발행 전 |
| 6 | 7,389 | 18,535 | 65 | 2.169 | 🔴 **+28.3%** | 6 | 🔴 **+822** |
| **7** | **7,511** | **17,916** | **0** | **2.058** | 🔴 **−14.8%** | **3** | 🔴 **−361** |

⚠️ 편7의 링크 몫 0 은 **미달이 아니라 아직 쓰지 않은 예산**이다(§10-14-a). 링크가 붙지 않은 편은
**링크를 뺀 설계값과 비교**한다.

---

## 알려진 문제

- 🔴 **`compose` 통과는 배정 타당성이 아니다.** 성분 실측 **바로 다음에** 배정 원본의 금칙어를
  직접 세라. `LC_ALL=C` 필수다.
- 🔴 **표 배율을 `원본 표 B × 배율` 로 예측하지 마라.** 표는 **절대값 예산**으로 관리한다.
- SOFT 2건이 `agentic-coding/thirty-agent-catalog.md:195` 에 상시 남아 있다 — 기존 건이며 정당하다.
- 분량 하한 SOFT 5편(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은 이 배치 밖의 옛 편들이다.
- 「강의」는 SOFT 금칙어다. 원본이 강의 노트라 **출처를 지칭하면 걸린다** — 편7 초고에서 1건
  나왔고 주체를 지워 해결했다.

---

## 도구 함정 — 이번 세션에서 실제로 겪은 것

`docs/TOOL-TRAPS.md` 가 전체(41건)를 담고 있다. 이번에 다시 걸린 것만 적는다.

| 함정 | 이번 세션에서 |
| --- | --- |
| **히어독이 한글 마크다운에서 죽는다** | 치환 스크립트를 `python - <<'PYEOF'` 로 넘기다 `unexpected EOF`. **`Edit` 로 전환**했다. 다만 편7에서는 **짧은 치환 목록을 파이썬 heredoc 으로 넘기는 것이 성공**했다 — 실패하는 것은 긴 한글 **본문**이지 스크립트가 아니다 |
| **파이썬 쓰기가 개행을 바꾼다** | `io.open(..., newline='')` 으로 열고 써서 CR 0 을 유지했다. **쓰고 나서 `tr -cd '\r' \| wc -c` 로 확인하라** |
| **`grep -c` 는 줄 수를 센다** | 링크 3개를 2 로 셌다. 위 5번 |
| **awk `length()` 는 바이트가 아니다** | 편7 성분 실측에서 표가 `length()` 1,725 대 `wc -c` **3,657** 로 **2.12배** 어긋났다. 오류 없이 그럴듯한 표가 나온다 |
| **Bash 도구의 작업 디렉터리가 유지된다** | `cd` 한 뒤 다음 호출의 상대 경로가 어긋났다. **절대 경로를 변수에 담아 쓴다** |
| **`core.autocrlf` 경고** | 커밋 시 7개 파일에 「LF will be replaced by CRLF」가 뜬다. 정상 동작이며 **깨끗한 diff 는 증거가 아니다** |
| **서브에이전트에게 `Write` 가 없다** | 두 리뷰어 모두 히어독 또는 PowerShell here-string 으로 보고서를 썼다. 브리프에 그 방법을 적어 두면 유휴가 되지 않는다 |
