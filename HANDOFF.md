# HANDOFF

> 마지막 갱신 2026-08-30 · 인증·쿠버네티스 **6편 배치 완료**

## 지금 상태

`backend-engineering` 카테고리에 **인증·쿠버네티스 6편이 전부 발행됐다.** 총 167편이다.

| 편 | 슬러그 | B | 밴드 |
| :---: | --- | ---: | :---: |
| 1 지도 | `auth-service-launch-map` | 21,715 | 고정 비용 방식 |
| 2 | `auth-domain-and-requirements` | 21,318 | ✅ |
| 3 | `requirements-spec-to-erd` | 17,958 | ✅ |
| 4 | `session-vs-token-authentication` | 17,373 | ✅ |
| 5 | `kubernetes-deployment-and-rollout` | 19,450 | ✅ |
| 6 Q&A | `auth-kubernetes-qna` | 19,777 (판정 19,483) | ✅ |

배치 총량 **117,591 B** — 설계 목표 113,624 대비 **+3.5%**.
설계서 `docs/superpowers/plans/2026-08-29-auth-k8s-split-design.md` 의 **§10-8 이 마감 절**이다.
다음 배치는 그 절의 **§10-8-g 규칙 14개**를 먼저 읽어라.

### 이번 세션이 한 일

| # | 작업 | 커밋 |
| ---: | --- | --- |
| 1 | 편6 집필·리뷰·검증·발행 + 편1·편5 링크 수정 + README·CHANGELOG | `90e4af8` |
| 2 | 타 카테고리 인바운드 링크 3건 | `a02946d` |
| 3 | 설계서 §10-0 가설 판정 + §10-8 배치 마감 + 이 문서 | (이번 커밋) |

§8-1 일곱 검사 전부 통과했다 — 금칙어 자기 증명 15/15 · HARD 0 · dup-scan 자기 증명 7/7 ·
편6 관련 0건 · `npm test` 54/54 · `tsc --noEmit` 통과 · 6편 전부 밴드 안.

---

## 다음 할 일 — **범위를 정하는 것이 먼저다**

`pm-po` 와 `pm-harness` 원본군이 남아 있고, **아직 착수 승인을 받지 않았다.**

| 항목 | 값 |
| --- | --- |
| 원본 문서 수 | `pm-po` **17** + `pm-harness` **8** = **25개** |
| 대응 카테고리 | `product-management`(order 100) ← `pm-po/` · `ai-product-planning`(order 110) ← `pm-harness/` |
| 현재 상태 | 두 카테고리 모두 `categories.ts` 에 **등록만 돼 있고 발행본 0편** |

🔴 **「25편」이 아니다.** 25는 원본 문서 수이지 발행 편수가 아니다.
최근 세 배치 실측(원본 8,172 / 8,486 / 11,893 B 당 1편)으로 환산하면 **60~85편**이다.

| 안 | 범위 | 규모 | 시간 |
| :---: | --- | --- | --- |
| **A** | 전량 이관 | 60~85편 | 88~122h |
| **B** | 선별 이관 | 15~20편 | 22~30h |
| **C** | 지도편 2편만 | 2편 | 3~4h |

사용자가 A · B · C 중 하나를 고르면 그때 설계서를 쓴다.
**설계서 없이 착수하지 마라** — `/ted-run` 진입 게이트 A 가 막는다.

---

## 이번 배치가 남긴 것 중 다음 배치가 반드시 알아야 할 것

전체 14개는 설계서 §10-8-g 에 있다. 여기에는 **성격이 바뀐 것**만 적는다.

### 1. 🔴 검사가 「0건」을 냈는데 그 0이 틀린 경우가 처음 나왔다

편6 집필자가 「20자 이상 연속 겹침 0건」을 파이썬으로 재서 보고했는데 두 겹으로 틀렸다.

| 층 | 무엇이 틀렸나 |
| --- | --- |
| ① | **공백을 지우고 셌다** — 원문 그대로 재면 최장 20자 |
| ② | **검사 범위를 브리프가 준 「옮길 범위」로 좁혔다** — 원본 전체와 대조하니 다른 절에서 16자 |

앞의 세 사각지대는 *검사가 못 보는 결함*이었지만 이번은 **검사 자체가 틀렸다.**
집필자가 자기 산출물을 자기가 짠 검사로 재면 **검사의 전제가 산출물의 전제와 같아진다.**

⇒ **자기 검사 결과는 리뷰 대상이다. 「0건」을 받으면 독립적으로 다시 재라.**

### 2. 규칙을 「N건」으로 적으면 오독되고, 「세는 명령」으로 적으면 실행된다

규칙 12 「편6 발행 커밋에 편1~5 링크 수정을 함께」를 앞 세션이 「편1~5 각각에 편6 링크」로
읽어, 여유 2 B 인 편2를 깎아야 한다는 잘못된 결론에 이르렀다.
outbound 를 **실제로 세어 보니** 결손은 편1 4건 · 편5 1건뿐이었다.

### 3. 처방 누적이 세 편 연속 들었다

편5에서 만든 「「N개」 목록은 표의 각 행과 1:1 로 짚어라. 합계가 맞는지 보지 마라」가
편6에서 **같은 유형 4건**을 잡았다. 넷 다 합계는 맞아서 개수 검사로는 원리적으로 안 잡힌다.

⇒ **브리프에 넣은 한 줄이 결함 유형을 통째로 잡는다.** 처방 세 줄을 다음 배치에 그대로 물려라.

### 4. 리뷰 두 축은 서로를 대체하지 못한다 — **네 편 연속 겹침 0**

표본이 넷이 됐으므로 이것은 관측이 아니라 규칙이다. 축을 하나로 합치지 마라.

---

## 알려진 문제

- **`dup-scan` 의 「N건」은 결론이 아니라 사람이 열어 볼 목록이다.**
  종료 코드로 판정하지 마라 — 발견이 있어도 exit 0 이다.
- **편2의 밴드 여유가 2 B 다**(21,318 · 상단 21,320). 배치가 끝나 더 넣을 것은 없으나,
  나중에 편2를 손대야 할 일이 생기면 **먼저 뺄 자리를 찾아라.**
- **`grep -r` 로 리포를 훑으면 `out/` 과 `node_modules` 까지 스캔해 120초 타임아웃이 난다.**
  `Grep` 도구를 쓰거나 `--include` / 명시 경로를 붙여라.
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.**
  금칙어 스캔을 돌릴 때마다 나오지만 이번 배치와 무관하다.
