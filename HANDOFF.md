# HANDOFF

> 마지막 갱신 2026-09-02 · `pm` 배치 **시리즈 A 9/9 완결 · 시리즈 B 1/8**

## 지금 상태

발행본 **177편**이다. 새 카테고리 `product-management` 가 열렸고 **시리즈 B 가 시작됐다.**

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 2,370줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 ✅ · B `product-management` 8 |
| 발행 완료 | **A 편1~9 + B 편1 (177편)** |
| 다음 단계 | **B 편2 `product-org-and-strategy`** — 원본 `pm-po/01` §2·§3·§4 |
| 푸시 · 배포 | ⏳ **미완** — B 편1 커밋이 로컬에만 있다. 사용자 승인 뒤 `main` 에 올린다 |

**설계서 §1 의 절차표가 정본이다.** 진행률은 **`§10-N` 의 실측 절 번호**로 판단하라 —
§10-10 까지 있으면 B 편1 이 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | 편8·편9 커밋 2개를 `main` 에 푸시 | 배포 성공 |
| 2 | B 편1 `product-management-map` 집필·발행 (24,761 B) | `content/blog/product-management/product-management-map.md` |
| 3 | 카테고리 간 링크 쌍 부착 (고립 검사 선제 대응) | `agent-definition-by-department.md` · `thirty-agent-catalog.md` |
| 4 | 축자 겹침 12곳 고쳐 쓰기 (`dup-scan` 6건 + `source-overlap` 6건) | 편1 본문 |
| 5 | B 편1 실측과 **H-1 판정** | 설계서 **§10-10** (6개 하위 절) · §1 절차표 ⑤ · §10-0 H-1 |
| 6 | 새 도구 함정 41번 + 분류 표 누락(39·40) 보정 | `docs/TOOL-TRAPS.md` · `CLAUDE.md` |
| 7 | README 세 자리 (176 → 177 · 카테고리 7 → 8) | `README.md` |
| 8 | CHANGELOG 새 절 + 편7 절을 아카이브로 이동 | `CHANGELOG.md` · `docs/changelog/2026-09.md` (3절) |
| 9 | 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. ✅ 재환산은 **전부 끝났다** — 예산표를 그대로 쓴다

§3-6 이 남긴 재환산 지점 셋이 모두 지나갔다. **B 편2~8 은 §3-2 의 예산표를 고치지 않고
그대로 쓴다.**

| 항목 | 판정 |
| --- | --- |
| 고정 비용 상수 **2,455** | 유지 |
| k = **2.05** · 밴드 1.91 ~ 2.19 | 유지 |
| 반려선 (B-3 · 13,300 B) | 유지 |

근거는 **H-1 판정**(§10-10-a)이다. 지도편 고정 비용이 A 3,834 대 B **3,859** 로 25 B 차였고,
배정 원본이 8,646 대 6,019 로 30% 다른데도 같은 값이 나왔다. **시리즈 경계는 고정 비용에
아무 일도 하지 않는다.** 그래서 지도편 값을 본편으로 옮길 이유도 없다.

⇒ B 편2 초고 `compose` 에서 **마지막으로 검산만** 한다. 값이 밴드 안이면 그대로 간다.

### 2. 🔴 개행을 바꾸면 원본 대조가 **거짓 0** 을 낸다 — 이번 세션의 최대 함정

파이썬 치환으로 발행본을 고친 뒤 `source-overlap` 이 세 원본 모두에서 **20자 0건 · 16자
0건**을 냈다. 다 고쳐진 것으로 보였지만 사실은 **검사기가 대조할 문단 자체를 잃은 것**이었다.

| 시점 | ① 공백 보존 | ② 공백 제거 |
| --- | ---: | ---: |
| 고치기 전 | 11건 | 5건 |
| CRLF 상태 (**거짓**) | **0건** | **0건** |
| CR 제거 후 (참) | 6건 | 3건 |

원인은 `io.open(p, "w")` 의 기본 개행 변환이다. 파일 전체가 CRLF 가 되며 CR 이 **238개**
박혔다.

⇒ **발행본을 스크립트로 고쳤으면 검사기보다 `tr -cd '\r' | wc -c` 를 먼저 돌려라.**
읽기·쓰기 양쪽에 `newline=""` 를 준다. `Edit` 도구에는 이 문제가 없다.
재현은 [`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md) **41번**.

🔴 **이 함정의 일반형을 기억하라.** 「고쳤으니 줄었다」와 「검사기가 대상을 잃었다」는 출력이
같다. **임계값을 낮췄는데도 0 이면 검사기 쪽을 의심한다.**

### 3. 고립 검사는 **이미 처리됐다** — 커밋 20 에서 되돌아와야 한다

B 편1 커밋에 카테고리 간 링크를 나감 2건·들어옴 2건으로 넣어 `npm test` 가 한 번에
54/54 를 냈다. 다만 **들어오는 링크가 임시로 지도편을 가리킨다.**

| 링크 | 지금 | 커밋 20 에서 |
| --- | --- | --- |
| `agent-definition-by-department` → | B-1 지도 | **B-4** 기획·관리 |
| `thirty-agent-catalog` → | B-1 지도 | **B-3** 목표 수립 |

⇒ **§6-3 표의 4번·5번은 「미착수」가 아니라 「임시 대상으로 걸림」이다.** 다시 걸지 말고
대상만 바꾼다.

### 4. 지도편은 **네 덩어리**로 갈린다 — 다음 배치의 지도편 예산에 쓸 것

| 덩어리 | A 편1 | B 편1 | 크기를 정하는 것 |
| --- | ---: | ---: | --- |
| 고정 비용 | 3,834 | 3,859 | 형식 — 원본과 무관 |
| 용어절 | 5,367 | 6,092 | 용어 개수 — 둘 다 36개 |
| 예고절 | 3,465 | 3,586 | 형제편 수 — A 8편 · B 7편 |
| 본문 몫 | 10,681 | 11,224 | **원본** |
| **합계** | **23,347** | **24,761** | |

**원본 배율이 걸리는 곳은 마지막 행 하나뿐이다.** 지도편 목표를 원본 배율로 잡지 마라.

⚠️ 용어당 B 는 A 149.1 → B **169.2** 로 올랐지만 **추세로 읽지 마라** — §10-1-a 가 단조
상승을 이미 반증했다. 이번 편은 용어가 세 도메인에 걸쳐 뜻 열에 맥락이 한 겹 더 들어갔다.

### 5. 다음 배치가 참조할 상수 — 시리즈 A 에서 확정된 것

| 값 | 쓰는 자리 |
| --- | --- |
| Q&A편 **항목당 513 B** | 다음 배치의 Q&A편 예산 (§3-4 의 471 을 대체) |
| 표 배율 **1.478** | 「산문을 표로 바꾸면 늘어난다」에만. 🔴 **편9(원본 표 83.1%)를 표본에 넣지 마라** |
| 코드 계수 | 편6 **1.144**(주석만 한글로) 대 편8 **1.065**(원형 보존). 🔴 **평균 1.105 를 쓰지 말고 원형 보존 비중으로 나눠라** |
| 지도편 도식 배율 | **원본과 같은 크기**로 잡는다. 규칙 5 의 1.263 은 본편에만 (§10-1-d) |

---

## 시리즈 B 사양 요약

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (**편1 발행으로 열렸다**) |
| 원본 | `pm-po/01` · `02` · `04` — 배정 **58,636 B** |
| 편수 | 8편 (편1 지도 ✅ + 편2~8) |
| 예산 | k = **2.05** · 밴드 1.91 ~ 2.19 (§3-2) |
| frontmatter | `series: "product-management-domains"` · `seriesOrder` 1~8 (§7-4) |
| 🔴 낱말 | 「기획」은 A 와 뜻이 다르다. **B 편1이 이미 갈라 뒀다** — 편2~8은 그 용법을 따른다 |
| 🔴 편7 | 법과 시행령을 섞지 마라. 법령만 한 표의 **두 열**로 겹친다 (§3-2-b) |
| 🔴 편8 | 확정 제도와 논의 중인 쟁점을 같은 층으로 쓰지 마라 (§7-2) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  대상 편이 없어 지금은 걸 수 없다(§6-2). **B-3·B-4 발행 때 되돌아와 건다** — `A-6` 은 이미
  발행됐으므로 **파일을 다시 열어 고쳐야 한다.**
- **B 편1의 형제편 링크 7개가 비어 있다.** 제목만 두고 링크를 벗겼다. 편2~8 발행 시 **그 행에
  하나씩 붙이고**, 커밋 19-a 에서 전수 확인한다. 링크 몫 합계는 **404 B** 다.
- 🔴 **밴드와 하한은 여전히 기계 검사 밖에 있다.** `npm test` 는 하한 미달을 `stderr` 경고로만
  내고 상한은 검사기가 없다. **「테스트 초록」을 분량 근거로 쓰지 마라** — `compose` 출력과
  `[SOFT]` 목록을 눈으로 읽는다. 현재 하한 미달 **5편**은 전부 이번 배치 밖이다.
- **지도편이 목표를 17.9% 넘겼다.** A 편1도 11.2% 넘겼다. 지도편에는 밴드가 없어 반려선이
  작동하지 않는다 — **다음 배치는 위 4번의 덩어리 분해로 목표를 잡아라.**
- **`user-research` · `career` 태그가 싱글톤으로 남았다.** **글에 없는 내용으로 태그를 맞추지
  마라**(§11-8).
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 이번 세션에 편7 절을 `docs/changelog/2026-09.md` 로
  옮겨 아카이브가 **3절**이 됐다. 넘으면 여기로 옮기고 상단 인덱스 표를 갱신한다.
- ✅ **`check-counts` 를 발행 전 검사에 넣은 뒤로 배포 실패가 없다.** 편을 발행했으면 README
  **세 자리**와 CHANGELOG **최신 절**, 네 자리를 같은 커밋에 넣는다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **개행이 바뀌면 검사기가 0 을 낸다** | 파이썬 치환이 파일을 CRLF 로 바꿔 `source-overlap` 이 11건 → **0건**(거짓)을 냈다 | **CR 을 먼저 센다.** 위 2번 · TOOL-TRAPS 41번 |
| 🔴 **분류 표가 조용히 어긋나 있었다** | TOOL-TRAPS 분류 표에 **39·40 이 빠져** 있었다. 항목만 추가하고 표를 안 고친 결과다 | 항목을 넣을 때 **분류 표와 `CLAUDE.md` 개수를 같이** 고친다 |
| 🔴 **지도편끼리 정형 문구가 축자로 겹친다** | `dup-scan` 6건이 **전부** A 편1 과의 겹침이었다 — 「지도를 그릴 때는…」 같은 상투구다 | 지도편을 쓸 때는 **앞 지도편을 열어 두고 문장을 달리 짠다** |
| **축소 렌더의 도식 라벨 잘림은 별건이다** | 확대 보기에서는 온전했다. 편7 발행본도 같다 | **개수는 `grep`, 가독은 브라우저.** 확대 보기까지 확인하면 통과 |
| **`compose` 와 `wc -c` 의 1 B 오프셋** | 여섯 편에서 재현됐다 | 상수 오프셋이다. **1 B 차이를 「불일치」로 오독하지 마라** |
| **`cd` 뒤의 상대 경로가 깨진다** | 셸 도구의 작업 디렉터리가 리셋된 뒤 상대 경로가 죽었다 | 셸 도구에는 **절대 경로**를 쓰거나 `Grep` 도구를 쓴다 |
| **파이썬 stdout 의 한글이 깨져도 성공이다** | `6곳 교체 완료` 가 깨져 나왔지만 파일은 정상이었다 | 출력이 아니라 **파일을 다시 재서** 판정한다 (TOOL-TRAPS 40번) |
