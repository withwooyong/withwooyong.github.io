# HANDOFF

> 마지막 갱신 2026-08-31 · `pm` 배치 **설계 완료 · 집필 0편**

## 지금 상태

발행본은 **167편 그대로**다. 이번 세션은 한 편도 쓰지 않았다 — 범위를 정하고 설계서를 썼다.

| 항목 | 값 |
| --- | --- |
| 확정 범위 | **B 안(선별 이관)** · 선별 축 **기획 하네스 중심** |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (955줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 · B `product-management` 8 |
| 발행 후 총량 | 167 + 17 = **184편** |
| 다음 단계 | 설계서 **절차 ③** — 시리즈 A 편1(지도편) 집필 |

**설계서 §1 의 절차표가 정본이다.** ①②는 완료, ③부터 미착수다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | 이관 범위 A/B/C 중 **B 안** 확정 | — |
| 2 | 선별 축 3안 중 **기획 하네스 중심** 확정 | — |
| 3 | 원본 4개 성분 실측 (`compose:verify` 9/9 뒤) | 설계서 §2 |
| 4 | 🔴 **원본 금칙어 직접 실측 → 초안 뒤집기** | 설계서 §2-4-a · §2-4-b |
| 5 | 편 구성·예산·태그·링크·위험·검증 설계 | 설계서 §3~§9 |
| 6 | 설계서 자기 검산 (오류 2건 정정) | 설계서 §8-0 |
| 7 | CHANGELOG · 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 원본 경로가 어느 문서에도 없었다

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

이 세션은 경로를 몰라 `java-backend`·`langgraph` 같은 **다른 폴더명으로 역추적**해야 했다.
설계서 §1-1 에도 적었다. **다음 배치가 다른 원본군을 쓸 때도 루트부터 확인하라.**

### 2. 성분 실측이 통과해도 배정 가능량은 모른다

이번 배치가 찾은 가장 큰 것이다. `compose` 로 절별 성분을 재고 편 구성과 예산까지 확정한 뒤에
**원본에 금칙어가 실재하는지 세어 보니** `pm-po` 세 원본의 **14.6%**(10,065 B)가 발행 불가
자료였다 — `> 💼 **면접 활용**` 형태의 개인 경력 메모 20개이고, 금칙어 12건이 전부 그 안에 있다.

⇒ **다음 배치는 §2(성분 실측) 다음 단계로 「배정 원본 금칙어 실측」을 넣어라.** 명령은
설계서 §2-4-b 에 코드 블록으로 박아 뒀다. `LC_ALL=C` 가 빠지면 조용히 0을 낸다.

### 3. 처방 ③ 이 설계서 자신에게도 들었다

「「N개」 목록은 표의 각 행과 1:1 로 짚어라. 합계가 맞는지 보지 마라」를 설계서에 적용해
**빼는 조각 합계 8,946(실제 7,661)** 과 **B-3 목표 1 B 오차**를 잡았다. 앞 배치까지는 집필본에
쓰던 처방인데 **설계 단계에서 먼저 들었다.** 처방 세 줄에 이어 계속 물린다.

### 4. 이번 배치가 추가한 넷째 처방

> **원본이 다른 표로 나눈 것을 한 표의 행으로 섞지 마라. 열로는 합쳐도 된다.**

규칙 3(행을 겹쳐 총량을 줄여라)과 규칙 13(층을 합치면 사실이 뒤집힌다)이 부딪히는 자리를
설계서 §7-2 가 미리 지목했다 — 전자금융법과 시행령, 확정 제도와 논의 중인 쟁점, 정가·판매가·할인가.

---

## 다음 세션 착수 순서

| # | 할 일 | 설계서 |
| ---: | --- | --- |
| 1 | 설계서 §0 · §1 · §11 을 먼저 읽는다 | — |
| 2 | §6-3 접합부 표를 **grep 으로 채운다** (지금 비어 있다) | §6-3 |
| 3 | 시리즈 A 편1(`planning-harness-map`) 집필 | §3-1 · §4-1 |
| 4 | 초고 직후 `compose` 실행 → 고정 비용·용어당 B 확정 | §8-2 · 규칙 14 |
| 5 | §8-1 여덟 검사 통과 후 커밋 | §9-1 커밋 2 |
| 6 | 그 실측으로 A 편2~9 재환산 | §3-6 ① |

🔴 **§6-3 표가 비어 있는 것은 의도한 것이다.** 규칙 10 이 「제목만 보고 접합부를 정하지 말고
grep 으로 낱말 실재를 확인하고 행 번호를 적어라」고 했으므로, 착수 시점에 채우도록 남겼다.

---

## 알려진 문제

- **밴드 하단이 하한에 붙은 편이 셋 있다.** B-3(−113 B) · A-5(+195) · A-7(+305).
  초고가 **13,300 B 를 밑돌면 반려**한다. 인접 편에 합치지 말고 인용을 표로 펴서 채운다.
  🔴 **경력 메모로 채우는 것은 금지다.**
- **재환산 지점이 셋이다**(설계서 §3-6). A 편1 뒤 · A 편2 뒤 · **B 편1 뒤**.
  마지막 것이 이번 배치에만 있다 — 고정 비용이 시리즈 경계에서 리셋되는지 아무도 모른다.
  **B 편1 초고 `compose` 를 보기 전에 B 편2 를 쓰지 마라.**
- **`user-research` · `career` 태그가 싱글톤으로 남는다.** 글에 없는 내용으로 태그를 맞추지 마라.
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관하다.
- **`grep -r` 로 리포를 훑으면 `out/`·`node_modules` 까지 스캔해 120초 타임아웃이 난다.**
  이번 세션에서도 재현됐다. `Grep` 도구나 `--include` / 명시 경로를 써라.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| **로케일이 이모지를 숨긴다** | 같은 `^> 💼` 패턴이 `LC_ALL=C` 유무에 따라 **9와 0**으로 갈렸다 | 이모지·한글 패턴에는 `LC_ALL=C` 를 반드시 붙인다 |
| **awk `length()` 는 문자를 센다** | 절별 블록이 3,903 으로 나왔는데 `wc -c` 로는 **10,065** 였다 | 바이트가 필요하면 `wc -c` 로 재고, 교차 검증한다 |
| **`python3` 가 없다** | 검산 스크립트가 exit 49 로 죽었다 | `node` 를 쓴다 |
