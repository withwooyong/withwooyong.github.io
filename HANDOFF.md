# 인수인계 (HANDOFF)

**갱신일**: 2026-08-07
**브랜치**: `main` (origin과 동기, 배포 완료 `d543dac`)
**작업트리**: 깨끗함 / stash 없음

---

## 1. 이번 세션에서 한 일

**기술블로그를 신설하고 31편을 발행했다.** 별도 저장소(`C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge`)의 학습 정리 md 143편이 소스이고, **편집·검수를 거친 발행본만** 이 저장소에 들어온다.

```
https://withwooyong.github.io/blog/
  ├─ 검색 엔지니어링     6편   (1차)
  └─ RAG · 검색증강생성  25편   (2차 첫 카테고리)

sitemap 83 URL · 시리즈 7개 · 태그 44종 · 도식 103개
```

### 왜 이 구조인가 — 원본을 직접 읽지 않는다

원본을 빌드 때 직접 읽으면 **민감정보 필터를 코드로 보장**해야 하고, 필터에 구멍이 나면 그대로 사고다. 발행본을 분리하면 **검수를 통과한 것만 저장소에 존재**하므로 빌드는 필터를 가질 필요조차 없다. 안전장치를 코드가 아니라 구조로 옮긴 것이다.

---

## 2. 다음 세션이 이어받을 것

### 최우선: 2차 나머지 10개 카테고리 (103편)

```
143편 중  발행 완료 31편 · 발행 제외 9편 · 남음 103편
```

**시작 전에 반드시 읽을 것**: [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)

| 절 | 내용 |
| --- | --- |
| **§13** | 2차 확정 사양 — 카테고리 12개, 태그 81종, 발행 제외 9편, 파이프라인 함정 4건 |
| **§14** | `rag` 실행에서 확정된 규칙 — **여기가 가장 중요하다** |

### 다음 카테고리 권고: `ai-agent` (16편)

`rag/07~10`(2026 에이전트 아키텍처 담론) + `langgraph` 12편. 이유:

1. **저자 콘텐츠 중 가장 최신·가장 CTO급**이다
2. `rag`의 `rag-knowledge-map.md`가 이미 *"에이전트 아키텍처 쪽에서 따로 다룬다"*고 언급하고 있어 연결이 자연스럽다 (**단 링크는 아직 안 걸어뒀다** — 그 카테고리가 생기면 추가할 것)
3. **Q&A 이관 3건이 대기 중**이다 (아래)

#### `ai-agent`로 넘겨야 할 Q&A 3건

`rag` 변환자가 행 번호까지 기록해뒀다. 원본 `rag/11-면접-커닝페이퍼.md` §2:

| 원본 위치 | 질문 | 근거 문서 |
| --- | --- | --- |
| `11` §2-1 (72~78행) | AI 에이전트가 개발조직 구조를 어떻게 바꾸는가 | `rag/09` §5, `rag/10` §5 |
| `11` §2-2 (80~86행) | 팀 생산성을 AI로 어떻게 끌어올리는가 | `rag/09` §5, `rag/06` §9 |
| `11` §2-5 (104~112행) | 최근 관심 있게 본 기술 — Agent Harness → Loop Engineering | `rag/10` §4, `rag/00` §4-1 |

인벤토리 전문은 스크래치패드에 있었으나 **세션 종료로 사라진다.** 위 표가 남은 전부다.

### 진행 방식 — `rag`에서 검증된 절차

```
1단계  분할 설계만 하고 보고 → 승인          ← 설계가 틀리면 25편을 다시 만든다
2단계  4배치로 나눠 변환, 배치마다 커밋·보고
3단계  구현 미참여 에이전트가 독립 검증
4단계  지적 반영 → 병합 → 푸시
```

**배치를 나누는 이유**: 한 번에 20편 이상 쓰면 뒤로 갈수록 톤이 흔들린다. 첫 배치가 나머지의 기준이 되므로 완료 후 검토하고 넘어간다.

---

## 3. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. 도식의 **52%**에서 발생("Arxiv Search"가 "Searcl"로). **`rag` 결함이 아니다** — 1차 발행본과 프로덕션에서 동일 재현된다. 전역 렌더링 이슈로 별건 처리 필요 |
| 2 | **FR-1.4 스크롤스파이 미구현** | "목차를 자동 생성하고 **스크롤에 따라 현재 위치를 표시**한다" 중 후자가 없다. 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다. 위키와 함께 처리 권장 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 성능 77점. 히어로 진입 애니메이션(`hero-fade-up`)이 하이드레이션에 의존해 텍스트가 그때까지 `opacity:0`이다. **블로그 도입 이전에도 77점**이었다(베이스라인 대조 확인). 기존 연출이라 별건 |
| 4 | **싱글톤 태그 13종** | §13-3 목표는 "2차 신규 싱글톤 0"이었다. 대부분 후속 카테고리에서 회수될 성격(`mcp`→`agentic-coding`, `saas`→`product-management`)이라 **2차 전체를 마친 뒤 재측정**할 것 |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 아직 변환하지 않았다. Q1(재무지표 10개 × 개발조직 관점)은 **거의 그대로 발행 가능** — "면접 표현 예시"를 "실무 서술 예시"로 바꾸는 정도. 개발총괄 블로그의 드문 차별점이다 |

---

## 4. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

### 4-1. README에 블로그가 전혀 언급되지 않는다

README는 2026-05-02 이후 손대지 않았고 그 사이 **121커밋**이 쌓였다. 이번 세션에 블로그가 통째로 추가됐는데 README에는 한 줄도 없다.

| 낡은 곳 | 진실원 |
| --- | --- |
| 프로젝트 구조 트리에 `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택에 Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명에 블로그 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

**이번에 고친 것은 스크립트 표에 `npm test` 행 추가 하나뿐이다** ([`package.json`](package.json)의 `scripts.test` 확인 후).

### 4-2. ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

핸드오프 수집 스크립트가 이렇게 경고한다:

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/rag/*.md`의 코드 예제 안에 있는 것**이다. 이 사이트가 쓰는 환경변수가 아니라 RAG를 설명하는 **블로그 글 본문의 예시 코드**다.

여기서 `.env.example`을 만들거나 README에 환경변수 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

**검사 도구가 콘텐츠 md를 소스 코드로 오인한 것이다.** 블로그를 추가하며 새로 생긴 오탐 유형이고, 앞으로 카테고리가 늘수록 더 많은 키가 잡힐 것이다.

이 사이트가 실제로 쓰는 환경변수는 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

---

## 5. 알아둘 함정 — 다시 밟지 않도록

`rag` 25편을 만들며 실제로 겪은 것들이다. 상세 근거는 [요구사항 §14](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)에 있다.

### 5-1. "깨진 것이 없다"와 "있어야 할 것이 있다"는 다른 검사다

발견된 결함 대부분이 **"틀린 것"이 아니라 "없는 것"**이었고, **자동 검사를 전부 통과**했다.

| 결함 | 왜 안 잡혔나 |
| --- | --- |
| 도식 1개 누락 | 남은 4개가 멀쩡해 글이 자연스러웠다 |
| Q&A 링크 0개 (나가는·들어오는 양쪽) | **없는 링크는 깨지지도 않는다** |
| 지표 2건 소실 | 표가 문법적으로 정상이다 |
| 시리즈 prev/next 오류 | **빌드는 내부 링크를 검증하지 않는다** |

→ **개수 대조**(원본 97개 vs 발행본 95개)와 **존재 확인**("이 글에 링크가 있어야 하는데 없다")으로만 잡힌다.

### 5-2. 도식·표를 지울 때는 항목 수를 센다

`rag`에서 **같은 실수를 두 번** 했다. 두 번째는 첫 교훈을 알고도 반복한 것이다.

> mermaid를 "중복"이라 판단해 지웠는데 노드 9개 중 2개(`Precision@k`·`p95 지연`)가 표에 없었다. **"같은 3계층"이라는 인상으로 판단**했고, 세어 봤다면 바로 나왔다.

### 5-3. 산출물 비교의 거짓 양성 2종

`Compare-Object`로 빌드 산출물을 비교할 때:

| 원인 | 증상 | 대응 |
| --- | --- | --- |
| **Next.js buildId** (21자 nanoid) | 변경이 없어도 전 파일이 "차이 있음" | `/_next/static/[A-Za-z0-9_-]{21}/`를 정규화 |
| **Tailwind CSS 해시** | 새 유틸리티가 하나만 생겨도 전 페이지 `<link>`가 바뀜 | 룰 diff에서 **제거 0개, 순증만**이면 회귀 아님 |

그리고 **Tailwind는 주석 안의 문자열도 클래스로 스캔한다.** 코드에서 `divide-y`를 지웠는데 주석에 남은 `divide-y` 때문에 CSS 룰이 계속 생성됐다. **주석에 클래스명을 적지 마라.**

### 5-4. Vitest는 타입을 검사하지 않는다

테스트 전원 통과 상태에서 `tsc`가 오류를 잡은 사례가 실제로 있었다(`target: "es5"`에서 `[...map.entries()]`가 TS2802). **`npx tsc --noEmit`을 별도 검증 항목으로 돌릴 것.**

### 5-5. 측정 조건이 정상을 결함으로 만든다

| 사례 | 잘못된 결론 | 원인 |
| --- | --- | --- |
| Lighthouse 성능 61점 | "NFR 대폭 미달" | 압축 없는 서버로 측정. gzip 서버에서 92점 |
| 다크모드 대비 1.11:1 | "글자가 안 보인다" | 백그라운드 탭에서 IntersectionObserver 미발화 → `opacity:0` 상태 측정 |

### 5-6. 공유 작업트리에서 브랜치를 바꾸면 다른 에이전트가 깨진다

병렬 작업 중 한쪽이 `main` 기반 브랜치로 전환하면 **다른 쪽이 만든 파일이 디스크에서 사라진다.** `git worktree`로 격리할 것.

---

## 6. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 83개 URL

# 발행본 금칙어 — 기대 0건
Select-String -Path "content\blog\**\*.md" -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서"

# 민감정보·학습흔적 — 기대 0건 (rag에서 확장한 목록)
Select-String -Path "content\blog\rag\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|강의|teddylee|teddynote|argus"
```

> **알려진 오탐**: 민감정보 검사에 `공고`를 넣으면 `dify-workflow-patterns.md`의 "입찰 **공고**문"이 걸린다. 원본 워크플로우의 실제 용도(RFP)이므로 검사에서 제외했다.

---

## 7. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록 |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종. **이 목록 밖이면 빌드 실패** |
