# HANDOFF

> 마지막 갱신 2026-09-17 · 발행본 **193편** / 8개 카테고리 (`npm run check-counts:print` 실측)
>
> 이 파일은 **다음 세션이 바로 알아야 할 것만** 둔다. 🔴 **세션마다 덮어쓴다 — 절을 더하거나
> 세션 기록을 쌓지 않는다.** 네 절 · 150줄 상한을 `npm run check-handoff` 가 커밋과 CI 에서 판정한다. 2,798줄이던 이전 판은
> `git show 78be3790:HANDOFF.md` 로 볼 수 있다. 세션별 상세 기록은 거기와 `git log` 에 있다.

## 지금 상태

| 항목 | 상태 |
| --- | --- |
| 작업 흐름 | 1인 개발. PR 없이 `main` 에 직접 커밋·푸시하고 배포 run 의 성공만 확인한다 (`CLAUDE.md` 「작업 흐름」) |
| 브랜치 | 로컬·원격 모두 `main` 하나 |
| 도식 | 발행본 도식은 대부분 Mermaid 이고, `diagram-design` 형식의 SVG 는 `agentic-coding` 세 편에 9개(구조도 1 · 플로우차트 6 · 상태 1 · 시퀀스 1)다. **17번 넓히기는 2026-09-17 사용자 결정으로 중단했다** — 남은 Mermaid 는 그대로 둔다 |
| 검사기 · 테스트 수 | 문서에서 읽지 말고 **센다.** 케이스 수는 `npx vitest run` 요약 줄, 뮤턴트 수는 `npm run mutate` 출력에서 읽는다 |

이 표는 상태를 사실만 적고, 바뀌기 쉬운 수치는 세는 방법으로 대신한다.

## 남은 작업

| 번호 | 항목 | 사용자 결정 | 상태 |
| :---: | --- | :---: | --- |
| 14 | SVG 도식의 **다크 모드 해법** — 테마의 진실원이 `localStorage` 라 `prefers-color-scheme` 해법은 어긋난다. 후보는 두 벌 자산 · 인라인 SVG 둘 | 필요 | ⏸️ 보류. 사용자가 고정 색 SVG 를 라이트·다크 모두 「인상적」이라고 판정해 시급성이 낮아졌다. SVG 를 다시 늘릴 때 먼저 정한다 |
| 7 | `diagram-design` 후보 A · C | 받았다 | ⏸️ 사용자 판단으로 보류 (2026-09-06). 다시 꺼내려면 이전 판의 「후보 A 를 하지 않기로 한 근거」를 먼저 반박한다 |
| — | 도식 색 실물 대조 확장 — 브라우저로 잰 도식은 일부뿐이다 | 불필요 | 남은 종류(`gantt` · `journey` · `erDiagram` · `quadrantChart` · `mindmap`)가 문서에만 있어 우선순위가 낮다 |
| — | `reactStrictMode` 를 켤지 | 약간 | 켠 상태에서 콘솔 오류는 없었으나 다른 컴포넌트의 이중 마운트까지 확인하지 않았다 |

지금 급한 작업은 없고, 네 항목 모두 사용자 판단이나 새 계기가 있을 때 꺼낸다.

## 다시 읽을 것

### 다음 배치를 시작하면

새 발행 배치(원본 저장소 `C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/`)에서 되풀이될 교훈이다.

- **`compose` 는 본문을 마지막으로 만진 뒤 돌린다.** 리뷰 반영도 분량을 늘리고, 표 예산이 총량보다 먼저 터진다.
- **금칙어 때문에 뺀 블록은 낱말만 빼면 끝나지 않는다.** 그 블록의 판단이 본문으로 새지 않도록 「이 블록에서 온 판단은 쓰지 않는다」를 명제 단위로 적는다.
- **`dup-scan` 0건은 원본 대조의 0건이 아니다.** 원본과의 겹침은 `source-overlap` 이 본다.
- **예산 역산 k 는 2.05 에서 출발한다.** `pm-practice` 여섯 편이 2.03 ~ 2.08 에 모였다.
- **발행할 때 자기 파일만 고치지 않는다.** 지도편의 자기 행과 앞 편의 마지막 예고 문장에 링크를 건다.

### `/handoff` 수집기의 README 신호는 오탐이다

수집기는 판정을 기억하지 못해 매번 같은 신호를 낸다. 아래 넷은 검증을 마친 오탐이므로 작업으로 올리지 않는다.

| 신호 | 오탐인 이유 |
| --- | --- |
| `NPM_SCRIPTS_MISSING_IN_README` | `:verify` 는 README 에 전부 있다. 수집기가 표의 독립 행만 세고 명령 설명 안의 병기를 보지 못한다 |
| `ENV_KEYS_IN_CODE_BUT_NOT_IN_README` | 블로그 본문의 코드 예시에서 온 것이다. 정적 export 라 런타임 환경변수를 쓸 자리가 없다 |
| `SCRIPTS_DIR_MISSING_IN_README` | README 트리는 확장자 없이 적어 수집기의 파일명 매칭과 어긋날 뿐, 전량 나열되어 있다 |
| `UNRESOLVED_PATH_REFS` (`engines.node`) | 파일 경로가 아니라 `package.json` 의 필드 이름이다 |

오탐 판정은 이어받아도 되지만, 근거로 쓰는 수치는 매번 다시 센다.

## 도구 함정

전체 목록과 재현 절차는 [`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md) 에 있고, 요약은 `CLAUDE.md` 「이 환경의 도구 함정」 표에 있다. 도구가 설명되지 않는 결과를 낼 때 읽는다.
