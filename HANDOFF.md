# HANDOFF

> 마지막 갱신 2026-09-01 · `pm` 배치 **시리즈 A 6/9 발행 · 시리즈 B 0/8**

## 지금 상태

발행본 **173편**이다. `ai-product-planning` 카테고리에 여섯 편이 있고 편7부터 이어 쓴다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 1,900줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 · B `product-management` 8 |
| 발행 완료 | A 편1~6 (173편) |
| 다음 단계 | 설계서 **§10-7** — A 편7 `operate-stage` 집필 |
| 미푸시 | 이번 세션의 편6 커밋 — 사용자 승인 뒤에만 올린다 |

**설계서 §1 의 절차표가 정본이다.** 다만 절차표의 상태 칸은 갱신되지 않았으므로
**진행률은 `§10-N` 의 실측 절 번호로 판단하라** — §10-6 까지 있으면 편6까지 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | A 편6 `deliver-and-prd` 집필·발행 (20,447 B) | `content/blog/ai-product-planning/deliver-and-prd.md` |
| 2 | 편5 말미에 편6 링크 연결 | `architect-stage.md` |
| 3 | 편6 실측 + **편7 산문 예산 역산** | 설계서 **§10-6** · **§10-6-f** |
| 4 | CHANGELOG 편6 절 + 가장 오래된 절을 월별 아카이브로 이동 | `CHANGELOG.md` · `docs/changelog/2026-08.md` (34 → 35절) |
| 5 | 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 편7 산문 예산을 이미 역산해 뒀다 — 초고 전에 이 값을 지켜라

편4가 물리고 편5·편6에서 **연속 두 번 작동한** 처방이다. 편7 목표는 **14,511 B**,
밴드 **13,605 ~ 15,420** 이다.

| 성분 | 원본 §7 | 배율 | 발행 예산 |
| --- | ---: | ---: | ---: |
| 표 | 4,272 | 1.400 | **5,981** |
| 도식 | 649 | 1.05 | **681** |
| 코드 | 0 | — | 0 |
| frontmatter | — | — | 950 |
| **산문** | 338 | — | **6,899** |

표 배율은 세 실측(1.511 · 1.603 · **1.400**) 가운데 **하한**을 썼다 — §3-2-b 가 편7을
「지표표 둘을 한 표로 겹친다」로 계획했으므로 배율이 낮은 쪽에 붙는다. 도식은 네 표본 모두
1.263 을 밑돌아 1.05 로 내렸다.

⚠️ 원본 §7 의 **인용 9.6%(623 B)** 는 위 표에 칸이 없다. §7-3 이 「원본 인용은 발행본 산문으로
푼다」로 정했으므로 그 몫은 산문 6,899 안에서 소화된다. **인용을 따로 잡아 총량에 더하지 마라.**

### 2. 🔴 편7은 **하한 반려 대상**이다

`MIN_BYTES` 는 **13,300 B**(SOFT 게이트)이고 편7 밴드 하단은 13,605 로 여유가 **305 B** 뿐이다.
초고가 13,300 을 밑돌면 반려한다(§11-5).

| 하면 안 되는 것 | 해야 하는 것 |
| --- | --- |
| 인접 편에 합치기 | 원본 인용을 **표로 펴서** 채운다 |
| 🔴 **경력 메모로 채우기** — §11-3 이 금지한다 | 원본 §7 의 지표 정의를 행으로 벌린다 |

### 3. 복제 제거는 **+200 B** 로 미리 잡아 둬라

편5 +232 · 편6 +183 으로 두 편 연속 같은 방향이다. 원본과 겹치지 않게 고쳐 쓴 문장은
원래보다 길어진다. ⇒ **초고 직후의 값으로 밴드를 판정하지 마라. 복제 제거 → `compose`
재실행 → 필요하면 삭감 순이다.** 편3의 리뷰 반영이 +292 B 였으므로 상단 여유는
**500 B 이상**을 남겨야 리뷰를 받을 수 있다.

### 4. 편6이 편7을 예고한 낱말을 `grep` 으로 확인하라

편4가 물린 규칙이다(§10-4-f). 편6 말미의 예고는 이렇다.

> 다음 편은 만든 것을 내보낸 뒤의 이야기로 넘어간다. 무엇을 측정할지 **하나만 고르는 일**,
> 그리고 그 수치를 다시 **처음 단계의 입력으로 되돌리는 고리**가 거기서 갈린다.

지도편이 편7에 배정한 용어는 `단 하나의 지표`(OMTM) · `가드레일 지표` · `중앙값과 꼬리값`
(p50/p90) · `암묵지`(편8과 공유) · `사후 회고` · `사람 개입 지점`(편5와 공유) 이다.
예고의 「하나만 고르는 일」이 OMTM 에, 「되돌리는 고리」가 사후 회고에 대응한다.

### 5. 편7 발행 시 편6에 다음 편 링크를 단다

이 시리즈의 관례다 — 발행 시점에는 링크 없이 예고만 하고, 다음 편이 나올 때 링크를 건다.
편3 · 편4 · 편5가 모두 그렇게 처리됐다. 편6 마지막 문단의 「다음 편은」을
`[다음 편](/blog/ai-product-planning/operate-stage/)` 으로 바꾼다.

---

## 편7 사양 요약

| 항목 | 값 |
| --- | --- |
| 슬러그 | `operate-stage` |
| 원본 절 | `pm-harness/03` **§7** · 6,493 B |
| 목표 · 밴드 | **14,511 B** · 13,605 ~ 15,420 (🔴 13,300 미만이면 반려) |
| 태그 | `product-metrics` `data-driven` |
| 링크 | 앞 편 **`deliver-and-prd`** · 지도편. 다음 편 링크는 편8 발행 때 붙인다 |
| 주제 경계 | 운영과 순환 · 지표. 🔴 **팀 자산화는 편8이다** |
| 표 처리 | 🔴 **지표표 둘을 한 표로 겹친다**(§3-2-b) — 다만 층이 다르면 **열로만** 겹쳐라(§7-2) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **재환산 지점이 하나 남았다** — **B 편1 뒤**다(설계서 §3-6). 고정 비용이 시리즈 경계에서
  리셋되는지 아무도 모른다. **B 편1 초고 `compose` 를 보기 전에 B 편2 를 쓰지 마라.**
- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  시리즈 B 가 없어 지금은 걸 수 없다(§6-2). **B 시리즈 발행 때 되돌아와 건다** — `A-6` 은
  이미 발행됐으므로 **편6 파일을 다시 열어 고쳐야 한다.**
- **코드 배율의 표본이 아직 하나뿐이다(1.144).** 편8(원본 §9 · 코드 1,738 B)이 두 번째다.
  1.144 를 적용하면 발행 코드 1,988 B 로 편8 예산 16,605 의 12.0% 를 차지한다.
- **`user-research` · `career` 태그가 싱글톤으로 남는다.** 글에 없는 내용으로 태그를 맞추지 마라.
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관하다.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 넘으면 `docs/changelog/YYYY-MM.md` 로 옮기고 상단
  인덱스 표를 갱신한다. 이번 세션에 1개 절을 옮겨 `2026-08` 이 34 → **35절**이 됐다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **`cd` 가 리셋되지 않고 유지된다** | 낱말을 세려고 `cd content/blog/...` 를 한 뒤 `ls out/blog/...` 가 「없다」를 냈다. **같은 상황에서 `npm run build` 는 성공하고 `check-forbidden:built` 도 502파일 HARD 0 을 냈다** — npm 과 스캐너는 상위 탐색을 하고 `ls` 만 상대 경로였다. 「산출물이 없는데 0이 나왔다」로 오독할 뻔했다 | 리포 파일은 **절대 경로**로 열거나 `cd` 를 같은 명령에 붙인다. §10-5-d 는 반대 방향을 적었지만 처방은 같다 |
| **코드는 원본 대조에서 0건이 안 된다** | `sys.exit(2)` · `file=sys.stderr` 가 33자 겹쳤다. 훅의 요지가 그 관용구 자체라 달리 쓸 수 없다 | **정당 축자로 판정하고 기록한다.** 억지로 비틀면 동작하지 않는 코드가 발행된다 |
| **표 배율이 편마다 흔들린다** | 1.511 → 1.603 → **1.400**. 편6은 인수인계서가 채택한 1.511 보다 낮았다 | 역산에는 **최근 두 편의 평균**을 쓰되, 표를 겹치는 편은 **하한**을 쓴다 |
