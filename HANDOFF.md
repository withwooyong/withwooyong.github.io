# Session Handoff

> 갱신: 2026-08-29 (KST)
> 브랜치: `main` (푸시하지 않음 — origin 은 `514fdbe` 에 머물러 있다)
> 최신 커밋: `6488b18` docs(plan): 편2 실측을 §10-4 에 넣는다 — 불릿 비율 2.04, 편4·편5는 밴드를 다시 긋는다

## 현재 상태

원본 `java-backend/08-스프링부트-인증-쿠버네티스.md`(49,201 B 배정분)를 **6편**으로 나누는 배치가
진행 중이다. **편1·편2 발행 완료**, 절차 ④(편2 실측으로 편3·4·5 재환산)까지 끝났다.
다음은 **편3 집필**이고, 목표 바이트가 설계 초안 17,695 에서 **16,750** 으로 내려갔다.

## 이번 세션에 한 일

| # | 작업 | 커밋 | 산출물 |
| ---: | --- | --- | --- |
| 1 | 편2 집필·검증·발행 | `bfd8546` | `content/blog/backend-engineering/auth-domain-and-requirements.md` (21,264 B) · 편1 링크 수정 · README · CHANGELOG |
| 2 | 편2 실측을 설계서 §10-4 에 반영하고 편3·4·5 재환산 | `6488b18` | `docs/superpowers/plans/2026-08-29-auth-k8s-split-design.md` |

편2 검증 결과: `check-forbidden` **HARD 0**, `dup-scan` 편2 **0건**, 원본과의 축자 겹침 **20자 이상 0건**
(mermaid 문법 골격 제외), `npm test` **54개 통과**, `tsc --noEmit` 통과, `check-counts` 통과.

## 다음 할 일

| # | 작업 | 상태 | 메모 |
| ---: | --- | --- | --- |
| 1 | **편3 집필** (`requirements-spec-to-erd`) | 다음 차례 | 원본 §5·§6. 목표 **16,750 B**, 밴드 14,740 ~ 18,760 |
| 2 | 편2 → 편3 링크(55 B) 채우기 | 편3 발행 커밋에 포함 | 편2가 현재 밴드 상단에서 **56 B** 여유뿐이다. 링크를 넣으면 21,319 로 상단(21,320)에 1 B 남는다 |
| 3 | 편4 집필 (`session-vs-token-authentication`) | 대기 | 목표 **14,080 B**. 🔴 밴드 하단이 `MIN_BYTES` 13,300 을 910 밑돈다 — 브리프에 반려선을 적어라 |
| 4 | 편5 집필 (`kubernetes-deployment-and-rollout`) | 대기 | 목표 **16,810 B**. 불릿 3,499 를 2.04 로 푼 값이다 |
| 5 | 편6(Q&A) 집필 + **편1~5 링크 수정 5건** | 대기 | 규칙 12. 19,313 B · 42항목 |
| 6 | 타 카테고리 인바운드 링크 3건 | 대기 | §6-3 — `agentic-coding` 2편 · `ai-transformation` 1편 |
| 7 | 배치 마감 (§10-0 가설표) | 대기 | C-1 · C-2 · C-3 · C-4 · C-7 · C-8 |

## 이번 세션의 판단과 근거

- **편2를 밴드 상단(21,264)에 두고 목표 19,960 을 +6.5% 넘긴 채로 발행했다.** 성분을 재 보니
  초과의 원인이 표(+709) · 도식(+284) · frontmatter(+780)였고 **본문 산문은 오히려 계획보다 469 B
  적었다.** 더 깎으려면 표나 도식을 들어내야 하는데 §8-2 가 그것을 금한다. 밴드 안이므로 그대로 갔다.
- **편3~편5의 k 를 1.79 에서 1.53 ~ 1.70 으로 내렸다.** 편2가 준 불릿 비율이 **2.04** 로,
  §3-5-a 가 편5에 요구했던 2.97 과 크게 다르다. §3-5-b 의 지시대로 **글을 늘리지 않고 k 를 고쳤다.**
- **`compose` 분해식에 frontmatter 항이 없다는 것을 발견했다**(780 B). 다음 배치는 고정 비용 상수를
  3,300 에서 **3,100** 으로 내리고 frontmatter 를 별도 항으로 세워야 한다 — §10-4-b.
- 편1의 시리즈 구성 표에서 편2 로 링크를 걸어 **링크 몫 공식(슬러그 + 31)을 실측 검증**했다.
  순증 59 B 로 계산값과 정확히 일치한다. 남은 네 편의 링크 몫은 추정이 아니라 계산값이다.

## 알려진 문제

- **편2의 밴드 여유가 56 B 뿐이다.** 편3 발행 때 넣을 편2 → 편3 링크가 55 B 이므로 상단까지 1 B 만
  남는다. 그 커밋에서 편2 본문을 조금이라도 늘리면 밴드를 벗어난다.
- **`grep -r` 로 리포를 훑으면 `out/` 과 `node_modules` 까지 스캔해 120초 타임아웃이 난다.**
  이번 세션에서 실제로 재현됐다(`CLAUDE.md` 의 도구 함정 표 마지막 행). `Grep` 도구나
  `--include` / 명시 경로를 써라.
- `check-forbidden` SOFT 2건은 `agentic-coding/thirty-agent-catalog.md:195` 의 「면접」·「이력서」로,
  채용 에이전트를 설명하는 정당한 문맥이다. 이번 배치와 무관하다.

## README/문서 갱신 필요

수집 스크립트의 HIGH 신호 두 갈래는 **오탐이라 판정하고 고치지 않았다.** 근거를 남긴다.

| 신호 | 판정 | 근거 |
| --- | --- | --- |
| `ENV_KEYS_IN_CODE_BUT_NOT_IN_README` (`OPENAI_API_KEY` 등 5개) | **오탐** | 다섯 키 모두 `content/blog/` 의 **본문 코드 예제** 문자열이다(RAG·에이전트 글 5편). 이 사이트는 `output: "export"` 정적 export 라 런타임 환경변수 자체가 없다 |
| `NPM_SCRIPTS_MISSING_IN_README` (`check-counts:verify` · `check-counts:print` · `compose:verify`) | **오탐** | README 스크립트 표 87 · 88행이 `check-counts` 와 `compose` 행 안에서 `:verify` · `:print` 변종을 이미 설명한다. 검사기가 정확한 스크립트명 문자열만 찾아 생긴 신호다 |

`SCRIPTS_DIR_MISSING_IN_README` 는 실제 drift 여서 이번에 고쳤다 — README 디렉터리 트리의
`scripts/` 주석에 `check-baseline.mjs` · `check-counts.mjs` · `compose.mjs` 를 추가했다(미커밋).

## 다음 세션을 위한 맥락

사용자의 목표는 사내 지식 문서(`yanadoo-exit/shared/knowledge/`)를 기술 블로그 발행본으로 옮기는
배치 작업이고, 이 리포의 주된 작업 방식이다. **핵심은 분량을 감으로 정하지 않는 것**이다 —
설계서가 원본을 성분(산문·표·코드·도식·불릿·인용)으로 분해해 편별 예산을 계산하고, 편마다 실측해
남은 편의 예산을 재환산한다. 그 계보가 `docs/superpowers/plans/*-split-design.md` 에 쌓여 있다.

**편3을 시작할 때 읽을 것**

| 순서 | 대상 | 왜 |
| ---: | --- | --- |
| 1 | 설계서 **§10-4** | 편2 실측과 재환산 결과. 편3 목표 16,750 이 여기서 나왔다 |
| 2 | 설계서 **§3-2-b** · **§4-1** | 편3의 표 합칠 계획(§5 매핑표와 §6-2 엔티티 식별을 하나로)과 주제 경계 |
| 3 | **편1 도입과 편2 맺음말** | 규칙 11. 축자 중복이 실제로 여기서 두 번 나왔다 |
| 4 | 설계서 **§10-4-e** | 편2가 남긴 집필 관찰 — 아래 한 줄이 특히 중요하다 |

🔴 **초고를 쓴 직후 `npm run compose` 를 먼저 돌려라.** 이번 세션은 그러지 않고 산문부터 깎았고,
네 차례에 걸쳐 5,900 B 를 깎고도 밴드에 들지 못했다. 성분을 재고 나서야 깎을 곳이 산문이 아니라는
것이 드러났다.

발행 절차는 설계서 §8-1 의 일곱 검사를 순서대로 돌리고 §9-1 의 커밋 단위를 지킨다.
커밋은 `git commit -m "…" -- <경로>` 형태인데, **새 파일은 `git add` 를 먼저 해야 한다** —
경로를 명시해도 추적되지 않은 파일은 pathspec 에러로 죽는다(이번 세션에서 재현).

**푸시하지 않았다.** `main` 이 프로덕션이므로 사용자가 명시적으로 요청할 때만 푸시한다.

## 이번 세션에 바뀐 파일

```
CHANGELOG.md                                                  |  55 +
README.md                                                     |   8 +-
content/blog/backend-engineering/auth-domain-and-requirements.md | 201 +++++ (신규)
content/blog/backend-engineering/auth-service-launch-map.md    |   2 +-
docs/superpowers/plans/2026-08-29-auth-k8s-split-design.md     | 117 +++
HANDOFF.md                                                     | (이 파일)
```
