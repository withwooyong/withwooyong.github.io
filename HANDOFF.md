# HANDOFF

> 마지막 갱신 2026-09-02 · `pm` 배치 **시리즈 A 9/9 완결 · 시리즈 B 4/8 (편4 리뷰 반영 완료)**

## 지금 상태

발행본 **180편**이다. 이번 세션은 새 편을 쓰지 않았다. **편4에 리뷰 두 축을 뒤늦게 붙였고**,
지적이 본문 · 설계서 · 검사기 **세 층**으로 갈렸다. 편4가 세운 결론 하나가 측정 착오였음이
드러나 §10-12 의 취소를 다시 철회했고, 검사기 세 건을 고쳐 뮤테이션으로 증명했다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 2,970줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 ✅ · B `product-management` 8 |
| 발행 완료 | **A 편1~9 + B 편1~편4 (180편)** |
| 다음 단계 | **B 편5 `commerce-member-and-catalog`** — 원본 `pm-po/02` §0·§1·§2·§3 · 7,830 B · 설계 목표 **18,565 B** · 밴드 17,470 ~ 19,660 |
| 🔴 편5 목표 | 설계값이 아니라 **17,900 안팎**으로 잡아라 — 상단 여유 600(리뷰 몫) + **링크 몫 66**(55 가 아니다) + 도메인 편의 표 팽창분 |
| 푸시 · 배포 | 편4 발행까지 `origin/main` 반영됨. **이번 세션 변경은 미커밋** |
| ✅ 끝난 것 | **편4 리뷰 두 축** — 정확성 2건 · 검사 유효성 3건 반영 완료 (§10-13-h) |
| 🔴 남은 것 | 원본 대조·복제 검사기의 **훅·CI 편입**(§10-12-g-2) · CRLF 발행본 32편의 **과거 분량 재측정** |

**설계서 §1 의 절차표가 정본이다.** 진행률은 **`§10-N` 의 실측 절 번호**로 판단하라 —
§10-13 까지 있으면 B 편4 가 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | **편4 리뷰 두 축** — 정확성 2건(MAJOR) + 3건(MINOR) · 검사 유효성 3건(MAJOR) + 3건(MINOR) | 편4 본문 · 설계서 **§10-13-h** |
| 2 | 🔴 정규화를 **공용 모듈로 통합** — `dup-scan` 과 `source-overlap` 의 손복사본을 없앴다 | **`scripts/lib/normalize.mjs`**(신규) |
| 3 | 🔴 `source-overlap` 결함 2건 수정 (21/21 → **26/26**) — 화살표 오분류 · 손복사본 대조 | `scripts/source-overlap.mjs` |
| 4 | 🔴 `compose` 가 **CRLF 의 CR 을 흘리던 것** 수정 (9/9 → **10/10**) | `scripts/compose.mjs` |
| 5 | 뮤테이션 **5건**으로 수정이 실제로 무는지 증명 (M4~M8) | 설계서 §10-13-h |
| 6 | 편4 본문 수정 — 예약어 · 되짚기 · 절차 분할 · 원어 · 셀 값 다섯 | `planning-and-product-ops.md` (**19,008 → 19,352**) |
| 7 | 설계서 §10-13 정정 — 원본 B · 고정 비용 · 표 B · 여유 · 링크 몫 | 설계서 §10-13 · §10-13-b~d · **§10-13-h**(신규) |
| 8 | §10-12 의 **「수렴하지 않는다」 취소를 다시 철회** | 설계서 §10-12 (취소선 위에 철회) |
| 9 | CHANGELOG 새 절 · 편4 절 수치 정정 · 편2 절 아카이브 이동(**7절**) | `CHANGELOG.md` · `docs/changelog/2026-09.md` |
| 10 | 이 문서 | (다음 커밋) |


---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 0. 🔴 검사기 세 건이 **또** 「전부 통과」 뒤에서 새고 있었다 — 두 건은 편3 때와 같은 모양

| 검사기 | 무엇이 새고 있었나 | 실측 |
| --- | --- | --- |
| `source-overlap` | 문단에 `-->` **다섯 글자**만 있으면 그 문단이 통째로 도식 버킷으로 갔다 | 축자 복사 **56자 → 0건** |
| `source-overlap` | ⑫-c 가 진짜 `dup-scan` 이 아니라 **같은 파일 안의 손복사본**과 비교했다 | 정규화를 어긋내도 **21/21** |
| `compose` | **CRLF 파일의 CR 을 한 바이트도 세지 않았다** | LF 19,007 · CRLF 19,202 → **둘 다 19,008** |

셋 다 고쳤다. 정규화는 `scripts/lib/normalize.mjs` 하나로 모아 **드리프트가 생길 자리를 없앴고**,
도식 판정은 「화살표를 담았는가」에서 **「도식 선언으로 시작하는가」**로 바꿨으며, `compose` 는
`\n` 으로만 쪼개 CR 을 줄에 남긴다. 자기 검사는 **21 → 26** · **9 → 10** 이다.

🔴 **`dup-scan` 자신의 자기 검사 7건은 드리프트를 넣어도 그대로 통과했다**(뮤테이션 M5).
자기 검사는 자기가 무엇을 놓치는지 말해 주지 못한다 — **갈라진 것을 잡는 검사는 갈라진 두 쪽
밖에 있어야 한다.** 다음에 검사기를 고치면 이 자리를 먼저 물어라.

| 무엇이 | 몇 편 |
| --- | ---: |
| CRLF 발행본 (`compose` 가 CR 을 흘리던 대상) | **32편** |
| `source-overlap` 의 문단 경계가 달라진 편 | **33편** (32편이 CRLF) |
| 펜스 밖 화살표를 담은 산문 문단 | **0편** — 화살표 결함은 **잠복**이었다 |

⇒ 편1~편4 는 CR=0 이라 이 배치 수치는 무사하다. 그러나 **CRLF 편의 과거 분량 판정은 다시 재기
전에는 결론이 아니다.**

### 0-b. 🔴 `source-overlap` 이 **CRLF 파일에서 아무것도 보지 못하고 있었다** — 179편 중 32편 (앞 세션)

리뷰 검사 유효성 축이 잡았다. `toParagraphs` 의 프론트매터 정규식과 문단 분할이 **LF 전용**
이라, CRLF 파일은 `\r\n\r\n` 이 `\n{2,}` 에 걸리지 않아 **파일 전체가 한 덩어리**가 되고,
그 덩어리에 `-->` 가 하나라도 있으면 통째로 도식 버킷으로 가 **산문 문단이 0개**가 된다.

| 같은 내용, 줄바꿈만 다름 | 문단 | ① | ② |
| --- | ---: | ---: | ---: |
| LF | 54개 | 2건 (최장 42자) | 1건 (33자) |
| **CRLF** | **0개** | **0건** | **0건** |

**42자 축자 복사가 통째로 사라진다.** 자기 검사 13개가 전부 LF 리터럴이라 **13/13 PASS 가
줄바꿈에 대해 아무 말도 하지 않았다.** `dup-scan` 은 같은 자리에서 `\r?\n` 을 써 정상
동작했다 — **두 검사기가 갈렸는데 라벨은 「같은 정규화」라고 적혀 있었다.**

고쳤다(§10-12-g-1). 줄바꿈을 `\r?\n` 으로, ③의 라벨 대조에 ②와 같은 정규화를 태우고, ③
출력에 발행본 라벨 수를 더했다. **15/15 → 21/21** 이며 **뮤테이션으로 증명했다** — 줄바꿈을
되돌리면 18/21, 라벨 정규화를 되돌리면 19/21 이다.

⚠️ **이번 편은 CR=0 이라 판정이 바뀌지 않았다.** 그러나 **CRLF 인 32편의 과거 「0건」은
결론이 아니라 미검출**이었을 수 있다. 그 편들을 다시 볼 일이 생기면 이 사실을 먼저 떠올려라.

### 0-c. ✅ 「여유 600 B」가 편4에서 작동했다 — 그리고 **미부착 링크는 그와 별개다**

| 편 | 커밋 시점 여유 | 리뷰가 요구한 증가 | 결과 |
| --- | ---: | ---: | --- |
| 편2 | — | **+243** | 산문을 −212 깎아 맞췄다 |
| 편3 | 380 | **+551** | 🔴 상한 **+171 초과** → 산문을 되돌려 14,742 |
| **편4** | **1,242** | **+344** | 여유가 넉넉해 **한 번에 담았다.** 최종 19,352 · 밴드 안 |

⇒ **여유는 밴드 상단에서 최소 600 B 를 유지한다.**

🔴 **그런데 편3에서 다른 방향으로 터졌다.** 미뤄 둔 「다음 편」 링크를 편4 발행 때 붙이자
편3이 **14,742 → 14,796** 으로 상한을 **44 B 넘겼다.** 링크 몫 55 B 는 편3 예산에 이미 들어
있었지만 **파일에는 자리가 없었다.**

⇒ 🔴 **예산에 넣어 둔 것과 파일에 남겨 둔 자리는 다르다.** 다음 편 링크를 나중에 붙일 편은
**여유 600(리뷰 몫) + 링크 몫**을 함께 남겨라(§10-13-d). 편4도 편5 링크가 미부착이므로 실질
여유는 1,242 가 아니라 **841** 이다(리뷰 반영 +344 · 링크 몫 실측 57).

### 1. 🔴 반려선이 가리킨 방향과 **실제로 어긋난 방향이 반대였다**

§3-2-a 는 편3을 「밴드 하단이 하한을 113 B 밑도니 살이 모자라면 반려」로 관리했는데, 초고는
**17,945 B** 로 상한을 **21.6%** 넘겼다. 배치 최대 이탈이다.

| 지정한 위험 | 실제 위험 |
| --- | --- |
| 원본이 가장 작으니 하한에 걸릴 것이다 | 배율이 가장 크게 튀어 상한을 넘겼다 |

**예고는 예산표 안에 있었다** — §3-2 겉보기 비율에서 편3이 **2.499** 로 배치 최대였다.
⇒ **밴드 하단이 하한을 밑도는 편에도 상한 점검을 그대로 하라.** 반려선은 다른 쪽을 면제해
주지 않는다.

### 2. 🔴 「면접 활용」 블록에서 온 문장은 **검사기에 걸리지 않는다**

§11-3 이 금지한 배정 밖 블록의 내용이 초고에 둘 들어와 있었고, **`source-overlap` 은 원본
전체를 대조하면서도 0건을 냈다.**

걸릴 수가 없다 — **「익명화해서 살린다」는 정의상 낱말을 바꾼다는 뜻이고, 축자 대조는 낱말이
같을 때만 반응한다.** 검사기의 0건은 「옮기지 않았다」가 아니라 **「그대로 옮기지는 않았다」**
를 뜻한다.

⇒ **발행 전에 배정 밖 블록의 주제 목록을 손에 들고 본문을 훑어라.** 기계가 못 하는 일이므로
`PUBLISHING-CHECKLIST.md` 쪽 항목이다(§10-12-c).

### 3. 🔴 초고가 10% 넘게 넘쳤으면 **문단이 아니라 절 목록을 먼저 보라**

무엇을 손댔느냐로 회차당 감축이 한 자릿수 배씩 갈린다. 편4에서 층이 하나 더 드러났다.

| 무엇을 손대나 | 회차당 감축 | 무엇이 빠지나 |
| --- | ---: | --- |
| 문장 다듬기 | 100~230 | 수식어와 군더더기 |
| 문단·표 셀·항목 제거 | 343~687 | 중복 서술 한 덩어리 |
| 🔴 **절 경계 옮기기** | **2,643** | **도입 문장 한 벌 + 절 사이 이음말 + 제목 줄** |

편4 초고는 23,060(상한 +13.9%)이었는데 **절 14개를 12개로 합친 한 회차가 2,643 B 를 냈다.**
이어지는 두 절을 합치면 그 절이 자기를 소개하던 문장 전체가 사라진다(§10-13-a).

### 4. 🔴 「고정 비용이 수렴하지 않는다」는 **측정 착오였다** — 취소를 다시 철회했다

편4 실측이 고정 비용을 **3,346** 으로 적고 그것을 근거로 편3의 결론을 취소했는데, 그 3,346 이
**편2·편3 과 다른 것을 세고 있었다.**

| 편 | 고정 비용 | 라벨 | 상수 2,455 대비 | 두 k 의 차 |
| :---: | ---: | --- | ---: | ---: |
| 2 | 3,013 | frontmatter + 도입 + 정리 | +558 | 0.083 |
| 3 | 2,479 | frontmatter + 도입 + 정리 | +24 | **0.004** |
| **4 (옛)** | ~~3,346~~ | 🔴 **라벨 없음** — `(머리말)` + **용어 절** + 정리 | ~~+891~~ | ~~0.109~~ |
| **4 (정정)** | **2,286** | frontmatter + 도입 + 정리 | **−169** | **0.021** |

`compose` 의 `(머리말)` 행은 frontmatter 와 도입을 **이미 합친 값**이다(§10-2-b). 편4만 거기에
「이 편이 쓴 용어」 절을 하나 더 얹었다. 같은 정의로 재면 3,013 → 2,479 → **2,286** 의 **단조
감소**이며, 정의 B(용어 절 포함)로 통일해도 두 k 의 차가 0.194 → 0.138 → 0.110 으로 역시
단조 축소다. ⇒ **어느 정의로 통일하든 「수렴하지 않는다」가 나오지 않는다.**

⇒ **취소를 철회했다(§10-13-h).** 다만 세기는 낮췄다 — 「상수가 정확하다」가 아니라 **「세 편에서
상수 기준 k 가 모두 밴드 안이므로 상수를 고칠 이유가 아직 없다」** 이다.

🔴 **여기서 가져갈 것은 수렴 여부가 아니다.** 합으로 정의된 지표는 **값 옆에 성분을 적지 않으면
다음 편이 다른 것을 센다.** 편2·편3 은 라벨을 적었고 편4는 빠뜨렸는데, 빠진 한 편이 두 편의
결론을 뒤집는 반례로 기록됐다.

### 5. 🔴 표 배율을 `원본 표 B × 배율` 로 예측하지 마라

| 편 | 원본 표 B | 발행 표 B | 배율 | §3-2-b 예상 | 겹치는가 |
| :---: | ---: | ---: | ---: | ---: | :---: |
| 3 | 2,647 | 4,150 | 1.568 | 1.30 | 아니오 |
| **4** | **1,811** | **4,343** | 🔴 **2.398** | **1.05** | 예 |

**겹치기로 계획해 예상을 가장 낮게 잡은 편에서 배율이 가장 높게 나왔다.** 원인은 겹침 여부가
아니라 **분모**다 — 원본이 순서·목록을 산문으로 적어 둘수록 그것을 표로 편 몫이 전부 이 배율에
얹힌다. ⇒ **편5~8 은 배정 절을 열어 「원본이 표로 쓰지 않은 순서·목록의 개수」를 먼저 센다**(§10-13-b).

### 6. 도식 — 편4는 **원본에 도식이 없는 첫 편**이었다

편2 1.435 · 편3 1.421 로 배율 표본이 둘 모였지만, 편4 배정 절에는 도식이 하나도 없어 배율을
낼 분모가 없었다. 처방 ②(구조는 유지하고 라벨은 다시 쓴다)도 **유지할 구조가 없으면 적용되지
않는다.** 원본이 산문으로 적어 둔 순서 둘을 새로 그렸고(583 B) **배율 표본에는 넣지 않았다.**

⇒ **편5~8 은 배정 시점에 원본 도식 수를 먼저 세라.** 0 이면 편당 평균으로 예산을 잡고, 그렇게
그린 도식은 배율 계산에서 뺀다(§10-13-e).

### 7. 🔴 지도편의 배정 열이 **두 편 연속 틀렸다**

| 편 | 오류 | 무엇이었나 |
| :---: | ---: | --- |
| 3 | 3건 | 지도편이 편3으로 약속한 용어 셋이 편3 배정 원본에 없었다 |
| 4 | 1건 | `SLA / SOP` 가 편4·편5 로 배정됐는데 **두 배정 원본 어디에도 없다** |

배정 열은 원본을 열어 확인하지 않고 주제만 보고 채워진 것으로 보인다. ⇒ **편5~8 착수 시
`LC_ALL=C grep -c "<용어>" <배정 원본>.md` 를 먼저 돌려라**(§10-13-f).

### 8. 남은 링크 작업

| 작업 | 언제 |
| --- | --- |
| ✅ **A-6 → B-4** (§6-2 마지막 1건) | 편4 발행 때 부착 완료. **§6-2 의 4건이 모두 찼다** |
| 지도편 구성 표 형제편 링크 **4개** (편5~8) | 편마다 그 행에 붙이고 커밋 19-a 에서 전수 확인 |
| 편4 말미 「다음 편」 링크 | **편5 발행 때 부착.** 🔴 편3에서 이것 때문에 상한을 넘겼다 — 편4 여유에서 **57 B 를 미리 뺀 841** 로 계산하라 |
| `agent-definition-by-department` → **B-4** · `thirty-agent-catalog` → **B-3** | 커밋 20. 지금은 B-1 지도로 **임시로 걸려 있다** — 다시 걸지 말고 대상만 바꾼다 |

---

## 시리즈 B 사양 요약

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (편1~편4 발행) |
| 원본 | `pm-po/01` · `02` · `04` — 배정 **58,636 B** |
| 편수 | 8편 (편1 지도 ✅ · 편2 ✅ · 편3 ✅ · 편4 ✅ · 편5~8) |
| 예산 | k = **2.05** · 밴드 1.91 ~ 2.19 (§3-2). **산식은 그대로 쓴다**(§10-13-c) |
| frontmatter | `series: "product-management-domains"` · `seriesOrder` 1~8 (§7-4) |
| 🔴 낱말 | 「기획」은 A 와 뜻이 다르다. 편1이 갈라 뒀고 편2~편4 가 그 용법을 따랐다 |
| 🔴 편5 | 원본 `02` §0~§3 · 7,830 B · 설계 목표 18,565 · 밴드 17,470 ~ 19,660. §3-2-b 는 「겹치지 않는다 ≈1.35」로 잡았으나 **편4에서 예상이 반대로 빗나갔다**(실측 2.398) — 배정 절의 산문 목록 수를 먼저 세라 |
| 🔴 링크 몫 | **슬러그마다 다르다.** §3-0-a 표는 자기 슬러그 기준인데 §3-2 가 그대로 썼다. 편5 **57** · 편6 **66**(예약분 57 이라 −9) |
| 🔴 편7 | 법과 시행령을 섞지 마라. 법령만 한 표의 **두 열**로 겹친다 (§3-2-b) |
| 🔴 편8 | 확정 제도와 논의 중인 쟁점을 같은 층으로 쓰지 마라 (§7-2) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- 🔴 **`dup-scan` · `source-overlap` · `compose` 가 훅에도 CI에도 없다.** (이번 세션에 셋 다 고쳤지만 편입은 그대로 남았다.) 대상 지정이 필요한
  검사(`--category`, 원본 경로)라 인자 결정 규칙부터 설계해야 하므로 **다음 배치의 안건**으로
  넘겼다(§10-12-g-2). 그때까지 이 세 검사는 **사람의 기억에 걸려 있다.**
- **`source-overlap` 은 축자 복사를 찾아도 종료 코드 0 을 낸다.** 고치지 않기로 했다 —
  용어 원어 열처럼 정당한 겹침이 늘 남으므로 「사람이 열어 판정하라」가 설계다.
- 🔴 **밴드와 하한은 여전히 기계 검사 밖에 있다.** `npm test` 는 하한 미달을 `stderr` 경고로만
  내고 상한은 검사기가 없다. **「테스트 초록」을 분량 근거로 쓰지 마라** — `compose` 출력과
  `[SOFT]` 목록을 눈으로 읽는다. 현재 하한 미달 **5편**은 전부 이번 배치 밖이다.
- **겹침 수정이 분량을 줄인 사례가 처음 나왔다**(−49 B). 앞 세 사례는 +22 · +73 · +232 였다.
  🔴 **그래도 여유는 「+232 를 담을 만큼」으로 잡는 규칙을 유지한다** — 줄어드는 쪽은 손해를
  내지 않는다(§10-12-e).
- 🔴 **표 배율이 설계 예상을 두 편 연속 넘었다** — 편3 **1.568**(예상 1.30) · 편4 **2.398**
  (예상 1.05). 위 5번을 보라. §3-2-b 의 예상값은 편5~8 에서도 참고만 하고 근거로 쓰지 마라.
- **표 헤더가 원본과 통째로 같아지는 자리가 있다.** 표를 만들 때 헤더도 대조 대상으로 본다
  (§10-11-f).
- **`user-research` · `career` 태그가 싱글톤으로 남았다.** **글에 없는 내용으로 태그를 맞추지
  마라**(§11-8).
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 이번 세션에 지도편 절을 `docs/changelog/2026-09.md`
  로 옮겨 아카이브가 **6절**이 됐다. 넘으면 여기로 옮기고 **상단 인덱스 표의 절 수와 날짜
  목록을 함께** 갱신한다 — `check-counts` 는 이 표를 보지 않는다.
- ✅ **`check-counts` 를 발행 전 검사에 넣은 뒤로 배포 실패가 없다.** 편을 발행했으면 README
  **세 자리**와 CHANGELOG **최신 절**, 네 자리를 같은 커밋에 넣는다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **큰 수치일수록 먼저 의심하라** | 검사기 수정의 효과를 「145,232자 복귀」로 쟀는데, CRLF 파일의 문단 문자열에 `` 이 남아 **내용이 같은 문단을 새 문단으로 센** 것이었다 | 문단을 문자열로 비교하기 전에 **공백을 지우고** 키를 만든다. 놀라운 수치가 나오면 한 편을 열어 실물을 본다 |
| 🔴 **자기 검사는 자기가 놓치는 축을 말하지 못한다** | `dup-scan` 의 정규화를 어긋내도 그 자신의 7건은 전부 통과했다. 잡은 것은 **바깥에 있던** `source-overlap` 뿐이다 | 두 모듈이 같아야 하는 관계는 **검사로 지키지 말고 정의를 하나로 합쳐** 갈라질 자리를 없앤다 |
| 🔴 **값 비교로는 드리프트를 못 잡는다** | ⑫-c 가 손복사본과 비교해 언제나 참이었다 | 구조를 본다 — 「공용 모듈을 가져다 쓰는가」·「손복사본이 되살아나지 않았는가」 |
| **`sed`·`node -e` 로 JS 정규식을 치환하지 못한다** | 셸을 거친 `text.split(/?
/)` 치환이 **아무것도 바꾸지 않고 성공으로 보고**했다(적용 여부를 찍어서 알았다) | 치환은 `Edit` 로 한다. 스크립트로 할 거면 **적용 여부를 반드시 출력**한다 |
| **뮤테이션 복구는 역치환이 아니라 사본 복사다** | 고친 판 스냅샷을 먼저 뜨고 M4~M8 마다 그 사본으로 되돌렸다. `diff -r` 무출력으로 매번 확인했다 | 조작 전에 **pristine 과 fixed 두 사본**을 뜬다. `git checkout --` 는 미커밋 변경을 지운다 |
| **`grep -c` 는 0건일 때 종료 코드 1을 낸다** | `grep -c` 뒤에 `&&` 로 이어 붙였다가 정상인데도 명령줄 전체가 실패로 끝났다 | 0건을 기대하는 `grep` 은 **단독 실행**하거나 `|| true` 를 붙인다 |
| **문단 경계 탐색이 표 구분선에 걸린다** | 표를 교체하려고 `indexOf("---")` 를 썼는데 표 헤더의 `---:` 에 먼저 걸려 **옛 표가 절반 남았다** | 마크다운에서 `---` 은 구분선·표 구분선·frontmatter 셋이다. 앞뒤 문자까지 포함해 찾는다 |
| **CHANGELOG 아카이브 이동은 세 곳을 함께 고친다** | 절 이동 · 인덱스 표의 **절 수** · 인덱스 표의 **날짜 목록** | `check-counts` 는 이 표를 보지 않는다. 손으로 확인한다 |
| **Bash 히어독이 두 번 연속 `unexpected EOF`** | 리뷰어가 보고서를 쓸 때 오류 위치가 21행 → 1행으로 옮겨 다녔다 | 서브에이전트에는 히어독 대신 **PowerShell 홑따옴표 here-string** 을 대안으로 함께 알려 준다 |
| **`compose` 와 `wc -c` 의 1 B 오프셋** | 여덟 편에서 재현됐고 CRLF 를 고친 뒤에도 그대로다 | 상수 오프셋이다. **1 B 차이를 「불일치」로 오독하지 마라** |
