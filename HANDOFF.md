# Session Handoff

> 갱신: 2026-08-30 (KST)
> 브랜치: `main` — 이 세션에서 **origin 으로 푸시했다** (사용자 승인: 「푸시까지」)
> 최신 커밋: 편5 발행(`debc143`)과 설계서 §10-7 갱신(`3c8fae1`)

## 현재 상태

원본 `java-backend/08-스프링부트-인증-쿠버네티스.md`(49,201 B 배정분)를 **6편**으로 나누는 배치가
진행 중이다. **편1~편5 발행 완료**, 절차 ⑦(편5 실측으로 편6 재환산)까지 끝났다.
남은 것은 **편6 하나**이고, 목표 19,313 · 밴드 18,354 ~ 20,244 를 **그대로 유지**한다 —
편6은 항목 패리티 축이라 성분 회귀와 교차하지 않기 때문이다.

## 이번 세션에 한 일

| # | 작업 | 커밋 | 산출물 |
| ---: | --- | --- | --- |
| 1 | 편5 집필·리뷰·검증·발행 | `debc143` | `content/blog/backend-engineering/kubernetes-deployment-and-rollout.md` (19,400 B) · 편4 링크 · README · CHANGELOG |
| 2 | 편5 실측을 설계서 §10-7 에 반영하고 편6 재환산 | `3c8fae1` | 하위 절 8개 · Mermaid 3개 |

`/ted-run` 파이프라인으로 돌렸다. 검증 결과: 일곱 검사 전부 통과 —
`check-forbidden:verify` **15/15**, `check-forbidden` **HARD 0 · SOFT 2**(기존, 무관 파일),
`dup-scan:verify` **7/7**, `dup-scan` 편5 **1건**(Dockerfile 관용구, 정당),
`npm test` **54개 통과**, `tsc --noEmit` 통과, 크기 **19,400 B**(밴드 안).
`check-counts` 는 README 3곳과 CHANGELOG 1곳을 잡아 고친 뒤 **166편 일치**.

**목표를 정확히 맞췄다.** 설계서 계산값 19,363 을 반올림한 목표 19,400 에 실측이 ±0 이다.

## 다음 할 일

| # | 작업 | 상태 | 메모 |
| ---: | --- | --- | --- |
| 1 | **편6 집필** (`auth-kubernetes-qna`) | 다음 차례 | 원본 §12·§13. 목표 **19,313 B**, 밴드 18,354 ~ 20,244. 42항목의 항목 패리티. 🔴 **`role` 키를 넣으면 빌드가 죽는다**(§7 위험 ⑧) |
| 2 | **편1~5 링크 수정 5건** | 편6 발행 커밋에 포함 | 규칙 12. 편6은 마지막 편이라 들어오는 링크가 없어 `links.test.ts` 를 고립 1편으로 떨어뜨린다. 앞 배치에서 두 번 재현됐다 |
| 3 | 타 카테고리 인바운드 링크 3건 | 대기 | §6-3 — `agentic-coding` 2편 · `ai-transformation` 1편 |
| 4 | 배치 마감 (§10-0 가설표) | 대기 | C-4 · C-7 · C-8 은 §10-7 에서 **확정됐다.** 남은 것은 C-1 · C-2 · C-3 |

⚠️ **편2의 밴드 여유가 2 B 뿐이다.** 편3 링크를 넣어 21,318 이 됐고 상단이 21,320 이다.
규칙 12 로 편2에 편6 링크(50 B)를 넣으면 **밴드를 벗어난다.** 넣기 전에 편2에서 뺄 자리를
먼저 찾아라 — 이것이 이 배치에서 아직 풀리지 않은 유일한 예산 문제다.

## 이번 세션의 판단과 근거

- **C-4 가 단조로 확정됐다.** 표 비율 네 표본이 **행 흡수 0.89 < 부분 1.22 < 열 추가 1.407 <
  미합치 1.436** 순으로 늘어선다. ⇒ 표 비율을 정하는 것은 성분이 아니라 **합치기의 축**이라는
  §10-6-b 의 재정의가 참이다. 나아가 **열 추가와 미합치의 차가 0.029 뿐**이라
  「열을 붙여 합치기」는 절감 수단이 못 된다는 것이 정량으로 확인됐다.
- **C-7 이 다섯 편 연속 0으로 확정됐다.** 편5 원본 본문의 **99.7%가 불릿**이라 본문 배율
  2.626 이 사실상 불릿 단독 배율이다. 그 값은 산문 부류(2.97)에 가깝고 명사구 부류(1.70)는
  기각된다. ⇒ 불릿을 독립 성분으로 세우는 분해식을 폐기한다.
- **C-8 고정 비용은 2,462 다.** 3,338 → 3,026 → 2,715 → 2,518 → **2,462** 로 차가
  −312 · −311 · −197 · **−56** 이라 수렴이 확인됐다. 극한을 2,300 에서 **2,440 ~ 2,470** 으로 정정한다.
- **네 점 직선은 단독으로 쓰지 마라.** `발행 본문 = 5,872 + 1.090 × 원본 본문`(r = 0.846)인데
  절편이 7,182 → 5,317 → 5,872, 기울기가 0.813 → 1.361 → 1.090 으로 **표본마다 진동한다.**
  세 점 직선의 예측 10,093 이 실측 9,213 으로 −880(−8.7%) 빗나갔다.
- 🔴 **도식 예산의 근거가 오염 표본이었다.** 1.09 의 근거였던 편4 1.14 는 **원본과 동일한
  도식 2개가 섞인 값**이다. 다시 쓴 도식만으로 잡으면 **1.26** 이고, 편5 실측 1.263 이 그것과 맞는다.
- **링크 몫 공식이 두 번째로 정확히 맞았다.** 편4 → 편5 링크가 §3-0-a 계산값 **64 B** 그대로였다
  (17,309 → 17,373).

## 🔴 리뷰가 잡은 것 — 검사기 일곱 개를 전부 통과한 뒤에 나왔다

두 축을 나눠 돌렸고 **발견 21건이 한 건도 겹치지 않았다.** 편4에 이어 두 번째 확인이다.

| 축 | CRITICAL | HIGH | MEDIUM | LOW |
| --- | ---: | ---: | ---: | ---: |
| 경계·정합성 | 0 | 2 | 8 | 4 |
| 원본 축자 겹침 | 0 | 1 | 3 | 3 |

🔴 **가장 위험했던 것은 경계 축 H2 다.** 「오른쪽 열을 관통하는 원칙 다섯」이 표 다섯 행과
한 칸씩 어긋났는데, 표 4행에 대응하는 원칙이 없고 원칙 넷째에 대응하는 표 행이 없어
**두 오류가 서로를 가려 합계만 다섯으로 맞았다.** 개수를 세는 검사로는 원리적으로 잡히지 않는다.
⇒ **편6 브리프에 「수를 밝힌 문장은 개수가 아니라 1:1 대응을 확인하라」를 넣어라.**

**편4의 두 결함은 재발하지 않았다.** 브리프에 대응 지시를 넣은 것이 그대로 작동했다.

| 편4의 결함 | 편5 브리프에 넣은 것 | 결과 |
| --- | --- | --- |
| 편 번호 오지목 (CRITICAL) | 「앞 편을 가리키면 그 편 파일을 열어 확인하라」 | **0건** |
| 도식 통째 복사 | 「구조는 유지하되 라벨은 다시 쓴다」 | **0건** · 연속 20자 겹침도 0 |
| 편1이 정한 낱말 미사용 | 「이 편이 반드시 써야 하는 낱말」 목록 | 프로브 12 · 상태 있음/없음 3 · 정책 데이터 3 |

⚠️ **채택한 두 처방이 서로 충돌했다.** L3 이 한정어 없는 「프로브」를 지웠는데 M8 이 다른 자리에서
하나를 새로 만들었다. 편1 용어표가 못 박은 낱말이라 컨트롤러가 「생존·준비 두 프로브의 설정」으로
채워 양쪽을 만족시켰다. ⇒ **편6은 처방 반영 뒤 필수 낱말을 다시 세라.** 반영자는 임의 수정이
금지돼 있어 충돌을 스스로 풀지 못한다.

⚠️ **코드 비율이 정확히 1.000 이다**(725 → 725). 축자 겹침 검사가 코드 블록을 건너뛰고 있을
가능성이 있다. 편6은 코드가 거의 없어 영향이 작지만, 다음 배치에서 확인하라.

## 알려진 문제

- **편2의 밴드 여유 2 B.** 위 「다음 할 일」 ⚠ 참조. 편6 커밋에서 터진다.
- **`dup-scan` 의 「N건」은 결론이 아니라 사람이 열어 볼 목록이다.** 편5의 1건은 Dockerfile
  표준 구문이었다. 종료 코드로 판정하지 마라 — 발견이 있어도 exit 0 이다.
- **매니페스트 22행이 원본과 100% 동일하다.** YAML 키 구조는 대체 표기가 없어 정당하다고 판정했다.
  `name: auth-api` · `image: registry/auth:1.2.0` 은 사내 식별 정보가 아닌 자리표시자다.
- **`grep -r` 로 리포를 훑으면 `out/` 과 `node_modules` 까지 스캔해 120초 타임아웃이 난다.**
- `check-forbidden` SOFT 2건은 `agentic-coding/thirty-agent-catalog.md:195` 의 「면접」·「이력서」로,
  채용 에이전트를 설명하는 정당한 문맥이다. 이번 배치와 무관하다.
- `npm test` 가 SOFT 로 보고하는 **분량 하한 미달 5편**은 이번 배치 이전부터 있던 것이다.

## 다음 세션을 위한 맥락

사용자의 목표는 사내 지식 문서(`yanadoo-exit/shared/knowledge/`)를 기술 블로그 발행본으로 옮기는
배치 작업이고, 이 리포의 주된 작업 방식이다. **핵심은 분량을 감으로 정하지 않는 것**이다 —
설계서가 원본을 성분(산문·표·코드·도식·불릿·인용)으로 분해해 편별 예산을 계산하고, 편마다 실측해
남은 편의 예산을 재환산한다. 그 계보가 `docs/superpowers/plans/*-split-design.md` 에 쌓여 있다.

**편6을 시작할 때 읽을 것**

| 순서 | 대상 | 왜 |
| ---: | --- | --- |
| 1 | 설계서 **§3-3** | 편6은 성분 회귀가 아니라 **항목 패리티**다. 계산 방식이 편2~5와 다르다 |
| 2 | 설계서 **§10-7-g** | 편6 재환산 근거. 고정 비용·헤딩은 항목당 B 에 **이미 포함**되므로 더하지 마라 |
| 3 | 설계서 **§10-7-h** | 편5가 남긴 결함과 처방 충돌. 셋은 편6에도 그대로 온다 |
| 4 | 설계서 **§4-1** · **§7 위험 ⑧** | 🔴 **42항목 밖의 절을 만들지 마라**(규칙 5) · `role` 키 금지 |
| 5 | 편1 **§0 용어표** · 편5 **맺음말** | 규칙 11 |

편5 맺음말이 편6에 넘긴 것은 「왜 그 방식을 골랐느냐는 물음이 인증 방식을 훑던 편과 로그인 상태를
저울질하던 편에 함께 걸리고, 로그인이 안 된다는 신고 하나의 원인은 검사 순서에도 생존·준비 두
프로브의 설정에도 있을 수 있다」이며, 편6을 **선택지 · 실패 모드 · 문답** 세 벌로 예고했다.
편1 191행과 이 예고가 일치하는지가 편6 도입의 첫 확인 항목이다.

발행 절차는 설계서 §8-1 의 일곱 검사를 순서대로 돌리고 §9-1 의 커밋 단위를 지킨다.
커밋은 `git commit -F- -- <경로>` 형태인데, **새 파일은 `git add` 를 먼저 해야 한다** —
경로를 명시해도 추적되지 않은 파일은 pathspec 에러로 죽는다.

**푸시 정책** — `main` 이 프로덕션(GitHub Pages)이므로 사용자가 명시적으로 요청할 때만 푸시한다.
이번 세션은 파이프라인 시작 시점에 「푸시까지」를 승인받았다.

## 이 배치 다음에 논의된 것 — 아직 착수하지 않았다

편5 착수 전에 `pm-po` · `pm-harness` 원본군을 먼저 하자는 논의가 있었고, **편5·편6을 먼저
끝내기로 정했다.** 그 뒤 순서는 아직 정해지지 않았다.

원본 코퍼스 전체는 **143개 문서 · 4,997 KB** 이고 카테고리는 `content/blog/categories.ts` 에
**12개가 등록**되어 있는데 **6개가 아직 0편**이다. 현재 진척은 문서 수 기준 약 47%,
분량 기준 약 51% 다.

| 빈 카테고리 | 대응 원본군 | 원본 |
| --- | --- | ---: |
| `high-traffic` | `high-traffic/` | 11개 · 508 KB |
| `platform-architecture` | `bigdata/` · `pangyo-it/` | 14개 · 638 KB |
| `python-ml-serving` | `recommendation/` · `llm-search-recommend/` | 8개 · 121 KB |
| **`product-management`** | **`pm-po/`** | **16개 · 318 KB** |
| **`ai-product-planning`** | **`pm-harness/`** | **7개 · 395 KB** |
| `glossary` | `glossary/` | 2개 · 24 KB |

`pm-po` + `pm-harness` 는 원본 23개 · 713 KB 이고, 최근 세 배치의 실측(원본 8,200 ~ 11,900 B 가
발행본 1편)으로 환산하면 **발행 60 ~ 85편 · 88 ~ 122시간** 규모다. 사용자에게 세 선택지를
제시했으나 아직 고르지 않았다 — **A** 전량 / **B** 선별 5~6개(15~20편) / **C** 카테고리 지도편
1편씩만 먼저(2편). 편6을 마친 뒤 이것부터 물어라.
