# HANDOFF

**최종 갱신: 2026-08-29 · 이 세션에서 저장소는 2026-08-19 상태로 롤백됐다.**

> ✅ **이 파일은 2026-08-29 에 커밋됐다.** 롤백 직후에는 미커밋 로컬 메모였으나,
> 「롤백의 무엇을·왜」를 담은 유일한 기록이 로컬에만 있는 상태가 위험하다고 판단해
> `docs/TOOL-TRAPS.md` 복원과 함께 `main` 에 커밋했다. **푸시하지 않았다.**

---

## 0. 지금 저장소가 어떤 상태인가 — 가장 먼저 읽을 것

| 항목 | 값 |
| --- | --- |
| `main` | `27ae83d` — `revert: 1a9b8873 시점으로 되돌린다` |
| 그 트리 | `1a9b8873` (2026-08-19) 과 **해시까지 완전 일치** (`c2a0eb2f…`) |
| 배포 | 완료. `https://withwooyong.github.io/` 가 8/19 모습이다 |
| 되돌린 규모 | 커밋 88개(머지 제외 85), 140파일, +8,856 / −24,980 |

**코드를 읽고 「아틀라스가 없네, 만들어야겠다」고 판단하지 마라.** 있었고, 배포됐고,
사용자 판단으로 되돌린 것이다. 이력에 전부 살아 있다.

```
main = 27ae83d
  └─ bbdf3a3  (PR #2 머지 — 재설계 단계 2)
       └─ … 88커밋: 아틀라스 · 검색 · 홈 재설계 · E2E · Lighthouse CI
```

---

## 1. 이번 세션에서 한 일

| 순서 | 한 일 | 결과 |
| --- | --- | --- |
| 1 | PR #2(`feat/redesign-stage2` → `main`) 병합 전 검증 | CI 게이트 12단계를 **로컬에서 재현** — 전부 통과 |
| 2 | PR #2 을 merge commit 으로 병합 | `bbdf3a3` · 배포 성공 · E2E 238건 통과 |
| 3 | 배포본 실물 확인 | 브라우저로 `/`·`/work`·`/about`·스텁 이동까지 확인 |
| 4 | **사용자가 롤백 결정** | `1a9b8873` 까지 revert |
| 5 | 롤백 실행·검증·배포 | `27ae83d` · 게이트 9단계 통과 · 404 세 개로 반영 확인 |

**세션의 순 결과는 롤백 하나다.** 1~3 은 되돌려졌다.

---

## 2. ⚠️ 되돌린 것을 다시 가져올 때 — 재료가 어디에 있나

| 수단 | 명령 | 쓰임 |
| --- | --- | --- |
| 전체 복원 | `git revert 27ae83d` | 88커밋을 통째로 되살린다 |
| 지점 열기 | `git checkout pre-rollback-2026-08-29` | 롤백 직전 상태. **origin 에도 푸시돼 있다** |
| 브랜치 | `feat/redesign-stage2` (로컬 + origin) | 재설계 단계 2 의 14커밋 |
| 작업 브랜치 | `revert/to-1a9b8873` (로컬) | 롤백 커밋을 만든 브랜치 |

**부분 복원이 필요하면 태그를 열어 파일 단위로 가져오는 편이 낫다.** 아틀라스와 검색은
서로 독립이지만 둘 다 `components/site-header.tsx`·`pages/index.tsx` 에 진입점을 갖고
있어서, 파일만 복사하면 진입점이 없어 죽은 코드가 된다.

---

## 3. ⚠️ 롤백으로 함께 사라진 것 — 다시 겪지 않으려면 알아야 할 것

✅ **`docs/TOOL-TRAPS.md` 는 2026-08-29 에 복원했다** (707 → 724줄, 경고 머리말 추가).
`CLAUDE.md` 함정 절에서 링크로 참조한다. 머리말에 **지금 저장소에 없는 코드를 지목하는
항목**(아틀라스 15회 · Pagefind 7회 · Playwright/E2E 9회)을 표로 표시해 뒀다 —
원리는 유효하되 재현 절차는 그대로 실행할 수 없다.

여전히 사라진 채로 남은 것: 설계서 2건, 작업계획서 3건, 배치 보고서 7건, 목업 6건.
필요하면 같은 방식으로 꺼낸다:

```
git show pre-rollback-2026-08-29:<경로> > <경로>
```

---

## 4. 검사 체계가 어떻게 달라졌나

| 항목 | 롤백 전 | 롤백 후 |
| --- | --- | --- |
| vitest | 21파일 / 430건 | **5파일 / 54건** |
| E2E | 238건 (Playwright) | **없음** — `@playwright/test` 가 의존성에 없다 |
| CI 단계 | 18 | **9** |
| Lighthouse | 경고 전용으로 도입됨 | **없음** |
| 검색 색인 | `npx pagefind --site out` 이 `build` 에 포함 | **없음** |

**`npm run e2e` 는 이제 존재하지 않는 명령이다.** `CLAUDE.md` 의 E2E 절도 8/19 판에는
없다. 검사 순서는 `package.json` 의 `scripts` 와 `.github/workflows/deploy.yml` 이 정본이다.

---

## 5. 결정된 것 — 2026-08-29

| # | 사안 | 결정 | 상태 |
| --- | --- | --- | --- |
| 1 | 롤백 범위 | **이대로 둔다.** 8/19 상태 유지. 복구 재료는 origin 에 안전하니 서두르지 않는다 | 확정 |
| 2 | `HANDOFF.md` 커밋 | **커밋한다** (푸시는 안 한다) | 완료 |
| 3 | `docs/TOOL-TRAPS.md` | **복원한다** — 경고 머리말 + `CLAUDE.md` 링크 | 완료 |

**복구 재료가 origin 에 있음을 실측했다** (`git ls-remote`): 태그 `pre-rollback-2026-08-29`
(→ `bbdf3a3`) 와 브랜치 `feat/redesign-stage2` (→ `dafb327`) 둘 다 원격에 있다.
이 PC 가 사라져도 88커밋은 되살릴 수 있다.

---

## 6. 블로그 진행률 — 2026-08-29 실측

**롤백은 콘텐츠를 건드리지 않았다.** 156편은 8/19 에도 156편이었다.

| 항목 | 값 |
| --- | --- |
| 발행본 | **156편** / 등록 12 카테고리 중 **6개만 발행** |
| 마지막 콘텐츠 커밋 | `9074877` (2026-08-19) — CI/CD 편7 인바운드 링크 |
| 원본 저장소 | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge` (143개 `.md`) |
| 실측 변환율 | **약 11.9 KB 원본 → 1편** (java-backend 381 KB → 32편) |

### 6-1. 활성 트랙 — `java-backend` → `backend-engineering`

원본 10개 중 **6개 변환 완료**(01·02·03·04·05·06), **4개 남음**:

| 원본 | 크기 | 예상 편수 |
| --- | ---: | ---: |
| `07-멀티모듈-Kafka-알림센터.md` | 40,861 B | ~4편 |
| `08-스프링부트-인증-쿠버네티스.md` | 50,917 B | ~4편 |
| `09-DDD-헥사고날-Kafka-주문결제.md` | 89,728 B | ~8편 |
| `10-면접-커닝페이퍼.md` | 66,018 B | 성격이 달라 별도 판단 필요 |

**다음 배치는 `07` 이다.** 직전 배치 설계서 `docs/superpowers/plans/2026-08-19-cicd-split-design.md`
§10 을 먼저 읽고 브리프를 쓰는 것이 확립된 절차다.

### 6-2. 미착수 트랙 — 등록만 되고 0편인 카테고리 6개

| 카테고리 | 원본 디렉터리 | 원본 | 분량 | 예상 편수 |
| --- | --- | ---: | ---: | ---: |
| `high-traffic` | `high-traffic/` | 11개 | 508 KB | ~43편 |
| `product-management` | `pm-po/` | 17개 | 327 KB | ~27편 |
| `ai-product-planning` | `pm-harness/` | 8개 | 402 KB | ~34편 |
| `glossary` | `glossary/` | 2개 | 24 KB | ~2편 |
| `platform-architecture` | 미정 | — | — | — |
| `python-ml-serving` | 미정 | — | — | — |

⚠️ **예상 편수는 java-backend 트랙 하나에서 잰 비율의 외삽이다.** 다른 원본은 도식·코드
비중이 달라 실제로는 달라진다 — 직전 배치 설계서가 「원본 성분으로 비율을 예측하려는
시도가 5개 표본에서 전부 실패했다」고 기록한 바로 그 함정이다. **규모 감각으로만 쓰고,
배치 계획은 편1 을 먼저 쓰고 실측으로 재환산한다.**

---

## 7. 미커밋 변경

없다. `HANDOFF.md`·`docs/TOOL-TRAPS.md`·`CLAUDE.md` 는 커밋했고 **푸시하지 않았다.**
푸시하면 `main` 배포가 돈다 — 문서만 바뀌었으므로 화면은 그대로다.
