# 인수인계 (HANDOFF)

**갱신일**: 2026-08-08
**브랜치**: `main`
**작업트리**: 이 문서 · `CHANGELOG.md`만 미커밋 / stash 없음
**발행본**: **87편** (`ai-agent` 51 · `rag` 25 · `agentic-coding` 5 · 기타 6) · sitemap **148 URL**

---

## 1. 이번 세션에서 한 일

**`agentic-coding` B1을 발행했다(편1~5).** 그리고 **설계서 진단 2건이 실측과 어긋난 것을 발견해 정정했다.**

```
변환 1기 ──→ 검증 3기 병렬 ──→ 지적 21건 ──→ 반영 1기 ──→ 커밋
                 │                              │
    ┌────────────┼────────────┐                 └─ 반박 0건
    │            │            │
 실행 검증   근거 주체 축   팽창 출처 축
 (sonnet)     (opus)        (opus)
```

| 단계 | 산출 |
| --- | ---: |
| 변환 5편 | 103.1KB · 도식 19 · 표 52 |
| 검증 3기 | 보고 3건 · 지적 **21건** |
| 반영 | 21건 전량 · 반박 **0건** |

### 결함이 한 곳에 몰렸다 — 이번 세션 최대 소득

| 영역 | 결함 |
| --- | ---: |
| **원본을 옮긴 부분** | **0건** |
| ├ 금지선 9 (`01` §1~§8 부활) | 0 (42어휘 전수 grep) |
| ├ 조건 이관 #1·#2 | 0 (4/4 · 6/6) |
| ├ ㉢ 부정 보존 | 3/3 — **원본보다 고지가 강해졌다** |
| └ 용어표 실재성 | 11/12행 원문 그대로 |
| **변환자가 새로 쓴 요약문** | **18건** |

**지시가 촘촘한 곳은 지켜지고 빈 곳에서 사고가 난다.** 브리프가 명시한 항목은 전부 통과했고, 지시가 없던 자유 영역인 요약문에서만 났다.

---

## 2. 다음 세션이 이어받을 것

### 최우선: B2 착수 (`04` → 편9~11)

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§6-1과 §10에 정정 블록이 들어갔다. 그것부터 읽어라.**

| 배치 | 대상 | 편 | 편수 | 상태 |
| ---: | --- | --- | ---: | --- |
| ~~B1~~ | ~~`01`(축약) + `02`~~ | ~~1~5~~ | ~~5~~ | ✅ **완료** (`388519e`) |
| **B2** | `04` | 9~11 | 3 | **다음** — §5-3 정합성 부채 2건 |
| B3 | `05` | 12~14 | 3 | §6-7 제품 결함 3건 · §6-8 Forge |
| B4 | `03` + `team-productivity` | 6~8, 26 | 4 | **`02`·`04`·`05` 뒤라야 전방 참조가 닫힌다** |
| B5 | `07` | 15~18 | 4 | §5-4 CLAUDE.md 60/200 · §6-6 개인 이력 |
| B6 | `08`(개발축) | 19~21 | 3 | 편21 자기참조 **미채택 확정**(아래) |
| B7 | `09` | 22~25 | 4 | §6-5 10번 표 캡션 승격 |
| B8 | Q&A 4편 | Q1~Q4 | 4 | 전 배치 완료 후 |
| B9 | 지식 지도 | — | 1 | **맨 마지막** |

### 확정된 결정 (다시 묻지 마라)

| 사안 | 결정 | 근거 |
| --- | --- | --- |
| **편21 자기참조** — `08 §9`에서 이 리포의 실제 CI·배포 관행을 근거로 쓸 것인가 | **쓰지 않는다** (사용자 승인 2026-08-08) | `deploy.yml`이 preview 없이 `main`=프로덕션인데 §9의 "자동 롤백 임계"와 대비되면 약점 공개가 된다 |
| **분량 배율** | **1.76× 수용** | 하한 13.3 위반 0 · 최대 24.7KB로 상한 29.4 이내 · 줄일 대상이 전부 설계서가 명시로 요구한 항목(표 요약·조건 이관·편간 링크) |

### B2 브리프에 반드시 넣을 것 — B1이 알려 준 것

기존 금지선 12건에 더해 **아래 4건을 추가하라.** 전부 B1 실측에서 나왔다.

| # | 추가할 지시 | 근거 |
| --- | --- | --- |
| 1 | **요약문에 수를 쓸 때는 같은 화면의 표를 세고 대조하라.** 「N가지」·「N행 중 M」을 쓰면 즉시 세어 확인한다 | B1 결함 18건 중 5건이 이 유형. `b654a5a`에서 이미 한 번 정정한 유형 |
| 2 | **원문의 강도를 올리지 마라.** 확률 주장은 확률로, 「대부분」은 「대부분」으로 | B1의 어긋남이 **전부 세게 말하는 방향**이었다 |
| 3 | **발행본을 손댄 뒤에는 금칙어·SC-12 스캔을 재실행하라.** 지적 반영도 검증 대상이다 | 반영 중 `인용할 때`를 새로 넣었다가 스캔이 잡았다 |
| 4 | **착수 전 담당 파일의 표 밀도를 재라.** `bytes ÷ 표 개수` | 「표마다 한 줄 요약」 비용이 여기서 나온다. §10 정정 블록에 9개 파일 실측표가 있다 |

### 진행 방식 — 검증된 절차

```
1단계  분할 설계 → 승인                          ← 완료
2단계  배치로 나눠 변환, 배치마다 커밋·보고        ← B1 완료, B2부터
3단계  구현 미참여 에이전트가 독립 검증
4단계  지적 반영 → 커밋 → (요청 시) 푸시
```

**검증은 축을 나눠 2기 이상 병렬로 띄워라.** B1에서 「근거 주체」·「팽창 출처」 두 축으로 나눴더니 겹치는 지점(출처 없는 외부 권위)이 양쪽에서 잡혔다. **한 명에게 전부 맡기면 "설계대로 변환됨"으로 승인한다.**

---

## 3. ⚠️ 설계서 정정 2건 — 뒤 배치 추정이 바뀐다

### 3-1. §6-1 「`강의가 ~한다` 130여 개」는 실측 32다

| 값 | 초안 | **실측** | 비고 |
| --- | ---: | ---: | --- |
| `강의` 총 행수 | 160 | **160** | 초안이 맞다 |
| `강의가` + `강의는` | 「130여 개」 | **32** | **4.1배 과대** |
| 조사형 6종 전부 | — | 66 | 넓게 잡아도 절반 |

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge"
grep -ho '강의가' aiagent/*.md | wc -l   # 15
grep -ho '강의는' aiagent/*.md | wc -l   # 17
```

**이 카테고리 최대 위험을 「`강의` 주체 치환」으로 잡은 진단이 과대평가다.** `02` 단독은 19행이고 주어 형태가 0건이라 B1에서 「주어를 바꾸는 작업」이 발생하지 않았다. B1이 실제로 처리한 것은 ㉢ 3건과 시점 표지 6건이다.

> **위험의 무게중심은 「원본을 옮기는 일」이 아니라 「옮긴 뒤 덧붙이는 요약문」에 있다.**
> 배치 브리프의 경고 배분을 여기에 맞춰 다시 잡아라.

### 3-2. §10 팽창률 1.20 → 실측 1.76

| | 원본 B | 발행 | 배율 |
| --- | ---: | ---: | ---: |
| 설계 목표(편1~5) | 58,530 | 76.9KB | 1.20 |
| **B1 실측** | 58,530 | **103,108 B** | **1.76×** |

**26편 총량 468.2KB → 650~670KB 재추정.** 편당 평균 18.0KB → 약 25KB.

초과분 22.4KB 중 **6.6KB는 계상 방식 차이지 재량이 아니다** — frontmatter(~680B)와 축약 용어표(~1,300B)가 §4-1 「원본 B」 열에 없어 **편당 ~2KB가 배율 계산 밖에서** 발생한다.

**팽창은 `02` 고유가 아니라 전역이다.** 변환 보고서의 *"`02`는 표 밀도가 비정상적으로 높다"* 는 채택하지 않았다.

| 파일 | 01 | 02 | 03 | 04 | 05 | 07 | 08 | 09 | team-prod |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| **B당 표 1개** | 1,074 | **1,149** | 1,101 | **984** | **1,031** | 1,270 | 2,266 | 1,175 | 11,322 |

`02`는 9개 중 5위다. **덜 팽창할 파일은 `08`과 `team-productivity` 둘뿐** — B6·B4가 그렇다.

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. 1·2차 발행본과 프로덕션에서 동일 재현. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 77점. 히어로 진입 애니메이션이 하이드레이션에 의존. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **2차 전체를 마친 뒤 재측정** |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | **어휘 81종 중 30종이 0편** | `redis` `kafka` `docker` `aws` `spring` `java` `nosql` `load-testing` `ci-cd` `deployment` `distributed-systems` `event-driven` `cdc` `cqrs` `microservices` `scalability` `vector-database` `machine-learning` `product-discovery` `prd` `product-metrics` `ab-testing` `user-research` `commerce` `payments` `global-service` `startup` `team-building` `career` `recommender-systems`. **어휘에 있는 것과 살아 있는 것은 다르다** |
| 7 | ~~`tags.ts` 주석 불일치~~ | ✅ **완료** (`388519e`) — 어휘에 추가하지 않고 주석에서 삭제 |
| 8 | ~~요구사항 §13-1 정정~~ | ✅ **완료** (`388519e`) — `06`·`08` 이관을 각주로 기록 |

### `ai-transformation`으로 이월된 위험 3건

`06`을 이관하면 조사자 D가 찾은 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표.** 발행하지 않는 선택지 우선 검토 |
| 2 | **`06`은 발행본 51편과 서사 방향이 반대다.** 51편은 "위임을 늘려라", `06`은 "어디서 사람을 세울 것인가". 실은 `delegation-gap` L197의 3조건을 판정 격자로 편 것 — **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

**네 번째 이월이다.** README는 2026-05-02 이후 손대지 않았고 그 사이 **15커밋**이 쌓였다.

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 **87편·4카테고리**가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. 이 사이트가 쓰는 환경변수가 아니라 RAG·에이전트를 설명하는 **블로그 글 본문의 예시 코드**다. `.env.example`을 만들거나 README에 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이라 사람이 직접 부르지 않는다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)와 설계서 §13에 있다. **아래는 B1에서 새로 나온 것이다.**

### 6-1. 스스로 모순되지 않는 틀린 값은 내부 검산으로 안 잡힌다

설계 검증 2기가 지적 42건을 잡고도 **「130여 개」는 못 잡았다.** 검증이 사슬 연산·표 교차 대조 같은 **내부 정합성**에 집중했고, 이 값은 외부 실측이라 대조 상대가 없었기 때문이다.

> **원본에서 온 수는 원본에서 다시 세는 것 외에 검증 방법이 없다.**
> 설계서의 수를 브리프로 옮길 때 **"이 값을 재계수했는가"** 를 물어라.

### 6-2. 상쇄 오류가 두 단계에서 반복됐다

| 단계 | 무엇 |
| --- | --- |
| 설계 | 도식 총계가 두 표에서 둘 다 85. 참값 84 (`07`을 14로, `09`를 9로 세어 상쇄) |
| 변환 | 갈래 분류표가 `02` L1213을 `강의`로 오인용(실제는 `강사`)하고 L647을 누락. **1:1 상쇄돼 합계 19가 우연히 맞음** |

**합계 일치는 항목 일치를 뜻하지 않는다.** 표본 행을 원문에서 재확인해야 잡힌다. 이번엔 발행본 영향이 없었지만, **뒤 배치가 그 분류표를 템플릿으로 쓰면 상쇄되지 않아 실제 누락이 된다.**

### 6-3. 모집단 경계를 명시하지 않은 수는 양쪽 다 틀린다

- 설계서 「160행」은 **12편 합계**인데 파일별 작업량으로 읽혔다
- 리뷰어의 「원본은 11개 파일이지 12편이 아니다」는 **`aiagent/`만 센 것**이다. 12번째(`team-productivity.md`)가 디렉터리 밖에 있다 — **설계서가 맞다**

수를 적을 때 **무엇을 센 것인지**를 같이 적어라.

### 6-4. 요청 범위 밖 수정이 순이익이라는 보장이 없다

반영 라운드에서 **선택 항목**을 고치다 SC-12 금칙 표현(`인용할 때`)을 새로 넣었다. 검증 스캔이 1건으로 잡아 재수정했다.

> **발행본을 손댄 뒤에는 금칙어·SC-12 스캔을 반드시 재실행한다.**
> "지적 반영은 검증 대상이 아니다"라는 가정이 틀린다.

### 6-5. 서브에이전트 최종 응답 유실이 이번에도 2회

`idle`을 보고했으나 최종 응답 텍스트가 도달하지 않은 일이 **2회**(직전 세션 3회) 있었다. **파일 산출물을 지시한 건은 전부 회수됐다.**

> **브리프에 산출물 경로를 파일로 못박아라.** 유실 시에는 `SendMessage`로 회수할 수 있다 — idle 상태 에이전트는 컨텍스트가 살아 있다.

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 148개 URL, 정적 페이지 157

# 금칙어 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]"

# 민감정보 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 오탐 많음. 문맥 판정 필수
Select-String -Path "content\blog\**\*.md" `
  -Pattern "인용할 때|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안"
```

> **알려진 오탐 4종**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **87편 중 77편이 갖고 있으며 정규식은 영문만 잡는다.** 금칙 목록이 겨냥하는 것은 본문의 개인 실명·핸들(CR-3.5)이지 출처가 아니다
> - `채용` — `agentic-coding` 편14(`05 §10-2`)에 「**채용**과 JD 작성」이 온다. 구직이 아니라 **에이전트 정의를 채용에 비유**한 것 (B3에서 등장)
> - 화행 정규식 — `이렇게 구현하는 편이 안전하다`(구현 권고), `LLM이 답한다`(주어가 모델)는 정상

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge"
LC_ALL=C awk '
  /^```/ { fence=!fence; if (fence && $0 ~ /^```mermaid/) m++; next }
  !fence && /^## / { h2++ }
  fence && /^## / { fake++ }
  END { print FILENAME, "H2="h2, "mermaid="m, "fake="fake }' <파일>
```

**펜스를 추적하지 않으면 H2가 127이 아니라 159로 세어진다** — 코드블록 안 가짜 H2가 32개(`03`에만 10개). 절 경계가 25% 늘어 분할이 통째로 어긋난다.

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — **87편** |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, **4개 발행** |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종(실사용 51). **목록 밖이면 빌드 실패** |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15 |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§6-1·§10에 정정 블록** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |
| **B1 보고서 5건** | [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환·실행검증·리뷰2·반영 |

**B1 보고서 5건이 저장소에 남아 있다.** 뒤 배치는 이것을 템플릿으로 쓸 수 있다 — 다만 **변환 보고서 §4-2 분류표는 상쇄 오류가 있었으므로(§6-2) 그대로 복제하지 마라.**
