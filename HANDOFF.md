# HANDOFF

> 마지막 갱신 2026-09-02 · `pm` 배치 **시리즈 A 9/9 완결 · 시리즈 B 2/8**

## 지금 상태

발행본 **178편**이다. 시리즈 B 의 **첫 본편**이 나갔고, 이 편으로 예산 모델의 마지막 검산이 끝났다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 2,500줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 ✅ · B `product-management` 8 |
| 발행 완료 | **A 편1~9 + B 편1·편2 (178편)** |
| 다음 단계 | **B 편3 `tactics-and-goal-setting`** — 원본 `pm-po/01` §5·§6 · 🔴 **반려선 관리 대상** |
| 푸시 · 배포 | ⏳ **편2 커밋이 로컬에만 있다.** 편1까지는 `origin/main` 에 올라가 배포됐다 |

**설계서 §1 의 절차표가 정본이다.** 진행률은 **`§10-N` 의 실측 절 번호**로 판단하라 —
§10-11 까지 있으면 B 편2 가 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | B 편2 `product-org-and-strategy` 집필·발행 (16,913 B) | `content/blog/product-management/product-org-and-strategy.md` |
| 2 | 초고 19,143 → 다섯 차례 감축 + 겹침 수정 | 밴드 15,425 ~ 17,320 **안** (상한 여유 407 B) |
| 3 | 지도편 구성 표에 **편2 행 링크 부착** | `product-management-map.md` |
| 4 | 축자 겹침 처리 — `dup-scan` 4건 중 3건 · 원본 대조 표 헤더 1건 | 편2 본문 |
| 5 | B 편2 실측과 **k 검산** | 설계서 **§10-11** (6개 하위 절) · §1 절차표 ⑥ |
| 6 | README 세 자리 (177 → 178) · CHANGELOG 새 절 · 편8 절 아카이브 이동 | `README.md` · `CHANGELOG.md` · `docs/changelog/2026-09.md` (4절) |
| 7 | 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. ✅ 예산 모델은 검산이 끝났다 — **k 는 맞았고 고정 비용만 어긋난다**

§10-11-a 가 두 방식으로 역산해 원인을 갈랐다.

| 무엇을 상수로 두었나 | 역산 k | 판정 |
| --- | ---: | --- |
| 설계 상수 2,455 | 2.130 | 밴드 안 |
| **실측 고정 비용 3,013** | **2.048** | 설계 k 2.05 와 **0.002 차** |

**본문 몫은 설계가 예측한 그대로 나왔다.** 초과한 543 B 는 전부 도입·정리·frontmatter 라는
형식 비용에서 왔다.

⇒ 🔴 **그래도 상수 2,455 를 고치지 마라.** 한 산식에 실측 고정 비용과 설계 상수를 섞으면
비교가 깨진다(§10-3-b). **B 편3~8 은 §3-2 예산표를 그대로 쓴다.**

### 2. 🔴 다음 편(B-3)이 **유일한 반려선 관리 대상**이다

| 항목 | 값 |
| --- | ---: |
| 원본 (`01` §5·§6) | 5,590 B |
| 목표 | **13,969 B** |
| 밴드 | **13,187** ~ 14,752 |
| 하한 `MIN_BYTES` | **13,300** |

밴드 하단이 하한을 **113 B 밑돈다**(§3-2-a). **초고가 13,300 을 밑돌면 반려하고, 인접 편에
합치지 말고 원본 인용을 표로 펴서 채운다.** 🔴 **경력 메모로 채우는 것은 금지다.**

✅ **다만 위험은 생각보다 작다.** §10-11-b 가 확인했듯 시리즈 첫 편들의 고정 비용은 상수보다
550 안팎 높게 시작하고, 그 차이가 하단을 밀어 올린다.

### 3. 🔴 밴드 안이라는 판정과 **발행 가능하다는 판정은 다르다**

이번 초고는 상한을 10.5% 넘겨 다섯 번에 나눠 깎았는데, **밴드 안에 처음 들어온 회차에서
멈췄다면 겹침을 고치는 순간 다시 나갔을 것이다.**

| 회차 | 합계 B | 상한 여유 |
| --- | ---: | ---: |
| 4차 (밴드 첫 진입) | 17,307 | **13 B** |
| 5차 | 16,891 | 429 B |
| 겹침 수정 뒤 | **16,913** | 407 B |

겹침을 고쳐 쓰면 총량이 는다 — 실측 **+22 · +73 · +232 B** 다.
⇒ **상한 판정에 「겹침 수정분을 담을 여유」를 포함시켜라.**

### 4. 🔴 함정 41 의 판정 규칙이 완성됐다 — **임계값을 낮춰 건수가 늘면 검사기가 살아 있다**

편1은 `--min 16` 에서도 0건이라 **검사기가 대상을 잃은 것**이었다(개행이 CRLF 로 바뀌어 문단
분할이 깨졌다). 이번 편은 같은 절차에서 반대 결과가 나왔다.

| 임계값 | ① 공백 보존 | ② 공백 제거 |
| ---: | ---: | ---: |
| 20자 | 1 | 0 |
| **16자** | **6** | **1** |

| 관측 | 뜻 |
| --- | --- |
| 임계값을 낮췄더니 **건수가 늘었다** | 검사기가 문단을 쥐고 있다. 20자 0건은 결론이다 |
| 임계값을 낮췄는데도 **0건이다** | 🔴 검사기 쪽을 의심하라. `tr -cd '\r' \| wc -c` 를 먼저 돌린다 |

`Edit` 도구로 고치면 이 문제가 없다. 파이썬을 쓸 때는 읽기·쓰기 양쪽에 `newline=""` 를 준다.

### 5. 지도편의 형제편 링크는 **여섯 개 남았다**

편2 행에만 링크를 붙였다. 편3~8 발행 때 **그 행에 하나씩 붙이고**, 커밋 19-a 에서 전수
확인한다. 다음 편 링크는 편3 이 없으므로 본문에서 **문장만 두고 벗겼다** — A 편2 와 같은 처리다.

### 6. 고립 검사는 **이미 처리됐다** — 커밋 20 에서 되돌아와야 한다

| 링크 | 지금 | 커밋 20 에서 |
| --- | --- | --- |
| `agent-definition-by-department` → | B-1 지도 | **B-4** 기획·관리 |
| `thirty-agent-catalog` → | B-1 지도 | **B-3** 목표 수립 |

⇒ **§6-3 표의 4번·5번은 「미착수」가 아니라 「임시 대상으로 걸림」이다.** 다시 걸지 말고
대상만 바꾼다.

---

## 시리즈 B 사양 요약

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (편1·편2 발행) |
| 원본 | `pm-po/01` · `02` · `04` — 배정 **58,636 B** |
| 편수 | 8편 (편1 지도 ✅ · 편2 ✅ · 편3~8) |
| 예산 | k = **2.05** · 밴드 1.91 ~ 2.19 (§3-2) |
| frontmatter | `series: "product-management-domains"` · `seriesOrder` 1~8 (§7-4) |
| 🔴 낱말 | 「기획」은 A 와 뜻이 다르다. **편1이 갈라 뒀고 편2가 그 용법을 따랐다** |
| 🔴 편7 | 법과 시행령을 섞지 마라. 법령만 한 표의 **두 열**로 겹친다 (§3-2-b) |
| 🔴 편8 | 확정 제도와 논의 중인 쟁점을 같은 층으로 쓰지 마라 (§7-2) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  대상 편이 없어 지금은 걸 수 없다(§6-2). **B-3·B-4 발행 때 되돌아와 건다** — `A-6` 은 이미
  발행됐으므로 **파일을 다시 열어 고쳐야 한다.** 🔴 **B-3 은 다음 편이므로 그때 `B-3 → A-7`
  을 함께 건다.**
- 🔴 **밴드와 하한은 여전히 기계 검사 밖에 있다.** `npm test` 는 하한 미달을 `stderr` 경고로만
  내고 상한은 검사기가 없다. **「테스트 초록」을 분량 근거로 쓰지 마라** — `compose` 출력과
  `[SOFT]` 목록을 눈으로 읽는다. 현재 하한 미달 **5편**은 전부 이번 배치 밖이다.
- **표 헤더가 원본과 통째로 같아지는 자리가 있다.** 이번에 `| 항목 | 기능조직 | 목적조직 |`
  이 원본 헤더와 20자 일치했다. **셀 내용을 다시 써도 헤더는 원본 낱말을 그대로 고르기 쉽다**
  — 표를 만들 때 헤더도 대조 대상으로 본다(§10-11-f).
- **도식 배율 표본이 둘 다 1.03 언저리다.** 규칙 5 의 1.263 을 아직 고치지 않았다. 두 표본
  모두 「구조 유지 · 라벨만 다시 쓰기」였다(§10-11-e).
- **`user-research` · `career` 태그가 싱글톤으로 남았다.** **글에 없는 내용으로 태그를 맞추지
  마라**(§11-8).
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 이번 세션에 편8 절을 `docs/changelog/2026-09.md` 로
  옮겨 아카이브가 **4절**이 됐다. 넘으면 여기로 옮기고 상단 인덱스 표를 갱신한다.
- ✅ **`check-counts` 를 발행 전 검사에 넣은 뒤로 배포 실패가 없다.** 편을 발행했으면 README
  **세 자리**와 CHANGELOG **최신 절**, 네 자리를 같은 커밋에 넣는다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **초고는 밴드를 넘는 쪽으로 어긋난다** | 목표 16,370 에 초고가 **19,143** 이었다. 절 구성을 다 지켰는데도 산문이 부풀었다 | **초고 직후 `compose` 를 먼저 돌린다.** 성분을 재기 전에 깎지 않는다(§8-2) |
| **`cd` 뒤의 상대 경로가 깨진다** | 셸 도구에서 `cd` 한 뒤 상대 경로가 죽었다. 앞 세션과 같은 현상이다 | 셸 도구에는 **절대 경로**를 쓰거나 `Grep` 도구를 쓴다 |
| **`compose` 와 `wc -c` 의 1 B 오프셋** | 일곱 편에서 재현됐다 | 상수 오프셋이다. **1 B 차이를 「불일치」로 오독하지 마라** |
| **`check-counts` 는 네 자리를 본다** | README 세 자리를 고쳐도 **CHANGELOG 최신 절**이 남아 실패한다 | 새 절을 쓴 뒤 다시 돌린다. 자리는 넷이다 |
