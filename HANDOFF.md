# 인수인계 (HANDOFF)

**갱신일**: 2026-08-11
**브랜치**: `main` · 최신 커밋 `2fc5c9f`
**작업트리**: 이 문서 · `CHANGELOG.md`만 미커밋 / stash 없음
**발행본**: **101편** (`ai-agent` 51 · `rag` 25 · `agentic-coding` 19 · 기타 6) · sitemap **162 URL**

---

## 1. 이번 세션에서 한 일

**`agentic-coding` B5를 발행했다(편15~18, `agent-operations` 시리즈 신설).** 그리고 **설계서 진단 7건을 정정했다 — 5배치 중 최다다.**

```
조사 3기 병렬 ─→ 컨트롤러 실측 ─→ 브리프 ─→ 변환 1기 ─→ 검증 4축 ─→ 반영 ─→ 재검증 2기 ─→ 2차 반영 ─→ 최종검증 ─→ 커밋
      │              │                          │             │                    │
  ┌───┼───┐     설계서 오류 4건            [추론] 6건    ┌──┬──┼──┐           ┌────┴────┐
 원본 설계서 발행본  + 조사 오류 4건        표기 → 4건 정정  A  B  C  D          반영정확성  실행
 실측  회수   규격                                      틀림 없음 누락 실행      (opus)   (sonnet)
```

| 단계 | 산출 |
| --- | ---: |
| 변환 4편 | 110,827 B · 도식 13 · 표 54 |
| 검증 4축 | 지적 **33건** (중복 제거 후 20) |
| 1차 반영 | 20건 전량 · **반박 0건** |
| 재검증 2기 | 「사라졌는가」 **20/20** · **신규 결함 7건** |
| 2차 반영 | 8건 전량 (신규 7 + 참고 1) |
| 최종 | **119,262 B · 배율 1.97** |

### 실패가 「배정」에서 「총평」으로 이동했다 — 이번 배치의 최대 발견

| 유형 | B1 | B2 | B3 | B4 | **B5** |
| --- | ---: | ---: | ---: | ---: | ---: |
| 수치 불일치 | 5 | 0 | 0 | 0 | **0** (전수 3자 대조) |
| 원문 강도 어긋남 | 한 방향 | 양방향 6 | 0 | 3 | **1** (하) |
| 신규 도식의 거짓 관계 | — | — | 1 | 0 | **0** (신규 도식 0개) |
| 원문에 대한 부정 사실주장 실패 | — | — | — | 2 | **0** |
| **분류 항목 오배정** | — | — | 있음 | 있음 | **0** ← 처음 |
| **전수 배정 뒤 총평의 새 전칭** | — | — | — | — | **5** ← 처음 |
| 설계서 진단 오류 | 2 | 1 | 1 | 6 | **7** |
| 재검증 신규 결함 | — | — | 5 | 7 | **7** |
| 팽창률 | 1.76× | 1.42× | 1.31× | 1.77× | **1.97×** |

**배정 자체는 전부 정확했다.** 재검증자가 원문 표를 한 항목씩 손으로 배정해 대조한 결과다 — 4+1+1=6 · 3+2+3=8 · 8/8 · 2+3+5=10, **오배정 0건.**

**틀린 것은 배정 뒤에 붙인 한 줄짜리 총평이다.**

| 정밀해진 것 | 새로 틀린 것 |
| --- | --- |
| 「원 자료의 실습 예제」(오귀속) → 「이 글에서 바꿔 실었다」 | **6행 중 1행만 바뀌었는데 전칭이 됐다** |
| 「같은 동작이다」(단정) → 「판정되지 않는다」 | 공유 결과를 「**뒤쪽** 규칙」이라 적어 **위치 의존을 되살렸다** |
| 「위 둘 / 아래 둘」 → 「3+2+3」 | 마무리에 「**유일하게**」라는 새 전칭 |
| 「셋째만 고칠 수 있다」 → 계층별 성격 | 성격 구분을 지키려고 **반례 항목 하나를 열거에서 뺐다** |

> **전칭은 방향만 뒤집혀도 여전히 전칭이다.**
> **전수 배정을 마친 뒤 그것을 요약하려는 충동이 새 전칭을 만든다.** → 설계서 §11 **금지선 17**로 등록했다.

### ⚠️ 브리프 오류 2건 — 그중 하나는 컨트롤러가 조사 보고를 흘린 것이다

| # | 브리프가 쓴 것 | 실제 | 잡은 자리 |
| --- | --- | --- | --- |
| 1 | 「60·100줄은 `07`, 200줄은 `03`(편6)」 대조 구도 | **`07` 자체가 200줄을 §4-2에서 두 번 쓴다**(L503·L521) | **변환자** (편17 착수 중 발견) |
| 2 | 편17 배율 ~1.60 (표 밀도 연동) | **1.875** — 가설이 정확히 반대로 반증 | 변환자 |

**1번은 scout A가 정확히 보고했는데 컨트롤러가 옮기지 않았다.** 보고서에 「200줄 언급: L503·L521」이 적혀 있었다.

> **검증 시선이 「상위 문서가 맞나」에 고정되면 「상위 문서가 다루지 않은 것」이 시야에서 빠진다.**
> 설계서 진단(60↔200)을 검증하는 데 집중하다가 **설계서에 없는 사실**을 읽고도 흘렸다.

**순차 변환이 이것을 잡았다.** 편15 초안에 「여섯 행 중 마지막만 다른 자료에서 온다」고 쓴 뒤 편17을 쓰려 §4-2를 읽다 반증을 발견했다. **4편을 병렬로 나눴다면 편15 담당자는 §4를 열 일이 없었다** — 설계서 §13-8의 「배치 안에서는 순차」가 톤 유지 말고도 값을 한다.

---

## 2. 다음 세션이 이어받을 것

### 최우선: B6 착수 (`08` 개발축 → 편19~21)

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md)
— **§4-1·§4-4·§5-4·§5-5·§6-1·§6-3·§6-4·§6-5·§6-6·§9-1·§10·§11에 정정 블록이 있다. 그것부터 읽어라.**

| 배치 | 대상 | 편 | 편수 | 상태 |
| ---: | --- | --- | ---: | --- |
| ~~B1~~ | ~~`01`(축약) + `02`~~ | ~~1~5~~ | ~~5~~ | ✅ 완료 (`388519e`) |
| ~~B2~~ | ~~`04`~~ | ~~9~11~~ | ~~3~~ | ✅ 완료 (`f4b9c30`) |
| ~~B3~~ | ~~`05`~~ | ~~12~14~~ | ~~3~~ | ✅ 완료 (`cf948a1`) |
| ~~B4~~ | ~~`03` + `team-productivity`~~ | ~~6~8, 26~~ | ~~4~~ | ✅ 완료 (`a568c2e`) |
| ~~B5~~ | ~~`07`~~ | ~~15~18~~ | ~~4~~ | ✅ **완료** (`2fc5c9f`) |
| **B6** | `08`(개발축) | 19~21 | 3 | **다음** |
| B7 | `09` | 22~25 | 4 | §6-5 10번 표 캡션 승격 |
| B8 | Q&A 4편 | Q1~Q4 | 4 | `05 §10-1` 10문항이 여기로 이월됐다 |
| B9 | 지식 지도 | — | 1 | **맨 마지막** |

### B6 편 명세

| 편 | series / seriesOrder | slug | 원본 절 | 원본 B | 도식 |
| ---: | --- | --- | --- | ---: | ---: |
| **19** | `agent-definition-catalog` 1 | `thirty-agent-catalog` | `08` 머리말 **§0 전량** §1 §2 | 18,931 | 2 |
| **20** | `agent-definition-catalog` 2 | `definition-writing-quality` | `08` §3 §12 | 13,645 | 0→**1** |
| **21** | `agent-definition-catalog` 3 | `dev-org-transfer` | `08` §9 §11 | 11,627 | 2 |

`08` 전체 68,003 B · H2 15 · mermaid 10 · 표 30 · **가짜 H2 7**. `agent-definition-catalog` 시리즈 신설.

### B6 고유 사항 — 다른 배치에 없는 것

| # | 사항 | 근거 |
| --- | --- | --- |
| 1 | **`08`은 가짜 H2가 7개다** — 5배치 중 유일하게 많다 | 절별 실측 awk에 **펜스 추적이 없으면 H2가 과대계수되어 분할이 통째로 어긋난다.** §10-1 주석 |
| 2 | **표 밀도가 2,266 B/표로 가장 성기다** | 전 파일 중 `team-prod`(11,322) 다음. **팽창은 표 개수가 아니라 행 수에 붙는다**(B5 정정) — `08`은 표가 적지만 **30에이전트 카탈로그가 7열 30행**이라 행 수는 많다. **착수 전 표 행 총계를 세라** |
| 3 | **금지선 2 — 원본 내부 중복 1건을 제거한다** | `08 §3-4` tools 열의 `"기본"` **25행(약 200 B)**은 바로 위 3행 요약표가 이미 말한다 → **편20**. 이번 카테고리에서 **유일하게 승인된 제거**다 |
| 4 | **⚠️ B5가 넘긴 것 — `07 §7-1`과 `08`의 8슬롯 불일치** | 편18이 `08` L142~173과 셀 단위 대조를 마쳤다. **개수는 일치**(7부서·5·5·4×5=30)하나 **구성 슬롯은 22/30만 대응**하고 층위가 **3열 조직도 대 7열 정의서**로 다르다. **편19~21이 「같은 조직도의 두 판본」으로 다룰지 `07`판을 초기 스케치로 볼지 판단하라.** 대조표는 `CHANGELOG.md` B5 항목과 이 문서 §6-4에 |
| 5 | **편18은 B6로 링크를 걸지 않았다** | 미발행 slug로 링크하면 죽은 링크가 된다. **B6가 편18로 가는 역링크를 걸지 판단하라** |
| 6 | **편21 자기참조 미채택 확정** | 아래 「확정된 결정」 |
| 7 | **`08`의 전방 참조는 아직 실측되지 않았다** | 설계서 §9-1 정정이 「`04`·`07`·`09`도 재확인」이라 했고 **`07`은 0건으로 확인**됐다. `08`은 미실측 — **히트 행이 발행 대상 절 안인지까지 확인하라** |

### 확정된 결정 (다시 묻지 마라)

| 사안 | 결정 | 근거 |
| --- | --- | --- |
| **편21 자기참조** — `08 §9`에서 이 리포의 CI·배포 관행을 근거로 쓸 것인가 | **쓰지 않는다** (사용자 승인 2026-08-08) | `deploy.yml`이 preview 없이 `main`=프로덕션인데 §9의 "자동 롤백 임계"와 대비되면 약점 공개가 된다 |
| **분량 상한** | **29.4KB는 합격선이 아니다. 넘어도 된다** | §4-0 원문. B3 편14 30,897 B · **B5 편18 36,238 B(발행본 최대)** |
| **시리즈 페이지** | 없다 | `series`는 frontmatter 메타데이터일 뿐 라우트를 만들지 않는다. B5에서 4편에 sitemap +4로 재확인 |
| **frontmatter 필드명** | **`seriesOrder`** (`order` 아님) | 없으면 **빌드 실패.** 설계서 §4-1 정정 |
| **축약 용어표 판정 기준** | **개념 기준.** 본문이 한글로 써도 영문 용어 행은 남긴다 | 설계서 §4-4 정정. 단 **제거된 절에서만 쓰이던 용어는 함께 뺀다** |
| **`05 §10-1`·`§10-3`** | 각각 **B8 이월** · **드롭** | 설계서 §4-4 |

### B6 브리프에 반드시 넣을 것

**B1~B4가 준 17건 + B5가 준 아래 5건.**

| # | 추가할 지시 | 근거 |
| --- | --- | --- |
| **30** | **전수 배정을 마친 뒤 그것을 요약하는 총평 문장을 붙이지 마라** | 신규 결함 7건 중 **5건이 배정 아래 한 줄**에서 났다. 배정 자체는 오배정 0건이었다 |
| **31** | **전칭이 불가피하면 개수를 세어 문안에 적어라** — 「바꿔 실었다」가 아니라 「여섯 행 중 둘째 행 하나를」 | 오귀속을 고치자 **과잉귀속**이 됐다 |
| **32** | **금칙어를 피해 값을 바꾸면 그 값에 붙은 귀속 문장도 함께 갱신하라** | 최상위 결함. `이력서`→`지원서`로 바꾸고 「원 자료의 실습 예제」를 그대로 뒀다 |
| **33** | **값 대조는 파일 단위가 아니라 절 단위로 하라** | `07`이 200줄을 §2-2와 §4-2 두 목적으로 쓴다. **같은 파일 안에서도 절이 다르면 목적이 다르다** |
| **34** | **「X가 0건이다」류 부정 명제를 받으면 정규식을 넓혀 다시 돌려라** | 조사가 「§3 비용 0줄」이라 단언했으나 2행 있었다. **금지선 15는 조사 단계에도 걸린다** |

**B1~B4가 준 기존 17건**(요약문 수치 대조 / 원문 강도 상승 금지 / 수정 후 스캔 재실행 / 착수 전 표 밀도 측정 / `[실측]`·`[설계서]`·`[추론]` 표기 / 설계서 진단은 옮기기 전에 실측 / `tags.ts` 패싯 규칙 명시 / 재검증에 「사라졌는가」 / 상한은 합격선이 아님 명시 / 「이 글의 정리다」 귀속 / 분산 지시는 편별 체크리스트 / 신규 도식은 화살표마다 원문 근거 / 부정 주장은 검색 후 근거를 문안에 / 전칭은 전수 배정 / 담당 원본 L1~L3으로 기준일 확인 / 전방 참조는 발행 대상 절 안인지 확인 / 추정에는 「이건 추정이다」 명시)는 그대로 가져간다.

### 진행 방식 — 검증된 절차

```
1단계  분할 설계 → 승인                                    ← 완료
2단계  배치로 나눠 변환, 배치마다 커밋·보고                  ← B1~B5 완료, B6부터
3단계  구현 미참여 에이전트가 4축 독립 검증
4단계  지적 반영 → 재검증 2기 → 2차 반영 → 최종 실행 검증 → 커밋
```

**검증 4축은 그대로 쓴다.** 나누는 기준은 주제가 아니라 **결함이 드러나는 방식**이다.

| 축 | 무엇을 보는가 | 왜 별도 축이어야 하나 | 티어 |
| --- | --- | --- | --- |
| A | 쓰여 있는데 **틀린** 것 | grep으로 안 잡힌다. 세면 0건이라 자동 검증을 통과한다 | opus |
| B | 쓰여 있는데 **원문에 없는** 것 | 원문과 나란히 놓아야만 보인다 | opus |
| C | **없는** 것 | **원본 인벤토리를 먼저 만들고** 그 목록을 들고 발행본을 검색한다 | opus |
| D | **실행하면 나오는** 것 | 종료코드는 객관적이다 | sonnet |

> ### ⚠️ 검증자에게 브리프도 변환 보고서도 주지 마라 — B5에서 확립
>
> 브리프를 읽은 검증자는 **「브리프대로 됐나」만 본다.** 그건 검증이 아니라 준수 확인이다.
> **B5에서 브리프의 대조 구도 자체가 틀렸다는 것이 증명됐다.**
>
> | 축 | B5 단독 발견 |
> | --- | --- |
> | **B** | **최상위 1건** — 변경된 값에 원 자료 귀속이 남음. 축 A는 같은 줄을 보고 「표가 자기 설명과 어긋난다」로만 진단했다 |
> | **C** | 3건 — 각주 커버리지 공백 2자리 + **용어표 잉여 3행**(요건 밖에서 찾은 것) |
> | **A** | 5건 — 분류 판정의 항목 오배정. 「패러다임 전환 열」을 읽었다는데 실은 「주제」 열 |
>
> **대가는 오탐 2건이다** — 축 B가 §7-2 제거 승인을 몰라 「출처가 사라졌다」를 지적했다. **그 대가로 최상위 1건을 얻었다.**
> **축 C는 원본을 먼저 읽게 하라.** 발행본을 먼저 읽으면 시선이 「있는 것」에 고정되어 누락이 보이지 않는다.

**재검증은 2기로 나눈다.** B3 5건 · B4 7건 · **B5 7건.** 3배치 연속이므로 상설이다.

---

## 3. ⚠️ 설계서 정정 — B6 이후에 영향

**B5에서 7건을 정정했다. 전부 설계서 본문에 ⚠️ 블록으로 삽입돼 있다.**

| 절 | 정정 | 뒤 배치 영향 |
| --- | --- | --- |
| **§4-1** | 「series / **order**」는 필드명이 아니다. 실제는 **`seriesOrder`**이고 없으면 **빌드 실패** | **전 배치.** 선택 필드는 없으면 키 자체를 생략 |
| **§4-4** | 축약 용어표 「그 편이 쓰는 행만」의 판정 기준 부재. **개념 기준으로 확정** | **전 배치.** 문자열 `grep`만으로 판정하지 마라 |
| **§5-4** | 60줄 ↔ 200줄 이항 대립이 아니다. **100줄(「위험」)** 이 따로 있고 **`07`도 200줄을 §4-2에서 두 번 쓴다** | **B6·B7.** 값 대조는 **파일이 아니라 절 단위**로 |
| **§5-5** | 「§2-2는 25항목」 근거 없음(§0 용어표 25행과 혼동). **「비용 통제 1차 = `07 §3`」 진단은 옳았다** | **B9.** `00 §4`가 `07`을 참조하는 자리는 **5곳**이다(초안은 2건만) |
| **§6-6** | 「절대 수치를 전부 버린다」를 문자 그대로 적용하면 **예방책이 빈다.** 사건 수치와 설계 임계를 개별 판정 | **B6·B7.** 「X를 전부 버려라」류 지시는 대상 표를 열어 항목별로 판정 |
| **§10** | 팽창률 **1.97**. **표 밀도 가설 반증** — 팽창은 표 개수가 아니라 **행 수**에 붙는다 | **B6 분량 예측.** `08`은 표가 적지만 30행 카탈로그가 있다 |
| **§11** | 금지선 **17** 추가 — 전수 배정 뒤 총평 문장 금지 | 전 배치 |

### 앞 배치 정정도 유효하다

| 배치 | 정정 위치 | 요지 |
| --- | --- | --- |
| B1 | §6-1 | 「`강의가 ~한다` 130여 개」 → **실측 32** (4.1배 과대) |
| B1 | §10 | 팽창률 1.20 → 1.76 (B4 1.77 · **B5 1.97**) |
| B2 | §5-3 ①②③ | 발행본 열거 기준 · MCP 인과 · 발행본 시점 |
| B3 | §6-5 8번 | `05 §11`은 출처 대장이 아니라 **면접 화법 가이드** |
| B4 | §6-3·§6-4·§6-5·§9-1 | 편26 항목 3건 · 시점 기준일 `2026-07-26` · 인용 절 4건 · `Plan Mode` 링크 불가 |

**설계서의 진단은 브리프로 옮기기 전에 실측한다 — B1·B2·B3·B4·B5. 5배치 연속이다.**
**그리고 오류가 줄지 않는다** — 설계서는 한 번에 작성됐고 각 배치는 자기 담당 원본만 깊이 판다. **자기 절에 이르러서야 오류가 드러나는 구조다.**

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 77점. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **2차 전체를 마친 뒤 재측정** |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | **어휘 81종 중 0편 태그** | B5도 `scalability`를 피했다(편18 주제가 「10→100 확장」인데도). **목록은 설계서 §8-3에** |
| 7 | **훅 차단 채널이 두 발행본에서 다르다** | 편11은 **stdout JSON**, `settings-permissions-deny`는 **표준 에러**. 편11에 고지를 달아 막아 뒀으나 근본 해소는 아니다 |
| 8 | **원본 `04 §8-2` 「3대 구성 요소」인데 표는 4행** | 원본에서 이월된 불일치. 편11에 해명만 달았다 |
| 9 | **원본 `05` 내부 수치 불일치** | L56 도식 `frontmatter 5+4 필드` ↔ L145 표 `핵심 5 + 확장 5`. 편12가 양쪽을 원문대로 옮기고 「원 자료 안에서 판정되지 않는다」로 명기했다 |
| 10 | **`05 §3-2` 제품 결함 3건의 현재 유효성** | 「버전 표기가 없다」는 사실은 명기했으나 **세 동작이 지금도 그런지는 확인하지 않았다** |
| 11 | **무귀속 판정 잔존 2건** | 편14 L232 · 편13 L235. B3~B5에서 손대지 않았다 |
| 12 | **B1 발행본 편2~5의 `2026-04` 표기 재확인** | `2026-04`가 「강의 시점」 자리인지 「문서 기준일」 자리인지 구분되지 않았다. 기발행본이라 범위 밖으로 두었다 |
| 13 | **편7 `feedback` 예시가 원본 MEMORY.md 예시와 소재 겹침** | **B4 재검증 `[추론]`.** 손대면 새 검증 표면이 생겨 의도적으로 두었다 |
| 14 | **편8 용어표 「멀티턴 대화」 본문 0회** | **B4 재검증 실측.** 원지시가 편8은 「추가만」을 요구했다. **B5에서 판정 기준이 「개념 기준」으로 확정됐으므로 재판정 대상이다** |
| **15** | **기발행본의 축약 용어표 문자열 0회 행 미점검** | **B5 신규.** 편17은 문자열 기준으로 7행 중 5행이 0회였다(`롤백(Rollback)`→「되돌리기」 등). **개념 기준 확정으로 대부분 해소되나 기발행 15편은 재점검하지 않았다** |
| **16** | **편18이 발행본 최대 기록을 경신했다 (36,238 B)** | **B5 신규.** 기존 최대는 편14 30,897 B. 금지선 13에 따라 그대로 두었으나 **렌더링·ToC·로딩 체감은 확인하지 않았다** |
| **17** | **「채용」 오탐이 7건으로 늘었다** | **B5 신규.** 기존 2 + 신규 5. 전부 **에이전트 업무 도메인 이름**이며 저자의 구직 활동이 아니다. **검증 명령의 기대값을 갱신했다(§7)** |

### `ai-transformation`으로 이월된 위험 3건

`06`을 이관하면 조사자 D가 찾은 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표.** 발행하지 않는 선택지 우선 검토 |
| 2 | **`06`은 발행본과 서사 방향이 반대다.** 발행본은 "위임을 늘려라", `06`은 "어디서 사람을 세울 것인가". **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

**여덟 번째 이월이다.** README는 2026-08-07(`7f28866`) 이후 손대지 않았고 그 사이 **23커밋**이 쌓였다.

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 **101편·4카테고리**가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. `.env.example`을 만들거나 README에 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)와 설계서 §13, `CHANGELOG.md`의 B1~B4 항목에 있다. **아래는 B5에서 새로 나온 것이다.**

### 6-1. ⚠️ 정확한 배정 뒤에 붙이는 총평이 새 전칭을 만든다

**B3·B4에서는 배정 자체가 틀렸다. B5는 배정이 전부 정확했고**(오배정 0건) **그 아래 한 줄에서 5건이 났다.**

| 처방 | |
| --- | --- |
| 1 | **총평 문장을 새로 쓰지 마라.** 고치는 김에 「요컨대…」를 덧붙이지 마라 |
| 2 | 전칭이 불가피하면 **개수를 세어 문안에 적어라** |
| 3 | **범위를 좁히는 방향**으로 고쳐라. 넓은 주장 → 좁은 사실은 안전하고 반대는 위험하다 |
| 4 | **축자 인용할 수 있으면 요약하지 마라** |

**2차 반영에서 실효를 확인했다** — 새 총평 0건, 전칭 5곳 전부 개수 명시, 재검증 지적 8건 전량 해소.

### 6-2. 금칙어 제거가 새 결함을 만든다 — 앞 배치에 없던 실패 모드

금칙어(`이력서`·`면접`)를 피해 라우팅 키워드를 `지원서`·`전형`으로 바꿨는데, **그 표를 감싼 두 문장이 여전히 원 자료 귀속이었다.**

> `:142` 「**원 자료의** 라우팅 규칙은…」 · `:23` 「키워드 목록은 **원 자료의 실습 예제**」
> `grep -rn "지원서|전형|인사 심사"` → **어느 원본에도 없다.**

> **값을 바꿨으면 그 값의 출처 표시도 함께 갱신하라.** 안 하면 원문에 없는 것을 원문의 것이라 말하게 된다.

**고칠 때도 조심해야 한다** — 오귀속을 「이 글에서 바꿔 실었다」로 고치자 **6행 중 1행만 바뀌었는데 전칭**이 됐다(과잉귀속).

### 6-3. 부정 명제는 조사 단계에도 걸린다

조사가 *"§3에 비용 서술 **0줄**"*이라 단언했으나 **2행 있었다.** 설계서 진단(「2행뿐」)이 옳았다.

> **「2행 있다」는 그 2행을 대면 끝나지만 「0줄이다」는 파일 전체를 다 읽어야 참이 된다.**
> `비용` 단독이 아니라 `비용|과금|요금|청구|예산|토큰`으로 넓히자 즉시 드러났다.
> **금지선 15는 변환자만이 아니라 조사·컨트롤러 전 단계에 걸린다.**

### 6-4. 분할 발행은 진실원 없는 사실을 만든다

편17이 *"**첫 편**이 배치했던 15분·30분 점검"*이라 썼는데 그 표는 **편16**에 있다. 같은 글 `:138`은 「[앞 편](agent-jd-triggers)」로 정확히 적었다.

> **한 원본을 4편으로 쪼개면 「어느 편에 무엇이 있는지」가 새 사실이 되는데, 그것을 대조할 진실원이 원본에 없다.**
> 원문 대조로는 절대 잡히지 않는다. **편간 참조는 발행본끼리 대조해야 한다.**

### 6-5. 값 대조는 파일이 아니라 절 단위로

`07`이 200줄을 **§2-2(설계)와 §4-2(트러블슈팅) 두 목적**으로 쓴다. 340줄 떨어져 있다.

> **같은 파일 안에서도 절이 다르면 목적이 다르다.** `grep`을 파일 전체에 돌려 **히트 전건의 소속 절**을 확인해야 한다.

그리고 **원본은 60 / 100 / 200 세 값을 갖는다.** 설계서는 60↔200 이항 대립으로만 봤고, 100줄(「위험」)의 폐해 4가지 중 하나가 **준수율 개념**이다 — 즉 `07`도 준수율을 말하며 임계를 100줄로 잡는다.

### 6-6. 팽창은 표 개수가 아니라 표 행 수에 붙는다

「표가 성기면 요약 비용이 덜 붙어 배율이 낮다」는 가설이 **정확히 반대로** 반증됐다(편17 예상 1.60 → 실측 1.875).

> **큰 표일수록 「한 줄 요약」이 길어진다.** 10행×4열 표의 요약은 3계층 전수 배정 문단이 된다.
> **다음 배치는 표 개수 대신 「표 행 총계」로 예측하라.** 다만 B1~B5 편차(1.31~1.97)를 보면 **단일 계수 예측 자체의 신뢰 구간이 넓다.**

**그리고 정확성을 올리는 수정은 분량을 늘린다** — 1차 변환 110,827 B → 2차 반영 후 119,262 B. 망라 주장을 좁히고 원문을 축자 인용한 결과다.

### 6-7. 지적이 옳아도 해법은 여러 개다

재검증이 「자원 한도 계층에서 항목 하나가 빠졌다」고 지적했다. 항목을 그냥 추가하면 「자원 한도 = 멈추는 쪽」이라는 성격 구분이 흐려진다.
반영자는 **원문 두 행이 모두 「중단」으로 시작함을 확인하고**(L580 「실행 중단 →」·L581 「즉시 중단 후」) **「먼저 멈추는 쪽」으로 수식어를 붙여** 항목을 빼지 않고 구분을 지켰다.

### 6-8. 서브에이전트 최종 응답 유실이 이번엔 8회

`idle`을 보고했으나 최종 응답 텍스트가 도달하지 않은 일이 **8회**(scout 3 · converter 1 · 검증 3 · fixer 2) 있었다. **파일 산출물을 지시한 건은 전부 회수됐다 — 손실 0.**

> **브리프에 산출물 경로를 파일로 못박아라.** 5배치 연속으로 이 처방이 유효하다.

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과 (tests/blog/ 3파일)
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 162개 URL, 정적 페이지 171

# 금칙어 — 기대 0건 (설계서 §10-2가 진실원. HANDOFF보다 「예상 질문」이 더 있다)
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]|예상 질문"

# 민감정보 — 「채용」 7건만 기대
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 요구사항 §14가 진실원. 오탐 많음, 문맥 판정 필수
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "인용할 때|라고 말|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안|인용 시"
```

> **알려진 오탐 6종**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **정규식은 영문만 잡는다**
> - **`채용` 7건** — `subagent-definition-fields.md:39`(용어표) · `agent-team-operations.md:490`(유비표) · **`agent-jd-triggers.md:148·154`** · **`scaling-routing-collapse.md:104·318·330`**. 전부 **에이전트가 담당하는 업무 도메인의 이름**이며 저자의 구직 활동이 아니다. **2~4번은 구조적으로 제거 불가**(인사 부서의 업무를 달리 부를 이름이 없다)
> - `인용할 때` **1건** — `agent-team-operations.md:419`. 원 자료 수치 인용 지침이지 면접 화법이 아니다
> - `실적이 아니` **1건** — `scaling-routing-collapse.md`. 주어가 **표의 값**(대상)이라 서술문
> - 화행 정규식 일반 — `이렇게 구현하는 편이 안전하다`(구현 권고), `LLM이 답한다`(주어가 모델)는 정상
>
> **판정 기준**: 문장의 주어가 **독자의 발화**면 화법, **대상의 성질**이면 서술이다.

### 태그 패싯 분포 검사 (빌드가 못 잡는다)

```powershell
Select-String -Path "content\blog\agentic-coding\*.md" -Pattern "^tags:"
```

**패싯 C가 3개 이상이면 위반.** `claude-code`·`ai-agent`·`multi-agent`·`ai-governance`·`ai-automation`·`context-engineering`·`prompt-engineering`이 전부 C다. **B5 4편은 전부 정확히 2개**였다.
**`ai-agent`는 이 카테고리 26편 중 7편 한도**(설계서 §8-2). **현재 4편 사용 — 여유 3편.**

### mermaid 문법 검사 (도식을 손댄 배치에서만)

```bash
awk '/^```mermaid/,/^```$/' content/blog/agentic-coding/<파일>.md
```

노드 ID 미정의·중복, 점선 라벨 `-.->|"..."|` 짝, 특수문자(`·` `—`) 든 라벨의 따옴표, 블록 개폐 수를 본다.
**코드펜스(```) 총 개수가 짝수인지도 세라** — 수정 중 펜스가 깨지면 렌더가 통째로 무너진다.

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/aiagent"
LC_ALL=C awk '
  BEGIN { sec="머리말" }
  /^```/ { if (!fence && $0 ~ /^```mermaid/) mm[sec]++; fence=!fence; b[sec]+=length($0)+1; next }
  !fence && /^## / { sec=$0; order[++n]=sec; start[sec]=NR }
  !fence && /^\|[ ]*[-:][- :|]*\|[ ]*$/ { tb[sec]++ }
  { b[sec]+=length($0)+1 }
  END { t=0; for(i=1;i<=n;i++){s=order[i]; t+=b[s];
        printf "%s | L%d | %d B | 표 %d | 도식 %d\n",s,start[s],b[s],tb[s],mm[s]}
        printf "합계 %d B\n", t+b["머리말"] }' <파일>
```

**⚠️ `length($0)+1`의 `+1`이 개행이다. 빼먹으면 절마다 줄 수만큼 미달한다.**
**펜스를 추적하지 않으면 H2가 과대계수된다** — `07`은 가짜 H2가 0이지만 **`08`은 7개다.**
**절별 합계를 `wc -c`와 대조하라.** B5에서 `07` 67,316 B가 정확히 맞았다.
**합계 행을 믿지 마라** — B5 조사가 표 합계를 58로 냈으나 자기 절별 값을 더하면 53이었다.

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글 — 독립편은 구조적으로 여기 걸린다
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — **101편** |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, **4개 발행** |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종. **목록 밖이면 빌드 실패. 상단 주석에 패싯 규칙이 있다** |
| frontmatter 검증 | [`lib/blog/frontmatter.ts`](lib/blog/) — 형식 오류 먼저, 어휘 오류 나중. tags 최대 6개 |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15 |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§4-1·§4-4·§5-4·§5-5·§6-3·§6-4·§6-5·§6-6·§9-1·§10·§11에 정정 블록** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |
| **B1 보고서 5건** | [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환·실행검증·리뷰2·반영 |

**B1 보고서 5건이 저장소에 남아 있다.** 뒤 배치는 이것을 템플릿으로 쓸 수 있다 — 다만 **변환 보고서 §4-2 분류표는 상쇄 오류가 있었으므로 그대로 복제하지 마라.**

> **B2 보고서 7건 · B3 13건 · B4 11건 · B5 10건은 스크래치패드에만 있고 커밋하지 않았다.** 세션이 끝나면 사라진다.
> 핵심은 이 문서 §3·§6과 `CHANGELOG.md`, 그리고 **설계서 정정 블록**에 옮겨 적었다.
