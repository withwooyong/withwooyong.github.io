# HANDOFF

> 마지막 갱신 2026-09-01 · `pm` 배치 **시리즈 A 5/9 발행 · 시리즈 B 0/8**

## 지금 상태

발행본 **172편**이다. `ai-product-planning` 카테고리에 다섯 편이 있고 편6부터 이어 쓴다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 1,790줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 · B `product-management` 8 |
| 발행 완료 | A 편1~5 (172편) |
| 다음 단계 | 설계서 **절차 ⑧** — A 편6 `deliver-and-prd` 집필 |
| 미푸시 | **없다** — 이번 세션에서 편1~5 일곱 커밋을 `origin/main` 에 올렸다 |

**설계서 §1 의 절차표가 정본이다.** ①~⑦ 이 완료됐다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | A 편5 `architect-stage` 집필·발행 (14,742 B) | `content/blog/ai-product-planning/architect-stage.md` |
| 2 | 편4 말미에 편5 링크 연결 | `whether-gate.md` |
| 3 | 편5 실측 기록 | 설계서 **§10-5** |
| 4 | CHANGELOG 편2~5 절 작성 + **월별 아카이브** | `CHANGELOG.md` · `docs/changelog/2026-0{5,6,7,8}.md` |
| 5 | 이 문서 | (이번 커밋) |

편2~4 는 앞 세션들이 발행했으나 CHANGELOG 에 절이 없어 이번에 함께 기록했다.

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 편6 산문 예산을 이미 역산해 뒀다 — 초고 전에 이 값을 지켜라

편4 초고가 밴드를 **34% 초과**한 뒤 물린 처방이고, 편5에서 첫 적용에 작동했다(초고가 밴드 안).
편6 목표는 **19,767 B**, 밴드 **18,460 ~ 21,075** 다.

| 성분 | 원본 §6+§8 | 배율 | 발행 예산 |
| --- | ---: | ---: | ---: |
| 표 | 6,482 | 1.511 | **9,794** |
| 도식 | 497 | 1.263 | **628** |
| 코드 | 1,066 | ? | **1,066** (가정) |
| frontmatter | — | — | 850 |
| **산문** | 1,288 | — | **7,429** |

배율은 편4의 원본과 발행본을 성분별로 대비해 뽑은 실측값이다(설계서 §10-5).

### 2. 🔴 편6은 이 배치에서 **처음으로 코드 블록이 있는 편**이다

편1~5 의 코드는 전부 **0 B** 였는데 편6 원본에는 **1,066 B**(§6 훅 설정 306 · §8 스킬 정의 760)가
있다. **코드 배율을 아무도 모른다.** 위 표는 1.0 을 가정한 것이므로, 초고 `compose` 에서 코드
칸을 반드시 확인하고 설계서 §10-6 에 실측을 남겨라.

| 판단할 것 | 왜 |
| --- | --- |
| 주석을 한글로 옮기는가 | 옮기면 코드 B 가 늘어 산문 예산을 다시 깎아야 한다 |
| 금칙어 검사 | 코드·주석도 스캔 대상이다. `LC_ALL=C` 필수 |
| CLAUDE.md 규칙 | 코드에 속하는 텍스트는 **프로젝트 기존 관례**를 따른다 |

### 3. 복제 제거를 마친 뒤 `compose` 를 한 번 더 돌려라

편5에서 용어표 뜻풀이 넷과 산문 넉 줄을 다시 쓰자 **14,999 → 15,231 (+232 B)** 이 됐고 밴드
상단 여유가 **296 에서 65 로** 줄었다. 원본과 겹치지 않게 고쳐 쓴 문장이 원래보다 길어진다.
편3의 리뷰 반영이 +292 B 였으므로 **65 B 로는 리뷰를 받을 수 없다.**

⇒ **초고 직후의 값으로 밴드를 판정하지 마라.** 복제 제거 → `compose` → 필요하면 삭감 순이다.

### 4. 편5가 편6을 예고한 낱말을 `grep` 으로 확인하라

편4가 물린 규칙이다(설계서 §10-4-f). 편5 말미의 예고는 이렇다.

> 다음 편은 이 문서들을 실제로 만드는 단계에 넘긴다. 무엇이 갖춰져야 만들기 시작할 수
> 있는지, 그리고 **요구사항 문서**를 매번 다시 쓰지 않도록 **표준으로 굳히는** 방법이
> 거기서 갈린다.

⚠️ **「굳히는」이 편8(스킬화)과 겹쳐 읽힐 수 있다.** §4-1 은 편6을 「딜리버리 체크리스트 +
PRD Writer」로, 편8을 「스킬화와 Package-and-Ship」으로 갈라 놓았다. 편6 본문이 **스킬로 만드는
과정**까지 들어가면 편 경계를 넘는다. 넘어갈 것 같으면 편5의 예고 문장을 고쳐라.

### 5. 편6 발행 시 편5에 다음 편 링크를 단다

이 시리즈의 관례다 — 발행 시점에는 링크 없이 예고만 하고, 다음 편이 나올 때 링크를 건다.
편3·편4가 모두 그렇게 처리됐다. 편5 마지막 문단의 「다음 편은」을
`[다음 편](/blog/ai-product-planning/deliver-and-prd/)` 으로 바꾼다.

---

## 편6 사양 요약

| 항목 | 값 |
| --- | --- |
| 슬러그 | `deliver-and-prd` |
| 원본 절 | `pm-harness/03` **§6 + §8** · 9,333 B |
| 목표 · 밴드 | **19,767 B** · 18,460 ~ 21,075 |
| 태그 | `prd` `claude-code` |
| 링크 | 앞 편 **`architect-stage`** · 지도편. 다음 편 링크는 편7 발행 때 붙인다 |
| 주제 경계 | 딜리버리 체크리스트 + PRD Writer. 🔴 **스킬로 굳히는 과정은 편8이다** |
| 용어 배정 | 지도편 기준 `쓰기 차단 훅`(gate_guard) · `완료의 정의`(DoD) · `소리 내어 실패하기`(편4와 공유) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **A-7 은 밴드 하단 여유가 305 B 뿐이다.** 초고가 **13,300 B 를 밑돌면 반려**한다. 인접 편에
  합치지 말고 원본 인용을 표로 펴서 채운다. 🔴 **경력 메모로 채우는 것은 금지다**(설계서 §11-3).
- **재환산 지점이 하나 남았다** — **B 편1 뒤**다(설계서 §3-6). 고정 비용이 시리즈 경계에서
  리셋되는지 아무도 모른다. **B 편1 초고 `compose` 를 보기 전에 B 편2 를 쓰지 마라.**
- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  시리즈 B 가 없어 편6 시점에 걸 수 없다(설계서 §6-2). **B 시리즈 발행 때 되돌아와 건다.**
- **`user-research` · `career` 태그가 싱글톤으로 남는다.** 글에 없는 내용으로 태그를 맞추지 마라.
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관하다.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 넘으면 `docs/changelog/YYYY-MM.md` 로 옮기고 상단
  인덱스 표를 갱신한다. 이번 세션에 41개 절을 옮겼고 **절 제목 44개 전부 일치**로 손실을 확인했다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| **`grep -c` 가 도식 수를 절반으로 보고** | 산출물에서 `grep -c mermaid` 가 **1**을 냈는데 도식은 2개다. 두 표지가 한 줄에 있었다 | `grep -o … \| wc -l` 로 **출현 횟수**를 세고, **직전 편을 대조군**으로 같은 값이 나오는지 확인한다 |
| **복제를 지우면 분량이 는다** | 축자 복제 8곳을 고쳐 쓰자 +232 B. 밴드 상단 여유가 296 → 65 | 복제 제거 뒤 `compose` 재실행 |
| **`sed` 가 상대 경로를 잃는다** | 앞 명령의 `cd` 뒤 셸 작업 디렉터리가 리셋돼 설계서를 못 읽었다 | 리포 파일은 **절대 경로**로 열거나 `cd` 를 같은 명령에 붙인다 |
