# HANDOFF

> 마지막 갱신 2026-09-04 · `pm` 배치 **시리즈 A 9/9 · 시리즈 B 8/8 완결** ·
> **발행본 184편은 0회를 유지한다. 리포 안의 문서에 250회가 남아 있다 — 기전 ②의 조건을 정정했다**

## 지금 상태

발행본 **184편**이다. 배치는 편8로 끝났고, 그 뒤 세 세션이 **검사기의 사각지대**를 닫아 왔다.
직전 세션이 마크업 문법이라는 축을 세워 발행본 306회를 비웠다면, **이번 세션은 그 검사기를
발행본 밖으로 돌렸다 — 그리고 리포 문서에 250회가 남아 있음을 확인했다.**

| 항목 | 값 |
| --- | --- |
| 발행 완료 | **A 편1~9 + B 편1~편8 (184편)** · 시리즈 B 완결 |
| 검사기 | ✅ **여섯 종** · 뮤턴트 **28/28 잡힘 · 생존 0** · 훅 **5단** |
| 강조 렌더 실패 (발행본) | ✅ **0회 · 184편** — 직전 세션이 306회를 비웠다 |
| 강조 렌더 실패 (리포 문서) | 🔴 **250회 · 28개 파일** — 문서 125개를 처음 검사해 드러났다 |
| 다음 단계 | 🔴 **아래 0번** — 문서 250회의 교정 범위를 사용자가 정해야 한다 |
| 푸시 · 배포 | ✅ **9커밋을 `origin/main` 에 반영** (`1615c53..17b1abd`) — 아래 4번 참조 |
| 커밋 | 직전 5개(`0968901` `276eb19` `5d6a8f4` `d6ebab2` `b781e4f`) **+ 이번 4개** (`6f13063` `a6dcd39` `6cf0e91` `17b1abd`) |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` · **§10-17 까지 있으면 편8이 끝난 것이다** |

### 이번 세션이 한 일

| # | 무엇 | 어디에 남았나 |
| ---: | --- | --- |
| 1 | **교정기에 `--files` 를 붙였다** — 카테고리 밖의 문서도 고친다 | `scripts/fix-markup.mjs` |
| 2 | 🔴 **기전 ②의 조건이 좁게 적혀 있었다** — 「줄 끝」은 필요 조건이 아니다 | 아래 2번 · `docs/TOOL-TRAPS.md` **42번** |
| 3 | 문서 두 곳의 렌더 실패 교정 | `docs/changelog/2026-09.md` · `docs/TOOL-TRAPS.md` |
| 4 | 🔴 **리포 문서 125개를 처음 검사했다** — 28개 파일 **250회** | 아래 0번 |
| 5 | README 스크립트 표에 검사기 **7행 추가** — 표가 실제와 어긋나 있었다 | `README.md` |
| 6 | 🔴 **아카이브가 `check-counts` 를 깨뜨렸다** — 검사기를 고쳤다 (자기 검사 14 → **18**) | `scripts/check-counts.mjs` · 아래 3번 |
| 7 | 발행본 184편 재검사 — **0회 유지** 확인 · 뮤턴트 **28/28** | `npm run check-markup` · `npm run mutate` |
| 8 | 🔴 **직전 인수인계의 「푸시 완료」가 사실이 아니었다** — 9커밋이 한꺼번에 올라갔다 | 아래 4번 |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 0. 🔴 **리포 문서에 250회가 남아 있다 — 범위를 정해야 한다**

발행본은 0회지만, **검사기를 발행본 밖으로 처음 돌려 보니** 문서 125개 중 28개 파일에
250회가 있었다. GitHub 에서 읽히는 문서이므로 화면에 별표가 그대로 찍힌다.

| 어디 | 파일 | 회 |
| --- | ---: | ---: |
| `docs/superpowers/plans/` (설계서) | 11 | 144 |
| `docs/changelog/2026-08.md` | 1 | 42 |
| `docs/superpowers/reports/` · `specs/` · 진행 보고 | 10 | 40 |
| `docs/product-lead-loadmap/` | 4 | 10 |
| 기타 (`site-renewal-completion-report` · 체크리스트) | 2 | 14 |

⚠️ **전량 자동 교정을 돌리지 마라.** 교정기는 `--category` 없이는 동작하지 않도록 막아 두었고,
`--files` 도 경로를 명시해야 한다 — 검토 없이 문서 28개를 바꾸는 명령은 있어선 안 된다.
설계서는 다른 문서가 **번호로 인용**하므로 강조 위치가 바뀌면 인용이 어긋날 수 있다.

**남은 후보는 다음과 같다.**

| 후보 | 근거 |
| --- | --- |
| 🔴 문서 250회 교정 | 위 표. **문서 갈래별로 나눠** 진행하는 것이 안전하다 |
| 훅·CI 에 문서 검사 추가 | 지금 훅은 `content/blog/` 만 본다. 문서는 아무도 보지 않아 250회가 쌓였다 |
| 분량 하한 SOFT **5편** | `ai-agent` 1 · `rag` 1 · `search-engineering` 3. 이 배치 밖의 옛 편들이라 손대려면 별도 판단이 필요하다 |
| 시리즈 A·B 밖의 새 배치 | 원본이 어디에 있는지부터 정해야 한다 |
| 검사기의 다음 사각지대 | 지금 여섯 종이 보는 것은 낱말 · 겹침 · 분량 · 개수 · 마크업이다. **링크 유효성**과 **도식 문법**은 아무도 보지 않는다 |

### 1. 🔴 산출물 `grep` 의 숫자를 **위반 수로 읽지 마라**

인수인계에 「89개 페이지 · 409회」로 적혀 있었지만 실제 위반은 **306회**였다.

| 무엇 | 값 |
| --- | ---: |
| 산출물 `grep` | 409회 |
| **실제 렌더 실패** | **306회 · 75파일** |
| 코드 블록 안의 정당한 예시 | **109회** |

파일별 잔차 0건 · 역방향 0건으로 완전히 설명된다. 남은 6의 차이는 세션 초반에 잰 `out/` 이
**이전 세션의 낡은 빌드**였기 때문이며, 다시 빌드하니 사라졌다.

⇒ 🔴 **「무엇을 읽었는지」를 먼저 확인하라**(직전 인수인계 9번의 재확인이다). 그리고
**산출물을 세기 전에 빌드가 지금 소스를 담았는지 새 문구로 증명하라.**

### 2. 🔴 ②의 조건이 **실제보다 좁게 적혀 있었다** — 「줄 끝」은 필요 조건이 아니다

직전 인수인계는 기전을 둘로 적으면서 ②에 「닫는 `**` 가 **줄 끝**」을 조건으로 달았다.
**이번에 그 조건 없이 터지는 사례를 만났다.**

| # | 조건 | 정규식으로 | 실측 |
| ---: | --- | :---: | ---: |
| ① | CommonMark right-flanking — 닫는 `**` 앞이 구두점, 뒤가 문자 | 잡힌다 | 141쌍 |
| ② | 🔴 **GFM 취소선** — 물결 둘이 강조를 **가로지르면** 여는 별표를 삼킨다 | **안 잡힌다** | 3쌍 + 이번 1쌍 |

②는 취소선이 물결 사이를 먼저 가져가면서 **여는 별표를 삼키는** 것이다. 이번 사례는
닫는 별표 뒤가 **쉼표**였는데도 터졌다 — 「줄 끝」은 당시 실측된 3쌍의 우연한 공통점이었을
뿐이다. 범위 표기(`①~③`)를 쓰는 이 리포에서 구조적으로 재발한다.

```
✗ 배정 범위(§0~§3, 11~172행)가 아니라 **1~172행이고**, 818 은   ← 화면에 별표가 보인다
```

물결표가 괄호 안과 강조 안에 **둘 다** 있어야 짝이 강조를 가로지른다. 하나만으로는
걸리지 않으므로, 물결표를 세는 것만으로는 판정할 수 없다.

🔴 **이스케이프와 강조 축소가 모두 듣지 않았다** — 둘 다 2회를 그대로 냈다. 듣는 것은
**강조 안에서 물결표를 없애는 것**(말로 풀거나 인라인 코드로 감싸기)뿐이다.
재현과 대조군은 `docs/TOOL-TRAPS.md` **42번**에 있다.

```
✗ 가~나 계보, 다 적용. **라~마 안 보인다.**      ← 화면에 별표가 보인다
```

⇒ 🔴 **판정을 근사하지 마라. 페이지를 그리는 파서가 이미 리포에 있다.**
`react-markdown` 의 micromark 계열을 직접 돌리면 근사가 아니라 정답이고, 코드 블록과
인라인 코드가 **제외 코드를 따로 쓰지 않아도** 빠진다.

### 3. 🔴 CHANGELOG **아카이브가 개수 검사기를 깨뜨린다** — 고쳤지만 원리를 알아 둘 것

본체는 최신 3개 절만 둔다. **발행이 없는 세션이 절을 하나 쓰면** 절이 넷이 되어
마지막 발행 기록이 아카이브로 밀려나고, `check-counts` 의 ④가 「찾지 못했다」로 죽는다.

| 무엇 | 뜻 |
| --- | --- |
| 「찾지 못했다」 | 배치를 **기록하지 않았다** 또는 아카이브가 **정상 동작했다** |
| 둘을 같은 실패로 내면 | 다음 사람이 **멀쩡한 CHANGELOG 를 고치려 든다** |

⇒ 검사기가 본체 뒤에 월별 아카이브를 **최신순으로** 이어 붙여 보게 했다
(`chainChangelog` · `readArchives`). 파일명이 `YYYY-MM.md` 라 문자열 정렬이 곧 날짜
정렬이며, **순서가 곧 판정**이다 — 옛 것부터 붙이면 낡은 값을 최신으로 읽는다.

🔴 **되돌려 확인했다.** 이어 붙이기를 `return bodyText` 로 무력화하니 새 케이스 둘이
실제로 FAIL 했다. 나머지 둘은 뮤턴트에서도 통과했는데, 그 둘은 ④의 「처음 만나는 값」
규칙을 지키는 케이스라 정상이다. **통과만 보고는 케이스가 헛도는지 알 수 없다.**

### 3-b. ✅ 이 교정은 **바이트를 바꾸지 않는다**

조사를 강조 안으로 옮기는 것은 글자를 더하지도 빼지도 않는다(실측 23자 → 23자 · 47B → 47B).
72개 파일 가운데 바이트가 바뀐 것은 **전각 물결표를 쓴 한 파일의 +6 B** 뿐이며, 이는
3바이트 문자 셋이 1바이트 셋을 대체한 값과 정확히 맞는다.

⇒ **편별 발행 B · 역산 k · 표 예산을 다시 잴 필요가 없다.** 아래 「편별 실측」 표는 유효하다.

### 4. 🔴 인수인계의 「푸시 완료」를 **믿지 마라** — 실제로 안 되어 있었다

직전 인수인계는 상태 표에 「**이번 세션 5커밋까지 `origin/main` 반영**」이라고 ✅ 로
적어 두었다. 이번에 푸시하니 그 5커밋이 **함께 올라갔다.**

```
1615c53..17b1abd  main -> main      ← 9커밋. 직전 5 + 이번 4
```

| 인수인계가 적은 것 | 실제 |
| --- | --- |
| 5커밋 `origin/main` 반영 ✅ | 원격의 끝은 **`1615c53`** — 그 5커밋은 로컬에만 있었다 |

**왜 이렇게 되나.** 커밋과 푸시는 다른 명령이고, 이 리포는 `main` 이 보호 브랜치라
**푸시에 명시적 요청이 필요하다.** 그래서 「커밋했다」와 「푸시했다」가 갈리는데,
인수인계를 쓰는 시점에는 둘이 같아 보인다. 배포는 푸시에만 걸리므로 **다섯 커밋 분량의
변경이 배포되지 않은 채로 있었다.**

⇒ 🔴 **인수인계를 쓰기 전에 `git status -sb` 를 읽어라.** `## main...origin/main` 뒤에
`[ahead N]` 이 붙어 있으면 푸시되지 않은 것이다. 「커밋했다」를 「반영했다」로 적지 마라.

### 4-b. 🔴 조사의 경계를 기계가 안다고 전제하지 마라

교정기는 **알려진 조사일 때만** 옮기고, 아니면 손볼 자리로 남긴다.
「`**「가」**한국은`」에서 뒤의 한글을 통째로 옮기면 강조가 낱말을 삼킨다.

방어가 둘이다 — **조사 목록에 있는가**, 그리고 **그 조사 뒤가 또 한글은 아닌가**.
후자가 `**「가」**만족스럽다` 의 「만」과 `**「가」**다음 단계` 의 「다」를 걸러 낸다.

⚠️ **목록은 완전하지 않아도 된다.** 빠짐은 오교정이 아니라 **수작업**으로 나타나기 때문이다.
실측 12곳 가운데 11곳이 서술격·인용격 조사였고, 그것을 넣자 2곳으로 줄었다.

### 5. 🔴 뮤테이션이 **거짓 방어와 부정확한 주석**을 잡는다

| 무엇 | 어떻게 드러났나 |
| --- | --- |
| 역방향 반복이 아무것도 지키지 않았다 | 「앞에서 고치면 열이 밀린다」고 주석에 적어 뒀는데 앞에서부터 도는 뮤턴트가 **생존했다** ⇒ 주석을 사실로 고치고 뮤턴트를 뺐다 |
| 여는 별표를 교정 대상으로 세고 있었다 | 손볼 자리가 **12곳에서 165곳으로** 부풀어 진짜 12곳이 묻혀 있었다 |

⇒ **생존은 「그 자리를 아무도 지키지 않는다」이거나 「지킬 것이 애초에 없다」이다.**
후자면 코드가 아니라 **주석이 거짓말을 하고 있는 것**이므로, 뮤턴트가 아니라 주석을 고친다.

⚠️ **「치환 실패」는 신호다.** 조사 목록을 늘리자 F7 이 대상을 잃었다 — 러너의 고장이 아니라
**그 자리를 다시 볼 때가 됐다는 뜻**이라 F7 을 다시 짜고 F8 을 더했다.

### 6. 🔴 검사기는 **실행 파일이자 모듈**이다

`import.meta.url` 가드가 없어, 함수 둘을 가져다 쓴 스크립트가 전량 스캔 출력 **35KB** 를
뒤집어썼다. 교정기가 판정을 다시 구현하지 않고 검사기의 함수를 그대로 쓰려면 이 가드가 있어야
한다 — **판정이 둘로 갈리면 「검사기는 잡는데 교정기는 못 고치는」 자리**가 생기고, 그 자리는
어느 쪽 자기 검사에도 걸리지 않는다.

### 7. 🔴 보고하는 **줄 번호가 파일 기준인지** 확인하라

프론트매터를 지운 탓에 실제 48행의 위반이 **37행**으로 보고됐다. 고치러 간 사람은 엉뚱한
줄을 연다. **지우지 말고 같은 수의 빈 줄로 바꾼다** — 위반이 프론트매터 안에 있을 수는
없으므로 내용은 버려도 되지만 **줄 수는 버리면 안 된다.**

### 8. 🔴 줄바꿈 함정을 **양방향으로** 겪었다

| 방향 | 무엇이 일어났나 | 대응 |
| --- | --- | --- |
| **거짓 경보** | `git show` 가 LF 정규화본을 내어 14개 파일이 「+수백 B」로 보였다 | **미변경 파일을 대조군**으로 세어 원래 CRLF 였음을 확인 |
| **실제 오염** | `Edit` 도구가 훅 파일을 통째로 CRLF 로 재작성했다 | `tr -d '\r'` 로 되돌림 |
| **실제 오염** | 스크립트로 넣은 두 줄만 LF 라 `CLAUDE.md` 에 혼재가 생겼다 | 전체를 CRLF 로 통일 |

⇒ **바이트 비교는 `tr -d '\r'` 로 줄바꿈을 맞춘 뒤에 하고**, `git diff --shortstat` 의 변경
줄 수로 CRLF 전환 여부를 가른다(전환이면 파일당 수백 줄이 나온다).

### 9. 훅은 **좁게 시작해 넓힌다**

옛 편 306회가 남은 동안에는 「이번 커밋이 건드린 파일만」이었다. 전량을 걸면 콘텐츠 커밋이
전부 막히고 `--no-verify` 로 우회하게 되어 훅이 무력해지기 때문이다.
전량이 0 이 된 지금은 **184편 전량**을 본다 — 좁게 보면 다른 편을 깨뜨리는 변경이 지나간다.

---

## 검사기 여섯 종 — 순서는 「증명한 뒤 스캔」이다

| 검사기 | 무엇을 보나 | 자기 검사 |
| --- | --- | ---: |
| `check-forbidden` | 금칙어 (소스 · 산출물) | 63 |
| `dup-scan` | 발행본끼리의 축자 복제 | 12 |
| `source-overlap` | 리포 **밖 원본**과의 겹침 | 42 |
| `compose` | 성분 바이트와 규약 라벨 | 15 |
| `check-counts` | README 의 발행본 수 | — |
| `check-markup` | **렌더되지 않는 강조** · `--files` 로 발행본 밖도 본다 | 19 |
| `fix-markup` | 위 위반의 교정 · 🆕 `--files` 로 문서도 고친다 | 19 |

**뮤턴트 28개 · 잡힘 28 · 생존 0 · 치환실패 0.** `npm run mutate` 로 언제든 재현된다.

---

## 시리즈 B 사양 요약 — **완결**

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (편1~편8 발행 완료) |
| 원본 | `C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/` |
| 산식 | **목표 B = 2,455 + 원본 B × 2.05 + 링크 몫** · 밴드 k ± 0.14 (1.91 ~ 2.19) |
| 역산 k | **(발행 B − 2,455 − 링크 몫) / 원본 B** (§10-14-b) |
| 원본 B 정본 | **배정 절의 행 추출 − 「💼 면접 활용」 블록의 인용 줄만**(§10-14-c) |
| 표 예산 | **4,485 ± 335** (절대값) |
| 도식 예산 | **원본 도식 B × 1.263** (계수) |
| 링크 몫 | **슬러그 길이 + 12 + 카테고리 길이** — 편5→6 · 6→7 · 7→8 세 번 검증됐다 |
| 🔴 낱말 | 「기획」은 시리즈 A 와 뜻이 다르다. 편1이 갈라 뒀고 편2~편8 이 그 용법을 따랐다 |

### 편별 실측

발행 B 는 **현재 파일 크기**(`compose` 규약 = `wc -c` + 1)이며 리뷰 반영과 링크 부착이
누적된 값이다. **이번 세션의 강조 교정은 이 값을 바꾸지 않았다**(위 3번).

| 편 | 원본 B | 발행 B | 링크 몫 | 역산 k | 초고 이탈 | 감축·증량 회차 | 리뷰 반영 |
| :---: | ---: | ---: | ---: | ---: | ---: | :---: | ---: |
| 2 | 6,727 | 17,186 | 55 | 2.182 | — | — | +243 |
| 3 | 5,578 | 14,745 | 55 | 🔴 2.193 초과 | — | — | +551 |
| 4 | 8,118 | 19,409 | 57 | 2.081 | +13.9% | 2 | +344 |
| 5 | 7,834 | 19,500 | 56 | 2.169 | — | 3 | 발행 전 |
| 6 | 7,389 | 18,535 | 65 | 2.169 | 🔴 +28.3% | 6 | 🔴 **+822** |
| 7 | 7,511 | 17,973 | 57 | 2.058 | 🔴 −14.8% | 3 | 🔴 −361 |
| **8** | **9,430** | **22,456** | **0** | **2.121** | 🔴 **+22.6%** | **8 + 재감축 2회** | 🔴 **+136 → 감축** |

⚠️ 편8의 링크 몫 0 은 **미달이 아니다 — 다음 편이 없다.** 링크가 붙지 않은 편은 **링크를 뺀
설계값과 비교**한다(§10-14-a).

---

## 다음 배치를 시작하면 다시 읽을 것

배치 작업이 끝나 지금은 쓰이지 않지만, 새 배치에서 **그대로 되풀이될** 것들이다.

- 🔴 **`compose` 는 「본문을 마지막으로 만진 뒤」 돌린다.** 복제 제거뿐 아니라 **리뷰 반영**도
  분량을 늘린다 — 편8에서 상한을 세 번 넘겼고 원인이 매번 달랐다. **표 예산이 총량보다 먼저
  터진다**(표 여유가 편6 296 → 편7 65 → 편8 **13** 으로 좁아져 왔다).
- 🔴 **인수인계에 적힌 수치도 대조군 없이는 근거가 아니다.** 편8 착수 때 원본 B 9,682 가
  틀렸다(실측 9,430). **경계는 줄 수가 아니라 「인용 줄이 끊기는 지점」으로 찾아라.**
- 🔴 **금칙어 때문에 뺀 블록은 낱말만 빼면 되는 것이 아니다.** 배정 밖 블록의 **판단**이
  본문으로 새는 일이 편6·편7·편8 **세 편 연속**이었다. 집필 전에 「이 블록에서 온 판단은
  쓰지 않는다」를 **명제 단위로** 적어라.
- 🔴 **`dup-scan` 0건은 원본 대조의 0건이 아니다.** 편8은 `dup-scan` 7건을 고친 **뒤에**
  `source-overlap` 이 편7의 4.6배를 냈다. **표를 전치할 때 칸이 증발한다** — 전치 뒤에는
  원본과 칸 수를 대조하라.
- 🔴 **산식의 상수 2,455 는 상수 절의 예산이 아니다.** 회귀 절편이며, 상수 절의 초과분은
  배율 안에 이미 흡수돼 있다. **산식의 항을 문서의 부위에 대응시키지 마라.**
- **초고가 어느 쪽으로 벗어나는지는 원본의 표 비중이 정한다.** 편8은 이것을 예측에 썼고
  「약 +23% 넘친다」가 실측 +22.6% 로 맞았다.

---

## 알려진 문제

- 🔴 **`compose` 통과는 배정 타당성이 아니다.** 성분 실측 **바로 다음에** 배정 원본의 금칙어를
  직접 세라. `LC_ALL=C` 필수다.
- 🔴 **표 배율을 `원본 표 B × 배율` 로 예측하지 마라.** 표는 **절대값 예산**으로 관리한다.
- SOFT 2건이 `agentic-coding/thirty-agent-catalog.md:195` 에 상시 남아 있다 — 정당하다.
- 분량 하한 SOFT 5편(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은 이 배치 밖의 옛 편들이다.
- 「강의」는 SOFT 금칙어다. 원본이 강의 노트라 **출처를 지칭하면 걸린다.**
- 배치 전체 `dup-scan` 20건은 전부 용어 정의 · 법률명이며 정당하다.

---

## 도구 함정 — 이번 세션에서 실제로 겪은 것

`docs/TOOL-TRAPS.md` 가 전체(**42건**)를 담고 있다. 이번에 새로 걸리거나 다시 걸린 것만 적는다.

| 함정 | 이번 세션에서 |
| --- | --- |
| 🆕 **물결표 둘이 강조를 가로지른다** (42번) | 이스케이프도 강조 축소도 듣지 않았다. **강조 안에서 물결표를 없애야** 한다 |
| 🆕 **함정을 설명하는 문서가 그 함정에 걸린다** | 42번을 쓰면서 별표를 문자로 언급한 줄이 강조로 파싱됐다. **별표를 본문에 적을 때는 인라인 코드로 감싼다** |
| 🔁 **`grep -r` 이 `out/` 과 `node_modules` 를 훑는다** | `package.json` 과 환경변수를 한 명령으로 보려다 **120초 타임아웃**. `Grep` 도구로 갈아탔다 |
| 🔁 **파이프 뒤의 종료 코드** | `process.exit(2)` 를 확인하려는데 `\| tail` 때문에 **0** 이 왔다. 종료 코드를 읽을 명령은 **단독 실행** |
| **`Edit` 도구가 파일을 통째로 CRLF 로 재작성한다** | 이번에는 전부 스크립트로 고치고 **`tr -cd '\r' \| wc -c` 를 매번 확인**해 0 을 유지했다 |
| **`grep` 이 한글 파일을 바이너리로 본다** | `grep -n` 이 「Binary file matches」만 냈다. **`-a`** 를 붙인다 |
| **`grep -c` 는 줄 수를 센다** | 출현 횟수는 `grep -o … \| wc -l` |
| **`core.autocrlf` 경고** | 커밋마다 뜬다. 정상 동작이며 **깨끗한 diff 는 증거가 아니다** |
| **`AskUserQuestion` 이 길면 파싱에 실패한다** | 옵션 설명을 짧게 유지했다 |
