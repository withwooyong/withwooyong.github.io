# 인수인계 (HANDOFF)

**갱신일**: 2026-08-09
**브랜치**: `main` · 최신 커밋 `a568c2e`
**작업트리**: 이 문서 · `CHANGELOG.md`만 미커밋 / stash 없음
**발행본**: **97편** (`ai-agent` 51 · `rag` 25 · `agentic-coding` 15 · 기타 6) · sitemap **158 URL**

---

## 1. 이번 세션에서 한 일

**`agentic-coding` B4를 발행했다(편6~8, 26).** 그리고 **설계서 진단 6건을 정정했다 — 4배치 중 최다다.**

```
조사 3기 병렬 ──→ 컨트롤러 실측 ──→ 브리프 ──→ 변환 1기 ──→ 검증 4축 ──→ 반영 ──→ 재검증 2기 ──→ 2차 반영 ──→ 최종 검증 ──→ 커밋
     │                  │                                        │                          │
 ┌───┼───┐         정정 6건 발견            ┌────┬────┬────┼────┐          ┌──────┴──────┐
원본  발행본  규격                          축A   축B   축C   축D        반영 정확성   실행
실측  대조   태그                          틀림  없음  누락  실행         (opus)      (sonnet)
```

| 단계 | 산출 |
| --- | ---: |
| 변환 4편 | 93,964 B · 도식 10 · 표 63 |
| 검증 4축 | 지적 **31건** (중복 제거 후 24) |
| 1차 반영 | 24건 전량 |
| 재검증 2기 | 「사라졌는가」 24/24 · **신규 결함 7건** |
| 2차 반영 | 7건 전량 |
| 최종 | **103,653 B · 배율 1.77** |

### 결함이 또 이동했다 — 이번엔 「수정이 만든 것」으로

| 유형 | B1 | B2 | B3 | **B4** |
| --- | ---: | ---: | ---: | ---: |
| 수치 불일치 | 5 | 0 | 0 | **0** (약 40종 대조) |
| 원문 강도 어긋남 | 한 방향 | 양방향 6 | 0 | **3** |
| 신규 도식의 거짓 관계 | — | — | 1 | **0** (화살표 9개 전부 근거 있음) |
| 분산 지시 부분 미이행 | — | — | 2 | **0** (편별 체크리스트가 막았다) |
| **원문에 대한 부정 사실주장 실패** | — | — | — | **2** ← 처음 |
| **전칭→분류 전환 시 미배정** | — | — | — | **1** ← 처음 |
| 설계서 진단 오류 | 2 | 1 | 1 | **6** |
| 팽창률 | 1.76× | 1.42× | 1.31× | **1.77×** |

### ⚠️ 이번엔 컨트롤러 브리프 오류가 4건이었다

| # | 브리프가 쓴 것 | 실제 | 잡은 자리 |
| --- | --- | --- | --- |
| 1 | 가상 실습 소재를 편6·7에 배정 | `TechCorp`·`NextLevel`은 **발행 제외 절에만** 존재 | 변환자 |
| 2 | 시점 표기 `2026-04` | 원본 12편 전부 **`2026-07-26`**. `03`에 `2026-04` 0건 | 변환자 |
| 3 | 안티패턴 「5종」 | **6종** | 축 C |
| 4 | **「원 자료는 개별 수치와 출처를 행 단위로 연결하지 않았다」** | 원문 L145에 **`(Zapier·Tines 등)`** 실재 | **축 B** |

**4번이 가장 무겁다 — 브리프가 거짓 각주 문안을 제공해 최상위 결함 2건을 직접 만들었다.**

> 조사자가 *"수치 줄과 URL 줄이 **겹치지 않는다**"* 고 보고했고, 그것은 **위치 비교이지 내용 확인이 아니었다.**
> 컨트롤러가 그것을 확정으로 옮겼고 변환자는 브리프 문안을 따랐다. **L145 괄호를 아무도 열어 보지 않았다.**
>
> **「매칭이 없다」는 부정 명제다. 위치가 겹치지 않는다는 관측으로는 증명되지 않는다.**

**오류 4건의 공통점: 원본을 열지 않고 상위 문서(설계서·조사 보고)를 신뢰한 자리만 틀렸다.** 컨트롤러가 원문을 직접 연 6건(§10-2 6행 · `Plan Mode` 면접 절 · L631 · L1084 · 200줄 모순 · 강의 10건)은 전부 맞았다.

---

## 2. 다음 세션이 이어받을 것

### 최우선: B5 착수 (`07` → 편15~18)

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md)
— **§4-4·§5-3·§5-4·§6-1·§6-3·§6-4·§6-5·§9-1·§10·§11에 정정 블록이 있다. 그것부터 읽어라.**

| 배치 | 대상 | 편 | 편수 | 상태 |
| ---: | --- | --- | ---: | --- |
| ~~B1~~ | ~~`01`(축약) + `02`~~ | ~~1~5~~ | ~~5~~ | ✅ 완료 (`388519e`) |
| ~~B2~~ | ~~`04`~~ | ~~9~11~~ | ~~3~~ | ✅ 완료 (`f4b9c30`) |
| ~~B3~~ | ~~`05`~~ | ~~12~14~~ | ~~3~~ | ✅ 완료 (`cf948a1`) |
| ~~B4~~ | ~~`03` + `team-productivity`~~ | ~~6~8, 26~~ | ~~4~~ | ✅ **완료** (`a568c2e`) |
| **B5** | `07` | 15~18 | 4 | **다음** |
| B6 | `08`(개발축) | 19~21 | 3 | 편21 자기참조 **미채택 확정** |
| B7 | `09` | 22~25 | 4 | §6-5 10번 표 캡션 승격 |
| B8 | Q&A 4편 | Q1~Q4 | 4 | `05 §10-1` 10문항이 여기로 이월됐다 |
| B9 | 지식 지도 | — | 1 | **맨 마지막** |

### B5 편 명세

| 편 | series / order | slug | 원본 절 | 원본 B | 도식 |
| ---: | --- | --- | --- | ---: | ---: |
| **15** | `agent-operations` 1 | `automation-scope-criteria` | `07` **§0 전량** §1 §2서두~§2-2 | 14,872 | **2** |
| **16** | `agent-operations` 2 | `agent-jd-triggers` | `07` §2-3 §2-4 §2-5 §3 | 14,500 | 6 |
| **17** | `agent-operations` 3 | `troubleshooting-three-layers` | `07` §4 | 11,786 | 2 |
| **18** | `agent-operations` 4 | `scaling-routing-collapse` | `07` §5 §6 §7 | 19,200 | 3 |

`07` 전체 67,316 B · H2 10 · mermaid 13 · 표 53 · **가짜 H2 0**. `agent-operations` 시리즈 신설.

### B5 고유 사항 — 다른 배치에 없는 것

| # | 사항 | 근거 |
| --- | --- | --- |
| 1 | **§5-4 1번 「60줄」이 여기서 나온다** | `07 §2-2` 절 제목이 「CLAUDE.md 설계 — 60줄 원칙」(항목당 6줄 × 10항목). **편6이 확정한 「200줄 = 준수율 임계」와 다른 것을 재고 있으며 그 구분은 원본이 한 주장이다. 편15 ↔ 편6을 서로 링크한다** |
| 2 | **§5-4 2번 재시도 상한이 3회 ↔ 5회** | `07` L117(안정성 3요소) vs L259(외부 API 연동). **편15·편16이 각각 적용 범위를 문장으로 밝힌다.** 원본이 문맥을 달리 쓴 것이 실측으로 확인됐다 |
| 3 | **§6-6 개인 이력 제거가 다른 절의 근거를 무너뜨린다** | `07 §7-2`(1,410 B)는 **개인 사업 이력**(6개월 만에 PMF, 2주 만에 예상의 3배 청구, 30에이전트 운영)이라 CR-3.5로 제거가 맞다. **그런데 §7-4 반복 실패 패턴 4가지가 이 회고에서 도출됐다(L854).** 처리: **패턴 이름과 예방책만 남기고 절대 수치는 함께 버린다.** 「예상의 3배 청구」 → 「예산 초과」. **4항목 수는 유지**(→ 편18) |
| 4 | **§6-5 9번 `07 §9` L920(1,497 B) 시점 고지** | **편15~18의 해당 수치에 개별 각주.** 「전 편에 하나씩」이 아니라 **수치가 나오는 자리마다**다 |
| 5 | **§5-5 `00 §4` 교차참조표 오류 2건이 B5에 걸린다** | ① 「CLAUDE.md 작성」 항목에 **`07 §2-2`(4,255 B·25항목)가 통째로 누락**됐다 → 편15 ② 「비용 통제 1차」를 `07 §3`이라 했으나 **§3의 비용 서술은 2행뿐이고 본체는 `07 §5-5`** → 편18 |
| 6 | **편18은 상한 근접** | 19,200 × 1.7 ≈ 32KB 예상. **상한은 연성이다. 넘어도 된다** |

### 확정된 결정 (다시 묻지 마라)

| 사안 | 결정 | 근거 |
| --- | --- | --- |
| **편21 자기참조** — `08 §9`에서 이 리포의 CI·배포 관행을 근거로 쓸 것인가 | **쓰지 않는다** (사용자 승인 2026-08-08) | `deploy.yml`이 preview 없이 `main`=프로덕션인데 §9의 "자동 롤백 임계"와 대비되면 약점 공개가 된다 |
| **분량 상한** | **29.4KB는 합격선이 아니다. 넘어도 된다** | §4-0 원문. B3 편14 30,897 B · B4 편7 29,134 B |
| **시리즈 페이지** | 없다 | `series`는 frontmatter 메타데이터일 뿐 라우트를 만들지 않는다. B4에서 4편에 sitemap +4로 재확인 |
| **`05 §10-1`·`§10-3`** | 각각 **B8 이월** · **드롭** | 설계서 §4-4 |

### B5 브리프에 반드시 넣을 것

**B1~B3이 준 12건 + B4가 준 아래 5건.**

| # | 추가할 지시 | 근거 |
| --- | --- | --- |
| **25** | **「원 자료는 X를 하지 않았다」를 쓸 때는 X가 있을 만한 자리를 검색하고 그 근거를 문안에 남긴다** | 귀속이 붙어도 **2/2 실패**했다. 통과한 반례는 원문을 축자 인용하고 추론 경로를 노출했다 |
| **26** | **전칭(「전부」)을 분류로 바꿀 때는 항목을 전수 배정하고 세어라** | 「전부」는 반례 하나로, 「N갈래」는 미배정 하나로 무너진다 |
| **27** | **담당 원본의 L1~L3을 먼저 읽어 작성 기준일을 확인하라** | `2026-04`는 「강의 시점」 한정. 문서 기준일은 12편 전부 `2026-07-26` |
| **28** | **전방 참조를 옮길 때 히트 행이 발행 대상 절 안인지 확인하라** | `grep`은 면접 절과 본문을 구분하지 않는다. `03` L1056이 그래서 무효였다 |
| **29** | **추정으로 쓴 지시에는 「이건 추정이다」를 명시하라** | B4에서 표기한 1건은 변환자가 즉시 검증했고, 확신에 차서 쓴 2건은 발행 직전까지 갔다 |

**B1·B2·B3이 준 기존 12건**(요약문 수치 대조 / 원문 강도 상승 금지 / 수정 후 스캔 재실행 / 착수 전 표 밀도 측정 / `[실측]`·`[설계서]`·`[추론]` 표기 / 설계서 진단은 옮기기 전에 실측 / `tags.ts` 패싯 규칙 명시 / 재검증에 「사라졌는가」 / 상한은 합격선이 아님 명시 / 「이 글의 정리다」 귀속 / 분산 지시는 편별 체크리스트 / 신규 도식은 화살표마다 원문 근거)는 그대로 가져간다.

### 진행 방식 — 검증된 절차

```
1단계  분할 설계 → 승인                                    ← 완료
2단계  배치로 나눠 변환, 배치마다 커밋·보고                  ← B1~B4 완료, B5부터
3단계  구현 미참여 에이전트가 4축 독립 검증
4단계  지적 반영 → 재검증 2기 → 2차 반영 → 최종 실행 검증 → 커밋
```

**검증 4축은 그대로 쓴다.** 나누는 기준은 주제가 아니라 **결함이 드러나는 방식**이다.

| 축 | 무엇을 보는가 | 왜 별도 축이어야 하나 | 티어 |
| --- | --- | --- | --- |
| A | 쓰여 있는데 **틀린** 것 | grep으로 안 잡힌다. 세면 0건이라 자동 검증을 통과한다 | opus |
| B | 쓰여 있는데 **원문에 없는** 것 | 원문과 나란히 놓아야만 보인다 | opus |
| C | **없는** 것 | 읽어서는 안 드러난다. **원본 기준 역방향**으로 훑어야 한다 | opus |
| D | **실행하면 나오는** 것 | 종료코드는 객관적이다 | sonnet |

**B4에서 축 B의 값이 증명됐다.** 최상위 결함(「연결하지 않았다」가 거짓)은 **축 A·C 둘 다 통과**했다 — 축 A는 발행본에 **없는** 괄호를 대조 대상으로 삼지 않고, 축 C는 URL 유실은 잡았지만 **그 유실이 다른 문장을 거짓으로 만든다**는 연결은 보지 않는다.

**재검증은 2기로 나눈다.** B3에서 5건, **B4에서 7건**을 추가로 찾았다. 2배치 연속이다.

---

## 3. ⚠️ 설계서 정정 — B5 이후에 영향

**B4에서 6건을 정정했다. 전부 설계서 본문에 ⚠️ 블록으로 삽입돼 있다.**

| 절 | 정정 | 뒤 배치 영향 |
| --- | --- | --- |
| **§5-4** | 내부 모순 3건 → **4건**. `01` L452 「상한」 ↔ `03` L108 「하드 리밋 아님」 | **B5가 60줄을 다룬다.** 같은 값의 **성격**이 원본마다 다를 수 있음을 전제로 대조하라 |
| **§6-3** | `team-productivity` 3건 (L145 괄호 실재 · 분류 라벨 3건 · 안티패턴 6종) | 편26 완료. 뒤 배치 영향 없음 |
| **§6-4** | `2026-04`는 「강의 시점」 한정. 문서 기준일은 **`2026-07-26`** | **B5·B6·B7 전부.** 담당 원본 L1~L3을 먼저 읽어라 |
| **§6-5** | 3번 §10-2는 **6행** · 5번 소재 4종 중 **2종은 발행 범위 밖** · **L1084 항목 누락** · 11번 근거는 L631이 아니라 **L1103** | **§6-5 9번(`07 §9` L920)이 B5 대상이다. 그 절도 열어서 확인하라** |
| **§9-1** | `03` L1056 `Plan Mode`는 **§10 면접 절 소속 → 링크 불가** | **`04`·`07`·`09`의 전방 참조도 같은 방식으로 재확인하라** |
| **§11** | 금지선 **15·16** 추가 | 전 배치 |

### 앞 배치 정정도 유효하다

| 배치 | 정정 위치 | 요지 |
| --- | --- | --- |
| B1 | §6-1 | 「`강의가 ~한다` 130여 개」 → **실측 32** (4.1배 과대) |
| B1 | §10 | 팽창률 1.20 → **1.76** (B4에서 **1.77**로 재확인) |
| B2 | §5-3 ①②③ | 발행본 열거 기준 · MCP 인과 · 발행본 시점 |
| B3 | §6-5 8번 | `05 §11`은 출처 대장이 아니라 **면접 화법 가이드** |

**설계서의 진단은 브리프로 옮기기 전에 실측한다 — B1·B2·B3·B4. 4배치 연속이다.**

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 77점. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **2차 전체를 마친 뒤 재측정** |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | **어휘 81종 중 30종이 0편** | `redis` `kafka` `docker` `aws` `spring` `java` `nosql` `load-testing` `ci-cd` `deployment` `distributed-systems` `event-driven` `cdc` `cqrs` `microservices` `scalability` `vector-database` `machine-learning` `product-discovery` `prd` `product-metrics` `ab-testing` `user-research` `commerce` `payments` `global-service` `startup` `team-building` `career` `recommender-systems`. **어휘에 있는 것과 살아 있는 것은 다르다** |
| 7 | **훅 차단 채널이 두 발행본에서 다르다** | 편11은 **stdout JSON**, `settings-permissions-deny`는 **표준 에러**. 두 원본 사이의 부채. **편11에 고지를 달아 막아 뒀으나 근본 해소는 아니다** |
| 8 | **원본 `04 §8-2` 「3대 구성 요소」인데 표는 4행** | 원본에서 이월된 불일치. 편11에 해명만 달았다 |
| 9 | **원본 `05` 내부 수치 불일치** | L56 도식 `frontmatter 5+4 필드` ↔ L145 표 `핵심 5 + 확장 5`. 편12가 양쪽을 원문대로 옮기고 「원 자료 안에서 판정되지 않는다」로 명기했다 |
| 10 | **`05 §3-2` 제품 결함 3건의 현재 유효성** | 「버전 표기가 없다」는 사실은 명기했으나 **세 동작이 지금도 그런지는 확인하지 않았다** |
| 11 | **무귀속 판정 잔존 2건** | 편14 L232 · 편13 L235. B3·B4에서 손대지 않았다 |
| **12** | **B1 발행본 편2~5의 `2026-04` 표기 재확인** | **B4에서 발견.** `2026-04`가 「강의 시점」 자리인지 「문서 기준일」 자리인지 구분되지 않았다. `02` L171은 강의 시점이 맞으나 **설정 키·경로에 붙은 각주는 문서 기준일(`2026-07-26`)이어야 한다.** 기발행본이라 이번 범위 밖으로 두었다 |
| **13** | **편7 `feedback` 예시가 원본 MEMORY.md 예시와 소재 겹침** | **B4 재검증 `[추론]`.** 「원 자료가 가상 실습 소재로 열거한 목록에 이 표는 들어 있지 않다」는 **표 단위로는 참**이나, 그 표의 feedback 칸(원문 L354)이 원문 L963~965 MEMORY.md 예시와 같은 빌더 소재다. **손대면 새 검증 표면이 생겨 의도적으로 두었다** |
| **14** | **편8 용어표 「멀티턴 대화」 본문 0회** | **B4 재검증 실측.** 편7에 적용한 삭제 기준(본문 0회면 뺀다)을 적용하면 대상이나, 원지시가 편8은 「추가만」을 요구했다. **지시 위반은 아니다** |

### `ai-transformation`으로 이월된 위험 3건

`06`을 이관하면 조사자 D가 찾은 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표.** 발행하지 않는 선택지 우선 검토 |
| 2 | **`06`은 발행본과 서사 방향이 반대다.** 발행본은 "위임을 늘려라", `06`은 "어디서 사람을 세울 것인가". **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

**일곱 번째 이월이다.** README는 2026-08-07(`7f28866`) 이후 손대지 않았고 그 사이 **21커밋**이 쌓였다.

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 **97편·4카테고리**가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. 이 사이트가 쓰는 환경변수가 아니라 RAG·에이전트를 설명하는 **블로그 글 본문의 예시 코드**다. `.env.example`을 만들거나 README에 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이라 사람이 직접 부르지 않는다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)와 설계서 §13, 그리고 `CHANGELOG.md`의 B1~B3 항목에 있다. **아래는 B4에서 새로 나온 것이다.**

### 6-1. ⚠️ 귀속은 만능이 아니다 — B3 규칙이 절반만 참이다

B3는 *"「이 글의 정리다」 귀속을 붙이면 거짓이 될 수 없다"* 를 **6/6**으로 확인했다. **B4에서 깨졌다.**

| 귀속 문장의 형태 | B4 결과 | 왜 |
| --- | :---: | --- |
| ① **자기 해석** — 「이렇게 가르는 것은 이 글의 정리다」 | **전부 통과** | 진리조건이 「이 글이 그렇게 정리했다」로 축소된다 |
| ② **원문에 대한 긍정** — 「원 자료가 X를 했다」 | **전부 통과** | 원문 행을 대면 확인된다 |
| ③ **원문에 대한 부정** — 「원 자료는 X를 **하지 않았다**」 | **2/2 실패** | 귀속이 방패가 아니다. **원문 전체가 검증 대상이 된다** |

**같은 배치에 통과한 반례가 있다.** 편7 커맨드 8종 각주도 부정 주장인데 통과했다 —

> *"…다만 문서 말미에서 「도구의 세부 스펙은 버전에 따라 바뀐다」고 **일반론으로 적어 두었고**, 이 글은 그 일반 서술을 근거로 … **원 자료가 이 여덟 개를 짚어 버전 결속을 말한 것은 아니다.**"*

**차이는 하나 — 원문 문장을 축자 인용하고 자기 추론 경로를 노출했다.**

**2차 반영 후 부정 주장 14건을 전수 검사해 반증 0건**을 확인했다. 이 처방은 실효가 있다.

### 6-2. 전칭을 분류로 바꾸면 실패 모드가 바뀔 뿐이다

| | 반영 전 | 반영 후 |
| --- | --- | --- |
| 편6 문장 | 「일곱 행이 **전부** 판정할 수 없는 말」 | 「**세 갈래**로 갈린다」 |
| 왜 틀리나 | **반례 3행**으로 무너짐 | **미배정 3행**으로 무너짐 |

**근본 처방은 항목을 옮기는 것이 아니라 범주 이름의 추상 수준을 올리는 것이다.** 갈래 이름을 「버전과 커맨드가 빠져」(특정 결함) → 「판정은 되는데 내용이 비어 있는 말」(상위 개념)로 바꾸자 미배정 3행이 자연스럽게 들어갔다.

**같은 처방을 편7 로드 타이밍 분류에도 적용했다** — 세 값을 요약하지 않고 **표 문구를 축자 인용**하면 어떤 값도 밖으로 밀려나지 않는다. 요약하면 또 한 값이 나간다.

### 6-3. 「위치가 겹치지 않는다」는 내용 확인이 아니다

조사자가 *"정량 수치 줄(14·32·63…)과 URL 줄(137~145)이 겹치지 않는다"* 고 보고했고 **위치 비교가 맞았다.** 그런데 L145 제목 안에 `(Zapier·Tines 등)`이 있었다.

> **부정 명제(「매칭이 없다」)를 확정하려면 대상 전건의 내용을 읽어야 한다.**
> `grep -n "^- \[" <원본>` 한 번이면 잡혔다.

### 6-4. 지시에 「이건 추정이다」를 적으면 구현자가 검증한다

브리프 §3-2에 *"위 배정은 절 제목 기준 **추정**이므로 본문을 열어 확인한 뒤 옮겨라"* 라고 적었더니 변환자가 3건을 정정했다. **확신에 차서 쓴 2건(날짜·안티패턴 수)은 발행 직전까지 갔다.**

**지시의 확신도를 실제 근거 수준에 맞추는 것이 그 자체로 안전장치다.**

### 6-5. 독립편은 구조적으로 링크 고아가 된다

편26만 `series`가 없어 **들어오는 링크 0건**이었다(SC-10). 나머지 3편은 같은 시리즈끼리 자동으로 서로를 링크한다.

> **「독립편으로 만든다」는 결정이 곧 「링크 고아가 된다」를 함의하는데 설계서에도 브리프에도 그 연결이 없었다.**
> 검사가 잡았지만 **검사는 발생한 뒤에만 작동한다.**

### 6-6. 정확도를 올리는 수정이 새 검증 부담을 만든다 (B3에 이어 재발)

**원지적 24건을 반영하자 재검증이 신규 7건을 찾았다.** 1차 반영이 「전부/…만」을 정밀한 분류로 바꿨고 그 분류가 불완전했다.

**2배치 연속이므로 재검증 2기는 상설로 둔다.** B3 5건 · B4 7건.

### 6-7. 서브에이전트 최종 응답 유실이 이번엔 6회

`idle`을 보고했으나 최종 응답 텍스트가 도달하지 않은 일이 **6회**(scout 3 · converter 1 · 검증 1 · fixer 1) 있었다. **파일 산출물을 지시한 건은 전부 회수됐다.**

> **브리프에 산출물 경로를 파일로 못박아라.** 이 세션은 전 에이전트에 못박아 유실 손실이 0이었다.

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과 (tests/blog/ 3파일)
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 158개 URL, 정적 페이지 167

# 금칙어 — 기대 0건
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]"

# 민감정보 — 「채용」 2건만 기대
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 오탐 많음. 문맥 판정 필수
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "인용할 때|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안"
```

> **알려진 오탐 5종**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **정규식은 영문만 잡는다**
> - `채용` **2건** — `subagent-definition-fields.md:39`(용어표 「채용 공고」) · `agent-team-operations.md:490`(유비표 「채용과 JD 작성」). **B4 신규 4편은 0건**
> - `인용할 때` **1건** — `agent-team-operations.md:419`. 원 자료 수치 인용 지침이지 면접 화법이 아니다. **B4 신규 4편은 0건**
> - 화행 정규식 일반 — `이렇게 구현하는 편이 안전하다`(구현 권고), `LLM이 답한다`(주어가 모델)는 정상

### 태그 패싯 분포 검사 (빌드가 못 잡는다)

```powershell
# tags.ts 상단 주석: "같은 패싯에서 최대 2개"
Select-String -Path "content\blog\agentic-coding\*.md" -Pattern "^tags:"
```

**패싯 C가 3개 이상이면 위반.** `claude-code`·`ai-agent`·`multi-agent`·`ai-governance`·`ai-automation`·`context-engineering`·`prompt-engineering`이 전부 C다. **B4 4편은 전부 정확히 2개**였다.

**`claude-code`가 2편 → 12편으로 살아났다.** 설계서 §8-3의 「죽은 태그」 진단은 B1~B4로 해소됐다.

### mermaid 문법 검사 (도식을 손댄 배치에서만)

```bash
# 빌드는 mermaid를 렌더하지 않는다. 블록 전수를 눈으로 봐야 한다
awk '/^```mermaid/,/^```$/' content/blog/agentic-coding/<파일>.md
```

노드 ID 미정의·중복, 점선 라벨 `-.->|"..."|` 짝, 특수문자(`·` `—`) 든 라벨의 따옴표, 블록 개폐 수를 본다.

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/aiagent"
LC_ALL=C awk '
  BEGIN { sec="머리말" }
  /^```/ { if (!fence && $0 ~ /^```mermaid/) mm[sec]++; fence=!fence; b[sec]+=length($0)+1; next }
  !fence && /^## / { sec=$0; order[++n]=sec; start[sec]=NR }
  !fence && /^\|[ ]*[-:][- :|]*\|[ ]*$/ { tb[sec]++ }
  { b[sec]+=length($0)+1 }
  END { t=0; for(i=1;i<=n;i++){s=order[i]; t+=b[s];
        printf "%s | L%d | %d B | 표 %d | 도식 %d\n",s,start[s],b[s],tb[s],mm[s]}
        printf "합계 %d B\n", t+b["머리말"] }' <파일>
```

**⚠️ `length($0)+1`의 `+1`이 개행이다. 빼먹으면 절마다 줄 수만큼 미달한다.**
**펜스를 추적하지 않으면 H2가 과대계수된다** — `07`은 가짜 H2가 0이지만 `08`은 7개다.
**절별 합계를 `wc -c`와 대조하라.** B4에서 `03` 53,967 B가 정확히 맞았다.

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글 — 독립편은 구조적으로 여기 걸린다
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — **97편** |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, **4개 발행** |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종. **목록 밖이면 빌드 실패. 상단 주석에 패싯 규칙이 있다** |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15 |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§4-4·§5-3·§5-4·§6-1·§6-3·§6-4·§6-5·§9-1·§10·§11에 정정 블록** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |
| **B1 보고서 5건** | [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환·실행검증·리뷰2·반영 |

**B1 보고서 5건이 저장소에 남아 있다.** 뒤 배치는 이것을 템플릿으로 쓸 수 있다 — 다만 **변환 보고서 §4-2 분류표는 상쇄 오류가 있었으므로 그대로 복제하지 마라.**

> **B2 보고서 7건 · B3 보고서 13건 · B4 보고서 11건은 스크래치패드에만 있고 커밋하지 않았다.** 세션이 끝나면 사라진다.
> 핵심은 이 문서 §3·§6과 `CHANGELOG.md`, 그리고 **설계서 정정 블록**에 옮겨 적었다.
