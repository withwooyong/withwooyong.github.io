# 인수인계 (HANDOFF)

**갱신일**: 2026-08-11
**브랜치**: `main` · 최신 커밋 `06166e9`
**작업트리**: 이 문서 · `CHANGELOG.md`만 미커밋 / stash 없음
**발행본**: **104편** (`ai-agent` 51 · `rag` 25 · `agentic-coding` 22 · 기타 6) · sitemap **165 URL**

---

## 1. 이번 세션에서 한 일

**`agentic-coding` B6를 발행했다(편19~21, `agent-definition-catalog` 시리즈 신설).**
**그리고 설계서 진단 오류가 5배치 만에 처음으로 0건이었다 — 대신 오류가 브리프와 조사로 옮겨갔다.**

```
조사 3기 병렬 ─→ 컨트롤러 실측 ─→ 브리프 ─→ 변환 1기 ─→ 검증 4축 ─→ 1차 반영 ─→ 재검증 2기 ─→ 2차 반영 ─→ 최종검증 ─→ 커밋
      │              │                          │             │              │          │
  ┌───┼───┐    설계서 오류 0건            [실측] 5건     ┌──┬──┼──┐      새 문장    신규결함    독립
 원본 설계서 발행본   조사 오류 5건+         브리프 오류 2건 A  B  C  D       19곳       8건    (verifier)
 실측  회수   규격                                    틀림 없음 누락 실행
```

| 단계 | 산출 |
| --- | ---: |
| 변환 3편 | 76,480 B · 도식 5 · 표 21 |
| 검증 4축 | 지적 **28건** (중복 제거 후 22) |
| 1차 반영 | 반영 24 · 반박 3 · 보류 1 · **새 문장 19곳** |
| 재검증 2기 | 「사라졌는가」 **미해소 0** · **신규 결함 8건** |
| 2차 반영 | 8건 전량 + 부분해소 1자리 · **반박 0건** |
| 최종 | **78,201 B · 배율 1.77** |

### ⚠️ 실패가 「설계서」에서 「전달 과정」으로 이동했다 — 이번 배치의 최대 발견

| 유형 | B1 | B2 | B3 | B4 | B5 | **B6** |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| **설계서 진단 오류** | 2 | 1 | 1 | 6 | 7 | **0** ← 처음 |
| **컨트롤러 브리프 오류** | — | — | — | — | 2 | **2** |
| **조사 에이전트 오류** | — | — | — | — | — | **5+** ← 처음 |
| 원본 내부 모순 신규 발견 | — | — | — | 1 | 2 | **3** |
| 재검증 신규 결함 | — | — | 5 | 7 | 7 | **8** |
| 팽창률 | 1.76× | 1.42× | 1.31× | 1.77× | 1.97× | **1.77×** |

**편19~21의 원본 바이트·도식 수가 설계서 값과 정확히 일치했다** — 18,931 / 13,645 / 11,627, 도식 2 / 0→1 / 2.

> **설계서는 배치가 진행될수록 자기 오류를 소진한다.** 한 번에 작성됐고 각 배치가 자기 담당 절만 깊이 파므로,
> 앞 배치들이 이미 훑은 규칙(§4-1 필드명·§4-4 용어표·§6-4 귀속)은 더 이상 틀리지 않는다.
>
> **그러나 브리프와 조사는 매 배치 새로 만들어진다. 오류가 줄지 않는다.**
> **「설계서를 실측하라」에서 「전달 과정을 실측하라」로 옮겨가야 한다.**

### ⚠️ 컨트롤러 브리프 오류 2건 — 둘 다 「상위 문서에 있는데 옮겨지지 않은」 형태다

| # | 브리프가 한 것 | 실제 | 잡은 자리 |
| --- | --- | --- | --- |
| 1 | 편19 핵심 수치를 **`08 §1-1` 요약표 값 그대로**(`상12·중15·하3`) 옮김 | **§2 카탈로그 30행 집계는 `상11·중17·하2`** | **변환자** (직접 세어 반증) |
| 2 | 설계서 §8-3이 「0편 태그 30종 **목록을 그대로 첨부**하라」고 했는데 **목록을 붙이지 않음** | 변환자가 `ci-cd`(목록 9번째)를 선택 | **실행 검증** (sitemap +4) |

**1번은 브리프에 「값 대조는 절 단위로 하라」를 적어놓고 정작 요약표 ↔ 상세표를 대조하지 않은 것이다.**

> **양쪽 합이 다 30이라 합계 검산으로는 절대 잡히지 않는다.**
> **요약표는 상세표를 세어 만든 것이므로, 둘이 어긋나면 요약이 낡은 것이다.**

**2번은 자동 검사의 사각이다** — `ci-cd`는 `tags.ts` 어휘에 **있는** 태그라 빌드가 막지 않는다.
어휘 검증은 「존재하는가」만 보고 빌드는 「몇 편이 쓰는가」를 세지 않는다.

### ⚠️ 조사 에이전트(haiku) 오류 5건+ — 이 티어의 한계가 드러났다

| 무엇 | 오류 |
| --- | --- |
| 표 데이터행 수 | **4건 오답** (§0 표3 13→11 · §11 표20 9→10 · §11 표23 1→4 · §12 표25 10→12) |
| 편21 자기참조 결정 | **정반대 보고** (「기본값 사용」 ← 실제는 사용자 승인으로 「쓰지 않는다」 확정) |
| §6-5·§5-4의 B6 해당분 | 나열한 것이 전부 `03`·`07` 관련으로 **B6와 무관** |
| 문자 깨짐 | `08`→` 8`, `agentic-coding`→`gentic-coding`, `ai-agent`→`i-agent` |
| 발행본 frontmatter | **`description` 필드 누락**, 편 번호 뒤죽박죽 |

> **조사는 「어디에 있나·몇 개인가」에만 쓰고 「무엇을 해야 하나」는 묻지 마라.**
> 위치 보고(줄번호·파일명·slug)는 정확했다. **틀린 것은 전부 세기와 판단이다.**
> **컨트롤러가 세어 대조하지 않았다면 틀린 기준값 5개가 그대로 변환자에게 갔다.**

---

## 2. 다음 세션이 이어받을 것

### 최우선: B7 착수 (`09` → 편22~25)

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md)
— **§4-1·§4-4·§5-4·§5-5·§6-1·§6-3·§6-4·§6-5·§6-6·§8-2·§8-3·§9-1·§10·§11·§12-3·§12-4에 정정 블록이 있다. 그것부터 읽어라.**

| 배치 | 대상 | 편 | 편수 | 상태 |
| ---: | --- | --- | ---: | --- |
| ~~B1~~ | ~~`01`(축약) + `02`~~ | ~~1~5~~ | ~~5~~ | ✅ 완료 (`388519e`) |
| ~~B2~~ | ~~`04`~~ | ~~9~11~~ | ~~3~~ | ✅ 완료 (`f4b9c30`) |
| ~~B3~~ | ~~`05`~~ | ~~12~14~~ | ~~3~~ | ✅ 완료 (`cf948a1`) |
| ~~B4~~ | ~~`03` + `team-productivity`~~ | ~~6~8, 26~~ | ~~4~~ | ✅ 완료 (`a568c2e`) |
| ~~B5~~ | ~~`07`~~ | ~~15~18~~ | ~~4~~ | ✅ 완료 (`2fc5c9f`) |
| ~~B6~~ | ~~`08`(개발축)~~ | ~~19~21~~ | ~~3~~ | ✅ **완료** (`06166e9`) |
| **B7** | `09` | 22~25 | 4 | **다음** |
| B8 | Q&A 4편 | Q1~Q4 | 4 | `05 §10-1` 10문항 이월 · **`ai-agent` 태그 판단 필요** |
| B9 | 지식 지도 | — | 1 | **맨 마지막** |

### B7 편 명세

| 편 | series / seriesOrder | slug | 원본 절 | 원본 B | 도식 |
| ---: | --- | --- | --- | ---: | ---: |
| **22** | `rules-hooks-skills` 1 | `rules-hooks-skills-layers` | `09` **§0 전량 §1-1 §1-2** §2 | 19,118 | 3 |
| **23** | `rules-hooks-skills` 2 | `prompt-four-elements` | `09` §3 §8 | 10,600 | 2 |
| **24** | `rules-hooks-skills` 3 | `empty-folder-to-deploy` | `09` §4 §7 | 12,900 | 2 |
| **25** | `rules-hooks-skills` 4 | `deploy-checklist-debugging` | `09` §5 §6 **§1-3** | 22,988 | 1→**2** |

**편23은 하한 근접(12.7KB), 편25는 상한 근접(27.6KB)이다** — 설계서 §4-1의 △ 표시.

### B7 고유 사항

| # | 사항 | 근거 |
| --- | --- | --- |
| 1 | **`09`의 전방 참조를 실측하라** | 설계서 §9-1 정정이 「`04`·`07`·`09`도 재확인」이라 했고 **`07`·`08`은 확인 완료**(각 0건·1건). `09`는 미실측. **히트 행이 발행 대상 절 안인지까지 확인하라** |
| 2 | **§6-5 10번 — 표 캡션 승격** | 설계서 §9(배치 계획)가 B7 과제로 명시 |
| 3 | **§5-4 3번 — Core Web Vitals INP 병기 + 시점 고지** | 편25 |
| 4 | **`09 §1-3`(MCP 6종)이 편25로 간다** | §4-4 부속 절 배정. **편25 하한 확보용**이라 빼면 안 된다 |
| 5 | **가짜 H2를 먼저 세라** | `08`은 7개였다. 펜스 추적 없이 `grep -c '^## '`를 돌리면 절 경계가 통째로 어긋난다 |
| 6 | **`09` 원본 내부 모순을 미리 찾아라** | B4 1건 · B5 2건 · **B6 3건**으로 늘고 있다. **요약표 ↔ 상세표, 절 제목 ↔ 표 행 수**를 먼저 대조하라 |

### 확정된 결정 (다시 묻지 마라)

| 사안 | 결정 | 근거 |
| --- | --- | --- |
| **분량 상한** | **29.4KB는 합격선이 아니다. 넘어도 된다** | §4-0 원문. 발행본 최대는 편18 36,238 B |
| **시리즈 페이지** | 없다 | `series`는 frontmatter 메타데이터일 뿐 라우트를 만들지 않는다 |
| **frontmatter 필드** | `title` → **`description`** → `category` → `tags`(인라인 배열) → `date` → `updated` → `series` → **`seriesOrder`** → `featured` → `draft` → `source` | 없으면 **빌드 실패.** B6에서 조사가 `description`을 누락 보고했으니 **실물을 열어 확인하라** |
| **`source` 값** | `"AI 에이전트 실무 과정"` | 편26만 예외(비움) |
| **축약 용어표 판정** | **개념 기준.** 본문이 한글로 써도 영문 용어 행은 남긴다 | 설계서 §4-4 정정 |
| **`05 §10-1`·`§10-3`** | 각각 **B8 이월** · **드롭** | 설계서 §4-4 |

### ⚠️ B8이 판단해야 할 것 — `ai-agent` 태그 한도가 소진됐다

**설계서 §8-2 배정은 편12·13·14 + 편19·20·21 + Q3 = 7편이었다.**
그런데 **B5에서 편16(`agent-jd-triggers`)이 배정 외로 부착**했고, B6가 편19~21에 3편을 달아 **7/7에 도달했다.**

| 선택지 | 대가 |
| --- | --- |
| ① Q3에 달지 않는다 | 설계서 배정 위반이지만 기발행본을 건드리지 않는다. **B6는 이것을 전제로 진행했다** |
| ② 편16에서 떼고 Q3에 준다 | 기발행본 수정 — 새 검증 표면이 생긴다 |
| ③ 한도를 8로 올린다 | §8-2 집중도 계산(76%)이 다시 틀어진다 |

### B7 브리프에 반드시 넣을 것

**B1~B5가 준 22건 + B6가 준 아래 5건.**

| # | 추가할 지시 | 근거 |
| --- | --- | --- |
| **35** | **설계서가 「목록을 그대로 첨부하라」고 한 것은 목록 전문을 붙여라.** 규칙 이름만 옮기면 구현자는 적용할 수 없다 | `ci-cd` 위반. **0편 태그 30종 목록은 설계서 §8-3에 있다** |
| **36** | **요약표와 상세표가 같은 것을 세면 둘을 대조하라.** 어긋나면 요약이 낡은 것이다 | 검수 필요도 12/15/3 ↔ 11/17/2. **양쪽 합이 30이라 합계 검산을 통과한다** |
| **37** | **모집단·범위를 바꿨으면 그 범위에서 처음부터 다시 세라.** 앞 문장의 숫자를 재사용하지 마라 | 2차 반영 최상위 신규 결함. 원본 기준 「다섯」을 3편 기준으로 승계했다 |
| **38** | **미발행 절에만 근거가 있는 주장은 쓰지 마라.** 독자가 검증할 수 없다 | SWOT 반례가 §10(미발행)에만 있었다 |
| **39** | **「이 목록 밖에 없다」류 자기지시 주장을 쓰지 마라.** 그 자리에 이름을 적으면 문장이 스스로를 반증한다 | 값을 고쳐도 결함이 남는 형태. **주장을 지우는 것이 유일한 해법이었다** |

**B1~B5가 준 기존 22건**(요약문 수치 대조 / 원문 강도 상승 금지 / 수정 후 스캔 재실행 / 착수 전 표 밀도 측정 / `[실측]`·`[설계서]`·`[추론]` 표기 / 설계서 진단은 옮기기 전에 실측 / `tags.ts` 패싯 규칙 명시 / 재검증에 「사라졌는가」 / 상한은 합격선이 아님 명시 / 「이 글의 정리다」 귀속 / 분산 지시는 편별 체크리스트 / 신규 도식은 화살표마다 원문 근거 / 부정 주장은 검색 후 근거를 문안에 / 전칭은 전수 배정 / 담당 원본 L1~L3으로 기준일 확인 / 전방 참조는 발행 대상 절 안인지 확인 / 추정에는 「이건 추정이다」 명시 / 전수 배정 뒤 총평 금지 / 전칭에는 개수 명시 / 금칙어 회피 시 귀속 문장도 갱신 / 값 대조는 절 단위 / 부정 명제는 정규식을 넓혀 재확인)는 그대로 가져간다.

### 진행 방식 — 검증된 절차

```
1단계  분할 설계 → 승인                                    ← 완료
2단계  배치로 나눠 변환, 배치마다 커밋·보고                  ← B1~B6 완료, B7부터
3단계  구현 미참여 에이전트가 4축 독립 검증
4단계  지적 반영 → 재검증 2기 → 2차 반영 → 최종 독립 검증 → 커밋
```

**검증 4축은 그대로 쓴다.** 나누는 기준은 주제가 아니라 **결함이 드러나는 방식**이다.

| 축 | 무엇을 보는가 | 왜 별도 축이어야 하나 | 티어 |
| --- | --- | --- | --- |
| A | 쓰여 있는데 **틀린** 것 | grep으로 안 잡힌다. 세면 0건이라 자동 검증을 통과한다 | opus |
| B | 쓰여 있는데 **원문에 없는** 것 | 원문과 나란히 놓아야만 보인다 | opus |
| C | **없는** 것 | **원본 인벤토리를 먼저 만들고** 그 목록을 들고 발행본을 검색한다 | opus |
| D | **실행하면 나오는** 것 | 종료코드는 객관적이다 | sonnet |

> ### ⚠️ 검증자에게 브리프도 변환 보고서도 주지 마라 — B5에서 확립, B6에서 재확인
>
> **B6에서 3개 축이 서로 모른 채 같은 결함 3건에 도달했다** — 「대부분」 전칭 · 화살표 오계수 · few-shot 부정 주장.
> 같은 문장이라도 축 A는 「세어 보니 10/30」으로, 축 B는 「원문에 그런 서술이 없다」로 도달한다.
>
> **대가는 오탐 2건이다**(금칙어를 피해 뺀 값을 축 C가 누락으로 지적). **판정은 컨트롤러가 한다.**
> **축 C는 반드시 원본을 먼저 읽게 하라** — B6에서 축 C가 「발행본이 자기가 세운 규칙을 어긴 것」을 찾았다.
> 편21이 「원 자료의 표에 영문 약어로 등장하는 것은 남겼다」고 **스스로 규칙을 선언**해 놓고 `STAR`만 빠뜨렸다.
> **원문 대조로는 절대 안 잡힌다 — 원문에 그 규칙 자체가 없기 때문이다.**

**재검증은 2기로 나눈다.** B3 5건 · B4 7건 · B5 7건 · **B6 8건.** 4배치 연속이므로 상설이다.

> **반영자에게 「새로 쓴 문장 전량과 각각의 검증 가능한 주장」을 기록하게 하라.**
> B6에서 반영자가 19곳을 기록했고 **재검증이 그 목록을 표적으로 8건을 찾았다.**
> **목록이 없으면 78KB에서 무엇이 새 문장인지 구분할 방법이 없다.**

**최종 독립 검증을 한 번 더 돌려라.** 2차 반영자의 자기 검증은 검증이 아니다.
B6에서 최종 검증(verifier)이 **컨트롤러 실측 오류 1건**을 잡았다.

---

## 3. ⚠️ 설계서 정정 — B7 이후에 영향

**B6에서 9건을 정정했다. 전부 설계서 본문에 ⚠️ 블록으로 삽입돼 있다.**

| 절 | 정정 | 뒤 배치 영향 |
| --- | --- | --- |
| **§4-1** | ✅ **기록** — 편19~21 설계값이 전부 맞았다. 5배치 만에 진단 오류 0건 | 오류가 브리프·조사로 이동 |
| **§5-4** | `08` 내부 모순 **3건 추가**(검수 필요도 · 위임 링크 8/13 · 절 제목 6 대 표 7) | **B7.** 요약표 ↔ 상세표를 먼저 대조하라 |
| **§8-2** | **`ai-agent` 한도 7편 소진.** B5에서 편16이 배정 외 부착 | **B8.** Q3 몫이 없다 |
| **§8-3** | 「목록을 브리프에 그대로 첨부한다」가 지켜지지 않아 `ci-cd` 위반 | **전 배치.** 목록 전문을 붙여라 |
| **§9-1** | `08` L682 절간 참조 추가 (편21 → 편20) | **B7.** 절간 참조도 표에 올려라 |
| **§10** | 팽창률 **1.77**. **행 수 가설 반증** — 표 바이트 비율이 변수. 6배치 편차 1.31~2.06 | **B7 분량 예측.** 예측보다 실측 |
| **§11 금지선 2** | 「약 200 B」 → **실측 ~150 B** + **승인된 제거에도 역참조 검색이 필요하다** | 전 배치 |
| **§12-3** | ✅ **해소** — `08 §11-5` ↔ `enterprise-ai-adoption-actions` 중복 아님 | 다시 하지 마라 |
| **§12-4** | 이월 위험 **4번 추가** — `08 §12` 임계값 3건 | `ai-transformation` |

### 앞 배치 정정도 유효하다

| 배치 | 정정 위치 | 요지 |
| --- | --- | --- |
| B1 | §6-1 · §10 | 「`강의가 ~한다` 130여 개」 → **실측 32** / 팽창률 1.20 → 1.76 |
| B2 | §5-3 ①②③ | 발행본 열거 기준 · MCP 인과 · 발행본 시점 |
| B3 | §6-5 8번 | `05 §11`은 출처 대장이 아니라 **면접 화법 가이드** |
| B4 | §6-3·§6-4·§6-5·§9-1 | 편26 항목 3건 · 시점 기준일 `2026-07-26` · 인용 절 4건 · `Plan Mode` 링크 불가 |
| B5 | §4-1·§4-4·§5-4·§5-5·§6-6·§10·§11 | `seriesOrder` · 용어표 개념 기준 · 100줄 · 절대 수치 · 금지선 17 |

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 77점. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **B6에서 `ci-cd` 싱글톤이 생겼다가 해소됐다.** 2차 전체를 마친 뒤 재측정 |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | **어휘 81종 중 0편 태그** | **목록은 설계서 §8-3에 있다. B7 브리프에 전문을 첨부하라** |
| 7 | **훅 차단 채널이 두 발행본에서 다르다** | 편11은 **stdout JSON**, `settings-permissions-deny`는 **표준 에러**. 편11에 고지를 달아 막아 뒀으나 근본 해소는 아니다 |
| 8 | **원본 `04 §8-2` 「3대 구성 요소」인데 표는 4행** | 원본에서 이월된 불일치. 편11에 해명만 달았다 |
| 9 | **원본 `05` 내부 수치 불일치** | L56 도식 `frontmatter 5+4 필드` ↔ L145 표 `핵심 5 + 확장 5` |
| 10 | **`05 §3-2` 제품 결함 3건의 현재 유효성** | 「버전 표기가 없다」는 명기했으나 **세 동작이 지금도 그런지는 미확인** |
| 11 | **무귀속 판정 잔존 2건** | 편14 L232 · 편13 L235. B3~B6에서 손대지 않았다 |
| 12 | **B1 발행본 편2~5의 `2026-04` 표기 재확인** | 「강의 시점」인지 「문서 기준일」인지 구분되지 않았다 |
| 13 | **편7 `feedback` 예시가 원본 MEMORY.md 예시와 소재 겹침** | **B4 재검증 `[추론]`.** 의도적으로 두었다 |
| 14 | **편8 용어표 「멀티턴 대화」 본문 0회** | **개념 기준 확정으로 재판정 대상** |
| 15 | **기발행본의 축약 용어표 문자열 0회 행 미점검** | **개념 기준 확정으로 대부분 해소되나 기발행 18편은 재점검하지 않았다** |
| 16 | **편18이 발행본 최대 (36,238 B)** | **렌더링·ToC·로딩 체감은 확인하지 않았다** |
| **17** | **`08 §12` 임계값 3건이 미발행 절 근거** | **B6 신규.** CTR 1.5%/1%(§5) · OCR 80%·「병렬 절대 금지」(§8). 편20에 축자 이식돼 있고 **문장은 끊기지 않는다.** `ai-transformation`에서 §5·§8 발행 시 교차 링크로 해소 (설계서 §12-4 4번) |
| **18** | **`ai-agent` 태그 한도 소진** | **B6 신규.** 위 §2의 B8 판단 사항 |
| **19** | **`08 §12-1` 원본 모순을 발행본이 병기로만 처리** | **B6 신규.** 위임 링크가 8인지 13인지 원 자료 안에서 판정되지 않는다. **원본이 읽기 전용이라 해소 불가** |

### `ai-transformation`으로 이월된 위험 4건

`06` 전량 + `08 §4~§8·§10`을 이관하면 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표** |
| 2 | **`06`은 발행본과 서사 방향이 반대다.** **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |
| **4** | **`08 §12` 임계값 3건** (위 미해결 17번) |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

**아홉 번째 이월이다.** README는 2026-08-07(`7f28866`) 이후 손대지 않았고 그 사이 **25커밋**이 쌓였다.

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 **104편·4카테고리**가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. 정적 export 사이트라 런타임 환경변수가 없다.
실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README에 이미 문서화돼 있다.
`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)와 설계서 §13, `CHANGELOG.md`의 B1~B5 항목에 있다. **아래는 B6에서 새로 나온 것이다.**

### 6-1. ⚠️ 모집단을 바꾸면 앞 문장의 숫자가 조용히 거짓이 된다

1차 반영이 「원문에 없는 재규정」을 고치며 **모집단을 원본 전체 → 발행 3편으로 바꿨는데, 원본 기준으로 세었던 목록(5개)을 그대로 승계했다.** 3편 기준으로는 9개 토큰 / 6행이다.

> **더 나쁜 것은 그 문장에 「이 글이 세 편을 검색해 본 것이다」라는 자기 표기가 붙어 있었다는 점이다.**
> **귀속을 정직하게 달았기 때문에 세지 않은 사실이 명시적 거짓이 된다.**
> **금지선 14(귀속을 붙여라)와 금지선 32(값을 바꾸면 딸린 문장도 갱신하라)가 충돌하는 형태다.**

### 6-2. 자기지시 주장은 값을 고쳐도 참이 되지 않는다

*「이 용어표 **밖으로** 한 번도 나오지 않는다」*고 쓰면서 그 자리에 약어 이름을 적으면
**그 이름들은 방금 용어표 밖에 등장한다. 문장이 스스로를 반증한다.**

> 원 문안도 같은 구조였고 **「주장 문장 자신은 세지 않는다」는 암묵 규칙에 기대 가려져 있었다.**
> 재검증은 「9/6으로 갱신하라」고 했으나 **값을 고쳐도 결함이 남는다.**
> **2차 반영은 주장을 지웠다 — 원 자료는 한 행도 빠지지 않았고 사라진 것은 이 글이 덧붙인 판정뿐이다.**

### 6-3. 「정확성을 올리는 수정은 분량을 늘린다」는 절반만 참이다

B5의 결론인데 B6에서 **편19가 반영 후 줄었다**(28,886 → 28,458 B).

| 수정 유형 | 분량 |
| --- | --- |
| 원문을 축자 인용해 요약을 대체 | **늘어난다** |
| 이 글이 덧붙인 판정을 지움 | **줄어든다** |

### 6-4. 개수 일치는 동일성의 증거가 되지 못한다

편18의 7부서 30에이전트 표(`07` 출처)와 `08 §2` 카탈로그는 **7·5·4·30이 전부 맞아떨어지는데 셀을 열면 22/30만 대응**한다.

> **결정적 반증을 `08` 자신이 제공한다** — §11-6이 *「30종 중 **테스트를 만드는 에이전트가 없다**」*고 적는데
> 편18의 개발 부서에는 「테스트」가 있다. **두 표가 같은 조직도라면 성립할 수 없다.**
> 설계서 §11 사례표의 「`08`은 `06`의 카탈로그다 → 서로 다른 세트」와 같은 패턴이다.

### 6-5. 발행본이 스스로 세운 규칙에 자기가 걸린다 — 원문 대조로는 안 잡힌다

편21이 *「본문이 한글로 풀어 쓰더라도 원 자료의 표에 영문 약어로 등장하는 것은 남겼다」*고 **규칙을 선언**해 놓고
같은 성격의 `JTBD`·`OKR`·`PRD`·`ADR`·`5 Whys`는 전부 실으면서 **`STAR`만 빠뜨렸다.**

> **원문에는 「축약표에 무엇을 남길까」라는 규칙 자체가 없으므로 원문 대조로는 절대 안 잡힌다.**
> **인벤토리를 만들어 전수 대조해야 드러난다 — 축 C에 「원본을 먼저 읽어라」를 강제한 값이 여기서 나왔다.**

### 6-6. 승인된 제거일수록 역참조 검색을 건너뛴다

`§3-4` tools 열 제거는 설계서가 **명시 승인**한 유일한 제거였다. 그런데 **요약표 첫 행이 그 열을 가리키고 있었다** —
*「아래 표에서 "기본"으로 표기된 전부」*. 열만 지우면 이 문장이 없는 열을 가리킨다.

> **제거 승인은 「무엇을 지울까」만 정하고 「무엇이 그것을 가리키나」를 정하지 않는다.**
> **이미 판단이 끝났다고 여기기 때문에 오히려 검색을 건너뛰기 쉽다.**

### 6-7. 컨트롤러가 자기 규칙을 자기에게는 적용하지 않았다

브리프에 **「펜스를 추적하라」**(가짜 H2 7개)를 적어 변환자에게 경고해 놓고,
**발행본 실측에는 펜스를 추적하지 않는 단순 `grep`을 썼다.** 편20의 표를 11개로 오계수했다(실제 10개).
4틱 펜스 안의 템플릿 예시 텍스트를 표로 셌다. **최종 독립 검증이 잡았다.**

### 6-8. 서브에이전트 산출물 유실 — 원인이 밝혀졌다

재검증 1기가 파일을 쓰지 않았고 응답 텍스트도 오지 않았다. 재요청하자 **「제 세션에서 `Write` 도구가 비활성화돼 있습니다. Bash 히어독으로 기록했습니다」**라고 답했다.

> **6배치 동안 「최종 응답 유실」로 뭉뚱그렸던 현상의 실제 원인일 수 있다.**
> **브리프에 「`Write`가 막혀 있으면 Bash 히어독으로 써라」와 「작업 중간에도 append하라」를 넣어라.**
> 재요청 시 **「검증을 다시 하지 말고 이미 내린 판정을 옮겨 적어라」**와
> **「기억나지 않으면 미확인이라고 적어라」**를 함께 지시했다 — **유실된 판정을 추측으로 메우는 것이 유실보다 위험하다.**

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과 (tests/blog/ 3파일)
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 165개 URL, 정적 페이지 174

# 금칙어 — 기대 3건 (전부 thirty-agent-catalog.md, 아래 오탐 목록 참조)
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]|예상 질문"

# 민감정보 — 회사명·핸들은 0건 기대. 「채용」은 아래 오탐
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 요구사항 §14가 진실원. 오탐 많음, 문맥 판정 필수
Select-String -Path "content\blog\agentic-coding\*.md" `
  -Pattern "인용할 때|라고 말|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안|인용 시"
```

> **알려진 오탐**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **정규식은 영문만 잡는다**
> - **`채용`** — `subagent-definition-fields.md` · `agent-team-operations.md` · `agent-jd-triggers.md` · `scaling-routing-collapse.md` · **`thirty-agent-catalog.md`(B6 신규 2자리)**. 전부 **에이전트가 담당하는 업무 도메인의 이름**이며 저자의 구직 활동이 아니다. **인사 부서의 업무를 달리 부를 이름이 없어 구조적으로 제거 불가**
> - **`이력서`·`면접` — `thirty-agent-catalog.md`(B6 신규)** — `recruiter` 에이전트의 「주요 입력: 포지션 정의, 요구 역량, **이력서**」와 「스크리닝·**면접** 질문」이다. **위와 같은 성격**
> - `실적이 아니` — `scaling-routing-collapse.md` · **`thirty-agent-catalog.md`(B6 신규)**. 주어가 **대상의 성질**(「사람 검수 등급은 …이 글이 운영해 얻은 실적이 아니다」)이라 서술문이며, **오히려 귀속을 명시한 좋은 문장**이다
> - `인용할 때` — `agent-team-operations.md`. 원 자료 수치 인용 지침이지 면접 화법이 아니다
>
> **판정 기준**: 문장의 주어가 **독자의 발화**면 화법, **대상의 성질**이면 서술이다.

### 태그 패싯 분포 검사 (빌드가 못 잡는다)

```powershell
Select-String -Path "content\blog\agentic-coding\*.md" -Pattern "^tags:"
```

**패싯 C가 3개 이상이면 위반.** `claude-code`·`ai-agent`·`multi-agent`·`ai-governance`·`ai-automation`·`context-engineering`·`prompt-engineering`이 전부 C다.
**`ai-agent`는 7편 한도이며 B6에서 정확히 7/7에 도달했다 — 여유 0.**
**0편 태그 30종은 설계서 §8-3에 목록이 있다. 어휘에 있어도 쓰면 안 된다 — 빌드는 막지 않는다.**

### mermaid 문법 검사 (도식을 손댄 배치에서만)

```bash
awk '/^```mermaid/,/^```$/' content/blog/agentic-coding/<파일>.md
```

노드 ID 미정의·중복, 점선 라벨 `-.->|"..."|` 짝, 특수문자(`·` `—`) 든 라벨의 따옴표, 블록 개폐 수를 본다.
**코드펜스(```) 총 개수가 짝수인지도 세라.** 현재 값: 편19 4 · 편20 **8**(4틱 펜스 안에 3틱 중첩) · 편21 4.

> **⚠️ 발행본의 표·H2를 셀 때도 펜스를 추적하라.** 편20은 4틱 펜스 안에 **표처럼 보이는 템플릿 예시 텍스트**가 있어
> 단순 `grep`으로 세면 11개, 펜스를 인지하면 **10개**다. **원본의 「가짜 H2」와 같은 함정이 발행본에도 있다.**

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/aiagent"
LC_ALL=C awk '
  BEGIN { sec="머리말" }
  /^```/ { if (!fence && $0 ~ /^```mermaid/) mm[sec]++; fence=!fence; b[sec]+=length($0)+1; next }
  !fence && /^## / { sec=$0; order[++n]=sec; start[sec]=NR }
  !fence && /^\|[ ]*[-:][- :|]*\|[ ]*$/ { tb[sec]++ }
  { b[sec]+=length($0)+1 }
  END { t=0; for(i=1;i<=n;i++){s=order[i]; t+=b[s];
        printf "%s | L%d | %d B | 표 %d | 도식 %d\n",s,start[s],b[s],tb[s],mm[s]}
        printf "합계 %d B\n", t+b["머리말"] }' <파일>
```

**⚠️ `length($0)+1`의 `+1`이 개행이다. 빼먹으면 절마다 줄 수만큼 미달한다.**
**펜스를 추적하지 않으면 H2가 과대계수된다** — `07`은 가짜 H2가 0이지만 **`08`은 7개**였다.
**절별 합계를 `wc -c`와 대조하라.** B6에서 `08` 68,003 B가 정확히 맞았다.
**표 데이터행은 별도로 세라** — 조사 에이전트가 B6에서 4건을 틀렸다.

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글 — 독립편은 구조적으로 여기 걸린다
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — **104편** |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, **4개 발행** |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종. **목록 밖이면 빌드 실패. 상단 주석에 패싯 규칙이 있다** |
| frontmatter 검증 | [`lib/blog/frontmatter.ts`](lib/blog/) — 형식 오류 먼저, 어휘 오류 나중. tags 최대 6개 |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15 |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§4-1·§5-4·§8-2·§8-3·§9-1·§10·§11·§12-3·§12-4에 B6 정정 블록** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |
| **B1 보고서 5건** | [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환·실행검증·리뷰2·반영 |

**B1 보고서 5건이 저장소에 남아 있다.** 뒤 배치는 이것을 템플릿으로 쓸 수 있다 — 다만 **변환 보고서 §4-2 분류표는 상쇄 오류가 있었으므로 그대로 복제하지 마라.**

> **B2 보고서 7건 · B3 13건 · B4 11건 · B5 10건 · B6 12건은 스크래치패드에만 있고 커밋하지 않았다.** 세션이 끝나면 사라진다.
> 핵심은 이 문서 §3·§6과 `CHANGELOG.md`, 그리고 **설계서 정정 블록**에 옮겨 적었다.
