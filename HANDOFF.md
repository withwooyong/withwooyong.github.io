# HANDOFF

> 마지막 갱신 2026-09-03 · `pm` 배치 **시리즈 A 9/9 · 시리즈 B 8/8 — 배치 전체가 완결됐다**

## 지금 상태

발행본 **184편**이다. 이번 세션은 **편5 · 편6 · 편7 · 편8 네 편을 썼고 편8로 배치가 끝났다.**
편8이 남긴 것은 **검사기가 보는 대상과 실제로 지켜야 할 것 사이의 틈**이다 — 금칙어 때문에
원본에서 뺀 블록의 **낱말**은 지웠는데 그 블록의 **판단**이 본문을 거쳐 정리 절까지 올라가
있었고, 검사기의 자기 검사는 **자기가 아는 것**만 지키고 **판정 기준 자체**는 보지 않았다.

| 항목 | 값 |
| --- | --- |
| 발행 완료 | **A 편1~9 + B 편1~편8 (184편)** · 시리즈 B 완결 |
| 다음 단계 | 🔴 **검사기 사각지대 6건 수정** (아래 8번). 배치가 끝나 과거 수치를 다시 잴 필요가 없어졌다 |
| 푸시 · 배포 | ✅ **편8까지 `origin/main` 반영 · Pages 배포 성공.** 라이브에서 h2 13개 · 도식 렌더 · 형제편 링크 도달을 확인했다 |
| 커밋 | 편5 `71b40b5` · 편6 `3908175` · 편7 `6ee8c33` · 편8 (이번 커밋) |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` · **§10-17 까지 있으면 편8이 끝난 것이다** |

### 이번 세션이 한 일

| # | 무엇 | 어디에 남았나 |
| ---: | --- | --- |
| 1 | **편5 발행** — `commerce-member-and-catalog` | 커밋 `71b40b5` · 설계서 §10-14 |
| 2 | **편6 발행** — `commerce-pricing-and-order` 18,535 B · k 2.169 | 커밋 `3908175` · 설계서 §10-15 |
| 3 | **편7 발행** — `fintech-regulation-and-open-banking` **17,973 B** · k 2.058 | 커밋 `6ee8c33` · 설계서 §10-16 |
| 4 | **편8 발행** — `mydata-and-market-structure` **22,456 B** · k **2.121** | 설계서 **§10-17** |
| 5 | 리뷰 두 축 4연속 — 편6 15+9 · 편7 20+9 · 편8 **22+12** | 설계서 §10-15-g~h · §10-16-d~g · **§10-17-g** |
| 6 | 🔴 **링크 몫 공식이 세 번 검증됐다** — 편6 56 · 편7 65 · 편8 **57** | 설계서 §3-0-a · §10-17-f |
| 7 | README 세 자리 · CHANGELOG 편6·편7·편8 절 · 아카이브 이동 3회(**11절**) | `README.md` · `CHANGELOG.md` · `docs/changelog/2026-09.md` |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 0. 🔴 배치가 끝났다 — **첫 안건은 새 글이 아니라 검사기다**

편1~편8 의 수치를 다시 잴 필요가 없어졌으므로, 「고치면 과거 수치가 흔들린다」는 이유로
미뤄 온 것들을 지금 고칠 수 있다. 목록은 8번이다.

### 0-1. 🔴 **`**` 가 본문에 그대로 찍힌 페이지가 90개 있다** — 편8 배포 뒤 실물에서 발견했다

닫는 `**` 앞이 구두점(`」` · `"` · `)`)이고 뒤가 한글이면 **CommonMark 의 right-flanking 조건이
깨져 강조가 걸리지 않는다.** 조사가 낱말에 붙는 한국어에서 구조적으로 발생한다.

```
✗ **「무엇을 요구할 수 있는가」**를 정한다      ← 별표가 화면에 보인다
✅ **「무엇을 요구할 수 있는가」를** 정한다
```

| 무엇 | 값 |
| --- | ---: |
| 렌더된 본문에 `**` 가 남은 페이지 | **90개 · 411회** |
| 소스의 「구두점 + `**` + 문자」 | **145건 · 77개 파일** (`」` 29 · `"` 93 · `)` 23) |
| 고친 것 | **편8 1건뿐** |

⇒ 🔴 **산출물을 셀 때는 `sed '/__NEXT_DATA__/,$d'` 로 먼저 잘라라.** 자르지 않으면 원본
마크다운이 함께 잡혀 184편 전부가 `**` 를 가진 것으로 나오고(21,172회) 진짜 실패가 묻힌다.

```bash
for f in $(find out/blog -name index.html); do
  n=$(sed '/__NEXT_DATA__/,$d' "$f" | LC_ALL=C grep -o '\*\*' | wc -l)
  [ "$n" -gt 0 ] && echo "$n $f"
done | sort -rn
```

⇒ **검사기에 넣을 자리가 있다.** 기존 검사 다섯은 낱말·겹침·분량만 보고 **마크업 문법을
보지 않는다.** 89개 페이지 수정은 **사용자 판단을 받아야 한다** — 이 배치 밖의 옛 편들이다.

### 1. 🔴 금칙어 때문에 **뺀 블록**은 낱말만 빼면 되는 것이 아니다

편8에서 가장 무거운 지적이었다. 배정 밖 「💼 면접 활용」 블록의 판단
(「수집·저장은 되어 있으나 **가공에서 갈린다**」)이 본문을 거쳐 **정리 절까지** 올라갔다.
검사기는 낱말만 보므로 `check-forbidden` 도 `source-overlap` 도 이것을 잡지 못한다.

배정 밖 블록의 주장 유입이 **편6 2건 · 편7 2건 · 편8 1건으로 세 편 연속**이다.

⇒ 🔴 **집필 전에 「이 블록에서 온 판단은 본문에 쓰지 않는다」를 명제 단위로 적어라.**
편7에서 유일하게 막힌 한 건은 **「본문에서 이렇게 쓰면 위반」으로 번역해 둔 것**이었다.

### 2. 🔴 `compose` 는 「복제를 지운 뒤」가 아니라 **「본문을 마지막으로 만진 뒤」** 돌린다

편8에서 상한을 **세 번** 넘겼고 원인이 매번 달랐다.

| 시점 | 발행 B | 표 B | 실질 상한 22,507 대비 |
| --- | ---: | ---: | ---: |
| 감축 8회차 직후 | 22,415 | — | −92 ✅ |
| 복제·원본 겹침 제거 후 | 🔴 22,719 | — | +212 ❌ |
| 재감축 후 (초판) | 22,518 | 4,768 | +11 |
| **리뷰 두 축 반영 후** | 🔴 **22,654** | 🔴 **4,943** | **+147** ❌ |
| 재감축 후 (확정) | **22,456** | **4,807** | **−51** ✅ |

`CLAUDE.md` 에는 **복제 제거**만 적혀 있었는데 **리뷰 반영**도 같은 일을 한다 — 앞은 겹침을
피하느라, 뒤는 증발한 근거를 되살리느라 문장이 길어진다.

⚠️ **이번에는 표 예산이 총량보다 먼저 터졌다.** 표 여유가 편6 296 → 편7 65 → 편8 **13** 으로
좁아져 왔다. 총량만 보고 통과를 판정하지 마라.

### 3. 🔴 인수인계에 적힌 수치도 **대조군 없이는 근거가 아니다**

편8 착수 때 인수인계의 원본 B **9,682** 가 틀렸다(실측 **9,430** · −252). 배정 밖 블록 세 개가
모두 3줄이라 적혀 있었는데 **마지막 블록만 5줄**이었다 — 파일이 302행에서 끝나 뒤에 빈 줄이
없었던 탓이다.

**교차 검증이 실측을 지지했다.** §2-4-a 의 파일 합계 **2,992** = 편7 1,369 + 편8 **1,623**.

⇒ 🔴 **뺀 값을 「몇 줄」로 기억하면 파일 끝에서 무너진다. 경계는 줄 수가 아니라
「인용 줄이 끊기는 지점」으로 찾아라.** 그리고 **합계가 맞는다고 편별 값이 맞는 것은 아니다**
(§10-14-c 가 경고한 것의 세 번째 사례다 — 편7 −11 과 편8 +12 가 상쇄돼 합계를 거의 맞췄다).

### 4. 🔴 산식의 상수 2,455 는 **상수 절의 예산이 아니다**

편8 착수 노트가 2,455 를 머리말 · 용어절 · 정리에 배분했는데, 그 셋의 합이 초고에서
**5,015** 였다. 편7을 대조하니 편7도 **5,084** 였다 — 2,455 는 **회귀 절편**이고 상수 절의
초과분은 배율 2.05 안에 이미 흡수돼 있다.

⇒ **산식의 항을 문서의 부위에 그대로 대응시키지 마라. 대조군을 먼저 재라.**

### 5. 🔴 `dup-scan` 0건은 **원본 대조의 0건이 아니다**

편8은 `dup-scan` 7건을 고친 **뒤에** `source-overlap` 이 편7의 **4.6배**를 냈다.

| 편 | ① 공백 보존 | ① 최장 | ③ 도식 라벨 |
| :---: | ---: | ---: | ---: |
| 6 | 2건 | 23자 | 0 |
| 7 | 8건 | 24자 | 0 |
| **8 (초고)** | 🔴 **37건** | 🔴 **210자** | 🔴 **2건** |
| **8 (확정)** | 17건 | 29자 | **0건** |

210자는 **국가별 데이터 정책 표가 통째로 같았던 것**이다. 표를 전치하고(항목×국가 →
국가×항목) 인용 넷을 재서술해 내렸다. 남은 17건은 법률명 · 정책 문서명 · 권리명이다.

⚠️ **표를 전치할 때 칸이 증발한다.** 편8에서 「특징」 행의 미국·EU 두 칸이 사라졌고 한국이
통제 주체로 잘못 섰다 — 정확성 리뷰가 잡았다. **전치 뒤에는 원본과 칸 수를 대조하라.**

### 6. 🔴 초고가 어느 쪽으로 벗어나는지는 **원본의 표 비중**이 정한다 — 양방향으로 성립한다

| 배정 원본 표 비중 | 초고 이탈 | 편 | 회차 |
| ---: | ---: | :---: | :---: |
| 41% | 🔴 −14.8% | 7 | 3 (증량) |
| **34.9%** | 🔴 **+22.6%** | **8** | **8** |
| 낮음 | +28.3% | 6 | 6 |

편8은 이것을 **예측에 썼다.** 착수 노트가 표 비중과 산문 배율(편7 3.03 대 편8 2.47)로
「약 +23% 넘친다」고 적었고 실측이 +22.6% 였다.

### 7. 🔴 링크 몫 공식 — **세 번 검증됐다**

> **링크 몫 = 슬러그 길이 + 12 + 카테고리 길이**

| 편 | 슬러그 | 예측 | 실측 |
| :---: | --- | ---: | ---: |
| 5 → 6 | `commerce-pricing-and-order` | 56 | **56** ✅ |
| 6 → 7 | `fintech-regulation-and-open-banking` | 65 | **65** ✅ |
| 7 → 8 | `mydata-and-market-structure` | 57 | **57** ✅ |

⚠️ **링크 수는 `grep -c` 로 세지 마라 — 줄 수를 센다.**

```bash
LC_ALL=C grep -o '](/blog/' <발행본> | wc -l
```

### 8. 🔴 검사기 사각지대 **6건** — 이제 미룰 이유가 없다

| # | 무엇 | 실측 | 상태 |
| ---: | --- | --- | --- |
| 1 | `dup-scan:verify` 가 **공용 정규화를 하나도** 검증하지 않는다 | 뮤턴트 5종 **전부 PASS** 인데 실제 스캔은 20 → 26 · 54 · 55건 | 편6부터 · **범위가 더 넓은 것으로 판명** |
| 2 | `compose` 의 `wc -c` 라벨이 파일에서 재지 않는다 | 183편에서 어긋남 0 (당장 해는 없다) | 편6부터 |
| 3 | 🔴 `source-overlap` 의 **임계값이 자기 검사와 끊겨 있다** | `MIN_DEFAULT` 20 → 200 이면 편8이 **17건 → 0건**인데 `--self-test` 는 **28/28** | 편7부터 |
| 4 | 링크 제목 추출 단계를 **두 자기 검사 어느 쪽도** 보지 않는다 | 무력화 시 16 → 44건인데 7/7 · 28/28 | 편7부터 |
| 5 | 🔴 **펜스가 원본 대조를 통째로 가린다** | 원본 175자를 ` ```text ` 로 감싸 붙이자 출력이 **손대지 않은 발행본과 완전히 동일**(문단 87개 · 17건 · 5건 · 0건) | **편8 신규** |
| 6 | `check-forbidden` 자기 검사 커버리지 | HARD **18개 중 5개** · SOFT **8개 중 2개**만 증명 | 편7부터 |

**뮤테이션 28개 중 9개가 살아남았다.**

⇒ 🔴 3번의 기전이 핵심이다 — `selfTest()` 가 `MIN_DEFAULT` 를 **한 번도 참조하지 않고**
임계값 20을 **8곳에 손복사**해 뒀다. `normalize.mjs` 로 정규화를 합쳐 없앤 손복사 드리프트가
**임계값에서 되살아났다. 공용 모듈로 묶는 것은 묶은 것만 지킨다.**

⇒ 🔴 고친 뒤에는 **옛 판으로 되돌려 그 케이스가 실제로 FAIL 하는지** 확인하라. 통과만 보고는
케이스가 헛도는지 알 수 없다.

### 9. 🔴 산출물 검사는 **무엇을 읽었는지** 확인한 뒤에 믿어라

편7에서 `check-forbidden:built` 가 편7을 **한 번도 보지 않은 채** HARD 0 · 종료 코드 0 을 냈다.
편8은 이 순서를 지켰고 통과가 진짜임을 리뷰가 전건 재확인했다.

```bash
npm run build
stat -c '%y' <발행본>                                     # 소스 시각
ls --time-style=full-iso out/blog/<카테고리>/<슬러그>/index.html   # 산출물이 더 나중인가
LC_ALL=C grep -rl '<슬러그>' out/ | wc -l                 # 0 이면 중단
LC_ALL=C grep -c '<이번에 새로 넣은 문구>' out/.../index.html   # 낡은 산출물이 아님을 증명
npm run check-forbidden:built
```

마지막 줄이 편8에서 더한 것이다 — **시각 비교만으로는 「빌드가 돌았다」까지만 알 수 있고,
「이번 수정이 들어갔다」는 새 문구로만 증명된다.**

⇒ 🔴 **금칙어는 손으로도 세라.** 자기 검사가 덮지 않는 13개 때문이다. 산출물의 「허우용」은
JSON-LD `author` 면제(`BUILT_ALLOW`)이며 정당하다 — 그 문자열 밖에 나오면 위반이다.

### 10. 남은 작업

| 대상 | 상태 |
| --- | --- |
| 편7 말미 「다음 편」 링크 | ✅ 부착 (+57) |
| 지도편 구성 표 형제편 링크 | ✅ **편1~편8 모두 채워졌다** |
| 배치 전체 `dup-scan` | ✅ 20건 — 전부 용어 정의·법률명이며 정당 |
| 푸시 | ✅ **편8 발행(`c2d976b`)과 렌더 수정까지 완료** |
| 검사기 사각지대 6건 | 🔴 **다음 세션 첫 안건** (위 8번) |
| 🔴 `**` 렌더 실패 89개 페이지 | **미수정.** 사용자가 편8 1건만 고치기로 했다 — 전수 수정은 별도 세션 (위 0-1번) |

---

## 시리즈 B 사양 요약 — **완결**

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (편1~편8 발행 완료) |
| 원본 | `C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/` |
| 산식 | **목표 B = 2,455 + 원본 B × 2.05 + 링크 몫** · 밴드 k ± 0.14 (1.91 ~ 2.19) |
| 역산 k | **(발행 B − 2,455 − 링크 몫) / 원본 B** (§10-14-b) |
| 원본 B 정본 | **배정 절의 행 추출 − 「💼 면접 활용」 블록의 인용 줄만**(§10-14-c) |
| 표 예산 | **4,485 ± 335** (절대값) |
| 도식 예산 | **원본 도식 B × 1.263** (계수) |
| 🔴 낱말 | 「기획」은 시리즈 A 와 뜻이 다르다. 편1이 갈라 뒀고 편2~편8 이 그 용법을 따랐다 |

### 편별 실측

발행 B 는 **현재 파일 크기**(`compose` 규약 = `wc -c` + 1)이며 리뷰 반영과 링크 부착이 누적된 값이다.

| 편 | 원본 B | 발행 B | 링크 몫 | 역산 k | 초고 이탈 | 감축·증량 회차 | 리뷰 반영 |
| :---: | ---: | ---: | ---: | ---: | ---: | :---: | ---: |
| 2 | 6,727 | 17,186 | 55 | 2.182 | — | — | +243 |
| 3 | 5,578 | 14,745 | 55 | 🔴 2.193 초과 | — | — | +551 |
| 4 | 8,118 | 19,409 | 57 | 2.081 | +13.9% | 2 | +344 |
| 5 | 7,834 | 19,500 | 56 | 2.169 | — | 3 | 발행 전 |
| 6 | 7,389 | 18,535 | 65 | 2.169 | 🔴 +28.3% | 6 | 🔴 **+822** |
| 7 | 7,511 | 17,973 | 57 | 2.058 | 🔴 −14.8% | 3 | 🔴 −361 |
| **8** | **9,430** | **22,456** | **0** | **2.121** | 🔴 **+22.6%** | **8 + 재감축 2회** | 🔴 **+136 → 감축** |

⚠️ 편8의 링크 몫 0 은 **미달이 아니다 — 다음 편이 없다.** 링크가 붙지 않은 편은 **링크를 뺀
설계값과 비교**한다(§10-14-a).

---

## 알려진 문제

- 🔴 **`compose` 통과는 배정 타당성이 아니다.** 성분 실측 **바로 다음에** 배정 원본의 금칙어를
  직접 세라. `LC_ALL=C` 필수다. 편8 배정 원본에도 「야나두」가 1회 실재했고 **검사기가 아니라
  손 세기가 막았다.**
- 🔴 **표 배율을 `원본 표 B × 배율` 로 예측하지 마라.** 표는 **절대값 예산**으로 관리한다.
- SOFT 2건이 `agentic-coding/thirty-agent-catalog.md:195` 에 상시 남아 있다 — 기존 건이며 정당하다.
- 분량 하한 SOFT 5편(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은 이 배치 밖의 옛 편들이다.
- 「강의」는 SOFT 금칙어다. 원본이 강의 노트라 **출처를 지칭하면 걸린다** — 편8은 원본의
  「강의 시점 기준」을 **「집계 시점 기준」**으로 옮겨 피했다. 초판의 「2022년 기준」은
  **원본에 없는 연도를 만든 것**이라 정확성 리뷰가 잡았다.

---

## 도구 함정 — 이번 세션에서 실제로 겪은 것

`docs/TOOL-TRAPS.md` 가 전체(41건)를 담고 있다. 이번에 다시 걸린 것만 적는다.

| 함정 | 이번 세션에서 |
| --- | --- |
| **히어독이 한글 마크다운에서 죽는다** | 치환 스크립트를 `python - <<'PYEOF'` 로 넘기다 `unexpected EOF`. **`Edit` 로 전환**했다. 실패하는 것은 긴 한글 **본문**이지 짧은 스크립트가 아니다 |
| **`sed` 의 구분자가 표에서 충돌한다** | CHANGELOG 표를 `s|…|…|` 로 고치려다 파이프와 부딪혔다. **`Edit` 로 전환** |
| **파이썬 쓰기가 개행을 바꾼다** | `io.open(..., newline='')` 으로 열고 써서 CR 0 을 유지했다. **쓰고 나서 `tr -cd '\r' \| wc -c` 로 확인하라** |
| **`grep -c` 는 줄 수를 센다** | 편7에서 링크 3개를 2 로 셌다 |
| **awk `length()` 는 바이트가 아니다** | 편8 하위 절을 재려다 걸렸다. 바이트는 `wc -c` 다 |
| **`core.autocrlf` 경고** | 커밋 시 「LF will be replaced by CRLF」가 뜬다. 정상 동작이며 **깨끗한 diff 는 증거가 아니다** |
| **서브에이전트에게 `Write` 가 없다** | 두 리뷰어 모두 히어독으로 보고서를 썼다. 브리프에 그 방법을 적어 두면 유휴가 되지 않는다 |
| **`AskUserQuestion` 이 길면 파싱에 실패한다** | 옵션 설명을 줄여 다시 호출했다 |
