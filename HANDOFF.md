# 인수인계 (HANDOFF)

**갱신일**: 2026-08-08
**브랜치**: `main`
**작업트리**: 이 문서 · `CHANGELOG.md` · `agentic-coding` 설계서만 미커밋 / stash 없음
**발행본 변경 없음** — 이번 세션은 설계만 했다. 블로그는 82편 그대로다.

---

## 1. 이번 세션에서 한 일

**`agentic-coding` 분할 설계를 마쳤다. 원고는 한 편도 쓰지 않았다.**

```
조사 6기 병렬 ─→ 설계서 초안 ─→ 검증 2기 (양쪽 반려)
                     ↓
                  개정 ─→ 재검증 ─→ 재개정 ─→ 3회차 검증
                     ↓
              설계 확정 (승인 대기)
```

| 단계 | 산출 |
| --- | ---: |
| 조사 6기(파일 축 5 + 전역 축 1) | 보고 6건 · 총 301KB |
| 검증 2기(산술·논리) 3회차 | 지적 **42건** 전량 반영 |
| 설계서 | 1,036행 |

**조사자 6기 전원이 컨트롤러 브리프의 전제를 뒤집었다.** 그리고 검증 2기가 **설계서에만 있는 오류** 42건을 잡았다 — 조사 단계에는 없던 것들이다.

---

## 2. 다음 세션이 이어받을 것

### 최우선: `agentic-coding` 1단계 승인 → B1 착수

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md)

```
원본 555,706 B (12편 542.7KB)
  − 80,246  이관 — 06 전량 + 08 §4~§8·§10   → ai-transformation
  − 50,477  면접 절 8 + 인용 절 8            → Q&A 수확 + 조건 이관
  − 20,602  01 드롭 (카테고리 부적합)
  − 19,999  00 + 10                          → 지도 재작성 / 문항 목록
  −  1,833  머리말 4편분 + CRLF
  ────────
   382,549  →  발행 31편 · 468.2KB · 편당 18.0KB · 도식 85
```

### ⚠️ 착수 전 반드시 할 것 2건

| # | 작업 | 왜 먼저인가 |
| --- | --- | --- |
| 1 | **요구사항 §13-1 정정** — `ai-transformation` 18편 → `+ aiagent/06 전량 · 08 §4~§8·§10`, `agentic-coding` 12편 → 실질 10.5편 | 기록하지 않으면 `ai-transformation` 설계가 **`06`이 오는 줄 모른 채** 18편으로 시작한다. diff는 설계서 §2-1에 있다 |
| 2 | **`tags.ts` L30 주석 수정** — `agentic-coding`을 예외 목록에서 삭제 | 변환자가 그 태그를 달면 `isKnownTag`가 false를 반환해 **빌드가 막힌다.** diff는 설계서 §8-1에 있다 |

### 착수 전 결정 1건 (기본값 있음)

| 사안 | 기본값 | 근거 |
| --- | --- | --- |
| **편21 자기참조** — `08 §9`에서 이 리포지터리의 실제 CI·배포 관행을 근거로 쓸 것인가 | **쓰지 않는다** | 기회: 발행본 51편의 「조직에 옮기면」 표가 전부 가정법인데 이 편만 실측 근거를 댈 수 있다. 위험: `deploy.yml`이 preview 없이 `main`=프로덕션이라는 사실이 `08 §9`의 "자동 롤백 임계"와 대비되면 **약점 공개**가 된다 |

### 배치 계획 — 순서가 개념 의존을 따른다

| 배치 | 대상 | 편 | 편수 | 이 배치에서 해소할 것 |
| ---: | --- | --- | ---: | --- |
| **B1** | `01`(축약) + `02` | 1~5 | 5 | §6-2 출처 3층 규칙 확립 |
| **B2** | `04` | 9~11 | 3 | §5-3 정합성 부채 2건 |
| **B3** | `05` | 12~14 | 3 | §6-7 제품 결함 3건 · §6-8 Forge |
| **B4** | `03` + `team-productivity` | 6~8, 26 | 4 | **`02`·`04`·`05` 뒤라야 전방 참조가 닫힌다** |
| **B5** | `07` | 15~18 | 4 | §5-4 CLAUDE.md 60/200 · §6-6 개인 이력 |
| **B6** | `08`(개발축) | 19~21 | 3 | 편21 자기참조 결정 적용 |
| **B7** | `09` | 22~25 | 4 | §6-5 10번 표 캡션 승격 |
| **B8** | Q&A 4편 | Q1~Q4 | 4 | 전 배치 완료 후 |
| **B9** | 지식 지도 | — | 1 | **맨 마지막** |

**`03`이 네 번째인 이유**: `03` 본문이 `PreToolUse` 훅(→`04`), `Agent Teams`(→`05`), `Plan Mode`(→`02`)를 **설명 없이 쓴다.** 앞에 두면 미정의 개념이 등장한다.

### 진행 방식 — 검증된 절차

```
1단계  분할 설계만 하고 보고 → 승인       ← 완료. 승인 대기
2단계  배치로 나눠 변환, 배치마다 커밋·보고
3단계  구현 미참여 에이전트가 독립 검증
4단계  지적 반영 → 푸시
```

**조사와 변환의 전략이 정반대다.** 조사는 맹점을 깨려고 나누고(6기 병렬), 변환은 톤을 지키려고 합친다(배치당 1기 순차).

---

## 3. 이번 카테고리 고유의 위험 — 설계서가 다루는 것

### 3-1. `강의`가 문서의 서술 주체다 — 160행

| 금칙어 | 행 수 | 직전 카테고리 |
| --- | ---: | ---: |
| **`강의`** | **160** | 1건 |
| `면접` | 68 | — |
| 화행 잔재(SC-12) | 25 | 9 |

160행 중 **130여 개가 `강의가 ~한다` 형태**다. 단어를 지우는 문제가 아니라 **문장의 주어를 바꾸는 문제**이고, CR-1.10이 정확히 금지한 작업이다. 설계서 §6-4가 세 갈래 판별 기준을 준다.

| 갈래 | 판별 | 처리 |
| --- | --- | --- |
| ㉠ 개념·비유 | 출처 없이 읽어도 **검증 가능한 주장이 되지 않는다** | 주어를 지우고 대상의 성질로 |
| ㉡ 수치·벤치마크 | 출처 없으면 **검증 불가능해진다** | `원 자료`로 주체 보존 + 시점 |
| ㉢ **귀속 구분** | 문장이 **"원 자료에 있다/없다"를 말한다** | `원 자료`로 치환하되 **부정을 보존** |

㉢은 실측 3건이다. `02` L294 『이 3등급 분류는 **원본 강의에 그대로 나오지는 않는다**』 — ㉠을 쓰면 주체가 사라지고 ㉡만으로는 **부정이 뒤집힌다.**

### 3-2. 개수 대조가 통과하는 정합성 부채 2건 — 새 유형

CR-6.1은 "항목 수를 세라"고 한다. **이 둘은 세면 0인데도 문제다.**

| # | 부채 | 해소 위치 |
| --- | --- | --- |
| 1 | 발행본 `harness-five-primitives`는 「**다섯** 가지」, 원본 `04`는 「**4종**」. 두 목록이 서로를 포함하지 않는다(한쪽 Memory·Agent, 다른 쪽 MCP·플러그인). **인접 위치에 놓이면 독자는 둘 중 하나가 틀렸다고 읽는다** | 편9 — 대조 문단 4요건 |
| 2 | MCP 컨텍스트 비용을 두고 **발행본과 결론이 반대**다. 발행본 "필요 시 로드라 상시 비용 아님" / 원본 "상시 토큰 점유, CLI 대비 35배". **어느 쪽도 시점을 명시하지 않았다** | 편10 — 병기 + 양쪽 시점 추정 금지 |

### 3-3. 금칙어 절이 붙들고 있는 조건 11건

「인용 시 주의」 절은 원본 8편이 갖고 있고 **그중 3건은 출처 대장**이다. 통째로 걷으면 본문 수치가 조건 없는 항구 사실로 남는다.

가장 위험한 것: `09 §6-1`이 *"이 수치는 원 자료가 제시한 예시 값이며 실측 벤치마크가 아니다"*를 **세 번 반복**한다. 이 고지가 사라지면 8셀 표(2~3시간↔15~30분, 40%↔95%)가 **검증 가능한 실측처럼 발행된다.** 설계서 §6-5가 11건의 목적지를 편 번호로 지정했다.

### 3-4. 원본 내부 모순 3건

| 축 | 값 A | 값 B | 처리 |
| --- | --- | --- | --- |
| CLAUDE.md 상한 | **60줄**(`07 §2-2`) | **200줄**(`03 §2-4`) | **원본이 구분을 이미 했다** — 60은 설계 권고치, 200은 `03` L108이 직접 쓴 **준수율 임계**. 각 편에서 성격을 밝히고 서로 링크 |
| 재시도 상한 | 3회 | 5회 | 편15·16이 적용 범위를 문장으로 |
| Core Web Vitals | `FID(INP)` 병기 | `FID`만 | 편25에서 `INP` 병기 + 시점 |

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. 1차·2차 발행본과 프로덕션에서 동일 재현. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 성능 77점. 히어로 진입 애니메이션이 하이드레이션에 의존. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **2차 전체를 마친 뒤 재측정** |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | ~~`tags.ts` 주석 불일치~~ | **판정 완료** — 어휘에 추가하지 않고 주석에서 삭제한다(설계서 §8-1). 실행은 다음 세션 |
| 7 | **어휘 81종 중 30종이 0편** | `redis` `kafka` `docker` `aws` `spring` `java` `nosql` `load-testing` `ci-cd` `deployment` `distributed-systems` `event-driven` `cdc` `cqrs` `microservices` `scalability` `vector-database` `machine-learning` `product-discovery` `prd` `product-metrics` `ab-testing` `user-research` `commerce` `payments` `global-service` `startup` `team-building` `career` `recommender-systems`. **어휘에 있는 것과 살아 있는 것은 다르다** — 한 카테고리에서만 쓰면 싱글톤으로 죽는다 |

### `ai-transformation`으로 이월된 위험 3건

`06`을 이관하면 조사자 D가 찾은 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표.** 발행하지 않는 선택지 우선 검토 |
| 2 | **`06`은 발행본 51편과 서사 방향이 반대다.** 51편은 "위임을 늘려라", `06`은 "어디서 사람을 세울 것인가". 실은 모순이 아니라 `delegation-gap` L197의 3조건을 판정 격자로 편 것 — **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

### 5-1. README에 블로그가 여전히 언급되지 않는다

**세 번째 이월이다.** README는 2026-05-02 이후 손대지 않았고 그 사이 **13커밋**이 더 쌓였다(직전 이월 시점 대비).

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 82편·3카테고리가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

### 5-2. ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. 이 사이트가 쓰는 환경변수가 아니라 RAG·에이전트를 설명하는 **블로그 글 본문의 예시 코드**다. `.env.example`을 만들거나 README에 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이라 사람이 직접 부르지 않는다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정(중복 판정 뒤집힘·금칙어 충돌·자동 검사 사각 3종)은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)에 있다. **아래는 이번에 새로 나온 것이며 설계서 §13에 규칙으로 적어 뒀다.**

### 6-1. 조사 보고를 표로 옮길 때도 세어야 한다

**초안이 반려된 최대 원인이다.** 조사자들이 계산에 포함한 `§0 용어 배분` 열을 표로 옮기며 지웠고, 그 결과 **4편이 하한 미달**이 됐다. 모집단 표시(`06` 제외)를 빼서 검산이 불가능해지기도 했다.

> §15-1은 "중복 판정이 조사→설계서→브리프를 거치며 뒤집힌다"고 했다.
> **이번에 뒤집힌 것은 종합 단계에서 새로 들어간 오류다.** 조사자는 정확히 셌다.

### 6-2. 검산 표를 두 개 만들면 오류가 상쇄될 수 있다

도식 총계가 §4-1(편별)과 §4-3(집계)에서 **둘 다 85**였는데 참값은 84였다. `07`을 한쪽이 14로, `09`를 다른 쪽이 9로 세어 정확히 상쇄됐다. **두 표를 교차 대조해도 통과한다.** 원본을 다시 재야 잡힌다.

### 6-3. 정정은 그 값이 흐르는 **모든 곳**에 반영된다 — 네 번 반복했다

| 회차 | 무엇 |
| --- | --- |
| N1 | `§12-2` 배분을 주석표엔 맞게, **분할표엔 반대로** |
| N2 | §4-2에 예외 배율을 승인해 놓고 **§1 총량은 일괄 1.2** |
| N3 | 링크를 「위와 동일」로 둬 **다른 편 slug**를 가리킴 |
| R1 | 시점 규칙을 고치고 **「확정 처리」 표는 옛 지시 유지** |

넷 다 **규칙은 맞게 고쳤는데 그 규칙을 쓰는 자리가 옛 값을 유지**했다. sed 일괄 치환도 같은 함정이 있다 — 무관한 절과 과거 오류 설명문까지 바꿨다.

### 6-4. 추정치가 확정 연산자로 승격된다

`~800`이라고 쓴 값이 두 곳에서 `800 B`로 바뀌어 사슬 연산에 들어갔다. 검증자가 열 단위로 재니 **실측 1,244 B**였다. **`~` 하나가 빠지면서 추정이 사실이 됐다.**

### 6-5. 「N행」 축약 표기를 금지한다

`§12-2 5행`이 「행 번호 5」인지 「행이 5개」인지 구분되지 않아 **배분이 실제로 뒤집혔다.** 항상 `행5 하나`·`행1·2·3·4·6`처럼 번호를 나열한다.

### 6-6. 잔차로 정의된 항을 「검산 오차 0」이라 부르지 마라

사슬 마지막 항이 차이를 흡수하면 **어떤 행 합이 와도 0이 되어** 검산이 성립하지 않는다. 실제 차(13 B)를 그대로 적는다.

### 6-7. 서브에이전트 최종 응답은 유실될 수 있다

검증자 2기가 `idle`을 보고했으나 **최종 응답 텍스트가 컨트롤러에 도달하지 않은 일**이 세 번 있었다. 조사자 6기는 파일 산출물을 지시해 6건 전부 남았다.

> **브리프에 산출물 경로를 파일로 못박아라.** "최종 응답으로 보고하라"만으로는 유실된다.

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 142개 URL

# 금칙어 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]"

# 민감정보 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 오탐 많음. 문맥 판정 필수
Select-String -Path "content\blog\**\*.md" `
  -Pattern "인용할 때|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안"
```

> **알려진 오탐 4종**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **82편 중 77편이 갖고 있으며 정규식은 영문만 잡는다.** 금칙 목록이 겨냥하는 것은 본문의 개인 실명·핸들(CR-3.5)이지 출처가 아니다
> - `채용` — `agentic-coding` 편14(`05 §10-2`)에 「**채용**과 JD 작성」이 있다. 구직이 아니라 **에이전트 정의를 채용에 비유한 것**
> - 화행 정규식 — `이렇게 구현하는 편이 안전하다`(구현 권고), `LLM이 답한다`(주어가 모델)는 정상

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge"
LC_ALL=C awk '
  /^```/ { fence=!fence; if (fence && $0 ~ /^```mermaid/) m++; next }
  !fence && /^## / { h2++ }
  fence && /^## / { fake++ }
  END { print FILENAME, "H2="h2, "mermaid="m, "fake="fake }' <파일>
```

**펜스를 추적하지 않으면 `agentic-coding` 원본의 H2가 127이 아니라 159로 세어진다** — 코드블록 안 가짜 H2가 32개(`03`에만 10개). 절 경계가 25% 늘어 분할이 통째로 어긋난다.

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — 82편 |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, 3개 발행 |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종(실사용 51). **목록 밖이면 빌드 실패** |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15. **§13-1을 먼저 정정할 것** |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **승인 대기** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |

**조사·검증 산출물 8건은 세션 스크래치패드에 있었고 이 저장소에 남지 않는다.** 설계서가 그 결론을 전부 담고 있으므로 다음 세션이 조사를 다시 할 필요는 없다 — 다만 **설계서를 세어 보고 어겨도 된다**(금지선 1).
