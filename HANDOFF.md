# 인수인계 (HANDOFF)

> 갱신: 2026-08-18 · 브랜치 `main` · 최신 커밋 `124d64b`
> `fix(blog): F3 해소 — source 프론트매터 109편을 걷고 금칙어 검사기를 세운다`
>
> **발행본 124편 · sitemap 187 URL**

---

## 1. 이번 세션에서 한 일

사용자가 §10의 **F3(최우선)** 을 골랐고, `source:` 109편은 **전량 제거**로 판정했다.

| # | 무엇 | 규모 |
| ---: | --- | --- |
| 1 | `source:` 프론트매터 제거 | **109편** (파일당 정확히 1줄) |
| 2 | `source` 필드를 타입·파서·렌더·테스트에서 제거 | **6곳** |
| 3 | 「패스트캠퍼스」 예시 교체 → 「스타벅스」 | `search-engineering` **2편 · 10문자열** |
| 4 | `scripts/check-forbidden.mjs` **신설** | self-test 8건 |
| 5 | 규칙 문서 갱신 | GC-11 · CR-1.10 · 스키마 · CLAUDE.md · README |

**검증(전부 단독 실행)**: `npm test` 36건 · `tsc` 0 · `build` 0 · sitemap **187 불변** ·
`index.html` 196개 · self-test **8/8** · 발행본 **HARD 0건** · `out/` 잔존 **0회**

---

## 2. ⚠️⚠️ 최상위 — **거짓 음성은 「못 찾음」이 아니라 「없음을 확인함」으로 기록된다** (금지선 53)

직전 세션의 CHANGELOG가 이렇게 적었다:

> 실측 결과 금칙어는 **124편 중 이 한 편에만 7건** 집중돼 있었다(다른 4개 카테고리 0건).

**거짓이었다.** 같은 카테고리의 다른 **두 편에 6건**이 더 살아 있었다.
측정이 틀린 게 아니라 **측정 도구(금칙어 목록)가 결함**이었고, 결함 있는 도구가 낸 0건이
「확인함」이라는 확신으로 문서에 박혔다.

⇒ **검사기는 「무엇을 잡는지 증명한 뒤에만」 0건을 주장할 수 있다.**
`dup-scan`의 `--self-test` 관례가 왜 있는지를 이번에 검사기 자신이 증명했다 —
`check-forbidden.mjs`를 만들자마자 **두 번 실패**했고 **둘 다 self-test가 잡았다**
(히어독 백슬래시로 인한 `SyntaxError` · `heats`가 `Cheatsheet`에 걸린 오탐).

---

## 3. ⚠️⚠️ **규칙을 문서에 복사하면 갈라진다 — 목록의 정본은 코드다** (금지선 54)

F3의 원인은 「낱말이 빠졌다」가 아니라 **「목록이 사는 자리」** 였다.

| 실측 | 값 |
| --- | ---: |
| 정본 규칙 `GC-11`의 낱말 수 | **5개** (전부 취지 낱말. 고유명사는 **아예 없었다**) |
| 실무 목록(HANDOFF)의 낱말 수 | 20개 이상 |
| 목록이 복사돼 있던 문서 수 | **9개** |
| 그중 한글 표기형을 담은 문서 | **0개** |

정본은 2026-08-07에 멈춰 있었고 실무 목록만 자랐다. 어느 쪽이 정본인지 알 수 없으니
아무도 갱신하지 않았다. ⇒ GC-11을 **「목록의 정본은 `scripts/check-forbidden.mjs`다.
문서에 복사하지 마라」** 로 바꿨다.

---

## 4. ⚠️⚠️ **지우는 조작에도 파급이 있다 — 지운 것이 남의 근거일 수 있다** (금지선 55)

직전 세션이 「지우기는 안전하고 **메우기**가 위험하다」로 닫았다(금지선 44).
이번에 그 명제의 **빈틈**이 나왔다.

`CR-1.10`이 이렇게 적혀 있었다:

> 주체를 지우는 쪽이 없는 주체를 세우는 쪽보다 안전하다. **출처는 frontmatter `source`가 이미 담고 있다**

`source`를 걷자 이 규칙의 **뒷받침이 사라졌다.** 규칙 자체는 유효하되 근거가 없어졌다.
⇒ 해당 자리에 **근거 폐기를 명시**하고 「대조 경로가 없어져 오히려 더 엄해졌다」로 고쳤다.

**지우기가 안전한 것은 「그 자리」에서다. 다른 자리가 그것을 참조하고 있으면 안전하지 않다.**
지우기 전에 참조 지형을 먼저 실측하라.

---

## 5. ⚠️⚠️ **한 규칙으로 두 표기 체계를 다루면 오탐이 난다** (금지선 56)

검사기가 만들어지자마자 `heats`(「히츠」의 라틴 표기라며 **컨트롤러가 근거 없이 지어낸 것**)가
"C**heats**heet"에 **7건** 걸렸다.

| 표기 체계 | 단어 경계 | 근거 |
| --- | :---: | --- |
| 라틴 | **요구한다** | 요구하지 않으면 `Cheatsheet`에 걸린다 |
| 한글 | **요구하면 안 된다** | 조사가 붙어 「테디노트**의**」가 되므로 **경계가 존재하지 않는다** |
| 1인칭 | **검사하지 않는다** | 양방향 실패 실측 — 「메이**저는**」(위양성) · 「**내** 검색 커리어」(위음성) |

⚠️ **「한글 표기형을 추가하라」가 「라틴 표기형을 지어내라」는 뜻이 아니다.**
실재를 확인하지 않은 표기형을 넣으면 오탐이 된다. 이 오탐은 **self-test 케이스로 고정**했다.

**못 하는 일을 하는 척하는 검사기가 가장 위험하다** — 1인칭을 목록에 넣었다면 오탐에 묻혀
진짜 위반이 안 보였을 것이다.

---

## 6. ⚠️ **파생값이 얽힌 표는 낱말 하나만 바꿀 수 없다** (금지선 57)

「패스트캠퍼스」를 바꾸려는데 표의 **네 칸이 그 낱말에서 파생**된 값이었다.

| 기능 | 기존 | 신규 |
| --- | --- | --- |
| 초성 추출 | 패스트캠퍼스 → `ㅍㅅㅌㅋㅍㅅ` | 스타벅스 → `ㅅㅌㅂㅅ` |
| 자소 분해 | → `ㅍㅐㅅㅡㅌㅡㅋㅐㅁㅍㅓㅅㅡ` | → `ㅅㅡㅌㅏㅂㅓㄱㅅㅡ` |
| 영타 → 한글 | `votmxmzoavjtm` → | `tmxkqjrtm` → |
| 한타 → 영문 | `ㄹㅁㄴㅅㅊ…` → `fastcampus` | `ㄴㅅㅁㄱㅠ…` → `starbucks` |

손으로 계산하면 자판 매핑 한 글자가 틀려도 **기술 문서가 거짓**이 된다
(직전 세션이 바로 그 결함을 리뷰에서 잡았다). 두벌식 변환 스크립트를 짜되,
**기존 값 2개를 역산해 재현되는지 먼저 확인**하고 나서 대체값을 뽑았다 —
검사 도구를 먼저 검사한다는 §2와 같은 원리다.

「스타벅스」를 고른 것은 같은 문서가 이미 「'스벅'→'스타벅스'」를 동의어 예시로 쓰고 있어서다.

---

## 7. ⚠️ **적용 범위가 곧 정책이다** (금지선 58)

`--all`로 리포 전체를 훑으면 HARD **490건**이 나온다. 그런데 발행본은 **0건**이다.

| 자리 | HARD | 성격 |
| --- | ---: | --- |
| `pages/index.tsx` · `data/portfolio.ts` · `pages/product-lead*` | 92 | **포트폴리오 — 실명을 담는 것이 목적이다** |
| `docs/**` · `HANDOFF.md` · `CHANGELOG.md` | 약 400 | 내부 문서. 금칙어 목록 자체를 담는다 |
| `content/blog/**` | **0** | 정책 대상 |

전부 위반으로 처리하면 검사기가 늑대소년이 되고 진짜 위반이 소음에 묻힌다.
⇒ **`--all`은 판정하지 않는다(종료 코드 언제나 0).** 조사 도구이고 판정은 사람이 한다.

---

## 8. 다음 세션이 이어받을 것

### ▶ **다음 배치는 미정이다. 사용자가 정한다.**

| 카테고리 | 편수 | 설계서 | 지도편 | 상태 |
| --- | ---: | --- | :---: | --- |
| `ai-agent` | 51 | 있음 | ✅ | 완료 |
| `agentic-coding` | 31 | 있음 | ✅ | 완료 |
| `rag` | 25 | **없음** | ✅ | — |
| `ai-transformation` | 11 | 있음 | ✅ | 완료 |
| `search-engineering` | 6 | **없음** | ❌ | 익명화·고립 해소 완료 |

**합계 124편 · sitemap 187 URL.**

### S3(지도편) — **보류 판정. 재검토하려면 이 판정부터 반증하라**

조사 α가 「지도편 필요」를 권했으나 채택하지 않았다. 근거:

1. **6편이면 카테고리 인덱스로 충분하다.** 지도편이 정당했던 곳은 `ai-transformation`(11편)·`rag`(25편)다
2. 당시 이 카테고리는 **양방향 고립**이었다 — 지도편을 얹어도 **7번째 섬**이 될 뿐이었다
3. `ai-transformation` 지도편이 통한 것은 그 카테고리가 **이미 밖과 이어져 있었기** 때문이다

⇒ **S2로 고립이 해소돼 근거 2가 약해졌다.** 다시 판정하려면 위 3개를 각각 다뤄라.

### 원본에 남은 미사용 소스

`C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` (**읽기 전용 · 수정 금지**)

| 디렉터리 | 문서 | 분량 | 비고 |
| --- | ---: | ---: | --- |
| `java-backend` | 11 | 638 KB | |
| `langgraph` | 12 | 510 KB | `rag`에 이미 3편 반영됨 |
| `high-traffic` | 11 | 508 KB | |
| `pm-harness` | 8 | 402 KB | |
| `pangyo-it` | 6 | 346 KB | |
| `pm-po` | 17 | 327 KB | |
| `bigdata` | 8 | 293 KB | |
| `learning` | 10 | 198 KB | |
| `recommendation` | 7 | 84 KB | 검색과 일부 교차 |
| `llm-search-recommend` | 1 | 37 KB | ⚠️ `rag`와 주제 충돌 — §10-3 |

⚠️ **`search/` 6개는 발행본 6편으로 1:1 소진됐다.** 미사용 소재 없음.

새 카테고리를 열면 **설계서부터 쓴다**(분할표 · 배치 계획 · 축 B 정의 · 금지선 승계).

### 확정된 결정 (다시 묻지 마라)

| 항목 | 결정 |
| --- | --- |
| 원본 | **읽기 전용.** 수정 금지 |
| 푸시 | **사용자가 명시적으로 요청할 때만** |
| 커밋 메시지 | **한글** |
| 커밋 구성 | 작업 커밋 = 코드 + CHANGELOG + README + 설계서 / 인수인계 커밋 = HANDOFF **별도** |
| ~~`source` 프론트매터~~ | **2026-08-18 폐지.** 필드 자체를 제거했다. 되살리지 마라 |
| **사명 표기** | **익명화한다** — 「OTT 서비스」·「IPTV 서비스」. 단 **제품명은 유지**(코난테크놀러지·IDOL·Elasticsearch) |
| **금칙어 목록** | **`scripts/check-forbidden.mjs`가 정본.** 문서에 복사하지 마라 |
| 태그 | 기존 통제 어휘 안에서만. `MAX_TAGS=6` |

---

## 9. ⚠️ 도구 함정 — 이 세션에서 실제로 걸린 것

| # | 함정 | 대응 |
| ---: | --- | --- |
| **53** | **히어독으로 코드를 쓰면 이중 백슬래시가 한 겹 벗겨진다.** 따옴표를 붙인 `<<'EOF'`인데도 정규식의 이중 백슬래시가 하나로 줄어 `SyntaxError`가 났다 | 단일 이스케이프(줄바꿈·캐리지리턴·점)는 무사하다. **이중 백슬래시만** 벗겨진다. `String.fromCharCode(92)`로 우회하거나 그 부분만 `Edit`/`Write` 도구로 써라. `node -e` 안의 템플릿 리터럴에서도 같은 일이 난다. **히어독 안에 셸 인용 표기가 섞이면 heredoc 자체가 깨지기도 한다** — 이 문서도 그래서 `Write` 도구로 썼다 |
| **54** | **서브에이전트가 유휴가 돼도 보고서가 오지 않는다.** `scout` 2기·`verifier` 1기가 **세 번의 유휴 알림** 동안 아무것도 보고하지 않았다 | `SendMessage`로 **명시적으로 회수**하라. 그래도 안 오면 컨트롤러가 직접 실행하고 **「제3자 검증이 아님」을 결과에 명시**하라. 기존 메모에 `reviewer` 사례가 있는데 **`scout`·`verifier`에서도 재발**한다 |
| **55** | **grep으로 CRLF를 검사하면 거짓 음성이 난다.** 「LF 전용」으로 보고했으나 실제로는 CR이 393개였다 | `tr -cd` 로 **바이트를 세라**. grep은 캐리지리턴을 줄 끝으로 처리해 못 잡는다 |
| **56** | **`core.autocrlf=true`면 `git diff`가 줄바꿈 변경을 숨긴다.** 정규화 후 비교하므로 파일 전체가 바뀌어도 diff는 깨끗하다 | **diff가 깨끗한 것은 무사하다는 증거가 아니다.** 파일의 CR/LF 개수를 직접 세어 대조군과 비교하라. 이 리포는 CRLF와 LF가 **원래부터 섞여 있다**(수정군 111편 중 CRLF 26 / LF 85) |
| **49** | 파이프 뒤의 종료 코드는 마지막 명령의 것이다 | 종료 코드를 볼 명령은 **단독 실행**하라. 이번에도 `tsc`를 `tail`에 물려 놓고 읽어 걸릴 뻔했다 |
| **51** | 한국어 1인칭은 grep으로 분리되지 않는다 | 매칭 줄을 열어 읽어라. **검사기에 넣지 마라**(§5) |
| — | `grep -r`이 `out/`·`node_modules`를 훑어 타임아웃 | `--include` 또는 경로 제한 |
| — | 한글·이모지 grep이 로케일 때문에 거짓 0건 | `LC_ALL=C` 고정 |

---

## 10. 미해결 과제

### 10-1. ⚠️⚠️ **`yanadoo`가 블로그 전 페이지의 OG 이미지 URL에 있다** (최우선 · 사용자 판정 필요)

| 항목 | 실측 |
| --- | --- |
| 자리 | `og:image` · `twitter:image` → `https://withwooyong.github.io/images/Ted_yanadoo.png` |
| 출현 | `out/blog/` 아래 **366회** (페이지당 2회) |
| 성격 | **F3의 거울상** — 이번엔 한글(「야나두」)이 목록에 있었고 **라틴(`yanadoo`)이 없었다** |
| 왜 안 고쳤나 | 파일명을 바꾸면 **이미 공유된 링크의 OG 미리보기가 깨진다**(GitHub Pages라 옛 경로는 404) |

처리하려면 ① `public/images/Ted_yanadoo.png` 개명 ② 참조처 전부 수정
(`pages/index.tsx` · OG 설정 · `data/`) ③ **옛 URL을 남길지 판단**이 필요하다.

> 같은 산출물의 **「허우용」 127회는 JSON-LD `author` 표기**이며 기술 블로그의 표준이라
> **위반으로 보지 않는다.** §10-2의 F1 판정과 같은 선상이다.

### 10-2. F1 — 익명화 전제가 사이트 구조상 무효 (수치가 확정됐다)

`--all` 실측으로 §7의 표가 나왔다. `pages/index.tsx` 37 · `data/portfolio.ts` 23 ·
`pages/product-lead*` 32.

> **이 작업이 실제로 한 일은 신원 은폐가 아니라 「기술글이 이력서처럼 읽히는 것」을 막은 것이다.**
> 사용자가 이를 알고 익명화를 택했다(2026-08-18). **효과의 이름을 바꿔 부르되 작업은 유효하다.**
> 진짜로 은폐하려면 사이트 구조를 바꿔야 하는데 그건 별건이다.

### 10-3. `llm-search-recommend` 37 KB — 발행 제안이 **충돌 미확인 상태**

조사 β가 「벡터 검색 — Sparse·Dense·Hybrid」로 발행을 권했으나 **β는 `rag`를 보지 않았다.**
γ 실측으로 `rag/rag-pipeline-retrieval.md`가 이미 그 범위를 다룬다.
⇒ **발행하려면 겹침을 먼저 실측하라.**

### 10-4. `search-engineering` inbound 0인 두 편

`search-engineering-qna`(inbound 0 / outbound 3) · `search-system-overview`(0 / 10).
조사·리뷰 **모두 「억지 아닌 자리가 없다」로 판정**했고 개수를 채우지 않았다.
후자는 카테고리의 **입구 편**이라 inbound 0이 자연스럽다는 견해도 있다.

### 10-5. SOFT 2건 — **판정 완료. 고치지 마라**

`content/blog/agentic-coding/thirty-agent-catalog.md:195`의 「면접 질문」·「이력서」는
`recruiter` 에이전트의 **기능 설명**이다. 채용 담당 에이전트가 하는 일이지 글쓴이의 구직 활동이
아니다. 검사기가 SOFT로 보고하지만 **정당한 문맥으로 판정했다.**

### 10-6. 이전 이월 (T7·T6·T5 계열)

`git show 6186437:HANDOFF.md`의 §9를 승계한다. 주요한 것:

| # | 자리 | 요지 |
| ---: | --- | --- |
| **T7-d** | 편11 전반 | 자기 감사 분량 — 「빼도 될 30줄」 |
| **T7-h** | 편11 주제 1~13 **1차** 칸 | ⚠️ **아무도 안 쟀다** |
| **T6-a** | 편10 `:143`↔`:145` | ⚠️ **게이트를 세면 5 vs 6** |
| I-13 | 편10 `:230` | 「아홉 편」 — **수정 불요** 판정. 재검토하려면 그 판정부터 반증하라 |
| 1 | mermaid 노드 라벨 우측 클리핑 | `foreignObject` 폭 |
| 3 | 메인 페이지 LCP 6.6초 | Lighthouse 77점 |
| 5 | `cto_learning_roadmap` Q1·Q3 | 발행 가능 판정을 받았으나 미변환 |

---

## 11. 검증 명령

### ⚠️ 순서가 규칙이다 — **증명 먼저, 스캔 나중**

```bash
cd C:/Users/aeby/vscode/withwooyong.github.io

# 1) 검사기가 실제로 잡는지 먼저 증명한다 (8/8이어야 한다)
npm run check-forbidden:verify

# 2) 발행본 스캔 — HARD 0건이어야 발행한다
npm run check-forbidden

# 3) 축자 복제도 같은 순서
npm run dup-scan:verify && npm run dup-scan
```

**1번을 건너뛴 2번의 「0건」은 신뢰할 수 없다.** F3가 정확히 그렇게 생겼다(§2).

### 빌드·테스트 — 종료 코드를 볼 명령은 **단독 실행**

```bash
npm test                 # 36건 통과
npx tsc --noEmit         # 출력 0줄
npm run build            # sitemap 187 URL
```

### 산출물 잔존 검사 — ⚠️ 매칭 줄 수가 아니라 **출현 횟수**를 세라

```bash
# grep -c 는 매칭 '줄 수'다. HTML은 한 줄이라 3회 나와도 1이 나온다
for w in 테디노트 패스트캠퍼스 야나두 yanadoo; do
  echo "$w: $(LC_ALL=C grep -ro "$w" out/ | wc -l)회"
done
```

`source`는 본문뿐 아니라 **`__NEXT_DATA__` JSON에도 직렬화**됐었다(페이지당 2회).
`getStaticProps`가 `post` 객체를 통째로 props로 넘기기 때문이다.
⇒ **화면에서 사라진 것은 해소의 증거가 아니다. `out/`을 세라.**

### 카테고리 간 링크 지형

(A) 링크 출현 횟수와 (B) 고유 파일 수는 **다른 값이다.** 두 정의를 반드시 구분하라.
검산: 내부 링크만 존재하면 inbound 총합과 outbound 총합이 같아야 한다.

---

## 12. 소스 위치

| 무엇 | 경로 |
| --- | --- |
| 원본 (**읽기 전용**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 리포 | `C:\Users\aeby\vscode\withwooyong.github.io` |
| **금칙어 목록 (정본)** | **`scripts/check-forbidden.mjs`** |
| 설계서 | `docs/superpowers/plans/2026-08-{08,14}-*-category-split-design.md` |
| 규칙 GC-* | `docs/superpowers/plans/2026-08-07-tech-blog-phase1.md` |
| 규칙 CR-* | `docs/superpowers/specs/2026-08-07-tech-blog-requirements.md` |
| 금지선 1~20 | `2026-08-08-agentic-coding-…-design.md` L1994-2022 |
| 금지선 21~34 | `2026-08-14-ai-transformation-…-design.md` §11 |
| 금지선 35~43 | `git show 6186437:HANDOFF.md` §2~§5·§8 |
| 금지선 44~52 | `git show 964c8f3:HANDOFF.md` §2~§6·§8 |
| **금지선 53~58** | **이 문서 §2~§7** |

---

## 13. README/문서 갱신

### ✅ 이번 세션 — README **4곳 수정**

| 자리 | 무엇 | 근거 |
| --- | --- | --- |
| 카테고리 내역 | `ai-transformation` **8 → 11** | **실측 오류였다.** 합계 124는 맞는데 내역 합이 121이었다 |
| npm 스크립트 표 | `check-forbidden` · `check-forbidden:verify` 2행 추가 | 수집기 `[HIGH] NPM_SCRIPTS_MISSING_IN_README` |
| 디렉터리 목록 | `scripts/`에 `check-forbidden.mjs` 추가 | 같음 |
| 발행 절차 표 | 「금칙어 검사」 행 추가 | 신설 검사기를 절차에 넣지 않으면 아무도 돌리지 않는다 |

`CLAUDE.md`도 고쳤다 — **「There are no test or lint scripts wired up」이 거짓**이었다
(`npm test`·`npm run lint` 둘 다 있다). 발행 전 검사 절차 표를 추가했다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

수집 스크립트가 `[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README` 5건을 낸다:
`LANGCHAIN_PROJECT` · `LANGCHAIN_TRACING_V2` · `OPENAI_API_KEY` · `TAVILY_API_KEY` · `UPSTAGE_API_KEY`

**소스코드(`.ts`/`.tsx`/`.js`)에 0건**이고 **블로그 글 본문의 코드 예시**에만 있다.
정적 export라 런타임 환경변수가 없다. **README에 「OPENAI_API_KEY 필요」라고 적으면 거짓이 된다.**

> 이 신호는 매 세션 반복해서 뜬다. **매번 다시 조사하지 말고 이 항목을 근거로 넘겨라.**

### 이번 세션 산출물 — `/clear` 후에도 이 경로로 읽어라

`C:\Users\aeby\AppData\Local\Temp\claude\C--Users-aeby-vscode-withwooyong-github-io\0a6b820f-ffca-4256-9ace-90a5a8751181\scratchpad\`

| 파일 | 무엇 |
| --- | --- |
| `hangul.js` | 두벌식 자판·자소 변환기. **기존 문서 값을 역검증**하는 데 썼다 |
| `strip-source.js` | 프론트매터 범위를 계산해 `source:`만 지운 스크립트 |
| `replace.js` · `doc-update.js` · `readme.js` | 치환 건수를 세며 고치는 패턴 (매칭 1건이 아니면 쓰지 않는다) |
| `build.log` · `test.log` · `tsc.log` | 검증 원문 |

⚠️ **스크래치패드는 세션별이다.** 다른 세션에서 열려면 이 절대 경로를 그대로 써라.
