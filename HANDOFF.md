# 인수인계 (HANDOFF)

> 갱신: 2026-08-18 · 브랜치 `main` · 이번 세션 커밋 **27개**
> **발행본 128편 · sitemap 196 URL · 카테고리 6개 발행 (12개 등록)**
>
> 이번 세션의 본체는 **발행 게이트 2단 신설**과 **규칙 105개 전수 분류**,
> 그리고 그 게이트를 처음 통과시킨 **`backend-engineering` 4편 발행**이다.

---

## 1. 이번 세션에서 한 일

사용자 질문은 「왜 며칠째 걸리는가」였고, 답은 **발행이 느려진 게 아니라 발행을 하고 있지 않았다**였다.

| 구간 | 편수 | 작업일 | 일평균 |
| --- | ---: | ---: | ---: |
| 08-07 ~ 08-08 | 89 | 2 | **44.5** |
| 08-09 ~ 08-13 | 26 | 4 | 6.5 |
| 08-16 ~ 08-18 | 11 | 3 | **3.7** |

12배 감속인데 원인은 변환 난이도가 아니었다. **인수인계를 뺀 작업 커밋 7개 중 발행은 1개**였고
나머지 6개는 이미 낸 124편을 되짚는 소급 수정이었다. 그 소급 수정 4건
(`source` 109편 · OG 366곳 · 금칙어 13건 · 카테고리 고립 6편)이 **전부 기계가 잡을 수 있는
종류**였는데 자동 게이트가 **0**이었다 — 훅 없음, CI는 `lint`와 `build`만.

| # | 무엇 | 규모 |
| ---: | --- | --- |
| 1 | 감속 원인 실측 | 진단 |
| 2 | 게이트 2단 (pre-commit 훅 + CI 8단계) | 커밋 4 |
| 3 | 검사기 5종 신설·보강 | 커밋 9 |
| 4 | 규칙 105개 전수 분류, 사장 14건 폐기 | 커밋 4 |
| 5 | `backend-engineering` 첫 배치 발행 | **4편** |

**검증(전부 단독 실행)**: `check-forbidden:verify` 15/15 · `check-forbidden` HARD 0(130파일) ·
`vitest` 50/50 · `tsc` 0 · `build` 0(sitemap 196) · `check-forbidden:built` HARD 0(384파일) ·
`check-baseline` 14개 불변.

---

## 2. ⚠️⚠️ 최상위 — **다음 세션이 가장 먼저 할 일: `dup-scan`을 한 편씩 돌려라**

같은 4편을 **한 번에** 넘기면 **0건**, **한 편씩** 넘기면 **10건**이 나왔다.

```
$ npm run dup-scan -- <4개 파일>
축자 복제 스캔 — 대상 4편 · 대조 124편     ← 대상 4편이 대조 집합에서 빠진다
합계 0건

$ npm run dup-scan -- <1개 파일>  (x4회)
축자 복제 스캔 — 대상 1편 · 대조 127편
합계 2 + 4 + 1 + 3 = 10건
```

**문서화된 사용법(`dup-scan <파일...>`)이 가장 필요한 경우 — 새 배치를 통째로 검사하는 경우 —
에 거짓 0을 낸다.** `:verify`는 통과한다. 검사기는 작동하는데 **호출 방식이 대조 집합을 비운다.**
금지선 53(「증명 없는 0건은 거짓 음성과 구분되지 않는다」)의 새로운 형태다.

⇒ **다음 배치는 반드시 한 편씩 돌려라.** 원본 10개면 25~40편이 나오고, 그 안에서 서로를 베끼는
것은 기존 128편을 베끼는 것보다 훨씬 잦다.

⇒ **고치는 편이 낫다**: `dup-scan`이 여러 대상을 받으면 대상끼리도 대조하게 하거나, 최소한
「대상 간 대조는 하지 않는다」를 출력에 명시하게 하라. 지금은 **조용히 0을 낸다.**

---

## 3. ⚠️⚠️ **pre-commit 훅은 인덱스가 아니라 작업트리를 본다**

훅의 `vitest run tests/blog`는 `readPosts()`로 `content/blog`를 **파일시스템에서** 읽는다.
스테이지에 올린 것만 보지 않는다.

⇒ **여러 편을 먼저 다 써 두고 하나씩 커밋하면 각 커밋은 통과하는데, 그 커밋을 체크아웃하면
링크가 깨진다.** 이번 배치는 한 편씩 쓰고 바로 커밋해 문제가 되지 않았다.
**다음 배치는 편수가 많아 그렇게 하기 쉽다.**

임시 대응: **편별로 쓰고 즉시 커밋**한다. 근본 대응(훅이 `git stash -k`로 작업트리를 인덱스
상태로 맞춘 뒤 검사)은 훅을 느리게 하고 실패 시 복구가 까다로워진다 — 판단이 필요하다.

---

## 4. ⚠️⚠️ **`git commit -m`이 다른 에이전트의 스테이지를 쓸어 담는다**

`git add`를 **명시 경로로만** 해도 `git commit -m "…"`은 **인덱스 전체**를 커밋한다.
이번에 편1 첫 커밋에 다른 에이전트의 `tests/blog/content/links.test.ts` 수정이 함께 들어갔고,
`git reset --soft HEAD~1` 후 아래 형태로 원복했다.

```bash
git commit -m "메시지" -- <경로1> <경로2>
```

| 방식 | 남의 스테이지 |
| --- | :---: |
| `git add .` → `git commit -m` | 쓸어 담김 |
| `git add <경로>` → `git commit -m` | **쓸어 담김** |
| `git add <경로>` → `git commit -m "…" -- <경로>` | 안전 |

⇒ **병렬 에이전트 브리프에 「`git add` 명시 경로만」으로는 부족하다.** 커밋 **형태**를 지정하라.
금지한 것은 `add`인데 새는 곳은 `commit`이었다.

---

## 4-b. ⚠️⚠️ **`GC-6` 기준선은 빌드 환경에 종속된다 — CI에서 뺐다**

**첫 CI 실행(run `32103823116`)이 `GC-6 baseline`에서 실패했다.** Windows 로컬에서 만든 기준선을
ubuntu CI에서 검사하니 **Next.js가 생성한 13개 파일이 전부 「변경」**으로 잡혔다.
`public/`에서 그대로 복사되는 `notion/index.html` **하나만 일치**했다 —
즉 `buildId` 마스킹 문제가 아니라 **산출물 자체가 환경에 따라 다르다**(청크 해시 등).

로컬 8단계는 전부 EXIT=0이었다. **로컬 통과가 CI 통과를 증명하지 않는다.**

⇒ `deploy.yml`에서 해당 단계를 **주석 처리**했다(사유와 복구 방법을 주석에 남겼다).
**로컬에서는 그대로 유효하다** — `npm run build && npm run check-baseline`.

**되살리려면 둘 중 하나:**

| 방안 | 내용 |
| --- | --- |
| (a) | 기준선을 **CI와 같은 환경**(ubuntu-latest · node 20)에서 만들어 커밋한다 |
| (b) | **CI 안에서 두 번 빌드해 서로 비교**한다 — 기준선 파일 자체를 없앤다 |

⇒ **무엇이 다른지 먼저 실측하라.** 아티팩트를 받아 13개 파일을 diff 하면 나온다.

⚠️ 이 실패는 **예측돼 있었다.** Task 7 구현자가 우려 3으로 「로컬은 Windows, CI는 ubuntu —
경로 구분자·대소문자가 다르다」를 적었고, 컨트롤러가 **「최종 리뷰로 이월」하고 넘어갔다.**
이월한 것이 첫 푸시에서 터졌다. **다음 세션은 이월 항목을 이월로 끝내지 마라.**

---

## 5. ⚠️ **경로가 결합을 대리하지 않는다** (Critical · 해소됨)

`check-baseline`(`GC-6`)을 「비블로그 = 경로가 `blog/`로 시작하지 않는 것」으로 정의했는데,
**실제 결합은 데이터 흐름을 탄다.** `pages/index.tsx:37-38`이
`getPostSummaries().filter(p => p.featured)`를 쓰므로 featured 4편의 제목·요약이
`out/index.html`에 **JSON과 렌더 HTML 양쪽에** 인라인된다.

⇒ featured 글을 고치면 GC-6가 실패해 **배포가 막힌다.** 유일한 복구인 `--update`가 습관이 되면
스크립트 자신이 경고한 「검사가 아무것도 막지 못한다」가 실현되고, 진짜 지켜야 할 `product-lead*`
7개까지 함께 덮인다.

**해소**: `490d3ba`가 두 자리를 마스킹했다. 제3자 검증 —
featured 변경 → **EXIT=0** · featuredPosts **밖** 변조 → **EXIT=1** ·
빈 `out/`에서 `--update` → **EXIT=2** · 빈 기준선으로 검사 → **EXIT=2**.

⚠️ **다른 페이지가 블로그 데이터를 쓰기 시작하면 재발한다.** `pages/` 아래에서
`getPostSummaries`·`readPosts`를 부르는 곳이 늘어나는지 지켜보라.

---

## 6. ⚠️ **「링크가 살아 있다」와 「약속한 내용을 준다」는 다르다**

`CR-1.7`(링크가 실재 슬러그를 가리킬 것 · **기계가 한다**)과
금지선 45(대상 편이 약속한 내용을 실제로 줄 것 · **사람이 한다**)를 한 묶음으로 자동화하려 했으나
둘은 다른 것을 요구한다. 45의 원 사례는 **링크가 살아 있었고** 대상 편 251줄에 잇는 서술이 0건이었다.

⇒ 45를 「사람」으로 되돌렸다. 그대로 뒀으면 **링크 검사기 통과를 45 준수로 착각**하게 만들 뻔했다.
`PUBLISHING-CHECKLIST.md` §6에 있다.

---

## 7. ⚠️ **재기 쉬운 지표가 재고 싶은 것을 대리하지 않는다** — 이번에 3번 밟았다

| 무엇 | 추정 | 실측 | 원인 |
| --- | ---: | ---: | --- |
| 분량 하한 미달 | 4편 | **6편** | `du -k`(디스크 블록)로 쟀다 |
| 남은 발행 규모 | 약 200편 | **233편** | 원본 **개수**로 쟀다 (크기 37~90 KB로 제각각) |
| 지도편 | 4편 | **5편** | **inbound 0**으로 식별했다 |

`ai-agent-knowledge-map`은 51편 카테고리의 지도편인데 inbound가 **1**이라 그물에 안 걸렸다.
그 1개가 사라지면 게이트가 오탐을 내고, 그때 사람은 **게이트를 만족시키려 억지 링크를 만든다.**
`6939868`로 `role: "map"`을 부여해 해소했다.

---

## 8. 다음 세션이 이어받을 것

### ▶ **다음 배치는 `java-backend` 나머지 10개 원본이다.**

| 원본 | 개수 | 바이트 | 예상 편수 |
| --- | ---: | ---: | ---: |
| **java-backend (잔여)** | **10** | **591,382** | **약 46** |
| langgraph | 12 | 510,215 | 40 |
| high-traffic | 11 | 508,162 | 39 |
| pm-harness | 8 | 401,936 | 31 |
| pm-po | 17 | 327,255 | 25 |
| bigdata | 8 | 292,763 | 23 |
| learning | 10 | 197,717 | 15 |
| recommendation | 7 | 83,778 | 7 |
| llm-search-recommend | 1 | 37,113 | 3 |
| **합계** | **84** | **2,950,321** | **약 229** |

⚠️ **코퍼스 평균 변환 비율(1.73)로 편수를 잡지 마라.** 이번 원본은 **1.40**이었다(표 밀도가 높으면
설명체로 바꿔도 거의 안 늘어난다. Q&A 절만 재면 1.26). 5편 계획으로 시작해 편1을 쓴 뒤
실측 1.47로 재환산하니 두 편이 하한을 깼고 4편으로 다시 잡았다.
⇒ **첫 편을 쓴 뒤 비율을 재측정하고 나머지 계획을 갱신하라.**

### 편당 소요 실측 — 7.0분

| | 값 |
| --- | --- |
| 총 소요 | 28분 4초 |
| 편수 | 4 |
| **편당** | **7.0분** |
| 대조: 08-07~08-08의 44.5편/일 | 8시간 기준 10.8분/편 |

포함 범위: 원본·요구사항·게이트 코드 읽기, 분할 설계서 작성(314줄), 4편 집필, 게이트 차단 1회와
재설계, 링크 편집 6곳, 검증 7종 2회, 문서 갱신.
⚠️ **그대로 외삽하지 마라** — 분할 설계서가 원본 1개분이었고 다음 배치는 원본 10개를 묶는다.

### ⚠️ 시리즈 지도편은 「완성해서 커밋」할 수 없다 — 규칙 둘이 서로를 막는다

`CR-4.3`은 시리즈 첫 글에 **전체 목차**를 요구하고, 링크 검사는 **대상이 실재할 것**을 요구한다.
편별 커밋을 유지하면 지도편이 항상 먼저 나오므로 목차 링크가 전부 죽는다.

**해소법(이번에 쓴 것)**: 지도편을 **링크 없이** 먼저 내고, 이후 각 편 커밋에 **그 편의 목차 링크
한 줄을 함께** 넣는다.

### 카테고리 고립 — 밖에서 오는 링크가 1개뿐이다

`backend-engineering` 4편으로 들어오는 **카테고리 밖** 링크는
`ai-agent/ai-agent-qna-operations.md:194` **하나**다. `search-engineering` 6편이 통째로 고립됐던
전례를 생각하면 얇다. **다음 배치에서 연결을 넓혀라** — 특히 `high-traffic`을 발행하면 캐시·부하
주제로 자연스러운 연결이 생긴다.

### 확정된 결정 (다시 묻지 마라)

| 항목 | 결정 |
| --- | --- |
| 원본 | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` **읽기 전용** |
| 푸시 | **사용자가 명시적으로 요청할 때만** |
| 커밋 메시지 | **한글** |
| 발행 제외 | **`pangyo-it` 6개만** (2026-08-18 사용자 판정). `bigdata` AWS 구성도·`learning` 커리어 로드맵은 익명화·탈개인화해 **발행한다** |
| 사명 표기 | 익명화 — 「OTT 서비스」·「IPTV 서비스」. 단 제품명은 유지 |
| 금칙어 목록 | **`scripts/check-forbidden.mjs`가 정본.** 문서에 복사하지 마라 |
| self-test 개수 | **문서에 적지 않는다.** 코드가 세고 명령이 출력한다 |
| 태그 | `content/blog/tags.ts` 통제 어휘 안에서만. 어휘 확장은 **별도 커밋** |
| 분량 | 목표 15~25 KB · 하한 13.3 KB(SOFT) · **상한 29.4 KB를 넘어도 된다**(금지선 13) |

---

## 9. 게이트 사용법

```bash
# content/blog 를 고치면 pre-commit 훅이 자동으로 돈다 (설정 불필요)
#   1/3 금칙어 self-test -> 2/3 콘텐츠 불변식 -> 3/3 금칙어 스캔
# 훅이 막으면 우회하지 말고 고쳐라. --no-verify 금지.

# 발행 전 수동 검증 — 종료 코드를 볼 명령은 단독 실행한다
npm run check-forbidden:verify   # 증명 먼저
npm run check-forbidden          # HARD 0 이어야 한다
npm run dup-scan -- <파일 1개>    # 반드시 한 편씩 (§2)
npx vitest run                   # [SOFT] 분량 경고가 stderr 로 나온다
npx tsc --noEmit                 # Vitest 는 타입을 검사하지 않는다

# 빌드 뒤
npm run build
npm run check-forbidden:built    # 소스가 깨끗해도 템플릿이 넣은 것은 여기서만 잡힌다
npm run check-baseline           # 비블로그 산출물 14개 불변
```

⚠️ **파이프 뒤의 `$?`는 마지막 명령의 것이다.** 이번 세션에서 컨트롤러가 실제로 밟아
「검출됐는데 EXIT=0」으로 오판할 뻔했다.

---

## 10. 소스 위치

| 무엇 | 경로 |
| --- | --- |
| 원본 (**읽기 전용**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 게이트 설계 | `docs/superpowers/specs/2026-08-18-publishing-gate-redesign.md` |
| 게이트 구현 계획 | `docs/superpowers/plans/2026-08-18-publishing-gate-implementation.md` |
| **규칙 전수 분류표** | `docs/superpowers/reports/2026-08-18-rule-triage.md` |
| **발행 체크리스트 (사람 판단만)** | `docs/superpowers/PUBLISHING-CHECKLIST.md` |
| 이번 배치 분할 설계 | `docs/superpowers/plans/2026-08-18-backend-engineering-category-split-design.md` |
| 금칙어 목록 (정본) | `scripts/check-forbidden.mjs` |
| 규칙 `GC-*` | `docs/superpowers/plans/2026-08-07-tech-blog-phase1.md` |
| 규칙 `CR-*` | `docs/superpowers/specs/2026-08-07-tech-blog-requirements.md` |
| 금지선 1~58 (사장 14건은 취소선) | 위 두 설계서 §11 + `git show` 이전 HANDOFF |

---

## 11. README/문서 갱신

### ✅ 이번 세션 — README 2곳 추가

| 자리 | 무엇 | 근거 |
| --- | --- | --- |
| npm 스크립트 표 | `check-baseline` · `check-baseline:update` 2행 | `package.json` 실측 |
| 발행 전 검사 절 | 「산출물 불변 검사」 행 + **「게이트는 자동으로 돕니다」** 절 신설 | 훅·CI가 이번에 생겼는데 README에 없었다 |

### ⚠️ 다음 세션이 확인할 것

- **`CLAUDE.md`의 `dup-scan` 사용법이 §2의 결함을 반영하지 않았다.** 지금은
  「`npm run dup-scan --category <slug>`」로만 적혀 있어 여러 파일을 넘기는 경우의 거짓 0을
  경고하지 않는다. 진실원은 `scripts/dup-scan.mjs`의 대조 집합 구성 로직이다.
- README의 카테고리 내역(「12개 등록 · 6개 발행」)은 배치마다 갱신이 필요하다.

---

## 12. 이 세션의 작업 방식 — 다음 세션이 참고할 것

서브에이전트 **13기**(구현 9 · 리뷰 4)를 띄웠고, 리뷰가 **Critical 1 · Important 6 · Minor 15+**를
잡았다. **그중 구현자 이탈은 0건**이었다 — 전부 계획서·설계서·브리프의 결함이었고 구현자들은
지시를 정확히 따랐다.

| 효과가 컸던 것 | 왜 |
| --- | --- |
| 브리프에 **위험을 지목하고 검증 방법까지** 적기 | 「잘 봐 달라」만 하면 diff를 읽고 승인한다 |
| 「이건 내가 확인 없이 쓴 것이다」를 명시 | Task 4가 근거를 대고 예시 문안을 기각했다(금지선 20) |
| **실행해서 수치를 내라**를 요구 | 「코드를 읽어 보니 괜찮다」는 검증이 아니다 |
| 유휴 알림 뒤 **명시 회수** | 알림은 「할 일이 없다」이지 「보고했다」가 아니다. 이번에도 전원이 회수를 필요로 했다 |
| **파일 소유권 표**를 pre-flight에 작성 | 병렬 실행의 충돌을 미리 갈랐다. 단 §4의 커밋 형태까지 지정해야 완전하다 |
