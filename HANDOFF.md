# 인수인계 (HANDOFF)

**갱신일**: 2026-08-08
**브랜치**: `main` · 최신 커밋 `f4b9c30`
**작업트리**: 이 문서 · `CHANGELOG.md` · 설계서만 미커밋 / stash 없음
**발행본**: **90편** (`ai-agent` 51 · `rag` 25 · `agentic-coding` 8 · 기타 6) · sitemap **151 URL**

---

## 1. 이번 세션에서 한 일

**`agentic-coding` B2를 발행했다(편9~11).** 그리고 **설계서 §5-3의 진단 3건이 실측과 어긋난 것을 발견해 정정했다.**

```
조사 2기 병렬 ──→ 브리프 ──→ 변환 1기 ──→ 검증 4기 병렬 ──→ 반영 ──→ 재검증 ──→ 커밋
     │                                          │
 ┌───┴───┐                    ┌────────┬────────┼────────┐
 원본 실측  발행본 대조        축A       축B      축C      축D
 (haiku)   (haiku)         정합성 부채  신규서술  누락·이관  빌드 실행
                            (opus)    (opus)   (opus)   (sonnet)
```

| 단계 | 산출 |
| --- | ---: |
| 변환 3편 | 61.4KB · 도식 9 · 표 45 |
| 검증 4기 | 보고 4건 · 지적 **15건** |
| 반영 | 15건 전량 · 재검증 7항목 전부 통과 |

### 결함이 이동했다 — 이번 세션 최대 소득

| 유형 | B1 | B2 |
| --- | ---: | ---: |
| 수치 불일치 | 5건 | **0건** (35건 전수 대조) |
| 불필요한 요약 | — | **0건** (표 40개에 요약 15개) |
| 강도 어긋남 방향 | 전부 **한 방향** | **양방향** (상승 2 · 하강 4) |
| 팽창률 | 1.76× | **1.42×** |

B1의 교훈을 브리프 금지선으로 넣자 그 유형이 사라졌다. **대신 결함이 브리프가 새로 연 작업 — 「다른 문서를 끌어와 대조한 대목」 — 으로 옮겨갔다.**

### ⚠️ 그리고 최상위 지적 2건의 출처가 컨트롤러의 브리프였다

**변환자는 브리프를 정확히 따랐고, 브리프가 틀린 인과를 지시했다.**

| 브리프 문장 | 무엇이 문제였나 |
| --- | --- |
| "하네스 쪽은 **컨텍스트를 무엇이 소비하는가**로 센다 — 이 문구를 그대로 쓰라" | **설계서 진단을 실측 없이 옮김.** 훅이 반증 |
| "상시 비용이 아니라 목록에서 빠진 것이다" | **컨트롤러의 추론**을 실측처럼 적음 |
| "용어표 35항목" | **scout 보고를 재계수 없이** 옮김. 실제 34 |

셋 다 같은 조작이다 — **여러 출처의 값을 한 문서로 합치면서 출처 표기를 잃었다.**

> scout의 **바이트 값은 재계수해서 오류를 잡았다.** 설계서라는 **대조 상대가 있었기 때문**이다.
> 대조 상대가 없는 값(열거 기준·용어표 행 수)은 전부 통과했다.
> **HANDOFF §6-1이 B1에서 적은 것과 정확히 같은 구조다.**

---

## 2. 다음 세션이 이어받을 것

### 최우선: B3 착수 (`05` → 편12~14)

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md)
— **§5-3·§6-1·§10에 정정 블록이 있다. 그것부터 읽어라.**

| 배치 | 대상 | 편 | 편수 | 상태 |
| ---: | --- | --- | ---: | --- |
| ~~B1~~ | ~~`01`(축약) + `02`~~ | ~~1~5~~ | ~~5~~ | ✅ 완료 (`388519e`) |
| ~~B2~~ | ~~`04`~~ | ~~9~11~~ | ~~3~~ | ✅ **완료** (`f4b9c30`) |
| **B3** | `05` | 12~14 | 3 | **다음** — §6-7 제품 결함 3건 · §6-8 Forge |
| B4 | `03` + `team-productivity` | 6~8, 26 | 4 | **`02`·`04`·`05` 뒤라야 전방 참조가 닫힌다** |
| B5 | `07` | 15~18 | 4 | §5-4 CLAUDE.md 60/200 · §6-6 개인 이력 |
| B6 | `08`(개발축) | 19~21 | 3 | 편21 자기참조 **미채택 확정**(아래) |
| B7 | `09` | 22~25 | 4 | §6-5 10번 표 캡션 승격 |
| B8 | Q&A 4편 | Q1~Q4 | 4 | 전 배치 완료 후 |
| B9 | 지식 지도 | — | 1 | **맨 마지막** |

### 확정된 결정 (다시 묻지 마라)

| 사안 | 결정 | 근거 |
| --- | --- | --- |
| **편21 자기참조** — `08 §9`에서 이 리포의 CI·배포 관행을 근거로 쓸 것인가 | **쓰지 않는다** (사용자 승인 2026-08-08) | `deploy.yml`이 preview 없이 `main`=프로덕션인데 §9의 "자동 롤백 임계"와 대비되면 약점 공개가 된다 |
| **분량 배율** | **1.4~1.8× 수용, 상한 29.4KB** | 발행본 51편 실측 범위가 13.3~29.4KB. B1 1.76 · B2 1.42 |
| **시리즈 페이지** | 없다 | `pages/blog/` 라우트는 index·category·slug·tags뿐. `series`는 frontmatter 메타데이터일 뿐 페이지를 만들지 않는다. **B1의 sitemap +6은 시리즈가 아니라 `agentic-coding` 카테고리 페이지 신설분이다** |

### B3 브리프에 반드시 넣을 것

**B1이 준 4건은 효과가 확인됐다(수치 불일치 0건). 유지하고 아래 4건을 더한다.**

| # | 추가할 지시 | 근거 |
| --- | --- | --- |
| **17** | **브리프의 모든 수치·인용에 `[실측]`·`[설계서]`·`[추론]` 표기를 단다** | 변환자에게는 셋이 구분되지 않는다. **검증자에게 요구하면서 컨트롤러가 안 지켰다** |
| **18** | **설계서의 진단을 브리프로 옮기기 전에 실측한다** | B1은 §6-1·§10, B2는 §5-3. **2배치 연속 설계서 오류** |
| **19** | **`tags.ts` 상단 주석의 부여 규칙을 브리프에 옮긴다** — 「같은 패싯에서 최대 2개」 | `isKnownTag()`는 어휘 소속만 검사해 **빌드가 못 잡는다** |
| **20** | **재검증 브리프에 「사라졌는가」 항목을 넣는다** | 추가는 눈에 띄고 **제거는 안 띈다.** 틀린 문장이 남은 채 새 문장만 덧붙는 실패 모드 |

**B1이 준 기존 4건**(요약문 수치 대조 / 원문 강도 상승 금지 / 수정 후 스캔 재실행 / 착수 전 표 밀도 측정)은 그대로 가져간다.

### 진행 방식 — 검증된 절차

```
1단계  분할 설계 → 승인                          ← 완료
2단계  배치로 나눠 변환, 배치마다 커밋·보고        ← B1·B2 완료, B3부터
3단계  구현 미참여 에이전트가 독립 검증
4단계  지적 반영 → 재검증(다른 에이전트) → 커밋 → (요청 시) 푸시
```

**검증은 축을 4개로 나눠라.** B1의 2축에서 늘렸고 값을 했다. 나누는 기준은 주제가 아니라 **결함이 드러나는 방식**이다.

| 축 | 무엇을 보는가 | 왜 별도 축이어야 하나 | 티어 |
| --- | --- | --- | --- |
| A | 쓰여 있는데 **틀린** 것 | grep으로 안 잡힌다. 세면 0건이라 자동 검증을 통과한다 | opus |
| B | 쓰여 있는데 **원문에 없는** 것 | 원문과 나란히 놓아야만 보인다 | opus |
| C | **없는** 것 | 읽어서는 안 드러난다. **원본 기준 역방향**으로 훑어야 한다 | opus |
| D | **실행하면 나오는** 것 | 종료코드는 객관적이다 | sonnet |

**A·B·C를 한 명에게 맡기면 서로를 가린다.** 누락을 찾는 일과 오류를 찾는 일은 **읽는 방향이 반대**다.

---

## 3. ⚠️ 설계서 정정 3건 — B3 이후에 영향

### 3-1. §5-3(1) 발행본의 열거 기준은 「소비」가 아니다

| | 설계서 초안 | **B2 실측** |
| --- | --- | --- |
| `harness-five-primitives` 다섯의 기준 | 「컨텍스트를 무엇이 **소비**하는가」 | **「컨텍스트 관리를 상위 목적으로 공유하는 수단」** |

근거는 그 글 L48~57 — 도식 최상위 노드가 `Context Management (WHY)`이고 본문이 *"최상위 노드가 다섯 중 하나가 아니라 **다섯의 이유**"* 라고 명시한다.

**반증**: 그 글 컨텍스트 분해표(L144~154) 9행에 **커맨드와 훅은 행 자체가 없는데 다섯에 들어 있다.** 소비 기준이면 둘이 들어갈 수 없다.

### 3-2. §5-3(1) 요건 4의 인과는 성립하지 않는다

*"상시 비용이 아니라 목록에서 빠진 것"* — **그 글은 MCP를 다섯에서 뺀 이유를 밝히지 않는다.** 확인되는 것은 L154의 값이 `—`라는 사실까지다.

**처리 방식이 중요하다** — 틀린 인과를 조용히 지우면 독자는 여전히 표를 세다가 같은 의문에 걸린다. **반례를 본문에 적고 "원 자료가 답하지 않는다"고 밝혔다.**

### 3-3. §5-3(2) 발행본 시점은 「없음」이 아니다

`harness-five-primitives` **L19에 시점 고지가 실재한다** — *"이 글은 **2026년 3월 자료를 정리한 것**이다."* 설계서 지시대로 *"시점을 명시하지 않았다"* 라고 쓰면 **거짓 서술**이 된다.

원본 쪽 판정(`04` L484~494에 시점 표기 없음)은 **재확인 결과 맞다.**

> ### 세 건 다 놓친 경로가 같다
>
> 설계서가 §5-3(2)에서 스스로 적었다 — *"발행본 쪽 근거 시점을 **아무도 조사하지 않았다**"*.
> **그 미조사 상태에서 「없음」으로 확정한 것**이 오류의 경로다. ①②도 같다 — 발행본을 **열지 않고** 표만 보고 기준을 규정했다.
>
> **문서가 자기 공백을 표시해 둔 곳이 가장 먼저 찔러 볼 곳이다.**

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 77점. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **2차 전체를 마친 뒤 재측정** |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | **어휘 81종 중 30종이 0편** | `redis` `kafka` `docker` `aws` `spring` `java` `nosql` `load-testing` `ci-cd` `deployment` `distributed-systems` `event-driven` `cdc` `cqrs` `microservices` `scalability` `vector-database` `machine-learning` `product-discovery` `prd` `product-metrics` `ab-testing` `user-research` `commerce` `payments` `global-service` `startup` `team-building` `career` `recommender-systems`. **어휘에 있는 것과 살아 있는 것은 다르다** |
| 7 | **훅 차단 채널이 두 발행본에서 다르다** | 편11은 **stdout JSON**, `settings-permissions-deny`는 **표준 에러**. 두 원본 사이의 부채다. **편11에 고지를 달아 막아 뒀으나 근본 해소는 아니다** — 어느 쪽이 현재 사양인지 확인되면 정리 |
| 8 | **원본 `04 §8-2` 「3대 구성 요소」인데 표는 4행** | 원본에서 이월된 불일치. 편11에 *"제목은 셋인데 표는 네 행이다"* 로 해명만 달았다 |

### `ai-transformation`으로 이월된 위험 3건

`06`을 이관하면 조사자 D가 찾은 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표.** 발행하지 않는 선택지 우선 검토 |
| 2 | **`06`은 발행본과 서사 방향이 반대다.** 발행본은 "위임을 늘려라", `06`은 "어디서 사람을 세울 것인가". **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

**다섯 번째 이월이다.** README는 2026-08-07(`7f28866`) 이후 손대지 않았고 그 사이 **17커밋**이 쌓였다.

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 **90편·4카테고리**가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. 이 사이트가 쓰는 환경변수가 아니라 RAG·에이전트를 설명하는 **블로그 글 본문의 예시 코드**다. `.env.example`을 만들거나 README에 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이라 사람이 직접 부르지 않는다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)와 설계서 §13, 그리고 `CHANGELOG.md`의 B1 항목에 있다. **아래는 B2에서 새로 나온 것이다.**

### 6-1. 브리프가 결함원이다 — 그리고 검증이 그것을 잡는다

**변환자는 브리프를 정확히 따랐다.** 지적 15건 중 최상위 2건이 브리프의 문장에서 나왔다. CLAUDE.md에 적힌 *"리뷰가 걸러낸 실질 결함 17건 중 15건이 계획 자체의 결함이었고, 구현자들은 브리프를 정확히 따랐다"* 가 그대로 재현됐다.

> **검증자에게 「요건 밖도 보라」고 열어 두지 않으면 이런 건 안 잡힌다.**
> 요건 체크리스트는 "지시한 것을 했는가"만 검사하므로, **지시 자체가 틀리면 통과가 곧 결함**이다.
> B2에서 지시한 14개 요건은 **전부 충족**이었고, 지적 15건은 전부 요건 **밖**에서 나왔다.

### 6-2. 글이 어색하게 좁아지는 곳에 잘못된 전제가 있다

틀린 전제("다섯은 소비 기준으로 센 목록")를 받은 변환자가 *"메모리와 에이전트는 컨텍스트를 먹지만 확장 지점은 아니고"* 라며 **다섯 중 둘만 들고 훅은 언급하지 않았다.** 훅을 넣는 순간 자기 규정이 깨지기 때문이다.

**실수가 아니라 틀린 전제 위에서 문장을 성립시키려면 그럴 수밖에 없었던 것이다.**

### 6-3. 오차의 부호가 일정하면 계측 방법의 결함이다

scout가 절별 바이트를 전부 1.8~2.3% 미달로 보고했다. **차이가 절의 줄 수와 정확히 일치**했다(§0=43, §11=26, §13=24). 개행 1바이트씩 빠진 것이다.

| 신호 | 뜻 |
| --- | --- |
| 오차 **부호가 섞임** | 무작위 실수 |
| 오차 **부호가 일정** | **계측 방법이 체계적으로 뭔가를 빠뜨렸다** |

그리고 보고서가 **스스로 모순됐다** — §0을 한 표에서 3,489, 다른 표에서 3,532로 적었고, 표 대표 위치가 절 범위를 벗어났다(§0 범위 L11~53인데 위치가 L86).

> **합계·항목·위치를 모두 내게 하면 셋이 어긋날 때 오류가 드러난다.**
> 결론만 요구했다면 틀린 값이 그대로 브리프에 들어갔다.

### 6-4. 제거는 확인하지 않으면 드러나지 않는다

재검증 브리프에 *"`컨텍스트를 무엇이 소비하는가`라는 문구가 **사라졌는가**"* 를 명시적으로 넣었다. **추가는 눈에 띄지만 제거는 안 띈다** — 고쳤다고 주장하면서 새 문장만 덧붙이고 틀린 문장이 남는 것이 가장 흔한 실패 모드다.

### 6-5. 빌드가 통과하는 규칙 위반이 있다

`tags.ts` 상단 주석이 **「같은 패싯에서 최대 2개」** 를 규정하는데 `isKnownTag()`는 **어휘 소속만 검사한다.** 편9·편11이 패싯 C를 3개 달았는데 빌드가 통과했다. **B1 5편은 전부 지키고 있었으므로 규칙이 깨진 것은 브리프가 그것을 옮기지 않아서다.**

### 6-6. 브리프의 비용 추정은 지시가 아니라 기본값이다

축C에게 "전수는 비용이 크니 표본 9개로 하라"고 했는데 **awk로 표 경계를 자동 추출할 수 있어 45개 전수**를 돌렸다. 비용 가정이 깨지면 더 싸게 더 많이 하는 것이 맞다.

### 6-7. 서브에이전트 최종 응답 유실이 이번에도 3회

`idle`을 보고했으나 최종 응답 텍스트가 도달하지 않은 일이 **3회**(직전 세션 2회, 그 전 3회) 있었다. **파일 산출물을 지시한 건은 전부 회수됐다.**

> **브리프에 산출물 경로를 파일로 못박아라.** 유실 시에는 `SendMessage`로 회수할 수 있다 — idle 상태 에이전트는 컨텍스트가 살아 있다.

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 151개 URL, 정적 페이지 160

# 금칙어 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]"

# 민감정보 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 오탐 많음. 문맥 판정 필수
Select-String -Path "content\blog\**\*.md" `
  -Pattern "인용할 때|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안"
```

> **알려진 오탐 4종**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **90편 중 대부분이 갖고 있으며 정규식은 영문만 잡는다**
> - `채용` — `agentic-coding` 편14(`05 §10-2`)에 「**채용**과 JD 작성」이 온다. 구직이 아니라 **에이전트 정의를 채용에 비유**한 것 (**B3에서 등장**)
> - 화행 정규식 — `이렇게 구현하는 편이 안전하다`(구현 권고), `LLM이 답한다`(주어가 모델)는 정상

### 태그 패싯 분포 검사 (빌드가 못 잡는다)

```powershell
# tags.ts 상단 주석: "같은 패싯에서 최대 2개"
# A 기술스택 / B 엔지니어링 / C AI·LLM / D 프로덕트·조직 / E 검색·추천 / F 기타
Select-String -Path "content\blog\agentic-coding\*.md" -Pattern "^tags:"
```

**패싯 C가 3개 이상이면 위반**이다. `claude-code`·`ai-governance`·`ai-automation`·`context-engineering`·`prompt-engineering`이 전부 C라 AI 계열 글에서 쉽게 넘는다.

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/aiagent"
LC_ALL=C awk '
  BEGIN { sec="머리말" }
  /^```/ { if (!fence && $0 ~ /^```mermaid/) mm[sec]++; fence=!fence; b[sec]+=length($0)+1; next }
  !fence && /^## / { sec=$0; order[++n]=sec }
  !fence && /^\|[ ]*[-:][- :|]*\|[ ]*$/ { tb[sec]++ }
  { b[sec]+=length($0)+1 }
  END { for(i=1;i<=n;i++){s=order[i]; printf "%s | %d B | 표 %d | 도식 %d\n",s,b[s],tb[s],mm[s]} }' <파일>
```

**⚠️ `length($0)+1`의 `+1`이 개행이다. 빼먹으면 절마다 줄 수만큼 미달한다** — B2에서 실제로 발생했다.
**펜스를 추적하지 않으면 H2가 과대계수된다** — 코드블록 안 가짜 H2가 `03`에만 10개다.

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — **90편** |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, **4개 발행** |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종(실사용 51). **목록 밖이면 빌드 실패. 상단 주석에 패싯 규칙이 있다** |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15 |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§5-3·§6-1·§10에 정정 블록** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |
| **B1 보고서 5건** | [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환·실행검증·리뷰2·반영 |

**B1 보고서 5건이 저장소에 남아 있다.** 뒤 배치는 이것을 템플릿으로 쓸 수 있다 — 다만 **변환 보고서 §4-2 분류표는 상쇄 오류가 있었으므로 그대로 복제하지 마라.**

> **B2 보고서 7건은 스크래치패드에만 있고 커밋하지 않았다.** 세션이 끝나면 사라진다.
> 핵심은 이 문서 §3·§6과 `CHANGELOG.md`에 옮겨 적었다.
