# HANDOFF

> 마지막 갱신 2026-09-02 · `pm` 배치 **시리즈 A 9/9 완결 · 시리즈 B 0/8**

## 지금 상태

발행본 **176편**이다. `ai-product-planning` 카테고리 아홉 편이 모두 나갔고 **시리즈 A 가 끝났다.**

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 2,225줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 ✅ · B `product-management` 8 |
| 발행 완료 | **A 편1~9 (176편)** |
| 다음 단계 | **시리즈 B 편1 지도편** — 새 카테고리 `product-management` 의 첫 편이다 |
| 푸시 · 배포 | ⏳ **미완** — 편8·편9 커밋이 로컬에만 있다. 사용자 승인 뒤 `main` 에 올린다 |

**설계서 §1 의 절차표가 정본이다.** 다만 절차표의 상태 칸은 갱신되지 않았으므로
**진행률은 `§10-N` 의 실측 절 번호로 판단하라** — §10-9 까지 있으면 A 시리즈가 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | A 편9 `planning-harness-qna` 집필·발행 (15,835 B) | `content/blog/ai-product-planning/planning-harness-qna.md` |
| 2 | 편8 말미에 편9 링크 연결 | `skill-packaging.md` |
| 3 | 지도편 구성 표 **편9 행** 링크 부착 + 「플러그인 묶음」 뜻 정정 | `planning-harness-map.md` |
| 4 | 리뷰 2축 반영 (정확성 7건 · 검사 유효성 1건) | 편9 본문 10곳 |
| 5 | 편9 실측 + 시리즈 A 마감 | 설계서 **§10-9** (6개 하위 절) · **§11-9** 승격 |
| 6 | README 세 자리 갱신 (175 → 176 · `ai-product-planning` 8 → 9) | `README.md` |
| 7 | CHANGELOG 편9 절 + 편6 절을 아카이브로 이동 | `CHANGELOG.md` · `docs/changelog/2026-09.md` (2절) |
| 8 | 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 🔴 B 편1은 **재환산 지점**이다 — 편2를 먼저 쓰지 마라

설계서 §3-6 이 남긴 마지막 재환산 지점이다. **고정 비용이 시리즈 경계에서 리셋되는지 아무도
모른다.** A 편1에서 고정 비용은 리셋되는 정도가 아니라 **더 높게 시작했다**(§10-1-b).

⇒ **B 편1 초고의 `compose` 출력을 보기 전에 B 편2 를 쓰지 마라.** 값을 보고 B 편2~8 예산을
다시 긋는다.

### 2. 🔴 새 카테고리의 첫 편은 **반드시 테스트에 걸린다**

§10-1-c 가 A 편1에서 겪은 것이고, B 편1에서 그대로 재현된다. 카테고리에 편이 하나뿐이면
고립 검사가 걸린다.

⇒ **커밋 11 에는 카테고리 간 링크 한 쌍이 들어간다**(§9-1). A 편1 때와 같은 처리다.

### 3. 🔴 임계값을 통과했다는 것이 겹치지 않는다는 뜻은 아니다 — 이번 편의 발견

`source-overlap` ①이 20자에서 **0건**이었는데 `--min 16` 으로 낮추자 **6건**이 나왔고,
그중 4건이 같은 표 행 계열이었다. ①②③④ 항목 구조와 순서까지 원본과 같고 사이사이가
축자였다 — **「겹치지 않는다」가 아니라 「20자를 넘지 않게 잘게 끊겼다」였다.**

⇒ **`--min 16` 으로도 돌리고, 임계 바로 아래에 무더기가 있으면 20자 0건을 통과로 세지 마라.**
고친 뒤에는 셀 경계 조각 1건만 남았고, 그 상태가 편5·편6이 「셀 경계만 남았다」로 판정한 것과
같다.

### 4. 🔴 도식은 **화면에서 확인한다** — `grep` 은 개수만 말한다

subgraph 에 노드를 연결하는 문법을 처음 썼는데, 산출물에 실린 것까지는 `grep` 으로 확인됐으나
브라우저로 열자 **원형 노드 `(( ))` 에서 한글 라벨이 잘렸다.** 기존 편 관례인 `["…"]` 로 바꿔
해결했다.

| 확인 방법 | 무엇을 말해 주나 |
| --- | --- |
| `grep -o mermaid` | 도식이 **몇 개** 실렸는지 |
| 브라우저 | 라벨이 **읽히는지** |

⚠️ **축소 렌더에서 라벨이 잘리는 것 자체는 편7 발행본도 같다.** 확대 보기가 그것을 전제로
붙어 있고 확대하면 온전히 보인다. **사이트 전반의 별건이지 편의 결함이 아니다.**

### 5. 🔴 성분 배율은 **원본의 성분 구성이 비슷할 때만** 이어진다

편9의 표 배율이 **1.113** 으로 나와 앞 다섯 표본의 1.478 에서 크게 벗어났다. 원인은 원본
§10 의 표 비율이 **83.1%** 였다는 것이다.

⇒ **표 배율 표본에 편9를 넣지 마라.** 넣으면 평균이 1.478 → 1.417 로 내려가 다음 배치의
산문 예산이 근거 없이 늘어난다. 1.478 은 「산문을 표로 바꾸면 늘어난다」를 재던 값이다.

### 6. 초고를 **밴드 상단 가까이 맞추지 마라**

편8까지는 복제 제거분 200 B 안팎만 보면 됐다. 편9에서는 **리뷰 반영이 +734 B** 로 그 네 배가
들어왔다 — 정확성 축이 잡은 누락 요소를 되살리는 작업이었다.

| 단계 | B | 증감 |
| --- | ---: | ---: |
| 초고 | 14,857 | — |
| 복제 제거 | 15,014 | +157 |
| 링크 부착 | 15,051 | +37 |
| **리뷰 반영** | 15,785 | **+734** |
| 도식 표기 교체 | 15,835 | +50 |

### 7. Q&A편의 항목당 값은 **513 B** 다

§3-4 가 앞 배치에서 가져온 471 B 보다 8.9% 높다. **다음 배치의 Q&A편은 513 으로 잡아라.**

---

## 시리즈 B 사양 요약

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (**새 카테고리**) |
| 원본 | `pm-po/01` · `02` · `04` — 배정 **58,636 B** |
| 편수 | 8편 (편1 지도 + 편2~8) |
| 예산 | k = **2.05** · 밴드 1.91 ~ 2.19 (§3-2) |
| frontmatter | `series: "product-management-domains"` · `seriesOrder` 1~8 (§7-4) |
| 지도편 | §0 용어표 **3개를 합쳐 중복을 제거**한 뒤 승계 (§11-4) |
| 🔴 낱말 | 「기획」은 A 와 뜻이 다르다. **B 편1이 「다른 뜻으로 쓴다」고 명시한다**(§4-2) |
| 🔴 선행 독서 | **A 편1 도입을 B 편1 집필자가 반드시 먼저 읽는다**(§4-2) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  시리즈 B 가 없어 지금은 걸 수 없다(§6-2). **B 시리즈 발행 때 되돌아와 건다** — 두 편 모두
  이미 발행됐으므로 **파일을 다시 열어 고쳐야 한다.**
- **코드 계수는 두 표본이 성격으로 갈린다.** 편6 **1.144**(주석만 한글로) 대 편8 **1.065**
  (원형 보존 넷). **평균 1.105 를 그대로 쓰면 원형 보존이 많은 편에서 예산이 남는다.**
  다음 배치는 **원형 보존 비중으로 나눠** 잡아라.
- 🔴 **밴드와 하한은 여전히 기계 검사 밖에 있다.** `npm test` 는 하한 미달을 `stderr` 경고로만
  내고 상한은 검사기가 없다. **「테스트 초록」을 분량 근거로 쓰지 마라** — `compose` 출력과
  `[SOFT]` 목록을 눈으로 읽는다. 현재 하한 미달 **5편**은 전부 이번 배치 밖이다.
- **`user-research` · `career` 태그가 싱글톤으로 남았다.** 편9가 `career` 를 썼지만 그래도
  둘뿐이다. **글에 없는 내용으로 태그를 맞추지 마라**(§11-8).
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 이번 세션에 편6 절을 `docs/changelog/2026-09.md` 로
  옮겨 아카이브가 2절이 됐다. 넘으면 여기로 옮기고 상단 인덱스 표를 갱신한다.
- ✅ **`check-counts` 를 발행 전 검사에 넣은 뒤로 배포 실패가 없다.** 편을 발행했으면 README
  **세 자리**와 CHANGELOG **최신 절**, 네 자리를 같은 커밋에 넣는다.
- ✅ **지도편의 「플러그인 묶음」 정의는 이번 커밋에서 고쳤다.** 근거 없던 「다섯 스테이지를
  스킬로 구현해 한데 묶은 것」을 편8 실물(스킬 둘)에 맞춰 정정했다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **임계값 아래의 무더기** | `source-overlap` 20자 0건이 `--min 16` 에서 6건이 됐다. 항목 구조가 원본과 같은 편에서 축자가 잘게 끊겨 숨는다 | **`--min 16` 으로도 돌린다.** 0건을 통과로 세기 전에 임계 아래를 본다 |
| 🔴 **`grep` 은 도식이 읽히는지 모른다** | 산출물에 `mermaid` 1회가 확인됐지만 브라우저에서는 라벨이 잘려 있었다 | **개수는 `grep`, 가독은 브라우저.** 새 도식 문법을 쓰면 반드시 화면을 연다 |
| 🔴 **한 축을 고치면 다른 축이 움직인다** | 지도편 용어 뜻을 편8 문장에 맞추자 `dup-scan` 이 4 → 5건이 됐다. 어순을 바꿔 되돌렸다 | **반영 뒤에 전 검사를 다시 돌린다.** 편8에 이어 두 번째다 |
| **`cd` 뒤의 상대 경로가 깨진다** | `cd content/blog/…` 가 「No such file or directory」로 죽었다. 셸 도구의 작업 디렉터리가 리셋된 뒤였다 | 셸 도구에는 **절대 경로**를 쓰거나 `Grep` 도구를 쓴다 |
| **`compose` 와 `wc -c` 의 1 B 오프셋** | 다섯 편에서 재현됐다 | 상수 오프셋이다. **1 B 차이를 「불일치」로 오독하지 마라** |
| **서브에이전트에는 `Write` 가 없다** | `reviewer` 두 명 모두 히어독으로 보고서를 썼다. 브리프에 명시하지 않으면 보고 없이 유휴가 된다 | 브리프에 **히어독 예시를 그대로 넣는다** |
| **에이전트 가드가 「작업 한계」를 요구한다** | 블록 없이 호출하면 거부된다 | 브리프 말미에 **작업 한계 5줄**을 붙인다 |
