# 인수인계 (HANDOFF)

**갱신일**: 2026-08-09
**브랜치**: `main` · 최신 커밋 `cf948a1`
**작업트리**: 이 문서 · `CHANGELOG.md` · 설계서만 미커밋 / stash 없음
**발행본**: **93편** (`ai-agent` 51 · `rag` 25 · `agentic-coding` 11 · 기타 6) · sitemap **154 URL**

---

## 1. 이번 세션에서 한 일

**`agentic-coding` B3를 발행했다(편12~14).** 그리고 **설계서 §6-5 8번의 진단이 실측과 어긋난 것을 발견해 정정했다.**

```
조사 2기 병렬 ──→ 브리프 ──→ 변환 1기 ──→ 검증 4기 병렬 ──→ 반영 ──→ 재검증 2기 ──→ 커밋
     │                                          │                          │
 ┌───┴───┐                    ┌────────┬────────┼────────┐          ┌──────┴──────┐
 원본 실측  발행본 대조        축A       축B      축C      축D        반영 정확성   실행
 (haiku)   (haiku)          틀린 것  없는 주장  누락    실행        (opus)     (sonnet)
                            (opus)    (opus)   (opus)  (sonnet)
```

| 단계 | 산출 |
| --- | ---: |
| 변환 3편 | 77,353 B · 도식 17 · 표 59 |
| 검증 4기 | 지적 **27건** (중복 제거 후 22) |
| 반영 | 22건 전량 |
| 재검증 2기 | 「사라졌는가」 14/14 통과 · **추가 5건** |
| 최종 | 27건 전량 반영 · 79,824 B |

### 결함이 또 이동했다 — 이번엔 「처음 하는 작업」으로

| 유형 | B1 | B2 | **B3** |
| --- | ---: | ---: | ---: |
| 수치 불일치 | 5 | 0 | **0** (284행 대조) |
| 원문 강도 어긋남 | 한 방향 | 양방향 6 | **0** (60건 대조) |
| 설계서 진단 오류 | 2 | 1 | **1** |
| **신규 도식의 거짓 관계** | — | — | **1** ← 처음 |
| **분산 지시의 부분 미이행** | — | — | **2** ← 처음 |
| 팽창률 | 1.76× | 1.42× | **1.31×** |

**B3에서 처음 한 작업이 둘이다** — 원본에 없는 도식을 새로 그린 것, 그리고 원본 자산 하나를 3편에 나눠 넣은 것. **결함이 정확히 그 둘에서 났다.**

### ⚠️ 그리고 이번에도 최상위급 결함 하나의 출처가 컨트롤러였다

| 무엇 | 무엇이 문제였나 |
| --- | --- |
| 브리프가 *"편14가 29.4KB를 넘으면 보고하라"* 로만 적음 | §4-0 원문은 **"실측 분포이지 합격선이 아니다"**. 그 문장을 옮기지 않았다 |
| 결과 | 변환자가 초고 30,528 B를 세 차례 깎아 29,373 B로 맞추며 **축약 용어표 행을 삭제** |
| 반영 지시 F-9 문면 | 축 B가 지적문에 적은 「3행도 실행 시점 절차」를 **취합하며 떨어뜨림** → 재검증 N-1로 재발 |

> **"넘으면 보고하라"만 받은 구현자는 보고를 피하려고 내용을 깎는다.**
> 제약을 줄 때는 **경성인지 연성인지 함께** 말해야 한다.
> 그리고 **검증 → 취합 → 지시 사이에도 전달 손실이 있고, 그 손실은 지적 자체보다 조용하다.**

---

## 2. 다음 세션이 이어받을 것

### 최우선: B4 착수 (`03` + `team-productivity` → 편6~8, 26)

**읽을 것**: [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md)
— **§4-4·§5-3·§6-1·§6-5·§10·§11에 정정 블록이 있다. 그것부터 읽어라.**

| 배치 | 대상 | 편 | 편수 | 상태 |
| ---: | --- | --- | ---: | --- |
| ~~B1~~ | ~~`01`(축약) + `02`~~ | ~~1~5~~ | ~~5~~ | ✅ 완료 (`388519e`) |
| ~~B2~~ | ~~`04`~~ | ~~9~11~~ | ~~3~~ | ✅ 완료 (`f4b9c30`) |
| ~~B3~~ | ~~`05`~~ | ~~12~14~~ | ~~3~~ | ✅ **완료** (`cf948a1`) |
| **B4** | `03` + `team-productivity` | 6~8, 26 | 4 | **다음** — 전방 참조가 이제 닫힌다 |
| B5 | `07` | 15~18 | 4 | §5-4 CLAUDE.md 60/200 · §6-6 개인 이력 |
| B6 | `08`(개발축) | 19~21 | 3 | 편21 자기참조 **미채택 확정** |
| B7 | `09` | 22~25 | 4 | §6-5 10번 표 캡션 승격 |
| B8 | Q&A 4편 | Q1~Q4 | 4 | **`05 §10-1` 10문항이 여기로 이월됐다** |
| B9 | 지식 지도 | — | 1 | **맨 마지막** |

### B4가 지금 가능해진 이유

설계서 §9가 *"`02`·`04`·`05` 뒤라야 전방 참조가 링크로 닫힌다"* 라고 했고 **셋 다 끝났다.** `03`의 전방 참조 4건이 전부 발행된 slug를 가리킨다.

| 원본 참조 | 방향 | 발행 링크 |
| --- | --- | --- |
| `03` L489 (훅 3종) | 편7 → 편11 | `/blog/agentic-coding/commands-skills-hooks-plugins/` |
| `03` L487 (`allowManagedPermissionRulesOnly`) | 편7 → 편4 | `/blog/agentic-coding/settings-permissions-deny/` |
| `03` L922 (`Agent Teams`) | 편8 → **편14** | `/blog/agentic-coding/agent-team-operations/` ← **B3에서 생김** |
| `03` L1056 (`Plan Mode`) | 편8 → 편2 | `/blog/agentic-coding/claude-code-autonomy-tiers/` |

### B4 고유 사항 — 다른 배치에 없는 것

| # | 사항 | 근거 |
| --- | --- | --- |
| 1 | **편26은 `source:`를 비운다** | `team-productivity`는 `> 원본:` 필드가 없는 **웹 조사 종합본**이다. 강의 출처를 달면 사실이 틀린다. 외부 URL 9개는 본문 각주로 재배치 (§6-3) |
| 2 | **`team-productivity.md`는 CRLF다** (나머지는 LF) | awk가 `\r`를 떨궈 145 B 차이가 난다. **변환 전 정규화 1스텝 필요** (§10-1) |
| 3 | **`03`은 가짜 H2가 10개로 최다** | 펜스를 추적하지 않으면 절 경계가 통째로 어긋난다 (§10-1) |
| 4 | **편26은 표 1개·도식 0개** | `11,322 B/표`로 카테고리 안에서 혼자 이질적이다. 불릿 → 표·도식 전환이 승인된 팽창(배율 1.25) (§4-2) |
| 5 | **§5-4 1번 「200줄」** | `03 §2-4` L108 원문이 *"하드 리밋이 아니라 **준수율 임계**"* 라고 직접 쓴다. 편15의 「60줄 설계 권고치」와 **다른 것을 재고 있으며 그 구분은 원본이 한 주장**이다. 서로 링크한다 |
| 6 | **§6-5 조건 이관 4건** | 3번(`03 §10-2` 주의 열 → 편6·8) · 4번(`03 §11` L1103 → 편6·8) · 5번(`TechCorp`·`NextLevel 커머스`는 **가상 실습 소재** → 편6·7) · 11번(기본 커맨드 8종은 버전에 강하게 묶임 → 편7 `2026-04 기준` 명기) |
| 7 | **편8 도입부는 편7 링크로 시작해야 한다** | 조사자 B가 *"§7 도입부가 L699에서 §6을 명시 참조한다. 분리하면 §7이 홀로 서지 못한다"* 며 반대했다. KB 기준으로 3편 병합하면서 갈렸으므로 **인라인 링크와 한 문장 요약**이 없으면 편8이 존재하지 않는 앞 편을 참조한다 (§9-1) |

### 확정된 결정 (다시 묻지 마라)

| 사안 | 결정 | 근거 |
| --- | --- | --- |
| **편21 자기참조** — `08 §9`에서 이 리포의 CI·배포 관행을 근거로 쓸 것인가 | **쓰지 않는다** (사용자 승인 2026-08-08) | `deploy.yml`이 preview 없이 `main`=프로덕션인데 §9의 "자동 롤백 임계"와 대비되면 약점 공개가 된다 |
| **분량 상한** | **29.4KB는 합격선이 아니다. 넘어도 된다** | §4-0 원문. B3 편14가 **30,897 B**로 발행본 최대치를 갱신했다 |
| **시리즈 페이지** | 없다 | B3에서 `subagent-design` 시리즈를 신설했는데 **sitemap이 정확히 +3만 늘었다.** `series`는 frontmatter 메타데이터일 뿐 라우트를 만들지 않는다 |
| **`05 §10-1`·`§10-3`** | 각각 **B8 이월** · **드롭** | 설계서 §4-4에 기록했다 |

### B4 브리프에 반드시 넣을 것

**B1이 준 4건 + B2가 준 4건은 효과가 확인됐다. 유지하고 아래 4건을 더한다.**

| # | 추가할 지시 | 근거 |
| --- | --- | --- |
| **21** | **상한을 줄 때 「합격선이 아니다」를 함께 적는다** | B3에서 상한을 맞추려 용어표 행을 삭제했다. **제약이 경성인지 연성인지 말하지 않으면 구현자는 경성으로 읽는다** |
| **22** | **새 판정에는 「이 글의 정리다」 귀속을 붙이게 한다** | 귀속 붙은 요약 **6/6 통과** · 축 B 최상위 지적 5건은 **전부 무귀속 요약**에서 나왔다 |
| **23** | **「N편에 분산」 지시는 편별 체크리스트로 준다** | 사실 경계 고지와 출처 각주가 **편13에서만 누락**됐다. 「3편 각주로」라고만 적으면 한 편이 샌다 |
| **24** | **도식을 새로 그리면 화살표마다 원문 근거를 대게 한다** | 편12 신규 도식의 여섯 화살표 중 하나가 거짓이었다. **노드는 전부 근거가 있었고 관계만 지어냈다** |

**B1·B2가 준 기존 8건**(요약문 수치 대조 / 원문 강도 상승 금지 / 수정 후 스캔 재실행 / 착수 전 표 밀도 측정 / `[실측]`·`[설계서]`·`[추론]` 표기 / 설계서 진단은 옮기기 전에 실측 / `tags.ts` 패싯 규칙 명시 / 재검증에 「사라졌는가」)는 그대로 가져간다.

### 진행 방식 — 검증된 절차

```
1단계  분할 설계 → 승인                              ← 완료
2단계  배치로 나눠 변환, 배치마다 커밋·보고            ← B1·B2·B3 완료, B4부터
3단계  구현 미참여 에이전트가 4축 독립 검증
4단계  지적 반영 → 재검증(다른 에이전트 2기) → 커밋 → (요청 시) 푸시
```

**검증 4축은 그대로 쓴다.** 나누는 기준은 주제가 아니라 **결함이 드러나는 방식**이다.

| 축 | 무엇을 보는가 | 왜 별도 축이어야 하나 | 티어 |
| --- | --- | --- | --- |
| A | 쓰여 있는데 **틀린** 것 | grep으로 안 잡힌다. 세면 0건이라 자동 검증을 통과한다 | opus |
| B | 쓰여 있는데 **원문에 없는** 것 | 원문과 나란히 놓아야만 보인다 | opus |
| C | **없는** 것 | 읽어서는 안 드러난다. **원본 기준 역방향**으로 훑어야 한다 | opus |
| D | **실행하면 나오는** 것 | 종료코드는 객관적이다 | sonnet |

**B3에서 축 C의 값이 증명됐다.** 「편13에만 고지가 없다」는 A·B 어느 축도 찾을 수 없다 — 발행본을 읽으면 편13은 완결돼 보이고, 원문 대조를 해도 「L1044가 편12·14에 있다」까지만 확인된다. **원본 자산 하나가 N편 중 몇 편에 도달했는지 세는 것**은 역방향으로만 나온다.

**재검증은 2기로 나눈다.** B3에서 반영 정확성(opus)과 실행(sonnet)을 나눴고, 반영 정확성 쪽이 **5건을 더 찾았다.**

---

## 3. ⚠️ 설계서 정정 — B4 이후에 영향

### 3-1. §6-5 8번 `05 §11`은 출처 대장이 아니다 (B3에서 정정 완료)

| | 설계서 초안 | **B3 실측** |
| --- | --- | --- |
| `05 §11`(1,523 B)의 성격 | **출처 대장** | **면접 화법 가이드** |
| 옮길 양 | 전량을 편12·13·14 각주로 | **L1044 사실 경계 1문장뿐** |

**표가 7번(§10-4)과 8번(§11)을 둘 다 「출처 대장」이라 적었으나 대장은 하나뿐이다.**

> **놓친 경로**: 절 제목이 `인용 시 주의 — 사실 경계`다. 제목만 보면 출처 관리 절로 읽히고 본문은 화법 가이드다.
> §5-3 정정이 남긴 교훈(*"발행본을 열지 않고 표만 보고 기준을 규정했다"*)이 이번엔 **원본 쪽에서** 반복됐다.

**설계서의 진단은 브리프로 옮기기 전에 실측한다 — B1(§6-1·§10) · B2(§5-3) · B3(§6-5). 3배치 연속이다.**

### 3-2. 앞 배치 정정도 유효하다

| 배치 | 정정 위치 | 요지 |
| --- | --- | --- |
| B1 | §6-1 | 「`강의가 ~한다` 130여 개」 → **실측 32** (4.1배 과대) |
| B1 | §10 | 팽창률 1.20 → **1.76** (구조적 초과 6.6KB는 재량이 아님) |
| B2 | §5-3 ① | 발행본 다섯의 열거 기준은 「소비」가 아니라 **「컨텍스트 관리를 상위 목적으로 공유하는 수단」** |
| B2 | §5-3 ② | 「MCP를 뺀 이유」는 그 글에 **없다.** 인과를 관측으로 낮추고 반례를 본문에 적었다 |
| B2 | §5-3 ③ | 발행본 시점은 「없음」이 아니라 **2026년 3월 자료 기준**(L19 명시) |

---

## 4. 미해결 과제

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | **mermaid 노드 라벨 우측 1~9px 클리핑** | `foreignObject` 폭이 텍스트보다 좁다. **전역 렌더링 이슈로 별건 처리 필요** |
| 2 | **FR-1.4 스크롤스파이 미구현** | 기존 위키(`wiki-shell.tsx`)에도 없어 신규 후퇴는 아니다 |
| 3 | **메인 페이지 LCP 6.6초** | Lighthouse 77점. **블로그 도입 이전에도 77점**이었다 |
| 4 | **싱글톤 태그 재측정** | §13-3 목표는 "2차 신규 싱글톤 0". **2차 전체를 마친 뒤 재측정** |
| 5 | **`cto_learning_roadmap` Q1·Q3** | 발행 가능 판정을 받았으나 미변환 |
| 6 | **어휘 81종 중 30종이 0편** | `redis` `kafka` `docker` `aws` `spring` `java` `nosql` `load-testing` `ci-cd` `deployment` `distributed-systems` `event-driven` `cdc` `cqrs` `microservices` `scalability` `vector-database` `machine-learning` `product-discovery` `prd` `product-metrics` `ab-testing` `user-research` `commerce` `payments` `global-service` `startup` `team-building` `career` `recommender-systems`. **어휘에 있는 것과 살아 있는 것은 다르다** |
| 7 | **훅 차단 채널이 두 발행본에서 다르다** | 편11은 **stdout JSON**, `settings-permissions-deny`는 **표준 에러**. 두 원본 사이의 부채다. **편11에 고지를 달아 막아 뒀으나 근본 해소는 아니다** |
| 8 | **원본 `04 §8-2` 「3대 구성 요소」인데 표는 4행** | 원본에서 이월된 불일치. 편11에 해명만 달았다 |
| 9 | **원본 `05` 내부 수치 불일치** | L56 도식 `frontmatter 5+4 필드` ↔ L145 표 `핵심 5 + 확장 5`. **편12가 양쪽을 원문대로 옮기고 「원 자료 안에서 판정되지 않는다」로 명기**했다. 판정하지 않은 것이 맞다 |
| 10 | **`05 §3-2` 제품 결함 3건의 현재 유효성** | 「버전 표기가 없다」는 사실은 명기했으나 **세 동작이 지금도 그런지는 확인하지 않았다.** 원 자료 밖 확인이 필요하고 이번 범위가 아니었다 |
| 11 | **무귀속 판정 잔존 2건** | 편14 L232 · 편13 L235. 축 A·B·C가 통과시킨 것들이라 B3에서 손대지 않았다. **B4 기준 항목으로 올린다** |

### `ai-transformation`으로 이월된 위험 3건

`06`을 이관하면 조사자 D가 찾은 위험도 함께 간다. **설계서 §12-4에 상세가 있다.**

| # | 위험 |
| --- | --- |
| 1 | `06 §4-6`·`§8-7` **정량 개선 수치 9행.** 원본이 세 겹으로 방어하는데 발행 제외 절 + `강의` 제거로 셋 다 사라진다 → **출처 없는 성과표.** 발행하지 않는 선택지 우선 검토 |
| 2 | **`06`은 발행본과 서사 방향이 반대다.** 발행본은 "위임을 늘려라", `06`은 "어디서 사람을 세울 것인가". **연결 문장이 없으면 모순으로 읽힌다** |
| 3 | `06 §2-8` ↔ `loop-antipatterns-and-checklist` 셀 단위 대조 **미실시** |

---

## 5. README/문서 갱신 필요 (이번 세션에서 고치지 않음)

**여섯 번째 이월이다.** README는 2026-08-07(`7f28866`) 이후 손대지 않았고 그 사이 **19커밋**이 쌓였다.

| 낡은 부분 | 왜 낡았나 | 확인할 진실원 |
| --- | --- | --- |
| 프로젝트 구조 트리 | `content/`·`scripts/`·`tests/` 없음 | 실제 디렉터리 |
| 기술 스택 | Vitest·react-markdown·mermaid 없음 | [`package.json`](package.json) |
| 기능 설명 | 블로그 **93편·4카테고리**가 통째로 없음 | [`pages/blog/`](pages/blog/), [`content/blog/`](content/blog/) |

**고치지 않은 이유**: 기능 설명·아키텍처 서술은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 세션 끝이라 컨텍스트가 가장 얕다. 그 상태에서 손대면 "그럴듯하지만 틀린 README"가 커밋된다.

### ⚠️ 환경변수 검사 신호는 **오탐이다. 고치지 마라**

```
[HIGH] ENV_KEYS_IN_CODE_BUT_NOT_IN_README
  LANGCHAIN_PROJECT · LANGCHAIN_TRACING_V2 · OPENAI_API_KEY
  TAVILY_API_KEY · UPSTAGE_API_KEY
```

**전부 `content/blog/**/*.md`의 코드 예제 안**이다. 이 사이트가 쓰는 환경변수가 아니라 RAG·에이전트를 설명하는 **블로그 글 본문의 예시 코드**다. `.env.example`을 만들거나 README에 표를 추가하면 **"이 포트폴리오 사이트가 OpenAI API를 사용한다"는 거짓 정보**가 된다. 정적 export 사이트라 런타임 환경변수가 없다.

실제로 쓰는 것은 [`lib/site.ts`](lib/site.ts)의 `NEXT_PUBLIC_SITE_URL` **하나**이며 README 47~51행에 이미 문서화돼 있다.

`[LOW] SCRIPTS_DIR_MISSING_IN_README: generate-sitemap.mjs`도 **오탐** — `next build` 후 자동 실행되는 훅이라 사람이 직접 부르지 않는다.

---

## 6. 알아둘 함정 — 이번 세션에서 새로 배운 것

직전까지의 함정은 [요구사항 §14·§15](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md)와 설계서 §13, 그리고 `CHANGELOG.md`의 B1·B2 항목에 있다. **아래는 B3에서 새로 나온 것이다.**

### 6-1. 제약을 줄 때 경성인지 연성인지 함께 말해야 한다

**"넘으면 보고하라"만 받은 구현자는 보고를 피하려고 내용을 깎는다.**

| 브리프 | 구현자 행동 |
| --- | --- |
| *"29.4KB를 넘으면 보고하라"* | 초고 30,528 B → 세 차례 깎아 **29,373 B(여유 27 B)** · 축약 용어표 행 삭제 |
| *"상한은 합격선이 아니다. 넘어도 된다"* (반영 지시) | **30,897 B** — 누락됐던 용어표 5행 복원 |

설계서 §4-0에 *"실측 분포이지 합격선이 아니다"* 가 **이미 있었는데 브리프로 옮기지 않았다.**

### 6-2. 귀속 표기가 방어선으로 작동한다

| 요약 유형 | 검증 결과 |
| --- | --- |
| 「이 글의 정리다」 **귀속을 붙인** 요약 6건 | **6 / 6 통과** |
| 귀속이 **없는** 요약 | 축 B 최상위 지적 5건이 **전부 여기서** |

귀속이 붙으면 그 문장은 「원문의 주장」이 아니라 **「이 글의 해석」**이 되어 거짓이 될 수 없다. **검증자가 요건 밖에서 찾아낸 규칙이다.**

### 6-3. 도식은 노드가 아니라 화살표에서 지어낸다

편12 신규 도식의 **노드 9개는 전부 원문 근거가 있었고, 화살표 여섯 중 하나가 거짓**이었다.

```
Y3(확장 5) --> V(10-Point Validator)
   ↳ Validator 10항목이 이름을 대는 필드는 7개
     isolation · skills · disallowedTools 는 한 항목에도 없다 → 실제로는 5 중 2
```

**화살표는 인과·순서·포함을 단언한다.** 원문이 나열만 했다면 그 관계는 변환자가 만든 것이다. 도식이 없었다면 독자가 스스로 알아차렸을 공백을 도식이 덮는다.

### 6-4. 「N편에 분산」 지시는 한 편에서 샌다

사실 경계 고지와 출처 각주가 **편12·14에는 있고 편13에만 없었다.** 두 건 다 같은 형태다.

> **편은 독립 URL로 유입된다.** 「3편 각주로」라고만 적으면 편별 체크가 없어 한 편이 빠진다.
> **분산 지시는 편별 체크리스트로 줘야 한다.**

### 6-5. 정확도를 올리는 수정이 새 검증 부담을 만든다

편12 L117의 잘못된 행 이름(「수명」, 실제는 「상태·지속성」)은 반영 전에도 있었고 **축 A·B·C 셋 다 통과시켰다.** 그런데 반영이 그 문장을 **「4+1+2=7 전수 정산」**으로 다시 쓰자 독자가 일곱 이름을 표에 대응시키게 됐고 **그 순간 이름 불일치가 결함이 됐다.**

「대충 넷쯤」은 틀릴 수 없지만 「4+1+2=7」은 틀릴 수 있다. **정밀한 진술은 그만큼 넓은 검증 표면을 갖는다.**

### 6-6. 검증 → 취합 → 지시 사이에도 전달 손실이 있다

축 B의 원래 지적문에 *"3행의 「종료 요청 → 전원 응답 확인 → 해체 순서 준수」도 실행 시점 절차다"* 가 있었는데, **컨트롤러가 22건을 취합해 처리 문면을 쓰면서 그 한 줄을 떨어뜨렸다.** 재검증 N-1로 재발했다.

**그 손실은 지적 자체보다 조용하다** — 지적은 보고서에 남지만 취합 누락은 아무 데도 안 남는다.

### 6-7. 반영은 지적을 문자 그대로 처리하면 오히려 이상해진다

지적은 문장 하나를 가리켰으나 그 문장은 **「대비 구조」의 한쪽 항**이었다. 한쪽만 지우면 남은 쪽(「~만 반대 방향」)이 비교 대상 없이 떠다닌다. 반영자가 blockquote 제목까지 사실 서술로 바꿔 해소했다.

**다만 대비 구조를 버리면서 정보까지 버렸는지는 별도 확인이 필요하다** — 재검증 결과 원본 정보는 표 두 곳에 온존했다.

### 6-8. mermaid는 빌드가 검증하지 않는다

**클라이언트 렌더링이라 문법 오류가 있어도 종료코드 0으로 통과하고 브라우저에서만 깨진다.** 도식을 손댄 배치에서는 블록 전수를 눈으로 봐야 한다. B3에서 17개 전수 검토했고 이상 없었다.

### 6-9. 서브에이전트 최종 응답 유실이 이번에도 3회

`idle`을 보고했으나 최종 응답 텍스트가 도달하지 않은 일이 **3회**(scout 1 · converter 2) 있었다. **파일 산출물을 지시한 건은 전부 회수됐다.**

> **브리프에 산출물 경로를 파일로 못박아라.** 「메시지로 보내라」고 한 건이 유실됐다.

---

## 7. 검증 명령

```powershell
cd C:\Users\aeby\vscode\withwooyong.github.io

npm test              # 36개 통과 (tests/blog/ 3파일)
npx tsc --noEmit      # 0
npm run build         # 0, [sitemap] 154개 URL, 정적 페이지 163

# 금칙어 — 기대 0건
Select-String -Path "content\blog\**\*.md" `
  -Pattern "면접|커닝페이퍼|암기용|화이트보드|이력서|강의|수강|Part [0-9]|CH0[0-9]"

# 민감정보 — agentic-coding 기준 「채용」 2건만 기대
Select-String -Path "content\blog\**\*.md" `
  -Pattern "히츠|야나두|TVING|티빙|BTV|허우용|채용|teddylee|teddynote|argus|FASTCAMPUS|실장"

# 화행 잔재(SC-12) — 오탐 많음. 문맥 판정 필수
Select-String -Path "content\blog\**\*.md" `
  -Pattern "인용할 때|수준으로 말|것이 안전|실적이 아니|반박당|의심받|감점|말해야 |말하면 안"
```

> **알려진 오탐 5종**
> - `허우용` — `out/` 기준으로 스캔하면 nav·푸터·SEO 메타가 잡힌다. **원고(`content/`) 기준으로 스캔할 것**
> - `테디노트`·`패스트캠퍼스` — `source:` 출처 표기다. **정규식은 영문만 잡는다**
> - `채용` **2건** — `subagent-definition-fields.md:39`(용어표 「채용 공고」) · `agent-team-operations.md:490`(유비표 「채용과 JD 작성」). 구직이 아니라 **에이전트 정의를 채용에 비유**한 것 (**B3에서 발생**)
> - `인용할 때` **1건** — `agent-team-operations.md:418` 「인용할 때는 정정된 수치를 쓴다」. 원 자료 수치 인용 지침이지 면접 화법이 아니다 (**B3에서 발생**)
> - 화행 정규식 일반 — `이렇게 구현하는 편이 안전하다`(구현 권고), `LLM이 답한다`(주어가 모델)는 정상

### 태그 패싯 분포 검사 (빌드가 못 잡는다)

```powershell
# tags.ts 상단 주석: "같은 패싯에서 최대 2개"
# A STACK / B ENGINEERING / C AI / D PRODUCT / E SEARCH / F MISC
Select-String -Path "content\blog\agentic-coding\*.md" -Pattern "^tags:"
```

**패싯 C가 3개 이상이면 위반**이다. `claude-code`·`ai-agent`·`multi-agent`·`ai-governance`·`ai-automation`·`context-engineering`·`prompt-engineering`이 전부 C라 AI 계열 글에서 쉽게 넘는다.

> **B3에서 실제로 걸렸다** — 설계서 §8-2가 편12·13·14에 `ai-agent`를 지정했고 §8-3이 `claude-code`를 주력으로 세웠는데 **둘 다 C**다. 편13은 `multi-agent`(C)가 주제라 **`claude-code`를 뺐다.**

### mermaid 문법 검사 (도식을 손댄 배치에서만)

```bash
# 빌드는 mermaid를 렌더하지 않는다. 블록 전수를 눈으로 봐야 한다
awk '/^```mermaid/,/^```$/' content/blog/agentic-coding/<파일>.md
```

노드 ID 미정의·중복, 점선 라벨 `-.->|"..."|` 짝, 특수문자(`·` `—`) 든 라벨의 따옴표, 블록 개폐 수를 본다.

### 원본 실측 재현 (펜스 추적 필수)

```bash
cd "C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/aiagent"
LC_ALL=C awk '
  BEGIN { sec="머리말" }
  /^```/ { if (!fence && $0 ~ /^```mermaid/) mm[sec]++; fence=!fence; b[sec]+=length($0)+1; next }
  !fence && /^## / { sec=$0; order[++n]=sec; start[sec]=NR }
  !fence && /^\|[ ]*[-:][- :|]*\|[ ]*$/ { tb[sec]++ }
  { b[sec]+=length($0)+1 }
  END { t=0; for(i=1;i<=n;i++){s=order[i]; t+=b[s];
        printf "%s | L%d | %d B | 표 %d | 도식 %d\n",s,start[s],b[s],tb[s],mm[s]}
        printf "합계 %d B\n", t+b["머리말"] }' <파일>
```

**⚠️ `length($0)+1`의 `+1`이 개행이다. 빼먹으면 절마다 줄 수만큼 미달한다** — B2에서 실제로 발생했다.
**펜스를 추적하지 않으면 H2가 과대계수된다** — 코드블록 안 가짜 H2가 `03`에만 10개다.
**절별 합계를 `wc -c`와 대조하라.** B3에서 이 대조로 조사 보고의 정확성을 확인했다(머리말 423 B + 절별 60,461 = 60,884).

### 링크·SC-10 검사 (bash)

```bash
# 내부 링크 전건 실재 확인
grep -ho '/blog/[a-z-]*/[a-z0-9-]*/' content/blog/*/*.md | sort -u | while read l; do
  cat=$(echo "$l" | cut -d/ -f3); slug=$(echo "$l" | cut -d/ -f4)
  [ -n "$slug" ] && [ ! -f "content/blog/$cat/$slug.md" ] && echo "MISSING: $l"
done

# SC-10 들어오는 링크가 0인 글
for f in content/blog/*/*.md; do
  s=$(basename "$f" .md)
  n=$(grep -rl "/$s/" content/blog/ --include=*.md | grep -v "/$s.md" | wc -l)
  [ "$n" -eq 0 ] && echo "들어오는 링크 0: $s"
done
```

---

## 8. 소스 위치

| | 경로 |
| --- | --- |
| 원본 (읽기 전용, **수정 금지**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 발행본 | [`content/blog/<카테고리>/<slug>.md`](content/blog/) — **93편** |
| 카테고리 정의 | [`content/blog/categories.ts`](content/blog/categories.ts) — 12개 등록, **4개 발행** |
| 태그 통제 어휘 | [`content/blog/tags.ts`](content/blog/tags.ts) — 81종(실사용 51). **목록 밖이면 빌드 실패. 상단 주석에 패싯 규칙이 있다** |
| 요구사항 | [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — §13·§14·§15 |
| **`agentic-coding` 설계서** | [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — **§4-4·§5-3·§6-1·§6-5·§10·§11에 정정 블록** |
| `ai-agent` 설계서 (템플릿) | [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) |
| **B1 보고서 5건** | [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환·실행검증·리뷰2·반영 |

**B1 보고서 5건이 저장소에 남아 있다.** 뒤 배치는 이것을 템플릿으로 쓸 수 있다 — 다만 **변환 보고서 §4-2 분류표는 상쇄 오류가 있었으므로 그대로 복제하지 마라.**

> **B2 보고서 7건 · B3 보고서 13건은 스크래치패드에만 있고 커밋하지 않았다.** 세션이 끝나면 사라진다.
> 핵심은 이 문서 §3·§6과 `CHANGELOG.md`, 그리고 **설계서 정정 블록**에 옮겨 적었다.
