# Session Handoff

> 갱신: 2026-08-30 (KST)
> 브랜치: `main` — 이 세션에서 **origin 으로 푸시했다** (사용자 승인: 「푸시까지」)
> 최신 커밋: 편4 발행(`762af43`)과 설계서 §10-6 갱신(`d0cd914`)

## 현재 상태

원본 `java-backend/08-스프링부트-인증-쿠버네티스.md`(49,201 B 배정분)를 **6편**으로 나누는 배치가
진행 중이다. **편1·편2·편3·편4 발행 완료**, 절차 ⑥(편4 실측으로 편5 재환산)까지 끝났다.
다음은 **편5 집필**이고, 목표가 18,200 에서 **19,400** 으로 올라갔으며 **밴드는 그대로 두었다.**

## 이번 세션에 한 일

| # | 작업 | 커밋 | 산출물 |
| ---: | --- | --- | --- |
| 1 | 편4 집필·검증·발행 | `762af43` | `content/blog/backend-engineering/session-vs-token-authentication.md` (17,309 B) · 편3 링크 · README · CHANGELOG |
| 2 | 편4 실측을 설계서 §10-6 에 반영하고 편5 재환산 | `d0cd914` | `docs/superpowers/plans/2026-08-29-auth-k8s-split-design.md` |

`/ted-run` 파이프라인으로 돌렸다. 검증 결과: `check-forbidden` **HARD 0 · SOFT 2**(둘 다 이번 배치
무관), `dup-scan` 편4 **6건**(전부 정당 — 아래 참조), 원본과의 축자 겹침 **20자 이상 0건**,
`npm test` **54개 통과**, `tsc --noEmit` 통과, `check-counts` **165편 일치**.

## 다음 할 일

| # | 작업 | 상태 | 메모 |
| ---: | --- | --- | --- |
| 1 | **편5 집필** (`kubernetes-deployment-and-rollout`) | 다음 차례 | 원본 §9·§10·§11. 목표 **19,400 B**, 밴드 16,000 ~ 20,400. 🔴 **상단 여유가 1,000 B 뿐이다** — 편4가 목표를 +8.9% 넘겼으므로 같은 경향이면 밴드를 벗어난다 |
| 2 | 편4 → 편5 링크(64 B) 채우기 | 편5 발행 커밋에 포함 | 편4는 밴드 상단까지 691 B 여유라 들어간다 |
| 3 | 편6(Q&A) 집필 + **편1~5 링크 수정 5건** | 대기 | 규칙 12. 19,313 B · 42항목 |
| 4 | 타 카테고리 인바운드 링크 3건 | 대기 | §6-3 — `agentic-coding` 2편 · `ai-transformation` 1편 |
| 5 | 배치 마감 (§10-0 가설표) | 대기 | C-1 · C-2 · C-3 · **C-4(수정됨)** · **C-7(굳음)** · **C-8(수정됨)** |

## 이번 세션의 판단과 근거

- **C-4 를 다시 썼다.** 편3 실측에서 「표 비율은 합치기 계획을 지켰는가가 정한다」로 결론지었는데
  편4가 뒤집었다. 편4는 계획을 **그대로 지켜** 6표를 5표로 줄였는데 비율이 **1.407** 로 편3(0.89)의
  1.6 배다. 갈린 것은 **합치기의 축**이다 — 편3은 행을 흡수해 셀을 지웠고, 편4는 열을 붙여 남은 행
  전부에 칸을 하나씩 얹었다. ⇒ §3-2-b 의 계획 칸에 **합치기의 축(행/열)**을 반드시 적어야 한다.
- **두 점으로 그은 직선이 1,246 B 빗나갔다.** §10-5-c 가 편2·편3으로 그은
  `발행 본문 = 7,182 + 0.813 × 원본 본문` 은 편4를 8,780 으로 예측했으나 실측은 7,534 다.
  세 점으로 다시 그으면 `5,317 + 1.361 × 원본 본문` 이 되어 절편이 1,865 내려가고 기울기가
  0.548 오른다. §10-5-c 의 유보가 옳았다 — **두 점 직선은 예산에 쓰지 않는다.**
- **C-8 의 감소 폭이 꺾였다.** 3,338 → 3,026 → 2,715 → **2,518**. 차가 −312 · −311 · **−197** 이라
  등차가 아니라 수렴이다. 편5·편6이 2,300 언저리에 머물면 그 값이 진짜 상수다.
- **C-7 은 네 편 연속 0으로 굳었다.** 원본 불릿 1,604 B 가 이 배치에서 편2 다음으로 큰데도 전량
  본문과 표로 흡수됐다. ⇒ 불릿을 독립 성분으로 세우는 분해식을 폐기한다.
- **헤딩 항은 절 수 × 45 B** 로 잡는다. 편3 47 · 편4 41 이라 §10-5-a 의 권고(50)보다 낮다.

## 🔴 리뷰가 잡은 것 — 검사기 여섯 개를 전부 통과한 뒤에 나왔다

편5·편6에도 그대로 적용된다.

| 등급 | 결함 | 왜 기계가 못 잡나 |
| --- | --- | --- |
| CRITICAL | 도입이 「앞 편의 비기능 요구 **두 줄**」이라 했으나 편3 비기능 표 5줄에 회수 요구가 없다. 그것은 **편2의 기능 요구 4번**이다 | 편 사이의 출처 추적은 검사기에 없다 |
| HIGH | 「여덟 시간」 요구를 「앞 편의 명세」로 적었으나 편3에 0건, 편2:143 에 있다 | 같은 위 |
| HIGH | 편1 용어표가 못 박은 **「세션」이 본문 0회**(제목에만 1회) | 🔴 **낱말이 없는 것은 grep 으로 못 찾는다** |
| HIGH | 접근 토큰 8 : 「짧은 토큰」 3 · 갱신 토큰 9 : 「갱신용」 2 | 편3의 27:6 과 같은 모양 |
| HIGH | 「하나의 증표로 동시에 만족시킬 방법은 **없다**」를 자기 표 2행이 반증 | 편3과 같은 **한 방향 → 양방향 확대** |

🔴 **편3과 세는 방향이 반대인 결함이 나왔다.** 편3은 같은 대상을 두 이름으로 불러 두 낱말을 다
세면 드러났지만, 편4는 편1이 정한 낱말을 **아예 쓰지 않아** 앞 편의 예고가 비었다.
⇒ **편5·편6 브리프에는 표기 규칙만이 아니라 「이 편이 반드시 써야 하는 낱말」 목록을 넣어라.**
편5는 「프로브」·「상태 있음 · 상태 없음」·「정책 데이터」가 그것이다.

⚠️ 축자 겹침 축에서 **도식 2개가 따옴표만 빼고 원본과 완전 동일**하게 나왔다(`diff` 차이 0줄).
본문은 전부 다시 썼는데 도식만 남은 것은 「도식을 산문으로 풀지 마라」가 **「도식은 손대지 마라」로
읽혔기 때문**이다. ⇒ 편5 브리프에 **「도식은 구조를 유지하되 라벨은 다시 쓴다」**를 명시하라.

⇒ **리뷰 축을 「경계·정합성」과 「원본 축자 겹침」 둘로 나눠 돌려라.** 편4에서도 한 축이 잡은 것을
다른 축은 하나도 보지 못했다. 리뷰어는 `Write` 도구가 없으므로 브리프에 **보고 파일 경로와
heredoc 작성**을 반드시 명시한다.

## 알려진 문제

- **편2의 밴드 여유가 2 B 뿐이다.** 편3 링크를 넣어 21,318 이 됐고 상단이 21,320 이다.
  앞으로 편2 본문을 한 글자라도 늘리면 밴드를 벗어난다.
- **편5 표 비율 가정 1.25 가 이 배치에서 가장 약한 근거다.** 실측 셋이 0.89 · 1.22 · 1.407 로
  흩어져 있고, 편5만 표를 합치지 않는 편이라 대응하는 표본이 없다.
  초고에서 표가 4,300 을 넘으면 그때 재조정하라(§10-6-f).
- **`dup-scan` 의 「N건」은 결론이 아니라 사람이 열어 볼 목록이다.** 편4의 6건은 프레임워크
  클래스명 5건과 편2 요구사항 인용 1건으로 전부 정당했다. 특히 인용 1건은 **리뷰가 「인용부호 안
  문구가 원문과 다르다」고 지적해 원문대로 고친 결과**라, 겹침이 생긴 것이 옳다.
- **`grep -r` 로 리포를 훑으면 `out/` 과 `node_modules` 까지 스캔해 120초 타임아웃이 난다.**
  `Grep` 도구나 `--include` / 명시 경로를 써라.
- `check-forbidden` SOFT 2건은 `agentic-coding/thirty-agent-catalog.md:195` 의 「면접」·「이력서」로,
  채용 에이전트를 설명하는 정당한 문맥이다. 이번 배치와 무관하다.
- `npm test` 가 SOFT 로 보고하는 **분량 하한 미달 5편**(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은
  이번 배치 이전부터 있던 것이다. 빌드를 막지 않는다.

## 다음 세션을 위한 맥락

사용자의 목표는 사내 지식 문서(`yanadoo-exit/shared/knowledge/`)를 기술 블로그 발행본으로 옮기는
배치 작업이고, 이 리포의 주된 작업 방식이다. **핵심은 분량을 감으로 정하지 않는 것**이다 —
설계서가 원본을 성분(산문·표·코드·도식·불릿·인용)으로 분해해 편별 예산을 계산하고, 편마다 실측해
남은 편의 예산을 재환산한다. 그 계보가 `docs/superpowers/plans/*-split-design.md` 에 쌓여 있다.

**편5를 시작할 때 읽을 것**

| 순서 | 대상 | 왜 |
| ---: | --- | --- |
| 1 | 설계서 **§10-6-f** | 편5 목표 19,400 의 성분별 계산 근거. 상단 여유 1,000 B 경고가 여기 있다 |
| 2 | 설계서 **§10-6-b** · **§10-6-g** | 표 합치기의 축 문제와, 편4가 남긴 결함 다섯. 셋은 편5에도 그대로 온다 |
| 3 | 설계서 **§3-2-b** · **§4-1** | 편5는 **6표 전부 유지**한다(합치지 않는 유일한 편). 주제 경계는 「인증 로직 자체는 편4가 이미 답했다」 |
| 4 | 편1 **§0 용어표** · 편4 **맺음말** | 규칙 11. 편4 맺음말이 편5에 넘긴 질문은 「몇 개를 띄우고, 환경마다 다른 값과 남에게 보이면 안 되는 값을 어디에 두고, 새 판을 올리는 동안 멈추지 않게 하려면 무엇이 필요한가」다 |

🔴 **초고를 쓴 직후 `npm run compose` 를 먼저 돌려라.** 편3·편4가 이 절차를 지켜 초고가 목표를
각각 1.7% · 15.9% 넘는 데 그쳤다(편4는 그 뒤 산문만 압축해 밴드에 들었다). 편2는 이 절차 없이
산문부터 깎아 네 차례에 걸쳐 5,900 B 를 깎고도 밴드에 못 들었다.

⚠️ **편5는 밴드 상단을 넘긴 채로 리뷰에 넘기지 마라.** 20,400 을 넘으면 그 자리에서 깎는다.
편4는 리뷰 처방이 순증(+1,018)이라 초고가 빠듯했으면 밴드를 벗어났을 것이다.

발행 절차는 설계서 §8-1 의 일곱 검사를 순서대로 돌리고 §9-1 의 커밋 단위를 지킨다.
커밋은 `git commit -F- -- <경로>` 형태인데, **새 파일은 `git add` 를 먼저 해야 한다** —
경로를 명시해도 추적되지 않은 파일은 pathspec 에러로 죽는다.

**푸시 정책** — `main` 이 프로덕션(GitHub Pages)이므로 사용자가 명시적으로 요청할 때만 푸시한다.
이번 세션은 파이프라인 시작 시점에 「푸시까지」를 승인받았다.
