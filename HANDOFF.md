# 인수인계 (HANDOFF)

> 갱신: 2026-08-27 · 브랜치 `feat/site-renewal` → `main` 병합 · 이번 배치 커밋 **64개**
> **발행본 156편 불변 · 아틀라스 노드 162 · 엣지 1,053 · Pagefind 조각 405 · E2E 74건**
>
> 📌 **이번 배치는 글을 한 편도 늘리지 않았다.** 늘어난 것은 **읽는 방법 둘**이다 —
> 글끼리의 관계를 보는 `/atlas` 와 한국어 전문 검색 `⌘K`.
> **메인 페이지와 `/blog` 156편은 한 글자도 바뀌지 않았다**(GC-11·GC-12, `git diff` 로 0건 확인).
>
> 남길 값어치가 있는 산출물은 기능이 아니라 **「했다고 주장하는데 아무 일도 안 하는 것」을
> 다섯 가지 얼굴로 만난 것**이다. 아래 §3 이 그 목록이고, 전부 초록이라 신호가 없었다.

---

## 1. 이번 배치에서 한 일

| # | 무엇 | 규모 |
| ---: | --- | --- |
| 1 | 새 셸(헤더·푸터·드로어·테마 토글) · 디자인 토큰 | 커밋 다수 |
| 2 | `⌘K` 커맨드 팔레트 + Pagefind 정적 인덱스 | 커밋 6 |
| 3 | 아틀라스 데이터 — zod 스키마 · MDX 매핑 · 무결성 게이트 | 커밋 8 |
| 4 | `/atlas` 그래프·목록 뷰 + 노드 상세 **162장** | 커밋 4 |
| 5 | **Task 12** — 헤더 NAV 등재 · 아틀라스 E2E · 스펙 역반영 · 기준선 1회 갱신 | 커밋 5 |
| 6 | 품질 게이트 5종 신설 + CI 배치 | 커밋 다수 |

**계획서:** [`docs/superpowers/plans/2026-08-26-atlas-and-search.md`](docs/superpowers/plans/2026-08-26-atlas-and-search.md) (T1~T12 **완료**)
**설계서:** [`docs/superpowers/specs/2026-08-25-redesign-and-atlas.md`](docs/superpowers/specs/2026-08-25-redesign-and-atlas.md) (D-1·D-2 역반영 완료)

---

## 2. ⚠️ 다음 세션이 가장 먼저 할 일

### 2-1. **선행 계획서의 단계 2 가 아직 보류 중이다**

[`docs/superpowers/plans/2026-08-25-redesign-phase-1-2.md`](docs/superpowers/plans/2026-08-25-redesign-phase-1-2.md) 의 **T9~T13** 은
삭제가 아니라 **보류**다. 사용자가 현재 히어로를 유지하기로 해서 검색·아틀라스를 먼저 했다(D-1).

거기에는 히어로 재설계 · `/work` · `/about` · `product-lead*` 스텁이 들어 있다.
**꺼내기 전에** 아틀라스·검색 계획서의 §「이월된 것 — 선행 계획서의 단계 2」와
§「T9가 이월된 이유 — 실측 4건」을 읽어라. 다시 설계할 때의 출발점이 거기 있다.

### 2-2. 후속 계획서로 넘긴 것 — **실물을 보고 정해야 하는 것들**

아틀라스가 이제 화면에 있으므로 **지금이 그 「실물」이다.**

| 항목 | 무엇을 보고 정하나 |
| --- | --- |
| Canvas 2D 렌더러 · 렌더러 자동 선택 | **엣지 1,053** 을 SVG 하나로 그리고 있다. 화면에서 뭉개지는지 눈으로 보라 |
| 렌즈 3종 (전체·토픽별·클러스터별) | 클러스터 탐지(Louvain)가 이 엣지 밀도에서 쓸모 있는 결과를 내는지 |
| 좌측 토픽·태그 사이드바 필터 | D-2 가 「태그는 사이드바 필터로만」이라 정해 놓고 **그 사이드바를 아직 안 만들었다.** 고유 태그 64개 중 10개는 1회만 등장한다 |
| 검색 결과에 아틀라스 노드 섹션 | 지금은 노드 상세를 인덱스에서 **뺐다**(원문과 중복이라). 섹션을 따로 두려면 그 결정을 다시 봐야 한다 |

### 2-3. 남은 접근성 결함 **3장** — 이번엔 못 고쳤다

`<main id="main">` 에 `tabIndex={-1}` 이 없어 검색으로 도달했을 때 포커스가 본문으로 안 간다.
`focus()` 는 **예외를 던지지 않고 조용히 아무것도 안 한다** — 그래서 신호가 없다.

| 페이지 | 왜 못 고쳤나 |
| --- | --- |
| `/en/` · `/notion/` | `check-baseline` 의 **금지 대상**이다. 건드리면 기준선이 바뀌는데, 그 둘은 재설계 대상이 아니라 변경 자체가 회귀 신호다 |
| `/` (메인) | **GC-11 로 봉인.** 단계 2 에서 히어로를 다시 만들 때 함께 |

`/notion/` 은 `id="main"` **자체가 없다.** 셋 다 검색 인덱스에는 살아 있다.

---

## 3. 이번 배치가 만난 다섯 얼굴 — **전부 초록이라 신호가 없었다**

이 리포의 `CLAUDE.md` 가 「**「없다」와 「읽을 수 없었다」가 같은 출력으로 나온다**」고 적어 둔 것의
다섯 가지 변종이다. **뮤턴트를 심어 보지 않았으면 다섯 건 다 통과했다.**

| # | 무엇이 거짓 초록이었나 | 어떻게 드러났나 |
| --- | --- | --- |
| 1 | `focus()` 가 `tabIndex` 없는 요소에서 **예외 없이 무동작** | NAV 등재로 skip 26건이 깨어나면서 |
| 2 | 「내비에 죽은 링크가 없다」가 **모바일에서 구조적으로 항상 0** | `NAV` 에 가짜 항목을 넣어 봤더니 desktop 만 빨개졌다 |
| 3 | 「노드 상세가 존재한다」가 **`__NEXT_DATA__` 의 props JSON** 을 보고 초록 | 뮤턴트가 두 번 생존해서 |
| 4 | 같은 검사가 **`<head>` 의 `<title>`·`og:title`** 로도 초록 — **본문을 통째로 비워도 통과** | 리뷰어 실측 → 대조군 뮤턴트로 확정 |
| 5 | 주석이 「이 검사가 X 를 잡는다」고 **단언하는데 X 를 지워도 초록** | X 를 실제로 지워 봐서 |

**여기서 나온 규칙 하나 —**

> **빌드 산출물에서 문자열을 찾아 「렌더됐다」를 증명하지 마라.**
> Next 는 props 를 `__NEXT_DATA__` 에, 제목을 `<title>`·`og:title` 에 **중복해 싣는다.**
> 본문을 보려면 `</head>` 이후만 봐야 한다. 그리고 **파싱에 실패하면 빈 문자열을 반환해 빨개지게** 하라 —
> 「못 찾았으니 통과」는 이 리포가 반복해서 데인 바로 그 모양이다.

이 규칙은 아직 `CLAUDE.md` 에 **넣지 않았다**(§6 참조).

---

## 4. 알아야 할 함정 (이번에 추가)

[`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md) 에 **#35~#37** 이 추가됐다. `CLAUDE.md` 표에는 **#35 만** 올렸다(#23 과 1:1 교환, 122줄 유지).

| # | 무엇 |
| --- | --- |
| **#35** | **Git Bash 가 `node` 인자 안의 `/atlas/` 를 `C:/Program Files/Git/atlas/` 로 바꾼다** — 홑따옴표도 못 막는다. 뮤턴트가 엉뚱한 문자열로 주입되는데 **테스트는 빨개져서 「사멸」로 읽힌다.** `MSYS_NO_PATHCONV=1` 또는 스크립트 파일로 |
| #36 | 히어독은 `\b` 를 **온전히 통과시킨다.** 0x08 을 만드는 것은 `echo -e`·`printf` 다 — **히어독을 피해 `printf` 로 우회하는 것이 오히려 원인이다**(4경로 `od -c` 실측) |
| #37 | 파일 원복이 `autocrlf` 로 **LF→CRLF 를 만든다.** 이후 `\n` 앵커 편집이 조용히 어긋난다. 바이트 백업(`cp`) 후 `cp` 복원이 안전하다 |

### 지금 당장 걸릴 것 하나

**`check-baseline:update` 를 돌린 직후라 `out/` 이 「낡음」 판정이다.**
`scripts/` 가 `e2e/global-setup.ts` 의 `SOURCES` 에 들어 있기 때문이다.
다음에 `npm run e2e` 를 돌리려면 **`npm run build` 가 먼저**다 —
안 그러면 **테스트를 하나도 안 돌리고 종료 코드 1** 이 나오고, 그것이 진짜 실패와 똑같이 보인다.

---

## 5. README / 문서 갱신 필요 — **이월(고치지 않았음)**

| 항목 | 왜 이월했나 | 확인할 진실원 |
| --- | --- | --- |
| README 의 **서술 drift** | README 는 `f36692d`(2026-08-19) 이후 **65커밋** 동안 손대지 않았는데 그 사이 셸·검색·아틀라스가 통째로 들어왔다. 「기능 설명·아키텍처 서술·페이지 구성」은 여러 소스를 교차 대조해야 정확한데, 핸드오프는 **세션 끝 — 컨텍스트가 가장 얕은 시점**에 돈다. 지금 손대면 「그럴듯하지만 틀린 README」가 남는다 | `pages/` 실제 라우트 · `docs/superpowers/specs/2026-08-25-redesign-and-atlas.md` |
| `## 페이지 구성` · `## 프로젝트 구조` 절 | 위와 같음. `/atlas`·`/atlas/<id>`·`components/atlas/`·`lib/atlas/`·`lib/search/` 가 빠져 있다 | `find pages -name '*.tsx'` · `ls components lib` |

**이번에 고친 것:** `## 스크립트` 표에 **누락된 명령 11개**를 추가했다(`e2e`·`e2e:ui`·`check-pagefind{,:verify}`·`probe-search{,:verify}`·`check-counts{,:verify,:print}`·`compose{,:verify}`).
근거는 `package.json` 의 `scripts` 를 직접 읽은 것이고, 각 행에 **그 명령의 함정**을 함께 적었다.

**오탐이라 넣지 않은 것:** 수집기가 `OPENAI_API_KEY`·`TAVILY_API_KEY`·`UPSTAGE_API_KEY`·`LANGCHAIN_PROJECT` 를
「코드가 읽는데 README 에 없다」로 신고했으나, **7건 전부 `content/blog/**` 의 예제 코드**다.
이 사이트는 그 변수를 읽지 않는다. `.env.example` 도 만들지 않았다.

---

## 6. 승인 대기 — `CLAUDE.md` 에 올릴지 결정해야 할 것 **1건**

§3 의 규칙이다. **아직 쓰지 않았다.** `CLAUDE.md` 는 매 세션 로드되므로 **누적되면서 비용을 내는 유일한 파일**이고,
상한 200줄 · 「한 줄을 더하면 한 줄을 뺀다」 규칙이 있다(현재 **122줄**).

```diff
 ## 검사 — 0 을 믿기 전에 계수기가 살아 있음을 먼저 증명한다
+
+**산출물에서 문자열을 찾아 「렌더됐다」를 증명하지 마라.** Next 는 props 를 `__NEXT_DATA__` 에,
+제목을 `<title>`·`og:title` 에 중복해 싣는다 — 본문을 통째로 비워도 그 검사는 초록이다.
+`</head>` 이후만 보고, **파싱 실패 시 빈 문자열을 반환해 빨개지게** 하라.
```

**판단 근거:** 조건 3개를 모두 만족한다 — ① 몰라서 수정 라운드 3회를 치렀다 ② 코드를 읽어서는 알 수 없다
(Next 의 직렬화 동작이다) ③ 재발한다(이 리포는 정적 export 라 「산출물에 X 가 있는가」 검사를 계속 쓴다).
**넣는다면 한 줄을 빼야 한다.**

---

## 7. 상태

| 항목 | 값 |
| --- | --- |
| 브랜치 | `feat/site-renewal` → `main` 병합 |
| 미커밋 변경 | **없음** (핸드오프 문서 커밋 후 기준) |
| stash | 없음 |
| `npx tsc --noEmit` · `npm run lint` | **0 · 0** |
| `npm test` | **354** (15파일) |
| 훅 범위 `vitest run tests/blog tests/atlas` | **200** (11파일) |
| `npm run e2e` | **74 passed · 0 failed · 0 skipped** |
| `check-pagefind` · `check-counts` · `probe-search` | ✔ 405 · ✅ 156편 · ✔ 8/8 |
| `check-baseline` | **0** (이번 배치에서 1회 갱신) |
| GC-11 · GC-12 | **0 · 0** |

**`main` 은 프리뷰 없는 프로덕션이다. 배치 마감마다 푸시 여부를 사용자에게 물어라.**

---

## 8. 이번 세션의 작업 원장 (gitignore)

`.superpowers/sdd/2026-08-26-atlas-and-search/progress.md` 에 이번 세션의 **판단 22건**이
근거·비용과 함께 남아 있다. 그중 **2건은 컨트롤러가 틀렸고 실측으로 뒤집힌 것**이라
같은 착각을 반복하지 않으려면 읽을 값어치가 있다(취소선으로 남겨 뒀다).

**`.superpowers/` 는 gitignore 라 `git clean -fdx` 로 사라진다.** 남길 것이 있으면 옮겨라.
