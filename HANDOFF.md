# 인수인계 (HANDOFF)

> 갱신: 2026-08-19 · 브랜치 `main` · 이번 배치 커밋 **41개**
> **발행본 156편 · sitemap 232 URL · 카테고리 6개 발행 (12개 등록)**
>
> 이번 배치의 본체는 `java-backend/06-CI-CD`(83,252 B)를 **7편 169,345 B로 분할 발행**한
> 것이다. 그런데 남길 값어치가 있는 산출물은 글이 아니라 **예산이 새는 두 경로를 처음 센 것**이다 —
> 두 축 리뷰의 처방은 서로를 모른 채 합산되고, 배치 마감의 인바운드 링크는 어느 편의 예산에도 없다.
> 그 둘 때문에 편3·편4가 밴드를 넘었다. 상세는 설계서 **§10-2**.

---

## 1. 이번 배치에서 한 일

| # | 무엇 | 규모 |
| ---: | --- | --- |
| 1 | 원본 06 분할 설계 (§1~§9, 최종 234 KB) | 커밋 다수 |
| 2 | `backend-engineering` CI/CD 배치 발행 | **7편** |
| 3 | 편별 두 축 리뷰 + 반영 (편1~편7) | 커밋 다수 |
| 4 | 검사기 넷 신설 — 카테고리 고립 · 앵커 실존 · 발행본 수 · `compose.mjs` 도식/코드 분리 | 커밋 3 |
| 5 | 배치 완료 회고 (설계서 §10-0 ~ §10-5) | 커밋 1 |

| 편 | slug | 원본 B | 발행 B | 비율 | 커밋 |
| :---: | --- | ---: | ---: | ---: | --- |
| 1 | `cicd-pipeline-fundamentals` (`role: "map"`) | 7,175 | 20,867 | 2.91 | `976fca3` |
| 2 | `version-control-as-cicd-premise` | 11,424 | 22,655 | 1.98 | `38aee11` |
| 3 | `branching-strategy-and-containers` | 10,262 | 21,505 | 2.10 | `92fe3c0` |
| 4 | `github-actions-pipeline` | 11,418 | 22,040 | 1.93 | `84dd06c` |
| 5 | `jenkins-pipeline-and-agents` | 11,658 | 25,046 | 2.15 | `445ec2a` |
| 6 | `zero-downtime-and-quality-gates` | 12,754 | 28,040 | 2.20 | `6ee868b` |
| 7 | `questions-that-come-back` (Q&A) | 15,916 | 29,192 | 1.83 | `e2f5565` |
| | **합계** | **80,607** | **169,345** | **2.101** | |

**검증(전부 단독 실행)**: `check-forbidden:verify` 15/15 · `check-forbidden` HARD 0(158파일) ·
`vitest` 54/54 · `tsc` 0 · `build` 0(sitemap 222→232) ·
`check-forbidden:built` HARD 0(456파일) · `check-baseline` 14개 불변 ·
`check-counts:verify` 14/14 · `check-counts` 일치 ·
`dup-scan:verify` 7/7 · `dup-scan` 배치 7편 **42건**(전부 카테고리 내부) · CRLF 0.

⚠️ **가설 판정** (설계서 §10-0): C-1 본편 비율 발행순 상승 **지지**(2.101 ≥ 2.06) ·
C-2 Q&A 항목당 B **지지**(503 ≥ 496, 2회 재현) · C-3 지도편 용어 비율 **검정 불가** ·
C-4 절 단위 성분 **미검정**.

---

## 2. ⚠️⚠️ 최상위 — **다음 세션이 가장 먼저 할 일: 앞 배치 설계서의 `§10`을 읽어라**

**이번 배치는 이 지시를 따랐고 값을 했다.** 앞 배치 §10이 「Q&A편은 비율이 아니라 항목 수」로
갈아탔다고 적었고, 그대로 썼더니 편7이 **밴드 안 여유 8 B**로 착지했다. 비율로 잡았다면
목표는 1.76으로 실측 최소(1.41)와 최대(2.18) 사이 아무 데나 찍는 값이었다.

⇒ **브리프를 쓰기 전에 앞 배치 설계서의 `§10`(배치 완료 기록)을 통째로 읽어라.**
표만 읽으면 그 표가 왜 틀렸는지가 적힌 문단을 건너뛴다. **이번 §10은 `§10-5`에 「가져갈 것」
10줄을 표로 뽑아 뒀다** — 거기부터 읽고, 각 줄이 가리키는 절로 들어가라.

### 물려받은 공식 (2회 재현 — 이제 상수가 아니라 방법이다)

```
Q&A편 목표 B = (선택지 + 실패 모드 + 문답) 항목 수 × 항목당 B + 고정 장치
```

| | 항목 수 | 실측 B | 항목당 B |
| --- | ---: | ---: | ---: |
| `websocket-realtime-qna` | 46 | 21,399 | **465** |
| `spring-batch-qna` | 45 | 20,441 | **454** |
| **`questions-that-come-back`** | **58** | **29,192** | **503** |

⚠️ **항목당 B는 상수가 아니다 — 원본 크기로 커진다**(가설 C-2, 2회 지지). 454~503 사이에서
움직이므로 **앞 배치 값을 그대로 쓰지 말고 원본 Q&A 절 크기로 보간하라.**

본편은 **값이 아니라 ±10% 밴드**를 물려줘라. 배치별 본편 비율은 1.40 → 1.93~2.06 →
2.13~2.18 → 1.93~2.20으로 **통째로 움직인다.**

## 2-b. 🔴 **예산이 새는 두 경로 — 이번 배치가 처음 셌다**

목표 B는 「집필 초안」에만 걸려 있는데 실제 바이트는 그 뒤로 두 번 더 들어온다.
**둘 다 계획서 어디에도 줄이 없었고, 그 때문에 편3·편4가 밴드를 넘었다.**

| 누출 | 무슨 일 | 크기 | 대응 |
| --- | --- | ---: | --- |
| ① **두 축 리뷰가 서로를 모른다** | 각 리뷰어가 자기 축 순증만 센다. 편6에서 합산 1,334 B가 나 상한을 587 B 넘겼다 | 편당 수백 B | 브리프에 「처방마다 ±B」·「순증에는 뺄 자리 동반」·「**내 축 순증 합계를 첫머리에**」 |
| ② **마감 인바운드 링크** | 마지막 편이 나온 뒤 앞 편들에 링크를 건다. 어느 편의 예산에도 없다 | **+55 B/편** (지도편 +114) | 본편 예산에 **60 B/편**(지도편 120)을 미리 넣어라 |

①의 대응은 **즉시 효과가 났다** — 편7이 여유 8 B로 착지했다. 다만 **13 B가 여전히 어긋났다**:
리뷰어가 처방 표 **밖**에 적은 「~하면 함께 고쳐야 한다」는 **조건부 처방**은 합계에 안 잡힌다.
⇒ 브리프에 한 줄 더 넣어라 — **「조건부 처방도 표 안에. 조건이 성립할 때의 ±B를 적어라」.**

### 📌 이전 §2(「`dup-scan`을 한 편씩 돌려라」)는 해소됐다

스캐너가 고쳐져 **각 대상을 자기 자신만 뺀 전체와 대조**한다. 배치를 통째로 넘겨도 안전하고,
자기 검사 ⑤·⑥이 그 동작을 붙잡아 둔다. 다만 **읽는 법**에 새 주의가 생겼다 — §7-b.

---
---

## 3. ⚠️⚠️ **pre-commit 훅은 인덱스가 아니라 작업트리를 본다**

훅의 `vitest run tests/blog`는 `readPosts()`로 `content/blog`를 **파일시스템에서** 읽는다.
스테이지에 올린 것만 보지 않는다.

⇒ **여러 편을 먼저 다 써 두고 하나씩 커밋하면 각 커밋은 통과하는데, 그 커밋을 체크아웃하면
링크가 깨진다.** 이번 배치는 한 편씩 쓰고 바로 커밋해 문제가 되지 않았다.
**다음 배치는 편수가 많아 그렇게 하기 쉽다.**

임시 대응: **편별로 쓰고 즉시 커밋**한다. 근본 대응(훅이 `git stash -k`로 작업트리를 인덱스
상태로 맞춘 뒤 검사)은 훅을 느리게 하고 실패 시 복구가 까다로워진다 — 판단이 필요하다.

---

## 4. ⚠️⚠️ **`git commit -m`이 다른 에이전트의 스테이지를 쓸어 담는다**

`git add`를 **명시 경로로만** 해도 `git commit -m "…"`은 **인덱스 전체**를 커밋한다.
이번에 편1 첫 커밋에 다른 에이전트의 `tests/blog/content/links.test.ts` 수정이 함께 들어갔고,
`git reset --soft HEAD~1` 후 아래 형태로 원복했다.

```bash
git commit -m "메시지" -- <경로1> <경로2>
```

| 방식 | 남의 스테이지 |
| --- | :---: |
| `git add .` → `git commit -m` | 쓸어 담김 |
| `git add <경로>` → `git commit -m` | **쓸어 담김** |
| `git add <경로>` → `git commit -m "…" -- <경로>` | 안전 |

⇒ **병렬 에이전트 브리프에 「`git add` 명시 경로만」으로는 부족하다.** 커밋 **형태**를 지정하라.
금지한 것은 `add`인데 새는 곳은 `commit`이었다.

---

## 4-b. ⚠️⚠️ **`GC-6` 기준선은 빌드 환경에 종속된다 — CI에서 뺐다**

**첫 CI 실행(run `32103823116`)이 `GC-6 baseline`에서 실패했다.** Windows 로컬에서 만든 기준선을
ubuntu CI에서 검사하니 **Next.js가 생성한 13개 파일이 전부 「변경」**으로 잡혔다.
`public/`에서 그대로 복사되는 `notion/index.html` **하나만 일치**했다 —
즉 `buildId` 마스킹 문제가 아니라 **산출물 자체가 환경에 따라 다르다**(청크 해시 등).

로컬 8단계는 전부 EXIT=0이었다. **로컬 통과가 CI 통과를 증명하지 않는다.**

⇒ `deploy.yml`에서 해당 단계를 **주석 처리**했다(사유와 복구 방법을 주석에 남겼다).
**로컬에서는 그대로 유효하다** — `npm run build && npm run check-baseline`.

**되살리려면 둘 중 하나:**

| 방안 | 내용 |
| --- | --- |
| (a) | 기준선을 **CI와 같은 환경**(ubuntu-latest · node 20)에서 만들어 커밋한다 |
| (b) | **CI 안에서 두 번 빌드해 서로 비교**한다 — 기준선 파일 자체를 없앤다 |

⇒ **무엇이 다른지 먼저 실측하라.** 아티팩트를 받아 13개 파일을 diff 하면 나온다.

⚠️ 이 실패는 **예측돼 있었다.** Task 7 구현자가 우려 3으로 「로컬은 Windows, CI는 ubuntu —
경로 구분자·대소문자가 다르다」를 적었고, 컨트롤러가 **「최종 리뷰로 이월」하고 넘어갔다.**
이월한 것이 첫 푸시에서 터졌다. **다음 세션은 이월 항목을 이월로 끝내지 마라.**

---

## 5. ⚠️ **경로가 결합을 대리하지 않는다** (Critical · 해소됨)

`check-baseline`(`GC-6`)을 「비블로그 = 경로가 `blog/`로 시작하지 않는 것」으로 정의했는데,
**실제 결합은 데이터 흐름을 탄다.** `pages/index.tsx:37-38`이
`getPostSummaries().filter(p => p.featured)`를 쓰므로 featured 4편의 제목·요약이
`out/index.html`에 **JSON과 렌더 HTML 양쪽에** 인라인된다.

⇒ featured 글을 고치면 GC-6가 실패해 **배포가 막힌다.** 유일한 복구인 `--update`가 습관이 되면
스크립트 자신이 경고한 「검사가 아무것도 막지 못한다」가 실현되고, 진짜 지켜야 할 `product-lead*`
7개까지 함께 덮인다.

**해소**: `490d3ba`가 두 자리를 마스킹했다. 제3자 검증 —
featured 변경 → **EXIT=0** · featuredPosts **밖** 변조 → **EXIT=1** ·
빈 `out/`에서 `--update` → **EXIT=2** · 빈 기준선으로 검사 → **EXIT=2**.

⚠️ **다른 페이지가 블로그 데이터를 쓰기 시작하면 재발한다.** `pages/` 아래에서
`getPostSummaries`·`readPosts`를 부르는 곳이 늘어나는지 지켜보라.

---

## 6. ⚠️ **「링크가 살아 있다」와 「약속한 내용을 준다」는 다르다**

`CR-1.7`(링크가 실재 슬러그를 가리킬 것 · **기계가 한다**)과
금지선 45(대상 편이 약속한 내용을 실제로 줄 것 · **사람이 한다**)를 한 묶음으로 자동화하려 했으나
둘은 다른 것을 요구한다. 45의 원 사례는 **링크가 살아 있었고** 대상 편 251줄에 잇는 서술이 0건이었다.

⇒ 45를 「사람」으로 되돌렸다. 그대로 뒀으면 **링크 검사기 통과를 45 준수로 착각**하게 만들 뻔했다.
`PUBLISHING-CHECKLIST.md` §6에 있다.

---

## 7. ⚠️ **재기 쉬운 지표가 재고 싶은 것을 대리하지 않는다** — 앞 배치에서 3번 밟았다

| 무엇 | 추정 | 실측 | 원인 |
| --- | ---: | ---: | --- |
| 분량 하한 미달 | 4편 | **6편** | `du -k`(디스크 블록)로 쟀다 |
| 남은 발행 규모 | 약 200편 | **233편** | 원본 **개수**로 쟀다 (크기 37~90 KB로 제각각) |
| 지도편 | 4편 | **5편** | **inbound 0**으로 식별했다 |

`ai-agent-knowledge-map`은 51편 카테고리의 지도편인데 inbound가 **1**이라 그물에 안 걸렸다.
그 1개가 사라지면 게이트가 오탐을 내고, 그때 사람은 **게이트를 만족시키려 억지 링크를 만든다.**
`6939868`로 `role: "map"`을 부여해 해소했다.

---

## 7-b. ⚠️ **`dup-scan` 건수는 절대값으로 읽으면 안 된다 — 대조 집합이 움직인다**

| 스캔 대상 | 합계 | 편당 |
| --- | ---: | ---: |
| 04 WebSocket 5편 | 84 | 16.8 |
| 05 Spring Batch 5편 | 21 | 4.2 |
| **06 CI/CD 7편 (이번)** | **42** | **6.0** |
| 카테고리 전체 (10편 시점 → **32편 지금**) | 134 → **176** | — |

05의 21건만 보면 「중복이 있다」로 읽힌다. **04를 같은 명령으로 재기 전까지 과다로
판정할 뻔했다.** 실제로는 4분의 1이었다. 카테고리 전체가 커지는 것도 같은 이유다 —
앞 배치끼리의 중복까지 센다. **편당으로 정규화해야 배치끼리 비교된다.**

이번 42건은 **전부 카테고리 내부**다. 상위 유형은 워크플로 경로(`.github/workflows/*.yml`) ·
코드 조각(`StrictHostKeyChecking=no` · `docker system prune -f`) · 용어(`Continuous Integration` ·
`Declarative Pipeline` · `Workflow → Job → Step`)이고, 산문 반복은 **시리즈 명제 1건과
무중단 구간 정의 1건**이다. ⚠️ 유형으로 분류한 것이지 42건을 전부 열어 보지는 않았다 —
편별 마감에서 편7 4건만 전수 판정했다.

⇒ **「이번 작업이 만든 중복」을 재려면 대상을 이번 배치로 좁히고, 앞 배치를 같은 명령으로
잰 값과 나란히 놓아라.** §7의 「재기 쉬운 지표가 재고 싶은 것을 대리하지 않는다」의 4번째 사례다.

### 그리고 남은 건수를 결함 목록으로 읽지 마라

원본 05는 같은 재료를 해설 절(§8·§9)에서 한 번, 트레이드오프·Q&A 절(§12·§13)에서 더
무뚝뚝하게 한 번 적는다. 분할하면 두 조각이 다른 편으로 가고 **축자 중복으로 검출된다.**
세 번 만났고 세 번 다 유지했다 — 통일하면 원본의 서술 곡선이 뭉개진다.

⇒ 이런 원본에서는 **「본문과 Q&A가 일치할 것」과 「축자 중복이 없을 것」이 상충한다.**
`dup-scan`이 마지막 줄을 「판정하지 않았다 — 사람이 열어 판정하라」로 끝내는 이유다.

### 의미 중복은 스캐너가 못 잡는다 — 분할 설계에서 지목하라

05 배치에서 「파티셔닝」이 이 카테고리의 **세 번째 뜻**으로 등장했다(DB 파티셔닝 · 샤딩 ·
Batch Partitioning). 앞의 둘은 저장 구조를 영구히 바꾸고 그것은 실행이 끝나면 사라진다.
`dup-scan`은 **같은 단어가 다른 것을 가리키는 위험을 못 본다.** 설계서 §7이 미리 지목했고
편4가 3방향 구분표로 막았다.

**06 배치에서 같은 일이 두 낱말에 일어났다.** 「Stage」가 셋째 뜻(Git의 Index · Jenkins의
`stage('...')`)으로, 「Job」이 넷째 뜻(Batch Job · GHA Job · Jenkins Job · 등록 작업 항목)으로
들어왔다. 설계서 §7-1-b가 **카테고리 안팎의 기존 용법까지 포함한 구분표**를 편1에 실었고,
「재정의 금지」를 편별 브리프에 못 박았다. ⇒ **원본 하나에 도구가 둘 이상이면 용어 충돌을
먼저 세라.** 편이 늘어난 뒤에 발견하면 이미 여러 편을 고쳐야 한다.

### 🔴 Q&A편은 **정합성 점검 자체가 오염원**이다 — 점검 뒤에 다시 스캔하라

04 배치가 「참고한 발행본이 출처인 중복」이라는 유형에 이름을 붙였다 — 형식을 베끼려고 읽은
발행본에서 문장이 딸려 온다. 05 배치에서 **두 번째 경로**가 드러났다.

Q&A편은 본문편과 답이 어긋나면 안 되므로 **형제편을 정독해야 한다.** 그 정독이 그대로
오염원이 된다. 편5 집필자가 자기 감사로 6건을 찾았는데 상대가 형식 원본이 아니라
**편1~4**였다 — 편3이 굵게 쓴 자기 테제, 편1 목차 표의 설명 열까지 딸려 왔다.

**형식을 베끼는 것은 의식적이라 경계하는데, 정합성 확인은 「맞는지 보려고」 읽는 것이라
경계가 풀린다.**

⇒ **Q&A편 브리프에 「대조하려고 읽은 형제편도 오염원이다」를 넣고, §6 정합성 점검 뒤에
`dup-scan`을 다시 돌려라.** 점검 **전에** 돌린 결과는 무효다.

---

## 8. 다음 세션이 이어받을 것

### ▶ **다음 배치는 `java-backend` 나머지 4개 원본이다.** (01~06 완료)

| 원본 | 개수 | 바이트 | 예상 편수 |
| --- | ---: | ---: | ---: |
| **java-backend (잔여)** | **4** | **247,524** | **약 24** |
| langgraph | 12 | 510,215 | 40 |
| high-traffic | 11 | 508,162 | 39 |
| pm-harness | 8 | 401,936 | 31 |
| pm-po | 17 | 327,255 | 25 |
| bigdata | 8 | 292,763 | 23 |
| learning | 10 | 197,717 | 15 |
| recommendation | 7 | 83,778 | 7 |
| llm-search-recommend | 1 | 37,113 | 3 |
| **합계** | **78** | **2,606,463** | **약 207** |

| 잔여 원본 | B | 비고 |
| --- | ---: | --- |
| `07-멀티모듈-Kafka-알림센터` | 40,861 | 최소 |
| `08-스프링부트-인증-쿠버네티스` | 50,917 | |
| `09-DDD-헥사고날-Kafka-주문결제` | 89,728 | 잔여 최대 |
| `10-면접-커닝페이퍼` | 66,018 | ⚠️ 제목에 SOFT 금칙어 「면접」 |

⚠️ **옛 표의 java-backend 「10개 591,382 B」는 `README.md`(9,397 B)를 원본으로 세고 있었다.**
9개 원본 581,985 + README 9,397 = 591,382으로 정확히 맞는다. 위 표는 README를 뺀 값이다.
**다른 카테고리 행은 다시 세지 않았다** — 같은 오차가 있을 수 있다.

예상 편수 24는 이번 배치 실측 비율 **2.101**과 편당 평균 **21,600 B**로 환산한 값이다
(247,524 × 2.101 ÷ 21,600 ≈ 24.1). 다른 행은 옛 추정 그대로라 방법이 섞여 있다.


⚠️ **비율을 값으로 물려주지 마라 — 배치마다 움직인다.**

| 배치 | 본편 비율 | Q&A편 비율 | Q&A 항목당 B |
| --- | --- | ---: | ---: |
| 03 Redis | 1.40 (표 밀도가 높은 원본) | 1.26 | — |
| 04 WebSocket | 1.93 ~ 2.06 | 1.49 | 465 |
| 05 Spring Batch | 2.13 ~ 2.18 | 1.63 | 454 |
| **06 CI/CD** | **1.93 ~ 2.20** | **1.83** | **503** |

네 배치가 전부 다르다. 이번은 **폭 자체가 벌어졌다**(0.27) — 편4가 1.93, 편6이 2.20이다.
⇒ **첫 편을 쓴 뒤 재측정하고, 나머지 편에는 값이 아니라 ±10% 밴드를 줘라.**
⇒ **Q&A편은 비율로 잡지 마라.** `항목 수 × 항목당 B + 고정 장치` (§2).
⇒ **밴드에 마감 인바운드 링크 몫 60 B/편을 미리 넣어라** (§2-b).

### 편당 소요 실측 — 7.0분 *(앞 배치 03 Redis 값. 이번 배치는 재지 않았다)*

| | 값 |
| --- | --- |
| 총 소요 | 28분 4초 |
| 편수 | 4 |
| **편당** | **7.0분** |
| 대조: 08-07~08-08의 44.5편/일 | 8시간 기준 10.8분/편 |

포함 범위: 원본·요구사항·게이트 코드 읽기, 분할 설계서 작성(314줄), 4편 집필, 게이트 차단 1회와
재설계, 링크 편집 6곳, 검증 7종 2회, 문서 갱신.
⚠️ **그대로 외삽하지 마라** — 이번 배치는 설계서가 666줄이었고 집필자가 압축을 13회 돌았다.

### ⚠️ 시리즈 지도편은 「완성해서 커밋」할 수 없다 — 규칙 둘이 서로를 막는다

`CR-4.3`은 시리즈 첫 글에 **전체 목차**를 요구하고, 링크 검사는 **대상이 실재할 것**을 요구한다.
편별 커밋을 유지하면 지도편이 항상 먼저 나오므로 목차 링크가 전부 죽는다.

**해소법(3배치 연속 사용)**: 지도편을 **링크 없이** 먼저 내고, 이후 각 편 커밋에 **그 편의 목차
링크 한 줄을 함께** 넣는다. 마지막 편의 목차 링크를 빠뜨리면 그 편이 고립으로 훅에 막힌다 —
Q&A편은 **지도편 목차가 유일한 inbound**이기 때문이다.

### 🔶 카테고리 고립 — **나가는 길은 뚫렸다. 들어오는 길은 그대로다**

| `backend-engineering` | 03 Redis 직후 | 05 직후 | **지금** |
| --- | ---: | ---: | ---: |
| 편수 | 4 | 25 | **32** |
| **나가는** 카테고리 간 링크 | 0 | **0** | **4** ✅ |
| **들어오는** | 1 | 1 | **1** 🔴 |

이번 배치가 나가는 링크를 0 → 4로 만들었다(목표 3). 편1이 3건(`ai-transformation/sdlc-human-gates` ·
`ai-agent/agent-vs-workflow` · `search-engineering/elasticsearch-operations`), 편6이 1건
(`agentic-coding/agentic-coding-qna-governance`)이다.

🔴 **그런데 들어오는 길은 1건 그대로고 편수만 25 → 32로 늘었다.** 유일한 진입로는 여전히
`ai-agent/ai-agent-qna-operations.md` 하나다. 결함의 **종류가 바뀐 것**이지 없어지지 않았다 —
전에는 양방향으로 죽어 있었고 지금은 **바깥으로 말하는데 아무도 찾아오지 않는다.**

⚠️ **검사기가 이것을 잡지 않는다.** 이번에 신설한 `links.test.ts`의 카테고리 고립 검사는
「한 방향이라도 **0**인가」를 본다. 비율 임계를 안 쓴 것은 의도된 설계다(배치마다 분모가 달라져
새 글마다 깨진다). **1은 0이 아니므로 초록이다. 고립의 「정도」에는 게이트가 없다.**

⇒ **다음 배치는 `backend-engineering`으로 들어오는 링크를 만들어라.** `high-traffic`을 발행하면
캐시·부하 주제로 자연스럽게 걸리고, `rag`·`ai-agent`에서 DB·배치·실시간으로 거는 것도 가능하다.
재현 스크립트는 설계서 **§10-1**에 있다.

### 확정된 결정 (다시 묻지 마라)

| 항목 | 결정 |
| --- | --- |
| 원본 | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` **읽기 전용** |
| 푸시 | **사용자가 명시적으로 요청할 때만** |
| 커밋 메시지 | **한글** |
| 발행 제외 | **`pangyo-it` 6개만** (2026-08-18 사용자 판정). `bigdata` AWS 구성도·`learning` 커리어 로드맵은 익명화·탈개인화해 **발행한다** |
| 사명 표기 | 익명화 — 「OTT 서비스」·「IPTV 서비스」. 단 제품명은 유지. **PG/결제 도메인은 익명화 대상이 아니다** |
| 금칙어 목록 | **`scripts/check-forbidden.mjs`가 정본.** 문서에 복사하지 마라 |
| self-test 개수 | **문서에 적지 않는다.** 코드가 세고 명령이 출력한다 |
| 태그 | `content/blog/tags.ts` 통제 어휘 안에서만. 어휘 확장은 **별도 커밋** |
| 분량 | 목표 15~25 KB · 하한 13.3 KB(SOFT) · **상한 29.4 KB를 넘어도 된다**(금지선 13) |

---

## 9. 게이트 사용법

```bash
# content/blog 를 고치면 pre-commit 훅이 자동으로 돈다 (설정 불필요)
#   1/3 금칙어 self-test -> 2/3 콘텐츠 불변식 -> 3/3 금칙어 스캔
# 훅이 막으면 우회하지 말고 고쳐라. --no-verify 금지.

# 발행 전 수동 검증 — 종료 코드를 볼 명령은 단독 실행한다
npm run check-forbidden:verify   # 증명 먼저
npm run check-forbidden          # HARD 0 이어야 한다
npm run dup-scan:verify          # 증명 먼저
npm run dup-scan -- --category <slug>   # 배치를 통째로 넘겨도 안전하다 (§2 📌)
npx vitest run                   # [SOFT] 분량 경고가 stderr 로 나온다
npx tsc --noEmit                 # Vitest 는 타입을 검사하지 않는다

# 빌드 뒤
npm run build
npm run check-forbidden:built    # 소스가 깨끗해도 템플릿이 넣은 것은 여기서만 잡힌다
npm run check-baseline           # 비블로그 산출물 14개 불변

# 배치 마감 — README·CHANGELOG가 낡았는지 기계가 판정한다
npm run check-counts:verify      # 증명 먼저
npm run check-counts             # README 3자리 + CHANGELOG 최신 발행 기록
```

⚠️ **파이프 뒤의 `$?`는 마지막 명령의 것이다.** 컨트롤러가 실제로 밟아
「검출됐는데 EXIT=0」으로 오판할 뻔했다.

⚠️ **`dup-scan`은 자문 도구라 발견이 있어도 EXIT 0이다.** 종료 코드를 합격 기준으로 삼지 마라 —
verifier 브리프에 「발견 0」을 기준으로 적었다가 어긋남으로 보고받았다. **건수를 사람이 판정한다.**

---

## 10. 소스 위치

| 무엇 | 경로 |
| --- | --- |
| 원본 (**읽기 전용**) | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\` |
| 게이트 설계 | `docs/superpowers/specs/2026-08-18-publishing-gate-redesign.md` |
| 게이트 구현 계획 | `docs/superpowers/plans/2026-08-18-publishing-gate-implementation.md` |
| **규칙 전수 분류표** | `docs/superpowers/reports/2026-08-18-rule-triage.md` |
| **발행 체크리스트 (사람 판단만)** | `docs/superpowers/PUBLISHING-CHECKLIST.md` |
| **이번 배치 분할 설계 (§10 회고 포함)** | `docs/superpowers/plans/2026-08-19-cicd-split-design.md` |
| 앞 배치 분할 설계 | `docs/superpowers/plans/2026-08-18-spring-batch-split-design.md` |
| 금칙어 목록 (정본) | `scripts/check-forbidden.mjs` |
| 규칙 `GC-*` | `docs/superpowers/plans/2026-08-07-tech-blog-phase1.md` |
| 규칙 `CR-*` | `docs/superpowers/specs/2026-08-07-tech-blog-requirements.md` |
| 금지선 1~58 (사장 14건은 취소선) | 위 두 설계서 §11 + `git show` 이전 HANDOFF |

---

## 11. README/문서 갱신

### ✅ 이번 배치 — 이제 **손이 아니라 검사기가** 잡는다

| 자리 | 무엇 |
| --- | --- |
| README 요약표 (9행) | 발행본 **149 → 156편** |
| README 디렉터리 트리 (109행) | 발행본 **149 → 156편** |
| README 카테고리 내역 (137행) | `backend-engineering` **25 → 32** |
| CHANGELOG | 배치 기록 1건 추가 (총 156편) |
| `PUBLISHING-CHECKLIST.md` | 낡은 「아직 없다」 3건 정정 + **앵커 실존** 검사 등재 |

🔴 **네 자리를 전부 `npm run check-counts`가 검출했다.** 손으로 세지 않았다 —
검사기가 파일 시스템에서 세어 문서와 대조하고, 어긋난 4자리를 이름으로 찍어 줬다.

### ⚠️ 다음 세션이 확인할 것

- ✅ 이전 항목 둘(「발행본 수 검사기가 없다」·「CHANGELOG가 마감 절차에 없다」)은 **해소됐다.**
  `scripts/check-counts.mjs`가 README 3자리와 CHANGELOG 최신 발행 기록을 함께 본다.
  **마감에서 `check-counts`가 통과할 때까지 배치는 안 끝난 것이다.**
- 🔴 **`backend-engineering`으로 들어오는 링크가 32편에 1건이다** (§8). 검사기는 「0인가」만
  보므로 초록이다 — 고립의 정도에는 게이트가 없다.
- ⚠️ **가설 C-4(절 단위 성분)가 미검정으로 남았다.** 다음 배치가 본편 6편에서 절별로 재면
  「원본 성분으로 비율을 정하지 않는다」가 절 단위에서 뒤집힐 수 있다 (설계서 §10-0).

---

## 12. 이 배치의 작업 방식 — 다음 세션이 참고할 것

**편 단위 순차**로 갔다. 편1을 쓰고 실측한 뒤 그 값으로 편2~편7 예산을 재환산했고, 그다음부터
편마다 「집필 전 결정 문서 → 집필 → 두 축 리뷰 → 반영 → 커밋」을 돌렸다. 앞 배치의 5기 병렬과
다른 선택인데, **편1의 실측이 나머지 전부의 예산을 바꾸기 때문**이다(실제로 8차례 재환산됐다).

| 효과가 컸던 것 | 왜 |
| --- | --- |
| **집필 전 결정 문서**를 편마다 따로 씀 (§7-6 ~ §7-10) | 브리프는 계획서에서 잘라낸 것이라 **앞 편에서 배운 것을 모른다.** 컨트롤러가 손으로 옮겨야 한다 |
| 리뷰를 **위험 축 2개**로 나눔 (원본 충실도 / 시리즈 정합) | 한 명에게 전부 맡기면 「계획대로 구현됨」으로 승인한다 |
| 리뷰어에게 **「내 축 순증 합계를 첫머리에 적어라」** | 편6에서 587 B를 넘긴 원인이 축 사이 합산이었다. 편7은 여유 8 B로 착지 |
| 검사기를 만들 때 **자기 증명을 먼저** | `check-counts`가 마감에서 4건을 즉시 잡았다 |
| 앵커 검사기를 **발행본을 실제로 깨뜨려** 증명 | 합성 fixture는 실물 구조를 안 닮아 통과해도 0건을 낼 수 있다 |

| 컨트롤러가 저지른 것 | 뿌리 |
| --- | --- |
| 두 축 처방을 다 반영해 편6이 상한을 587 B 넘겼다 | **각 리뷰어는 자기 축만 센다** (§2-b) |
| 마감 인바운드 링크가 편4를 밴드 밖으로 밀었다 | 마감 순증이 **어느 편의 예산에도 없었다** (§2-b) |
| verifier 브리프에 `dup-scan` 합격 기준을 「발견 0」으로 적었다 | **자문 도구는 발견이 있어도 EXIT 0이다** (§9) |
| 히어독으로 정규식이 든 파일을 쓰려다 조용히 실패 | 히어독이 백슬래시를 **한 겹 벗긴다.** `Write`/`Edit` 도구로 |
| 긴 히어독으로 셸을 세 번 죽였다 (집필자·리뷰어·컨트롤러) | `unexpected EOF`. **처음부터 나눠 쓰거나 `Write` 도구로** |

**한 줄 요약**: 앞 배치의 결함이 「재는 도구가 그럴듯한 값을 준다」였다면, 이번 결함은
**「센 값이 맞는데 셀 자리를 안 셌다」**는 부류다. 두 축의 순증도, 마감의 링크도 각각은
정확히 세어졌고 **합쳐서 세는 자리가 없었다.** 다음 배치는 예산표에 **집필 이후로 들어오는
순증 칸**을 미리 만들어라.
