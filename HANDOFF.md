# HANDOFF

**최종 갱신: 2026-08-29 · 알림센터 배치(원본 07 → 5편)를 마감하고 푸시했다.**

---

## 0. 지금 저장소가 어떤 상태인가 — 가장 먼저 읽을 것

| 항목 | 값 |
| --- | --- |
| `main` | `552db08` — `docs(plan): 배치를 마감한다` |
| origin | **동기화됨.** 이 세션에서 14커밋을 푸시했다 (`27ae83d..552db08`) |
| 배포 | ✅ **완료** (run `33248455204`, 2m5s). `https://withwooyong.github.io/blog/backend-engineering/kafka-notification-qna/` 가 200 이다 |
| 미커밋 변경 | 없다 |
| 발행본 | **161편** / 등록 12 카테고리 중 6개 발행 |

**푸시가 곧 배포다.** 미리보기 환경이 없으므로 `main` 에 올라간 것은 바로 공개된다.

---

## 1. 이번 세션에서 한 일 — 원본 07 배치 마감

원본 `java-backend/07-멀티모듈-Kafka-알림센터.md` 를 5편으로 나누는 작업의 **마지막 편과
배치 회고**를 끝냈다. 편1~4는 앞 세션들에서 발행했고 이번에 편5가 나갔다.

| 커밋 | 내용 |
| --- | --- |
| `b71071f` | 편5 `kafka-notification-qna` 발행 (19,602 B, 44항목) + 편1·편4에 편5 링크 + README·CHANGELOG |
| `8e61332` | `backend-engineering` 으로 **들어오는** 링크를 1 → 3 으로 (`rag`·`ai-agent` 각 1편 → 편3) |
| `552db08` | 설계서 §10 마감 · `TOOL-TRAPS` #39·#40 · `CLAUDE.md` 함정 수 37 → 40 |

### 배치 결과

| 편 | slug | 원본 B | 발행 B | 비율 |
| :---: | --- | ---: | ---: | ---: |
| 1 | `notification-center-architecture-map` | 5,496 | 19,254 | 3.50 |
| 2 | `read-write-separation-and-multimodule` | 7,081 | 15,508 | 2.19 |
| 3 | `kafka-messaging-and-delivery-guarantees` | 10,063 | 22,503 | 2.24 |
| 4 | `notification-storage-and-query-performance` | 6,292 | 14,229 | 2.26 |
| 5 | `kafka-notification-qna` | 10,045 | 19,602 | 항목당 **439.2** |
| | **합계** | **38,977** | **91,096** | **2.337** |

### 🔴 이 배치의 수확 — 비율 예측 축 하나가 떨어졌다

여섯 배치 동안 「발행 순서」와 「원본 B」가 함께 커져 와서 어느 쪽이 비율을 미는지 가를 수
없었다. 이번 원본은 **가장 늦은 배치이면서 가장 작은 원본**이라 두 축이 반대를 가리켰고,
실측은 발행 순서 쪽이었다.

- **C-1**(발행 순서) 지지 · **C-6**(원본 B, r=+0.522) **반증** ⇒ 다음 배치는 원본 B 축을 쓰지 않는다
- **C-2**(Q&A 항목당 B) 지지 — 예측 442 / 실측 439.2. 코퍼스 구간 **아래로 나간 첫 외삽**이 맞았다
- **C-3**(지도편 용어당 B) 지지 — 208.7

자세한 내용은 설계서 `docs/superpowers/plans/2026-08-29-multimodule-kafka-split-design.md`
**§10** 이 정본이다. 표만 보지 말고 문단째 읽어라.

---

## 2. 다음 세션이 할 일 — 원본 08 설계 착수

| 항목 | 값 |
| --- | --- |
| 원본 | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge\java-backend\08-스프링부트-인증-쿠버네티스.md` |
| 크기 | **50,917 B** — 코퍼스 관측 구간(46.8k~80.6k) **안**이라 외삽 문제가 없다 |
| 카테고리 | `backend-engineering` (현재 37편) |
| 앞 배치 설계서 | `docs/superpowers/plans/2026-08-29-multimodule-kafka-split-design.md` **§10-5 의 12개 규칙** |
| **착수본** | `docs/superpowers/plans/2026-08-29-auth-k8s-split-design.md` — **§2 실측까지 채워져 있다** (`6f278ea`) |

**§3 부터 이어서 쓰면 된다.** 배정 원본 49,201 B · Q&A 항목 **42** · 용어 23개가 이미 실측돼 있다.

🔴 **이번 배치의 새 변수는 「불릿」이다.** 08은 07과 성분이 정반대다 — 산문이 21.6% → 8.5%로
떨어지고 불릿이 1.6% → **18.6%** 로 올라왔다. 여섯 배치의 성분 회귀에 불릿이 유의미한 비중으로
들어온 적이 없어, 그것이 「옮길 때 늘 자리가 없는 성분」인지 산문처럼 부푸는 성분인지 모른다.
**편1 실측에서 불릿을 따로 세라.**

### 착수 절차 — 앞 배치가 확립한 순서 그대로

| # | 단계 |
| ---: | --- |
| ① | ✅ **완료** — `compose:verify` 9/9 확인 후 절별 성분 실측 |
| ② | 설계서를 쓰되 **편1을 먼저 발행하고 그 실측으로 나머지를 재환산** |
| ③ | 편마다 검증하고 **즉시 커밋** — pre-commit 훅이 작업트리를 본다 |
| ④ | `dup-scan` 은 편마다, 그리고 Q&A편 뒤에 배치 전체로 한 번 더 |
| ⑤ | 커밋은 `git commit -m "…" -- <경로>` 형태로 경로를 명시 |

### §10-5 규칙 중 설계서에 반드시 반영할 것 넷

| # | 규칙 |
| ---: | --- |
| 1 | 본편 목표 비율을 **2.20** 으로 잡아라. 2.10 이 아니다 |
| 2 | 편별 예산은 **도입·정리 3,300 B 를 상수로 먼저 빼고** 나머지에 비율을 곱하라 |
| 4 | 성분 예산을 잡을 때 **원본 표의 개수와 합칠 계획을 먼저 적어라** |
| 10 | 카테고리 간 링크 **집계 스크립트를 설계서에 박아라** — 값만 옮기면 다음 배치가 증감을 못 센다 |

⚠️ **`role` 은 `"map"` 하나만 허용된다.** `lib/blog/frontmatter.ts` 가 다른 값에 던지므로
Q&A편에는 `role` 키를 아예 적지 않는다. 07 설계서 초판이 여기서 빌드를 죽였다.

⚠️ **`date` 는 원본 작성 기준일, `updated` 가 발행일이다.** `backend-engineering` 37편이
예외 없이 그렇다.

---

## 3. 블로그 진행률 — 2026-08-29 실측

| 항목 | 값 |
| --- | --- |
| 발행본 | **161편** / 등록 12 카테고리 중 **6개만 발행** |
| 원본 저장소 | `C:\Users\aeby\vscode\yanadoo-exit\shared\knowledge` (143개 `.md`, 읽기 전용) |

### 3-1. 활성 트랙 — `java-backend` → `backend-engineering` (37편)

원본 10개 중 **7개 변환 완료**(01~07), **3개 남음**:

| 원본 | 크기 | 상태 |
| --- | ---: | --- |
| `08-스프링부트-인증-쿠버네티스.md` | 50,917 B | **다음 배치** |
| `09-DDD-헥사고날-Kafka-주문결제.md` | 89,728 B | 코퍼스 최대. 편수가 많아진다 |
| `10-면접-커닝페이퍼.md` | 66,018 B | 성격이 달라 별도 판단 필요 |

### 3-2. 카테고리 위상 — `backend-engineering`

| 축 | 배치 전 | 배치 후 |
| --- | ---: | ---: |
| 편수 | 32 | **37** |
| 나가는 카테고리 간 링크 | 5 | **11** |
| **들어오는** | **1** | **3** |

들어오는 링크는 `links.test.ts` 가 **「한 방향이라도 0인가」만** 보므로 1도 초록이다.
손 놓으면 편수가 늘수록 나빠진다 — 배치마다 산출물에 「기존 발행본 수정」을 포함하라.

### 3-3. 미착수 트랙 — 등록만 되고 0편인 카테고리 6개

| 카테고리 | 원본 디렉터리 | 원본 | 분량 | 예상 편수 |
| --- | --- | ---: | ---: | ---: |
| `high-traffic` | `high-traffic/` | 11개 | 508 KB | ~43편 |
| `product-management` | `pm-po/` | 17개 | 327 KB | ~27편 |
| `ai-product-planning` | `pm-harness/` | 8개 | 402 KB | ~34편 |
| `glossary` | `glossary/` | 2개 | 24 KB | ~2편 |
| `platform-architecture` | 미정 | — | — | — |
| `python-ml-serving` | 미정 | — | — | — |

⚠️ **예상 편수는 java-backend 트랙 하나에서 잰 비율의 외삽이다.** 규모 감각으로만 쓰고,
배치 계획은 편1을 먼저 쓰고 실측으로 재환산한다.

---

## 4. ⚠️ 롤백 맥락 — 코드에 없는 것을 「만들어야겠다」고 판단하지 마라

**2026-08-28 에 저장소가 88커밋 되돌아갔다**(`27ae83d`). 아틀라스·검색(Pagefind)·
Playwright E2E·Lighthouse 는 **있었고, 배포됐고, 사용자 판단으로 되돌린 것**이다.

| 수단 | 명령 |
| --- | --- |
| 지점 열기 | `git checkout pre-rollback-2026-08-29` — **origin 에도 있다** |
| 파일 단위 | `git show pre-rollback-2026-08-29:<경로> > <경로>` |
| 전체 복원 | `git revert 27ae83d` |

**부분 복원은 진입점을 함께 봐야 한다.** 아틀라스와 검색은 서로 독립이지만 둘 다
`components/site-header.tsx`·`pages/index.tsx` 에 진입점을 갖고 있어, 파일만 복사하면
죽은 코드가 된다.

`docs/TOOL-TRAPS.md` 는 복원본이다(현재 **40항목 / 770줄**). 머리말이 **지금 저장소에 없는
코드를 지목하는 항목**을 표로 표시한다 — 원리는 유효하되 재현 절차는 실행할 수 없다.

### 검사 체계 현황

| 항목 | 현재 |
| --- | --- |
| vitest | **5파일 / 54건** |
| E2E · Lighthouse · 검색 색인 | **없다.** `npm run e2e` 는 존재하지 않는 명령이다 |

검사 순서의 정본은 `package.json` 의 `scripts` 와 `.github/workflows/deploy.yml` 이다.

---

## 5. README/문서 갱신 필요 — 다음 세션이 판단할 것

| 항목 | 무엇이 낡았나 | 진실원 |
| --- | --- | --- |
| README 환경변수 | 검사기가 `OPENAI_API_KEY`·`TAVILY_API_KEY`·`LANGCHAIN_*` 5개를 「코드에 있는데 README 에 없다」고 보고한다. **오탐일 가능성이 높다** — 이 값들은 `content/blog/rag/` 발행본의 **코드 예시 블록**에서 나왔고 이 사이트는 서버 런타임이 없다 | `content/blog/rag/*.md` 의 코드 펜스를 열어 확인. 실제 런타임 변수라면 `.env.example` 부터 만들어야 한다 |

이번 세션에 **고치지 않았다.** 표에 행을 추가하는 자동 수정 범위를 넘어, 값의 출처를
교차 대조해야 판단할 수 있는 항목이기 때문이다.

---

## 6. 미커밋 변경

없다. 이 세션의 커밋 3개는 전부 **푸시됐다.**
