# HANDOFF

> 마지막 갱신 2026-09-02 · `pm` 배치 **시리즈 A 9/9 완결 · 시리즈 B 4/8**

## 지금 상태

발행본 **180편**이다. 편3 리뷰가 세운 「여유 600 B」를 편4에 처음 적용해 **설계값을 밑돈 첫
편**이 나왔고, 그 실측에서 편3이 세운 결론 두 개가 함께 무너졌다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 2,970줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 ✅ · B `product-management` 8 |
| 발행 완료 | **A 편1~9 + B 편1~편4 (180편)** |
| 다음 단계 | **B 편5 `commerce-member-and-catalog`** — 원본 `pm-po/02` §0·§1·§2·§3 · 7,830 B · 설계 목표 **18,565 B** · 밴드 17,470 ~ 19,660 |
| 🔴 편5 목표 | 설계값이 아니라 **17,900 안팎**으로 잡아라 — 상단 여유 600(리뷰 몫) + 링크 몫 58 + 도메인 편의 표 팽창분 |
| 푸시 · 배포 | 🔴 **편4 발행 커밋(`6b4a6d3`)이 아직 푸시되지 않았다.** 편3 리뷰 반영(`adde8f6`)까지는 `origin/main` 반영됨 |
| 🔴 남은 것 | 편4 **리뷰 두 축을 아직 돌리지 않았다** · 원본 대조·복제 검사기의 **훅·CI 편입**(§10-12-g-2) |

**설계서 §1 의 절차표가 정본이다.** 진행률은 **`§10-N` 의 실측 절 번호**로 판단하라 —
§10-13 까지 있으면 B 편4 가 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | 편3 **리뷰 두 축** — 정확성 10건 · 검사 유효성 3건 반영 | 편3 본문 · 지도편 · 설계서 **§10-12-g** |
| 2 | 🔴 **`source-overlap` 결함 3건 수정 + 뮤테이션 증명** (15/15 → **21/21**) | `scripts/source-overlap.mjs` |
| 3 | B 편4 `planning-and-product-ops` 집필·발행 (**19,008 B**) | `content/blog/product-management/planning-and-product-ops.md` |
| 4 | 초고 23,060 → **네 차례** 감축. 1차가 **−2,643** | 밴드 17,985 ~ 20,250 **안** (상단 여유 **1,242**) |
| 5 | 지도편 배정 오류 `SLA / SOP` 1건 수정 · 편4 행 링크 · 예고 라벨 정정 | `product-management-map.md` |
| 6 | 편3 말미 **다음 편 링크** 부착 → 상한 초과 → 편3에서 51 B 되돌림 | `tactics-and-goal-setting.md` (14,745) |
| 7 | 시리즈 간 링크 **A-6 → B-4** 부착 — **§6-2 4건 완료** | `deliver-and-prd.md` (20,924) |
| 8 | B 편4 실측 | 설계서 **§10-13** (7개 하위 절) · §1 절차표 ⑥ · §10-12 에 취소선 |
| 9 | README 세 자리 (179 → 180) · CHANGELOG 새 절 · 지도편 절 아카이브 이동 | `README.md` · `CHANGELOG.md` · `docs/changelog/2026-09.md` (6절) |
| 10 | 이 문서 | (다음 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 0. 🔴 `source-overlap` 이 **CRLF 파일에서 아무것도 보지 못하고 있었다** — 179편 중 32편

리뷰 검사 유효성 축이 잡았다. `toParagraphs` 의 프론트매터 정규식과 문단 분할이 **LF 전용**
이라, CRLF 파일은 `\r\n\r\n` 이 `\n{2,}` 에 걸리지 않아 **파일 전체가 한 덩어리**가 되고,
그 덩어리에 `-->` 가 하나라도 있으면 통째로 도식 버킷으로 가 **산문 문단이 0개**가 된다.

| 같은 내용, 줄바꿈만 다름 | 문단 | ① | ② |
| --- | ---: | ---: | ---: |
| LF | 54개 | 2건 (최장 42자) | 1건 (33자) |
| **CRLF** | **0개** | **0건** | **0건** |

**42자 축자 복사가 통째로 사라진다.** 자기 검사 13개가 전부 LF 리터럴이라 **13/13 PASS 가
줄바꿈에 대해 아무 말도 하지 않았다.** `dup-scan` 은 같은 자리에서 `\r?\n` 을 써 정상
동작했다 — **두 검사기가 갈렸는데 라벨은 「같은 정규화」라고 적혀 있었다.**

고쳤다(§10-12-g-1). 줄바꿈을 `\r?\n` 으로, ③의 라벨 대조에 ②와 같은 정규화를 태우고, ③
출력에 발행본 라벨 수를 더했다. **15/15 → 21/21** 이며 **뮤테이션으로 증명했다** — 줄바꿈을
되돌리면 18/21, 라벨 정규화를 되돌리면 19/21 이다.

⚠️ **이번 편은 CR=0 이라 판정이 바뀌지 않았다.** 그러나 **CRLF 인 32편의 과거 「0건」은
결론이 아니라 미검출**이었을 수 있다. 그 편들을 다시 볼 일이 생기면 이 사실을 먼저 떠올려라.

### 0-b. ✅ 「여유 600 B」가 편4에서 작동했다 — 그리고 **미부착 링크는 그와 별개다**

| 편 | 커밋 시점 여유 | 리뷰가 요구한 증가 | 결과 |
| --- | ---: | ---: | --- |
| 편2 | — | **+243** | 산문을 −212 깎아 맞췄다 |
| 편3 | 380 | **+551** | 🔴 상한 **+171 초과** → 산문을 되돌려 14,742 |
| **편4** | **1,242** | (리뷰 미실시) | 목표를 밴드 중앙 아래로 잡아 **설계값을 −0.6% 밑돌았다** |

⇒ **여유는 밴드 상단에서 최소 600 B 를 유지한다.**

🔴 **그런데 편3에서 다른 방향으로 터졌다.** 미뤄 둔 「다음 편」 링크를 편4 발행 때 붙이자
편3이 **14,742 → 14,796** 으로 상한을 **44 B 넘겼다.** 링크 몫 55 B 는 편3 예산에 이미 들어
있었지만 **파일에는 자리가 없었다.**

⇒ 🔴 **예산에 넣어 둔 것과 파일에 남겨 둔 자리는 다르다.** 다음 편 링크를 나중에 붙일 편은
**여유 600(리뷰 몫) + 링크 몫**을 함께 남겨라(§10-13-d). 편4도 편5 링크가 미부착이므로 실질
여유는 1,242 가 아니라 **1,187** 이다.

### 1. 🔴 반려선이 가리킨 방향과 **실제로 어긋난 방향이 반대였다**

§3-2-a 는 편3을 「밴드 하단이 하한을 113 B 밑도니 살이 모자라면 반려」로 관리했는데, 초고는
**17,945 B** 로 상한을 **21.6%** 넘겼다. 배치 최대 이탈이다.

| 지정한 위험 | 실제 위험 |
| --- | --- |
| 원본이 가장 작으니 하한에 걸릴 것이다 | 배율이 가장 크게 튀어 상한을 넘겼다 |

**예고는 예산표 안에 있었다** — §3-2 겉보기 비율에서 편3이 **2.499** 로 배치 최대였다.
⇒ **밴드 하단이 하한을 밑도는 편에도 상한 점검을 그대로 하라.** 반려선은 다른 쪽을 면제해
주지 않는다.

### 2. 🔴 「면접 활용」 블록에서 온 문장은 **검사기에 걸리지 않는다**

§11-3 이 금지한 배정 밖 블록의 내용이 초고에 둘 들어와 있었고, **`source-overlap` 은 원본
전체를 대조하면서도 0건을 냈다.**

걸릴 수가 없다 — **「익명화해서 살린다」는 정의상 낱말을 바꾼다는 뜻이고, 축자 대조는 낱말이
같을 때만 반응한다.** 검사기의 0건은 「옮기지 않았다」가 아니라 **「그대로 옮기지는 않았다」**
를 뜻한다.

⇒ **발행 전에 배정 밖 블록의 주제 목록을 손에 들고 본문을 훑어라.** 기계가 못 하는 일이므로
`PUBLISHING-CHECKLIST.md` 쪽 항목이다(§10-12-c).

### 3. 🔴 초고가 10% 넘게 넘쳤으면 **문단이 아니라 절 목록을 먼저 보라**

무엇을 손댔느냐로 회차당 감축이 한 자릿수 배씩 갈린다. 편4에서 층이 하나 더 드러났다.

| 무엇을 손대나 | 회차당 감축 | 무엇이 빠지나 |
| --- | ---: | --- |
| 문장 다듬기 | 100~230 | 수식어와 군더더기 |
| 문단·표 셀·항목 제거 | 343~687 | 중복 서술 한 덩어리 |
| 🔴 **절 경계 옮기기** | **2,643** | **도입 문장 한 벌 + 절 사이 이음말 + 제목 줄** |

편4 초고는 23,060(상한 +13.9%)이었는데 **절 14개를 12개로 합친 한 회차가 2,643 B 를 냈다.**
이어지는 두 절을 합치면 그 절이 자기를 소개하던 문장 전체가 사라진다(§10-13-a).

### 4. 🔴 「고정 비용이 상수에 수렴한다」를 **취소했다**

| 편 | 고정 비용 | 상수 2,455 대비 | 상수 기준 k | 실측 기준 k | 두 k 의 차 |
| :---: | ---: | ---: | ---: | ---: | ---: |
| 2 | 3,013 | +558 | 2.163 | 2.080 | 0.083 |
| 3 | 2,479 | +24 | 2.188 | 2.184 | **0.004** |
| **4** | **3,346** | 🔴 **+891** | **2.030** | **1.921** | **0.109** |

§10-12 가 편3의 0.004 를 근거로 「상수 2,455 가 첫 편 이후로는 정확하다」고 적었는데 편4가
반례다. 세 표본이 흩어져 있어 수렴이 아니다 — **두 k 가 붙는 것은 고정 비용이 우연히 상수와
가까울 때 자동으로 일어나는 일이지 경향의 증거가 아니었다.**

⇒ **예산 산식(상수 2,455 · k 2.05)은 그대로 쓴다.** 세 편의 상수 기준 k 가 모두 밴드
1.91~2.19 안이다. 취소하는 것은 산식이 아니라 **수렴한다는 문장**이다(§10-13-c).

### 5. 🔴 표 배율을 `원본 표 B × 배율` 로 예측하지 마라

| 편 | 원본 표 B | 발행 표 B | 배율 | §3-2-b 예상 | 겹치는가 |
| :---: | ---: | ---: | ---: | ---: | :---: |
| 3 | 2,647 | 4,150 | 1.568 | 1.30 | 아니오 |
| **4** | **1,811** | **4,255** | 🔴 **2.350** | **1.05** | 예 |

**겹치기로 계획해 예상을 가장 낮게 잡은 편에서 배율이 가장 높게 나왔다.** 원인은 겹침 여부가
아니라 **분모**다 — 원본이 순서·목록을 산문으로 적어 둘수록 그것을 표로 편 몫이 전부 이 배율에
얹힌다. ⇒ **편5~8 은 배정 절을 열어 「원본이 표로 쓰지 않은 순서·목록의 개수」를 먼저 센다**(§10-13-b).

### 6. 도식 — 편4는 **원본에 도식이 없는 첫 편**이었다

편2 1.435 · 편3 1.421 로 배율 표본이 둘 모였지만, 편4 배정 절에는 도식이 하나도 없어 배율을
낼 분모가 없었다. 처방 ②(구조는 유지하고 라벨은 다시 쓴다)도 **유지할 구조가 없으면 적용되지
않는다.** 원본이 산문으로 적어 둔 순서 둘을 새로 그렸고(583 B) **배율 표본에는 넣지 않았다.**

⇒ **편5~8 은 배정 시점에 원본 도식 수를 먼저 세라.** 0 이면 편당 평균으로 예산을 잡고, 그렇게
그린 도식은 배율 계산에서 뺀다(§10-13-e).

### 7. 🔴 지도편의 배정 열이 **두 편 연속 틀렸다**

| 편 | 오류 | 무엇이었나 |
| :---: | ---: | --- |
| 3 | 3건 | 지도편이 편3으로 약속한 용어 셋이 편3 배정 원본에 없었다 |
| 4 | 1건 | `SLA / SOP` 가 편4·편5 로 배정됐는데 **두 배정 원본 어디에도 없다** |

배정 열은 원본을 열어 확인하지 않고 주제만 보고 채워진 것으로 보인다. ⇒ **편5~8 착수 시
`LC_ALL=C grep -c "<용어>" <배정 원본>.md` 를 먼저 돌려라**(§10-13-f).

### 8. 남은 링크 작업

| 작업 | 언제 |
| --- | --- |
| ✅ **A-6 → B-4** (§6-2 마지막 1건) | 편4 발행 때 부착 완료. **§6-2 의 4건이 모두 찼다** |
| 지도편 구성 표 형제편 링크 **4개** (편5~8) | 편마다 그 행에 붙이고 커밋 19-a 에서 전수 확인 |
| 편4 말미 「다음 편」 링크 | **편5 발행 때 부착.** 🔴 편3에서 이것 때문에 상한을 넘겼다 — 편4 여유에서 **55 B 를 미리 뺀 1,187** 로 계산하라 |
| `agent-definition-by-department` → **B-4** · `thirty-agent-catalog` → **B-3** | 커밋 20. 지금은 B-1 지도로 **임시로 걸려 있다** — 다시 걸지 말고 대상만 바꾼다 |

---

## 시리즈 B 사양 요약

| 항목 | 값 |
| --- | --- |
| 카테고리 | `product-management` (편1~편4 발행) |
| 원본 | `pm-po/01` · `02` · `04` — 배정 **58,636 B** |
| 편수 | 8편 (편1 지도 ✅ · 편2 ✅ · 편3 ✅ · 편4 ✅ · 편5~8) |
| 예산 | k = **2.05** · 밴드 1.91 ~ 2.19 (§3-2). **산식은 그대로 쓴다**(§10-13-c) |
| frontmatter | `series: "product-management-domains"` · `seriesOrder` 1~8 (§7-4) |
| 🔴 낱말 | 「기획」은 A 와 뜻이 다르다. 편1이 갈라 뒀고 편2~편4 가 그 용법을 따랐다 |
| 🔴 편5 | 원본 `02` §0~§3 · 7,830 B · 설계 목표 18,565 · 밴드 17,470 ~ 19,660. §3-2-b 는 「겹치지 않는다 ≈1.35」로 잡았으나 **편4에서 예상이 반대로 빗나갔다** — 배정 절의 산문 목록 수를 먼저 세라 |
| 🔴 편7 | 법과 시행령을 섞지 마라. 법령만 한 표의 **두 열**로 겹친다 (§3-2-b) |
| 🔴 편8 | 확정 제도와 논의 중인 쟁점을 같은 층으로 쓰지 마라 (§7-2) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- 🔴 **`dup-scan` · `source-overlap` · `compose` 가 훅에도 CI에도 없다.** 대상 지정이 필요한
  검사(`--category`, 원본 경로)라 인자 결정 규칙부터 설계해야 하므로 **다음 배치의 안건**으로
  넘겼다(§10-12-g-2). 그때까지 이 세 검사는 **사람의 기억에 걸려 있다.**
- **`source-overlap` 은 축자 복사를 찾아도 종료 코드 0 을 낸다.** 고치지 않기로 했다 —
  용어 원어 열처럼 정당한 겹침이 늘 남으므로 「사람이 열어 판정하라」가 설계다.
- 🔴 **밴드와 하한은 여전히 기계 검사 밖에 있다.** `npm test` 는 하한 미달을 `stderr` 경고로만
  내고 상한은 검사기가 없다. **「테스트 초록」을 분량 근거로 쓰지 마라** — `compose` 출력과
  `[SOFT]` 목록을 눈으로 읽는다. 현재 하한 미달 **5편**은 전부 이번 배치 밖이다.
- **겹침 수정이 분량을 줄인 사례가 처음 나왔다**(−49 B). 앞 세 사례는 +22 · +73 · +232 였다.
  🔴 **그래도 여유는 「+232 를 담을 만큼」으로 잡는 규칙을 유지한다** — 줄어드는 쪽은 손해를
  내지 않는다(§10-12-e).
- 🔴 **표 배율이 설계 예상을 두 편 연속 넘었다** — 편3 **1.568**(예상 1.30) · 편4 **2.350**
  (예상 1.05). 위 5번을 보라. §3-2-b 의 예상값은 편5~8 에서도 참고만 하고 근거로 쓰지 마라.
- **표 헤더가 원본과 통째로 같아지는 자리가 있다.** 표를 만들 때 헤더도 대조 대상으로 본다
  (§10-11-f).
- **`user-research` · `career` 태그가 싱글톤으로 남았다.** **글에 없는 내용으로 태그를 맞추지
  마라**(§11-8).
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 이번 세션에 지도편 절을 `docs/changelog/2026-09.md`
  로 옮겨 아카이브가 **6절**이 됐다. 넘으면 여기로 옮기고 **상단 인덱스 표의 절 수와 날짜
  목록을 함께** 갱신한다 — `check-counts` 는 이 표를 보지 않는다.
- ✅ **`check-counts` 를 발행 전 검사에 넣은 뒤로 배포 실패가 없다.** 편을 발행했으면 README
  **세 자리**와 CHANGELOG **최신 절**, 네 자리를 같은 커밋에 넣는다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **축자 대조는 「바꿔 쓴 위반」을 못 잡는다** | 배정 밖 블록에서 온 문장 2건이 `source-overlap` 0건을 통과했다 | 배정 밖 블록의 **주제 목록**으로 사람이 훑는다 |
| 🔴 **행 수 1:1 대조가 소실을 놓친다** | 여섯 표 모두 행 수는 맞았는데, 목록이 산문으로 풀린 자리와 **셀 안쪽**에서 소실이 났다 | 처방 ③에 「행 수가 맞아도 셀 안의 값이 남았는지 본다」를 붙였다 |
| 🔴 **자기 검사가 통과해도 케이스에 없는 축은 미검증이다** | 13/13 PASS 가 **줄바꿈에 대해 아무 말도 하지 않았다** | 검사기를 고친 뒤 **옛 판으로 되돌려 새 케이스가 실제로 FAIL 하는지** 확인한다 |
| **`sed` 로 JS 정규식을 치환하지 못한다** | 뮤테이션 때 `\\r?\\n` 이 들어간 치환식이 아무것도 바꾸지 못했다 | 뮤테이션도 `Edit` 로 한다. 되돌린 뒤 pristine 사본과 `diff` |
| 🔴 **문장 다듬기는 세 번째 회차부터 수확이 급감한다** | 3·5·7·8 회차가 각각 −231 · −114 · −184 · −102 였다 | 10% 넘게 넘쳤으면 **절 경계**를 먼저 손댄다 (편4에서 −2,643) |
| 🔴 **`grep -c` 는 줄이 아니라 「그 낱말이 있는 줄」을 센다** | 산출본 HTML 이 한 줄이라 도식 2개가 `flowchart LR` **1건**으로 나왔다 | 산출물 검증은 `grep -o … \| wc -l` 로 **출현 횟수**를 센다 |
| **Bash 도구에 PowerShell 히어스트링을 쓰면 조용히 섞인다** | `-m @'…'@` 로 커밋해 제목 앞에 `@` 가 붙었다 | 메시지는 파일에 쓰고 `-F` 로 넘긴다 |
| **`/tmp` 가 셸과 파이썬에서 다른 경로다** | Bash 히어독으로 쓴 `/tmp/x.md` 를 파이썬이 「없다」고 했다 | 스크래치패드 **절대 경로**를 쓴다 |
| **`cd` 뒤의 상대 경로가 깨진다** | 셸 도구에서 `cd` 한 뒤 `cat` 이 「파일 없음」을 냈다. 네 세션 연속이다 | 셸 도구에는 **절대 경로**를 쓴다 |
| **`compose` 와 `wc -c` 의 1 B 오프셋** | 여덟 편에서 재현됐다 | 상수 오프셋이다. **1 B 차이를 「불일치」로 오독하지 마라** |
| **`check-counts` 는 네 자리를 본다** | 검사기가 네 자리를 이름까지 짚어 알려 준다 | 새 절을 쓴 뒤 다시 돌린다 |
