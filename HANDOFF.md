# HANDOFF

> 마지막 갱신 2026-09-01 · `pm` 배치 **시리즈 A 7/9 발행 · 시리즈 B 0/8**

## 지금 상태

발행본 **174편**이다. `ai-product-planning` 카테고리에 일곱 편이 있고 편8부터 이어 쓴다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 1,990줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 · B `product-management` 8 |
| 발행 완료 | A 편1~7 (174편) |
| 다음 단계 | 설계서 **§10-7** — A 편8 `skill-packaging` 집필 |
| 푸시 · 배포 | ✅ **완료** — `9000de8` 까지 `main` 에 올렸고 Pages 배포가 성공했다 |

**설계서 §1 의 절차표가 정본이다.** 다만 절차표의 상태 칸은 갱신되지 않았으므로
**진행률은 `§10-N` 의 실측 절 번호로 판단하라** — §10-7 까지 있으면 편7까지 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | A 편7 `operate-stage` 집필·발행 (14,992 B) | `content/blog/ai-product-planning/operate-stage.md` |
| 2 | 편6 말미에 편7 링크 연결 | `deliver-and-prd.md` |
| 3 | 🔴 지도편 형제편 링크 **편5·편6·편7 세 행**을 붙였다 — 편5부터 끊겨 있었다 | `planning-harness-map.md` |
| 4 | 리뷰 2축 반영 (정확성 7건 · 검사 유효성 6건) | 편7 본문 6곳 |
| 5 | 편7 실측 + **편8 산문 예산 역산** | 설계서 **§10-7** · **§10-7-f** |
| 6 | CHANGELOG 편7 절 + 가장 오래된 절을 월별 아카이브로 이동 | `CHANGELOG.md` · `docs/changelog/2026-08.md` (35 → 36절) |
| 7 | 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 편8 산문 예산을 이미 역산해 뒀다 — 초고 전에 이 값을 지켜라

편4가 물리고 편5·편6·편7에서 **연속 세 번 작동한** 처방이다. 편8 목표는 **16,605 B**,
밴드 **15,540 ~ 17,670** 이며 하한 반려 대상은 **아니다**.

| 성분 | 원본 §9 | 배율 | 발행 예산 |
| --- | ---: | ---: | ---: |
| 표 | 4,262 | 1.500 | **6,393** |
| 코드 | 1,738 | 1.144 | **1,988** |
| 도식 | 290 | 1.00 | **290** |
| frontmatter | — | — | 950 |
| **산문** | 564 | — | **6,984** |

표 배율은 **겹치지 않는 편의 두 실측(1.603 · 1.400)의 평균**을 썼다. §3-2-b 가 편8을
「겹치지 않는다」로 계획했기 때문이다. 🔴 **다만 겹침 여부와 실측 배율은 잘 대응하지 않는다** —
편4는 겹치는데 1.511, 편5는 겹치지 않는데 1.603 이었다.

⚠️ 원본 §9 의 **인용 3.3%(252 B)** 는 표에 칸이 없다. §7-3 에 따라 산문 6,984 안에서 소화된다.
**인용을 따로 잡아 총량에 더하지 마라.**

### 2. 🔴 도식 계수는 **1.00** 이다 — 처방을 지킬수록 도식이 줄어든다

다섯 표본이 모두 1.263 을 밑돌았고 편7은 **0.778** 로 등가마저 깼다. 우연이 아니라 기전이 있다.

처방 ②가 「구조는 유지하되 **라벨은 다시 쓴다**」를 요구하는데, 원본 라벨은 예시를 달고 있어서
(`출시 전 게이트 가설<br/>예: 지불자가 돈을 낸다`) 다시 쓰면 예시가 본문 산문으로 빠지고 라벨이
짧아진다. ⇒ **계수를 크게 잡으면 산문 예산이 그만큼 깎여 초고가 하한 쪽으로 밀린다.**

### 3. 🔴 편8은 **코드 배율의 두 번째 표본**이다 — 반드시 실측하라

편6의 **1.144** 가 이 배치의 유일한 표본이고, 그 값이 낮았던 이유는 **주석과 문자열만 한글로
옮겼기 때문**이다. 편8 원본 §9 는 코드가 1,738 B 로 원본의 22.8% 를 차지한다.

⚠️ **편8에는 원형 보존 코드 블록이 넷이다** — `discover/SKILL.md` · 플러그인 구조 · 배포 절차 ·
출시 체크리스트. §11-2 가 부록 A·B 를 배정에서 뺐지만 **§9 안의 원형 보존 블록은 배정 안**이다.
원형 보존 자료는 원본 대조에서 축자 겹침이 0 이 되지 않는다(§10-6-c 가 파이썬 관용구로 겪었다).
**억지로 비틀지 말고 정당 축자로 판정해 기록하라.**

### 4. 복제 제거 몫은 **무엇이 겹쳤는지에 달렸다**

| 편 | 증분 | 겹친 것 |
| :---: | ---: | --- |
| 5 | +232 | 산문 |
| 6 | +183 | 산문 |
| 7 | **+22** | **표 셀 한 칸** |

「+200」은 산문이 겹쳤을 때의 값이다. ⇒ **초고 직후의 값으로 밴드를 판정하지 마라. 복제 제거 →
`compose` 재실행 → 필요하면 삭감 순이다.** 리뷰 반영은 편3 +292 · 편7 +66 이었으므로
상단 여유는 **300 B 이상**을 남긴다.

### 5. 🔴 리뷰가 편7에서 잡은 새 유형 두 가지 — 편8에도 적용된다

| 유형 | 무슨 일이 있었나 | 규칙 |
| --- | --- | --- |
| **열 겹침 표의 빈 칸** | 원본 두 표를 열로 갈랐더니 한쪽 층에 **원본 근거가 없는 칸**이 생겼고 그것을 창작으로 메웠다. §7-2 가 예고한 자리에서 예고하지 않은 방식으로 터졌다 | 🔴 **열로 겹쳤으면 셀마다 원본 근거 위치를 확인하라. 근거가 없으면 행을 빼거나 산문으로 옮겨라** |
| **용어 실재의 거짓 통과** | 배정 용어 6개를 세어 각 1회씩 나오자 실재한다고 판정했는데, **그 1회가 용어표 행 자신**이었다 | 🔴 **용어 실재는 용어표 절을 제외한 범위에서 센다** |

처방 ③(「N개」 목록을 표의 각 행과 1:1 로 짚어라)이 MAJOR 2건을 혼자 잡았다. **처방 네 줄을
그대로 물려라.**

### 6. 🔴 밴드와 하한이 **기계 검사 밖에 있다** — 「테스트 초록」을 분량 근거로 쓰지 마라

| 무엇이 없나 | 근거 |
| --- | --- |
| 밴드를 판정하는 검사기 | `grep -rn '13605\|15420' scripts/ tests/` **0건**. `compose` 출력에 합격 줄이 없다 |
| 하한 미달을 **실패로** 내는 검사 | `tests/blog/content/schema.test.ts:62` 가 `stderr` 로 경고만 한다 — 이미 **5편이 13,300 B 미달**인데 54/54 통과 |

분량 판정은 stderr 의 `[SOFT]` 목록과 `compose` 출력을 **눈으로 읽어야** 한다.
부수 실측 — **`compose` 총량은 `wc -c` 보다 항상 1 B 크다**(세 편에서 재현). 상수 오프셋이므로
교차 검증에서 1 B 차이를 「불일치」로 오독하지 마라.

### 7. 편7이 편8을 예고한 낱말을 `grep` 으로 확인하라

편4가 물린 규칙이다(§10-4-f). 편7 말미의 예고는 이렇다.

> 다음 편은 여기서 뽑아낸 노하우를 한 사람의 것으로 두지 않는 방법으로 넘어간다.
> 판단을 어떻게 **도구의 형태로 굳히는지**, 그리고 그것을 **남에게 건네 쓰게** 만들 때 무엇이
> 갖춰져 있어야 하는지가 거기서 갈린다.

지도편이 편8에 배정한 용어는 `플러그인 묶음` · `암묵지`(편7과 공유) · `에이전트 스킬`
(Agent Skills) · `스킬 생성기`(skill-creator) · `공개 우선`(Public-First) · `모범 사례`(BP) 다.
예고의 「도구의 형태로 굳히는지」가 에이전트 스킬에, 「남에게 건네 쓰게」가 패키징과 배포에
대응한다.

⚠️ 편7이 **편8의 문제 정의를 네 곳에서 선점**했다(검사 유효성 축 발견 ⑤) — 「판단의 이력이
쌓인다」 · 「노하우가 사람 머릿속에만 있으면 그 사람이 떠날 때 함께 사라진다」 · 「배운 것이
쌓이지 않는다」 · 「규칙 초안을 쓴다」. 해법은 하나도 서술하지 않았으므로 침범이 아니지만,
**편8 도입이 같은 문제 제기로 시작하면 겹친다.** 시작점을 해법 쪽에 두라.

### 8. 편8 발행 시 링크 두 곳을 함께 건다

| 어디에 | 무엇을 |
| --- | --- |
| 편7 마지막 문단 | 「다음 편은」을 `[다음 편](/blog/ai-product-planning/skill-packaging/)` 으로 |
| 지도편 구성 표 **편8 행** | 🔴 편5~7 은 이번에 붙였다. **편8 행은 아직 링크가 없다** |

---

## 편8 사양 요약

| 항목 | 값 |
| --- | --- |
| 슬러그 | `skill-packaging` |
| 원본 절 | `pm-harness/03` **§9** · 7,624 B |
| 목표 · 밴드 | **16,605 B** · 15,540 ~ 17,670 (하한 반려 대상 아님) |
| 태그 | `claude-code` `knowledge-management` |
| 링크 | 앞 편 **`operate-stage`** · 지도편. 다음 편(A-9 Q&A) 링크는 편9 발행 때 |
| 주제 경계 | 스킬화와 패키징. 🔴 **면접 활용은 편9다** |
| 표 처리 | 겹치지 않는다(§3-2-b) — 스킬 패키징 단계표 하나다 |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **재환산 지점이 하나 남았다** — **B 편1 뒤**다(설계서 §3-6). 고정 비용이 시리즈 경계에서
  리셋되는지 아무도 모른다. **B 편1 초고 `compose` 를 보기 전에 B 편2 를 쓰지 마라.**
- **A-9(Q&A편) 발행 전에 §6-1 의 형제편 outbound 세는 명령을 실행하라.** 명령이 설계서에
  코드 블록으로 들어 있다. 세지 않고 결손 목록을 만들면 앞 배치처럼 오독한다.
- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  시리즈 B 가 없어 지금은 걸 수 없다(§6-2). **B 시리즈 발행 때 되돌아와 건다** — 두 편 모두
  이미 발행됐으므로 **파일을 다시 열어 고쳐야 한다.**
- 🔴 **관례는 검사기가 없으면 조용히 끊긴다.** 지도편 형제편 링크가 편5부터 두 편 동안 빠져
  있었는데 어떤 검사에도 걸리지 않았고, 설계서는 「붙여 왔다」고 적고 있었다.
- **원본은 p95 인데 시리즈는 p90 으로 쓴다.** 지도편 187행이 `p50 / p90` 으로 배정했고 편7이
  그대로 따랐다. **시리즈 내부는 일관하므로 고치지 마라** — 다만 원본을 바꾼 결정임을 안다.
- 🔴 **`check-counts` 를 발행 전 검사에 반드시 넣어라.** README 의 발행본 수가 편1 이후 갱신되지
  않아 **편2~7 동안 Pages 배포가 계속 실패**했고, 편7 세션이 푸시한 뒤에야 발견했다.
  로컬 빌드도 pre-commit 훅도 이것을 보지 않는다 — CI 의 `Check documented counts` 에서만 죽는다.
  ⇒ **편을 발행했으면 README 세 자리(요약표 · 디렉터리 트리 · 카테고리 내역)를 같은 커밋에 넣는다.**
- **`user-research` · `career` 태그가 싱글톤으로 남는다.** 글에 없는 내용으로 태그를 맞추지 마라.
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관하다.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 넘으면 `docs/changelog/YYYY-MM.md` 로 옮기고 상단
  인덱스 표를 갱신한다. 이번 세션에 1개 절을 옮겨 `2026-08` 이 35 → **36절**이 됐다.

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **낱말을 세도 「어디서」를 세지 않으면 거짓 통과가 난다** | 배정 용어 6개를 `grep -o … \| wc -l` 로 세어 각 1회를 얻고 실재한다고 판정했는데, **그 1회가 용어표 행 자신**이었다. 본문 등장은 0회였고 리뷰가 잡았다 | **범위를 잘라 센다.** 용어 실재는 용어표 절을 **제외한** 구간에서 |
| **`compose` 와 `wc -c` 가 항상 1 B 어긋난다** | 세 편에서 재현됐다(14,992/14,991 · 14,788/14,787 · 16,199/16,198). 두 방법 교차 검증에서 「불일치」로 보인다 | 상수 오프셋이다. 판정에 영향 없으므로 **1 B 차이를 오독하지 마라** |
| **`npm test` 초록은 분량 하한의 증거가 아니다** | `schema.test.ts:62` 가 `stderr` 로 경고만 해서 **5편이 미달인 채 54/54 통과**했다 | 분량은 stderr `[SOFT]` 목록과 `compose` 출력을 **눈으로** 읽는다 |
| **파이썬 heredoc 의 출력이 콘솔에서 깨진다** | `print("완료")` 가 `?? ?? ??` 로 나왔다. 파일 쓰기는 정상이었다 | **콘솔 출력이 깨진 것을 실패로 읽지 마라.** 결과는 파일을 다시 열어 확인한다 |
| **`grep -c` 가 산출물의 도식 수를 절반으로 본다** | 산출물이 한 줄로 minify 돼 `grep -c 'mermaid'` 가 1 을 냈다. `grep -o` 는 2 로 소스와 일치했다 | 줄이 아니라 **출현 횟수**를 센다. §10-5-d 와 같은 함정이 재현됐다 |
