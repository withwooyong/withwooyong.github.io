# HANDOFF

> 마지막 갱신 2026-09-04 · `pm` 배치 **시리즈 A 9/9 · 시리즈 B 8/8 완결** ·
> **문서를 지키는 게이트를 걸었다.** 마크업과 링크가 훅과 CI 양쪽에 있고, 발행본과 리포 문서가 모두 0 이다

## 지금 상태

발행본 **184편**이다. 배치는 편8로 끝났고, 그 뒤 다섯 세션이 **검사기의 사각지대**를 닫아 왔다.
직전 세션이 리포 문서 250회를 비워 0회를 만들고 「훅·CI 에 걸 수 있는 유일한 시점」이라고
남겼다. **이번 세션이 그것을 걸었고**, 이어서 링크까지 닫았다.

| 항목 | 값 |
| --- | --- |
| 발행 완료 | **A 편1~9 + B 편1~편8 (184편)** · 시리즈 B 완결 |
| 검사기 | ✅ **여덟 종** · 뮤턴트 **38/38 잡힘 · 생존 0** |
| 훅 | ✅ 발행본 **7단** · 문서 **4단** · 코드만이면 즉시 통과 |
| CI | ✅ **19스텝** — 마크업 3단 · 링크 3단이 새로 들어갔다 |
| 강조 렌더 실패 | ✅ 발행본 **0회 · 184편** · 리포 문서 **0회 · 54개** |
| 깨진 링크 | ✅ 발행본 **0곳**(1,612개 검사) · 리포 문서 **0곳** — 이번 세션이 42곳을 비웠다 |
| 푸시 · 배포 | ✅ `56e8ebd` · `git status -sb` 에 `[ahead]` 없음 실측 · CI 두 회차 모두 success |
| 커밋 | `c686c9f`(문서 마크업 게이트) · `56e8ebd`(링크 검사기) |
| 다음 단계 | 아래 **「남은 사각지대」** — Mermaid 문법과 앵커 유효성 |

### 이번 세션이 한 일

| # | 무엇 | 어디에 남았나 |
| ---: | --- | --- |
| 1 | **문서 마크업 검사를 훅·CI 에 걸었다** — 250회가 쌓인 원인을 닫았다 | `c686c9f` |
| 2 | 🔴 **검사기가 대상을 조용히 버리던 것을 고쳤다** — 54개 중 50개만 보고 0을 냈다 | 아래 1번 |
| 3 | **링크 검사기를 새로 두었다** — 규칙 셋, 자기 검사 23건 | `56e8ebd` |
| 4 | **문서의 깨진 링크 42곳을 비웠다** | 아래 4번 |
| 5 | 🔴 **거짓 양성을 처음 겪었다** — 없는 위반 31건을 만들어 냈다 | 아래 2번 |
| 6 | 뮤턴트 10개 추가(`M6`~`M8` · `CL1`~`CL7`) · ID 충돌 해소 | 아래 5번 |
| 7 | 도구 함정 **43·44번** 신설, `CLAUDE.md` 의 낡은 수치 정정 | `docs/TOOL-TRAPS.md` |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 🔴 **「대상 없음」 가드는 「일부 누락」을 막지 않는다**

문서 게이트의 대상 집합을 정하다가 드러났다. 54개를 넘겼는데 **50개만 스캔하고 종료 코드 0**
이 나왔다.

| 무엇을 | 결과 |
| --- | ---: |
| `git ls-files '*.md'` | 54줄 · 그중 **4줄이 `.md` 로 끝나지 않음** |
| `git -c core.quotePath=false ls-files '*.md'` | 54줄 · 전부 `.md` 로 끝남 |

`core.quotePath` 기본값이 한글 경로를 `"pages/…\355\227\210….md"` 로 감싼다. 끝이 `.md` 가
아니게 되고, 검사기는 `.md` 아닌 인자를 `continue` 로 건너뛰고 있었다.

⇒ **일부 누락은 대상 없음보다 다루기 어렵다.** 숫자가 그럴듯하게 나오기 때문이다.
지금은 대상이 되지 못한 경로가 있으면 **종료 코드 2** 다. 수집은 검사기 안(`--docs`)으로
옮겨 `core.quotePath` 를 끄는 자리를 한 곳으로 모았다 — 호출부에서 `git ls-files` 로
뽑아 넘기지 마라.

### 2. 🔴 **검사기는 「없는 위반」도 만들어 낸다 — 0건일 때만 의심하는 습관은 절반이다**

링크 실태를 파악하려고 정규식으로 짠 일회성 스크립트가 **두 번 연달아** 틀린 위반을 냈다.

| 보고 | 실제 |
| --- | --- |
| 발행본 2곳이 없는 `.md` 를 가리킨다 | ```markdown 코드 블록 **안의 예시**였다 |
| 발행본 29곳이 없는 편을 가리킨다 | 앵커가 붙어 있었을 뿐 편은 전부 있었다 |
| 문서 44곳이 깨졌다 | 파서로 세면 **42곳** |

최종 실측은 **발행본 1,612개 링크 전부 온전**이었다. 처음 보고한 「발행본이 404 를 낸다」는
통째로 사실이 아니었고, 그대로 믿었다면 멀쩡한 편을 손댔을 것이다.

🔴 **거짓 양성은 스스로 검증되는 것처럼 보인다.** 위반 목록이 파일명과 줄 번호까지 갖추고
나오므로 「검사기가 일했다」는 인상을 준다. 0건은 의심받지만 42건은 의심받지 않는다.

⇒ **위반이 나왔을 때도 몇 건을 열어 확인한 뒤에 고치러 가라.** 전체 경위는
`docs/TOOL-TRAPS.md` 44번에 있다.

### 3. 🔴 **일회성 스크립트라는 이유로 규칙을 낮추지 마라**

위 실수의 직접 원인이다. `check-markup` 이 이미 「판정은 파서가 한다」를 지키고 있었는데도,
실태 파악용이라는 이유로 정규식으로 짰다. 마크다운의 **의미**를 묻는 검사는 일회성이어도
파서로 짜야 한다 — 코드 블록과 인라인 코드가 저절로 빠지는 것이 파서의 본질적 이득이다.

⚠️ 파서를 써도 **의식적으로 다뤄야 하는 것**이 둘 있다.

| 무엇 | 왜 |
| --- | --- |
| 앵커 분리 | 뗄 수는 있지만 **떼기로 결정한 사람**이 있어야 한다 |
| 참조식 링크 | `mdast` 에서 URL 은 `link` 가 아니라 **`definition` 노드**에 있다. `link` 만 순회하면 `[가][ref]` 형식이 통째로 빠진다 |

### 4. ✅ 문서 링크 42곳을 어떻게 갈랐나

| 유형 | 개수 | 처리 |
| --- | ---: | --- |
| A — 리포 루트 기준으로 적혔고 **대상이 존재** | 22 | 상대 경로로 고쳐 썼다 |
| B — 대상이 **리포 밖**이라 고칠 수 없음 | 20 | 링크만 벗기고 글자는 남겼다 (사용자 결정) |

유형 B 는 대부분 `pages/product-lead-loadmap/` 에서 `../../../shared/knowledge/…` 를
가리킨다. 다른 워크스페이스의 문서라 이 리포 안에서는 영원히 깨진다. **제외 목록을 만들지
않고 링크를 벗긴 이유**는, 제외 목록이 생기면 그 안은 영원히 검사되지 않기 때문이다.

34줄만 바뀌었다(한 줄에 여러 곳이 있었다). 링크 구문만 건드렸으므로 분량 예산과 무관하다.

### 5. 🔴 **뮤턴트 ID 는 검사기마다 갈라라**

`check-links` 의 뮤턴트를 `L1` 부터 `L7` 까지로 붙였는데 **`dup-scan` 이 이미 `L1` 부터
`L3` 까지를 쓰고 있었다.** 뮤테이션 보고서에 같은 ID 가 두 줄씩 나왔고, 생존이 나왔을 때
어느 자리를 봐야 하는지 알 수 없게 된다. `CL1` 부터 `CL7` 까지로 바꿨다.

⚠️ 이 줄을 고치면서 함정 **42번**을 그대로 밟았다 — 물결표로 범위를 쓰면 강조와 같은 줄에
놓일 때 여는 별표가 삼켜진다. 게이트가 잡았다.

⇒ 새 검사기의 뮤턴트를 추가하기 전에 **ID 중복을 먼저 세라.**

```
grep -ao 'id: "[A-Z]*[0-9]*"' scripts/mutate.mjs | sed 's/id: //' | tr -d '"' | sort | uniq -d
```

### 6. ⚠️ `scripts/mutate.mjs` 는 grep 이 **바이너리로 본다**

NUL 바이트가 **한 개** 들어 있다. 결함이 아니라 **의도**다 — 「없는 문자열」을 소스에
리터럴로 적으면 그 순간 존재하게 되므로, 런타임에 `join` 으로 조합하려고 NUL 을 구분자로
썼다. `grep -a` 를 붙여 읽어라.

### 7. 🔴 **훅만 있는 검사는 지켜지지 않는 검사다**

마크업 검사가 훅에는 5단으로 들어 있으면서 **CI 에는 아예 없었다.** `--no-verify` 로
지나갈 수 있고 웹 UI 편집은 애초에 훅을 타지 않는다. 지금은 둘 다 있다.

⇒ 검사기를 새로 만들면 **훅과 CI 양쪽에** 넣어라. 빌드가 필요한 것만 CI 전용이다.

### 8. ✅ 세 검사 모두 **전량**을 본다

스테이징된 파일만 보면 **깨뜨린 쪽이 아니라 깨진 쪽이 통과한다.** 편을 지우거나 슬러그를
바꾸면 그 편을 가리키던 다른 편이 깨지는데, 정작 커밋이 건드린 파일에는 아무 문제가 없다.

문서 전량 스캔은 실측 **1.9초**다. 느려서 끄고 싶어질 만한 값이 아니다.

---

## 검사기 여덟 종 — 순서는 「증명한 뒤 스캔」이다

| 검사기 | 무엇을 | 자기 검사 |
| --- | --- | ---: |
| `check-forbidden` | 금칙어 (소스 · 빌드 산출물) | 15건 |
| `dup-scan` | 발행본 사이의 축자 복제 | 7건 |
| `source-overlap` | 리포 밖 **원본**과의 겹침 | 28건 |
| `compose` | 성분별 분해 (분량 예산) | — |
| `check-counts` | README·CHANGELOG 의 발행본 수 | — |
| `check-markup` | 렌더되지 않는 강조 | **24건** |
| `fix-markup` | 위 위반의 교정 | 19건 |
| 🆕 `check-links` | 깨진 링크 (R1·R2·R3) | **23건** |

**증명하기 전에는 0을 믿지 마라.** 그리고 이번 세션이 더한 것 — **위반이 나왔을 때도
검사기를 의심하라.**

`npm run mutate` 로 뮤턴트 **38개**를 되살려 자기 검사가 잡는지 본다. 검사기를 고쳤으면
반드시 돌린다. 실행에 3~4분 걸리므로 백그라운드로 돌리고, **다른 파일 작업과 병행하지 마라** —
러너가 파일을 심었다 되돌리는 동안이다.

---

## 남은 사각지대 — 다음 후보

| 후보 | 규모 | 비고 |
| --- | ---: | --- |
| **Mermaid 문법 검사** | 발행본 544 + 문서 104 = **648개** | 마지막 사각지대. 실제 파서를 붙여야 해서 이번 둘보다 크다 |
| **앵커 유효성** | 발행본 앵커 링크 **28개** | `check-links` 는 앵커를 **분리만** 하고 판정하지 않는다. 한글 제목의 슬러그 규칙을 렌더러에서 확인해야 한다 |
| 분량 하한 SOFT **5편** | `ai-agent` 1 · `rag` 1 · `search-engineering` 3 | 이 배치 밖의 옛 편들이라 별도 판단이 필요하다 |
| 시리즈 A·B 밖의 새 배치 | — | 원본이 어디에 있는지부터 정해야 한다 |

---

## 다음 배치를 시작하면 다시 읽을 것

배치 작업이 끝나 지금은 쓰이지 않지만, 새 배치에서 **그대로 되풀이될** 것들이다.

- 🔴 **`compose` 는 「본문을 마지막으로 만진 뒤」 돌린다.** 복제 제거뿐 아니라 **리뷰 반영**도
  분량을 늘린다 — 편8에서 상한을 세 번 넘겼고 원인이 매번 달랐다. **표 예산이 총량보다 먼저
  터진다**(표 여유가 편6 296 → 편7 65 → 편8 **13** 으로 좁아져 왔다).
- 🔴 **인수인계에 적힌 수치도 대조군 없이는 근거가 아니다.** 편8 착수 때 원본 B 9,682 가
  틀렸다(실측 9,430). **경계는 줄 수가 아니라 「인용 줄이 끊기는 지점」으로 찾아라.**
- 🔴 **금칙어 때문에 뺀 블록은 낱말만 빼면 되는 것이 아니다.** 배정 밖 블록의 **판단**이
  본문으로 새는 일이 편6·편7·편8 **세 편 연속**이었다. 집필 전에 「이 블록에서 온 판단은
  쓰지 않는다」를 **명제 단위로** 적어라.
- 🔴 **`dup-scan` 0건은 원본 대조의 0건이 아니다.** 편8은 `dup-scan` 7건을 고친 **뒤에**
  `source-overlap` 이 편7의 4.6배를 냈다. **표를 전치할 때 칸이 증발한다** — 전치 뒤에는
  원본과 칸 수를 대조하라.
- 🔴 **산식의 상수 2,455 는 상수 절의 예산이 아니다.** 회귀 절편이며, 상수 절의 초과분은
  배율 안에 이미 흡수돼 있다. **산식의 항을 문서의 부위에 대응시키지 마라.**
- **초고가 어느 쪽으로 벗어나는지는 원본의 표 비중이 정한다.** 편8은 이것을 예측에 썼고
  「약 +23% 넘친다」가 실측 +22.6% 로 맞았다.

---

## 알려진 문제

- 🔴 **`compose` 통과는 배정 타당성이 아니다.** 성분 실측 **바로 다음에** 배정 원본의 금칙어를
  직접 세라. `LC_ALL=C` 필수다.
- 🔴 **표 배율을 `원본 표 B × 배율` 로 예측하지 마라.** 표는 **절대값 예산**으로 관리한다.
- SOFT 2건이 `agentic-coding/thirty-agent-catalog.md:195` 에 상시 남아 있다 — 정당하다.
- 분량 하한 SOFT 5편(`ai-agent` 1 · `rag` 1 · `search-engineering` 3)은 이 배치 밖의 옛 편들이다.
- 「강의」는 SOFT 금칙어다. 원본이 강의 노트라 **출처를 지칭하면 걸린다.**
- 배치 전체 `dup-scan` 20건은 전부 용어 정의 · 법률명이며 정당하다.
- ⬜ 강조 렌더 실패 **14회**가 `.superpowers/sdd/` 에 남아 있다. `*` 로 gitignore 된 도구
  부산물이라 커밋되지도 렌더되지도 않는다 — **검사 대상이 아니다.**

---

## README/문서 갱신 필요

이번 세션이 **고치지 않고 남긴** 것이다.

| 무엇 | 왜 남겼나 | 확인할 진실원 |
| --- | --- | --- |
| README 에 없는 npm 스크립트 `check-counts:verify` · `check-counts:print` · `map-terms:verify` · `compose:verify` · `source-overlap:verify` · `mutate:verify` | 전부 `:verify` 계열이고, README 는 본 명령 줄에 「`:verify` 는 자체 검사」로 함께 적는 방식을 쓴다. **일관된 의도로 보이므로** 세션 끝에 판단해 바꾸지 않았다 | `README.md` 91~97줄의 서술 방식 |
| `check-forbidden:verify` 의 「15건」 | 이번 세션에서 건드리지 않았고 출력 형식이 달라 자동 대조가 안 됐다 | `node scripts/check-forbidden.mjs --self-test` |

⚠️ 수집 스크립트의 `ENV_KEYS_IN_CODE_BUT_NOT_IN_README`(`OPENAI_API_KEY` 등 5개)는
**오탐이다.** 코드에는 없고 **블로그 본문 3편의 코드 예시**에서 왔다. 이 프로젝트는 정적
포트폴리오라 그런 환경변수를 쓰지 않는다 — README 에 넣지 마라.

---

## 도구 함정 — 이번 세션에서 실제로 겪은 것

전체 **44건**은 [`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md)(986줄)에 있다. 이번에 더한 둘:

| # | 무엇 |
| ---: | --- |
| **43** | `git ls-files` 가 한글 경로를 따옴표로 감싸, 검사기가 일부를 조용히 버린다 |
| **44** | 링크를 정규식으로 세면 **없는 문제**가 만들어진다 — 이 문서의 첫 거짓 양성 |

되풀이해 겪은 것들:

- **`grep -r .` 이 `out/` 을 훑어 120초 타임아웃.** 이번에도 밟았다. `Grep` 도구나 명시 경로.
- **`git stash`/`pop` 이 autocrlf 로 CR 을 다시 붙인다.** 같은 파일이 같은 세션에서 CR=0 과
  CR=934 로 갈렸다. **작업 트리 CR 계수는 무결성 지표가 아니다** — 정본은 `git cat-file blob`.
- **파이썬의 콘솔 출력이 cp949 로 죽어도 파일 쓰기는 끝나 있다.** `UnicodeEncodeError` 를
  보고 「실패했다」고 읽지 마라. 파일을 확인하라.
- **`|` 뒤의 종료 코드는 마지막 명령의 것이다.** 종료 코드를 읽을 명령은 단독 실행.
