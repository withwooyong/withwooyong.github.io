# HANDOFF

> 마지막 갱신 2026-09-02 · `pm` 배치 **시리즈 A 8/9 발행 · 시리즈 B 0/8**

## 지금 상태

발행본 **175편**이다. `ai-product-planning` 카테고리에 여덟 편이 있고 **편9(Q&A)** 하나가 남았다.

| 항목 | 값 |
| --- | --- |
| 설계서 | `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` (약 2,120줄) |
| 계획 편수 | **17편** — A `ai-product-planning` 9 · B `product-management` 8 |
| 발행 완료 | A 편1~8 (175편) |
| 다음 단계 | 설계서 **§10-8-g** — A 편9 `planning-harness-qna` 집필 |
| 푸시 · 배포 | ⏳ **미완** — 편8 커밋이 로컬에만 있다. 사용자 승인 뒤 `main` 에 올린다 |

**설계서 §1 의 절차표가 정본이다.** 다만 절차표의 상태 칸은 갱신되지 않았으므로
**진행률은 `§10-N` 의 실측 절 번호로 판단하라** — §10-8 까지 있으면 편8까지 끝난 것이다.

### 이번 세션이 한 일

| # | 작업 | 산출 |
| ---: | --- | --- |
| 1 | A 편8 `skill-packaging` 집필·발행 (16,549 B) | `content/blog/ai-product-planning/skill-packaging.md` |
| 2 | 편7 말미에 편8 링크 연결 | `operate-stage.md` |
| 3 | 지도편 구성 표 **편8 행** 링크 부착 | `planning-harness-map.md` |
| 4 | 리뷰 2축 반영 (정확성 8건 · 검사 유효성 5건) | 편8 본문 9곳 |
| 5 | 편8 실측 + 편9 착수 사항 | 설계서 **§10-8** · **§10-8-g** |
| 6 | README 세 자리 갱신 (174 → 175 · `ai-product-planning` 7 → 8) | `README.md` |
| 7 | CHANGELOG 편8 절 + 가장 오래된 절을 **새 아카이브**로 이동 | `CHANGELOG.md` · `docs/changelog/2026-09.md` (신규 · 1절) |
| 8 | 이 문서 | (이번 커밋) |

---

## 🔴 다음 세션이 반드시 먼저 알아야 할 것

### 1. 편9는 **예산을 역산하지 않는다** — 항목 패리티다

편2~8 에 네 편 연속 작동한 산문 예산 역산은 **본편의 방식**이다. 편9는 Q&A편이라 원본 배율이
아니라 **항목 수**로 잡는다(설계서 §3-4). ⇒ **역산 표를 만들지 마라.** 원본 §10 의 항목을
하위 절별로 센 표가 §3-4 에 이미 있다.

🔴 **§11-9 가 「26항목 밖의 절을 만들지 마라」로 막고 있다.** 두 방식(항목 수 · 원본 배율)이
교차 검증되는 구조이므로 항목을 늘려 분량을 맞추면 검증이 무너진다.

### 2. 🔴 착수 전에 반드시 하는 셋

| # | 할 일 | 근거 |
| ---: | --- | --- |
| 1 | §6-1 의 형제편 outbound 세는 **명령을 실행한 뒤** 결손 목록을 만든다 | 규칙 9 — 앞 배치가 세지 않고 목록을 만들어 오독했다 |
| 2 | 편8 말미의 예고 낱말이 편9 본문에 실재하는지 `grep` 한다 | §10-4-f — 편4가 물린 규칙이다 |
| 3 | 편9 커밋에 형제편 링크 수정을 **함께** 넣는다 | §9-1 커밋 20 |

편8 말미의 예고는 이렇다.

> 하네스를 실제로 굴려 본 사람이 자기가 만든 것을 설명해야 하는 자리에서 어떤 질문을
> 받게 되는지, 그리고 그 하나하나에 어느 단계의 무엇으로 답하는지가 거기서 정리된다.

⚠️ **「면접」이라는 낱말은 일부러 쓰지 않았다.** 금칙어 SOFT 이기 때문이다. 편9 본문도 같은
경계를 지킨다 — 원본 §10 의 제목이 「면접 활용 포인트」이므로 **옮길 때 낱말이 따라온다.**

### 3. 🔴 처방 ②의 두 요구가 서로 당긴다 — 요소는 유지하고 낱말을 바꾼다

이번 편에서 처음 드러난 유형이다. 정확성 리뷰가 도식 라벨에서 원본 다섯 요소 중 하나가
빠진 것을 잡았는데, 그것을 그대로 되살리자 라벨이 **원본과 한 글자도 다르지 않게** 됐다.

| 상태 | 라벨 | 원본 대조 ③ |
| --- | --- | ---: |
| 초고 | `① 의도 언제·입력·출력·검증` | 16건 |
| 리뷰 반영 | `① 의도 무엇을·언제·입력·출력·검증` | **17건 — 통째 일치** |
| 최종 | `① 의도 무엇을·언제·넣을 것·낼 것·검증` | 16건 |

⇒ **요소 수는 유지하고 낱말만 바꾼다.** 요소를 빼면 정확성에 걸리고 그대로 되살리면 축자에
걸린다. 이 절차를 밟은 결과 도식 배율이 **1.052** 로 여섯 표본 중 처음 1.0 을 넘겼다.

⚠️ 그래도 **도식 계수는 1.00 을 유지하라.** 여섯 표본이 0.778 ~ 1.052 로 흩어져 있고,
계수를 올리면 산문 예산이 깎여 초고가 하한으로 밀린다(§10-7-a 의 근거는 유효하다).

### 4. 🔴 원본이 **표로 둔 것을 산문으로 풀지 마라**

초고가 목표보다 918 B 모자랐던 원인이다. 원본 §9-8 의 피치 카드(5행)를 산문 한 문단으로
풀어 썼더니 표가 예산 대비 **753 B** 모자랐고, 표로 되살리자 15,687 → 16,246 으로 올랐다.

⇒ **총량이 두 방향으로 깎인다** — 표 예산에서 빠지고, 같은 내용이 산문에서는 더 짧게 끝난다.
§7-3 의 「인용은 산문으로 푼다」는 **인용에만** 적용된다. 표는 인용이 아니다.

### 5. 🔴 원형 보존 자료의 **수치와 키는 바꾸지 마라**

정확성 리뷰의 MAJOR 였다. 축자 겹침을 피하려다 정의 파일의 `3~5개` 를 「서넛」(3~4)으로 옮겨
**원형 보존 자료의 스펙 값이 달라졌다.**

| 바꿔도 되는 것 | 바꾸면 안 되는 것 |
| --- | --- |
| 주석 문장 · 나열 **순서** · 한글 설명 | 수치 · 명령어 · 경로 · JSON 키 · 필드명 |

§10-7-f 는 「원형 보존의 축자 겹침은 정당 축자로 판정하라」고 예고했는데, 실제 사고는
**겹침을 피하려 값을 흐린 것**이었다. 예고한 자리에서 예고하지 않은 방식으로 터졌다.

### 6. 🔴 보고값의 **신선도**가 리뷰에 걸린 첫 사례다

원본 대조를 「11건」으로 보고했으나 재측정은 **10건**이었다. 값이 틀린 것이 아니라 **잰 시점이
낡았다** — 마지막 수정 뒤 다시 재지 않았다.

⇒ **검사값은 파일을 고칠 때마다 다시 재고, 보고 직전에 다시 잰 값으로 적는다.** 규칙 7이
지금까지는 값이 틀린 자리에서만 걸렸다.

### 7. 용어표는 **뜻 칸과 도입문을 편마다 다시 쓴다**

복제 제거 +176 B 가 전부 용어표에서 나왔다. 지도편의 뜻 칸을 그대로 옮기면 46자·34자·22자가
한꺼번에 걸리고, 도입문(「지도편이 정리해 둔 어휘 가운데…」)도 편7 과 한 글자도 같았다.

**편3 은 같은 자리를 다르게 썼다**(「여기서 실제로 쓰인 것만 옮겼다」). 관례를 물려받되
문장은 새로 쓴다. 🔴 **뜻이 지도편과 어긋나면 안 되므로 문장만 바꾼다.**

### 8. 편9 발행 시 링크 두 곳을 함께 건다

| 어디에 | 무엇을 |
| --- | --- |
| 편8 마지막 문단 | 「다음 편」을 편9 경로 링크로 |
| 지도편 구성 표 **편9 행** | 편8 행은 이번에 붙였다. **편9 행은 아직 링크가 없다** |

---

## 편9 사양 요약

| 항목 | 값 |
| --- | --- |
| 슬러그 | `planning-harness-qna` (§6-1 의 명령이 이 이름으로 센다) |
| 원본 절 | `pm-harness/03` **§10** — 26항목 |
| 분량 | **항목 패리티**(§3-4). 역산하지 않는다 |
| 태그 | `career` `product-strategy` |
| 링크 | 앞 편 **`skill-packaging`** · 지도편 · 본문에서 인용하는 편들 |
| 주제 경계 | 🔴 **26항목 밖의 절을 만들지 마라**(§11-9) |

원본 경로는 다음과 같다. **어느 문서에도 적혀 있지 않아 앞 세션이 역추적해야 했다.**

```
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-harness/
C:/Users/aeby/vscode/yanadoo-exit/shared/knowledge/pm-po/
```

---

## 알려진 문제

- **재환산 지점이 하나 남았다** — **B 편1 뒤**다(설계서 §3-6). 고정 비용이 시리즈 경계에서
  리셋되는지 아무도 모른다. **B 편1 초고 `compose` 를 보기 전에 B 편2 를 쓰지 마라.**
- **코드 계수는 두 표본이 성격으로 갈린다.** 편6 **1.144**(주석만 한글로) 대 편8 **1.065**
  (원형 보존 넷). **평균 1.105 를 그대로 쓰면 원형 보존이 많은 편에서 예산이 남는다.**
  다음 배치는 **원형 보존 비중으로 나눠** 잡아라.
- **시리즈 간 링크 4건 가운데 2건이 아직 걸리지 않는다.** `A-6 → B-4` 와 `B-3 → A-7` 은
  시리즈 B 가 없어 지금은 걸 수 없다(§6-2). **B 시리즈 발행 때 되돌아와 건다** — 두 편 모두
  이미 발행됐으므로 **파일을 다시 열어 고쳐야 한다.**
- 🔴 **지도편의 「플러그인 묶음」 정의에 근거가 없다.** 176행이 「**다섯 스테이지**를 스킬로
  구현해 한데 묶은 것」이라 적었는데 원본에도 편8 에도 근거가 없다(편8 의 예시 묶음은 스킬
  둘이다). 정확성 리뷰가 「편8 의 결함은 아니다」로 분류했다. **편9 커밋에서 고칠지 판단하라.**
- 🔴 **밴드와 하한은 여전히 기계 검사 밖에 있다.** `npm test` 는 하한 미달을 `stderr` 경고로만
  내고 상한은 검사기가 없다. **「테스트 초록」을 분량 근거로 쓰지 마라** — `compose` 출력과
  `[SOFT]` 목록을 눈으로 읽는다. 현재 하한 미달 **5편**은 전부 이번 배치 밖이다.
- **`user-research` · `career` 태그가 싱글톤으로 남는다.** 편9가 `career` 를 쓰지만 그래도
  둘뿐이다. **글에 없는 내용으로 태그를 맞추지 마라.**
- **`agentic-coding/thirty-agent-catalog.md:195` 의 SOFT 2건은 기존 건이다.** 이번 배치와 무관.
- **CHANGELOG 본체는 최신 3개 절만 둔다.** 이번 세션에 `docs/changelog/2026-09.md` 를 **새로
  만들었다**(1절). 넘으면 여기로 옮기고 상단 인덱스 표를 갱신한다.
- ✅ **`check-counts` 는 이번 세션의 발행 전 검사에 넣었다.** 편2~7 동안 배포를 계속 깨뜨린
  누락인데, 리뷰가 아니라 **검사표에 넣어 두었더라면** 애초에 걸리지 않았을 자리다.
  ⇒ **편을 발행했으면 README 세 자리와 CHANGELOG 최신 절, 네 자리를 같은 커밋에 넣는다.**

## 도구 함정 — 이번 세션에서 실제로 겪은 것

| 함정 | 무슨 일이 있었나 | 대응 |
| --- | --- | --- |
| 🔴 **검사값의 신선도** | 파일을 고친 뒤 다시 재지 않고 한 건 전의 값(11건)을 보고했다. 재측정은 10건이었고 검사 유효성 축이 「어느 수치와도 맞지 않는다」로 잡았다 | **고칠 때마다 다시 재고, 보고 직전 값으로 적는다** |
| 🔴 **겹침을 피하려다 값을 흐린다** | 원형 보존 정의 파일의 `3~5개` 를 「서넛」으로 옮겼다. 축자는 줄었지만 **스펙이 달라졌다** | 원형 보존에서 바꿀 수 있는 것은 **주석과 나열 순서뿐**이다 |
| 🔴 **리뷰 반영이 다른 검사를 깨뜨린다** | 도식 라벨에 빠진 요소를 되살렸더니 원본과 통째로 일치해 `source-overlap` ③ 이 16 → 17건이 됐다 | **반영 뒤에 전 검사를 다시 돌린다.** 한 축을 고치면 다른 축이 움직인다 |
| **`cd` 뒤의 글롭이 조용히 깨진다** | `cd content/blog/… && grep *.md` 가 「No such file or directory」로 죽었다. 셸의 작업 디렉터리가 리셋된 뒤였다 | 셸 도구에는 **절대 경로**를 쓰거나 `Grep` 도구를 쓴다 |
| **`compose` 와 `wc -c` 의 1 B 오프셋** | 네 편에서 재현됐다 | 상수 오프셋이다. **1 B 차이를 「불일치」로 오독하지 마라** |
| **산출물의 도식 수는 `grep -o` 로 센다** | `grep -c` 는 minify 된 한 줄을 1로 센다. 이번 편은 도식 1개라 우연히 같았다 | 줄이 아니라 **출현 횟수**를 센다 |
