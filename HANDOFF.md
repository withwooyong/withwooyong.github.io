# 인수인계 (HANDOFF)

> 갱신: 2026-08-28 · **`main` 배포 완료** (`3c8e30c`) · 이번 세션 커밋 **3개**(문서 2 + 병합 1)
>
> 📌 **셸·검색·아틀라스는 이미 라이브다.** 65커밋 배치는 PR #1(`0e98f9d`)로 병합돼 배포까지 끝났고,
> 이번 세션은 그 배치가 남긴 **문서 부채 둘**을 닫았다. 코드는 한 줄도 바뀌지 않았다.
>
> ⚠️ **다음 세션이 가장 먼저 할 일: `git fetch` 다.** 이 세션은 fetch 없이 `origin/main` 을 읽어
> 「65커밋이 아직 배포 안 됐다」고 오판한 채 시작했다. `git log origin/main` 은 **마지막 fetch 시점의 값**이다.

---

## 1. 이번 세션에서 한 일

| # | 무엇 | 결과 |
| ---: | --- | --- |
| 1 | `CLAUDE.md` — 산출물 렌더 증명 금지 규칙 (이전 세션의 승인 대기 건) | `b62f49c` · **122줄 유지** |
| 2 | `README.md` — 65커밋 동안 밀린 서술 drift 정정 | `cef8743` · +55 −24 |
| 3 | `main` 병합 · 푸시 · 배포 | `3c8e30c` |

### 1-1. README 가 **거짓 보증**을 하고 있었다

가장 값어치 있는 발견이다. README 는 「CI 가 산출물 불변(`check-baseline`)을 돌린다」고 적어 놨는데,
[`deploy.yml:99-100`](.github/workflows/deploy.yml) 에서 **주석 처리돼 있다.** 즉 그 게이트는 **로컬 전용**이고,
README 를 믿은 사람은 돌지 않는 검사를 돌고 있다고 여겼다. 문서에 「로컬 전용」임을 명시했다.

### 1-2. 게이트가 내 편집을 잡았다 — **README 의 수치는 검사기가 지킨다**

프로젝트 구조 트리의 `content/blog/ # 발행본 156편` 을 「6개 분류」로 바꿨더니 `check-counts` 가 실패했다.
`scripts/check-counts.mjs` 는 README 안의 **네 자리**를 정규식으로 찾아 실제 파일 수와 대조하고,
**자리 자체가 사라지면 실패**시킨다. 되돌렸다.

> **README 의 숫자를 지우기 전에 `npm run check-counts` 를 돌려라.** 어떤 숫자는 게이트가 지키고 있다.

썩은 채였던 것은 게이트가 **없는** 숫자들이었다 — 라우트 수 `12→14`, sitemap URL `196→233`, 컴포넌트 `23→31`.
이것들은 숫자를 지우고 **확인 명령**으로 바꿨다.

---

## 2. ⚠️ 다음 세션이 할 일 — 셋 다 **결정이 병목**이다

```
B 접근성 3장 ── 정책 결정 필요 (기준선 금지 대상 · GC-11 봉인)
C 아틀라스 후속 ── 실물을 눈으로 봐야 함 (지금 라이브다)
D 선행 계획서 단계 2 ── 재설계 브레인스토밍부터
```

### 2-1. B — 남은 접근성 결함 **3장**

`<main id="main">` 에 `tabIndex={-1}` 이 없어 검색으로 도달했을 때 포커스가 본문으로 안 간다.
`focus()` 는 **예외를 던지지 않고 조용히 아무것도 안 한다** — 그래서 신호가 없다.

| 페이지 | 막는 것 |
| --- | --- |
| `/en/` · `/notion/` | `check-baseline` 의 **금지 대상**. 건드리면 기준선이 바뀌는데, 그 둘은 재설계 대상이 아니라 변경 자체가 회귀 신호다 |
| `/` (메인) | **GC-11 로 봉인.** 단계 2 에서 히어로를 다시 만들 때 함께 |

`/notion/` 은 `id="main"` **자체가 없고**, `pages/` 가 아니라 [`public/notion/index.html`](public/notion/) 정적 파일이다.
셋 다 검색 인덱스에는 살아 있다.

### 2-2. C — 아틀라스 후속: **지금 실물이 라이브다**

| 항목 | 무엇을 보고 정하나 |
| --- | --- |
| Canvas 2D 렌더러 · 렌더러 자동 선택 | **엣지 1,053** 을 SVG 하나로 그리고 있다. 화면에서 뭉개지는지 눈으로 보라 |
| 렌즈 3종 (전체·토픽별·클러스터별) | 클러스터 탐지(Louvain)가 이 엣지 밀도에서 쓸모 있는 결과를 내는지 |
| 좌측 토픽·태그 사이드바 필터 | D-2 가 「태그는 사이드바 필터로만」이라 정해 놓고 **그 사이드바를 아직 안 만들었다.** 고유 태그 64개 중 10개는 1회만 등장 |
| 검색 결과에 아틀라스 노드 섹션 | 지금은 노드 상세를 인덱스에서 **뺐다**(원문과 중복이라). 섹션을 따로 두려면 그 결정을 다시 봐야 한다 |

### 2-3. D — 선행 계획서의 단계 2 는 **삭제가 아니라 보류**다

[`docs/superpowers/plans/2026-08-25-redesign-phase-1-2.md`](docs/superpowers/plans/2026-08-25-redesign-phase-1-2.md) 의 **T9~T13**.
히어로 재설계 · `/work` · `/about` · `product-lead*` 스텁이 들어 있다. 사용자가 현재 히어로를 유지하기로 해서
검색·아틀라스를 먼저 했다(D-1).

**꺼내기 전에** 아틀라스·검색 계획서의 §「이월된 것 — 선행 계획서의 단계 2」와 §「T9가 이월된 이유 — 실측 4건」을 읽어라.

---

## 3. 이번 세션에 새로 드러난 리스크 — `.gitattributes` 가 **없다**

| 항목 | 값 |
| --- | --- |
| 추적 파일 | 369 |
| 워킹트리가 CRLF 인 파일 | **144 (39%)** |
| `.gitattributes` | **없음** |
| `core.autocrlf` | `true` |

커밋본(blob)은 지금 CR 0 으로 일관하지만 **그것을 강제하는 것이 아무것도 없다.**
다른 기계나 설정에서 CRLF blob 이 커밋되면 함정 [#14](docs/TOOL-TRAPS.md#t14)·[#16](docs/TOOL-TRAPS.md#t16) 이 되살아난다.

**이번 세션에서 실제로 겪은 것:** 브랜치 전환 + 병합만으로 워킹트리 CR 이 `0 → 122` 로 바뀌었다.
`package.json`(손대지 않은 **대조군**)도 똑같은 패턴이라 **정상 동작임이 드러났다** — 대조군이 없었으면 오진했다.

> **워킹트리의 CR 을 세지 마라. `git show HEAD:<파일> | tr -cd '\r' | wc -c` 로 커밋본을 세라.**

---

## 4. 이전 배치가 만난 다섯 얼굴 — **전부 초록이라 신호가 없었다**

`CLAUDE.md` 가 「**「없다」와 「읽을 수 없었다」가 같은 출력으로 나온다**」고 적어 둔 것의 다섯 변종.
**뮤턴트를 심어 보지 않았으면 다섯 건 다 통과했다.**

| # | 무엇이 거짓 초록이었나 | 어떻게 드러났나 |
| --- | --- | --- |
| 1 | `focus()` 가 `tabIndex` 없는 요소에서 **예외 없이 무동작** | NAV 등재로 skip 26건이 깨어나면서 |
| 2 | 「내비에 죽은 링크가 없다」가 **모바일에서 구조적으로 항상 0** | `NAV` 에 가짜 항목을 넣어 봤더니 desktop 만 빨개졌다 |
| 3 | 「노드 상세가 존재한다」가 **`__NEXT_DATA__` 의 props JSON** 을 보고 초록 | 뮤턴트가 두 번 생존해서 |
| 4 | 같은 검사가 **`<head>` 의 `<title>`·`og:title`** 로도 초록 — **본문을 통째로 비워도 통과** | 리뷰어 실측 → 대조군 뮤턴트로 확정 |
| 5 | 주석이 「이 검사가 X 를 잡는다」고 **단언하는데 X 를 지워도 초록** | X 를 실제로 지워 봐서 |

**여기서 나온 규칙은 이번 세션에 `CLAUDE.md` 에 반영됐다** — 「산출물의 문자열로 「렌더됐다」를 증명하지 마라」.

---

## 5. 알아야 할 함정

[`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md) 의 **#35~#37**. `CLAUDE.md` 표에는 **#35 만** 올라 있다.

| # | 무엇 |
| --- | --- |
| **#35** | **Git Bash 가 `node` 인자 안의 `/atlas/` 를 `C:/Program Files/Git/atlas/` 로 바꾼다** — 홑따옴표도 못 막는다. 뮤턴트가 엉뚱한 문자열로 주입되는데 **테스트는 빨개져서 「사멸」로 읽힌다.** `MSYS_NO_PATHCONV=1` 또는 스크립트 파일로 |
| #36 | 히어독은 `\b` 를 **온전히 통과시킨다.** 0x08 을 만드는 것은 `echo -e`·`printf` 다 — **히어독을 피해 `printf` 로 우회하는 것이 오히려 원인이다** |
| #37 | 파일 원복이 `autocrlf` 로 **LF→CRLF 를 만든다.** 이후 `\n` 앵커 편집이 조용히 어긋난다 (위 §3 참조) |

### 지금 당장 걸릴 것 하나

`out/` 은 이번 세션의 빌드로 **신선하다.** 다만 `e2e/global-setup.ts` 가 mtime 을 보므로
`scripts/` 에 파일을 하나 추가하거나 `components/` 아래 **주석만 고쳐도** 무효가 된다.
그때는 `npm run build` 가 먼저다 — 안 그러면 **테스트를 하나도 안 돌리고 종료 코드 1** 이 나오고,
그것이 진짜 실패와 똑같이 보인다. **`$?` 말고 첫 줄을 읽어라.**

---

## 6. 상태 — 이번 세션에서 전수 실측

| 항목 | 값 |
| --- | --- |
| 브랜치 | **`main` = `3c8e30c` (배포 완료)** · `feat/site-renewal` = `cef8743` (병합됨) |
| 미커밋 변경 | **없음** |
| stash | 없음 |
| `npm run lint` · `npx tsc --noEmit` | **0 · 0** |
| `npm test` (vitest 전체) | **354 passed** (15파일) |
| `check-forbidden:verify` → `check-forbidden` | **15/15** → 158파일 **HARD 0** · SOFT 2 |
| `check-forbidden:built` | 산출물 456파일 **HARD 0회** |
| `check-counts:verify` → `check-counts` | **14/14** → **156편 / 6개 카테고리** (검사한 자리 4곳) |
| `check-pagefind:verify` → `check-pagefind` | **12건** → ko **405 페이지 / 405 조각** |
| `probe-search:verify` → `probe-search` | **10건** → 기본형 **8/8** |
| `check-baseline` | **비블로그 산출물 14개 불변** |
| `npm run e2e` | **74 passed · 0 failed · 0 skipped** |
| sitemap | **233 URL** |

**순서가 규칙이다** — 모든 `:verify` 를 스캔보다 **먼저** 돌렸다. `pagefind` 는 아무것도 색인하지 않아도 0 으로
종료하므로, 「405 조각」이라는 초록은 **검사기가 0 건일 때 빨개진다는 증명이 있어야만** 의미가 있다.

**`main` 은 프리뷰 없는 프로덕션이다. 배치 마감마다 푸시 여부를 사용자에게 물어라.**

---

## 7. 남은 문서 부채

| 항목 | 상태 |
| --- | --- |
| `CHANGELOG.md` | **이번 세션 미갱신.** 문서 2건 + 병합 1건을 아직 안 적었다 |
| `.gitattributes` | **없음** (위 §3). 만들지 여부는 결정 사항 |
| `docs/site-renewal-roadmap.md` · `completion-report.md` | 2026-07 이후 미갱신. 아틀라스·검색이 반영돼 있지 않다 |

---

## 8. 작업 원장 (gitignore)

`.superpowers/sdd/2026-08-26-atlas-and-search/progress.md` 에 이전 배치의 **판단 22건**이 근거·비용과 함께 있다.
그중 **2건은 컨트롤러가 틀렸고 실측으로 뒤집힌 것**이라 같은 착각을 반복하지 않으려면 읽을 값어치가 있다.

**`.superpowers/` 는 gitignore 라 `git clean -fdx` 로 사라진다. 남길 것이 있으면 옮겨라.**
