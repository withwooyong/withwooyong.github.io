# CLAUDE.md

Claude Code 가 이 리포에서 작업할 때 참고하는 지침이다. 전역 `~/.claude/CLAUDE.md` 가 먼저 적용되고,
이 파일은 **이 리포에서만 다른 것**과 **도구가 이 리포에서 거짓을 보고하는 방식**만 담는다.
전역과 같은 말(커밋 메시지 한글 · 승인 지점 · 팀장/팀원 분할 · 컨텍스트 규칙 · 문서 형식)은 되풀이하지 않는다.

## 작업 흐름 — 1인 개발 (2026-09-17 사용자 결정)

전역 규칙이 말하는 「프로젝트 선승인」 표다. **자동**으로 적힌 것은 확인 없이 실행하고 결과만 보고한다.

| 변경 | 푸시 | 근거 |
| --- | --- | --- |
| `main` 에 커밋 · 푸시 (코드 · 발행본 · 문서 전부) | **자동** | 1인 개발이라 PR 을 열지 않는다. `deploy` job 이 `needs: build` 라 CI 가 실패하면 배포되지 않으므로, 푸시 뒤 **배포 run 의 성공 여부만** 확인한다 |
| PR · 원격 merge | 하지 않는다 | 브랜치를 만들지 않으므로 생기지 않는다 |
| 이력 재작성 (`push --force` · 원격 브랜치 삭제) | **확인** | 전역 규칙 그대로 |

| 항목 | 규칙 |
| --- | --- |
| 기록 | 🔴 **HANDOFF 는 덮어쓴다.** 네 절(지금 상태 · 남은 작업 · 다시 읽을 것 · 도구 함정)과 150줄 상한을 `check-handoff` 가 훅과 CI 에서 판정한다. `/handoff` 가 「이번 세션이 한 일」 절을 더하려 해도 따르지 않는다(2,798줄까지 쌓인 적이 있다). CHANGELOG 는 작업 단위마다 **한두 줄**. PR 번호 · merge 커밋을 옮겨 적는 절차는 없다 |
| 보고 · 확인 | 짧게, 한국어로. 실물 캡처는 새 형식을 처음 만들 때만 보인다. 토큰 절약이 목적이다 |
| 전역 마커 파일 | `.claude/no-edit-guard` · `no-web-guard` · `no-commit-clear-gate` 를 **두지 않는다.** 코드 프로젝트로 보고 커밋을 `/clear` 경계로 삼는다 |
| 🔴 세션을 닫기 전에 `git status -sb` 의 `ahead` 를 읽는다 | 문서 커밋은 배포에 영향을 주지 않아 밀려도 아무도 알아채지 못한다. 실측으로 기록 커밋 하나가 **사흘 동안** 원격에 없었다 |

## 명령

| 명령 | 설명 |
| --- | --- |
| `npm test` | `tests/blog/` 를 Vitest 로 돌린다. Vitest 는 타입을 **지울 뿐 검사하지 않으므로** `npx tsc --noEmit` 을 **따로** 돌린다 |
| 발행 전 검사 · `npm run mutate` | 명령표와 각 검사가 거짓 0 을 내는 방식은 스킬 `blog-publish-checks`(`.claude/skills/blog-publish-checks/SKILL.md`)에 있다. **`content/blog/` 나 문서를 고치기 전에 불러라.** 검사기를 고쳤으면 `npm run mutate:verify` → `npm run mutate`. 약 30분이 걸리므로 세션 시작에 백그라운드로 건다 |

## 증명하기 전에는 0을 믿지 마라 — 수를 세는 규칙

증명되지 않은 「0건」은 거짓 음성과 구분되지 않는다. 실측으로 금칙어 목록에 한글 표기가 빠져 거짓 0 이
CHANGELOG 에 사실로 남았고, `Ted_yanadoo.png` 가 산출물 366곳에 남는 동안 소스 스캔은 계속 0 을 냈다.

**문서의 수치도 대조군 없이는 근거가 아니다.** 세 세션 연속으로 옮겨 적은 수가 낡았다(뮤턴트 16·38 대 실제 48 ·
훅 7단 대 9단 · 케이스 175 대 180). 그래서 이 파일은 **현재 수를 적지 않고 세는 법만 적는다.** 수는 이어받지
말고 매번 세며, 늘어나기만 한다는 전제를 두지 않는다. 갈래를 걷어내면 줄어든다.

| 세는 것 | 세는 법 | 틀리는 방식 (실측) |
| --- | --- | --- |
| 검사기 종 수 | `scripts/*.mjs` 중 **위반을 찾아 종료 코드를 내는 것.** `generate-sitemap` · `map-terms` · `mutate` 는 판정하지 않으므로 뺀다 | 파일 수로 세면 셋이 더 들어오고, `grep -l self-test` 로 세면 `map-terms` · `mutate` 가 들어온다. `check-baseline` 은 자기 검사가 없어 종 수에서 빠진 채 **1년 가까이 CI 에서 꺼져 있었다** |
| 뮤테이션이 돌리는 검사 수 | `scripts/mutate.mjs` 의 `CHECKS` | 종 수와 다른 수다. `check-counts` 가 빠지고 `blog-unit` 이 더해진다 |
| 테스트 케이스 수 | `npx vitest run` 의 요약 줄 | `it(` 를 grep 하면 여러 줄에 걸친 선언을 놓친다 (179 대 180) |
| 뮤턴트 수 · 훅 단수 | `mutate.mjs` · `.githooks/pre-commit` 을 직접 센다 | 요약 숫자를 내지 않는 자리(`dup-scan` · `fix-markup`)가 먼저 낡는다 |
| 새 테스트 파일 | `blog-unit` 은 `tests/blog` 를 통째로 돌리지 않고 **파일을 이름으로 나열**한다. 새 파일을 거기 더하고, 새 뮤턴트 id 를 `sort \| uniq -d` 로 기존과 대조한다 | 나열하지 않은 채 파일 셋을 더했더니 새 뮤턴트 아홉 중 **여덟이 생존**했고 id(`C1`~`C4`)가 겹쳤다. 기존 파일에 케이스를 더할 때는 해당 없다 |
| 검사기의 출력 라벨 | 「무엇을 한다」는 문구도 자기 검사 항목이다. 고친 뒤 **옛 판으로 되돌려 그 케이스가 실제로 FAIL 하는지** 본다 | `source-overlap` 이 자기 검사 12/12 를 내면서 「`dup-scan` 과 같은 정규화」라고 찍었는데 실제로는 공백만 지웠다. `**` 두 쌍을 씌우자 27자가 0건이 됐다 |

🔴 **검사기 · 뮤턴트 · 훅 단계의 수를 바꿨으면 그 수가 적힌 문서를 같은 커밋에서 고쳐라.**
**금칙어 목록의 정본은 `scripts/check-forbidden.mjs` 다.** 문서로 복사하지 마라. 목록이 코드와 문서로 갈라진 것이 거짓 0 의 원인이었다.

## 게이트

**발행 규칙은 산문이 아니라 검사기에 산다.** 누적 규칙 105개를 전수 분류한 결과
(`docs/superpowers/reports/2026-08-18-rule-triage.md`) 규칙이 살 자리는 넷이다.

| 규칙이 사는 곳 | 무엇이 들어가나 |
| --- | --- |
| 검사기 (`scripts/`, `tests/blog/`) | 기계가 판정하는 것: 스키마, 금칙어, 링크, 분량, 산출물 |
| `docs/superpowers/PUBLISHING-CHECKLIST.md` | 사람의 판단만: 출처 표기, 배정 누락, 삭제 여파, 익명화, 도식 근거, 어조 |
| 이 파일 | 도구가 실패하는 방식과, **빌드가 강제하지 않는** 코드 제약 |
| 설계서 `§11` (취소선) | 배치와 함께 만료된 규칙. 다른 문서가 번호로 인용하므로 남긴다 |

두 방향 모두 금지다. **검사기의 판정 기준을 문서로 복사하지 말고**, 배치 한정 지시를 영구 규칙
목록에 넣지 마라. 영구 규칙은 **모든** 배치에서 참이어야 한다 (105개 중 14개가 탈락했다).

`.githooks/pre-commit` 이 커밋의 내용에 따라 두 갈래로 갈린다. `npm install` 이 훅을 연결하며,
손으로 하려면 `git config core.hooksPath .githooks` 다.

| 커밋이 건드린 것 | 훅이 도는 것 | CI 가 도는 것 |
| --- | --- | --- |
| `content/blog/**.md` | 11단: 금칙어 증명 · 불변식 · 금칙어 스캔 · 마크업 증명 · 마크업 스캔 · 링크 증명 · 링크 스캔 · 도식 증명 · 도식 스캔 · 표 증명 · 표 스캔 | 좌동 + 빌드 산출물 스캔 · 개수 검사 |
| 그 밖의 `**.md` | 10단: 마크업 · 링크 · 도식 · 표의 증명과 `--docs` 스캔, HANDOFF 증명과 분량 · 절 | 좌동 |
| 코드만 | 없음, 즉시 통과 | 린트 · 타입 · 빌드 |

- **검사 다섯 모두 스테이징된 파일이 아니라 전량을 본다.** 좁게 보면 깨뜨린 쪽이 아니라 **깨진 쪽**이 통과한다. 편을 지우거나 슬러그를 바꾸면 그 편을 가리키던 다른 편이 깨지는데, 커밋이 건드린 파일에는 아무 문제가 없기 때문이다.
- **훅만 있는 검사는 지켜지지 않는 검사다.** `--no-verify` 로 지나갈 수 있고 웹 UI 편집은 훅을 타지 않는다. 빌드가 필요한 검사는 CI 에만 둔다.
- **CI 는 푸시 뒤에 돈다.** PR 을 열지 않으므로 검사는 `main` 에 들어간 뒤에야 돌고, 프리뷰 환경이 없어 `main` 이 곧 프로덕션이다. 실패하면 배포는 막히지만 잘못된 커밋은 이미 `main` 에 있으므로 **훅이 첫 방어선이다.** `pull_request` 트리거는 남겨 둔다.
- 🔴 **2단의 「불변식」이 `npx vitest run tests/blog` 다. 검사기는 `scripts/` 에만 살지 않는다.** `tests/blog/content/` 가 발행본 전량을 본다(링크 무결성 · 고립 · 앵커 실존 · 스키마 · 분량 · 시리즈 · 링크 지형 · 도식 이미지 자산). TypeScript 부품이 필요한 판정은 구조적으로 이쪽에만 살 수 있다. 이름이 Vitest 를 가리킨다는 것이 드러나지 않아 앵커 실존 검사가 도는 동안 「남은 사각지대」 목록에 세 세션 머문 적이 있다.

### 빌드가 잡지 못하는 제약

어겨도 빌드가 통과하고 배포까지 된다.

| 제약 | 어기면 |
| --- | --- |
| **App Router 를 쓰지 않는다** | Next.js 는 `app/` 을 거부하지 않지만 이 프로젝트의 전제가 깨진다 |
| **경로 별칭** `@/lib/...` · `@/components/...` | 상대 경로도 빌드를 통과한다 |
| **`tsconfig.json` 은 동결** | `target` 을 바꾸면 전체가 재방출되어 「기존 페이지 불변」이 깨진다. 타입 오류는 호출부에서 고친다 |
| 한글 본문에 **`break-keep`**, 새 컴포넌트마다 **`dark:`** | 줄바꿈과 다크모드가 깨진다 |
| 🔴 포털로 그려지는 것(`Dialog` 등)은 **색을 스스로 지정한다.** `styles/globals.css` 의 `body` 에 둔 `color: hsl(var(--foreground))` 를 지우지 마라 | `dark:` 짝 검사는 **색 클래스가 아예 없는 자리를 못 본다.** 포털은 `blog-shell` 바깥에 그려져 `body` 색을 상속하므로, `DialogContent` 에 `text-foreground` 가 빠지자 검색 팔레트가 **대비 1.06** 으로 배포됐다. `body` 색 한 줄이 이를 7.72 로 닫으며, 지우면 결함이 조용히 되살아난다 |

## 이 환경의 도구 함정

내용 규칙이 아니라 **도구가 거짓을 보고하는 방식**이며 전부 실제로 겪었다. 여기에는 공통 원리로 막을 수
없는 개별 지뢰만 두고, 재현 절차와 실측값을 담은 전체 목록은 [`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md) 에 있다.
매 세션 읽지 말고 **도구가 설명되지 않는 결과를 낼 때** 읽어라. 행을 넣을 때는 뺄 행을 함께 고른다.

| 함정 | 실제로 일어나는 일 | 대응 |
| --- | --- | --- |
| **파이프 뒤의 종료 코드** | `$?` 는 **마지막** 명령의 것이라 `grep … \| uniq -c` 는 항상 0 | 종료 코드를 읽을 명령은 **단독 실행** |
| **한국어 1인칭** | 양방향으로 실패한다. 「동시성 문**제가**」는 거짓 양성, 「**내** 검색 커리어」는 거짓 음성 | 걸린 줄을 열어 읽는다. **1인칭을 검사기에 넣지 마라** |
| **한국어 종결어미는 줄 끝에 없다** | 「…있습니다.」는 마침표로 끝나 `습니다$` 가 아무것도 매칭하지 않는다 | 앵커가 아니라 **부분 문자열**로, 줄이 아니라 **출현 횟수**를 센다. 0 이 아님을 아는 절과 대조한다 |
| **마크다운 강조가 낱말을 쪼갠다** | `**IDOL**을` 은 `IDOL을` 과 매칭되지 않는다 | 강조를 벗긴 패턴으로도 검색한다 |
| **로케일이 한글과 이모지를 숨긴다** | `LC_ALL=C` 없이는 같은 이모지 패턴이 **9와 0** 으로 갈린다. `grep -P '[\x{1F300}-\x{1FAFF}]'` 는 "range out of order" 로 죽는데 출력 없음은 발견 없음과 똑같아 보인다 | `LC_ALL=C` 고정 · 이모지는 UTF-8 바이트 패턴으로. `\|\| echo "0건"` 을 붙이지 마라 |
| **정규식으로 마크다운의 의미를 세면 양방향으로 틀린다** | 코드 블록 안의 예시가 위반으로 올라와 **31건 전부 거짓 양성**이었고, `^\s*` 가 인용 블록 안의 도식을 통째로 놓쳐 135 대 파서 **136** 이었으며(함정 56), 종류가 섞인 집합에서 「지정되지 않은 것」을 세니 속성이 없는 종류까지 들어와 152 대 **137** 이었다(함정 58) | 마크다운의 의미를 묻는 검사는 파서로. 도식 수는 `check-mermaid.mjs` 의 `extractDiagrams` 를 부른다. **위반이 나왔을 때도 몇 건을 열어 확인한다.** 두 방법이 일치한 것은 입력의 성질이지 방법의 타당성이 아니다 |
| **브라우저용 라이브러리를 Node 에서 부르면 검사가 조용히 비어 버린다** | `mermaid.parse()` 는 DOM 이 없으면 `DOMPurify.addHook is not a function` 으로 죽는다. 이를 「위반 없음」으로 세면 **649개 중 621개**를 보지 않고 초록이 난다 | 환경 오류(`TypeError` · `ReferenceError`)를 **열거**하고 나머지를 위반으로 둔다. **판정에 도달한 수**를 대상 수와 대조하고, 반드시 통과해야 할 **카나리** 하나를 스캔 앞에 둔다 |
| **`npm install` 이 `engines` 를 강제하지 않는다** | 로컬 Node 에서만 만족되는 의존성이 **경고 없이** 설치되고 **CI 에서만 죽는다.** 오류 메시지가 원인을 가리키지 않는다 | `check-engines` 가 설치본 **전량**을 본다. 직접 의존성으로 좁히지 마라. CI 를 죽인 넷은 전부 transitive 였다. 검사기 전용 의존성은 낮은 요구를 가진 버전을 고른다 |
| **`git ls-files` 로 대상을 모으면 두 갈래로 조용히 빠진다** | ① `core.quotePath` 기본값이 한글 경로를 따옴표로 감싸 끝이 `.md` 가 아니게 된다(54개 중 50개만 스캔). ② untracked 파일은 색인에 없어 목록에 오르지 않는다(62개 위반 0 이 `git add` 뒤 63개 위반 2) | ①은 `git -c core.quotePath=false ls-files`. ②는 새 `.md` 를 **스테이징한 뒤에** 검사한다. 공통으로 **직전 실행의 스캔 수와 대조**한다 |
| **낱말 안쪽의 부분 일치** | `grep -o '비용'` 은 「대**비용**」도 센다 | 걸린 줄을 열어 확인한다 |
| **`dup-scan` 의 사각지대** | 「같은 뜻, 다른 낱말」을 보지 못한다 | 문자열뿐 아니라 **의미로도** 대조한다 |
| **셸과 `sed` 가 백슬래시를 한 겹씩 먹고, 백틱은 세로줄을 보호하지 않는다** | 표 셀의 `\|` 가 벗겨져 셀 3개짜리 행이 6개로 갈라졌다. 어긋난 행 넷 중 셋은 코드 스팬 안의 세로줄이었다 | `Write`/`Edit` 로 쓰고 **넣은 뒤 파일을 다시 읽는다.** 판정은 `check-table` 이 한다 |
| **CRLF 는 grep 도 diff 도 숨긴다** | grep 은 CR 을 줄 끝으로 처리해 CR 393개인 파일을 「LF 전용」으로 보고한다. `core.autocrlf=true` 는 diff 전에 정규화해 통째로 재작성한 파일을 깨끗하게 보인다. 워크트리 생성도 체크아웃이라 새 워크트리에서 첫 `mutate` 가 개행을 담은 뮤턴트 7개를 치환 실패로 냈다 | `tr -cd` 로 CR 을 센다. 깨끗한 diff 는 증거가 아니다. **치환 실패를 코드 변경으로 읽기 전에 CR 을 세라** |
| **서브에이전트가 보고 없이 유휴가 된다** | `reviewer` · `scout` 에는 `Write` 도구가 없다 | 파일로 판단한다. `SendMessage` 로 끌어오되 **원래 질문을 그대로** 다시 보낸다 |
| **`grep -r` 이 `out/` 과 `node_modules` 를 훑는다** | 120초 타임아웃 | `Grep` 도구 또는 `--include` · 명시 경로 |
| **`compose` 통과는 배정 타당성이 아니다** | 성분 실측은 인용을 한 성분으로 셀 뿐이다. `pm-po` 배정의 14.6% 가 금칙어를 담은 경력 메모였는데 예산이 확정된 뒤에야 드러났다 | 성분 실측 **바로 다음에 배정 원본의 금칙어를 직접 센다.** `LC_ALL=C` 필수 |
| **크기가 같아도 산출물이 다르다** | Next.js 청크 파일명의 콘텐츠 해시가 빌드 환경에 따라 갈리는데 길이가 고정이라 **파일 크기는 바이트까지 일치한다.** 이 한 자리 때문에 `check-baseline` 이 「환경마다 다르다」는 진단을 얻고 1년 가까이 꺼져 있었다 | 크기가 아니라 `cmp` 로 첫 차이 위치를 잡아 읽는다. 배포된 페이지가 곧 CI 산출물이므로 같은 커밋이면 `curl` 로 받아 대조한다 |
| **awk `length()` 는 바이트가 아니다** | 문자 수를 센다. 한글 블록이 `length()` 3,903 대 `wc -c` **10,065** 로 어긋났는데 그럴듯한 표가 나왔다 | 바이트는 `wc -c` 로 재고, 예산에 넣기 전 **두 방법으로 교차 검증** |

## 배포

- **프리뷰 환경이 없고 `main` 이 곧 프로덕션이다.** 푸시 전에는 pre-commit 훅을 통과시키고, 푸시 뒤에는 배포 run 의 성공을 확인한다.
- 🔴 **CI 의 Node 는 22 이고 내리지 않는다.** 22 로 올린 근거였던 발표본 갈래는 2026-09-12 에 걷어냈지만, Node 를 바꾸면 산출물이 달라져 GC-6(비블로그 페이지 불변)이 빨간불이 된다.
- `check-engines` 가 이 자리를 판정한다. CI 가 `npm ci` 를 돌리는 디렉터리와 검사기의 대상 목록이 어긋나면 자기 검사가 걸린다. **「이 사례를 잡는가」는 되돌려 봐야 안다.** 직접 의존성만 보는 판은 `node-version` 을 20 으로 되돌려도 위반 0 을 냈고, 전량으로 재니 CI 를 죽인 transitive 넷이 정확히 나왔다. 발표본 트리를 걷어낸 뒤 대조군은 Node 16 으로 낮췄다. 20 에서는 위반이 0 이라 헛돌기 때문이다.
