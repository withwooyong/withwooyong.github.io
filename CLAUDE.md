# CLAUDE.md

Claude Code 가 이 리포에서 작업할 때 참고하는 지침이다.

## 명령

| 명령 | 설명 |
| --- | --- |
| `npm run dev` | 개발 서버 (http://localhost:3000) |
| `npm run build` | 정적 산출물을 `out/` 에 만든다. 별도의 `next export` 단계는 없다 |
| `npm run start` | 프로덕션 빌드 서빙. 정적 export 라 쓸 일이 드물다 |
| `npm test` | `tests/blog/` 를 Vitest 로 돌린다. Vitest 는 타입을 **지울 뿐 검사하지 않으므로** `npx tsc --noEmit` 을 **따로** 돌려야 한다 |
| `npm run lint` | `next lint` |

### 발행 전 검사 — `content/blog/` 를 건드릴 때마다

| 명령 | 무엇을 검사하나 |
| --- | --- |
| `npm run check-forbidden:verify` | **가장 먼저.** 금칙어 스캐너가 실제로 잡는지 증명한다 |
| `npm run check-forbidden` | `content/blog` 스캔. 발행 전 **HARD 0** 이어야 한다 |
| `npm run check-forbidden:built` | **빌드 산출물**(`out/blog` + `_next/data`) 스캔. 빌드 뒤에 돌린다. 템플릿이 `og:image` 와 제목을 주입하므로 **소스가 깨끗해도 페이지는 오염될 수 있다.** 산출물이 없으면 거짓 0 대신 종료 코드 2 |
| `npm run check-markup:verify` → `npm run check-markup` | 🔴 **렌더되지 않는 강조.** 기존 검사 다섯은 낱말과 바이트만 보고 **마크업 문법을 보지 않아** 별표가 화면에 찍힌 편이 75개·306회 쌓였다. 판정은 정규식이 아니라 **페이지를 그리는 파서**가 한다 — 코드 블록 안의 정당한 예시 109회가 저절로 빠진다. 기전이 둘이라 정규식으로는 반쪽만 잡힌다 |
| `npm run check-markup:docs` | 🔴 **발행본 밖의 리포 문서.** 위 명령은 `content/blog/` 만 보므로 설계서·인수인계·`CLAUDE.md` 는 아무도 보지 않았고, 그 사이 **250회**가 쌓였다. 대상 수집은 검사기가 직접 한다 — 호출부에서 `git ls-files` 로 뽑아 넘기지 마라. `core.quotePath` 기본값이 한글 경로를 따옴표로 감싸는 바람에 54개 중 **4개가 조용히 빠진** 적이 있다 |
| `npm run check-links:verify` → `npm run check-links` · `npm run check-links:docs` | 🔴 **깨진 링크.** 발행본은 `/blog/<카테고리>/<슬러그>/` 만 유효하고 상대 `.md` 는 정적 export 에서 **반드시 404** 다. 문서는 그 파일이 실제로 있어야 하며 **기준은 리포 루트가 아니라 그 문서의 디렉터리**다 — 실측 22곳이 루트 기준으로 적혀 있었다. 판정은 파서가 한다: 정규식으로 세면 코드 블록 안의 예시가 위반이 되고 앵커를 벗기지 않으면 멀쩡한 편이 위반이 된다 (실제로 둘 다 겪었다) |
| `npm run check-mermaid:verify` → `npm run check-mermaid` · `npm run check-mermaid:docs` | 🔴 **그려지지 않는 도식.** 리포에 Mermaid 블록이 **667개**(발행본 544 · 문서 123) 있는데 문법이 어긋나면 화면에는 흐름도 대신 빨간 오류 상자가 뜬다. 판정은 정규식이 아니라 **화면을 그리는 mermaid 파서**가 한다. 🔴 위험은 위반을 놓치는 것이 아니라 **거짓 0** 이다 — Node 에는 DOM 이 없어 파싱에 닿기도 전에 죽는데 그것을 「위반 없음」으로 세면 초록이 나온다. **실측 649개 중 621개가 그렇게 빠졌다.** 그래서 검사기는 판정에 **도달한** 수를 함께 세고, 카나리 도식 하나를 먼저 파싱해 파서가 살아 있는지 본다. 못 미치면 종료 코드 2 |
| `npm run fix-markup:verify` → `npm run fix-markup -- --category <slug>` | 위 위반의 교정. 조사를 강조 **안으로** 옮긴다. **알려진 조사일 때만** 자동이고 아니면 손볼 자리로 남긴다 — 기계가 조사의 경계를 안다는 전제를 두지 않는다. `--dry` 로 먼저 본다. 바이트가 보존되므로 **분량 예산과 역산 배율이 흔들리지 않는다** |
| `npm run dup-scan:verify` → `npm run dup-scan -- --category <slug>` | 축자 복제 스캔. 순서는 같다 — 증명한 뒤 스캔. 대상이 없으면 종료 코드 1. 배치를 통째로 넘겨도 안전하다 |
| `npm run source-overlap:verify` → `npm run source-overlap -- <발행본> <원본>` | **원본** 대조. `dup-scan` 은 발행본끼리만 보므로 리포 밖 원본과의 겹침은 여기서만 잡힌다. 공백 보존·공백 제거 두 정규화를 모두 돌리고, 도식 라벨은 임계값이 아니라 **통째 일치**로 판정한다 |
| `npm run check-counts:verify` → `npm run check-counts` | 🔴 **README 의 발행본 수**가 실제와 맞는지. 이 표에서 빠져 있던 탓에 **편2~7 동안 Pages 배포가 계속 실패했다** — 로컬 빌드도 pre-commit 훅도 이것을 보지 않고 CI 의 `Check documented counts` 에서만 죽는다. 편을 발행했으면 **네 자리**를 같은 커밋에 넣는다 — README 셋(요약표 · 디렉터리 트리 · 카테고리 내역)과 **CHANGELOG 의 「총 N편」** 이다. 🔴 이 항목이 오래 「세 자리」로 적혀 있었고 실제로 그 넷째에서 걸렸다. 자리 수를 문서에서 읽지 말고 **검사기의 출력 끝줄(「검사한 자리 N곳」)에서 읽어라** |
| `npm run search-index:verify` → `npm run search-index` | 🔴 **검색 인덱스.** `out/_next/data` 의 `post.toc` 를 재사용해 `out/blog/search-index.json` 을 만든다 — 소스를 다시 파싱하지 않으므로 헤딩 텍스트와 앵커 id 의 진실원이 하나다. `npm run build` 안에서 sitemap 다음에 돈다. 가드 다섯이 **대상 없음 · buildId 모호(디렉터리가 둘 이상이면 사전순 첫 항목을 고르지 않고 종료 2) · 수 대조(184 = 184) · 카나리 · `decideExit` 순수 함수**를 본다. 루트가 아니라 `out/blog/` 아래에 두는 이유는 `check-forbidden --built` 의 스캔 범위이기 때문이다 — 루트에 두면 제목·설명·헤딩 2,671개가 금칙어 검사를 우회한다 |
| `npm run compose -- <발행본>` | **복제를 지운 뒤 다시 돌린다.** 겹침을 피해 고쳐 쓴 문장은 원래보다 길어져 분량이 는다 — 실측 **+232 B** 로 밴드 상단 여유가 296 에서 **65** 로 줄었다. 밴드를 판정한 값과 실제 발행되는 값이 **다른 파일**이 되는데, 검사도 빌드도 통과하므로 다음 편에서 배율을 역산할 때에야 드러난다 |

**증명하기 전에는 0을 믿지 마라.** 증명되지 않은 「0건」은 거짓 음성과 구분되지 않는다.
실제로 금칙어 목록에 라틴 표기만 있고 한글 표기가 빠져 거짓 0 이 CHANGELOG 에 사실로 기록됐고,
`Ted_yanadoo.png` 가 산출물 366곳에 남은 동안 소스 스캔은 계속 0 을 반환했다.

🔴 **검사기·뮤턴트·훅 단계의 수를 늘렸으면 그 수가 적힌 문서를 같은 커밋에서 고쳐라.**
세 세션 연속으로 낡았다 — 뮤턴트 수(16·38 대 실제 48) · 훅 단수(7단 대 9단) · 자기 검사
건수 다섯 자리(15·15·7·28·여덟 대 63·63·12·42·아홉). **문서의 수치도 대조군 없이는 근거가
아니다.** 특히 `dup-scan`·`fix-markup` 처럼 **검사기가 요약 숫자를 내지 않는 자리**가 낡는다.
이번에도 `scripts/` 검사기가 아홉 종에서 **열 종**(`build-search-index` 추가)으로, 뮤턴트가
50개에서 **58개**로, `tests/blog` 가 5파일 54케이스에서 **8파일 87케이스**로 늘며 세 수가
`CLAUDE.md`·`README.md`·`HANDOFF.md` 에 갈라져 있었다 — 「검사기 종 수」와 「뮤테이션이 돌리는
검사 수(그중 `check-counts` 는 빠지고 `blog-unit` 이 더해진다)」는 애초에 다른 수이므로 나눠 썼다.
그 뒤 지역 그래프 위젯이 뮤턴트를 **65개**로, `tests/blog` 를 **11파일 126케이스**로 늘렸고,
LOW 3건을 닫은 세션이 다시 뮤턴트를 **74개**로, `tests/blog` 를 **13파일 155케이스**로 늘렸고,
도식 팔레트 세션이 다시 뮤턴트를 **92개**로 늘렸고, 카테고리 그래프 세션이 다시 뮤턴트를
**96개**로, `tests/blog` 를 **14파일 194케이스**로 늘렸고, 도식 라벨 잘림을 닫은 세션이
다시 뮤턴트를 **98개**로, `tests/blog` 를 **14파일 200케이스**로 늘렸다
(검사기 종 수와 뮤테이션이 돌리는 검사 수는 각각 **열 종**·**열 개**로 그대로다).
세 수를 이어받지 말고 **매번 세라** — 위 실패는 전부 세지 않고 이어받은 것이다.

🔴 **바로 위 문단에 적혀 있던 「14파일 175케이스」가 그 실패의 최신 사례였다.**
세 문서가 나란히 175 라고 적고 있었지만 작업 전 기준선을 `git stash` 로 되돌려 실제로
재니 **180** 이었다. 파일 수만 맞고 케이스 수는 아무도 세지 않은 채 옮겨 적힌 것이다.
⇒ **케이스 수는 `it(` 를 grep 하지 말고 `npx vitest run` 의 요약 줄에서 읽어라.** 실측으로
grep 이 179, 러너가 180 이었다 — 여러 줄에 걸친 선언 하나를 정규식이 보지 못한다.

🔴 **「뮤테이션이 돌리는 검사에 들어 있다」와 「그 검사가 내 새 케이스를 본다」는 다른 말이다.**
`blog-unit` 은 `tests/blog` 를 통째로 돌리지 않고 **파일을 이름으로 나열**한다. 그것을 모른 채
테스트 파일 셋을 새로 추가했더니, 새 뮤턴트 아홉 중 **여덟이 생존했다** — 뮤턴트가 되살린 결함을
볼 케이스가 아예 실행되지 않았기 때문이다. 같은 실행에서 **id 가 겹친 것**도 함께 드러났다
(`C1`~`C4` 가 `check-forbidden` 계열에 이미 있었다). ⇒ **테스트 파일을 새로 만들었으면
`scripts/mutate.mjs` 의 `CHECKS` 에서 `blog-unit` 이 그것을 나열하는지 보고, 새 뮤턴트 id 가
기존과 겹치지 않는지 `sort | uniq -d` 로 확인하라.** 케이스를 기존 파일에 더할 때는 해당 없다.

**금칙어 목록의 정본은 `scripts/check-forbidden.mjs` 다.** 문서로 복사하지 마라 — 목록이 코드와
문서로 갈라진 것이 위 실패의 원인이었다.

🔴 **자기 증명이 통과해도 증명하지 않은 동작은 미검증이다.** `source-overlap` 은 자기 검사
12/12 를 내면서도 ②가 **공백만** 지웠는데 출력에는 「`dup-scan` 과 같은 정규화」라고 찍었다.
원본 문장을 그대로 복사한 뒤 `**` 두 쌍만 씌우면 ②가 27자에서 **0건**으로 떨어졌다.
⇒ **검사기가 출력하는 라벨도 자기 검사 항목이다.** 「무엇을 한다」고 적어 둔 것과 실제 동작이
같은지를 케이스로 고정하라. 고친 뒤에는 **정규화를 옛 판으로 되돌려 그 케이스가 실제로
FAIL 하는지** 확인한다 — 통과만 보고는 케이스가 헛도는지 알 수 없다.

### 되돌려 보는 일은 손으로 하지 않는다 — `npm run mutate`

| 명령 | 무엇을 하나 |
| --- | --- |
| `npm run mutate:verify` | **먼저.** 러너 자신이 작동하는지 — 뮤턴트가 실제로 적용되고, 검사 뒤 파일이 **바이트까지** 복구되는지 |
| `npm run mutate` | 알려진 결함 **98개**를 하나씩 되살려 검사 **열 개**(스크립트 검사기 열 종 중 `check-counts` 를 제외한 아홉 + `blog-unit` Vitest)의 자기 검사가 잡는지 본다. **생존이 하나라도 있으면 종료 코드 1** |

**검사기를 고쳤으면 이것을 돌려라.** 자기 검사의 통과는 「케이스가 있다」만 말할 뿐
「그 케이스가 무언가를 지킨다」는 말하지 않는다. 실측으로 `source-overlap` 은
임계값을 20 → 200 으로 바꿔 실제 스캔이 52자 축자 복사를 0건으로 보고하는 동안에도
**28/28 을 그대로 냈고**, `dup-scan` 은 공용 정규화를 통째로 무력화해도 **7/7 을 냈다.**

⚠️ **「치환 실패」는 러너의 고장이 아니라 신호다.** 뮤턴트의 대상 문자열은 실제 코드 조각이라,
코드를 고치면 못 찾는다. 그때는 **그 자리를 다시 볼 때가 된 것이므로** 종료 코드 1 로 다룬다.

⚠️ **없는 것을 소스에 리터럴로 적으면 그 순간 존재하게 된다.** 러너의 자기 검사가 「없는
문자열」을 코드에 적었다가 자기 자신에 매칭돼 헛돌았고, 같은 실수를 `source-overlap` 의 ⑰-b
(케이스 **이름**에 상수 이름을 적어 자기 자신을 매칭)에서도 했다. 이름의 등장이 아니라
**호출의 인자**를, 리터럴이 아니라 **런타임 조합**을 보라.

⚠️ **필터를 통과한 집합으로 그 필터를 검사할 수 없다.** 「수집한 경로가 전부 `.md` 로
끝난다」는 케이스가 자기 검사 21/21 안에서 계속 PASS 였는데, `core.quotePath` 를 켜면
한글 경로는 **있지도 않은 이름**이 되어 `existsSync` 에서 걸러지고 남은 것은 정의상
조건을 만족한다 (실측 54개 대 50개). ⇒ **대조할 것이 실제로 있는지를 먼저 세라.**
그리고 **가드는 `main` 이 아니라 순수 함수로 뽑아라** — 흩어져 있으면 통째로 지워도
케이스가 전부 통과한다.

## 게이트

**발행 규칙은 산문이 아니라 검사기에 산다.** 누적 규칙 105개를 전수 분류한 결과
(`docs/superpowers/reports/2026-08-18-rule-triage.md`) 규칙이 살 자리는 넷이다.

| 규칙이 사는 곳 | 무엇이 들어가나 |
| --- | --- |
| 검사기 (`scripts/`, `tests/blog/`) | 기계가 판정하는 것 — 스키마, 금칙어, 링크, 분량, 산출물 |
| `docs/superpowers/PUBLISHING-CHECKLIST.md` | 사람의 판단만 — 출처 표기, 배정 누락, 삭제 여파, 익명화, 도식 근거, 어조 |
| 이 파일 | 도구가 실패하는 방식과, **빌드가 강제하지 않는** 코드 제약 |
| 설계서 `§11` (취소선) | 배치와 함께 만료된 규칙. 다른 문서가 번호로 인용하므로 남긴다 |

두 방향 모두 금지다. **검사기의 판정 기준을 문서로 복사하지 말고**, 배치 한정 지시를 영구 규칙
목록에 넣지 마라. 영구 규칙은 **모든** 배치에서 참이어야 한다 (105개 중 14개가 탈락했다).

`.githooks/pre-commit` 이 커밋의 내용에 따라 두 갈래로 갈린다. `npm install` 이 훅을 연결하며,
손으로 하려면 `git config core.hooksPath .githooks` 다.

| 커밋이 건드린 것 | 훅이 도는 것 | CI 가 도는 것 |
| --- | --- | --- |
| `content/blog/**.md` | 9단 — 금칙어 증명·불변식·금칙어 스캔·마크업 증명·마크업 스캔·링크 증명·링크 스캔·도식 증명·도식 스캔 | 좌동 + 빌드 산출물 스캔 · 개수 검사 |
| 그 밖의 `**.md` | 마크업 증명 → `--docs` 스캔 → 링크 증명 → `--docs` 스캔 → 도식 증명 → `--docs` 스캔 | 좌동 |
| 코드만 | 없음 — 즉시 통과 | 린트 · 타입 · 빌드 |

**네 검사 모두 스테이징된 파일이 아니라 전량을 본다.** 좁게 보면 깨뜨린 쪽이 아니라
**깨진 쪽**이 통과한다 — 편을 지우거나 슬러그를 바꾸면 그 편을 가리키던 다른 편이 깨지는데,
정작 커밋이 건드린 파일에는 아무 문제가 없기 때문이다.

**훅만 있는 검사는 지켜지지 않는 검사다.** `--no-verify` 로 지나갈 수 있고 웹 UI 편집은 애초에
훅을 타지 않는다. 마크업 검사가 훅에만 있고 CI 에는 없던 것이 그 상태였다 — 지금은 둘 다 있다.
빌드가 필요한 검사는 CI 에만 둔다.

🔴 **CI 가 언제 도는지가 검사 목록만큼 중요하다.** 트리거가 `push: [main]` 뿐이던 동안
23스텝은 **merge 된 뒤에야** 돌았고, 이 리포에는 프리뷰 환경이 없어 `main` 이 곧 프로덕션이다 —
PR 단계에서 걸러야 할 것이 프로덕션에서 드러나는 구조였다. 실제로 다크 모드 대비 1.06 결함이
그 경로로 배포됐다. 지금은 `pull_request: branches: [main]` 이 함께 걸려 있고, `deploy` job 과
`Upload artifact` 스텝만 `github.event_name != 'pull_request'` 로 빠진다.
⇒ **검사를 CI 에 추가했다면 그것이 PR 에서도 도는지 확인하라.** 「CI 에 있다」는 「merge 전에
돈다」와 다른 말이다.

🔴 **2단의 「불변식」이 `npx vitest run tests/blog` 다 — 검사기는 `scripts/` 에만 살지 않는다.**
이름이 Vitest 를 가리킨다는 것이 드러나지 않아 거기 사는 검사가 **없는 것으로 세어진 적이
있다**: 앵커 실존 검사가 이미 훅과 CI 양쪽에서 도는 동안 「남은 사각지대」 목록에 세 세션을
머물렀다. **검사를 찾을 때 `scripts/` 만 훑지 마라.** `tests/blog/content/` 의 **31케이스**가
발행본 전량을 본다 (링크 무결성 · 고립 · **앵커 실존** · 스키마 · 분량 · 시리즈 · 링크 지형). 슬러거처럼
TypeScript 부품이 필요한 판정은 구조적으로 이쪽에만 살 수 있다.

### 빌드가 잡지 못하는 제약

어겨도 빌드가 통과하고 배포까지 된다.

| 제약 | 어기면 |
| --- | --- |
| **App Router 를 쓰지 않는다** | Next.js 는 `app/` 을 거부하지 않지만 이 프로젝트의 전제가 깨진다 |
| **경로 별칭** `@/lib/...` · `@/components/...` | 상대 경로도 빌드를 통과한다 |
| **`tsconfig.json` 은 동결** | `target` 을 바꾸면 전체가 재방출되어 「기존 페이지 불변」이 깨진다. 타입 오류는 호출부에서 고친다 |
| 한글 본문에 **`break-keep`**, 새 컴포넌트마다 **`dark:`** | 줄바꿈과 다크모드가 깨진다 |
| 🔴 포털로 그려지는 것(`Dialog` 등)은 **색을 스스로 지정한다** | `dark:` 짝 검사는 **색 클래스가 아예 없는 자리를 못 본다** — 짝지을 대상이 없기 때문이다. `DialogContent` 에 `bg-background` 만 있고 `text-foreground` 가 없어, 검색 팔레트의 편 제목 20개와 **입력창 자신**이 `rgb(0,0,0)` 대 `rgb(10,10,10)` 으로 **대비 1.06** 이 된 채 배포됐다. 포털은 `blog-shell` 의 `dark:text-slate-100` 바깥에 그려지므로 `body` 색을 상속받는다. ⇒ **원인은 `styles/globals.css` 의 `body` 에 `color: hsl(var(--foreground))` 를 두어 닫았다.** 실측으로 그 한 줄만으로 팔레트가 대비 **1.06 → 7.72**(입력창 18.97)가 된다. 그래도 이 행을 남기는 이유는 **`body` 색을 지우면 결함이 조용히 되살아나기 때문이다** |
| **커밋 메시지는 한글** · `git push` 는 명시적 요청이 있을 때만 | — |
| 🔴 **문서 갱신만을 위한 PR 을 열지 마라** | 그 PR 자신의 merge 커밋과 배포 run 은 어느 문서에도 적히지 못해 기록이 영구히 한 칸 뒤처진다. PR #14·#15 가 연속으로 그랬다. 문서 갱신은 **실질 작업의 PR 에 함께 실어라** |

## 이 환경의 도구 함정

내용 규칙이 아니라 **도구가 거짓을 보고하는 방식**이며 전부 실제로 겪었다. 아래는 요약이고,
재현 절차와 실측값을 담은 전체 49건은 [`docs/TOOL-TRAPS.md`](docs/TOOL-TRAPS.md)(1,272줄)에 있다.
매 세션 읽지 말고 **도구가 설명되지 않는 결과를 낼 때** 읽어라.

| 함정 | 실제로 일어나는 일 | 대응 |
| --- | --- | --- |
| **파이프 뒤의 종료 코드** | `$?` 는 **마지막** 명령의 것이라 `grep … \| uniq -c` 는 항상 0 | 종료 코드를 읽을 명령은 **단독 실행** |
| **한국어 1인칭** | 양방향으로 실패한다. 「동시성 문**제가**」는 거짓 양성, 「**내** 검색 커리어」는 거짓 음성. `내 [가-힣]{2,6}` 로 넓히면 「인덱스 **내** 문서」가 쏟아진다 | 걸린 줄을 열어 읽는다. **1인칭을 검사기에 넣지 마라** |
| **한국어 종결어미는 줄 끝에 없다** | 「…있습니다.」는 마침표로 끝나 `습니다$` 가 **아무것도** 매칭하지 않고 구조적 0 을 낸다 | 앵커가 아니라 **부분 문자열**로, 줄이 아니라 **출현 횟수**를 센다 (한 줄에 둘이 온다). 0 이 아님을 아는 절과 대조한다 |
| **마크다운 강조가 낱말을 쪼갠다** | `**IDOL**을` 은 `IDOL을` 과 매칭되지 않는다. 조사가 붙는 한국어에서 구조적으로 발생한다 | 강조를 벗긴 패턴으로도 검색한다 |
| **로케일이 한글과 이모지를 숨긴다** | `LC_ALL=C` 없이는 이모지 grep 이 거짓 0 을 낸다 — 같은 패턴이 로케일 유무에 따라 **9와 0** 으로 갈렸다. `grep -P '[\x{1F300}-\x{1FAFF}]'` 는 "range out of order" 로 죽는데, 출력 없음은 발견 없음과 똑같아 보인다 | `LC_ALL=C` 고정 · 이모지는 UTF-8 바이트 패턴으로. `\|\| echo "0건"` 을 붙이지 마라 — 「파일 없음」과 「0건」이 같아진다 |
| **링크·마크업을 정규식으로 세면 없는 위반이 생긴다** | 코드 블록 안의 예시가 위반으로 올라오고, 앵커를 안 벗기면 멀쩡한 편이 「없는 편」이 된다. 실측으로 **31건이 전부 거짓 양성**이었다 — 0건만 의심하는 습관으로는 잡히지 않는다 | 마크다운의 **의미**를 묻는 검사는 파서로. **위반이 나왔을 때도 검사기를 의심하고** 몇 건을 열어 확인한 뒤 고치러 간다 |
| **브라우저용 라이브러리를 Node 에서 부르면 검사가 조용히 비어 버린다** | `mermaid.parse()` 는 DOM 이 없으면 파싱에 닿기도 전에 `DOMPurify.addHook is not a function` 으로 죽는다. 이 실패를 「위반 없음」으로 세면 **649개 중 621개**를 보지 않고 초록이 나온다. 파서 오류(`Error`)와 환경 오류(`TypeError`·`ReferenceError`)가 **다른 종류**인데 `catch` 한 곳에서 뭉개진다 | 환경 오류 쪽을 **열거**하고 나머지를 위반으로 둔다 (반대로 두면 새 오류 형태가 위반을 삼킨다). **판정에 도달한 수**를 세어 대상 수와 대조하고, 반드시 통과해야 할 **카나리** 하나를 스캔 앞에 둔다 |
| **`npm install` 이 `engines` 를 강제하지 않는다** | 로컬 Node 에서만 만족되는 devDependency 가 **경고 없이** 설치되고 **CI 에서만 죽는다.** `jsdom@30`(`^22.22.2 \|\| ^24.15.0 \|\| >=26.0.0`)이 로컬 Node 24 에서 21/21 을 내고 CI 의 Node 20 에서 죽어 배포가 실패했다. 오류 메시지(`webidl.util.markAsUncloneable is not a function`)가 원인을 가리키지 않는다 | **설치된 패키지의 `engines` 와 워크플로의 `node-version` 을 대조하는 케이스를 자기 검사에 둬라** (`check-mermaid` 의 ㉖). 검사기 전용 의존성은 **낮은 요구를 가진 버전**을 고른다 |
| **`git ls-files` 로 대상을 모으면 두 갈래로 조용히 빠진다** | ① `core.quotePath` 기본값이 한글 경로를 `"pages/…\355\227\210….md"` 로 내보내 끝이 `.md` 가 아니게 된다 — **54개 중 50개만** 스캔하고 종료 코드 0 을 냈다. ② 🆕 **untracked 파일은 색인에 없어 애초에 목록에 오르지 않는다** — 방금 쓴 설계서를 두고 `--docs` 세 검사가 **직전과 같은 59개**를 스캔하고 「위반 0」을 냈다 | ①은 `git -c core.quotePath=false ls-files`. ②는 **`git add` 뒤에 검사한다.** 공통으로 **직전 실행의 스캔 수와 대조하라** — 파일을 더했는데 수가 그대로면 그 0은 결론이 아니다 |
| **낱말 안쪽의 부분 일치** | `grep -o '비용'` 은 「대**비용**(對比用)」도 센다 | 걸린 줄을 열어 확인한다 |
| **`dup-scan` 의 사각지대** | 「같은 뜻, 다른 낱말」을 보지 못한다 | 문자열뿐 아니라 **의미로도** 대조한다 |
| **히어독이 백슬래시 한 겹을 먹는다** | `<<'EOF'` 에서도 `\\` 가 `\` 로 도착해 `SyntaxError` 를 낸다. 홑겹은 살아남고 겹겹만 벗겨진다 | `Write`/`Edit` 로 쓰거나 `String.fromCharCode(92)` 로 만든다 |
| **grep 은 CRLF 를 탐지하지 못한다** | CR 을 줄 끝으로 처리해 CR 393개인 파일이 「LF 전용」으로 보고됐다 | `tr -cd` 로 바이트를 센다 |
| **`core.autocrlf=true` 가 줄바꿈 변경을 숨긴다** | git 이 diff 전에 정규화해 통째로 재작성한 파일이 깨끗한 diff 로 보인다. 이 리포는 CRLF 와 LF 가 섞여 있다 | 깨끗한 diff 는 증거가 아니다. 대조군과 CR/LF 를 직접 센다 |
| **서브에이전트가 보고 없이 유휴가 된다** | `reviewer`·`scout` 에는 `Write` 도구가 없다 | 메시지가 아니라 파일로 판단한다. `ls -la <scratchpad>` 뒤 `SendMessage` 로 끌어오되 **원래 질문을 그대로** 다시 보낸다 — 「아까 물어본 대로」는 까다로운 항목부터 잃는다 |
| **`grep -r` 이 `out/` 과 `node_modules` 를 훑는다** | 120초 타임아웃 | `Grep` 도구 또는 `--include` / 명시 경로 |
| **`compose` 통과는 배정 타당성이 아니다** | 성분 실측은 인용을 한 성분으로 셀 뿐 발행 가능한지는 모른다. `pm-po` 배정의 14.6%(10,065 B)가 금칙어를 담은 경력 메모였는데, 편 구성과 예산이 확정된 뒤에야 드러났다 | 성분 실측 **바로 다음에 배정 원본의 금칙어를 직접 센다.** `LC_ALL=C` 필수 |
| **awk `length()` 는 바이트가 아니다** | 문자 수를 센다. 한글 블록이 `length()` 3,903 대 `wc -c` **10,065** 로 2.6배 어긋났는데 오류 없이 그럴듯한 표가 나왔다 | 바이트는 `wc -c` 로 재고, 예산에 넣기 전 **두 방법으로 교차 검증** |

## 아키텍처

GitHub Pages 에 정적 배포되는 한국어 단일 페이지 포트폴리오다 (허우용 / Ted).

| 항목 | 내용 |
| --- | --- |
| **프레임워크** | Next.js 14 · **Pages Router**(`pages/_app.tsx`, `pages/index.tsx`). App Router 가 아니며 `app/` 관례를 도입하지 않는다 |
| **정적 export** | `output: "export"` · `trailingSlash: true` · `images.unoptimized: true`. Node 런타임이 필요한 것(API 라우트, ISR, `next/image` 로더, 서버 액션)은 빌드를 깨뜨린다 |
| **콘텐츠** | 대부분이 `pages/index.tsx`(약 760줄) 하나에 있다. 섹션은 라우트가 아니라 앵커로 연결된다 |
| **UI** | `components/ui/` 아래 shadcn/ui("new-york", neutral) — `badge`·`button`·`card`·`dialog`. 새 컴포넌트도 같은 디렉터리에 넣는다 |
| **스타일** | Tailwind CSS · 커스텀 `primary` 팔레트 · Inter. 전역 CSS 는 `styles/globals.css`. 조건부 클래스는 `lib/utils.ts` 의 `cn()` |
| **경로 별칭** | `@/*` 가 리포 루트다 |
| **정적 자산** | `public/` 아래 (`public/images/Ted_yanadoo.png`, `public/favicon.svg`) |

## 배포

`.github/workflows/deploy.yml` 이 `main` 푸시마다 `npm ci && npm run build` 를 돌리고 `./out` 을
Pages 아티팩트로 배포한다. 워크플로는 하나이며 job 이 둘이다.

| job | 스텝 | 언제 도나 |
| --- | ---: | --- |
| `build` | **23** — 린트 · 타입 · 검사기 열 종 · 빌드 · 산출물 스캔 · 아티팩트 업로드 | `main` 푸시 · **`main` 을 향한 PR** · 수동 실행 |
| `deploy` | 1 — Pages 배포 | `main` 푸시 · 수동 실행 (**PR 에서는 건너뛴다**) |

PR 에서는 `Upload artifact` 스텝과 `deploy` job 이 `github.event_name != 'pull_request'` 로 빠지고
나머지 22스텝은 그대로 돈다. 배포하지 않을 아티팩트를 만들 이유가 없기 때문이다.

**프리뷰 환경이 없고 `main` 이 곧 프로덕션이다.** 가볍게 푸시하지 말고 로컬 빌드를 먼저 확인한다.
