# Changelog

이 저장소의 사용자에게 영향이 큰 변경만 날짜별로 간단히 적습니다. (커밋 해시는 선택적으로 추적합니다.)

## 지난 기록

본체에는 최신 3개 절만 둔다. 그 아래는 월별 파일로 옮겼다.

| 월 | 절 수 | 날짜 |
| --- | ---: | --- |
| [`2026-09`](docs/changelog/2026-09.md) | 45 | 2026-09-13 · 2026-09-12 · 2026-09-10 · 2026-09-10 · 2026-09-09 · 2026-09-09 · 2026-09-09 · 2026-09-09 · 2026-09-09 · 2026-09-08 · 2026-09-07 · 2026-09-07 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-05 · 2026-09-05 · 2026-09-05 · 2026-09-05 · 2026-09-04 · 2026-09-04 · 2026-09-04 · 2026-09-04 · 2026-09-03 · 2026-09-03 · 2026-09-03 · 2026-09-03 · 2026-09-03 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-01 · 2026-09-01 · 2026-09-01 |
| [`2026-08`](docs/changelog/2026-08.md) | 36 | 2026-08-31 · 2026-08-31 · 2026-08-30 · 2026-08-30 · 2026-08-30 · 2026-08-30 · 2026-08-29 · 2026-08-29 · 2026-08-29 · 2026-08-19 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-17 · 2026-08-17 · 2026-08-17 · 2026-08-17 · 2026-08-16 · 2026-08-16 · 2026-08-16 · 2026-08-13 · 2026-08-13 · 2026-08-12 · 2026-08-11 · 2026-08-11 · 2026-08-09 · 2026-08-09 · 2026-08-08 · 2026-08-08 · 2026-08-08 · 2026-08-08 · 2026-08-07 |
| [`2026-07`](docs/changelog/2026-07.md) | 5 | 2026-07-21 · 2026-07-08 · 2026-07-07 · 2026-07-06 · 2026-07-05 |
| [`2026-06`](docs/changelog/2026-06.md) | 1 | 2026-06-02 |
| [`2026-05`](docs/changelog/2026-05.md) | 1 | 2026-05-01 |

---

## 2026-09-17 — 🖼️ **도식을 Mermaid 에서 SVG 로 다시 그리기 시작했다** — 사용자가 실물을 보고 통과시켰다 · 🔍 **도식 확대 뷰어를 화면에 맞추고 제자리 클릭으로 닫는다** · ✅ **SVG 도식 안의 글자도 금칙어 검사를 받는다**

### 사용자 판정이 기준을 정했다

도식마다 색을 조금 얹는 방법(후보 D)은 실물을 본 사용자가 「이전과 다를바가 없어」라며 중단시켰다. 기준은 `multi-agent-system.svg` 처럼 직접 그린 SVG 다. 같은 형식으로 `human-gate-and-model-routing` 편의 상태 다이어그램을 먼저 그려 판정을 받았고, 통과 뒤 같은 편의 시퀀스 다이어그램까지 옮겨 **이 편의 도식 셋이 전부 SVG** 가 됐다.

| 도식 | 파일 | 원문과 같은 것 |
| --- | --- | --- |
| 4절 상태 다이어그램 | `public/images/blog/human-gate-state.svg` | 상태 여섯 · 전이 열두의 라벨 |
| 2절 시퀀스 다이어그램 | `public/images/blog/agent-contract-sequence.svg` | 참여자 여섯 · 호출 16 · `alt` 분기의 라벨 |

같은 날 넓히기 첫 편 `context-budget-session` 의 플로우차트 넷을 옮겼다. 플로우차트를 SVG 로 그린 것은 이번이 처음이다. 판단 노드는 강조색 마름모로, 작업으로 되돌아가는 간선은 점선으로 그렸다.

| 도식 | 파일 | 원문과 같은 것 |
| --- | --- | --- |
| 패턴 A 단계별 업무 분해 | `public/images/blog/task-decomposition-flow.svg` | 노드 넷의 라벨 |
| 3패턴의 진화 경로 | `public/images/blog/prompt-to-agent-evolution-flow.svg` | 노드 넷의 라벨 (「이름 — 설명」을 두 줄로 나눴다) |
| 세 가지 관리 도구 | `public/images/blog/context-management-tools-flow.svg` | 노드 여덟 · 간선 여덟 · 분기 라벨 셋 |
| 세션 연속성의 세 축 | `public/images/blog/session-continuity-flow.svg` | 노드 여섯 · 간선 일곱의 라벨 |

둘째 편은 `context-budget-session` 이 가장 많이 링크하는 `claude-md-scope-layers` 다. 위아래로 층이 쌓이는 도식은 세로로, 덩어리에 단계가 딸린 도식은 세 열의 나무로 그렸다. 원문의 방향 없는 선(`---`)은 화살표 없는 선으로 옮겨 순서를 뜻하는 화살표와 구분했다.

| 도식 | 파일 | 원문과 같은 것 |
| --- | --- | --- |
| 정보는 세 층에서 온다 | `public/images/blog/claude-md-information-layers-flow.svg` | 노드 여덟 · 간선 아홉의 라벨 (「층 — 출처」를 두 줄로 나눴다) |
| 3덩어리 × 7단계 | `public/images/blog/claude-md-three-chunks-flow.svg` | 노드 열 · 화살표 둘 · 방향 없는 선 일곱 |

발행본 Mermaid 는 **565 → 564 → 560 → 558** 이다.

사용자가 「LEGEND 계층이 색상이 유사해서 구분하기 힘들다」고 지적해 플로우차트 SVG 여섯의 노드 색을 종류별로 갈랐다. 종전에는 두 종류가 같은 채움(`#393e53`)에 윤곽선 밝기(`#f5f5f5` 대 `#bfc0c0`)만 달라 도식에서도 범례에서도 거의 같아 보였다.

| 종류 | 종전 | 지금 |
| --- | --- | --- |
| 주 노드(명령 · 파일 · 층 · 덩어리 · 단계) | 채움 `#393e53` · 윤곽 `#f5f5f5` | 파랑 채움 `rgba(122,162,247,.16)` · 윤곽 · 태그 `#7aa2f7` |
| 보조 노드(작업 · 세션 경계 · 세션 층 · 딸린 단계) | 채움 `#393e53` · 윤곽 `#bfc0c0` | 속이 빈 상자(배경색) · 윤곽 · 태그 `#8e98ac` |
| 강조 노드 · 판단 마름모 | 주황 `.10` | 주황 `.14` (색은 같다) |
| 범례 견본 | 16×12 · 선 0.8 | 22×16 · 선 1.5 |

상태 · 시퀀스 다이어그램(`human-gate-state.svg` · `agent-contract-sequence.svg`)과 구조도는 이 지적의 대상이 아니어서 그대로 두었다. 넓히는 순서는 HANDOFF 17번 행에 있다: `agentic-coding` 을 편 하나씩, 편마다 실물을 보이며 간다.

### 고친 것

| 번호 | 무엇 | 확인 |
| --- | --- | --- |
| 18 | 확대 뷰어가 폭 배율과 높이 배율 중 작은 쪽으로 연다 — 7918px 도식이 화면의 4.4배로 열리던 것을 닫았다. 두 뷰어의 중복을 `components/diagram-zoom-dialog.tsx` 와 `lib/diagram-zoom.ts` 로 뽑았다 | `tests/blog/diagram-zoom.test.ts` · 뮤턴트 `ZM1`~`ZM4` |
| 19 | 스크롤 영역을 제자리 클릭(6px 이내)하면 닫는다. 끌기 · 스크롤바 · 툴바는 닫지 않는다 | 실제 마우스로 눌러 봤다 |
| 20 | 캡션과 `aria-label` 이 도식 종류 이름을 말한다 (플로우차트 · 시퀀스 다이어그램 · 상태 다이어그램 · 구조도) | 단위 테스트 |
| 16 | `check-forbidden` 이 `public/images/blog` 와 `out/images/blog` 의 SVG 를 스캔한다 | 자기 검사 67/67 · 뮤턴트 `C6` |

### 🔴 기준선과 개발 서버

- 새 Tailwind 클래스가 전역 CSS 파일명을 바꿔 GC-6 이 비블로그 13건을 냈다. 변경 전 빌드와 대조해 buildId · CSS · webpack 해시 외에 바이트가 같음을 확인한 뒤 기준선을 갱신했다. 시퀀스 도식과 플로우차트 넷을 옮긴 커밋은 클래스를 더하지 않아 GC-6 이 그대로 통과했다.
- `next dev` 가 떠 있는 동안 `npm run build` 를 돌리면 둘이 같은 `.next` 를 써서 **빌드는 청크를 못 찾아 죽고 개발 서버는 500 을 낸다.** 빌드 전에 서버를 끄고 `.next` 를 지운다.

뮤턴트 **126개** · `tests/blog` **15파일 219케이스**.

## 2026-09-16 — 🔄 **구조도를 Mermaid 에서 SVG 파일로 바꿨다** — 발행본 최초의 이미지 도식 · 🔴 **`<p>` 안의 `<figure>` 가 하이드레이션을 202회 깨뜨렸다**(함정 55) · ✅ **이미지 자산 검사 다섯을 더해 사각지대를 닫았다**

> 사용자가 배포본을 보고 말했다. 「만들어진 mermaid 가 내가 원했던 기존 png 파일과 비교해서
> 퀄리티가 너무 떨어지는데.」 실물 둘을 나란히 놓고 보니 맞는 지적이었다.

### 🔴 앞 절의 판단을 뒤집는다

바로 위 절에서 「검사를 받는 쪽은 Mermaid 뿐이다」를 결정 근거의 맨 위에 두었다. **그것은 독자가
아니라 검사기를 위한 기준이었다.** 사용자가 이미 「이미지가 마음에 든다」고 말한 뒤였는데도
검사 커버리지를 그 위에 올렸다.

| 갖춘 것 | Mermaid 배포본 |
| --- | --- |
| 직교 엘보 커넥터, 모서리만 둥글다 | 휘는 베지에 곡선 넷이 오른쪽에서 교차한다 |
| 계층이 세 줄로 갈린다 (사람 / 파이프라인 3 / 지원 2) | 계약·기획 / 코드 / 리뷰·테스트 / 배포 — 계층이 깨졌다 |
| 통제 계약이 **가로 띠**로 전체를 덮는다 | 왼쪽 구석의 육각형 하나로 쪼그라들었다 |
| 타입 태그 여섯 · 구역 이름 셋 · 범례 다섯 | 전부 없다 |
| 초점 노드에 코랄 액센트 | 전부 같은 파랑 — 위계가 없다 |

⇒ **「틀렸을 때 누가 잡는가」는 옳은 질문이지만 첫 질문은 아니다.** 첫 질문은 「독자가 무엇을
보는가」이고, 검사 커버리지는 그 답을 고른 **뒤에** 채워 넣을 문제다. 실제로 그렇게 했다 —
아래의 이미지 자산 검사가 그 채움이다.

### 🔴 함정 55 — `<p>` 안의 `<figure>`

이미지를 넣자 그 편에서만 콘솔에 **React error #418 이 202회** 찍혔다. 화면은 멀쩡해 보인다.
서버 HTML 을 `fetch` 해 `DOMParser` 로 파싱하고 살아 있는 DOM 과 경로별로 대조해 첫 차이를 잡았다.

| | `<figure>` 위치 | 노드 수 |
| --- | --- | ---: |
| 서버 HTML | `/10` — `<p>` 의 **형제** | 820 |
| 브라우저 DOM | `/9/0` — `<p>` **안** | 1171 |

HTML 파서는 `<figure>` 를 만나면 `<p>` 를 먼저 닫는다. 그래서 하이드레이션이 루트째 실패하고
본문 전체가 다시 그려졌다 — 202 는 그때 다시 그려진 노드 수다.

⚠️ **고치려다 한 번 더 틀렸다.** 우회의 판정을 `only[0].type === DiagramImage` 로 했는데,
react-markdown 은 `img` **매핑 함수 자체**를 element 의 type 으로 쓰므로 언제나 거짓이었다.
판정을 마크다운 노드(`node.children` 의 `tagName === "img"`)로 옮겨 닫았다.

⚠️ **개발 서버에서 재면 0 이 나온다.** dev 는 경고 0, 프로덕션 빌드는 202 였다. 그리고 콘솔
버퍼가 페이지를 옮겨도 남아 **배포본이 멀쩡한데 202건으로 보였다** — 대조군을 재기 전에 비우고
되돌아온 메시지의 URL 과 타임스탬프를 확인해야 한다.

### ✅ 이미지 자산 검사를 더했다

`check-links` 는 `/blog/…` 경로만 보고 이미지 경로는 보지 않는다. 산출물 스캔도 `out/images/` 를
훑지 않는다. 그래서 SVG 도식은 **어느 검사도 보지 않은 채** 발행될 뻔했다.

| 새 케이스 | 무엇을 본다 |
| --- | --- |
| 이미지 경로를 뽑았다 | 0건이면 아래 넷이 공허참이 된다 |
| 루트 기준 절대 경로다 | 상대 경로는 `trailingSlash: true` 에서 반드시 404 다 |
| 파일이 `public/` 아래에 실재한다 | 이름이 바뀌면 빌드도 배포도 통과한 채 깨진 아이콘만 남는다 |
| 코드 블록 안의 이미지는 세지 않는다 | 추출기를 정규식으로 되돌리면 실패한다 |
| 🔴 없는 파일을 가리키면 잡는다 — 대조군 | 「실재한다」가 헛도는지 본다 |

**통과만 보고 끝내지 않았다.** SVG 를 치우고 돌려 `이미지 파일이 public/ 아래에 실재한다` 가
실제로 FAIL 하는 것을 확인한 뒤 되돌렸다. `tests/blog` 는 14파일 **205케이스**가 됐다.

### 🔴 그리고 CI 가 GC-6 에서 죽었다 — 본문은 한 글자도 안 바뀌었는데

푸시하니 `Check non-blog baseline` 이 `product-lead-wiki` 5개를 「변경」으로 잡고 배포가
건너뛰어졌다. 배포된 실물(직전 CI 산출물)과 대조해 첫 차이를 잡았다.

| | 배포본 | 로컬 |
| --- | --- | --- |
| 청크 참조 | `chunks/9232-<해시>.js` | `chunks/1121-<해시>.js` |
| 파일 길이 | 111,864 B | 111,864 B |

도식 컴포넌트 하나를 더하자 webpack 이 공용 청크 **번호**를 다시 매겼다. 검사기는 해시
접미사만 지우고 번호는 보므로 걸렸고, 번호가 둘 다 네 자리라 **길이는 바이트까지 같았다** —
함정 53의 다른 얼굴이다.

🔴 **검사기 주석에 적혀 있던 전제가 반증됐다.** 「참조가 바뀔 만한 변경은 렌더된 본문도 함께
바꾸므로 검사는 그쪽으로 잡는다」고 적혀 있었는데, 이번에는 본문이 한 글자도 바뀌지 않았다.
주석을 고치고, 번호를 마스킹하지 **않는** 이유를 함께 적었다 — 지우면 청크 구성 변화를 통째로
못 보게 되는데 그것은 「걸려서 한 번 확인하는 것」보다 나쁘다.

기준선은 바뀐 5자리만 갱신됐고 지켜야 할 나머지 열은 그대로다. Windows 로컬에서 만든 기준선이
ubuntu CI 를 통과한 전례(`61fc2804`)가 있어 청크 번호는 환경에 의존하지 않는다.
### 고친 것

| 파일 | 무엇 |
| --- | --- |
| `public/images/blog/multi-agent-system.svg` | 14,111 B. `docs/multi-agent-system.html` 의 SVG 를 독립 파일로 뽑고, 웹폰트가 로드되지 않는 `<img>` 문맥을 위해 한글 글꼴 스택을 27자리 넓혔다 |
| `components/diagram-image.tsx` | 새 컴포넌트. Mermaid 와 같은 확대 뷰어를 이미지에도 준다. 도식이 1040px 이라 좁은 화면에서 12px 글자가 4px 이 된다 |
| `components/markdown.tsx` | `img` 매핑과 문단 우회 |
| `lib/blog/graph.ts` | `extractImageUrls` — 판정은 정규식이 아니라 파서가 한다 |
| `tests/blog/content/links.test.ts` | 이미지 자산 케이스 다섯 |

🔴 **캐시된 이미지는 `onLoad` 가 발화하지 않는다.** 그러면 자연 폭을 영영 모른 채 뷰어가 「화면
맞춤」으로만 열린다 — **확대가 되지 않는 확대 버튼**이다. ref 가 붙는 순간의 `complete` 도 함께 본다.
---

## 2026-09-16 — 📐 `agentic-coding` **승인 지점과 모델·effort 배정 1편**(총 193편) · 🔴 **구조도를 SVG 가 아니라 Mermaid 로 그렸다** — 발행본에 인라인 SVG 가 **0건**이고, 검사를 받는 쪽은 Mermaid 뿐이다 · 🔴 **그런데 그 Mermaid 가 위아래로 뒤집혀 있었고 문법 검사는 위반 0 을 냈다**(함정 54) · ✅ **지도편이 비워 두었던 주제 10 의 1차가 채워졌다**

> 사용자가 참고 이미지 한 장을 주고 「구조도를 SVG 로 만들어야 하나, Mermaid 로 저 느낌이 날지
> 모르겠다」고 물었다. 판단의 근거는 취향이 아니라 **리포가 이미 무엇을 검사하고 있는가**였다.

### 🔴 SVG 를 고르지 않은 이유 넷

| 근거 | 실측 |
| --- | --- |
| 도식 관용 | 발행본 192편에 인라인 SVG·`<img>` 가 **0건**이고 Mermaid 블록이 **552개**다. SVG 를 넣으면 유일한 예외가 된다 |
| 느낌이 나는가 | **난다.** `lib/mermaid-theme.ts` 의 다크 팔레트가 배경 `#0f172a` · 노드 채움 `#0c4a6e` · 테두리 `#38bdf8` 로, 참고 이미지의 「어두운 배경 + 밝은 파랑 카드」와 같은 계열이다 |
| 🔴 검사를 받는가 | Mermaid 는 대비 짝 **43개**를 전수 판정하고 `check-mermaid` 가 파서로 문법을 본다. **SVG 는 어느 검사도 보지 않는다** |
| 되살아나는 결함 | 이 리포는 다크 모드 대비 **1.06** 을 프로덕션에 배포한 적이 있다. 검사 밖에 두는 것은 그 경로를 다시 여는 일이다 |

셋째 행이 결정적이다. **「보기 좋은가」가 아니라 「틀렸을 때 누가 잡는가」로 갈랐다.**
SVG 가 더 예쁘게 나올 수는 있지만, 다음 세션이 팔레트를 고쳤을 때 그것을 막을 것이 없다.

### 🔴 그리고 그 Mermaid 가 뒤집혀 있었다 — 함정 54

발행 직전에 실물을 열었더니 **「나 (사람)」이 맨 아래**에 있었다. 도식의 뜻은 「사람이 위에서
지시하고 에이전트가 아래에서 일한다」인데 화면은 정반대를 그렸다. 같은 시점에
`check-mermaid` 는 **도식 567개 · 위반 0곳**을 냈다.

| 재어 본 것 | 값 |
| --- | --- |
| 나 (사람) | y=390 — 클러스터보다 **아래** |
| 통제 계약 | y=546 |
| 「지원 에이전트」 클러스터 | y=8 — **맨 위** |
| 「핵심 파이프라인」 클러스터 | y=623 |

**기전은 서브그래프였다.** 클러스터 안의 노드가 클러스터 **밖**으로 간선을 보내면 순환이 생기고,
dagre 가 그 순환을 끊으면서 바깥 노드를 맨 아래 랭크로 내린다. 같은 설정으로 변형을 렌더해
좌표로 판정했더니 보이지 않는 간선도, 되먹임을 딴 데로 돌리는 것도 듣지 않았다 —
**서브그래프를 없애는 것만이 들었다.**

⚠️ **눈으로 보고 한 번 틀렸다.** 좁은 칸에 변형 셋을 나란히 놓고 「세 변형 모두 올바른 순서」라고
읽었는데, 좌표로 재니 전부 뒤집혀 있었다. ⇒ **도식의 배치는 인상이 아니라 `getBoundingClientRect`
로 판정한다.**

✅ **리포 전체는 무사하다.** 서브그래프를 쓰는 flowchart **49개**(발행본 · 문서 전량)를 훑어
「클러스터 안 → 클러스터 밖」 간선을 가진 것을 세었더니 **0개**였다. 그 0 을 믿기 전에 고치기 전의
도식을 대조군으로 넣어 선별기가 `R->H`·`D->H` 를 실제로 잡는지 먼저 확인했다.

🔴 **위 표의 셋째 행(「검사를 받는가」)을 이 발견이 좁힌다.** `check-mermaid` 가 보는 것은
**파싱되는가**뿐이고 배치는 보지 않는다. 뜻이 뒤집힌 도식은 문법이 정상이므로 영원히 위반 0 이다.
그래도 Mermaid 를 고른 판단은 뒤집히지 않는다 — SVG 였다면 대비 짝 43개도 파서도 아무것도
보지 않았을 것이고, 이 결함은 **실물을 열어 본 덕분에** 잡혔지 검사기가 잡은 것이 아니다.

### ✅ 주제 10 의 1차 자리가 채워졌다

[지도편](/blog/agentic-coding/topic-map-reading-paths/)은 15주제 교차 참조표에서 **주제 10(사람 개입과
승인 게이트)의 1차를 「없음」으로 비워 두고**, 그 이유를 한 절로 적어 두었다. 2차를 1차로 올려
적으면 없는 깊이를 있다고 말하는 것이 되므로 빈칸이 보이는 편이 낫다는 판단이었다.

새 편이 정확히 그 자리다. 지도편의 자리 수를 다시 세었다.

| 세어 본 것 | 전 | 후 |
| --- | ---: | ---: |
| 1차 자리 | 14 | **15** |
| 「함께 볼 것」 자리 | 10 | **11** |
| 배정한 자리 계 | 54 | **56** |
| 깊이 순서만 세었을 때 편 수 | 27 | **28** |
| 중복을 걷어낸 편 수 | 30 | **31** |
| 목적별 경로의 자리 | 34 | **35** |

**1차를 받지 못한 편이 열일곱이라는 수는 바뀌지 않았다.** 31편 중 1차를 받은 편이 열넷이므로
31 − 14 = 17 이고, 전에는 30 − 13 = 17 이었다. 새 편이 1차를 받으면서 분모와 분자가 함께 늘었다.

⇒ **빈칸을 2차로 메워 두었다면 이 자리는 지금도 비어 있었을 것이다.** 지도의 값어치는 자리를
다 채우는 데 있지 않고 어느 자리가 비어 있는지 보이게 하는 데 있다.

### 발행한 것과 고친 것

| 파일 | 무엇 |
| --- | --- |
| `content/blog/agentic-coding/human-gate-and-model-routing.md` | 새 편 29,540 B. 되먹임 경로 · 단계별 계약 아홉 · 도구 권한 · 모델과 effort 배정 · 실패 여섯 |
| `content/blog/agentic-coding/topic-map-reading-paths.md` | 30편 → 31편. 수가 적힌 자리 **열여섯 곳**과 주제 10 절을 다시 썼다 |
| `docs/multi-agent-system.html` · `docs/multi-agent-system.md` | 같은 구조를 리포 문서로 먼저 만든 것. 구조도는 diagram-design 의 org chart 타입이다 |
| `README.md` · `CHANGELOG.md` | 개수 네 자리. `check-counts` 가 어긋난 자리를 그대로 짚어 주었다 |

🔴 **`effort` 를 언급한 발행본은 이 편이 처음이다.** 192편 전량을 훑어 0건임을 확인하고 썼다.
모델명(Opus · Sonnet · Haiku)이 나오는 편은 다섯이지만 전부 곁가지 언급이다.

⚠️ **`sed -i` 가 README 의 CRLF 194개를 전부 LF 로 바꿨다.** `core.autocrlf=true` 라 커밋되는
blob 은 같고 `git diff` 도 세 줄만 보고했지만, **깨끗한 diff 가 그것을 증명한 것은 아니다** —
diff 전에 git 이 정규화하기 때문이다. 판정은 diff 가 아니라 `tr -cd` 로 CR 을 직접 세어서 했다.
