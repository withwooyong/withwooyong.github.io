# Changelog

이 저장소의 사용자에게 영향이 큰 변경만 날짜별로 간단히 적습니다. (커밋 해시는 선택적으로 추적합니다.)

## 지난 기록

본체에는 최신 3개 절만 둔다. 그 아래는 월별 파일로 옮겼다.

| 월 | 절 수 | 날짜 |
| --- | ---: | --- |
| [`2026-09`](docs/changelog/2026-09.md) | 31 | 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-06 · 2026-09-05 · 2026-09-05 · 2026-09-05 · 2026-09-05 · 2026-09-04 · 2026-09-04 · 2026-09-04 · 2026-09-04 · 2026-09-03 · 2026-09-03 · 2026-09-03 · 2026-09-03 · 2026-09-03 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-02 · 2026-09-01 · 2026-09-01 · 2026-09-01 |
| [`2026-08`](docs/changelog/2026-08.md) | 36 | 2026-08-31 · 2026-08-31 · 2026-08-30 · 2026-08-30 · 2026-08-30 · 2026-08-30 · 2026-08-29 · 2026-08-29 · 2026-08-29 · 2026-08-19 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-18 · 2026-08-17 · 2026-08-17 · 2026-08-17 · 2026-08-17 · 2026-08-16 · 2026-08-16 · 2026-08-16 · 2026-08-13 · 2026-08-13 · 2026-08-12 · 2026-08-11 · 2026-08-11 · 2026-08-09 · 2026-08-09 · 2026-08-08 · 2026-08-08 · 2026-08-08 · 2026-08-08 · 2026-08-07 |
| [`2026-07`](docs/changelog/2026-07.md) | 5 | 2026-07-21 · 2026-07-08 · 2026-07-07 · 2026-07-06 · 2026-07-05 |
| [`2026-06`](docs/changelog/2026-06.md) | 1 | 2026-06-02 |
| [`2026-05`](docs/changelog/2026-05.md) | 1 | 2026-05-01 |

---

## 2026-09-07 — `product-management` **프로덕트 실무 편3**(총 187편) · 🔴 **이번에는 초과의 67%가 배율 안에서 나왔다** — 편2와 정반대라 고친 상수는 건드리지 않았다

> 편3 [`why-data-and-metric-dictionary`](content/blog/product-management/why-data-and-metric-dictionary.md) 를 발행했다. 원본 `11-데이터-드리븐` 의 §1~§4 를 배정받아
> 데이터가 필요한 이유 · 정량과 정성의 분업 · 퍼널 지표의 정의 · 정성 데이터를 모으는
> 다섯 방법을 다룬다. 실습 사례는 설계서 §4-1 대로 편4 에 남겼다.
>
> 함께 실은 것은 PR [#20](https://github.com/withwooyong/withwooyong.github.io/pull/20) 의 merge 커밋과 배포 run 을 인수인계에 채운 것이다.
>
> ⚠️ **같은 함정을 두 세션이 각각 썼다.** 이 세션도 `CLAUDE.md` 가 비워 둔 자리에 백슬래시
> 함정을 50번으로 썼는데, 병렬로 진행된 PR [#21](https://github.com/withwooyong/withwooyong.github.io/pull/21) 이 같은 번호에 더 깊은 판(기전 분포 실측
> 넷 · `check-table` 검사기)을 이미 넣어 두었다. ⇒ **이쪽 판을 통째로 버렸다.** 워크트리를
> 갈라 놓아도 **문서의 「다음 빈 번호」는 공유 자원이라 충돌한다.**
>
> 그 자리를 대신한 것이 **52번**이다. 병합 뒤 검사를 다시 돌리다 뮤테이션이 낸 「치환 실패」
> 일곱 건을 추적하니 코드가 아니라 **워크트리의 줄바꿈**이 원인이었다. 아래에 따로 적었다.

### 🔴 초과의 출처가 편2와 반대였다 — 상수가 아니라 배율이 문제였다

초고가 **20,459 B** 로 목표 17,162 를 **19.2%** 넘겼다. 규칙 16 대로 절 목록이 아니라
**절별 배율**을 먼저 쟀고, 결과가 편2와 정반대였다.

| 원본 절 | 배정 원본 B | 발행본 대응부 B | 배율 |
| --- | ---: | ---: | ---: |
| §1 데이터가 필요한 이유 | 1,935 | 4,302 | 2.223 |
| §2 데이터의 종류 | 631 | 1,192 | 1.889 |
| §3 지표 용어 사전 | 1,861 | 4,963 | **2.667** |
| §4 정성 수집 5법 | 2,422 | 5,112 | 2.111 |
| **합계** | **6,849** | **15,569** | **2.273** |

| 초과의 출처 | 계산 | B | 비중 |
| --- | --- | ---: | ---: |
| 배율 | (2.273 − 1.95) × 6,849 | 2,212 | **67.1%** |
| 고정 비용 | 4,890 − 3,746 | 1,144 | 34.7% |
| 링크 몫 차 | — | −59 | — |
| **합계** | | **3,297** | 실측 초과와 일치 |

**편2 는 초과의 59.4%가 배율 밖이어서 고정 비용 상수를 2,455 → 3,746 으로 고쳤다.**
이번은 67.1%가 배율 안이므로 **상수를 다시 건드리지 않고 본문을 줄였다.** 고친 상수가
한 편 뒤에 다른 원본에서도 대체로 맞았다는 뜻이며, 이것이 상수를 지지하는 첫 대조군이다.

### 무엇을 줄였나 — 배율이 가장 높은 절을 접었다

§3 대응부의 배율 2.667 이 가장 높았고, 그 안에 **「이름이 같아서 틀리는 두 쌍」이라는 절
하나가 통째로** 들어 있었다. 그 내용은 원본에서 **배정에서 뺀 경력 메모 블록**이 강조한
것이고 배정 안에는 정의만 있다. ⇒ 절을 없애고 앞 절 뒤의 한 문단으로 접었다.

| 단계 | 합계 B | 목표 대비 |
| --- | ---: | ---: |
| 초고 | 20,459 | +19.2% |
| 1차 축약 | 19,456 | +13.4% |
| 2차 축약 | 18,310 | +6.7% |
| 절을 접은 뒤 | 17,952 | +4.6% |
| **복제·축자 제거 뒤** | **17,850** | **+4.0%** (밴드 16,203 ~ 18,120) |

⚠️ **설계서 §8-2 의 「복제를 지우면 분량이 는다」가 이번에는 성립하지 않았다.** 앞 배치는
실측 **+232 B** 였는데 이번은 **−11 B** 다. 고쳐 쓴 두 문장이 원래보다 짧아졌기 때문이며,
⇒ **그 규칙은 방향이 아니라 「다시 재라」로만 읽어야 한다.** 늘 것을 전제로 여유를 잡으면
밴드 안에서 목표를 향해 더 줄일 기회를 잃는다.


### 🔴 빨강도 원인을 확인하기 전에는 결론이 아니다 — 함정 52번

병합 뒤 검사를 다시 돌리다가 `npm run mutate` 가 뮤턴트 105개 가운데 **7개를 「치환 실패」로**
보고했다. `CLAUDE.md` 는 그것을 **「그 자리를 다시 볼 때가 됐다는 신호」로** 읽으라고 적어 두었고,
그 지시를 따라 코드를 뒤졌다. 그런데 `origin/main` 과 검사기 넷을 대조하니 **차이가 0줄**이었다.

원인은 코드가 아니라 **작업 트리의 줄바꿈**이었다. `core.autocrlf=true` 이고 `.gitattributes` 가
없으므로 체크아웃이 LF 를 CRLF 로 펴는데, 러너의 대상 문자열은 JS 소스의 `\n` escape 라
**LF 한 글자**다. 그래서 **개행을 담은 뮤턴트만** 어긋난다.

🔴 **여기까지는 이 리포가 이미 함정 37번으로 적어 둔 것이다.** 새로운 것은 그 항목에 달려
있던 조건이 **필요조건이 아니었다**는 사실이다. 37번과 인수인계의 진단은 둘 다
**「`git checkout --` 로 원복했을 때」로** 조건을 달았고 「손으로 끼어들 때만 이 자리가
열린다」고 맺었다. 그런데 **워크트리 생성도 체크아웃이다** — 아무도 원복하지 않았고 아무도
편집하지 않았는데 처음부터 열려 있다.

| 언제 | CRLF 가 되는 파일 | 치환 실패 |
| --- | --- | ---: |
| 한 파일을 `git checkout --` 로 원복 | **그 한 파일** | 3건 — 전부 `check-forbidden` |
| 새 워크트리를 만든다 | **검사기 전부** | **7건** — `compose`·`fix-markup` 이 더해진다 |

건수의 차이가 곧 그 증거다. 37번이 3건으로 관측된 것은 그 세션이 한 파일만 원복했기
때문이며, **조건을 관측 상황에 맞춰 좁게 적는 바람에** 다음 사람이 같은 것을 새 문제로 보게
됐다. LF 로 정규화하고 다시 돌리니 **105개가 전부 잡혔다.**

⚠️ **곁가지를 인과로 착각할 뻔했다.** `scripts/mutate.mjs` 에는 NUL 바이트가 하나 있어
git 이 이 파일만 binary 로 보고 변환에서 빼는데, 이것을 원인 사슬에 넣어 적었다가
실측으로 지웠다 — 러너 사본을 CRLF 로 바꿔도 `from` 문자열은 **똑같이 LF** 이므로 매칭이
바뀌지 않는다. 그 NUL 이 실제로 하는 일은 `grep -n` 을 `Binary file … matches` 로 막는 것뿐이다.
⇒ **원인 후보가 둘이면 하나를 바꿔 보고 결과가 바뀌는지 재라.**
---

## 2026-09-07 — **「표본이 한 문서뿐」이라는 판단을 전량 실측이 반증했다.** 표의 열 수를 보는 검사기를 만들고 발표본을 넷 더했다 (PR [#21](https://github.com/withwooyong/withwooyong.github.io/pull/21))

> 직전 세션이 표 두 곳의 열 수가 어긋난 것을 찾고도 **표본이 적다는 이유로 검사기를 만들지
> 않기로** 판정했다. 이번 세션이 리포 전량을 세어 그 판단을 뒤집었다.
>
> | 무엇을 셌나 | 값 |
> | --- | ---: |
> | 표 (발행본 2,051 + 문서 1,727) | **3,778개** |
> | 어긋난 행 | **4곳** (발행본 2 · 문서 2) |
> | 그중 진짜 | **4곳** — 거짓 양성 0 |
>
> 🔴 **기전 분포가 예상과 어긋난 것이 더 중요하다.** 직전 세션이 원인으로 지목한 것은
> `sed` 가 백슬래시를 먹는 자리였는데, 전량에서 그 기전은 **0건**이었고 넷 중 **셋이 코드
> 스팬 안의 세로줄**이었다. 코드 스팬은 GFM 표에서 세로줄을 보호하지 않는다 — 세로줄이
> 셀 구분자로 **먼저** 파싱되고 백틱은 그 뒤에 해석된다.
> ⇒ **한 사례에서 읽은 기전을 전체의 기전으로 삼지 마라. 표본이 적다는 판단도 전량을
> 세어 본 뒤에 하라.**

### 무엇을 만들었나

| 자리 | 내용 |
| --- | --- |
| `scripts/check-table.mjs` | 표 행의 셀 수를 머리 행과 대조한다. 판정은 정규식이 아니라 파서(`mdast-util-gfm`)가 하므로 코드 블록 안의 표 예시가 저절로 빠진다. 대상 수집은 `check-markup.mjs` 의 함수를 그대로 쓴다 — 수집 자리를 하나로 유지한다 |
| 자기 검사 | **22건** (판정 ①~⑮ + 대상 수집 ⓵~⓻). 판정에 **도달한** 표 수를 함께 세어 대상 수와 대조한다 |
| 뮤턴트 | **TB1~TB7** 일곱. 전체는 98 → **105개** |
| 훅 | 발행본 9단 → **11단** · 문서 6단 → **8단** |
| CI | `build` job 23 → **26스텝** |

🔴 **`TB4` 는 처음 판이 되살릴 결함이 없는 뮤턴트였다.** 「줄 번호를 행의 끝으로 보고한다」로
썼는데 GFM 표의 행은 **언제나 한 줄**이라 `start.line` 과 `end.line` 이 늘 같다. 실측으로
확인한 뒤 「표의 첫 줄을 보고한다」로 바꾸니 자기 검사가 22/22 → **20/22** 로 떨어졌다.
⇒ **생존한 뮤턴트를 보면 검사기를 의심하기 전에 그 뮤턴트가 되살리는 결함이 실재하는지를
먼저 보라.**

### 고친 표 넷

| 파일 | 기전 |
| --- | --- |
| `content/blog/agentic-coding/claude-md-scope-layers.md` | 3열 표에 셀이 2개 |
| `content/blog/ai-agent/ai-agent-qna-fundamentals.md` | 코드 스팬 안의 세로줄 |
| `docs/superpowers/plans/2026-08-30-pm-batch-split-design.md` | 코드 스팬 안의 세로줄 |
| `docs/superpowers/reports/2026-08-08-B1-fixes.md` | 코드 스팬 안의 세로줄 (두 곳) |

교정은 전부 `Edit` 으로 했다. `sed` 로 하면 셸과 `sed` 가 백슬래시를 각각 한 겹씩 먹어
두 단계가 곱해진다.

### 발표본 넷 — 119장의 넘침을 카나리로 대조한 뒤에 쟀다

| 덱 | 장수 | 교정 전 넘침 | 교정 후 |
| --- | ---: | ---: | ---: |
| `slides-patterns` (에이전트 패턴 11종) | 29 | 1 | **0** |
| `slides-team-ops` (팀 운용) | 30 | 3 | **0** |
| `slides-governance` (사내 AI 거버넌스) | 25 | 2 | **0** |
| `slides-search` (검색 6편을 한 덱으로) | 35 | 0 | **0** |

`slides-search` 는 `pages/` 여섯 파일을 `src:` 로 불러 만든다. 조사 문서가 「확인하지
않았다」고 남긴 물음이었고 **실제로 빌드된다.**

🔴 **처음 잰 넘침 0 은 거짓이었다.** Slidev 인쇄 화면은 첫 장 말고는 `display: none` 이라
높이가 전부 0으로 나온다. 그 0을 「넘치지 않는다」로 읽으면 119장이 통째로 검사되지 않는다.
**일부러 400px 넘치게 만든 카나리를 먼저 넣어** 측정이 살아 있는지 본 뒤에야 0을 결론으로
썼다. 좌표에 `2.36413` 배율이 곱해져 있어 정규화도 함께 필요했다.

교정은 표 패딩을 `12px` → `4px` 로 줄이는 슬라이드 스코프 `<style>` 과 글자 크기 조정이다.
`leading-tight` 는 도움이 되지 않았다 — 행 높이 41px 중 **24px 가 패딩**이기 때문이다.

### 도구 함정 셋을 재현 절차와 실측으로 남겼다

`docs/TOOL-TRAPS.md` 가 49 → **51건**(1,428줄)이 되었다. 50번은 셸과 `sed` 의 백슬래시,
51번은 이번에 겪은 것이다 — **Git Bash 가 `--base /` 의 `/` 를 `C:/Program Files/Git` 으로
바꿔** 빌드는 성공하고 화면만 비었다. 콘솔에 오류도 없었다. `MSYS_NO_PATHCONV=1` 로 막는다.

---

## 2026-09-06 — **충돌하는 PR 은 검사가 빨간 것이 아니라 아예 돌지 않는다.** 마크다운 원고를 발표 슬라이드로 옮길 수 있는지 검증하고 조사 문서로 남겼다 (PR [#19](https://github.com/withwooyong/withwooyong.github.io/pull/19))

> 앞선 세 세션이 `slidev-poc/` 에 Slidev 52.19.1 을 설치해 형식을 검증하고(`slides.md` 7장),
> 발행본 한 편을 실제 슬라이드로 옮겼다(`slides-es.md` 20장). 이번 세션은 그 셋을
> [`2026-09-06-presentation-format-survey.md`](docs/superpowers/reports/2026-09-06-presentation-format-survey.md) 한 편으로 모았다.
>
> ⇒ 🔴 **PR 을 열었더니 `no checks reported` 가 나왔다.** 트리거는 `pull_request: branches: [main]`
> 으로 정상이었는데, PR 이 `CONFLICTING` 이라 GitHub 이 병합 ref 를 만들지 못했고 그래서
> 이벤트 자체가 발생하지 않았다. **「검사가 초록이 아니다」가 아니라 「검사가 없다」였다.**

### 결론 — 기능은 통과하고 분량이 걸린다

| 물음 | 답 | 근거 |
| --- | --- | --- |
| 마크다운 원고가 슬라이드가 되는가 | 된다 | 발행본 한 편이 20장으로 옮겨졌다 |
| 도식을 다시 그려야 하는가 | 아니다 | Mermaid 블록이 코드 펜스 그대로 동작한다 |
| 원고를 그대로 붙이면 되는가 | 아니다 | 첫 시도에서 4장이 넘쳤다 |
| 무엇이 넘치는가 | **표만 넘친다** | 도식 장은 0장이며 139px 이상이 남았다 |
| 무엇을 잃는가 | 편간 링크와 되짚는 문장 | 링크 2개가 0개가 되었다 |
| 배포가 되는가 | `--base` 를 주면 된다 | 자산이 전부 루트 절대 경로다 |

넘친 네 장의 여유는 -91px · -45px · -18px · -10px 이었고 **전부 표가 있는 장**이었다.
도식은 `scale` 로 통째로 줄일 수 있지만 표에는 그런 손잡이가 없다는 것이 기전이다.
교정은 `class: text-sm` · `layout: two-cols` · 되짚는 문장 삭제 · 문단 삭제 네 가지였다.

### 주장마다 근거를 코드와 산출물에서 찾았다

| 주장 | 근거 |
| --- | --- |
| `npm create slidev@latest` 의 자동 설치가 `TypeError` 로 깨진다 | 생성된 `README.md` 에 `[object Object] install` 이 박혀 있다 |
| `dist/index.html` 은 1,707 B 껍데기다 | 본문 낱말 **0회** · 번들은 약 4,476 KB. 그런데 `<title>` 에는 제목이 있다 |
| 하위 경로 배포에 `--base` 가 필요하다 | 자산이 전부 `/assets/…` 다. CLI 도움말은 `output base. Example: /demo/` |
| PDF 가 화면과 달리 밝게 나온다 | export 가 `colorScheme: dark ? "dark" : "light"` 로 색 모드를 직접 지정한다 |
| PDF export 에 playwright 별도 설치가 필요하다 | 없으면 `The exporting for Slidev is powered by Playwright…` 로 죽는다 |
| 슬라이드 높이 552px | `canvasWidth` 기본 980 · 화면비 기본 16:9 이므로 `980 ÷ (16/9) = 551.25` |

### 🔴 `git ls-files` 로 대상을 모으는 검사기는 추적되지 않은 파일을 보지 않는다

새 문서를 쓰고 문서 검사 셋을 돌렸더니 **62개 파일 · 위반 0** 이 나왔다. 그것을 결론으로
읽을 뻔했다. `git add` 뒤에 다시 돌리자 **63개 파일 · 마크업 위반 2회**가 드러났다.

| 시점 | 스캔한 파일 | 위반 |
| --- | ---: | ---: |
| `git add` 전 | 62개 | 0 |
| `git add` 후 | 63개 | **2회** |

⇒ **새로 만든 `.md` 는 스테이징한 뒤에 검사기를 돌려라.** 훅은 커밋 시점에 돌므로 이 함정을
겪지 않지만, 커밋 전에 미리 확인하려는 사람이 정확히 여기에 빠진다.

### 🔴 표의 열 수는 어느 검사기도 보지 않는다

리뷰에서 표 두 곳이 셀 3개짜리 행인데 **6개로 갈라져 있는 것**을 찾았다. `{1|3-6|8-10|*}`
이라는 Slidev 문법을 셀에 넣으면서 `sed` 로 `\|` 이스케이프를 넣었는데, 백슬래시가 셸을
지나며 한 겹 벗겨져 파일에는 맨 세로줄이 들어갔기 때문이다. `Edit` 로 다시 넣어 고쳤다.

**마크업 · 링크 · 도식 검사 셋이 전부 이것을 통과시켰다.** 강조와 링크와 도식을 볼 뿐
표의 구조를 보지 않기 때문이다.

⚠️ **더 볼 만한 것은 이것을 한 번 놓칠 뻔했다는 점이다.** 처음 돌린 정규식 검사가 두 줄을
잡았는데 「GFM 은 코드 스팬 안의 `\|` 를 이스케이프로 읽으니 거짓 양성이겠지」라고 판단했다.
파서로 다시 돌리자 진짜였다 — **이스케이프가 애초에 파일에 없었기 때문이다.**
규칙을 아는 것과 파일에 그 규칙이 적용됐는지는 다른 문제다.

### 수치 정정 — 도식 수가 이미 낡아 있었다

새 문서가 도식을 셋 더하면서 다시 셌다. PR [#18](https://github.com/withwooyong/withwooyong.github.io/pull/18) 이
같은 줄을 고쳐 `CLAUDE.md` 에서 충돌했고, 어느 쪽 수도 병합 뒤의 값이 아니므로 **한쪽을
고르는 것은 어느 쪽을 골라도 틀렸다.** 다시 재서 넣었다.

| 판 | 전체 | 발행본 | 문서 |
| --- | ---: | ---: | ---: |
| 이 브랜치 | 670 | 544 | 126 |
| main (PR #18) | 665 | 544 | 121 |
| **병합 뒤 실측** | **673** | **544** | **129** |
| **이 인수인계 커밋 뒤** | **674** | **544** | **130** |

118 은 이미 낡아 있었다. 앞선 세 세션이 `slidev-poc/` 에 더한 도식 다섯을 아무도 세지
않았기 때문이다. `CLAUDE.md` · `README.md` · `HANDOFF.md` 를 같은 커밋에서 맞췄다.
`README.md` 는 파일 수도 낡아 있어(59개 → 실측 64개) 함께 고쳤다.

### 커밋

| 커밋 | 무엇 |
| --- | --- |
| `d383383` · `0b5d8cc` · `6385d09` | 앞선 세 세션의 형식 검증과 이식 (`slidev-poc/`) |
| `08925ee` | 조사 문서 한 편 + `CLAUDE.md` 수치 |
| `bd4a7fc` | `main` 병합 · 도식 수 실측 반영 |
| `8b884e3` | 리뷰 교정 — 표 깨짐과 배율 오기 |

PR 단계 CI 는 run `34029167076`(2분 22초)과 `34030483379`(2분 18초)가 모두 **success** 이며
`build` 성공 · `deploy` 는 설계대로 **skipped** 다.

---

## 2026-09-06 — 🔴 **도식 라벨의 마지막 글자가 잘리던 것을 닫았다** — 리포 전체 549개 · 이미 배포되어 있었다 (발행본 186편, 변동 없음)

> 콘텐츠 변경은 없다. 직전 세션이 실물에서 발견하고 넘긴 항목 하나를 근본 원인까지 내려가
> 고쳤다. 검사기 열 종이 전부 초록인 동안 화면만 틀려 있었다 — **문법도 링크도 색도 개수도
> 멀쩡했고, 잘림은 브라우저가 레이아웃을 계산한 뒤에야 드러난다.**

### 무엇이 보이던가

「순이익」이 「순이읙」으로, 「꺼내는 장치」가 「꺼내는 장」으로, 「Discovery」가 「Discover」로
보였다. 한 편의 라벨 34개 가운데 **17개**가 2~10px 넘쳤다.

### 원인 — 폰트 이름이 아니라 **재는 자리와 그리는 자리**가 달랐다

mermaid 는 라벨 폭을 재려고 임시 컨테이너를 **`document.body` 바로 아래**에 붙인다
(`render` 에 `svgContainingElement` 를 주지 않는 경로). 그 경로에서는
`initialize({ fontFamily })` 가 **인라인 스타일로도 걸리지 않는다.**

| 자리 | 계산된 `font-family` | 무엇에 쓰이나 |
| --- | --- | --- |
| `body` 직속 임시 컨테이너 | `"Noto Sans KR"` | **폭을 잰다** |
| `.mermaid-figure` 안 | `__Inter_…, __Inter_Fallback_…, Inter, system-ui, sans-serif` | **그린다** |

`next/font` 가 만드는 `--font-inter` 변수가 `_app.tsx` 의 `<div>` 에만 있어 `body` 는 브라우저
기본 글꼴로 떨어진다. 재는 글꼴이 좁으면 상자가 그만큼 좁게 만들어지고, `foreignObject` 는
**SVG 사양상 자기 폭 밖을 클리핑한다** — `overflow: visible` 이어도 소용없다.

### 어떻게 확정했나 — 첫 가설을 브라우저가 기각했다

`document.body.style.fontFamily` 를 본문과 같게 맞추고 재렌더시켰는데 **폭이 소수점까지
그대로**였다. 확정은 같은 문자열을 두 컨텍스트에서 **직접 잰 것**이 냈다 — 「순이익」이 body
기본 폰트에서 **38.65**, Inter 체인에서 **42** 였고 앞의 값이 `foreignObject.width` 와
소수점까지 일치했다. 도식만 body 폰트로 강제하니 넘침이 **17 → 0**, 되돌리니 다시 **17** 이었다.

### 고친 것

| 파일 | 무엇을 |
| --- | --- |
| `lib/mermaid-theme.ts` | `resolveDiagramFontFamily` — 상속에 맡기는 값(`inherit`·`initial`·`unset`·`revert`·빈 값)을 거부하는 순수 함수와 대체 스택 |
| `components/mermaid.tsx` | 도식이 그려질 자리에서 `getComputedStyle` 로 **해석된** 스택을 읽어 넘긴다. 웹폰트 로드 전 측정을 막으려고 `document.fonts.ready` 를 기다린다 |

계산된 값은 `var()` 가 이미 풀린 뒤이므로 `body` 아래의 측정 컨테이너에서도 똑같이 해석된다.
🔴 **확대 뷰어도 함께 닫혔다** — 포털이라 `body` 아래에 그려지는데 폰트 스택이 SVG 자체에
박히므로 본문과 같은 글꼴이 된다.

### 실측 — 여섯 편 · 도식 29개 · 라벨 361개 · 라이트와 다크 양쪽

| 편 | 라벨 | 수정 전 | 수정 후 |
| --- | ---: | ---: | ---: |
| `product-cycle-and-metric-hierarchy` | 34 | **17** (최대 9.61px) | **0** |
| `search-system-overview` | 35 | **17** | **0** |
| `pm-practice-map` | 16 | 5 | **0** |
| `agent-pattern-catalog` | 214 | — | **0** |
| `langgraph-checkpointer-hitl` | 41 | — | **0** |
| `autogen-conversation-agents` | 21 | — | **0** |

확대 뷰어 라벨 **18개**도 0이다.

### 무엇으로 지키나

jsdom 에는 레이아웃이 없어 `scrollWidth` 가 언제나 0이므로 잘림 자체는 단위 테스트로 재현할
수 없다. 그래서 묶은 것은 **상속 키워드가 다시 들어오는 길**이다.

| 항목 | 이전 | 이번 |
| --- | ---: | ---: |
| `tests/blog` | 14파일 194케이스 | 14파일 **200**케이스 |
| 뮤턴트 | 96 | **98** (TH19 · TH20 추가 · 둘 다 잡힘) |
| `docs/TOOL-TRAPS.md` | 48건 | **49건** |

`npm run mutate` 는 **98/98 잡힘 · 생존 0 · 치환실패 0** 이다.

⚠️ `CLAUDE.md` 게이트 절의 `tests/blog/content/` 케이스 수가 **24** 로 낡아 있었다(실제 **31**).
앞 세션이 `README.md` 와 `HANDOFF.md` 만 고치고 이 자리를 빠뜨린 것이며 같은 커밋에서 고쳤다.

---
