# Changelog

이 저장소의 사용자에게 영향이 큰 변경만 날짜별로 간단히 적습니다. (커밋 해시는 선택적으로 추적합니다.)

## 2026-08-28 — 고아 자산 4종 삭제 · 기준선 게이트 복구 · **E2E 를 배포 게이트로 승격**

> **화면은 바뀌지 않았다.** 단계 2 동안 의도적으로 꺼 두었던 **게이트 둘을 끝에서 켠 것**이 요점이다.
>
> - `npm run e2e` 가 이제 `deploy.yml` 의 `build` 잡에서 돈다. `deploy` 는 `needs: build` 뒤라
>   **E2E 가 빨가면 배포되지 않는다.** `continue-on-error` 는 쓰지 않았다
> - `check-baseline` 이 T9 이후 15건 빨강이었다. 원인을 전수 규명한 뒤 **한 번만** 갱신해
>   `✅ 16개 불변` 으로 되돌렸다. `product-lead*` 스텁 9개는 그대로 보존됐다
> - 지운 것: `lib/wiki.ts` · `wiki-shell` · `roadmap-domain` · `product-lead-roadmap`. **4개다** —
>   계획서 목록에 있던 `product-lead-domains.ts` 는 `/work` 가 쓰고 있어 남겼다
>
> 새 검사기 `tests/ci/deploy-workflow.test.ts` 가 그 게이트를 **YAML 파싱으로** 지킨다(단언 5개).
> 문자열 grep 이었다면 「스텝을 주석 처리한」 뮤턴트가 살아남았을 것이다 — 주석 안에 같은 문자열이 남는다.
>
> 경위: [`docs/superpowers/reports/2026-08-28-t14-orphans.md`](docs/superpowers/reports/2026-08-28-t14-orphans.md) (R55~R61)

## 2026-08-28 — 문서 정합성 마감: 거짓 CI 보증 제거 · README 65커밋 drift 정정

> **코드는 한 줄도 바뀌지 않았다.** 앞 배치가 남긴 문서 부채 둘을 닫았다.
> 산출물이 동일하므로 사이트의 겉모습에는 변화가 없다.
>
> 값어치가 있는 것은 정정 자체가 아니라 **README 가 하고 있던 거짓 보증을 찾은 것**이다 —
> 「CI 가 산출물 불변 검사를 돌린다」고 적혀 있었으나 워크플로에서 주석 처리돼 있었다.

### Fixed

- **README 의 거짓 CI 보증** (`cef8743`) — 「CI 가 `check-baseline` 을 돌린다」고 적혀 있으나 [`deploy.yml:99-100`](.github/workflows/deploy.yml) 에서 **주석 처리**돼 있다. 문서를 믿은 사람은 **돌지 않는 게이트를 돌고 있다고 여긴다.** 「로컬 전용」임을 명시했다
- **README 의 CI 단계 서술** (`cef8743`) — 발행본 수·Pagefind 검사가 빠져 있었다. 워크플로 파일과 대조해 다시 적었다
- **`pre-commit` 이 `tests/atlas` 까지 도는 이유가 문서에 없던 것** (`cef8743`) — `tests/atlas/integrity.test.ts` 는 다른 어떤 글도 링크하지 않는 글을 실패시키는데, 새 글은 정의상 그런 글이다. 빠져나갈 길 둘(다른 글에서 링크 / `role: map`)을 함께 적었다
- **65커밋 동안 밀린 서술 drift** (`cef8743`) — `atlas`·`아틀라스` 가 README 에 **0건**이었다. 162노드짜리 기능이 문서에 없었다. 기술 스택(Pagefind·zod·Playwright)·페이지 구성(`/atlas`·`/atlas/[...id]`)·프로젝트 구조(`atlas/`·`search/`·`design/`·`ui/`·`e2e/`)·문서 표(TOOL-TRAPS·PUBLISHING-CHECKLIST)를 채웠다

### Changed

- **README 의 규모 표에서 게이트 없는 숫자를 걷어냈다** (`cef8743`) — 라우트 수 `12→14`, sitemap URL `196→233`, 컴포넌트 `23→31` 이 썩은 채였다. 숫자를 지우고 **확인 명령**으로 바꿨다. 발행본 156편만 남긴 것은 `check-counts` 가 지키기 때문이다
- **`CLAUDE.md` — 산출물 문자열로 「렌더됐다」를 증명하지 말라는 규칙 추가** (`b62f49c`) — Next 는 props 를 `__NEXT_DATA__` 에, 제목을 `<title>`·`og:title` 에 중복해 싣는다. 본문을 통째로 비워도 초록이었고 앞 배치에서 뮤턴트가 두 번 생존해서야 드러났다. 「한 줄을 더하면 한 줄을 뺀다」에 따라 **122줄을 유지**했고, 뺀 두 자리는 둘 다 이 파일 자신의 규칙(「근거·경위는 `docs/` 에」)을 어기고 있던 곳이다

### 이번에 배운 것 — **README 의 수치는 검사기가 지킨다**

프로젝트 구조 트리의 `content/blog/ # 발행본 156편` 을 「6개 분류」로 바꿨더니 `check-counts` 가 실패했다.
`scripts/check-counts.mjs` 는 README 안의 **네 자리**를 정규식으로 찾아 실제 파일 수와 대조하고,
**자리 자체가 사라지면 실패**시킨다. 되돌렸다.

> **README 의 숫자를 지우기 전에 `npm run check-counts` 를 돌려라.** 어떤 숫자는 게이트가 지키고 있다.

## 2026-08-27 — 사이트 리뉴얼 배포: 지식 아틀라스 `/atlas` · `⌘K` 전문 검색 · 새 셸

> `feat/site-renewal` **64커밋**을 `main` 에 병합했다. 발행본 **156편**은 그대로이고,
> **읽는 방법이 둘 늘었다** — 글끼리의 관계를 보는 `/atlas`(노드 162 · 엣지 1,053)와
> 한국어 전문 검색 `⌘K`(Pagefind 정적 인덱스, 조각 405).
> **LLM 런타임 호출이 없다.** 전부 빌드 시점에 만들어져 정적으로 나간다.
>
> **메인 페이지와 `/blog` 156편은 한 글자도 바뀌지 않았다** — 계획서가 내건 약속이고
> `git diff --name-only` 로 0건임을 확인했다(GC-11·GC-12).

### Added

- **`/atlas`** — 글 156편 + 토픽 6개를 노드 162개로, 본문 링크·시리즈·분류를 엣지 1,053개로 잇는 지식 그래프. 결정론적 방사형 레이아웃 + SVG 렌더러. `prefers-reduced-motion` 과 무관하게 **목록 뷰를 항상 함께 싣는다** — 그래프는 포인터 전용이라 키보드·스크린리더의 경로가 목록뿐이다
- **`/atlas/<id>` 노드 상세 162장** — 이웃 글로 이어지는 양방향 링크. 그래프 전체를 페이지마다 싣지 않고 `{ node, neighbors }` 만 넘겨 산출물 +70 MB 를 막았다
- **`⌘K` 커맨드 팔레트** — Pagefind 정적 인덱스에 한국어 질의. 조사 전처리로 「임베딩의」·「컨텍스트에」 같은 활용형도 잡는다
- **품질 게이트 5종** — `check-pagefind` · `probe-search`(실제 인덱스에 한국어 질의를 쏜다) · `check-counts` · `check-baseline` · 그래프 무결성 테스트. 전부 CI 에서 `deploy` 앞에 선다
- **Playwright E2E** — 74건. 빌드 산출물 `out/` 을 서빙하므로 「빌드를 빼먹고 어제 것을 검사」하는 것을 `global-setup` 이 종료 코드 1 로 막는다

### Changed

- 헤더 NAV 에 **Atlas** 등재 (`3ba2e33`) — 라우트가 실제로 생긴 시점에 올렸다
- 검색 인덱스에서 **아틀라스 노드 상세 162장 제외** (`3ba2e33`) — 노드는 글의 요약이라 원문과 중복 색인된다. 실측으로 쿼리 8종 상위 10 에 든 `/atlas/` 17건이 **전부 원문 중복**이었다. 목록 `/atlas/` 는 남긴다
- 비블로그 산출물 기준선을 **이 배치에서 1회 갱신** (`7c9faa5`) — 변경 9건이 전부 `product-lead*` 이고 델타가 `tabindex="-1"` 한 줄임을 확인한 뒤 눌렀다
- 설계서에 **D-1·D-2 역반영** (`7c9faa5`) — 태그를 그래프 노드로 넣지 않고 사이드바 필터로만 쓰기로 확정(§15 미확정 → §7.3 결정), 단계 2 와 3·4 의 순서가 바뀐 것을 §12 에 기록

### Fixed

- **검색으로 글에 도달해도 포커스가 본문으로 가지 않던 것** (`3ba2e33`) — `<main id="main">` 에 `tabIndex={-1}` 이 없어 `focus()` 가 **예외 없이 조용히 무동작**이었다. 스크린리더 사용자가 매번 페이지 처음부터 다시 훑어야 했다
- **같은 결함이 `product-lead*` 9장에 남아 있던 것** (`530da98`) — 그 9장 전부 검색 결과로 도달 가능하다
- **모바일에서 내비 검사가 무엇을 넣어도 통과하던 것** (`530da98`) — 390px 에서 데스크톱 내비가 `display:none` 인데 드로어를 열지 않아, 죽은 링크를 아무도 못 잡았다
- **「노드 상세가 정적으로 존재한다」 검사가 본문을 안 보고 있던 것** (`7e9bcfc`·`ca61f1f`) — Next 가 props 를 `__NEXT_DATA__` 에, 제목을 `<title>`·`og:title` 에 중복해 실어서 **본문을 통째로 비워도 초록**이었다. 관측 범위를 `</head>` 이후로 좁혔다
- 본문 링크 추출이 **코드 펜스·인라인 코드·HTML 주석 안의 예시 링크**를 진짜 링크로 세던 것 (`7d12581`)
- 소스에 박힌 생 NUL 2개 때문에 **ripgrep 이 파일을 통째로 건너뛰던 것** (`73d0425`) — 「없다」와 「못 읽었다」가 같은 침묵이었다

## 2026-08-19 — `backend-engineering` **CI/CD 7편**(총 156편), 두 축 리뷰의 순증은 합산된다

> 원본 `java-backend/06-CI-CD-GitHub-Actions-Jenkins.md`(83,251 B, §14 실명 경력 제외 후 **80,607 B**)
> 1개 → 7편 **169,345 B**. 겉보기 배치 비율 **2.10**.
> 실측 회고는 설계서 `docs/superpowers/plans/2026-08-19-cicd-split-design.md` **§10**.
>
> | 편 | slug | 원본 B | 발행 B | 비율 | 밴드 | 커밋 |
> | :---: | --- | ---: | ---: | ---: | --- | --- |
> | 1 | `cicd-pipeline-fundamentals` (`role: "map"`) | 7,175 | 20,867 | 2.91 | 고정비용 방식 | `976fca3` |
> | 2 | `version-control-as-cicd-premise` | 11,424 | 22,655 | 1.98 | 안 (여유 1,845) | `38aee11` |
> | 3 | `branching-strategy-and-containers` | 10,262 | 21,505 | 2.10 | 🔴 **+5 초과** | `92fe3c0` |
> | 4 | `github-actions-pipeline` | 11,418 | 22,040 | 1.93 | 🔴 **+40 초과** | `84dd06c` |
> | 5 | `jenkins-pipeline-and-agents` | 11,658 | 25,046 | 2.15 | 안 (여유 254) | `445ec2a` |
> | 6 | `zero-downtime-and-quality-gates` | 12,754 | 28,040 | 2.20 | 안 (**여유 10**) | `6ee868b` |
> | 7 | `questions-that-come-back` (Q&A) | 15,916 | 29,192 | 1.83 | 안 (**여유 8**) | `e2f5565` |
>
> 밴드 상한은 검사기가 강제하지 않는다(`tests/blog/content/schema.test.ts` — 「상한은 검사하지 않는다」).
> 초과 2건은 게이트 실패가 아니라 **계획이 못 센 순증**이고, 아래 두 절이 그 두 경로다.
>
> ### 🔴 두 축 리뷰의 처방은 서로를 모른 채 합산된다 — 편6이 상한을 587 B 넘겼다
>
> 리뷰를 위험 축별로 2명 이상 띄우는 것은 맹점을 깨기 위해서다. 그런데 **각 리뷰어는 자기 축의
> 순증만 센다.** 편6에서 두 축의 처방을 다 반영했더니 순증 1,334 B가 나 상한을 587 B 넘겼고,
> 되풀이 여섯 자리를 찾아 깎아서야 들어왔다. 충실도 리뷰어가 「순증 500~600 B」라 추정했는데
> 그것은 **자기 축만 센 값**이었다.
>
> ⇒ 편7 브리프부터 세 줄을 넣었다. **효과가 즉시 났다** — 편7은 여유 8 B로 착지했다.
>
> | 규칙 | |
> | --- | --- |
> | 처방마다 ±B를 적어라 | 추정이 아니라 **제안한 문안을 실제로 세어서** |
> | 순증 처방에는 같은 절에서 **뺄 자리를 함께 지목**하라 | 「늘려라」만 있고 「줄여라」가 없으면 채택 불가 |
> | **내 축 순증 합계를 보고 첫머리에 적어라** | 축 사이의 합산은 컨트롤러가 한다 |
>
> ⚠️ **그래도 13 B가 어긋났다.** 예측 −10 B, 실제 +3 B. 차이는 리뷰어가 처방 표 **밖**에 적은
> 「~하면 함께 고쳐야 한다」는 **조건부 처방**이었다. 표에 없으니 합계에도 없다.
> ⇒ 조건부 처방도 표 안에 넣게 하거나, 컨트롤러가 표 밖 문장을 따로 훑어야 한다.
>
> ### 🔴 마감 단계 링크는 예산 **밖에서** 들어온다 — 편4가 상한을 40 B 넘겼다
>
> 설계 §7-10-k는 편1~편6 → 편7 인바운드 링크를 **배치 마감의 별도 커밋**으로 미뤘다.
> 편7이 없는데 링크를 걸 수 없으니 그 판단 자체는 옳다. 문제는 **그 +55 B/편이 어느 편의
> 예산에도 없었다**는 것이다.
>
> | 편 | 마감 순증 | 결과 |
> | :---: | ---: | --- |
> | 1 | +114 (2자리) | 고정비용 방식이라 밴드 없음 |
> | 2 | +55 | 여유 1,845 → 흡수 |
> | 3 | +31 (§9-4 시점 표기) | 상한 **+5 초과** |
> | 4 | +55 | 상한 **+40 초과** — 마감 전에는 여유 15였다 |
> | 6 | +55 | 여유 65 → **10** |
>
> ⇒ **밴드는 편 안에서 닫히지 않는다.** 마지막 편이 발행되면 앞 편들이 다시 자란다.
> 다음 배치는 본편 예산에 **인바운드 링크 몫 60 B/편**을 미리 잡아라.
>
> 편3의 +31은 성격이 다르다 — `openjdk:21-jdk`에 「2026년 7월 기준」을 붙인 것이다.
> **시점 없는 버전 값은 시간이 지나면 틀려지고, 밴드는 원본을 나누기 위한 계획 장치일 뿐이다.**
> 발행된 편에서 군더더기를 찾아 5 B를 만드는 대신 넘기는 쪽을 골랐다.
>
> ### ⚠️ 겹친 자리는 배타가 아니라 **각자 반쪽**일 수 있다
>
> 앞선 규칙은 「두 리뷰어가 같은 줄에 다른 문안을 내면 하나를 고른다」였다. 편7에서 깨졌다.
> 한쪽은 **사실**(Jenkins 원격 트리거의 403은 방화벽이 아니라 CSRF)을, 다른 쪽은 **귀속**
> (편5가 이름만 적고 넘긴 자리)을 지적했다. 어느 하나를 고르면 나머지 절반이 사라진다.
>
> ⇒ 요구를 표로 갈라 판정하고, 갈라지면 **셋째 문안**을 세운다. 통합이 +10 B로 따로 적용
> (+20)의 절반이었다 — **겹침은 손실이 아니라 압축 기회다.**
>
> ### 가설 판정 — 6번째 표본
>
> | 가설 | 예측 | 실측 | 판정 |
> | --- | ---: | ---: | :---: |
> | **C-1** 본편 비율은 발행 순서로 커진다 | 05(2.06) 이상 | **2.10** | **지지** |
> | **C-2** Q&A 항목당 B는 원본 B로 커진다 | ≥ 496 | **503** (29,192 ÷ 58) | **지지** |
> | **C-3** 지도편 용어 비율은 단조 상승한다 | ≥ 1.81 | 0.96 | **검정 불가** — 용어를 51→26개로 억제했다 |
> | **C-4** 비율은 원본 성분에 갈린다 | 표 ≈1.4 · 비표 ≈2.3 | 표 1.14 · 비표 2.32 | 방향 지지 · 크기 미달 (n=1) |
>
> C-2가 두 번째로 지지됐다. **Q&A편은 비율이 아니라 항목 수에 건다**는 앞 배치의 전환이
> 다른 원본에서도 성립한다 — 이번 편7은 비율로 잡았으면 1.76(= 28,000 ÷ 15,916)이라
> 5배치 실측 최소보다 낮은 값을 목표로 삼을 뻔했다.
>
> ### 검사기 넷을 새로 세웠다 — 전부 자기 증명이 먼저다
>
> | 검사기 | 잡는 것 | 자기 증명 |
> | --- | --- | ---: |
> | `check-counts` | README 3자리 + CHANGELOG의 편수가 실제와 어긋나는 것 | 14/14 |
> | 앵커 실존 (`links.test.ts`) | 절이 사라져 닿지 않는 `#앵커` 링크 | 합성 9건 + 발행본 파괴 실험 |
> | 카테고리 고립 (`links.test.ts`) | 편이 아니라 **카테고리**가 떨어지는 것 | — |
> | `compose.mjs` 도식·코드 분리 | 「코드%」가 실은 「도식%」였던 앞 배치 결함 | — |
>
> `check-counts`는 마감에서 **즉시 4건을 잡았다**(README 3자리 + CHANGELOG). 편수는 틀려도
> 빌드가 깨지지 않아 조용히 낡는 자리다. 앵커 검사기는 합성 자기시험만으로는 부족해
> **발행본의 실제 앵커를 깨뜨려** 실패를 확인한 뒤 되돌렸다.
>
> ### 검사
>
> | 검사 | 결과 |
> | --- | --- |
> | `check-forbidden:verify` / `check-forbidden` | 15/15 · 158개 파일 **HARD 0** · SOFT 2(기존) |
> | `npm run build` / `check-forbidden:built` | EXIT 0 · 산출물 **456개 HARD 0** |
> | `check-baseline` | ✅ 비블로그 **14개 불변** |
> | `check-counts:verify` / `check-counts` | 14/14 · **일치** |
> | `vitest run tests/blog` | **54/54** |
> | `tsc --noEmit` | EXIT 0 |
> | `dup-scan:verify` / `dup-scan` | 7/7 · 배치 7편 **42건**(전부 카테고리 내부 · 유형 분류. 편7 4건만 전수 판정) |
> | 사이트맵 | 222 → **232** (글 7 + 태그 3) · **태그 어휘 확장 0** |
> | CRLF | 7편 전부 CR **0** |

## 2026-08-18 — `backend-engineering` **spring-batch 5편**(총 149편), Q&A편 예산을 항목 패리티로 교체

> 원본 `java-backend/05-Spring-Batch-대량데이터처리.md`(48,950 B) 1개 → 5편 **96,551 B**.
> 실측 회고는 설계서 `docs/superpowers/plans/2026-08-18-spring-batch-split-design.md` **§10**.
>
> | 편 | slug | B | 비율 | 커밋 |
> | :---: | --- | ---: | ---: | --- |
> | 1 | `batch-processing-fundamentals` (`role: "map"`) | 19,939 | 2.41 | `5262f30` |
> | 2 | `spring-batch-architecture-and-metadata` | 19,811 | 2.13 | `918d90e` |
> | 3 | `chunk-processing-and-item-readers` | 17,063 | 2.18 | `134dc87` |
> | 4 | `batch-performance-and-parallelism` | 19,297 | 2.14 | `27855d3` |
> | 5 | `spring-batch-qna` | 20,441 | 1.63 | `f85396f` |
>
> ### 🔴 Q&A편 예산은 비율이 아니라 **항목 수**다
>
> 편5를 앞 배치 실측 비율 1.49로 잡았는데 **1.63**으로 착지했다. 그 1.49는 성분 대조와
> 앞 배치 실측으로 **두 번 확인된 값**이었고, 세 번째에 틀렸다. 분모가 잘못됐다.
>
> | | 항목 수 | 실측 B | **항목당 B** |
> | --- | ---: | ---: | ---: |
> | 앞 배치 `websocket-realtime-qna` | 46 | 21,399 | **465** |
> | 이번 `spring-batch-qna` | 45 | 20,441 | **454** |
>
> 항목당 밀도는 2.4% 안에서 같다. 비율이 갈린 것은 원본이 같은 항목 수를 더 짧게 적었기
> 때문이지 이번 편이 부푼 것이 아니다.
>
> ⇒ **`목표 B = 항목 수 × 약 460 B + 고정 장치`.** 원본 절 크기 × 비율이 아니다.
>
> 앞 배치 회고가 이미 「Q&A 비율 1.77을 상수로 쓰지 마라」라고 적었는데, 그 자리에 1.49를
> **새 상수로 앉힌 것**이 이번 최대 실책이다. 상수를 바꾼 것이지 방법을 바꾼 것이 아니었다.
>
> ### ⚠️ 본편 비율은 값이 아니라 밴드로 물려줘라
>
> 본편 셋이 **2.13 ~ 2.18**에 모였다. 앞 배치 본편 셋(1.93 ~ 2.06)보다 **통째로 위로
> 이동했다.** 목표 2.0은 셋 다 초과했지만 ±10% 밴드는 셋 다 받아 냈다.
>
> ### ⚠️ 원본이 같은 재료를 두 번 적는다 — 규칙 둘이 상충한다
>
> 원본 05는 해설 절(§8·§9)에서 한 번, 트레이드오프·Q&A 절(§12·§13)에서 더 무뚝뚝하게
> 한 번 적는다. 분할하면 두 조각이 다른 편으로 간다. **세 번 만났고 세 번 다 유지했다.**
>
> 특히 Multi-threaded Step의 「언제 쓰나」는 §8-3이 권유("처리량을 올리고 싶고 Reader가
> 안전할 때"), §12-1이 경고("재시작을 포기할 수 있을 때")로 **같은 선택지를 다르게** 적는다.
> 통일하면 원본의 서술 곡선이 뭉개진다.
>
> ⇒ 이 원본에서는 **「본문과 Q&A가 일치할 것」과 「축자 중복이 없을 것」이 상충한다.**
> `dup-scan` 출력을 결함 목록으로 읽으면 안 되는 이유다.
>
> ### ⚠️ `dup-scan` 건수는 절대값으로 읽으면 안 된다 — 대조 집합이 움직인다
>
> | 스캔 대상 | 합계 | 내부 |
> | --- | ---: | ---: |
> | **이번 배치 5편** | **21** | 19 |
> | 앞 배치 5편 (동일 명령) | 84 | 82 |
> | 카테고리 전체 10편 | 134 | — |
>
> 이번 21건만 보면 「중복이 있다」로 읽힌다. **앞 배치를 같은 명령으로 재기 전까지 과다로
> 판정할 뻔했다.** 실제로는 4분의 1이다.
>
> ### 지어낸 수치 8건 제거 — 판정 기준을 다시 세웠다
>
> 원본 66행이 「배수·초 단위 실측값은 넣지 않는다」를 명시했고, 원본에 실재하는 구체
> chunk 값은 `.chunk(10, transactionManager)` **하나뿐**이다. 편1 2건 · 편3 4건 · 편4 2건.
>
> 다만 초기 기준 「원본에 없는 숫자인가」는 너무 넓어 정당한 예시까지 걷어 낸다.
> ⇒ **「틀렸을 때 독자가 시스템을 오해하는가」로 판정한다.** 「8조각 → 16조각」은 제거
> (단일 스레드 기준선이 원본에 없다), 「세 줄」은 유지 (바로 위 코드를 세면 확인된다).
>
> ### 📌 `compose.mjs` 측정 결함 — 「코드%」는 실은 「도식%」였다
>
> 펜스 안 바이트를 전부 코드로 넣는데 원본 05의 펜스 20개 중 **18개가 `mermaid`,
> `java`는 2개**다. 결론은 살아남지만(도식도 축자 이식) 라벨이 틀렸다. 마감 시점의 도구
> 변경은 이미 낸 수치의 재현성을 깨므로 수정은 다음 배치로 넘긴다.
>
> ### 검사
>
> | 검사 | 결과 |
> | --- | --- |
> | `check-forbidden:verify` / `check-forbidden` | 15/15 · 151개 파일 **HARD 0** · SOFT 2(기존) |
> | `npm run build` / `check-forbidden:built` | EXIT 0 · 산출물 **436개 HARD 0** |
> | `check-baseline` | ✅ 비블로그 **14개 불변** |
> | `vitest run tests/blog` | **50/50** (커밋 5회 전부) |
> | `tsc --noEmit` | EXIT 0 |
> | `dup-scan:verify` / `dup-scan` | 7/7 · 21건 전수 판정 |
> | 사이트맵 | 216 → **222** (글 5 + `commerce` 태그 1) |
> | CRLF | 5편 전부 CR **0** |
>
> ### 📌 이 문서가 3배치 밀려 있었다 — 128편 → 149편 보정
>
> 아래 세 배치는 발행·검증은 마쳤으나 **CHANGELOG에 기록되지 않았다.** 각 배치의 회고는
> 해당 설계서 §10에 있다. 여기서는 편수 계산만 맞춘다.
>
> | 원본 | 편수 | B | 커밋 | 누계 |
> | --- | ---: | ---: | --- | ---: |
> | `01-데이터베이스-기초` | 5 | 127,095 | `95ff1e3` · `e5cf649` | 133 |
> | `02-데이터베이스-확장` | 6 | 136,569 | `6bf6e35` · `7c79659` | 139 |
> | `04-WebSocket-실시간` | 5 | 101,448 | `3554ae2`…`173a74d` | 144 |
> | **`05-Spring-Batch`(이번)** | **5** | **96,551** | `5262f30`…`f85396f` | **149** |

## 2026-08-18 — `backend-engineering` **첫 발행 4편**(총 128편), 게이트 실효성 실측

> 발행 게이트 8조각을 세운 뒤 **처음으로 그 게이트를 통과시켜 낸 배치**다.
> 원본 `java-backend/03-Redis-캐시-성능개선.md`(46,777 B) 1개 → 4편 63,371 B.
> 카테고리 `backend-engineering`은 `categories.ts`에 등록만 돼 있고 발행본이 0편이었다.
>
> | 편 | slug | B | 커밋 |
> | :---: | --- | ---: | --- |
> | 1 | `redis-cache-design` (`role: "map"`) | 16,065 | `8420554` |
> | 2 | `cache-invalidation-and-concurrency` | 16,381 | `20282f2` |
> | 3 | `redis-storage-and-operations` | 16,430 | `dc498a5` |
> | 4 | `redis-cache-qna` | 14,495 | `8ef7ff1` |
>
> ### ⭐ 게이트가 실제로 무엇을 잡았나 — 3건
>
> | # | 무엇이 잡았나 | 무엇을 | 판정 |
> | :---: | --- | --- | --- |
> | 1 | `links.test.ts` (HARD, 커밋 차단) | 편1이 **미발행 4편을 가리키는 죽은 링크 4건** | 게이트가 옳다. 커밋 순서를 바꿔 해소 |
> | 2 | `schema.test.ts` (SOFT, 경고) | 편1 초안 **12,786 B < 하한 13,300 B** | 계획 자체의 결함이었다. 5편 → 4편 재설계 |
> | 3 | `dup-scan` (사람 판정) | 편1↔편2 **산문 축자 23자** | 수정(`76ed574`). 나머지 3건은 코드·API 리터럴이라 정당 |
>
> ### ⚠️ 코퍼스 평균 변환 비율로 편수를 잡으면 안 된다
>
> 발행본 전체의 원본 대비 변환 비율은 **1.73**인데 **이 원본은 1.40**이었다.
> 이 원본은 표 밀도가 유난히 높고, 표는 설명체로 바꿔도 거의 늘지 않는다.
> Q&A 절(§14)만 따로 재면 **1.26**이다 — 이미 「질문 + 한 줄 결론」으로 압축돼 있기 때문이다.
>
> ⇒ 5편 계획으로 시작해 편1을 쓴 뒤 **실측 1.47로 재환산하니 두 편이 하한을 깼다.** 4편으로 다시 잡았다.
> **다음 배치는 첫 편을 쓴 뒤 비율을 재측정하고 나머지 계획을 갱신하라.**
>
> ### ⚠️ 시리즈 지도편은 「완성해서 커밋」할 수 없다 — 규칙 둘이 서로를 막는다
>
> `CR-4.3`은 시리즈 첫 글에 **전체 목차**를 요구하고, 링크 검사는 **대상이 실재할 것**을 요구한다.
> 편별 커밋을 유지하면 지도편이 항상 먼저 나오므로 목차 링크가 전부 죽는다.
> 해소: 지도편을 **링크 없이** 먼저 내고, 이후 각 편 커밋에 **그 편의 목차 링크 한 줄을 함께** 넣었다.
>
> ### ⚠️ `dup-scan`에 여러 파일을 넘기면 배치 내부 복제를 못 본다
>
> 4편을 한 번에 넘기면 **0건**, 1편씩 넘기면 **10건**이 나왔다. 대상 파일 전체가 대조 집합에서 빠지기 때문이다.
> 문서화된 사용법(`dup-scan <파일...>`)이 **가장 필요한 경우(새 배치)에 거짓 0을 낸다.**
> ⇒ 새 배치는 **한 편씩** 돌려라.
>
> ### 검증 (전부 종료 코드 0)
>
> `check-forbidden:verify` 15/15 · `check-forbidden` HARD 0(130파일) · `vitest tests/blog` 50/50 ·
> `tsc --noEmit` · `build`(sitemap 196 URL) · `check-forbidden:built` HARD 0(384파일) · `check-baseline` 14개 불변.
>
> 도식은 원본 8개가 **전수 보존**됐다(편1 3 · 편2 3 · 편3 2 · 편4 0). 새로 그린 도식은 0개다.
> 카테고리 고립은 `ai-agent/ai-agent-qna-operations.md:194`(「무효화 없는 영구 캐시는 결함이다」)에서
> 편2로 링크를 걸어 해소했다 — 대상 편에 해당 내용이 실재함을 열어 확인했다(금지선 45).
> 설계·실측 전문은 [`docs/superpowers/plans/2026-08-18-backend-engineering-category-split-design.md`](docs/superpowers/plans/2026-08-18-backend-engineering-category-split-design.md).

## 2026-08-18 — **발행 게이트 2단 신설** + 규칙 105개 전수 분류

> **발행이 느려진 게 아니라 발행을 하고 있지 않았다.** 진단이 이 작업의 출발점이다.
>
> | 구간 | 편수 | 작업일 | 일평균 |
> | --- | ---: | ---: | ---: |
> | 08-07 ~ 08-08 | 89 | 2 | **44.5** |
> | 08-09 ~ 08-13 | 26 | 4 | 6.5 |
> | 08-16 ~ 08-18 | 11 | 3 | **3.7** |
>
> 12배 감속인데 원인은 변환 난이도가 아니었다. **인수인계를 뺀 작업 커밋 7개 중 발행은 1개**였고
> 나머지 6개는 이미 낸 124편을 되짚는 소급 수정이었다. 그리고 그 소급 수정 4건
> (`source` 109편 · OG 이미지 366곳 · 금칙어 13건 · 카테고리 고립 6편)이 **전부 기계가 잡을 수 있는
> 종류**였는데 자동 게이트가 **0**이었다 — 훅 없음, CI는 `lint`와 `build`만.
>
> ⇒ 한 편에서 놓친 결함이 나중에 발견되면 **124편 전수 재작업**이 된다. 발행 시점에 1편씩 걸렀다면
> 1편씩 고칠 일이 세션을 통째로 먹었다.
>
> ### 게이트 2단
>
> | 자리 | 언제 | 무엇 | 커밋 |
> | --- | --- | --- | --- |
> | `.githooks/pre-commit` | `content/blog` 커밋 | self-test → 불변식 → 금칙어 스캔 | `228e586` |
> | `.github/workflows/deploy.yml` | `main` 푸시 | 위 3개 + `tsc` + build + 산출물 2종 | `8c6fc3b` `eb32d90` |
>
> 훅은 **husky 없이** `core.hooksPath` + `prepare` 로 의존성 0으로 세웠다.
> 빌드가 필요한 검사는 훅에 넣지 않았다 — 커밋이 수십 초 걸리면 `--no-verify` 로 우회하게 되고,
> 그러면 훅이 있으나 마나다. 그것들은 CI가 받는다.
>
> ### 신설·보강한 검사기
>
> | 무엇 | 막는 사고 | 커밋 |
> | --- | --- | --- |
> | 스키마 외 프론트매터 키 차단 | `source:` 109편이 아무 검사에도 안 걸렸던 것 | `63bd4ff` |
> | 지도편 `role: "map"` (5편) | 링크 검사가 지도편을 고립으로 오판하는 것 | `e3a3a81` `6939868` |
> | 발행본 **전수** 스키마·분량 검사 | 테스트가 픽스처만 보고 발행본을 안 읽던 것 | `a28dcbf` `196a3fc` |
> | 링크 무결성·고립 검사 | `search-engineering` 6편이 통째로 고립됐던 것 | `1bf395c` `6d4a224` |
> | `GC-6` 산출물 불변 (`check-baseline`) | 비블로그 페이지가 조용히 바뀌는 것 | `bd6ede1` `490d3ba` |
>
> ### ⚠️ 「경로가 결합을 대리한다」가 틀렸다 — Critical
>
> `check-baseline` 을 「비블로그 = 경로가 `blog/` 로 시작하지 않는 것」으로 정의했는데,
> **실제 결합은 경로가 아니라 데이터 흐름을 탄다.** `pages/index.tsx` 가
> `getPostSummaries().filter(p => p.featured)` 를 쓰므로 featured 4편의 제목·요약이
> `out/index.html` 에 **JSON과 렌더 HTML 양쪽에** 인라인된다.
> ⇒ featured 글을 고치면 GC-6가 실패해 **배포가 막히고**, 유일한 복구인 `--update` 가 습관이 되면
> 검사가 아무것도 막지 못하게 된다. `490d3ba` 로 두 자리를 마스킹했다.
> 제3자 검증: featured 변경 → EXIT=0 · featuredPosts **밖** 변조 → EXIT=1 · 빈 `out/` `--update` → EXIT=2.
>
> ### 규칙 105개 전수 분류 (`059d557` · [`rule-triage.md`](docs/superpowers/reports/2026-08-18-rule-triage.md))
>
> | 판정 | 금지선 | GC | CR | 계 |
> | --- | ---: | ---: | ---: | ---: |
> | **사장** | 14 | 0 | 0 | **14** |
> | 자동 | 5 | 6 | 7 | 18 |
> | 사람 | 33 | 7 | 27 | 67 |
> | 환경 | 6 | 0 | 0 | 6 |
>
> **규칙은 자동화 가능성이 아니라 「수명」으로 먼저 갈린다.** 금지선 목록에는 일반 원칙과
> **그 배치에서만 유효한 스코프 지시**가 섞여 있었다. 금지선 8(「`06`과 `08 §4~§8·§10`은 이번 범위가
> 아니다」)은 커밋 `6a56f4a` 가 정확히 그 파일을 발행해 **사장이 증명된다** — 어겨진 게 아니라
> 유효기간이 끝났는데 목록에서 빠지지 않아 매 세션 읽혔다.
> 사장 규칙은 **삭제하지 않고** 취소선 + 사유로 남겼다(다른 문서가 번호로 참조한다 · 금지선 55).
> 사람 판단 규칙만 [`PUBLISHING-CHECKLIST.md`](docs/superpowers/PUBLISHING-CHECKLIST.md) 1장으로 모았다.
>
> ### ⚠️ 「링크가 살아 있다」와 「약속한 내용을 준다」는 다르다
>
> `CR-1.7`(링크가 실재 슬러그를 가리킬 것)과 금지선 45(대상 편이 약속한 내용을 실제로 줄 것)를
> 한 묶음으로 자동화하려 했으나 **둘은 다른 것을 요구한다.** 45의 원 사례는 링크가 살아 있었고
> 대상 편 251줄에 잇는 서술이 **0건**이었다 — 죽은 링크 검사를 100번 돌려도 통과한다.
> ⇒ 45를 「사람」으로 되돌렸다. 그대로 뒀으면 **링크 검사기 통과를 45 준수로 착각**하게 만들 뻔했다.
>
> ### 실측으로 정정한 수치 3건
>
> | 무엇 | 추정 | 실측 | 원인 |
> | --- | ---: | ---: | --- |
> | 분량 하한 미달 | 4편 | **6편** | `du -k`(디스크 블록)로 쟀다 |
> | 남은 원본 발행 규모 | 약 200편 | **233편** | 원본 **개수**로 쟀다(크기 37~90 KB로 제각각) |
> | 지도편 | 4편 | **5편** | **inbound 0**으로 식별했다(지도편이면서 inbound 1인 경우를 놓쳤다) |
>
> 셋 다 **재기 쉬운 지표가 재고 싶은 것을 대리한다**고 가정한 것이다.
>
> 설계 전문은 [`2026-08-18-publishing-gate-redesign.md`](docs/superpowers/specs/2026-08-18-publishing-gate-redesign.md),
> 구현 계획은 [`2026-08-18-publishing-gate-implementation.md`](docs/superpowers/plans/2026-08-18-publishing-gate-implementation.md).

## 2026-08-18 — OG 이미지 중립화 + 금칙어 검사기 **산출물 모드**(`--built`) 신설

> **소스가 깨끗한 것은 발행된 페이지가 깨끗하다는 증거가 아니다.** `content/blog`의 `yanadoo` 출현은 **0건**이었는데 `out/blog`에는 **366회** 있었다. 글이 아니라 글을 감싸는 템플릿이 넣고 있었고 — `components/site-head.tsx`의 `ogImagePath` 기본값 **한 줄** — 그 자리는 검사기의 스캔 범위 밖이었다.
>
> ### ⚠️⚠️ 인수인계 문서의 진단이 세 겹으로 틀렸다
>
> | 층 | 인수인계 §10-1의 서술 | 실측 |
> | --- | --- | --- |
> | ① 낱말 목록 | 「한글은 목록에 있었고 라틴 `yanadoo`가 없었다」 | **둘 다 있었다.** HARD 목록 첫 줄 |
> | ② 스캔 범위 | 언급 없음 | `content/blog`만 본다. 오염원은 `components/` |
> | ③ 경계 규칙 | 언급 없음 | `_`를 단어 문자로 쳐서 **`Ted_yanadoo`를 놓쳤다** |
>
> ③이 결정적이다. ②만 고쳐 `out/`을 보게 해도 경계 규칙 때문에 **여전히 0건**이 나왔을 것이다.
> ⇒ **원인을 하나로 지목한 진단은 나머지 원인을 가린다.** 고치기 전에 각 층을 따로 실측해야 한다.
> 이번에는 고치기 전에 `--built`를 먼저 세워 **개명 전 산출물에서 366회를 잡는 것**을 확인했다 —
> `grep -ro | wc -l`로 센 대조군과 정확히 일치했다. 그 뒤에야 개명했다.
> **먼저 고치면 이후의 「0건」이 「고쳐서 0」인지 「못 찾아서 0」인지 구분할 수 없다.**
>
> ### 밑줄은 단어 문자가 아니라 구분자다
>
> `isWordChar`가 `_`를 단어 문자로 판정해 `Ted_yanadoo.png`의 `yanadoo`가 경계 검사에서 탈락했다.
> 식별자·파일명에서 `_`는 낱말을 **잇는** 문자가 아니라 **나누는** 문자다. 경계로 바꿨고,
> 양쪽(`Ted_yanadoo` · `yanadoo_app`)을 self-test로 고정했다. 발행본 스캔에 회귀는 없다(HARD 0건 유지).
>
> ### 산출물은 「출현 횟수」로 세야 한다
>
> 기존 `scanText`는 한 줄에서 하나를 찾으면 `break`했다 — 소스는 줄이 나뉘어 있어 합리적이다.
> 그러나 **산출물 HTML은 한 줄이 수십 KB**라 페이지당 2회가 1건으로 접힌다. 판정(HARD>0 → 종료 1)에는
> 영향이 없지만 **수치가 거짓이 된다.** `--built`에만 `countAll`을 켜고, 접힘과 안 접힘을 각각 케이스로 박았다.
>
> ### 「허우용」 127회 — 인수인계의 수치도 갈라졌다
>
> §10-1은 127회를 통째로 「JSON-LD `author` 표기이며 위반이 아니다」로 판정했다. 실측은 갈렸다.
>
> | 형태 | 횟수 | 처리 |
> | --- | ---: | --- |
> | `"author":{"@type":"Person","name":"허우용"}` | **124** | 정당 문맥. 문자열로 못박아 면제 |
> | `<title>`·`og:title`·`twitter:title`의 「기술 노트 \| 허우용」 | **3** | **판정된 적 없던 것.** 제목을 「기술 노트」로 바꿈 |
>
> 후자는 `pages/blog/index.tsx` **한 곳만의 예외**이기도 했다 — 다른 블로그 페이지는 전부 `X | 기술 노트` 패턴이다.
> 면제를 「기술 블로그니까 이름은 괜찮다」로 넓히지 않고 **정확한 문자열 안쪽에서만** 적용했고,
> 「그 문맥 밖으로 새지 않는다」·「소스 스캔에는 적용되지 않는다」를 각각 케이스로 고정했다.
> **확정된 판정을 검사기가 반영하지 않으면 검사기가 영영 실패하고, 그러면 아무도 돌리지 않는다.**
>
> ### 변경
>
> | 대상 | 무엇 |
> | --- | --- |
> | `public/images/Ted_yanadoo.png` | → `Ted_profile.png` (`git mv`). **옛 경로는 남기지 않는다**(사용자 판정) |
> | 참조처 **9곳** | `site-head.tsx` 1 · `index.tsx` 2 · `en/index.tsx` 2 · `product-lead{,-v2}/index.tsx` 각 2 |
> | `pages/blog/index.tsx` | 제목에서 실명 제거 → `title="기술 노트"` |
> | `scripts/check-forbidden.mjs` | `--built` 모드 · `_` 경계 · `countAll` · 정당 문맥 면제. self-test **8 → 15건** |
> | `package.json` · `CLAUDE.md` · `README.md` | `check-forbidden:built` 등재. 발행 절차에 산출물 검사 행 추가 |
>
> ⚠️ **옛 URL은 404가 된다.** 이미 공유된 링크의 OG 미리보기가 깨진다. 복사본 유지가 가능했으나
> 「파일명에서 완전히 지운다」를 택했다(사용자 판정 2026-08-18).
>
> ### `--built`가 검사하는 것 — 「검사하는 척」을 하지 않는다
>
> `out/blog/**.html` 183개에 더해 **`out/_next/data/<buildId>/blog/**.json` 183개**를 함께 훑는다.
> 클라이언트 네비게이션용 데이터는 `__NEXT_DATA__`와 같은 props가 **별도 파일로 한 벌 더** 나가는데,
> 「HTML에 들어 있으니 됐다」로 넘기면 그 한 벌이 사각지대가 된다. 실측상 지금은 비어 있지만
> **지금 비어 있는 것과 검사 대상인 것은 다르다.** buildId는 빌드마다 바뀌므로 경로를 박지 않고 훑는다.
> 블로그 홈만 `<buildId>/blog.json`이라 `/blog/`를 지나지 않아 처음엔 **1개가 빠졌고**, 개수 대조로 잡았다.
>
> `out/blog`가 없으면 **종료 코드 2**로 막는다. 안 만든 것을 「깨끗함」으로 세지 않기 위해서다.
> 산출물에서 SOFT는 보지 않는다 — 소스에서 이미 판정한 문장이 그대로 다시 나올 뿐이다.
>
> ### 검증 (종료 코드를 볼 명령은 전부 단독 실행)
>
> | 항목 | 결과 |
> | --- | --- |
> | `check-forbidden:verify` | **15/15** · 종료 0 (신규 7건 모두 도입 전 RED 확인) |
> | `check-forbidden` (소스) | HARD **0건** · SOFT 2건(기판정) · 종료 0 |
> | `check-forbidden:built` | 산출물 **366개 파일** · HARD **0회** · 종료 0 |
> | 산출물 부재 시 `--built` | 종료 **2** (거짓 0건 차단 실측) |
> | `npm test` | **36건 통과**(3파일) · 종료 0 |
> | `npx tsc --noEmit` | 종료 0 · 출력 **0줄** |
> | `npm run build` | 종료 0 · sitemap **187 URL 불변** |
> | `out/` grep 대조군 | `Ted_yanadoo` **0회** · `Ted_profile` 416회(blog 366 + 그 외 50) |
> | 「야나두」 87회 | 전부 포트폴리오. `out/blog/` **0회** — 정책 대상 아님 |
>
> 검사 대상 366개는 `find`로 센 183 + 183과 독립적으로 일치한다.
>
> ### 도구 함정
>
> `sed -i`는 작업트리 파일을 **LF로 다시 쓴다**(CRLF였던 파일이 CR 0이 됨). 다만 `core.autocrlf=true`가
> blob을 정규화하므로 **저장소 관점의 변경은 의도한 1~2줄뿐**이다 — `git diff --numstat`으로 확인했다.
> 인수인계 §9-56이 「diff가 깨끗한 것은 무사하다는 증거가 아니다」라고 적었는데, 더 정확히는
> **작업트리는 바뀌고 저장소는 안 바뀐다.** 봐야 할 것은 `diff`가 아니라 `numstat`이다.
>
> `git status --porcelain`에 이어 `grep -rn`을 리포 전체에 돌렸다가 `.next/`를 훑어 **2분 타임아웃**이 났다.
> 제외 목록에 `out`·`node_modules`뿐 아니라 **`.next`도 넣어야 한다.**
>
> `npm run dup-scan`은 인자 없이 돌리면 「대상이 없다」로 **종료 코드 1**이다. `--category <slug>`가 필요하다.
> `CLAUDE.md`·`README.md`가 인자 없는 호출을 적어 두고 있어 함께 바로잡았다.

## 2026-08-18 — **F3 해소**: `source` 프론트매터 109편 제거 + 금칙어 검사기 신설

> **금칙어 규칙이 라틴 표기만 담고 있어 한글 표기형을 통째로 놓쳤다.** 그 결과 나온 「0건」이 거짓 음성이었는데, **거짓 음성은 「못 찾음」이 아니라 「없음을 확인함」으로 기록된다.** 실제로 직전 항목이 「124편 중 이 한 편에만 7건, 다른 4개 카테고리 0건」이라고 적었지만, 같은 카테고리의 다른 **두 편에 6건이 더** 살아 있었다.
>
> | 자리 | 무엇이 있었나 | 왜 안 걸렸나 |
> | --- | --- | --- |
> | `korean-text-search.md` ×4 · `search-engineering-qna.md` ×2 | 자판·자소 변환 표의 예시가 「패스트캠퍼스」·`fastcampus` | 목록에 `FASTCAMPUS`만 있고 한글 표기형이 없었다. `grep -i`도 고정돼 있지 않았다 |
> | `source:` 프론트매터 **109편** | 「테디노트 RAG 비법노트」·「패스트캠퍼스 LangGraph…」 | 검사 대상이 본문뿐이라 **프론트매터가 통째로 사각지대**였다 |
> | `tests/blog/frontmatter.test.ts` | 픽스처에 `source: "테디노트"` 하드코딩 | 검사 대상이 `content/` 뿐이라 **콘텐츠 밖은 아예 안 봤다** |
>
> **`source`는 화면에만 있는 것이 아니었다.** `getStaticProps`가 `post` 객체를 통째로 props로 넘기므로 Next.js가 `__NEXT_DATA__` JSON에 직렬화한다. 제거 전 한 페이지에 「테디노트」가 **3회**(본문 1 + `__NEXT_DATA__` 2) 있었다. ⇒ **렌더 블록만 지우는 것은 해법이 아니다** — 화면에서만 사라지고 페이지 소스에는 남는다. 필드 자체를 걷었다(사용자 판정 2026-08-18).
>
> ⚠️⚠️ **예시를 바꾸는 것은 낱말 하나를 바꾸는 일이 아니었다.** 표의 네 칸이 한 낱말에서 파생된 값이었다 — 초성 `ㅍㅅㅌㅋㅍㅅ` · 자소 `ㅍㅐㅅㅡ…ㅍㅓㅅㅡ` · 영타 `votmxmzoavjtm` · 한타 `ㄹㅁㄴㅅㅊ…`. 손으로 계산하면 자판 매핑 한 글자가 틀려도 **기술 문서가 거짓이 된다.** 두벌식 변환 스크립트를 짜되, **기존 값 2개를 역산해 재현되는지 먼저 확인**하고 나서 대체값을 뽑았다. 「스타벅스」를 고른 것은 같은 문서가 이미 `'스벅'→'스타벅스'`를 동의어 예시로 쓰고 있어 어휘가 일관되기 때문이다.
>
> ### 신설 — `scripts/check-forbidden.mjs` (`npm run check-forbidden`)
>
> ⚠️⚠️ **검사기를 만들자마자 스스로 거짓 양성을 냈고, 거기서 진짜 설계 규칙이 나왔다.** 「히츠」의 라틴 표기라며 근거 없이 지어낸 `heats`가 "C**heats**heet"에 7건 걸렸다.
>
> | 표기 체계 | 매칭 규칙 | 근거 |
> | --- | --- | --- |
> | 라틴 | 단어 경계 **요구** | 요구하지 않으면 `Cheatsheet`에 걸린다 |
> | 한글 | 단어 경계 **금지** | 조사가 붙어 「테디노트**의**」가 되므로 경계가 존재하지 않는다 |
> | 1인칭 | **검사하지 않는다** | 양방향 실패가 실측돼 있다 — 「메이**저는**」(위양성) · 「**내** 검색 커리어」(위음성). **못 하는 일을 하는 척하는 검사기가 가장 위험하다** |
>
> 한 규칙으로 두 표기 체계를 다루려 한 것이 오탐의 근원이었다. 이 오탐은 **self-test 케이스로 고정**했다(8건). `dup-scan`의 관례를 따라 `--self-test`를 먼저 돌리는 것을 규칙으로 삼았다 — **증명 없이 나온 0건은 거짓 음성과 구분되지 않는다.** 그리고 히어독으로 스크립트를 쓸 때 이중 백슬래시가 한 겹 벗겨져 `SyntaxError`가 났는데, **self-test가 그것을 즉시 드러냈다.**
>
> `--all`은 **판정하지 않는다.** 범위를 리포 전체로 넓히면 HARD 490건이 보고되지만 그중 발행본은 **0건**이고 나머지는 포트폴리오(`pages/index.tsx` 37 · `data/portfolio.ts` 23)와 내부 문서다. **포트폴리오가 실명을 담는 것은 위반이 아니라 목적**이므로, 판정하면 검사기가 늑대소년이 되고 진짜 위반이 소음에 묻힌다. ⇒ 적용 범위가 곧 정책이다.
>
> ### F3의 구조적 원인 — 목록이 문서에 복사돼 있었다
>
> 정본 규칙 `GC-11`은 낱말이 **5개뿐**이었고(「면접·커닝페이퍼·암기용·화이트보드·이력서」) 전부 취지 낱말이었다. 고유명사는 **정본에 아예 없었다.** 실무 목록이 20개 이상으로 자라는 동안 정본은 2026-08-07에 멈춰 있었고, 목록은 **9개 문서로 갈라져** 어느 쪽도 한글 표기형을 완비하지 못했다. ⇒ GC-11을 **「목록의 정본은 `scripts/check-forbidden.mjs`다. 문서에 복사하지 마라」**로 바꿨다.
>
> ⚠️⚠️ **지우는 조작에도 파급이 있었다.** 직전 항목이 「지우기는 안전하고 메우기가 위험하다」로 닫혔는데, 이번엔 **지운 것이 남의 근거였다.** `CR-1.10`이 *"주체를 지우는 쪽이 안전하다 — 출처는 frontmatter `source`가 이미 담고 있다"*라고 적혀 있었다. `source`를 걷자 이 규칙의 뒷받침이 사라졌다. 규칙의 앞부분(없는 주체를 세우지 마라)은 유효하되 **대조 경로가 없어져 오히려 더 엄해졌다**고 명시했다.
>
> ### 검증 (전부 단독 실행 — 파이프 뒤의 `$?`는 언제나 0이다)
>
> | 항목 | 결과 |
> | --- | --- |
> | `npm test` | **36건 통과** (3파일) · 종료 코드 0 |
> | `npx tsc --noEmit` | 종료 코드 0 · 출력 0줄 |
> | `npm run build` | 종료 코드 0 · sitemap **187 URL 불변** · `index.html` 196개 |
> | `npm run check-forbidden:verify` | **8/8** · 종료 코드 0 |
> | `npm run check-forbidden` | **HARD 0건** · SOFT 2건 · 종료 코드 0 |
> | **`out/` 잔존** | 「테디노트」·「패스트캠퍼스」·`teddynote`·`fastcampus`·`votmxmzoavjtm` **전부 0회** · 「학습 출처」 0회 · `"source":` 키 0회 |
>
> SOFT 2건은 `thirty-agent-catalog.md:195`의 `recruiter` 에이전트 기능 설명(「면접 질문」·「이력서」)이며 **정당한 문맥으로 판정**했다. 채용 담당 에이전트의 기능이지 글쓴이의 구직 활동이 아니다.
>
> **미해결 — `yanadoo`가 블로그 전 페이지의 OG 이미지 URL에 있다.** `og:image`·`twitter:image`가 `/images/Ted_yanadoo.png`를 가리켜 블로그 산출물에 **366회**(페이지당 2회) 나온다. **F3의 거울상이다** — 이번엔 한글(「야나두」)이 목록에 있었고 라틴(`yanadoo`)이 없었다. 파일명을 바꾸면 이미 공유된 링크의 OG 미리보기가 깨지므로 **사용자 판정이 필요한 별건으로 남긴다.** 같은 산출물의 「허우용」 127회는 JSON-LD `author` 표기이며 기술 블로그의 표준이라 위반으로 보지 않는다.

## 2026-08-18 — `rag` ↔ `search-engineering` **양방향 고립 해소**, 링크 4건

> **`search-engineering` 6편이 나머지 118편과 완전히 끊겨 있었다 — 외부 inbound 0 · 외부 outbound 0.** 내부 링크 20개만 있었고, 이 값은 **검산으로 확정**했다(내부 링크만 존재하면 inbound 총합과 outbound 총합이 같아야 한다 — 둘 다 20으로 일치). 카테고리 인덱스 페이지가 **유일한 진입로**였다.
>
> `rag` 25편을 전수 스캔하니 **10편**에 검색 어휘(`Elasticsearch`·`BM25`·`하이브리드`·`색인`·`토크나이저`)가 있었으나 전부 **개념 수준**이었다. 두 카테고리의 관계는 **`rag` = 검색엔진 사용자 / `search-engineering` = 검색엔진 구축자**다. 주제가 갈리는 것과 서로 안 가리키는 것은 다르다 — 층위가 갈리는 쪽이야말로 이어져야 독자가 옮겨 간다.
>
> | 방향 | 자리 | 대상 |
> | --- | --- | --- |
> | → | `rag-pipeline-retrieval` BM25 문단 | `korean-text-search` |
> | → | `rag-pipeline-retrieval` 벡터스토어 선택 절 | `elasticsearch-operations` |
> | → | `rag-qna-fundamentals` BM25 Q&A | `korean-text-search` |
> | ← | `search-system-overview:143` | `rag-pipeline-retrieval` |
>
> **rag → search-engineering 3 · search-engineering → rag 1.** 외부 inbound: `korean-text-search` **2** · `elasticsearch-operations` **1**. 빌드 exit 0 · sitemap **187 불변**(새 글 없음) · 후행 슬래시 누락 0건.
>
> ⚠️⚠️ **낱말이 같다고 이을 수 있는 것이 아니다.** 조사 단계에서 **「토크나이저」와 「색인」을 후보에서 제외**했다 — LLM의 토크나이저(BPE)와 검색엔진의 토크나이저(Nori·edge_ngram)는 다른 개념이고, 벡터스토어 색인과 역인덱스 색인도 구조가 다르다. grep으로는 완벽한 후보인데 링크하면 독자가 혼동한다. **그런데 같은 함정이 컨트롤러가 쓴 문장에서 재발했다** — 역방향 링크가 「리랭커를 어디에 두는지」라고 썼는데, 두 줄 위가 **LTR**(클릭 로그 학습)을 정의하고 링크 너머는 **Cross-Encoder**(질문·문서 동시 인코딩)라 원리·학습 데이터·배치가 전부 다르다. 리뷰가 잡아 「이 파이프라인의 LTR 자리에 Cross-Encoder 리랭커를 놓으면」으로 용어를 구분했다.
>
> ⚠️⚠️ **링크는 참조가 아니라 약속이다 — 「거기서 무엇을 다룬다」가 참이어야 한다.** 초안은 「형태소 분석기가 BM25 **점수에** 어떻게 개입하는지는 …에서 다룬다」였는데, 리뷰가 세 층으로 반박했다.
>
> | 층 | 근거 |
> | --- | --- |
> | 대상 편 실측 | `korean-text-search.md` 251줄 전문에서 **분석기와 점수를 잇는 서술 0건**. 두 주제가 서로 다른 구획에 격리돼 있다(분석기 30–88 / BM25·스코어 184–208) |
> | **출발 편이 세운 축** | `rag-pipeline-retrieval.md:15`가 「**아예 들어오지 않았거나**, 들어왔는데 **순위가 낮았거나**」로 갈라 뒀다. 대상 편의 분석기 내용은 전부 **recall 축**인데 삽입 문장은 **순위 축**에 붙였다 |
> | **리포 자신의 라우팅** | `search-system-overview.md:116`이 BM25 점수 질문의 행선지를 `korean-text-search`가 **아닌** `elasticsearch-architecture`로 안내한다 |
>
> ⇒ 「점수 개입」을 **「BM25가 세는 「단어」가 무엇인지는 색인 시점에 분석기가 정한다」**로 낮췄다. **「내가 보기에 과장이다」가 아니라 「글이 스스로 세운 규칙을 어겼다」는 논증이라 반박할 수 없다.**
>
> ⚠️ **인용문은 이 코퍼스에서 단정으로 끝나는 장치다.** 초안이 인용문(`>`) 안에 「단 …비용이 포함된다」는 단서와 링크를 넣어 **방금 준 결론을 같은 강조 블록 안에서 되물렀다.** 리뷰가 다른 인용문 4개(`korean-text-search:45`·`:119`·`elasticsearch-operations:99`·`:265`)를 실측해 「단서로 끝나는 인용문은 신규 문장이 유일하다」를 보였다. ⇒ 인용문 밖 별도 문단으로 뺐다.
>
> **미해소** — `search-engineering-qna`·`search-system-overview` 두 편은 inbound가 여전히 **0**이다. 조사·리뷰 모두 「억지 아닌 자리가 없다」로 판정했고 **개수를 채우지 않았다.** 다만 `search-system-overview`는 outbound를 얻어 카테고리가 밖으로 뻗는다.

## 2026-08-18 — `search-engineering` 사례편 **익명화 + 한영검색 사실오류 정정**, 리뷰 3기

> **`search-engineering-in-practice.md` 한 편에 사명·경력 연표가 남아 있었다.** 이 카테고리 6편은 `date: 2026-07-25`로 **금칙어 규칙 확립(2026-08-08) 이전에 발행**돼 규칙이 적용되지 않았다. 실측 결과 금칙어는 **124편 중 이 한 편에만 7건** 집중돼 있었다(다른 4개 카테고리 0건). 회사명·서비스명은 「OTT 서비스」·「IPTV 서비스」로 바꾸고, 「시기·회사·서비스·검색엔진·역할」 5열 **경력 연표를 「검색엔진·규격의 출처·다룬 범위·대표 산출물」 4축 기술 비교표로 재구성**했다. 「내 검색 커리어의 성장 축」·「핵심 차별점」 같은 1인칭·자기홍보 어조도 걷었다. 제품명(코난테크놀러지·IDOL·Elasticsearch)은 **유지**했다 — 개인을 특정하지 않으면서 글의 기술적 값어치를 지탱하는 정보다. 8,528 → 8,136 B · 구조(H2 5 · H3 6 · 표 4 · mermaid 3) 불변 · 빌드 exit 0 · sitemap **187 불변**.
>
> ⚠️⚠️ **수정 전부터 있던 사실오류를 리뷰가 잡았다 — 한영검색 예시가 자기 전문 영역에서 틀렸다.** 용어표가 「자판 오타를 교정해 검색. 예: "tjf" → "설", "샬" → "shal"」이라 적었는데, 리뷰어가 **두벌식 매핑을 스크립트로 왕복 검증**해 두 번째 예시가 자판 매핑이 아니라 **로마자 음차**임을 밝혔다.
>
> | 검증 | 결과 |
> | --- | --- |
> | 설 = ㅅㅓㄹ | `tjf` — 예1 정확 |
> | 샬 = ㅅㅑㄹ | **`tif`** — 글의 `shal`이 아니다 |
> | `shal` → 한글자판 | ㄴㅗㅁㅣ = **「노미」** — 반대 방향도 아니다 |
>
> 같은 칸의 정의문과 어긋나고 `:124` 표의 「한↔영 키맵 변환 로직」과도 어긋났다. `"tif" → "샬"`로 정정했다. **번짐은 없었고**(전 리포 1건), 같은 카테고리의 다른 편은 전부 자판 매핑으로 일관한다(`korean-text-search.md:12`의 「"tjfmf" → "설을"」은 검증상 정확).
>
> ⚠️⚠️ **익명화에서 위험한 것은 「지우는 조작」이 아니라 「빈자리를 메우려 새로 쓰는 조작」이었다.** 지우기는 안전했다 — 정보 소실은 「시기」 2칸뿐이고 논리를 끊지 않았다(리뷰 대조표 결함 0건). 반면 **새로 쓴 자리는 예외 없이 결함을 냈다.**
>
> | 새로 쓴 자리 | 무엇이 무너졌나 |
> | --- | --- |
> | 표 「규격의 출처」 행 신설 | 2기를 「직접 정의」 한 값으로 뭉갰는데 같은 화면 mermaid는 `IDOL(상용) → ES(오픈소스)` 두 국면으로 그린다 — **논지를 지탱하는 칸이 모순** |
> | 리드 문장 재작성 | 「서로 다른 회사·규모」를 빼자 남은 축이 서두와 같아져 **재진술** |
> | 벤더명 「3SoftPlus를 통해」 삭제 | 관형절을 받치던 부사구가 사라져 「도입**해** 사용했다」로 **주술 파괴**, 고친 뒤에도 「도입**한** … 사용했다」로 **동어반복** |
> | 회사·서비스 열 삭제 | 1기 「N-Screen 통합 API」와 2기 「IPTV N-Screen」의 **구분자가 함께 사라졌다** — 두 열이 구분자였다 |
> | 한 칸을 두 행으로 분할 | 「다룬 범위」와 「대표 산출물」이 **가로로 어긋났다**. 한 칸일 때는 불일치가 생길 수 없었다 |
>
> **⇒ 익명화는 낱말을 빼지만, 그 낱말이 지탱하던 문법·논리 구조가 함께 무너진다.** 빈자리를 메울 때가 결함이 들어오는 지점이다.
>
> ⚠️ **개별 대안이 각각 옳아도 동시 적용하면 충돌한다.** 리뷰 1판이 헤딩(`:32`)과 리드(`:34`)를 **다른 항목으로 따로** 지적했고 각 대안을 그대로 채택했더니, 두 줄이 두 칸 거리라 「두 번의 구축」·「나란히」를 공유하게 됐다. 리뷰어가 2판에서 **자기 제시 방식에 책임이 있다**고 적었다. ⇒ **항목별 리뷰라는 형식 자체의 사각지대**다. 반영은 한 파일에서 동시에 일어나므로 두 대안의 물리적 거리를 봐야 한다.
>
> ⚠️ **검사 명령이 양방향으로 실패했다.** 1인칭 검사 `grep -nE "저는|제가|…"`가 잡은 3건은 전부 거짓 양성(「동시성 문**제가**」·「메이**저는**」·「문**제가** 없다」)이었고, 진짜 1인칭 「**내** 검색 커리어」는 패턴에 `내`가 없어 **거짓 음성**이었다. 확장 패턴 `내 [가-힣]{2,6}`을 쓰면 이번엔 「인덱스 **내** 문서」·「규격 **내** 지원」 같은 한자어 「내(內)」가 대량 오탐된다. **⇒ 한국어 1인칭은 grep으로 분리되지 않는다. 매칭 줄을 열어 읽는 수밖에 없다.**
>
> ⚠️ **파이프 뒤의 `$?`는 마지막 명령의 종료 코드다.** `grep … | sort | uniq -c` 뒤의 `$?`는 `uniq`의 것이라 **언제나 0**이다. 0건 판정 근거로 쓰면 「0건」을 「히트 있음」으로 읽는다. 검증자가 스스로 이 함정을 발견해 grep 단독 재실행으로 재확증했다. 기존 규칙 `grep … || echo "0건"` 금지에 **이 항목을 추가한다.**
>
> ⚠️ **마크다운 강조가 낱말과 조사 사이에 끼면 grep이 못 잡는다.** `**상용 엔진 IDOL**을 사용했다`는 `IDOL을`로 검색되지 않는다. 한국어는 조사가 낱말에 붙어 이 문제가 구조적으로 생긴다(영어는 `**word** particle`로 공백이 있어 안 생긴다).
>
> ### 미해결로 남긴 것
>
> | # | 항목 | 내용 |
> | ---: | --- | --- |
> | **F1** | 익명화의 전제가 사이트 구조상 무효 | `pages/index.tsx`가 실명·회사·재직기간을 공개하고(`:71`·`:291`·`:297`·`:324`·`:327`·`:333`) 모든 블로그 페이지가 거기로 링크한다(`components/blog/blog-shell.tsx:94-101`, 코드 주석에 「요구사항 FR-4.3」). 블로그의 「1기 OTT / CMS·랭킹추천 / N-Screen」이 홈페이지 `:333`과 1:1로 매핑된다. **이 수정이 실제로 한 일은 신원 은폐가 아니라 「기술글이 이력서처럼 읽히는 것」을 막은 것이다** |
> | **F3** | 금칙어 규칙이 한글 표기형·소문자를 놓친다 | 목록에 `FASTCAMPUS`·`teddynote`가 **라틴 표기로만** 있어 「패스트캠퍼스」·「테디노트」가 검사에서 빠진다. `source:` 프론트매터 **109편**이 해당하고 `pages/blog/[category]/[slug].tsx:69-73`에서 **「학습 출처: …」로 공개 렌더**된다. 「ai-agent·rag 0건」이라는 기존 측정치가 거짓 음성이었다 |
>
> **리뷰 3기 · 검증 3회.** 익명화 완결성 축(조건부 · F1~F8) / 글 완결성 축(조건부 · 「반드시」 4건 → 2판에서 신규 1건) / 최종본 신규 검토 축(조건부 · 「반드시」 6건, 지적 목록을 주지 않은 새 눈). **두 리뷰어가 같은 줄(`:34`)을 다른 이유로 지적**해 그 자리가 진짜 문제임이 드러났다.

## 2026-08-17 — `ai-transformation` T7 발행 **1편**(총 124편), **카테고리 11/11 완료**, 에이전트 33기

> **발행본 열 편을 세 축으로 갠 지도편 `ai-transformation-knowledge-map`을 발행했다. 이로써 이 카테고리가 닫힌다.** **33,025 B · 197줄 · H2 7 · 표 10 · mermaid 2 · 내부 링크 107**. sitemap **186 → 187 URL** — 태그 4종이 전부 기존이라 태그 페이지는 **52 → 52**로 늘지 않았다(대상 파일을 빼고 넣어 전후를 실제로 재현해 확인). 빌드 exit 0 · md 124 ↔ `out/` 글 페이지 124 슬러그 집합 `diff` 완전 일치 · mermaid 2개 실브라우저 SVG 치환 확인.
>
> ⚠️⚠️ **이 배치의 지배적 결함은 하나의 형태를 여섯 번 반복했다 — 「같은 대상을 두 자리에서 재는데 술어의 넓이가 다르다」.** 값은 매번 정확했고, **좁은 술어로 잰 값을 넓은 술어에 그대로 옮긴 것**이 결함이었다.
>
> | # | 전칭 | 실제 | 잡은 시점 |
> | ---: | --- | --- | --- |
> | 1 | 「**두 편에서** 작업 틀 일반을 뜻하고」 | 세 편 | 재검증 1기 |
> | 2 | 「비용을 주어로 삼는 절이 **없다**」 | 두 곳 | 5축 검증 E |
> | 3 | 「**한 주제의 2차만** 편을 단위로」 | 두 주제 | 5차 검증 |
> | 4 | 「**다른 열세 주제는** 절이 서 있는데」 | 열두 주제 | 5차 검증 |
> | 5 | 「**안에서는 갈리지 않고** 밖과 갈리며」 | 세 층위로 갈린다 | 최종 검증 2 |
> | 6 | 「**셋째는**(=셋째만) 밖과도 갈린다」 | 오케스트레이터도 밖과 갈린다 | 최종 검증 3 |
>
> **⇒ 값을 고치면 다음 전칭이 나왔다. 끝낸 것은 「세는 것을 그만두기」였다.** 6차 반영이 「한 주제의 2차만」을 「표의 행이나 열로 있는 자리**도 있고**」라는 **존재 주장**으로 바꾸자 그 계열이 닫혔다. 어법의 선례가 발행본 안에 있었다 — `discovery-to-growth-tooling.md:88`이 「**쓰인 층위가 자리마다 다르다**」로 개수를 안 센다.
>
> ⚠️⚠️ **결함이 표가 아니라 「표를 가로로 요약한 문단」에서 났다.** 층위 표 4행은 각 항목을 **독립적으로** 서술해 안전한데, 요약 문단은 넷을 **서로 비교**해 「첫째는 A이고 셋째만 B다」라는 배타 전칭을 자동으로 만든다. 그 구역에서 결함이 **다섯 번 연속** 났고, 마지막에 **요약 두 문단(1,302 B)을 걷어내자** 결함 생산 자리 자체가 사라졌다. 표는 그대로 두었고 링크 107은 유지됐다. **⇒ 고치기보다 덜어내기가 나은 경우가 있다.**
>
> ⚠️⚠️ **정확한 글이 안 읽힐 수 있다 — 검증 19기 중 「좌표를 하나도 주지 않은 독자 축」 둘만 그것을 봤다.** 좌표 대조 축들이 「이 서술이 참인가」에 전부 참을 냈는데, 독자 축은 **첫 독자가 실제로 쓰는 경로 표에 닿기까지 91줄**이고 그 앞이 **아직 아무것도 안 읽은 사람에게 재방문자의 문제**(뜻이 158줄 뒤에 있는 이름 셋)와 **블로그 내부 사정**을 내민다고 보고했다. ⇒ **3차 반영에서 「목적별 읽는 순서」 절을 세 번째에서 맨 앞으로 옮겼고, 재검증에서 91줄 → 20줄로 확인됐다.** 내용은 한 줄도 버리지 않았다. **⇒ 검증 축은 「이 문장이 참인가」를 묻지 「독자가 이 문장을 필요로 하나」를 묻지 않는다. 정확성 검증이 강할수록 글에 자기 감사가 쌓인다.**
>
> ⚠️ **제목이 건 네 사례 중 하나가 성립하지 않았다.** 「사람 게이트」의 「루프 종료 조건」 뜻은 이 카테고리에 없고 두 편이 제목·본문에서 **명시적으로 기각**한다. 최종 검증이 **표의 빈칸을 증거로** 잡았다 — 네 행 중 그 행만 뜻 칸 두 개에 출처 편 링크가 없었다. 그런데 **수정이 또 틀렸다** — 「이 카테고리는 이쪽으로만 쓴다」로 고쳤더니 **같은 행 4열이 「또 다른 층위가 있다」고 적고 있었다.** 한 행을 **가로로** 읽으면 두 칸이 서로를 반박했다. ⇒ 실상은 **안에서도 자리마다 갈린다**였고, 그 편이 오히려 제목의 주장을 더 잘 받쳤다. **⇒ 표를 세로(열)로만 읽으면 안 보인다. 「한 행을 가로로 읽어라」가 검증 습관에 추가됐다.**
>
> ⚠️ **검사 명령의 실행 실패를 통과로 읽는 함정이 두 번 났다.** ① `LC_ALL=C grep -P '[\x{1F300}-\x{1FAFF}]'`는 **「범위 초과」 에러로 죽는데** 무출력이 0건으로 보인다 — UTF-8 바이트 패턴 `'\xf0\x9f|\xe2\x9c|\xe2\x9d|\xef\xb8\x8f'`를 써야 한다. ② `grep … || echo "0건"`은 **파일이 없어 실패한 것**과 0건 발견을 같은 문자열로 만든다.
>
> ⚠️ **낱말 빈도를 그대로 쓰면 안 된다.** 「비용」 18회 중 하나가 「**대비용**(對比用)」이었다. **컨트롤러가 이 오탐으로 결함 1건을 잘못 올렸고 반영자가 반증했다** — 검증자가 이미 「raw grep은 18, 오탐 1건 배제 필요」로 정확히 적어 둔 것을 컨트롤러가 되돌린 셈이다.
>
> ⚠️ **축자 복제 스캔은 「같은 뜻 다른 글자」를 원리적으로 못 잡는다.** 구현자가 자체 검사기(임계 20자·4단 자기검사)로 선례와의 축자 복제 12건(최장 54자)을 잡아냈는데 **편10의 하네스 정의는 놓쳤다** — 글자가 달라서다. 구현자·컨트롤러가 같은 도구를 써서 같은 자리에서 멈췄다.
>
> ⚠️ **브리프가 「미확정」이라 적는 것 자체가 실측이 필요한 주장이다.** 컨트롤러가 「오케스트레이터 갈림은 구현자의 판정이다」로 넘긴 자리를 **조직 설계 편이 이미 본문에서 해소해 두었다.** 그대로 갔으면 그 편의 판정을 지도편이 가져오는 형태가 됐다(금지선 32 ③).
>
> **규모** — 에이전트 **33기**(조사 6 · 구현 1 · 검증 19 · 반영 7), 보고서 합계 약 **420 KB**. 반영 7차례(+3,624 → −155 → +767 → +590 → −56 → +90 → −1,135 B). **컨트롤러 판정 3건이 하위 에이전트에게 반증**됐고, **반영자 판정 2건이 검증에 뒤집혔다.** 리뷰어 보고서 미작성이 **3회** 발생 — 유휴 알림을 받으면 요약 메시지 유무와 무관하게 **파일 존재부터 확인**해야 한다.

## 2026-08-17 — `ai-transformation` T6 발행 **1편**(총 123편), 카테고리 10/11편, 검증 77건

> **`aiteam/08 §3`(가용 5,707 B) + 발행본 9편에서 수급한 10문항을 편10 `ai-transformation-qna-org-operations`로 발행했다.** 카테고리 11편 중 **10편**이 나갔다. **27,673 B · 230줄 · H2 20 · Q문항 18 · 표 2 · 코드펜스 0**. sitemap **185 → 186 URL** — 태그 4종이 전부 기존이라 태그 페이지는 늘지 않았다. npm test 36/36 · tsc 0 · build 0.
>
> ⚠️⚠️ **이 편은 앞의 9편과 성격이 반대다 — 쓰는 글이 아니라 연결하는 글이다.** 원본 `08 §3`의 9문항은 `01`+`02`+`07`을 면접 답변으로 압축한 것이고, **그 `01`+`02`+`07`이 이미 T3에서 편1로 발행됐다.** 즉 **원본 §3은 편1의 축약판**이라 팽창시키면 편1과 전면 중복이 난다. 그래서 답변을 「결론 한 줄 + 근거 2~3문단 + 넘김 문장」으로 고정하고 표·수치·각론을 전부 링크로 넘겼다. **기발행 Q&A 11편이 14~26 KB로 성립하는 이유가 이것이다.**
>
> **⇒ 그 결과 중복의 지위가 뒤집힌다.** 여느 배치에서 「이미 편1에 있다」는 결함이지만 이 글에서는 설계이고, **결함은 「이미 있는 것을 링크로 넘기지 않고 다시 풀어 쓴 자리」**다. 검증 축 E의 정의를 배치별로 재정의해야 한다는 규칙(설계서 §9-1)이 축 E에까지 적용된 첫 사례다.
>
> ⚠️ **인수인계가 경고한 「하한 미달 위험」은 성립하지 않았다.** 소스가 5,707 B뿐이라 팽창률 1.63~2.71을 곱하면 8.2~15.5 KB로 하한 13.3 KB에 걸린다는 우려였는데, **Q&A 편의 분량은 원본 크기가 아니라 문항 수가 정한다.** 기발행 11편 실측으로 문항당 585~1,631 B(`agentic-coding` 1,197~1,631)를 얻어 18문항 → 21.6~25.2 KB로 예측했고 실측 27,673 B가 나왔다. ⚠️ **예측이 하한을 벗어난 것은 요약절·용어 정리·도입·마무리라는 고정 절 약 7 KB를 예측식이 빠뜨렸기 때문**이고, 이는 구현자가 반증했다.
>
> ⚠️⚠️ **이 배치 결함의 최대 무리는 「좌표는 정확한데 가리키는 것이 다르다」였다.** 축 A의 착지점 오류 5건, 축 B의 「서술 순서를 규범으로」 2건이 전부 이 형태다 — 인용한 좌표는 실재하고 문면도 정확한데 **그 문면이 내 주장을 받치지 않는다.** 대표 사례 둘: ① 「고객 접점 부서를 먼저 자동화하라」의 근거가 실은 **편7이 먼저 발행됐다는 사실**이었다(3계열 검색 0건, 원본 §3에 부서 이야기 없음) ② 「순서를 지키는 것이 요점이다」의 근거인 편9 `:17`은 「이 세 겹이 **원 자료의 순서 그대로다**」라는 **서술 순서** 진술이었다.
>
> **⇒ 원인은 이 글의 구조에 있다.** 여느 편은 자기 내용만 책임지면 되는데 **색인 글은 링크 30개마다 「저 글에 무엇이 있다」는 검증 가능한 주장을 함께 진다.** 결함이 더 난 것이 아니라 더 많이 드러나는 구조다.
>
> ⚠️⚠️ **「삭제는 두 가지를 남긴다」에 세 번째가 추가됐다 — 그 문장이 예외·한정을 제공하던 전칭.** 1차 반영이 「자격증명도 같은 이유로 밖에 남는다」를 지우자 **7줄 위의 「…훅이 전부 레포 파일이 된다」가 반례 없이 홀로 남았다.** 반영자는 금지선 31의 두 확인(① 사본 grep ② 앞뒤 5줄)을 실제로 돌리고 출력까지 실었는데도 놓쳤다 — **영향받은 것이 「앞뒤 5줄」이라는 기계적 범위 밖**이었기 때문이다. ⇒ 2차 브리프에 **「지우려는 문장이 속한 절 전체에서 전칭을 먼저 찾아라」**를 세 번째 확인으로 넣었다.
>
> ⚠️ **귀속에 세 번째 방향이 있다 — 다른 편의 판정을 내 판정으로 만드는 것도 강등이다.** 컨트롤러가 「편2 문장을 68자 축자로 가져오면서 「이 배정은 이 글의 정리다」 귀속을 뗐다」를 금지선 32 위반으로 판정했으나, **최종 검증 `review`가 반증했다** — 편2가 자기 판정에 귀속을 달았으므로 **편10이 같은 표지를 달면 그 판정이 편10의 것이 되어 버린다.** 붙이지 않는 것이 옳다. 같은 논리로 **요약표 8행을 전건 대조한 결과 이 글이 새로 만든 기준은 0개**였고(편1 `:233`·편7 `:15`·편6 `:136`·편5 `:423`·편4 `:70`·원본 §3 유래), 그래서 「이 글의 정리다」 어형이 글 전체에 0건인 것이 **정상**으로 판정됐다.
>
> ⚠️ **인수인계의 「확정된 결정」이 하나 반증됐다** — 「`ai-transformation`은 `series`·`seriesOrder`·`source`를 쓰지 않는다」는 전칭으로 거짓이다. **실측: 편6·7·8이 `series: "department-agents"`, 편6·7·8·9가 `source`를 쓴다(4/9).** 실제 규칙은 「`aiagent/*` 원본에서 온 편만 쓴다」이고, 편10 생략은 결과적으로 맞지만 **근거가 「카테고리 관례」가 아니라 「시리즈에 속하지 않고 소스가 `aiteam`이다」**로 바뀐다. T7에 그대로 승계하면 안 된다.
>
> **컨트롤러 오류 8건**(T3 2건 → T4 8건 → T5 9건 → **T6 8건**). **6건이 「브리프를 쓰는 단계」에서 났고 그중 4건이 같은 형태 — 조사자나 앞 문서의 요약을 검증 없이 옮긴 것**이다. 좌표는 전부 실재했다. 특히 **판정표가 축 A의 「높음」 1건을 채택·이월 어디에도 옮기지 않아 통째로 증발**했고 1차 반영자가 되잡았다 — **지적을 찾는 것과 그것을 처방 목록으로 옮기는 것은 다른 작업이고, 후자에는 검증자가 없었다.**
>
> **검증은 5축 54건(차단 0 · 교차 지목 13자리) · 재검증 2기 23건 · 최종 3기 전원 「발행 가능」.** 1차 반영 **−2,495 B**(채택 29 → 반영 22·변형 5·거부 2), 2차 반영 **−333 B**로 순 **−2,828 B**이고 2차 목표 3종(**새 문장 0자리** — 마침표 191→188로 삭제 3문장과 정확히 일치 · 구조 불변 · 9편 링크 유지)을 전부 달성했다. **하류가 판정표를 11건 반증했다**(1차 8 · 2차 3) — 검증 축 A·C가 낸 「높음」·「보통」이 **오판으로 되잡힌 것도 2건** 있다.
>
> **통독 역할이 다시 값을 했다.** `read`에게는 **좌표가 하나도 없는 별도 브리프**를 줬다 — 어느 자리가 고쳐졌는지 알면 다른 아홉 명과 같은 것을 보게 되기 때문이다. read가 아무도 못 본 것을 잡았다: `:143` 「여섯 단계로 자르고 **단계 사이마다** 게이트」(=5) ↔ `:145` 「앞의 둘·가운데 둘·뒤의 둘」(=6). **검증자 열 명이 이 절의 「3/3 분할」을 고치는 데 시선이 쏠려 두 줄 위의 「사이마다」는 아무도 세지 않았다.**

## 2026-08-17 — `ai-transformation` T5 발행 **1편**(총 122편), 12배치 이월 「훅」 해소, 검증 88건

> **`aiteam/09` + `09a` + `09b`(가용 20,481 B)를 편5 `lean-three-layer-harness`로 발행했다** (`e83cfd3`). 카테고리 11편 중 **9편**이 나갔다. **41,937 B · 469줄 · H2 10 · H3 11 · 표 14 · 코드펜스 9블록 · 도식 3**(팽창률 **2.048**). sitemap **184 → 185 URL** — 발행본 1편분이고 태그 4종이 전부 기존이라 태그 페이지는 늘지 않았다. npm test 36/36 · tsc 0 · build 0.
>
> **분량 예측을 코드펜스와 산문으로 갈라 계산해야 했다.** 이 배치의 원본은 **22.5%가 코드펜스**이고 `09b`만 보면 **55.8%**다 — 「복사해서 레포에 두라」는 wiki 템플릿 파일들이 통째로 들어 있다. **코드펜스는 산문으로 팽창하지 않으므로** 전체에 팽창률을 곱하면 과대 추정이 된다. 산문 15,720 B × 1.7~2.5 + 펜스 4,761 B × 1.0 = **31~44 KB**로 잡았고 실측 41,937 B가 그 안에 들어왔다.
>
> ⚠️ **12배치 동안 이월된 「훅 차단 채널이 세 자료에서 갈린다」를 해소했다. 결함은 하나가 아니라 셋이었다.** ① **발행본의 차단 채널이 4갈래**다 — `settings-permissions-deny:359`(exit 2+stderr) · `commands-skills-hooks-plugins:268`(stdout JSON) · `rules-hooks-skills-layers:188~193`(**같은 표 안 자기모순**) · `harness-five-primitives:257`(`permissionDecision`). ② **「훅」이라는 낱말이 원본 3자리에서 다른 것을 가리킨다** — 하네스 훅 · git `pre-push` · 「훅/스케줄」. ③ ⚠️ **`09:50`이 「로컬 선의로는 게이트를 강제할 수 없다」고 못박은 자리와 `09a:95`가 로컬 `pre-push` 훅을 「결정적 게이트 → 강제」라 부르는 자리가 부딪힌다.**
>
> **셋째가 이 배치 고유의 위험이었다 — 원본에서는 다른 파일이라 보이지 않았고 한 편으로 합치는 순간 드러난다.** 해소는 원문이 지지했다: 「강제」의 목적어가 다르다. 훅이 강제하는 것은 **리뷰의 실행**이고 머지 차단은 서버 approval rules다. 그리고 **발행본은 이미 처리 규범을 확립해 두었다** — `rules-hooks-skills-layers:216` 「**이 글은 그 판정을 뒤집지 않는다** … `exit 2` 직후의 출력 채널을 자기 환경에서 직접 확인한다」. **원본이 채널에 침묵하므로 편5도 침묵했다.** 채널을 하나 골랐다면 나머지 3편과 모순되고, 카테고리가 확립한 판정 유보 규범을 편5 혼자 깨는 모양이 됐을 것이다.
>
> ⚠️ **`09b` 코드펜스 안에 예시로 위장한 사내 데이터 8자리가 있었고 민감정보 패턴이 전건 놓쳤다.** `[[서비스-영어회화]] — OpenAI API+Google STT 발음평가 기능` · `[[팀-커머스]] — 커머스 스쿼드 구성·오너십` · `sources/2026-06-15-커머스기획.md` 등이다. **패턴은 `야나두`·`커머스개발`을 찾는데 원문은 회사명 없이 제품·조직만 쓴다** — 어휘 목록으로는 원리적으로 검출되지 않는 형태다. 걷어내지 않고 일반 예시로 치환했다(폴더 구조 예시는 구조를 보이는 것이 목적이라 예시가 비면 뜻이 사라진다). **선례가 이미 있었다** — `claude-md-scope-layers:249~`가 템플릿 내용을 `[프로젝트명]`·`[폴더명]` 대괄호 플레이스홀더로 익명화하고, `:269`에서 중첩 코드블록을 `~~~`로 바꾼 사실까지 본문에 공시한다.
>
> ⚠️ **「스캔 0건은 관찰값이다」의 한 단계 심화 — 검사 유효성 대조를 통과한 0건도 거짓 음성일 수 있다.** 조사 α가 「`rag` 25편에 RAG를 쓰지 말아야 할 조건을 다룬 편 0건」을 보고했고 **유효성 대조까지 마쳤다**. 그런데 `agent-harness-architecture`가 이미 같은 주장을 발행하고 있었다 — `:17` 「RAG를 "반드시 해야 하는 것"에서 "**규모에 따라 고르는 것**"으로 **격하**시킨다」 · `:325` 「**규모로 분기**한다 … 소규모·고빈도는 `.md` + 인덱스로 즉시 전환」. **α의 패턴(`RAG가 필요 없|RAG를 쓰지|…`)이 「격하」·「위상이 바뀐다」를 덮지 못했다.** 유효성 대조는 「**패턴이 작동하는가**」를 증명할 뿐 「**패턴이 개념을 덮는가**」는 증명하지 않는다. ⇒ 개념 검색은 직설어·완곡어·반의어 3계열로 나눈다.
>
> ⚠️⚠️ **삭제 처방은 두 가지를 남긴다 — 1차 반영에서 두 자리가 「고쳤는데 또 나왔다」로 돌아왔고 원인이 서로 달랐다.** ① **같은 주장의 다른 사본** — 판정표가 `:90` 한 좌표만 지목해 고쳤는데 `:76`·`:82`에 같은 전칭이 남아 열 줄 간격으로 부딪혔다. ② **그 문장이 떠받치던 근거** — 앞부분을 지웠더니 남은 문장이 「이 층을 여는 제목이 …라고 범위를 정하고 있다」로 **발행본에 없는 제목**(원본 `09:20`의 H3)을 가리키게 됐다. **T4가 확립한 「지운 뒤 지시대상을 확인하라」는 ②만 막는다.** ①은 **핵심어로 같은 주장을 다시 전수 grep**해야 잡힌다.
>
> ⚠️ **귀속 오류에 반대 방향이 있다는 것이 처음 관측됐다.** 지금까지 이 프로젝트의 귀속 규칙은 전부 「창작에는 「이 글의 정리다」를 붙여라」 한 방향이었다. 그런데 컨트롤러 판정표가 비교표 오른쪽 열 전체에 귀속을 붙이라고 처방했고, **1차 반영자가 「규모 행의 「그 이상」은 원본 `09:41` 축자」임을 실측해 되잡았다.** 지시대로 했으면 **원본에 있는 것을 이 글의 창작으로 강등**할 뻔했다. 반영자의 정리: 「**귀속 오류는 창작을 원본으로 미는 방향만 위험한 것이 아니다.**」
>
> **오탐 판정과 「손대지 마라」는 동치가 아니다.** 컨트롤러가 `09a:44` 「신규 **입사**자를 온보딩할 때」를 새 오탐으로 올리고 「고치지 마라」로 분류했는데, 구현자가 **원본 자신이 같은 행에서 「신규 합류자」를 쓴다**는 것을 찾아 축자 손실 없이 치환했다. **의미 손실 없이 치환 가능한 오탐은 치환하는 쪽이 낫다 — 검증 노이즈는 다음 배치가 계속 지불하는 비용이다.** 손대면 안 되는 것은 *고치면 뜻이 바뀌는* 오탐이다.
>
> **컨트롤러 오류가 9건 나왔고 전부 하류가 반증했다**(T3 2건 → T4 8건 → **T5 9건**). 조사 브리프의 awk가 `FNR`이 아니라 `NR`을 써서 **조사 보고의 좌표가 전부 무효**가 됐고, mermaid 카운트 명령이 `next`에 걸려 **3개를 0개로 오보**했고, 변환 브리프가 고유값표에서 「LLM Wiki」라는 이름이 이미 발행됐음을 놓쳤고(`agent-equals-model-plus-harness:37·119·168`, **Karpathy 귀속까지**), 링크 규약이 **선례 1건짜리 소수 패턴**으로 유도했고, 훅 범위 한정 지시가 105줄 뒤 자기 예시와 충돌했다 — 외 4건. **「지시하는 쪽이 가장 많이 틀렸다」가 세 배치 연속 재현됐고 건수가 늘고 있다.**
>
> **검증은 5축 62건 · 재검증 2기 26건(교차 지목 6자리) · 최종 3기 전원 「발행 가능」.** 1차 반영 **+492 B**, 2차 반영 **−644 B**로 순 **−152 B**이고 2차 목표 3종(바이트 음수 · 새 문장 0자리 · 구조 불변)을 전부 달성했다. **「1차는 늘고 2차는 준다」가 T4에 이어 재현됐다.** 최종 검증은 역할을 exec·review·**read**로 나눴다 — 앞의 열 명이 전부 좌표 단위로 봤으므로 **아무도 「글로서 성립하는가」를 보지 않았다**는 판단에서 통독 역할을 따로 세웠고, read가 낸 12건 중 10건이 다른 축과 겹치지 않는 독립 발견이었다.
>
> **편4·편2에 편5 링크를 넣었다.** 편4 `:59`·`:398`이 「그 얇은 쪽 아키텍처는 **후속 편에서 다룬다**」로 예고해 두고 링크가 없어 **편4 독자가 편5에 도달할 경로가 없었다.** 편2 `:101`은 원본 `03 L27`의 「전체 그림은 [09 린 레퍼런스 아키텍처]」가 T4에서 통째로 생략된 자리다(당시 편5 미발행). 두 편의 `updated`를 갱신했고 `date`는 정렬이 바뀌므로 손대지 않았다.

## 2026-08-17 — `ai-transformation` T4 발행 **2편**(총 121편), 총론↔각론 경계 계약, 검증 94건

> **`aiteam/03` + `04_영역별/{개발,QA검증,배포_운영}`(21,912 B)을 편2로, `04_영역별/{기획_요구사항,디자인,퍼포먼스마케팅,경쟁사분석}`(17,028 B)을 편3으로 발행했다** (`37e413b`). 카테고리 11편 중 **8편**이 나갔다. 편2 `sdlc-human-gates` **45,540 B · 464줄 · H2 12 · H3 21 · 표 18 · 도식 3**(팽창률 **2.078**), 편3 `discovery-to-growth-tooling` **41,624 B · 427줄 · H2 8 · H3 20 · 표 20 · 도식 4**(**2.444**)다. sitemap **181 → 184 URL** — 발행본 2 + **태그 페이지 `ci-cd` 신규 개설 1**이고, 이 태그를 쓰는 발행본은 아직 편2 하나뿐이다. npm test 36/36 · tsc 0 · build 0.
>
> **원본이 스스로 총론↔각론 대응을 적어 두었고, 그 대응이 배정과 어긋나 있었다.** 각 `04` 파일 L4 「목적」 줄에 「`03` 파이프라인 **N단계 심화**」가 박혀 있다 — 기획 1~2 · 디자인 3 · 개발 4 · QA 5 · 배포 6. **그런데 `03` 총론은 편2에 배정됐는데 ①②③단계의 각론은 편3에 있다.** 경쟁사분석만 `03`을 참조조차 하지 않고 상위가 `05`(=편4)다. **이 어긋남은 컨트롤러만 볼 수 있었다** — 조사자는 자기 담당 파일만 열기 때문에, 여덟 파일의 L4를 나란히 놓는 시야가 어느 조사자에게도 없었다. 그래서 두 편에 총론↔각론 경계 계약을 명시했다.
>
> **`03 §3`의 「대표 도구」 열이 각론 도구 표의 부분집합임을 실측했고, 그 실측에 반례가 하나 있었다.** 여섯 단계 전부에서 도구명이 겹친다(요구사항 6/6 · 기획 5/5 · 디자인 4/4 · 개발 5/5 · QA 5/5 · 배포 6/6). 그래서 편2가 그 열을 버렸는데 — **`Atlassian Intelligence`(`03:59`)는 각론 어디에도 없다.** **「여섯 단계 전부에서」라는 전칭이 열 하나를 통째로 버리는 유일한 근거였고, 그 전칭이 1/6에서 거짓이었다.** 실측 여섯 건 중 다섯이 맞으면 근거가 5/6만큼 성립하는 것이 아니라, 전칭을 쓰는 논증에서는 아예 성립하지 않는다.
>
> ⚠️ **「고치면 새 개수가 생긴다」 — T3의 「배정은 맞고 술어가 틀렸다」의 다음 단계다.** 편2 `:450`이 구현 시점에 「**금지**가 명시된 것은 **둘**」이었다. 5축이 지적했고 1차 반영이 술어를 정확하게 고쳤다 — 「**한계**를 못 박은 것은 **셋**」. 그러자 **재검증 두 기가 서로 모른 채 독립적으로 「⑤가 넷째다」**를 보고했다. **술어를 정확하게 만들자 그 술어에 걸리는 항목이 늘어난 것이다.** 부정확한 술어는 자기 범위를 좁게 잡아 개수를 맞춰 준다. **그래서 2차 반영 원칙을 「개수 + 술어로 된 총평은 고치지 말고 삭제한다」로 잡았고 목표 3/3을 달성했다**(편2 −1,408 B · 편3 −797 B · 새 문장 1자리 · 구조 불변).
>
> ⚠️ **`🖊️` grep이 로케일 때문에 무력했다 — 앞 세 배치가 이 명령으로 검사했다.** `grep -cE "🖊️" 디자인.md`는 **0**을 내고 `LC_ALL=C grep -cE "🖊️" 디자인.md`는 **1**을 낸다. 원본 8파일 **전부**에 「한 줄 메시지」 블록이 있는데 검사가 잡은 것은 `TODO` 문자열이 우연히 함께 있던 1건뿐이었다. **앞 세 배치의 발행본에서 실제 잔재는 0건이었으나 — 검사가 그것을 증명한 것이 아니다.** 「스캔 0건은 목표가 아니라 관찰값이다」의 실증이고, 이번에는 관찰값 0이 우연히 참이었을 뿐이다. **검사기 자신을 먼저 검사하는 절차(`--self-test`)가 왜 상설인지가 여기서 다시 드러난다.**
>
> **그리고 「한 줄 메시지」 블록 8건을 전량 삭제하라는 처방은 틀렸다.** 여덟 중 **여섯에는 1인칭도 금칙어도 없었다.** 다만 반대 방향도 함께 반증됐다 — **컨트롤러가 「살릴 기술 명제가 있다」고 지목한 두 건 중 하나도 무너졌다.** `경쟁사분석 L65`의 성분 5개 중 4개가 §5 제목과 `:56`에 이미 있었고, 신규분은 최상급 「가장」 하나뿐이었다. **「전량 삭제」도 「전량 보존」도 아니고, 여덟 블록을 각각 열어 보는 것 말고는 방법이 없었다.**
>
> **컨트롤러 브리프 오류가 8건 드러났고 전부 지시받은 쪽이 반증했다.** ① `classDef`를 기발행본 실재 문법이라 쓰고 지시했으나 **121편 중 0건**이다. ② 앵커 링크를 지시했으나 **발행본 관례에 0건**이다. ③ 「7자리 중 6자리」가 실제로는 **11자리 중 8자리**였다. ④ **§7-4가 모순된 두 규칙**(「각론 전량은 편3」 + 「게이트는 편2」)을 동시에 줘, 편3 구현자를 **「게이트는 없다고 선언하면서 게이트 표를 싣는」** 상태로 몰았다 — 외 4건. **브리프를 쓰는 속도와 확인하는 속도가 다르다는 것이 반복 원인이다**(T3 2건 → T4 8건).
>
> **반영자가 판정표 오류 14건을 되잡았다**(1차 11 · 2차 3). 대표 사례 — 2차 판정표가 「도식 노드 라벨을 `개발자 로컬`/`서버사이드`로 줄여라」고 지시했는데, **그대로 하면 `approval rules`가 용어 정리 표에서 고아가 된다.** 게다가 목표 ③(표 행 삭제 금지)이 그 고아를 지우는 것마저 막는다. **반영자가 `approval rules` 하나만 남기는 절충으로 세 자리(도식 라벨 · 용어표 · 목표 ③)의 정합을 동시에 만족시켰다.** 판정표를 액면대로 실행하면 결함이 늘어나는 구간이 있고, 그것을 볼 수 있는 것은 세 자리를 동시에 여는 반영자뿐이다.
>
> **날짜를 사실대로 적은 것이 구조 문제를 풀었다.** 작업이 자정을 넘겨 실제 발행일이 **2026-08-17**이 됐고, `date`를 반올림하지 않고 그대로 적자 **편2·편3이 카테고리 최상단에서 서로 이웃**이 됐다. [`lib/blog/loader.ts`](lib/blog/loader.ts)의 정렬 비교자를 Node로 이식해 재계산한 결과, T4 이전 **양방향 비대칭 6건**이던 것이 **두 편 사이에서는 대칭**이 됐다(잔존 4건은 series ↔ non-series 간 기존 설계 특성이라 이번 배치의 소관이 아니다). **「이전 글/다음 글」의 어긋남은 T3에서 축 E가 찾은 결함 형태인데, 이번에는 날짜를 속이지 않은 것만으로 해소됐다.**
>
> **편1 2자리를 동반 수정했다.** 편2 발행으로 편1 `:103`의 「넷 가운데 **셋**의 도착지가 이 블로그 안이다」가 거짓이 됐다 — 그 「셋」은 운영 4원칙 ①의 도착지가 비어 있었기 때문에 성립한 수였다. **「네 도착지가 모두」로 정정하고 전수 배정을 붙였으며**, `:98` ① 행 실무 함의 칸에 편2 링크를 달았다. **앞 편의 참값이 뒤 편 발행으로 거짓이 되는 자리는 「몇 개가 어디에 있다」 형태의 문장에 몰린다.**

### 추가

- **`ai-transformation` 편2** [`sdlc-human-gates.md`](content/blog/ai-transformation/sdlc-human-gates.md) — 요구사항→기획→디자인→개발→QA→배포·운영 **여섯 단계 게이트**를 총론과 각론 두 층위로 싣는다. 관통 원칙(AI는 초안·분석, 사람은 승인·정책) · 공통 안전장치 넷 · **로컬로 흡수할 것과 서버사이드에 남길 것의 강제 경계** · 원 자료가 같은 여섯 단계를 두 번 적은 **두 판본의 항목 단위 대조** · 개발·QA검증·배포운영 세 영역 각론(**도구 16종**과 선택 기준 · JetBrains에서 Copilot을 자동완성 너머로 쓰는 7기능 · 매니지드 PR 리뷰가 막힌 **AWS CodeCommit 환경의 세 선택지** · 세 영역 KPI 대조). **45,540 B · 464줄 · 표 18 · 도식 3** (`37e413b`)
- **`ai-transformation` 편3** [`discovery-to-growth-tooling.md`](content/blog/ai-transformation/discovery-to-growth-tooling.md) — 기획·요구사항 · 디자인 · 퍼포먼스 마케팅 · 경쟁사 분석 **네 영역의 도구 지형.** 네 문서가 공통으로 가진 **「사람이 정하는 것 / AI가 처리하는 것」 2열 표 4벌을 각각 그대로** 싣고, 회의 전사에서 PRD·우선순위화까지 9종 · Figma Make·v0·UX Pilot·Google Stitch 4종 · Meta Advantage+와 Google PMax 축의 크리에이티브·A/B·입찰·오디언스·대량생성 · Crayon·Klue·Visualping에서 **Firecrawl+n8n+LLM 자체 구축**까지를 출처 링크와 함께 나열한다. **벤더 자체 수치와 공식 1차 수치를 가르는 인용 단서** · 플랫폼이 부풀린 전환을 **holdout으로 걸러 내라는 반례** · 채용공고를 제품 발표 **6~18개월 전 선행 신호**로 읽는 법 · 사람 쪽에 남은 **열세 항목**. **41,624 B · 427줄 · 표 20 · 도식 4** (`37e413b`)

### 수정

- ✅ **README 수치 — 지시받은 3자리에 1자리가 더 있었다.** 「블로그 발행본 119 → **121편**」 · 「sitemap 181 → **184 URL**」 · 구조 트리 「발행본 119 → **121편**」에 더해, **「블로그 콘텐츠 규약」 표의 카테고리 내역 `ai-transformation` 4 → 8**을 함께 고쳤다. **이 자리는 T3에서 이미 어긋나 있었다** — 총계는 119로 갱신됐는데 내역은 4(=합계 117)로 남아 문서가 자체 모순이었다. 지금은 `ai-agent` 51 · `agentic-coding` 31 · `rag` 25 · `search-engineering` 6 · `ai-transformation` 8 = **121로 총계와 닫힌다**
- **편1** [`builder-to-orchestrator-shift.md`](content/blog/ai-transformation/builder-to-orchestrator-shift.md) **2자리 + `updated`** — `:103`의 「넷 가운데 **셋**」을 **「네 도착지가 모두」**로 정정하고 원칙 ①~④의 전수 배정을 붙였다. `:98` 운영 4원칙 표 ① 행에 **편2 링크 신설**. `updated: "2026-08-17"` 갱신 (`date`는 발행일 `2026-08-16` 유지)
- **설계서 §3 분할표** — 편2(21,912 B → **45,540 B**) · 편3(17,028 B → **41,624 B**) 발행 완료 표기. 팽창률 **2.078 · 2.444** 실측 등재
- **설계서 §7-4** — ⚠️ **모순된 두 규칙(「각론 전량은 편3」 + 「게이트는 편2」)을 해소하고 총론↔각론 대응표를 신설.** 각 `04` 파일 L4의 「`03` N단계 심화」 자기 선언(기획 1~2 · 디자인 3 · 개발 4 · QA 5 · 배포 6)과 **경쟁사분석만 상위가 `05`**임을 명시
- **설계서 §9 배치 계획** — T4(`37e413b`) 완료 표기. 11편 중 **8편 발행**
- **설계서 §10-2 검증 명령** — ⚠️ **이모지 grep에 `LC_ALL=C`를 필수로 못 박았다.** 로케일 기본값에서 `grep -cE "🖊️"`가 **0을 반환**해 앞 세 배치의 「한 줄 메시지」 블록 검사가 무력했다. `--self-test` 없는 검사 명령은 「0건」을 근거로 쓰지 못한다는 단서를 함께 적었다
- **설계서 §12 정정** — 신규 등재: **`03 §3` 대표 도구 부분집합의 반례 `Atlassian Intelligence`** · **「한 줄 메시지」 8블록 중 6블록은 무해**(전량 삭제 처방 반증) · `경쟁사분석 L65`의 신규 성분은 최상급 하나뿐 · **컨트롤러 브리프 오류 8건**(`classDef` 121편 중 0건 · 앵커 링크 관례 0건 · 「7자리 중 6자리」→**11자리 중 8자리**)
- **설계서 §13 미해결** — ⚠️ **「고치면 새 개수가 생긴다」 등재.** 술어를 정확하게 고치면 그 술어에 걸리는 항목이 늘어나므로, **「개수 + 술어 총평」은 수정이 아니라 삭제로 처리**한다는 원칙을 명문화. **판정표 오류 14건을 반영자가 되잡은 사례**(도식 라벨 축약이 용어표 고아를 만드는 3자리 연쇄)와, **정렬 비대칭 잔존 4건**(series ↔ non-series 간 설계 특성)을 함께 등재

---

## 2026-08-16 — `ai-transformation` T3 발행 **2편**(총 119편), 자기 PR 문서 변환의 첫 적용, 설계서 정정 6건

> **`aiteam/01·02·07`(22,244 B)을 편1로, `05·06`(13,517 B)을 편4로 발행했다** (`695cdad`). 카테고리 11편 중 **6편**이 나갔다. 편1 `builder-to-orchestrator-shift` **38,217 B · 321줄**, 편4 `enablement-infra-and-maturity` **36,688 B · 403줄**이고 **도식은 각 4개씩 전부 새로 그렸다.**
>
> **원본이 구직용 자기 PR 문서라 앞의 두 배치와 성격이 달랐다.** T1·T2는 강의 정리라 주체 치환으로 끝났지만, 여기서는 **절의 존재 이유가 「저자가 이걸 할 수 있다」인 자리**가 있어 **「제거하고 남는 것을 세는 작업」이 본체**였다. 가장 위험했던 자리는 `01 §2` 3단 모델 표의 **「나의 현재 위치」 열**이다 — L2 행에 「🎯 여기가 핵심 갭 — 공유만 하고 강제 안 함」이라는 진단이 들어 있어, 열을 지우면 그 진단이 사라진다. **그것을 「대부분의 조직이 멈추는 지점」으로 일반화하면 원문에 없는 전칭이 되므로 금지했고**(금지선 21·16), 5축 검증이 실제로 그 자리를 확인해 **전칭 0건**을 판정했다.
>
> **원본 `aiteam/` 18파일에 mermaid가 0개라 검산 축이 바뀐 첫 배치다.** 「원본 총계와 일치하는가」가 성립하지 않아 **「새 도식의 각 노드·화살표가 본문 어느 줄에 근거하는가」**(금지선 28)로 대체했고, **노드 35·화살표 31 전건에 근거 줄을 대조**했다. 축 B가 **화살표 3개가 본문에 없는 관계**임을 찾았다 — 노드 39개 중 36개는 근거가 확실했다. **개체는 본문에서 오지만 관계는 그리는 사람이 만든다.**
>
> **두 편이 같은 원본군에서 소재를 나눠 가져 경계 계약을 명시했다.** 성숙도 표·90일 로드맵·KPI·실패요인은 편4가, 3단 모델·기업 사례 11개·MIT/McKinsey 수치와 URL은 편1이 싣고 서로 링크한다. **이 계약은 컨트롤러만 만들 수 있었다** — 조사 α는 `01·02·07`만, β는 `05·06`만 봤으므로 **`06 L19`가 편1이 삭제할 열을 가리킨다**는 사실을 어느 쪽도 보고하지 않았다. 계약이 없었다면 편4는 **편1 발행본에 존재하지 않는 판정을 인용한 채** 나갔을 것이다.
>
> **설계서 판정 하나가 반증됐다 — 「`06` 성숙도·로드맵은 자체 정의, 외부 인용 없음」.** `06`만 열면 외부 URL이 0건인 것은 사실이지만, **출처는 `07 §4` L77에 있다**(`Heinz Marketing` · `PUNKU.AI`). 조사 β가 자기 담당 파일만 보고 판정한 결과이고, **금지선 27(「이 글의 정리다」 귀속을 붙여라)의 전제가 무너졌다** — 귀속이 아니라 출처를 붙여야 했고, 그대로 했다면 **원 자료가 밝힌 출처를 저자 추론으로 강등**하는 것이었다. **한 파일만 열고 「외부 인용 0건」을 판정하지 마라.**
>
> **컨트롤러가 검증 없는 전칭을 두 번 썼고 두 번 다 지시받은 쪽이 반증했다.** ① 조사 γ의 「발행본 15편 **전부** 에이전트 소프트웨어 층위」를 브리프에 옮겼는데, **`ai-agent/enterprise-ai-adoption-actions:147·290`이 Orchestrator를 프로덕트 오너에 매핑**해 이미 사람 층위로 발행돼 있었다. 구현자가 지시 문안을 쓰지 않고 두 편을 직접 읽어 각각의 층위를 적었다. ② 판정표에 「둘 다 상대 제목의 **실제 조각**」이라 썼는데 **편1→편4 방향에서 거짓**이었다(「팀 인프라와 성숙도」는 요약 호칭). **금지선 16은 구현자 규칙으로 쓰였지만 브리프·판정표를 쓰는 사람이 가장 자주 어긴다** — 브리프는 빨리 써야 하고 확인은 느리기 때문이다.
>
> **「배정은 맞고 술어가 틀렸다」가 이 배치의 결함 구조다.** 재검증 1기의 관찰이 정확하다 — **「개수 주장 21건은 전부 맞았고, 틀린 것은 언제나 그 옆에 붙은 술어였다.」** 「스물셋」은 맞고 「이 글이 **쓰는**」이 틀렸으며, 「다섯 원칙」은 맞고 「**수치로 받친다**」가 틀렸다. **금지선 17의 근거가 여기서 드러난다: 배정은 검산 가능하고 술어는 검산 가능하지 않다.** 결함이 「표 아래 총평 한 줄」에 5축 8건 · 재검증 7건으로 **두 번 몰렸다.**
>
> **`dup-scan` 단일 파일 모드가 형제 편 검출에 실제로 작동했다** — 편1 `:198`·`:220`과 편4 `:350`·`:365`를 서로 대조군에 넣어 잡았다(머리글 「대조 118편」). **`--category`였다면 `basename` 필터 때문에 원리상 0건**이었다(이월 7번의 우회책 실증). 다만 **「약속한 것과 실제 배치의 어긋남」은 스캔이 원리상 못 잡는다** — 축 E가 「편1이 편4를 「다음 편」이라 4회 부르는데 사이트가 렌더하는 다음 글은 백오피스 편」임을 찾은 것은 [`lib/blog/loader.ts:186`](lib/blog/loader.ts)을 읽고 **정렬 비교자를 재실행**해서였다.
>
> ⚠️ **커밋 사고 하나를 잡았다.** 구현 직후 `git add`로 diff baseline을 잡은 뒤 1·2차 반영을 마치고 `git commit`했더니 **staged 상태(최초 구현본)가 커밋됐다** — 아홉 명이 검증한 것과 다른 파일이 나갈 뻔했다. 잡아낸 것은 **커밋 직후 `git status`가 깨끗하지 않다**는 한 줄이고, 푸시 전이라 `--amend`로 복구했다. **baseline용 `add` 이후에는 커밋 전 재 `add`, 커밋 후 `status` 확인을 절차에 넣어야 한다.**

### 추가

- **`ai-transformation` 편1** [`builder-to-orchestrator-shift.md`](content/blog/ai-transformation/builder-to-orchestrator-shift.md) — AX 조직 정의 · 개인기→팀 시스템→조직 역량 3단 모델 · 빌더→오케스트레이터 직무 재정의 · 하이브리드 조직 구조 · NIST AI RMF 거버넌스 · 변화관리 · **실제 기업 사례 11개**(글로벌 7 + 한국 4, **U턴 반례 2건과 시점 표기 포함**). **38,217 B · 321줄 · 도식 4** (`695cdad`)
- **`ai-transformation` 편4** [`enablement-infra-and-maturity.md`](content/blog/ai-transformation/enablement-infra-and-maturity.md) — n8n 팀 자동화 · 지식관리 RAG 「회사 두뇌」 · MCP 팀 공유 표준 · 공유 프롬프트 라이브러리 · ROI 자동화 5선 · **성숙도 0→1→N(출처 2개 부착)** · 단계별 도입 순서 · KPI 4분류와 중단 기준 · 실패요인 5종. **36,688 B · 403줄 · 도식 4** (`695cdad`)

### 수정

- **설계서 §1 팽창률** — `1.63~2.50` → **`1.63~2.71`.** ⚠️ **규칙성이 드러났다 — 쪼개는 편은 낮고(편1 1.72) 묶는 편은 높다(편4 2.71).** 편4는 13.5 KB 불릿·표 뭉치에 도식과 층위 설명이 붙어 세 배 가까이 늘었다
- **설계서 §2-1** — ⚠️ **「`06` 성숙도·로드맵은 자체 정의, 외부 인용 없음」 반증.** 출처가 `07 §4` L77에 있음을 명시하고 **「귀속이 아니라 출처를 붙인다」**로 처리 규칙을 교체
- **설계서 §3 분할표** — 편1(22,244 B → 38,217 B) · 편4(13,517 B → 36,688 B) 발행 완료 표기
- **설계서 §7-2** — 「첫 90일에 할 일」 서식 **7편 → 8편** (`agentic-coding/reversibility-before-permission:176` 추가). **8편 전부 H2가 「조직에 옮기면 무엇이 되는가」**임과, 금지선 26이 막는 것은 **표 안의 기간 값이 아니라 편 제목·H2**임을 명시
- **설계서 §10-2 검증 명령** — 1인칭 정제식을 `(^|[^주전공과문])제가 `로 갱신. **오탐 트랩 5종 → 7종**(`과제가 `·`문제가 ` 신규). ⚠️ **`재채용`은 Klarna 사례 축자이므로 절대 지우지 마라**를 명시
- **설계서 §12** — 정정 5건 → **11건.** 신규 5건: 성숙도 출처 반증 · 90일 서식 8편 · MCP 「4계층」이 원본에 없음 · 「10인 팀 구성표」가 표가 아니라 불릿이고 두 자리 · **「중단 기준」이 `02 L70`·`06 L49` 두 원본에 축자로 있고 출처는 `02`에만**
- **설계서 §12-2 신설** — ⚠️ **실명·구직 맥락은 grep 패턴이 잡지 못하는 형태로도 존재한다.** 조사 패턴이 `02 L41`의 「**본 협상**/계약 워크스페이스 … 개인계약서 비공개」를 놓쳤다(`본 팀`은 있으나 `본 협상`은 없다). **부분 일치(`본 [가-힣]`)를 함께 돌리고 `> 🖊️ TODO(허우용):` 블록을 별도로 세라**
- **설계서 §13 미해결** — 항목 2(MCP 4계층) 해소 · **신규 5건**: 편1 `:281`의 72% 미회수 · 편1 링크 호칭 3종 · **`01 L24`↔`05 L4` 원본 모순**(발행본을 고쳐서는 해소 불가) · **mermaid 렌더는 CI로 검증 불가**(`DOMPurify.addHook` 부재) · ⚠️ **baseline용 `git add` 후 커밋 사고**

---

## 2026-08-16 — `ai-transformation` T1·T2 발행 **4편**(총 117편), 팽창률 1.20 반증, `dup-scan` 한계 2건 실측

> **`aiagent/06`을 3편(편6·7·8, `9c0b9fd`)으로, `aiagent/08 §4~§8·§10`을 1편(편9, `6a56f4a`)으로 발행했다.** 카테고리 11편 중 4편이 나갔다. 편9는 **42,630 B로 발행본 117편 중 최대**이고, 원본 17,065 B(§4~§8 14,073 + §10 2,992)에서 나왔다. **§9 개발기술은 이미 `dev-org-transfer.md`로 발행됐으므로 제외했다** — 이 원본은 이 프로젝트에서 유일하게 **일부가 이미 축자로 발행된** 파일이라, 「원본에 있으니 싣는다」가 안전하지 않은 첫 배치였다.
>
> **설계서의 팽창률 1.20이 반증됐다.** 실측은 T1 1.63~1.93 · **T2 2.50**이고, `agentic-coding` 승계값 1.20도 그 카테고리에서 이미 1.76이었다. **분할표 §3의 「예상 KB」 열 전체가 과소 추정이다** — 편1~9만 260~400 KB로 재산정된다. 함께 **편9 가용분 「14,000」이 §10(2,992 B)을 빠뜨린 오기**임을 실측으로 확인했다(§1 요약의 16,975가 맞다). **T3~T7은 분할표 값을 믿지 말고 가용 바이트를 재실측해야 한다.**
>
> **「처방도 검증 대상이다」(금지선 20)가 한 배치에서 세 번 값을 했다.** ① 구현자가 브리프의 `강의` 처방을 반증했다 — 지시대로 귀속을 붙였더니 원문의 「~중 하나다」 헤지가 **유일성 주장으로 바뀌었고** 같은 범위의 동종 지목 3곳이 자기 본문을 반증했다. ② 재검증 2기가 1차 판정표의 처방을 반증했다 — 술어를 「언제 넘기는가」에서 「무엇을 언제 넘기는가」로 **「넓혔더니」 반례가 11개에서 25개로 늘었다.** 「무엇을 언제」는 선언이 아니라 **연언**으로 읽혀 조건이 추가된 것이었다. ③ 2차 반영자가 2차 판정표의 삭제 처방을 반증했다 — 지웠다면 그 어구가 담당하던 **`:226`의 산술 「3 + 1 + 2 = 6」에서 「1」이 사라져** 없던 거짓 문장이 생겼을 것이다. **그리고 자기가 만든 처방까지 폐기했다** — 갈래 이름을 고쳤더니 커버리지는 5/7→6/7로 올랐지만 **갈래 밖 오탐이 1→12건**으로 늘어 되돌렸다.
>
> **⚠️ `dup-scan`의 구조적 한계 2건을 실측했다.** ① [`scripts/dup-scan.mjs:207`](scripts/dup-scan.mjs)의 `basename` 필터가 `--category` 실행 시 **형제 편들을 통째로 대조군에서 뺀다** — 같은 카테고리 편들끼리의 중복은 **원리상 검출 불가**이고 단일 파일 모드로 재실행해야 한다. ② **20자 임계값이 한국어 표 셀을 놓친다** — 이번에 발견된 **바이트 완전일치 3셀(정규화 15·17·18자)을 스캔이 전부 놓쳤고** 손 대조로만 나왔다. 한국어 표 셀은 조사가 없어 짧다. `selfTest()`도 같은 상수로 고정돼 있어 **「4/4 PASS」가 임계값의 타당성을 증명하지 않는다.**
>
> **「전수성」과 「배정 정확성」은 다른 검사다.** 프롬프트 기법 39항목의 여섯 갈래 배정이 **다섯 차례 검증에서 「중복 0 · 누락 0」을 통과**했는데, 그것은 빠짐없이 한 번씩 들어갔다는 것만 증명한다. **각 항목이 제자리인지는 아무도 묻지 않았고**, 재검증 2기가 C갈래(「표·사전으로 조회 전환」)에 **표도 사전도 아닌 항목**이 있음을 찾았다.
>
> **1인칭 검사 트랩에 다섯 번째가 추가됐다** — `공제가 `(「매입세액 **공제가** 인정되는」)가 정제식 `(^|[^주전])제가 `를 통과한다. 기존 4종은 `하나의 ` · `주제가 ` · `전제가 ` · `지원자 스크리너`다. **정규식에 배제 문자를 더하는 대신 알려진 오탐 목록에 넣는 쪽을 택했다** — 배제를 늘리면 언젠가 진짜를 놓친다. T1의 최상위 사고(스캔 0건 달성을 이득으로 계산해 오탐 판정을 뒤집고 에이전트를 개명)는 **재발하지 않았고**, 편9는 히트가 2→3으로 **늘어난 상태로 발행**됐다.

### 추가

- **`ai-transformation` 편6·편7·편8** — `aiagent/06`(63,181 B)을 부서별 3편으로 분할. `department-agent-blueprint`(28,286 B) · `department-automation-frontline`(37,679 B) · `department-automation-backoffice`(37,245 B). 원본 도식 11개 전건 승계 + 편8 신규 1개 (`9c0b9fd`)
- **`ai-transformation` 편9** [`agent-definition-by-department.md`](content/blog/ai-transformation/agent-definition-by-department.md) — 여섯 부서 26종 에이전트 정의서. **위임 흐름 도식 6개를 원본과 바이트 동일하게 승계**(`diff` 무차이, 세 검증자 독립 확인)하고 **인계 화살표 라벨 35개를 전건 보존**했다. 소속선 26 + 인계 35 = 61, 부서 밖 8. 프롬프트 설계 기법 39항목을 여섯 갈래로 전수 배정. **442줄 · 42,630 B** (`6a56f4a`)

### 수정

- **설계서 §1 팽창률** — `1.20` → **실측 1.63~2.50으로 반증 표기.** §3 「예상 KB」 열 전체가 과소 추정임을 명시
- **설계서 §3 분할표** — 편9 가용분 `14,000` → **`16,975`**(§10 누락 오기 정정) · T1·T2 발행 완료 표기 · 편1~9 재산정치 260~400 KB 병기
- **설계서 §9 배치 계획** — T1(`9c0b9fd`)·T2(`6a56f4a`) 완료 표기
- **설계서 §13 미해결** — `dup-scan` 한계 2건(항목 7·8) 신규 등재
- ✅ **README 전면 재작성 — 열다섯 번째 이월을 끝냈다.** 2026-08-07(`7f28866`) 이후 39커밋 동안 방치돼 **전제부터 낡아 있었다**: 「단일 페이지 포트폴리오」로 서술하는데 실제 라우트는 **12개**이고 블로그 5개·프로덕트 리드 계열 5개가 통째로 빠져 있었다. 소개·기술 스택·구조 트리를 실측대로 고치고 **「페이지 구성」·「블로그 콘텐츠 규약」 두 절을 신설**했다. **제3자 검증(`verifier`)에 링크 14종·수치 9종·라우트·의존성·서술 주장을 전부 실행시켰고 틀린 주장 1건을 잡아 고쳤다** — 「라우트 14개」는 `find pages -name "*.tsx"`의 파일 수였고 `_app.tsx`·`_document.tsx`는 라우트가 아니다. **README 자신의 상세 표를 세면 12개라 문서가 자체 모순이었고 검증자가 그것을 근거로 삼았다**
- **CHANGELOG** — 직전 세션이 빠뜨린 **T1 발행 기록을 이번 항목에 함께 담았다**

---

## 2026-08-16 — `ai-transformation` 분할 설계 확정(11편), 축자 복제 스캔 상설화, 이월 위험 정정 3건

> **발행 0편.** 다음 카테고리 `ai-transformation`(AI 전환 조직)의 분할 설계를 **11편**(본편 9 · Q&A 1 · 지도편 1, 예상 약 221 KB)으로 확정하고 승인받았다 (`065ad9b`). 원본 **199,128 B**(`aiteam/` 18파일 118,972 + `aiagent/06` 63,181 + `aiagent/08 §4~§8·§10` 16,975)에서 절 단위 제외 31,437 B와 1인칭 열·행 4,176 B를 걷어 **가용분 163,515 B**를 얻었고, **그 값이 편1~9의 합과 정확히 일치**한다 — 어느 항도 잔차로 정의하지 않았다(금지선 10). 조사는 **7축 병렬**로 돌렸고 기계적 실측(절 인벤토리 155행 · 금칙어 grep)은 컨트롤러가 직접 `awk`로 뽑아 브리프에 첨부했다.
>
> **이 카테고리는 앞의 9배치와 세 가지가 근본적으로 다르다.** ① `aiteam/` 18파일에 **mermaid가 0개**라 「원본 도식 총계 일치」 검산이 성립하지 않는다 — 질문이 **「새로 그린 도식의 노드·화살표에 본문 근거 줄이 있는가」**로 바뀐다(금지선 28). ② 원본이 **구직용 자기 PR 문서**라 실명 27자리·1인칭 제거가 문자열 치환으로 끝나지 않는다 — 절의 존재 이유가 「저자가 이걸 할 수 있다」인 경우 3인칭으로 옮기면 남는 것이 없을 수 있다. ③ 개별 파일이 2.7~13.9 KB로 **전부 하한 미달이라 쪼개는 것이 아니라 묶어야 한다.**
>
> **앞 설계서가 이월한 위험 6건을 실측하자 3건이 뒤집혔고 그중 1건은 방향이 정반대였다.** 「`06 §11` L1008이 자기 본문을 3등급이라 부르며 답변은 4등급이라 적는다」가 기록이었는데, 실측하니 **L1008의 「4」가 옳고** 틀린 곳은 `§2-7`의 **H3 제목(L225)과 리드(L227)이며 둘 다 본문**이다. 「4」라 적은 자리가 여섯 곳(mermaid 노드 4 · 표 4행 · §10-2 · §11)이고 「3」은 정확히 두 곳뿐이다. **기록대로 §11을 걷으면 정정 단서만 사라져 상태가 나빠진다.** 위험 1은 방어가 3자리가 아니라 **7자리**이고 **4자리가 본문에 남아** 「출처 없는 성과표」 상태가 아예 일어나지 않으며, 위험 5는 손실 0으로 **해제**됐다. **이월 목록도 검산 대상이어야 한다** — 「나중에 봐야 한다」를 적었을 뿐 한 번도 열어 보지 않은 항목이 두 배치를 건너오며 그대로 복사됐다.
>
> **조사가 컨트롤러 브리프의 오류를 잡았다.** 축 G가 태그를 배정하려고 [`content/blog/tags.ts`](content/blog/tags.ts)를 열었다가 **패싯 C가 7종이 아니라 17종**(L77 주석 `(17)`)임을 발견했다. 브리프에 「7종」이라 적혀 있었고 그대로 갔으면 **11편 전부가 패싯 규칙 위반**이었을 것이다 — 빌드는 이걸 막지 않는다. G는 **패싯 D도 상한 2**라는 것과 자기가 제안한 11조합 중 2건이 D3이라는 것도 함께 보고했다. **브리프를 쓴 사람은 브리프의 오류를 못 잡는다. 실물을 열게 하는 과업이 검증을 부수적으로 만든다.**

### 추가

- **`npm run dup-scan` · `npm run dup-scan:verify`** — [`scripts/dup-scan.mjs`](scripts/dup-scan.mjs). 신규 발행본이 기발행본의 문장·표 셀을 그대로 옮겼는지 **연속 20자 이상 일치로 전건 추출**한다. 마크다운 기호를 정규화해 **표 셀 경계를 넘는 일치**까지 잡고, 마크다운 링크 제목은 **별도 분류**한다. **판정하지 않고 양쪽 문자열과 위치만 보고**한다 (`065ad9b`)
- **`--self-test`가 본 스캔 전에 검사기 작동을 증명한다** — ① 19자 미검출 ② 20자 검출 ③ 기발행본 실재 40자 주입 → 검출 ④ 비실재 문자열 → 미검출. **113편·442,497 윈도우에서 4/4 PASS.** 「발견 0건」이 참인지 못 찾은 것인지 구분하기 위한 절차다 (`065ad9b`)
- **`ai-transformation` 분할 설계서** [`docs/superpowers/plans/2026-08-14-ai-transformation-category-split-design.md`](docs/superpowers/plans/2026-08-14-ai-transformation-category-split-design.md) — 13절 + 부록. 분할표 11편 · 발행 제외 절 단위 확정 · 교차 중복 소재군 10개 · **금지선 28개**(승계 20 + 고유 8) · 배치 계획 T1~T7 (`065ad9b`)

### 수정

- **설계서 §12 — 이월 위험 6건 전건 실측.** 3건 정정 · 1건 해제 · **1건 판정 반전** · 1건 신규(에이전트 ID 8종이 `06`·`08`·발행본 3편에 동시 존재) (`065ad9b`)
- **`dup-scan.mjs`의 링크 제목 미분류 결함** — 자체 검사 4/4는 **처음부터 통과**했으나, B9에서 사람이 0건으로 판정한 `topic-map-reading-paths.md`를 기준선으로 돌리자 **11건**이 나왔고 전부 링크 제목이었다. `linkTitles()`를 넣어 0건을 재현했다. **자체 검사는 「작동한다」만 증명한다 — 「사람 판정을 재현한다」는 별개 명제다** (`065ad9b`)
- **`HANDOFF.md` 전면 갱신** — §3에 이월 위험 실측 6건, §4에 미해결 31~33번 추가, 23번(축자 스캔 미상설화) **해소 표시**, §7에 `dup-scan` 명령과 **1인칭 잔재 검사**(이 카테고리 고유) 추가

## 2026-08-13 — `agentic-coding` B9 발행, **카테고리 완료(31편)**, 설계서 정정 4건

> **발행 1편(112 → 113편), sitemap 173 → 174 URL.** 마지막 배치로 지식 지도편 `topic-map-reading-paths`(**25,311 B**)를 발행해 `agentic-coding`이 **31편**이 됐다 — 설계서 §10 검산값 「31 = 본문 26 + Q&A 4 + 지도 1」과 **정확히 일치**한다 (`89beda4`). 5축 독립 검증이 **32건**, 재검증 2기가 **신규 9건**, 최종 통독이 **4건**을 냈고 **45건을 전건 판정**했다. 1차 반영 **+1,275 B(새 문장 17자리)** → 2차 반영 **−144 B(새 문장 0자리 · 구조 완전 불변)** → 컨트롤러 2건(**+18 B**).
>
> **이 배치는 번역이 아니라 재작성이었다.** 원본 `00-개요-지식맵`은 129줄·6.7KB인데 §5·§6이 발행 제외라 **가용분이 약 5KB**이고, 남은 §0~§4에서도 그대로 옮길 수 있는 셀이 사실상 없었다 — 「`Part N`」·「570줄」(원본 파일 줄 수)·「면접 우선순위」 열이 전부 제거 대상이고, **`06` 참조가 5자리 · 금칙어가 4자리**였다. 그래서 원본에서는 **골격만 빌리고 내용은 발행본 30편에서 다시 만들었다.** 시리즈 축(8시리즈 28편 + 독립 2)과 주제 축(15주제 × 깊이 3단, 자리 54 / distinct 30), 목적별 경로 8갈래(자리 34 / distinct 30)를 세웠다.
>
> **주제 10(사람 개입과 승인 게이트)은 1차를 비웠다.** 그 주제를 글의 주어로 삼는 편이 이 카테고리에 없기 때문이다. 2차를 1차로 올려 적으면 **없는 깊이를 있다고 말하는 것**이 되고, 그 편을 찾아간 독자가 기대한 것을 찾지 못한다. 빈칸을 그대로 두고 그 사실을 본문에 적었다.
>
> **세 축(A·B·E)이 서로 모른 채 주제 10에 독립적으로 도달했다** — 축 B는 「실사가 두 편을 빠뜨렸다」, 축 A는 「3차 배정이 미배정 편들보다 얕다」, 축 E는 「대상 편의 비중을 축소 서술했다」였다. 셋 다 브리프를 받지 않았고 서로의 산출물을 읽지 않았다. **축을 나눈 설계가 작동한 가장 강한 신호다.**
>
> **최상위 결함은 귀속이 반대로 뒤집힌 자리였다.** 원본 `00 §4`가 이미 *"깊이 순으로 정리한다"* + `1차/2차/3차` 3열 표를 갖고 있는데, 이 글이 그것을 **자기 창작이라 선언**했다. 기발행 `agentic-coding-qna-setup:39`는 같은 축에 대해 *"원 자료가 같은 형태의 안내표를 갖고 있어"* 라고 밝혀 두었으므로 **정반대 귀속**이었다. **1차 반영이 이 자리를 두 번 썼다** — 첫 문안이 「깊이 순서는 이 글의 판정이다」였는데 원본을 직접 열어 보고 같은 종류의 오귀속을 반복할 뻔한 것을 발견해 재작성했다.

### 추가

- `topic-map-reading-paths` — 30편 지도편. 시리즈 축 · 주제 축 15주제 교차 참조 · 목적별 경로 8갈래 · Q&A 4편 안내 (`89beda4`)

### 수정

- 설계서 §3 — 「본문 참조 52곳」 정정. 절별 실측은 **최소 69곳**이며 52는 **§1 도식과 §2 표를 세지 않은 값**으로 보인다 (`89beda4` 후속)
- 설계서 §9 — B9 완료 블록. 정정 4건(참조 개수 · 재작성 성격 · `00 §4`의 깊이 3단 · frontmatter 규격)
- **`HANDOFF.md` §2 「frontmatter 필드 11개. 없으면 빌드 실패」 정정** — `lib/blog/frontmatter.ts:77` 실측 결과 `series`·`seriesOrder`·`updated`·`source`는 **선택 필드**이고, 값이 있으면 `str()`이 **빈 문자열을 거부**해 **`series: ""`는 반드시 빌드를 죽인다.** 독립편은 필드 자체를 생략해야 한다. B1~B8은 전부 `series`가 있는 편이라 부딪히지 않았고 **B9가 처음 걸렸다**

## 2026-08-13 — `agentic-coding` B8 발행, 검증 축 E 신설, 설계서 정정 29건

> **발행 4편(108 → 112편), sitemap 169 → 173 URL.** 원본 9편의 「면접 활용 포인트」 절에서 **128문항**을 모아 중복을 수렴하고 **56문항**을 `agentic-coding-qna` 시리즈 Q1~Q4로 재구성했다. `128 = 대표 56 + 흡수 63 + 폐기 9`가 **전체·편별·클러스터·출처별 네 축에서 닫혔고 미배정 0건**이다. 검증 **5축이 71건**을 지적해 오탐 5건을 제외한 **67건**을 1차 반영했고(**−5,793 B**, 새 문장 37자리), 재검증 2기가 **미해소 1 · 신규 26건**(1기 8 + 2기 18)을 보고했으며, 2차 반영이 **−1,863 B · 새 문장 0자리 · 구조 완전 불변**으로 처리했다 (`76a50bf`). 편별 최종은 Q1 23,709 · Q2 14,365 · Q3 16,307 · Q4 22,010 B다.
>
> **이번 배치의 발견은 검증 축 E(기발행본 중복)를 신설하자 40건이 나왔다는 것이다.** B7이 *"이 설계가 만든 편들끼리의 중복은 검사하는 축 자체가 없었다"*를 경고했고, B8은 앞 7개 배치 내용을 Q&A로 다시 다루므로 위험이 최대였다. **중복-완전 23 · 중복-부분 17**이고 나머지 4축을 합친 것보다 많다. **축을 신설하지 않았으면 40건이 통째로 발행됐다.** 반복 실패 형태는 셋이다 — **표 전치·병합 복제 7**(행과 열을 바꾸면 겉보기 표가 달라 자체 정리처럼 보이지만 셀이 같다) · **열화 복제 4**(기발행본이 표 아래 달아 둔 **오독 방지 단서를 떼고** 값만 옮겨 **원본이 막아 둔 오독을 다시 연다**) · **링크하고도 복제**.
>
> **그리고 최상위 결함은 검증자의 처방 문안이 만들었다.** 축 E가 *"이렇게 고쳐라"*고 준 문안이 **기발행 `scaling-routing-collapse:137`의 연속 30자 축자**였고 원본 `07`에 grep **0건**이다. 전제절이 떨어져 **같은 파일 용어표와 정면 모순**까지 됐다. **판정자·검증자·반영자 셋이 같은 맹점을 공유했다** — 서브에이전트를 나누는 목적이 맹점을 깨는 것인데 **처방 단계에서는 축이 나뉘지 않는다.**

### 추가

- **`agentic-coding` Q&A 4편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `agentic-coding-qna-setup`(23.7KB·17문항) · `-extensions`(14.4KB·12) · `-agent-design`(16.3KB·10) · `-governance`(22.0KB·17). `agentic-coding-qna` 시리즈 신설 (`76a50bf`)
- **검증 축 E 신설 — 기발행 26편과의 중복 전담** — 기존 4축(A 틀린 것 / B 원문에 없는 것 / C 없는 것 / D 실행)에 다섯째를 세웠다. **지적 40건으로 나머지 4축 합계(30건)를 넘었다.** 축 E는 양방향 함정에 모두 대응한 기록을 남겼다 — 「개수가 같다」로 오판할 뻔한 2건은 셀을 열어 **정상-별개**로, 「이름이 다르다」로 놓칠 뻔한 4건은 셀을 열어 **중복**으로 판정했다 (`76a50bf`)
- **축자 복제 자동 스캔을 최종 검증에 도입했다** — n-gram 해싱으로 신규 4편과 기발행 26편을 대조해 **연속 20자 이상 일치를 전건 추출**했다(코퍼스 660KB). 원시 **10,498건** 중 10,458건이 상호 링크 제목이고 **실질 30건**(최장 47자)이다. **사람이 읽어 잡는 축자는 「낯익은 문장」뿐이다** — 표준 정의처럼 보이는 **42자 축자**가 두 번의 독립 재검증과 한 번의 반영을 통과했다 (`76a50bf`)
- **전수 배정표를 별도 에이전트가 만들었다** — 128문항을 `대표`/`흡수`/`폐기`/`재구성`으로 전건 배정하고 **네 축에서 검산**했다. 설계서 클러스터 12개를 실측 검증한 결과 **정확히 맞은 것은 C2·C11·C12 셋뿐**이었고 **12개를 신규 발견**했다 (`76a50bf`)
- **`10 §3` 질문 라우팅 표를 Q1 도입부로 재구성했다(사용자 승인)** — 설계서 §7-4가 다루지 않은 자산이다. 10행 중 **`06`을 가리키는 1행만 빼고 9행**이 발행본 링크로 닫힌다. 원본 절 번호는 노출하지 않고 slug 링크로만 실었다 (`76a50bf`)
- **`ai-agent` 태그가 8편이 됐다** — Q3 `agentic-coding-qna-agent-design`에 부착했다. 「주제가 에이전트 자체인 편에만 단다」는 규칙에 부합하며 **7편은 상한이 아니라 그 규칙을 적용한 결과**였다 (`76a50bf`)

### 수정

- **설계서 §7-1 클러스터 12개는 본문 질문 축으로만 만들어졌다** — **지뢰 축·역질문 축·답변 카드 축·같은 파일 안 본문끼리의 교차**를 세지 않았다. 실측 클러스터는 **24개**(설계서 12 + 신규 12)이고 최종 문항은 **약 80 → 56**이다. 그리고 **`03-10`이 C6와 C10 양쪽에 이중 계상**돼 있어 설계서의 「63출현」은 실제로 **62 distinct**다 (`76a50bf`)
- **설계서 §7-1 — `10`은 `06`도 흡수하고 있다** — `10` L5의 *「정리본 `01`~`09` 9종의 취합」*은 **원본의 자기 서술이며 검증되지 않았다.** `10 §2-5` 답변 카드가 `06 §11-1`의 **축자 복제**이고 `10 §4-3` 자동화 등급표는 `06` L244~246이 원 출처다. `06`은 `ai-transformation` 이관 확정·미발행이라 **금지선 8이 뚫릴 뻔했다** (`76a50bf`)
- **설계서 §5-4에 원본 내부 모순 1건 추가 — 근거 열이 파일 밖을 가리킨다** — `05 §10-1` L1004가 문항의 근거로 「자동화 등급 구분」을 대는데 **`05` 본문에 없다.** `05`의 「등급」 4곳(L151·L244·L823·L868)은 **전부 모델 등급**이다. **B4~B6은 개수 불일치, B7은 표 안 두 행의 서술 충돌, B8은 「근거 열이 파일 밖을 가리킴」** — 합계 검산도 행간 대조도 이 형태를 못 잡는다 (`76a50bf`)
- **설계서 §7-4 — `10`의 §1·§3·§4·§8·§9가 미언급이었다** — **§1 실적 경계표에 민감정보 3행**(개발실 규모·`TVING`·`BTV`)이 실재하고, **§4 대비표 4종 중 3종이 기발행본에 전용 편으로 이미 있다**(4-1 `extension-trigger-axis` · 4-2 `collaboration-patterns-isolation` · 4-4 `rules-hooks-skills-layers`). 4-3만 `06` 근거다 (`76a50bf`)
- **설계서 §7-4 — 역질문 4행을 별도 문항으로 실으면 4건이 통째 중복이다** — 전부 기존 문항과 같은 답으로 닫힌다(C9·C4·C18·C5-a). 그리고 **1행의 원 출처가 `08 §13-2` L857**이라 설계서가 지정한 Q4가 아니라 **Q3 소관**이다 (`76a50bf`)
- **설계서 §7-3 「상한 25KB」는 금지선 13과 충돌한다** — *"상한은 합격선이 아니다"*와 정면으로 부딪힌다. B3에서 상한을 맞추려다 용어표 행을 삭제한 사고가 바로 이 충돌에서 났다. **B8은 상한을 주지 않고 목표 18KB만 줬고 13.3KB 미만은 나오지 않았다**(최소 14,365 B) (`76a50bf`)
- **설계서 §12-3 해소 — `01 §11` 10문항 중 근거 생존 3 · 부분 2 · 소멸 5** — `01`은 §6-1·§9·§10만 살려 `reversibility-before-permission` 1편으로 축약됐다. **다만 소멸 5건 전부 다른 발행본에서 근거가 확보돼 문항 손실은 0**이다 — 링크를 `claude-code-autonomy-tiers`·`mcp-selection-practice` 등으로 걸었다 (`76a50bf`)
- **검증자의 처방 문안이 최상위 결함을 만들었다 — 처방도 검증 대상이다** — 중복을 찾으려 기발행본을 정독한 검증자가 **「이 자리를 어떻게 채울까」를 생각하는 순간 다시 기발행을 참조한다.** 처방 채택 전 **3검사**를 상설화했다 — ①기발행 전편 grep ②원본 근거 확인 ③전제절·단서 소실 여부. **2차 반영이 이 검사로 검증자 처방 9건 중 8건을 버렸다** (`76a50bf`)
- **「용어 정리」·「핵심 판정 기준 정리」 표가 두 차례 사각지대였다** — 1차 반영이 본문 표 16개를 지우면서 **이 표들을 한 번도 열지 않았다**(삭제·치환 60자리 중 조치 0건). 축자 복제가 거기서 살아남았고 **두 재검증이 독립적으로 같은 곳에 도달**했다. 2차 반영이 ≥20자 축자를 **5건 더** 찾았고 그중 **42자**짜리가 있었다. 가장 무거운 것은 `governance:233`으로, 용어표의 권한 등급 화살표가 **같은 편 `:56`이 복원한 관찰과 정면 모순**이었다 (`76a50bf`)
- **지시어는 지울 때도 끼워 넣을 때도 끊긴다** — 표·수치를 지우면 그것을 가리키는 「마지막 칸이」·「세 행의 구분은」·「앞 문항의 "7일간"」이 끊긴다(**6자리**). **반대로 문장을 삽입하면 뒤 문장의 지시어가 새 문장을 물어 버린다**(1자리 — 삽입이 「원칙」의 선행사를 빼앗아 링크가 거짓이 됐다). **`grep`으로 안 잡힌다** — 지운 문자열이 아니라 **그것을 가리키는 지시어**이기 때문이고 삭제 diff에도 보이지 않는다 (`76a50bf`)
- **「지우는 것이 기본 처방이다」를 1차 브리프에 주자 방향이 뒤집혔다** — B7 1차가 **+7,943 B**(신규 결함 13건)였던 것이 B8 1차는 **−5,793 B**다. **유일한 차이는 그 처방을 1차에 줬느냐다** — B7은 2차에만 줬다. 2차 반영은 **두 배치 연속으로 「구조 불변 + 새 문장 0자리」**를 달성했다(H2 65 · 문항 56 · 표행 166 · 펜스 0이 1차와 전부 동일) (`76a50bf`)
- **재검증 신규 결함이 26건으로 6배치 연속 증가했다**(B3 5 · B4 7 · B5 7 · B6 8 · B7 13 · **B8 26**) — 2기의 방법이 유효하다. 반영자가 기록한 **손댄 60자리에서 전칭·배타 표현 47개를 전수 추출해 17개가 결함(36%)**이었다 (`76a50bf`)
- **설계 밀도가 낮은 배치는 진단 오류가 폭증한다** — 설계서 진단 오류 **29건**은 이전 최다(B5 7건)의 4배다. **설계서가 나빠진 것이 아니라 그 자리에 설계가 없었다** — B1~B7은 §4-1(편별 분할표)을 받았고 **B8은 §7(문항 수급 표 하나)만** 받았다. 클러스터 구성 14건 · 편 배정 5건이 그 대가다. **B9도 같은 조건이다** (`76a50bf`)
- **오탐 판정을 하며 연 자리에서 진짜 결함이 나왔다 — 2배치 연속** — 축 C의 자동화 4등급 지적을 `06` 근거로 오탐 판정하면서 그 자리(`agent-design:64~74`)를 다시 열었더니 **답은 `08 §13`의 2축으로 완전**했으나, **같은 편 L84 표의 「~4배」에 출처 표기가 없는 것**이 드러났다. 다섯 줄 아래 L89는 출처를 명기해 **같은 편 안에서 수치 처리가 갈렸다** (`76a50bf`)
- **서브에이전트가 보고 없이 유휴가 됐다 — 3배치 연속, 원인이 매번 다르다** — B6는 `Write` 도구 비활성화, B7은 보고를 쓰는 응답이 API 오류로 끊김, **B8은 파일은 썼으나 최종 보고 없이 유휴이고 재요청도 무응답**이었다. **컨트롤러가 파일을 직접 확인하지 않았다면 세 번 다 「완료」로 처리됐다.** 유휴 알림만 오면 **재요청 전에 산출물의 크기와 mtime을 먼저 보라** (`76a50bf`)

## 2026-08-12 — `agentic-coding` B7 발행, 설계서 정정 8건

> **발행 4편(104 → 108편), sitemap 165 → 169 URL.** 원본 `09`(72,865 B)를 `rules-hooks-skills` 시리즈 편22~25로 변환하고 시리즈를 신설했다. 검증 4축이 **50건**을 지적해 중복 제거 후 **30건**(오탐 3건)으로 판정했고, 1차 반영이 29건을 처리하며(**반박 1 · 부분반박 1 · 판단변경 1**) **+7,943 B**를 썼다. 재검증 2기가 **미해소 0 · 과잉수정 0 · 신규 결함 13건**(B3 5 · B4 7 · B5 7 · B6 8 · **B7 13** — 5배치 연속이고 최다)을 보고했고, 2차 반영이 13건 전량을 **순수 삭제 9자리 · 새 주장 0건 · −1,235 B**로 해소했다. 배율 **1.78** (`909c476`). 편별 최종은 편22 32,667 · 편23 21,017 · 편24 23,168 · **편25 39,676 B로 발행본 최대를 경신**했다(직전 최대는 편18 36,238).
>
> **이번 배치의 발견은 분할 설계가 스스로 만드는 중복이 한 번도 검사된 적이 없었다는 것이다.** 설계서 §5-1의 「중복-완전 0건」은 **기존 발행본 82편(`ai-agent`·`rag` 등) ↔ 원본** 대조 결과이고, **B1~B6가 새로 만든 22편은 그 대조 대상이 아니었다.** B7에서 `09 §3`의 **37%(≈3,850 B)**가 편7·편8에 이미 발행돼 있음이 드러났다 — 편7 `dot-claude-directory`의 「패턴 1~4」가 `09 §3-2` 프롬프트 4요소와 **이름까지 같고 3,254 B로 더 상세**하다.

### 추가

- **`agentic-coding` 4편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `rules-hooks-skills-layers`(32.7KB) · `industry-claude-md-templates`(21.0KB) · `empty-folder-to-deploy`(23.2KB) · `deploy-checklist-debugging`(39.7KB). 원본 표 **62개·데이터행 380**과 도식 8개가 대상이었고, 축 A가 표 데이터행 **404행 전수 대조**에서 원본과 **전부 일치**함을 확인했다(셀 이스케이프 `\|`까지) (`909c476`)
- **`rules-hooks-skills` 시리즈 신설** — 편22~25. 편22가 `09 §0` 용어표 **31행 전량**을 **순서까지 축자로** 싣고(diff 확인), 편23~25는 **개념 기준**으로 축약한다 (`909c476`)
- **편23을 설계 원안에서 재조준했다(사용자 승인)** — `prompt-four-elements`(§3 전량) → **`industry-claude-md-templates`(§8 전량 + §3 고유분)**. 4요소·슬래시 커맨드·@파일 참조·파이프는 편7로, 70% 룰은 편8로 **링크 처리**했다. `09 §8`(업종별 CLAUDE.md 3종 10축 비교)은 편7의 「업종별」(5업종 NEVER 1행씩)과 **업종 목록조차 겹치지 않아** 고유하다 (`909c476`)
- **편25 → 편22 절간 링크** — `09` L753(*"§2의 Rules·Hooks 계층이 존재하는 이유"*)이 설계서 §9-1 표에 없던 실측이다. **B6의 `08` L682에 이어 2배치 연속**이다. `09`가 **다른 원본 파일**을 가리키는 참조는 넓은 정규식으로도 **0건** (`909c476`)
- **편22가 훅 차단 채널을 3자 대조로 열어 두었다** — 판정하지 않고 세 서술을 병기했다: 기발행 편4(**표준 에러**) · 편11(**stdout JSON `reason`**) · `09 §2-4`. **편11 L283의 「두 자료」는 수정하지 않았다**(사용자 결정) (`909c476`)
- **`09 §6-1` 예시값 고지를 표 캡션으로 승격했다** — 설계서 §6-5 10번이 B7 과제로 지정한 항목이다. 8셀 표(2~3시간↔15~30분 · 40%↔95% · 5만~15만↔8천~2만)가 실측처럼 읽히지 않게 한다 (`909c476`)
- **mermaid 8블록을 `mermaid-cli`로 실제 렌더링해 검증했다** — 앞 배치는 `awk` 문법 검사만 했다. `npx @mermaid-js/mermaid-cli` v11.16.0으로 SVG 8개를 생성했고(13~27KB) **종료코드 전부 0**이다 (`909c476`)

### 수정

- **설계서 §5-1 「중복-완전 0건」의 모집단에 이 설계가 만든 22편이 없다** — 대조는 **기존 82편 ↔ 원본**으로만 돌았다. B1~B6가 새로 만든 편들은 원본에서 나왔으므로 **원본끼리 겹치면 발행본끼리도 겹치는데**, 그 축은 검사된 적이 없다. `09 §3`의 37%가 편7·편8에 이미 있었다. **뒤 배치일수록 이 위험이 커진다** (`909c476`)
- **설계서 §4-1 편23 재조준 — 다만 나머지 진단은 정확했다** — 편22 19,067(설계 19,118) · 편24 12,907(12,900) · 편25 22,983(22,988)로 **오차 0.3% 이내**다. **B6에 이어 2배치 연속 진단 오류 0건**이며, 재조준의 원인은 바이트 진단이 아니라 위 §5-1 모집단 한계였다 (`909c476`)
- **설계서 §4-3 도식 총계 85 → 84** — `09 §3`의 유일한 도식이 「패턴 5의 컨텍스트 관리 흐름」인데 **패턴 5(70% 룰)를 편8로 링크 처리하면서 도식도 함께 드롭**됐다. 절을 링크로 넘기면 그 절에 딸린 자산도 같이 넘어간다 (`909c476`)
- **설계서 §5-4에 `09` 내부 모순 1건 추가 — 같은 표 안에서 두 행이 부딪힌다** — §2-4 훅 응답 규약 표의 L188(`exit 2` → **stderr가 모델에 전달**) ↔ L190(**stderr → 모델에 주입되지 않음**). 조건부로 읽으면 양립하나 **표가 그 조건을 명시하지 않는다.** 개수 모순이 아니라 **서술 충돌**이라 B6가 세운 합계 검산으로는 잡히지 않는다 (`909c476`)
- **설계서 §5-4 1번의 `09` 200줄은 2곳이 아니라 3곳** — L340·**L962**·L967이고 **셋 다 편23**이다. L962는 **mermaid 도식 노드 안**이라 산문만 훑는 grep에서 빠지기 쉽다 (`909c476`)
- **설계서 §6-5 10번 「원문이 세 번 반복」이 부정확하다** — L1023은 고지문이 아니라 §10 「금지」 열 셀이다. 고지문은 L652·L1008의 **2회**다. **그러나 위험은 실재했고 성격이 달랐다** — L652는 금칙어 `강의`를 포함해 **처리 중 변형될 위험**이었고, 실제로 인용부호 안에서 치환돼 **검증 3축이 전부 지적**했다 (`909c476`)
- **설계서 §9-1에 `09` 절간 참조 추가** — 편25 → 편22(위 「추가」 항목). 설계서의 전방 참조 표는 **원본 파일 사이의 참조만** 담고 있어 **한 원본 안의 절간 참조**를 놓친다 (`909c476`)
- **설계서 §10 배율 1.78** — 7배치 실측이 1.76 · 1.42 · 1.31 · 1.77 · 1.97 · 1.77 · **1.78**이다. **`09` 원본 실측은 설계서 §10-1 값과 5개 항목 전부 일치**했다(72,865 B · H2 11 · 도식 8 · 표 62 · 가짜 H2 0) (`909c476`)
- **「없다」가 세 번 틀렸다 — 부정 명제는 검색어 하나에 걸려 있다** — ① 조사 에이전트가 `RLS`·`Prompt Injection`을 「기발행본에 없음」이라 보고했으나 **두 발행본에 완전히 같은 문장으로 용어 정의가 실재**한다 ② 컨트롤러 브리프가 「XML 템플릿은 편7에 없다」고 적었으나 **`dot-claude-directory.md:356~373`에 실재**한다 ③ 검증 축 C가 `가장 자주 빠지`로 검색해 편7의 *"가장 **많이** 빠지는 요소"*를 놓쳤다. **표현이 한 글자만 달라도 부정 명제는 거짓이 된다** (`909c476`)
- **이름이 다르다고 내용이 다른 것도 아니다 — B6 교훈의 역방향** — 컨트롤러 브리프가 「`09`의 「작성 공식」 열은 편7에 없다」고 적었으나 **편7 L299의 「구조」 행이 `동사 + 무엇을 + 구체적 수식어`로 축자 동일**하다. **열 이름만 보고 셀을 열지 않았다.** B6의 「개수 일치는 동일성의 증거가 못 된다」와 **같은 실패의 양면**이다 — 양방향 모두 셀을 열어야 한다 (`909c476`)
- **컨트롤러 실측 도구에 결함이 있었다 — 인용블록 안의 표를 못 셌다** — 컨트롤러 `awk`가 `^\|`만 봐서 **`> |---|---|` 형태를 놓쳤다.** 편22·편25에 각 1개씩 있어 표 수가 15·28로 과소 계수됐고 **변환자가 16·29로 반증**했다. 원본 `09`에는 인용블록 표가 0건이라 원본 실측은 영향받지 않았다. **B6에서 컨트롤러가 펜스를 추적하지 않아 오계수한 것과 같은 유형**이다 (`909c476`)
- **재검증이 새 결함 13건을 찾았다 — 5배치 연속이고 최다다**(B3 5 · B4 7 · B5 7 · B6 8 · **B7 13**) — 높음 4건이 **전부 「고치는 행위 자체」가 만든 결함**이다: ① 정정 두 개가 서로를 반증(형제 글 대비) ② **모집단을 「세 편」→「마지막 편」으로 바꾸면서 「하나」를 승계**(금지선 37이 판정표에 명시돼 있었는데도 재발) ③ 셈법을 자기 표 전체에 적용하지 않음 ④ 한 문장 안에서 「여섯 중 하나만」과 「나머지 다섯은 세지 않는다」가 충돌 (`909c476`)
- **1차는 쓰고 2차는 지웠다 — 처방으로 사전 지시했을 때 작동했다** — 1차 반영이 **+7,943 B를 쓰고 13건의 결함을 낳았고**, 2차 반영이 **−1,235 B를 지우며 13건을 전부 해소하고 새 결함을 0건** 만들었다. 2차의 조치는 **순수 삭제 9자리 · 좁히기 치환 6자리 · 새 주장을 담은 새 문장 0건**이며, H2·표·도식·펜스 수치가 1차와 **전부 동일**하다 — 구조는 손대지 않고 산문만 줄였다 (`909c476`)
- **개수를 세지 못하면 개수를 쓰지 않는 것이 옳은 처방이다** — 「되돌아오는 자리가 **하나** 있다」를 「넷」으로 고치는 대신 **개수를 뺐다.** 「되돌아오는 자리」의 조작적 정의가 글에 없어 판정자마다 갈리기 때문이다. 형제 글 대비가 자기모순이 된 자리도 값을 고치는 대신 **대비 문장 2개를 통째로 삭제**했다 — **두 글이 같으면 대비할 것이 없다** (`909c476`)
- **서브에이전트 산출물 유실이 2배치 연속 발생했다** — 재검증 1기가 판정을 끝내고 **보고서를 쓰는 응답에서 API 오류로 끊겼다.** 헤더 880 B만 남고 판정 본문이 통째로 유실됐다. 브리프에 「중간에도 append 하라」를 적었으나 **에이전트가 컨텍스트에 쌓아 두고 마지막에 한 번에 쓰려다** 끊겼다. 재요청 시 **「다시 검증하지 말고 이미 내린 판정을 옮겨 적어라」 + 「나눠서 여러 번 append 하라」 + 「기억나지 않으면 미확인이라고 적어라」**를 지시해 30,482 B를 회수했다(해소 28 / 부분해소 1 / 미해소 0 / 미확인 0) (`909c476`)

## 2026-08-11 — `agentic-coding` B6 발행, 설계서 정정 9건

> **발행 3편(101 → 104편), sitemap 162 → 165 URL.** 원본 `08`(68,003 B) 중 개발축 8개 절(44,203 B)을 `agent-definition-catalog` 시리즈 편19~21로 변환하고 시리즈를 신설했다. 나머지 §4~§8·§10(부서 1~5·7)은 설계서 §2-1에 따라 `ai-transformation`으로 이관 대기한다. 검증 4축이 **28건**을 지적해 24건 반영·3건 반박·1건 보류했고, 반영 정확성 재검증이 **신규 8건**을 더 찾아 전량 처리했다. 배율 **1.77** (`06166e9`).
>
> **이번 배치의 발견은 오류가 설계서에서 브리프로 옮겨갔다는 것이다.** 편19~21의 원본 바이트·도식 수가 **5배치 만에 처음으로 전부 일치**했고(설계서 진단 오류 0건), 대신 결함이 **컨트롤러 브리프 2건**과 **조사 에이전트 5건 이상**에서 났다. 설계서는 배치가 진행될수록 자기 오류를 소진하지만 **브리프와 조사는 매 배치 새로 만들어지므로 오류가 줄지 않는다.**

### 추가

- **`agentic-coding` 3편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `thirty-agent-catalog`(28.5KB) · `definition-writing-quality`(25.8KB) · `dev-org-transfer`(24.0KB). 원본 표 **21개·데이터행 193**을 전량 이관했고 mermaid **노드 65개가 byte-identical**이다. 편20에 신규 도식 1개를 더해 도식 총 5개(설계서 §4-3 기준값과 일치) (`06166e9`)
- **`agent-definition-catalog` 시리즈 신설** — 편19~21. 편19가 `§0` 용어표 **37행 전량**을 싣고 편20·21이 **개념 기준**으로 축약한다(편20 7행 + `few-shot` 신규 1 · 편21 10행 + `STAR`·`few-shot` 2). 축약본 행이 편19와 문자 단위로 동일함을 재검증이 확인했다 (`06166e9`)
- **`08 §3-4` tools 열 중복 제거 — 이 카테고리에서 유일하게 승인된 제거** — 요약표 첫 행이 *「**아래 표에서 "기본"으로 표기된 전부**」*라고 상세표를 자기 참조하므로 상세표의 `기본` 25행은 순수 중복이다. 다만 **열만 지우면 요약표 문장이 없는 열을 가리키게 되어**, 셀 값을 「아래 두 행에 이름이 없는 나머지 25종 전부」로 함께 갱신했다 (`06166e9`)
- **편21 → 편20 절간 링크** — `08` L682의 *「§3의 스키마를 사내 표준으로 삼는 것」*이 설계서 §9-1 전방 참조 표에 없던 실측이다. `08`이 **다른 원본 파일**을 가리키는 참조는 넓은 정규식으로도 0건이다 (`06166e9`)
- **편19에 `07 §7-1`(편18)과의 관계 판단 기록** — 두 표는 개수가 같으나(7부서·5+5+4×5=30) **같은 조직도가 아니다.** 결정적 반증을 `08` 자신이 제공한다 — §11-6이 *「**30종 중 테스트를 만드는 에이전트가 없다**」*고 적는데 편18의 개발 부서에는 「테스트」가 있다 (`06166e9`)

### 수정

- **설계서 §8-2 `ai-agent` 태그 한도 7편이 소진됐다** — 배정은 편12·13·14 + 편19·20·21 + **Q3**였는데 **B5에서 편16(`agent-jd-triggers`)이 배정 외로 부착**했다. 편19~21에 3편을 달아 **7/7 도달·여유 0**이며 **Q3 몫이 없다.** `tags.ts` 어휘 검증은 「존재하는가」만 보고 빌드는 「몇 편이 쓰는가」를 세지 않아 **자동 검사가 잡지 못한다** (`06166e9`)
- **설계서 §8-3 「목록을 브리프에 그대로 첨부한다」가 지켜지지 않았다** — B6 브리프가 목록 없이 「0편 태그 30종도 쓰지 마라」라고만 적어 변환자가 편21에 **`ci-cd`**(목록 9번째)를 달았다. `tags.ts`에 **있는** 태그라 빌드가 막지 않았고, **실행 검증이 sitemap 3편에 +4 증가를 보고**해 `/blog/tags/ci-cd/` 페이지 생성으로 추적됐다. **「목록을 첨부하라」는 「목록을 요약하지 마라」는 뜻이다** (`06166e9`)
- **설계서 §10 팽창률 재재정정 — B5의 「행 수 가설」도 반증됐다** — 행이 가장 조밀한 편20(147 B/행)이 아니라 **가장 성긴 편21(277 B/행)이 배율 1위**(2.06)다. **표는 팽창하지 않고 산문이 팽창한다.** 예측 변수가 표 개수 → 표 행 수 → 표 바이트 비율로 **세 번 바뀌었고 6배치 편차가 1.31~2.06**이다. 예측을 정교화하기보다 착수 후 실측하고 하한만 지키는 편이 실용적이다 (`06166e9`)
- **원본 `08` 내부 모순 3건 — 전부 합계 검산을 통과한다** — ① **검수 필요도**가 §1-1 요약표 `상12·중15·하3` ↔ §2 카탈로그 30행 집계 `상11·중17·하2`로 갈리는데 **양쪽 합이 다 30**이다 ② **위임 링크 개수**가 산문 `8` ↔ 표 고유 이름 `13`으로 갈리는데 산문과 준수현황표가 서로 맞아 **2:1 다수결**이 된다 ③ **§3-2 절 제목이 「6섹션」인데 표는 7행**이고 빈 템플릿도 7섹션이다 — **제목은 아무도 세지 않는다.** 세 건 모두 병기 + 전수 배정 + 미판정으로 처리했다 (`06166e9`)
- **컨트롤러 브리프 오류 2건 — 둘 다 「상위 문서에 있는데 옮겨지지 않은」 형태다** — ① 브리프가 편19 핵심 수치로 §1-1 요약표 값을 그대로 옮겼고 **변환자가 §2 카탈로그 30행을 직접 세어 반증**했다. 브리프에 「값 대조는 절 단위로」를 적어놓고 정작 요약표 ↔ 상세표를 대조하지 않았다 ② §8-3 0편 태그 목록 미첨부 (`06166e9`)
- **반영이 새 결함 8건을 만들었다 — 4배치 연속이다**(B3 5 · B4 7 · B5 7 · **B6 8**) — 최상위는 **모집단을 바꾸고 목록을 승계한 것**이다. 1차 반영이 「원문에 없는 재규정」을 고치며 모집단을 원본 전체 → 발행 3편으로 바꿨는데 **원본 기준으로 세었던 「다섯 개」를 그대로 뒀다.** 3편 기준으로는 9개 토큰 / 6행이다 (`06166e9`)
- **2차 반영이 값을 고치는 대신 주장을 지웠다 — 자기지시 역설 때문이다** — *「이 용어표 **밖으로** 한 번도 나오지 않는다」*고 쓰면서 그 자리에 약어 이름을 적으면 **그 이름들은 방금 용어표 밖에 등장한다.** 9/6으로 고쳐도 결함이 남고, 문장에 「이 글이 세 편을 검색해 본 것이다」라는 **자기 표기**가 붙어 있어 계수 오류가 곧 명시적 거짓이 된다. **원 자료는 한 행도 빠지지 않았고 사라진 것은 이 글이 덧붙인 판정뿐이다** (`06166e9`)
- **「정확성을 올리는 수정은 분량을 늘린다」는 절반만 참이다** — B5의 결론인데 B6에서 **편19가 반영 후 줄었다**(28,886 → 28,458 B). 원문을 축자 인용하는 수정은 늘리지만 **이 글이 덧붙인 판정을 지우는 수정은 줄인다** (`06166e9`)
- **설계서 §12-3 미측정 항목 해소 — `08 §11-5` ↔ `enterprise-ai-adoption-actions`는 중복이 아니다** — 겉보기엔 둘 다 「단계적 도입」이지만 대상(파일 개수 대 조직 프로그램)·단위(개수 대 일수)·근거(**트리거 키워드 충돌** 대 **기준선 부재**)가 전부 다르다. **개념 유사성은 중복의 증거가 되지 못한다** (`06166e9`)

## 2026-08-11 — `agentic-coding` B5 발행, 설계서 진단 7건 정정

> **발행 4편(97 → 101편), sitemap 158 → 162 URL.** 원본 `07`(67,316 B)을 `agent-operations` 시리즈 편15~18로 변환하고 시리즈를 신설했다. 검증 4축이 **33건**을 지적해 중복 제거 후 20건을 반영했고, 반영 정확성 재검증이 **신규 7건**을 더 찾아 전량 반영했다. **설계서 진단 오류가 7건으로 5배치 중 최다**다. 배율 **1.97** (`2fc5c9f`).
>
> **이번 배치의 발견은 실패 지점이 한 층 위로 이동했다는 것이다.** B3·B4에서는 항목 배정 자체가 틀렸는데, B5는 **배정이 전부 정확했고**(4+1+1=6 · 3+2+3=8 · 8/8 · 2+3+5=10, 오배정 0건) **배정 아래 붙인 한 줄짜리 총평**에서 신규 결함 5건이 났다.

### 추가

- **`agentic-coding` 4편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `automation-scope-criteria`(30.3KB) · `agent-jd-triggers`(28.4KB) · `troubleshooting-three-layers`(24.3KB) · `scaling-routing-collapse`(36.2KB). 원본 표 50개·도식 13개를 **전량 이관**했고, 도식은 노드·화살표까지 원문과 **완전 일치**한다(`diff` exit 0). 하위 절 29개 전건 도달 (`2fc5c9f`)
- **`agent-operations` 시리즈 신설** — 편15~18. 앞선 4개 시리즈와 달리 **선행 편이 없어** 편15 도입부가 시리즈 전체 범위를 연다. 원본 L47의 구조 서술(`Part 7은 …을 다룬다`)이 금칙어라 그 자리를 다시 썼다 (`2fc5c9f`)
- **`07 §9`(인용 시 주의) 요구를 각주 21개로 이관** — 「전 편에 하나씩」이 아니라 **정량 수치가 나오는 자리마다** 달았다. 원문 L920이 지정한 수치(에이전트 3배 → 비용 7.4배 · 라우팅 오류율 5%→45% · MTTR 15분→4시간)가 §5·§6에 실재하며 전부 각주를 받았다 (`2fc5c9f`)
- **편15 ↔ 편17이 같은 사실을 양쪽에서 가리킨다** — `07`이 200줄을 §2-2(설계)와 §4-2(트러블슈팅) 두 목적으로 쓴다는 실측을 편15 대조표(6행)와 편17 §4-2 아래 역방향 서술로 **상호 링크**했다 (`2fc5c9f`)
- **편18에 `08`과의 셀 단위 대조 판단 기록** — `07 §7-1` 7부서 30에이전트 표가 `08`(B6 대상)과 **개수는 일치하나(5·5·4×5=30) 구성 슬롯은 22/30만 대응**하고 층위가 3열 조직도 대 7열 정의서로 다르다. 「완전 중복」이 성립하지 않아 **표를 유지**했다 (`2fc5c9f`)

### 수정

- **설계서 §5-4 — 60줄 ↔ 200줄은 이항 대립이 아니다** — 원본 `07 §2-2`에 **100줄(「위험」)** 이 따로 있고, 그 폐해 4가지 중 하나가 *"규칙이 100개면 모델이 그중 일부를 **무작위로** 무시한다"* 는 **준수율 개념**이다. 더구나 **`07` 자체가 200줄을 §4-2에서 두 번 쓴다**(L503·L521). 브리프가 지시한 대조 구도가 원문과 어긋나 있었고, **변환자가 편17을 쓰다 반증을 발견해 정정**했다 — 순차 변환이 아니었으면 편15 담당자가 §4를 열 일이 없었다 (`2fc5c9f`)
- **설계서 §5-5 「§2-2는 25항목」의 근거가 없다** — 4,255 B는 정확하나 §2-2 실측은 **표 4개(24행) + 불릿 8항목**이다. **`07 §0` 용어표가 정확히 25행**이라 두 절 값이 한 괄호에서 섞인 것으로 보인다. 「N항목」 총계를 쓰지 않고 원문이 스스로 부르는 이름만 썼다 (`2fc5c9f`)
- **「비용 통제 1차 = `07 §3`」 진단은 옳았다** — 조사가 *"§3에 비용 서술 0줄"*이라 보고했으나 **틀렸다.** L420(비교표 행)·L424(원칙 문장) **2행**이 실재한다. **「0줄이다」는 부정 명제라 좁은 정규식 하나로는 증명되지 않는다** — `비용` 단독이 아니라 `비용|과금|요금|청구|예산`으로 넓히자 즉시 드러났다. **금지선 15는 변환자만이 아니라 조사 단계에도 걸린다** (`2fc5c9f`)
- **설계서 §4-1 「series / order」는 필드명이 아니다** — 실제 필드는 **`seriesOrder`**이고, `series`만 있고 `seriesOrder`가 없으면 **빌드가 실패**한다. 선택 필드는 없으면 키 자체를 생략해야 한다(`undefined`는 Next.js JSON 직렬화 실패) (`2fc5c9f`)
- **설계서 §6-6 「절대 수치를 전부 버린다」를 문자 그대로 적용하면 예방책이 빈다** — §7-4 예방 열에도 §7-2에서 온 수치가 있다. **판별 기준은 수치가 무엇을 주장하는가**다 — 「2주 만에 예상의 3배 청구」는 **한 사람에게 일어난 사건**이라 버리고, 「기술 개발 30% 시점」·「3개 초과」는 **누구에게나 적용되는 설계 임계**라 남겼다 (`2fc5c9f`)
- **설계서 §4-4 축약 용어표 판정 기준이 정해져 있지 않았다** — **문자열** 기준이면 편17은 7행 중 5행이 위반(`롤백(Rollback)`은 본문이 「되돌리기」로 쓴다)이고, **개념** 기준이면 `Bounded Context`도 편16·18에서 쓰인다. 어느 쪽으로도 검증이 든 「3행만 예외」는 나오지 않는다. 규칙의 취지는 **비대화 방지**이므로 개념 기준으로 확정했다 (`2fc5c9f`)
- **설계서 §10 팽창률 재정정 — 표 밀도 가설이 반증됐다** — 「표가 성기면 요약 비용이 덜 붙어 배율이 낮다」는 추정이 **정확히 반대로** 나왔다(편17 예상 1.60 → 실측 1.875). 원인은 **큰 표일수록 「한 줄 요약」이 길어지기 때문**이다 — 10행 표의 요약은 전수 배정 문단이 된다. **팽창은 표 개수가 아니라 표 행 수에 붙는다** (`2fc5c9f`)
- **설계서 §11에 금지선 17 추가 — 전수 배정을 마친 뒤 총평 문장을 붙이지 마라** — 신규 결함 7건 중 **5건이 배정이 아니라 배정 아래 한 줄**에서 났다. 「원 자료의 실습 예제」(오귀속)를 「이 글에서 바꿔 실었다」로 고치자 **6행 중 1행만 바뀌었는데 전칭이 됐다** — **전칭은 방향만 뒤집혀도 여전히 전칭이다** (`2fc5c9f`)
- **변경된 값에 원 자료 귀속이 남아 있던 최상위 결함** — 금칙어(`이력서`·`면접`)를 피해 라우팅 키워드를 바꿨는데 그 표를 감싼 두 문장이 여전히 *"**원 자료의** 라우팅 규칙"*·*"키워드 목록은 **원 자료의 실습 예제**"* 였다. `grep -rn "지원서|전형|인사 심사"` → **어느 원본에도 없다.** **금칙어 제거가 새 결함을 만드는 경로**이며 앞 배치 기록에 없던 실패 모드다 (`2fc5c9f`)
- **편17이 「첫 편」과 「앞 편」을 자기 안에서 다르게 적었다** — `:19`는 「첫 편이 배치했던 15분·30분 점검」이라 했으나 그 표는 **편16**에 있고, 같은 글 `:138`은 「[앞 편](agent-jd-triggers)」로 정확히 귀속했다. **한 원본을 4편으로 쪼개면 「어느 편에 무엇이 있는지」가 새 사실이 되는데 그것을 대조할 진실원이 원본에 없다** (`2fc5c9f`)

## 2026-08-09 — `agentic-coding` B4 발행, 설계서 진단 6건 정정

> **발행 4편(93 → 97편), sitemap 154 → 158 URL.** 원본 `03`(53,967 B)을 `claude-md-context` 시리즈 편6~8로, `team-productivity`(11,177 B)를 독립편 편26으로 변환했다. 검증 4축이 **31건**을 지적해 중복 제거 후 24건을 반영했고, 반영 정확성 재검증이 **신규 7건**을 더 찾아 전량 반영했다. **설계서 진단 오류가 6건으로 4배치 중 최다**이며, 그중 4건은 **컨트롤러 브리프가 원본을 열지 않고 상위 문서를 신뢰한 자리**에서 났다. 배율 1.77 (`a568c2e`).

### 추가

- **`agentic-coding` 4편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `claude-md-scope-layers`(25.1KB) · `dot-claude-directory`(29.1KB) · `context-budget-session`(25.8KB) · `team-adoption-roi`(23.7KB). 원본 표 46개·도식 8개를 **전량 이관**하고 편26에 신규 도식 2개를 더했다. 하위 절 51개도 전건 도달 (`a568c2e`)
- **`claude-md-context` 시리즈 신설** — 편6·7·8. `03`의 §7이 §6을 명시 참조(L699)하므로 편8 도입부를 **편7 링크와 7패턴 한 문장 요약**으로 열었다. 조사자가 「분리하면 §7이 홀로 서지 못한다」며 반대했던 자리다 (`a568c2e`)
- **편26은 `source`·`series`·`seriesOrder` 세 키를 비웠다** — `team-productivity`는 `> 원본:` 필드가 없는 **웹 조사 종합본**이라 강의 출처를 달면 사실이 틀린다. 대신 참고자료 **URL 9건을 각주에 링크로 복원**했다 (`a568c2e`)
- **`03 §10-2` 수치 주의 6행 전건 이관** — 설계서가 2행으로 규정했으나 실제는 6행이다. `200줄`·`5~7개`는 편6, `70/85%`·`한국어 2배`·`18,000토큰`은 편8, **`80~120줄`은 §2-4·§4-1·§5 세 곳에 걸쳐 있어 편6·편7 양쪽**에 달았다 (`a568c2e`)
- **`03 §11` L1084 사실 경계 고지를 편6·7·8 세 편 전부에** — 설계서 §6-5 목록에 없던 항목이다. B3에서 편13 단독 누락 사고가 났던 유형이라 **편별 체크리스트로 지시**했고 3/3 도달했다 (`a568c2e`)

### 수정

- **설계서 §6-3 `team-productivity` 진단 3건이 틀렸다** — ① 원문 L145에 `(Zapier·Tines 등)`이 실재해 「행 단위로 매칭하지 않았다」가 **9건 중 8건에만 참**이다 ② 참고자료 9건 중 **3건에 원문 분류 라벨**(`— 공식 문서`×2·`— Anthropic 블로그`)이 있다 ③ 안티패턴은 5종이 아니라 **6종**이다. ①②는 브리프가 각주 문안을 그대로 제공해 **최상위 결함 2건을 직접 만들었다** (`a568c2e`)
- **설계서 §5-4 원본 내부 모순이 3건이 아니라 4건** — `01` L452 「200줄 안팎을 **상한**으로」와 `03` L108 「**하드 리밋이 아니라** 준수율 임계」가 같은 값을 다르게 규정한다. 편1과 편6이 **각자 원문에 충실했는데 결과가 부딪히는** 구조라 배치 단위 검증으로는 잡히지 않는다. 편6에서 링크와 층위 구분으로 해소했다 (`a568c2e`)
- **설계서 §6-4 시점 값 `2026-04`의 적용 범위** — 원본 **12편 전부** L1~L3이 `작성 기준일: 2026-07-26`이고 **`03`에 `2026-04`는 0건**이다. `2026-04`는 「강의 시점」이라 명시된 자리에 한정된다. 편6·7·8은 `2026-07-26`, 편26은 `조사일 2026-06`을 썼다 (`a568c2e`)
- **설계서 §6-5 5번 가상 실습 소재 4종 중 2종은 발행 범위 밖** — `TechCorp`·`NextLevel`은 `03` 전체에서 L1086·L1094 두 곳뿐이고 **둘 다 §10·§11(발행 제외 절)** 소속이다. 없는 이름에 고지를 달면 원문에 없는 대상을 만든다. 실제로 등장하는 2종만 국소 각주를 받았다 (`a568c2e`)
- **설계서 §9-1 `Plan Mode` 전방 참조는 걸 수 없다** — `03`에서 `Plan Mode`는 **L1056 한 곳뿐이고 §10 면접 표의 셀**이다. 발행 대상 절에 근거가 없어 링크를 만들면 지어내는 것이 된다. 나머지 3건(L487·L489·L922)은 본문 소속이라 전건 도달했다 (`a568c2e`)
- **설계서 §11에 금지선 15·16 추가** — **15: 「원 자료는 X를 하지 않았다」는 귀속으로 방어되지 않는다.** B3가 세운 「귀속을 붙이면 거짓이 될 수 없다」는 **자기 해석**에만 참이고, 원문에 대한 **부정 사실주장은 2/2 실패**했다. **16: 전칭(「전부」)을 분류로 바꾸면 실패 모드가 바뀔 뿐이다** — 「전부」는 반례 하나로, 「N갈래」는 미배정 하나로 무너진다 (`a568c2e`)
- **편26 각주가 검증 수단을 주지 않던 문제** — 「벤더 리서치는 측정 도구를 파는 쪽이 낸 자료라는 점을 감안해 읽어야 한다」고 **독자에게 출처 판정을 요구하면서 URL이 하나도 없었다**(`grep -c "http"` → 0). 9건 전부 링크로 복원하고, 근거 없는 `DataStudios`는 벤더 묶음에서 빼 「기업 사례 정리」로 다시 분류했다 (`a568c2e`)
- **편6 Scope 표 요약이 표와 정면 모순** — 「우선순위는 그 **역순**이다」라고 썼으나 공유 범위와 우선순위가 **같은 방향**으로 내려간다. 독자가 「Enterprise가 최하위」라는 정반대 결론에 도달한다. 혼동의 출처는 원문 L94의 **충돌 시 적용 규칙**(좁을수록 이김)인데, 표의 우선순위 열은 **Scope 등급**이라 다른 층위다 (`a568c2e`)
- **편1 링크 문구가 대상 글의 논지를 뒤집었다** — 「되돌릴 수 **없는** 것부터 막는다」로 걸었으나 편1 제목은 「되돌릴 수 **있어야** 권한을 **넓힌다**」이고 본문에 그 문구가 0건이다. 전자는 오히려 `settings-permissions-deny`의 논지다 (`a568c2e`)

## 2026-08-09 — `agentic-coding` B3 발행, 설계서 진단 1건 정정

> **발행 3편(90 → 93편), sitemap 151 → 154 URL.** 원본 `05`(60,884 B)를 `subagent-design` 시리즈 편12~14로 변환했다. 검증 4축이 **27건**을 지적했고 반영 후 제3자 재검증이 **5건**을 더 찾아 전량 반영했다. 앞 두 배치의 최다 유형(수치 불일치·원문 강도 어긋남)은 **284행·60건 전수 대조에서 0건**이다. 대신 결함이 **이번 배치에서 처음 한 작업** — 「도식을 새로 그리는 일」과 「자산을 3편에 분산하는 일」 — 로 옮겨갔다. `main` 배포 완료(`cf948a1`).

### 추가

- **`agentic-coding` 3편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `subagent-definition-fields`(29.3KB) · `collaboration-patterns-isolation`(19.6KB) · `agent-team-operations`(30.9KB). 원본 표 59개·도식 16개를 **행 수와 노드 줄 수까지 1:1로** 이관하고 편12에 신규 도식 1개를 더했다. 배율 1.31 (`cf948a1`)
- **`05 §10-4` 출처 각주 6건** — 6개 수치(Hub-and-Spoke 4.4↔17.2배 · Reasoning Sandwich 66.5% · 토큰 4↔15배 · 모델 계층화 +90.2/−50% · 병렬화 +81/−70% · Phase 분리 85%)를 **본문에서 실제로 만나는 편**에 각주로 달았다. 다섯은 외부 출처(Google/MIT·LangChain·Anthropic)이고 **하나만 원 자료 자체의 운영 사례**라 그 성격 차이를 명기했다 (`cf948a1`)
- **`05 §3-2` 제품 결함 3건에 「버전 핀 없음」 명기** — 원문이 미해결 버그 3건을 단언하는데 보고 주체·버전·관측 시점이 전부 없다. 발행본 선례 `tool-contract-and-partial-failure` 형식을 따르되 **「세 건 모두 버전 표기가 없다」는 사실 자체를 제목과 본문 양쪽에** 적었다. 시점 없이 발행하면 수정된 뒤에도 결함 주장이 화석으로 남는다 (`cf948a1`)

### 수정

- **설계서 §6-5 8번 「`05 §11`은 출처 대장」 → 실측과 다름** — 원문 L1042~L1062를 열어 보니 **면접 화법 가이드**다. 실제 출처 대장은 §10-4 하나뿐이다. 1,523 B를 통째로 각주로 옮겼다면 면접 화법이 발행본에 들어갔다. **L1044 사실 경계 1문장만** 가져왔다. 놓친 경로는 절 제목(`인용 시 주의 — 사실 경계`)이 출처 관리 절로 읽힌 것이다 (`cf948a1`)
- **설계서 §4-4에 미배정 절 2건 확정** — `05 §10-1`(2,799 B 면접 Q&A)은 **B8로 이월**, `05 §10-3`(876 B 압축 문장)은 **드롭**. 초안에 배정이 없어 역방향으로 훑어야 드러났다. §10-4 bytes도 `~800` → **968 실측**으로 정정 (`cf948a1`)
- **편12 신규 도식의 거짓 화살표** — `확장 5 --> 10-Point Validator`가 성립하지 않는다. **10항목이 이름을 대는 필드는 7개**이고 `isolation`·`skills`·`disallowedTools`는 한 항목에도 없다. 점선 + 「5 중 2」 라벨로 바꾸고 그 공백을 본문에 **귀속과 함께** 명기했다. 여섯 화살표 중 다섯은 참이었다 (`cf948a1`)
- **편13에만 없던 것 2건** — 사실 경계 고지와 `+81%/−70%`의 원 출처(Google/MIT 2025 연구)가 편12·14에는 있고 편13에만 없었다. 편은 독립 URL로 유입되므로 편13 독자에게는 닿지 않는다. **「3편에 분산」 지시가 한 편에서만 이행되지 않은 유형** (`cf948a1`)
- **축약 용어표 기준 재수립** — 「본문 출현 0회면 뺀다」가 선언만 되고 적용되지 않아 편14에서 `Mailbox`가 빠지고 `TaskList`·`blockedBy`(둘 다 0회)는 남았다. 기준을 **「이름이 아니라 개념」**으로 통일해 편13은 12→10행, 편14는 10→16행으로 다시 만들었다 (`cf948a1`)
- **자기 절 제목을 부정하던 요약 2건** — 편12가 원문 제목 `심화 5기법 — 프로덕션 급으로 올리기`를 남겨 두고 열 줄 아래에서 *"품질을 올리는 장치가 아니라"* 라고 썼다. 원본 실패 증상 열을 행별로 판정하면 **3·4·5는 단일 에이전트 문제**다. 편14는 `유비 (핵심 6개)`의 「핵심」을 지우고 전수 대응을 주장했다 (`cf948a1`)
- **설계서 금지선 2건 추가(13·14)** — 「상한 29.4KB는 합격선이 아니다」와 「새 판정에는 귀속을 붙여라」 (`cf948a1`)

### 알아둘 것

- **결함 하나의 원인이 컨트롤러 브리프의 누락이었다.** §4-0 원문은 *"13.3~29.4KB는 발행본의 실측 분포이지 **합격선이 아니다**"* 인데 브리프는 *"29.4KB를 넘으면 보고하라"* 로만 적었다. 변환자는 초고 30,528 B를 세 차례 깎아 29,373 B(여유 27 B)로 맞췄고 **마지막에 축약 용어표 행을 삭제했다.** **"넘으면 보고하라"만 받은 구현자는 보고를 피하려고 내용을 깎는다** — 제약을 줄 때 경성인지 연성인지 함께 말해야 한다
- **귀속 표기가 방어선이었다** — 변환자가 「이 글의 정리다」를 붙인 요약 **6건은 6/6 통과**했고, 축 B의 최상위 지적 5건은 **전부 표기가 없는 요약**에서 나왔다. 귀속이 붙으면 「원문의 주장」이 아니라 「이 글의 해석」이 되어 거짓이 될 수 없다. **검증자가 요건 밖에서 찾아낸 규칙이다**
- **축 C(없는 것)를 별도 축으로 둔 값이 증명됐다** — 「편13에만 고지가 없다」는 A·B 어느 축도 찾을 수 없다. 발행본을 읽으면 편13은 완결돼 보이고, 원문 대조를 해도 「L1044가 편12·14에 있다」까지만 확인된다. **원본 자산 하나가 3편 중 몇 편에 도달했는지 세는 것**은 역방향으로만 나온다
- **브리프의 `[추론]` 표기가 값을 했다** — §10-4 수치의 「어느 편에 나오나」 매핑은 절 제목만 보고 추정한 것이었고 **6건 중 3건이 틀렸다.** 표기 덕에 변환자가 본문 출현 위치로 재검해 바로잡았고, 축 A·B가 각각 독립적으로 「변환자가 옳게 뒤집었다」고 확인했다. **표기의 값은 맞는 것을 표시하는 데 있지 않고 검증되지 않은 것을 표시하는 데 있다**
- **정확도를 올리는 수정이 새 검증 부담을 만든다** — 편12 L117의 잘못된 행 이름(「수명」, 실제는 「상태·지속성」)은 반영 전에도 있었고 축 A·B·C 셋 다 통과시켰다. 그런데 반영이 그 문장을 **「4+1+2=7 전수 정산」**으로 다시 쓰자 독자가 일곱 이름을 표에 대응시키게 됐고 **그 순간 이름 불일치가 결함이 됐다**
- **재검증자가 부산물로 pre 이미지를 복원했다** — 반영 전 파일이 남아 있지 않은데 축 D가 남긴 연결본(77,353 B)을 바이트 오프셋으로 갈라 diff했다. **「새 산문을 늘리지 않았다」는 주장을 말로 받지 않고 바이트로 확인**해 8 hunk 전부가 지적 항목에 1:1 대응함을 보였다
- **`npm test`는 36개가 맞다** — 조사자가 「테스트 파일 0개」로 보고했으나 `tests/blog/` 아래 3파일이 실재한다. 조사 보고는 액면가로 받지 않는다
- **mermaid는 빌드가 검증하지 않는다** — 클라이언트 렌더링이라 문법 오류가 있어도 종료코드 0으로 통과하고 브라우저에서만 깨진다. 도식을 손댄 배치에서는 **17개 블록 전수를 눈으로 봐야 한다**

## 2026-08-08 (4) — `agentic-coding` B2 발행, 설계서 진단 3건 정정

> **발행 3편(87 → 90편), sitemap 148 → 151 URL.** 원본 `04`(47,241 B)를 `claude-code-extensions` 시리즈 편9~11로 변환했다. 검증을 **네 축**으로 나눠 돌려 **지적 15건을 전량 반영**했다. 직전 배치 최다 유형이던 **수치 불일치가 35건 전수 대조에서 0건**이고, 팽창률도 1.76배 → **1.42배**로 내려갔다. 대신 결함이 **다른 문서를 끌어와 대조한 대목**으로 이동했다 — 그리고 그 최상위 2건의 출처가 **컨트롤러의 브리프**였다. `main` 배포 완료(`f4b9c30`).

### 추가

- **`agentic-coding` 3편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `extension-trigger-axis`(15.3KB) · `mcp-selection-practice`(23.9KB) · `commands-skills-hooks-plugins`(22.2KB). 원본 mermaid 9개가 **편별 3·3·3으로 바이트 동일** 이관(신규 0 · 드롭 0), 표 45개는 행 단위 전수 대조에서 43개 IDENTICAL + 의도된 차이 2개, 코드블록 10개 전부 IDENTICAL. **기술 내용 드롭 0건** (`f4b9c30`)
- **§13 각주 이관 4건** — 원본 §13(발행 제외 절)이 *"수치(12,000+ 서버, 66% 취약점, 35배 토큰, 9,700만 다운로드) — 인용치임을 밝히고 사용. 직접 측정치가 아님"* 이라고 스스로 규정한다. 이 규정을 떼고 수치만 옮기면 **출처 없는 성과표**가 된다. 네 수치 전부 편10 등장 지점 근처에 고지를 달았다 (`f4b9c30`)

### 수정

- **설계서 §5-3(1) 「발행본 다섯은 컨텍스트를 무엇이 소비하는가로 센다」 → 실측과 다름** — `harness-five-primitives` 도식의 최상위 노드는 `Context Management (WHY)`이고 본문이 *"최상위 노드가 다섯 중 하나가 아니라 **다섯의 이유**"* 라고 명시한다. **훅은 그 글 컨텍스트 분해표에 행 자체가 없는데 다섯에 들어 있어** 소비 기준으로는 성립하지 않는다. 열거 기준을 「컨텍스트 관리를 상위 목적으로 공유하는 수단」으로 정정 (`f4b9c30`)
- **설계서 §5-3(1) 요건 4의 인과 삭제** — *"상시 비용이 아니라 목록에서 빠진 것"* 은 그 글에 없는 인과다. 그 글은 MCP를 뺀 이유를 밝히지 않는다. **인과를 지우는 대신 반례를 본문에 적었다** — "커맨드와 훅은 이 표에 행 자체가 없는데도 다섯에 들어 있다" (`f4b9c30`)
- **설계서 §5-3(2) 발행본 시점 「없음」 → 「2026년 3월 자료 기준」** — 그 글 L19에 시점 고지가 실재한다. 초안 지시대로 *"발행본은 시점을 명시하지 않았다"* 라고 쓰면 **거짓 서술**이 된다. 원본 쪽 판정(`04` L484~494에 표기 없음)은 재확인 결과 맞다 (`f4b9c30`)
- **태그 패싯 분포 위반 2편** — `tags.ts` 상단 주석이 **「같은 패싯에서 최대 2개」** 를 규정하는데 편9·편11이 패싯 C를 3개 달았다. `isKnownTag()`는 어휘 소속만 검사해 **빌드가 통과한다.** 편9 `ai-governance`→`org-design`, 편11 `ai-governance`→`observability`로 교체. B1 5편은 전부 지키고 있었다 (`f4b9c30`)
- **편11 훅 차단 채널 모순 고지** — 편11은 차단 사유가 **stdout JSON**으로 간다 하고, 편11이 직접 링크한 `settings-permissions-deny`는 **표준 에러**라고 쓴다. 두 원본 사이의 부채이며 전사 오류가 아니지만 **편11이 링크를 걸어 독자 눈앞에 나란히 놓았다.** 고칠 곳은 편11이라 판단해 한 문장을 붙였다 (`f4b9c30`)
- **편10 실무 함정 분류 정정** — 다섯 증상을 「인증 범위 / 데이터 형식」 두 갈래로 묶었는데 「권한 오류」(원인: 대상 문서 공유 설정)가 어디에도 안 들어간다. **합계 3·5는 맞고 항목이 틀린** 유형이라 세 갈래로 고쳤다 (`f4b9c30`)
- **강도 정렬 3건** — 「설치 순서를 **정한다**」→「이어진다」(본문은 「효율적이다」), 「첫 행이 정해지면 **따라 정해진다**」→「그 주체를 따라 갈린다」, 「실무 MCP 6종」→**「필수 MCP 6종」**(원문 복원) (`f4b9c30`)
- **축약 용어표 미정의 참조 4건 보강** — 편10 축약표가 자기 안에서 `SSE`를 참조하는데 행이 없었다. `SSE`·`Hook(훅)`·`플러그인`(편10) + `MCP 서버`(편11)를 원본 §0에서 **행 그대로** 옮겼다. 새 정의를 쓰지 않았다 (`f4b9c30`)

### 알아둘 것

- **지적 최상위 2건의 출처가 컨트롤러의 브리프였다.** 변환자는 브리프를 정확히 따랐고 브리프가 틀린 인과를 지시했다. CLAUDE.md에 적힌 *"리뷰가 걸러낸 실질 결함 17건 중 15건이 계획 자체의 결함이었고, 구현자들은 브리프를 정확히 따랐다"* 가 그대로 재현됐다
- **브리프의 오류 3건이 전부 「검증 없이 옮김」이다** — ①설계서 진단(열거 기준) ②컨트롤러의 추론(배제 인과) ③scout 보고(용어표 35→실제 34). scout의 **바이트 값은 재계수했는데** 그건 설계서라는 대조 상대가 있었기 때문이다. **대조 상대가 없는 값은 전부 통과했다**
- **축A와 축B가 서로 모른 채 같은 세 지점을 짚었다.** 그중 하나에서 축A는 **결과**(인용한 표에서 반증됨), 축B는 **원인**(그 글의 실제 열거 기준)을 찾았다. 같은 결함의 서로 다른 절반이라 한 명에게 맡겼으면 하나만 나왔다
- **변환자가 반례를 피해 간 흔적이 결함의 지표였다** — 틀린 전제를 받은 변환자가 *"메모리와 에이전트는 컨텍스트를 먹지만"* 이라며 **다섯 중 둘만 들고 훅은 언급하지 않았다.** 훅을 넣는 순간 자기 규정이 깨지기 때문이다. **글이 어색하게 좁아지는 곳에 잘못된 전제가 있다**
- **scout 보고서가 스스로 모순된 것이 재계수의 신호였다** — §0을 한 표에서 3,489, 다른 표에서 3,532로 적었고 표 대표 위치가 절 범위를 벗어났다. 차이가 **절의 줄 수와 정확히 일치**(43·26·24)해 개행 누락으로 확인됐다. 설계서 값이 맞고 scout이 틀렸다. **합계·항목·위치를 모두 내게 하면 셋이 어긋날 때 오류가 드러난다**
- **축C가 표본 9개 지시를 받고 전수 45개를 돌렸다** — awk로 표 경계를 자동 추출할 수 있어 비용 가정이 깨졌기 때문이다. **브리프의 비용 추정은 지시가 아니라 기본값**이다
- **원본보다 나아진 곳 1건** — 원본 `04` L624 표의 인라인 코드 안 파이프에 이스케이프가 없어 GFM에서 셀이 쪼개지는데 변환자가 백슬래시를 넣었다

## 2026-08-08 (3) — `agentic-coding` B1 발행, 설계서 진단 2건 정정

> **발행 5편(82 → 87편), sitemap 142 → 148 URL.** 원본 `01`(축약)·`02`를 편1~5로 변환했다. 검증을 세 축으로 나눠 돌려 **지적 21건을 전량 반영**했고 반박은 0건이다. 결함이 한 곳에 몰렸다 — **원본을 옮긴 부분은 결함 0이고, 18건 전부가 변환자가 새로 쓴 요약문에서 났다.** 그 과정에서 설계서 진단 2건이 실측과 어긋난 것을 발견해 정정했다. `main` 배포 완료(`388519e`).

### 추가

- **`agentic-coding` 5편** [`content/blog/agentic-coding/`](content/blog/agentic-coding/) — `reversibility-before-permission`(독립편) + `claude-code-tools` 시리즈 4편. 도식 19 · 표 52 · 내부 링크 6건 전수 실재. 원본 `02`의 mermaid 17개가 드롭 0으로 전량 보존됐고 표 49개도 1개도 빠지지 않았다 (`388519e`)
- **검증 보고서 5건** [`docs/superpowers/reports/`](docs/superpowers/reports/) — 변환 1 + 실행 검증 1 + 위험 축별 리뷰 2 + 반영 1. **구현자가 아닌 에이전트가 검증**했고, 리뷰는 「근거 주체」·「팽창 출처」 두 축으로 나눠 병렬로 돌렸다 (`388519e`)

### 수정

- **발행본 지적 18건** — 지배 유형은 **「수를 세어 쓴 주장이 같은 화면의 자기 표와 안 맞는다」**(5건). 「30일 항목이 다섯」이 실제 여섯이고, 「역할이 **둘** 더 붙는다」인데 표는 3행이며 8줄 뒤에 「**세 역할**은…」이 온다. `b654a5a`에서 정정한 유형이 그대로 재발했다. 나머지는 원본에 없는 주장·강도 강화 6건, 자기모순 3건, 출처 없는 외부 권위 2건, 고지 비대칭·용어표 미사용 2건 (`388519e`)
- **어긋남의 방향이 한쪽이다** — 원문의 **확률 주장이 단정으로**, 「대부분을 예방」이 「통째로 예방」으로 바뀌었다. 요약문은 원문보다 **세게 말하는 쪽으로만** 틀렸다 (`388519e`)
- **설계서 §6-1 「`강의가 ~한다` 형태 130여 개」 → 실측 32로 정정** — `강의가` 15 + `강의는` 17이며 조사형 6종을 전부 합쳐도 66이다. **4.1배 부풀려져 있었다.** `강의` 총 160행이라는 값은 맞다. 이 카테고리 **최대 위험으로 지목했던 진단이 과대평가**이며, `02` 단독은 주어 형태가 0건이라 B1에서 「주어를 바꾸는 작업」이 아예 발생하지 않았다 (`388519e`)
- **설계서 §10 팽창률 1.20 → 실측 1.76, 26편 총량 468.2KB → 650~670KB 재추정** — 초과분 22.4KB 중 **6.6KB는 계상 방식 차이지 재량이 아니다.** frontmatter와 축약 용어표가 §4-1 「원본 B」 열에 없어 **편당 ~2KB가 배율 계산 밖에서** 발생한다 (`388519e`)
- **요구사항 §13-1에 `06`·`08` 이관 기록, `tags.ts` 주석에서 `agentic-coding` 예외 문구 삭제** — 주석이 어휘 배열에 없는 태그를 예외로 선언하던 불일치를 해소했다. 변환자가 그 주석을 믿고 태그를 달면 `isKnownTag`가 false를 반환해 빌드가 막힌다 (`388519e`)

### 알아둘 것

- **변환자의 「`02`는 표 밀도가 비정상적으로 높다」는 채택하지 않았다.** 직접 계산하니 `02`(1,149 B/표)는 9개 파일 중 **5위**이고 `04`(984)·`05`(1,031)가 더 조밀하다. 팽창은 `02` 고유가 아니라 **전역**이며, 그래서 재추정이 오히려 단단해진다. 덜 팽창할 파일은 `08`(2,266)과 `team-productivity`(11,322)뿐이다
- **설계 검증 2기가 42건을 잡고도 「130」을 못 잡은 이유** — 검증이 사슬 연산·표 교차 대조 같은 **내부 정합성**에 집중했고 이 값은 외부 실측이라 대조 상대가 없었다. **스스로 모순되지 않는 틀린 값은 내부 검산으로 드러나지 않는다**
- **상쇄 오류가 또 나왔다** — 변환 보고서 분류표가 `02` L1213을 `강의`로 오인용하고(실제 단어는 `강사`) L647을 누락했는데 **두 오차가 1:1 상쇄돼 합계 19가 우연히 맞았다.** 처리 자체는 정상이라 발행본 영향은 없고 장부만 틀렸다. HANDOFF §6-2에 적힌 함정이 설계에 이어 변환 단계에서 재발했다
- **수정 중 자체 발생한 결함 1건** — 요청 범위 밖의 선택 항목을 고치다 SC-12 금칙 표현(`인용할 때`)을 새로 넣었고 검증 스캔이 잡아 재수정했다. **발행본을 손댄 뒤에는 금칙어 스캔을 반드시 재실행한다**

## 2026-08-08 (2) — `agentic-coding` 분할 설계

> **설계만 하고 멈췄다. 발행본 변경 없음.** 원본 12편 542.7KB를 조사 6기가 실측하고, 검증 2기가 설계서를 3회 반려·재검증했다. **80KB(`aiagent/06` 전량 + `08 §4~§8·§10`)를 `ai-transformation`으로 이관**하기로 하여 §13-1 사양이 바뀐다. 다음 세션이 승인 후 B1부터 착수한다.

### 문서

- **`agentic-coding` 분할 설계서** [`docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md`](docs/superpowers/plans/2026-08-08-agentic-coding-category-split-design.md) — 원본 12편 542.7KB → **발행 31편**(본문 26 + Q&A 4 + 지식 지도 1). 절별 KB 실측, 교차 중복 21건 판정, `series` 8종·`slug` 31개 확정(기발행 82편과 충돌 0), 도식 검산 85, 금지선 12건
- **범위 재정의 2건 (사용자 확정)** — ① `06` 전량(61.7KB)과 `08 §4~§8·§10`(16.7KB)을 **`ai-transformation`으로 이관.** `06` 전문에 `Claude Code`·`MCP`·`Skills`·`Hooks`가 **0건**이고 내용이 비개발 직군 업무 자동화다. ② `01`은 §6-1·§9·§10만 살려 **1편으로 축약.** `nvm`·`ssh-keygen`·`npx`·`iTerm2`가 발행본 82편에 각각 0편 — 이 블로그가 다룬 적 없는 층위다
- **출처·근거 주체 규칙 신설(설계서 §6)** — 원본에 `강의`가 **160행**(직전 카테고리는 1건), 그중 130여 개가 `강의가 ~한다` 형태라 **문서의 서술 주체가 "강의"**다. CR-1.10("주체를 갈아 끼우지 마라")만으로는 130건을 통과시킬 수 없어 ㉠개념·㉡수치·㉢귀속구분 **세 갈래 판별 기준**을 만들었다
- **`agentic-coding` 태그는 어휘에 추가하지 않기로 확정** — `tags.ts` L30 주석이 예외로 선언하는데 배열에 없던 불일치(직전 세션 이월 과제 §3-6). 신규 태그 4조건 중 ③(대체 불가)에서 탈락 — **`agentic-coding`만 잡고 `claude-code`는 못 잡는 편이 실측 0편**이다. 주석에서 삭제하는 diff를 설계서 §8-1에 적어 뒀다

## 2026-08-08

> **`ai-agent` 카테고리 51편 발행.** 원본 16편 878KB(`langgraph/00~11` + `rag/07~10`)를 15개 시리즈 51편으로 변환했다. 블로그가 **31편 → 82편**, sitemap이 **83 → 142 URL**로 늘었다. 도식 158개가 절별 대조로 전량 보존됐고, 발행본 51편 전부가 들어오는 링크를 1건 이상 갖는다. `main` 배포 완료(`629ea9c`).

### 추가

- **`ai-agent` 카테고리 51편** [`content/blog/ai-agent/`](content/blog/ai-agent/) — 기초 3시리즈 7편(`6792ac6`), 실행 모델 3시리즈 10편(`236fac8`), 제품 클론 4시리즈 9편(`840e280`), 2025 담론 2시리즈 8편(`ed468e0`), 2026 담론 2시리즈 12편(`68f98a2`), Q&A 4편(`45d3235`), 지식 지도 1편(`102d4c8`)
- **Q&A 4편** [`content/blog/ai-agent/ai-agent-qna-*.md`](content/blog/ai-agent/) — 원본 15편에 흩어진 면접 절을 회수해 **파일이 아니라 주제축으로 재편성**했다. 파일별로 모으면 같은 질문이 네 곳에 흩어진다. 94문항 (`45d3235`)
- **지식 지도** [`content/blog/ai-agent/ai-agent-knowledge-map.md`](content/blog/ai-agent/ai-agent-knowledge-map.md) — 개념 계보(각 단계가 앞 단계의 무엇을 풀고 대신 무엇을 만드는가), 문제→편 매핑 19행, 통합 용어집 41항. **참조가 전부 앞 편을 가리켜 slug가 확정된 마지막에 집필**했다 (`102d4c8`)
- **분할 설계서** [`docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md`](docs/superpowers/plans/2026-08-08-ai-agent-category-split-design.md) — 절별 KB 실측, 교차 중복 7건, 검산 기준값, 금지선 7건, 화행 잔재 수정 목록 (`c0c38fe`)

### 수정

- **`rag` ↔ `ai-agent` 양방향 링크** — `rag-knowledge-map.md`가 *"에이전트 아키텍처 쪽에서 따로 다룬다"*고 예고만 하고 **대상이 없던 상태**였다. 12개 링크로 연결하고, `langgraph-parallel-multiagent`·`agentic-rag-relevance-check`·`langgraph-module-boundaries`·`rag-qna-operations`에서 심화 편으로 잇는 링크를 추가했다 (`73f5b60`)
- **면접 화행 잔재 12건** [`content/blog/ai-agent/`](content/blog/ai-agent/) — `인용할 때는 … 수준으로 말하는 것이 안전하다`처럼 **독자에게 발화를 지시하는 문장**을 서술 규칙으로 바꿨다. 금칙어가 한 개도 없어 스캔에 안 잡히는 유형이다. 원인은 원본이 시점 조건과 화법을 한 문장에 붙여 놓은 것이라, 조건을 살리면 화법이 따라온다 (`73f5b60`)
- **B5 원본 항목 2건 복원** — 변환자가 자체 대조 중 말없이 줄인 것을 되돌렸다. `인력 감축 서사가 아니라 승수 서사로` → `감축 서사가 아니라`는 **논지를 뒤집을 뻔했다**. 무엇을 감축하지 않는다는 것인지가 사라진다 (`715a89f`)
- **독립 검증 지적 6건** — `13개 축`(실제 12행) 4군데, Q&A 제목 문항 수 오기 2건, 지식 지도 `50편`(실제 51), 원본 출처 구분 주석 누락, 도입 순서 주차 수치 소실, 복원한 주석의 근거 주체가 3사 비교표에 성립하지 않음. **셋이 "세어 쓴 수가 틀린" 유형이고 전부 자동 검사를 통과한다** (`b654a5a`, `629ea9c`)

### 문서

- **요구사항 §15 추가** [`docs/superpowers/specs/2026-08-07-tech-blog-requirements.md`](docs/superpowers/specs/2026-08-07-tech-blog-requirements.md) — `ai-agent` 실행에서 확정된 것. 중복 판정이 실행 단계에서 뒤집힌 사례 4건(CR-6.3·6.4), 금칙어 제거가 다른 축의 정확성을 깨뜨리는 두 유형(CR-1.9·1.10), 자동 검사가 못 잡는 3종(SC-12·13·14), 실명 기업 서술의 두 갈래, 검증자 미분리 구간 기록(SC-15)

## 2026-08-07

> **기술블로그 신설.** 별도 저장소(`yanadoo-exit/shared/knowledge`)의 학습 정리 md 143개를 소스로, **편집·검수를 거친 발행본만** 이 저장소에 두는 구조로 기술블로그를 추가했다. 1차 `search-engineering` 6편, 2차 첫 카테고리 `rag` 25편을 발행해 **총 31편**이 공개됐다. `main` 배포 완료(`d543dac`).

### 추가

- **블로그 콘텐츠 파이프라인** [`lib/blog/`](lib/blog/) — 타입(`types.ts`), frontmatter 검증(`frontmatter.ts`), 파일시스템 스캔 로더(`loader.ts`). 위키가 5개 문서를 배열에 하드코딩하던 방식을 **디렉터리 스캔**으로 일반화했다. 스키마를 위반하면 **빌드가 실패**해 잘못된 글이 발행되지 않는다 (`55b96cc`, `3dd2448`, `fc60c13`)
- **목차 생성 공용화** [`lib/toc.ts`](lib/toc.ts) — `lib/wiki.ts`의 `buildToc`을 추출해 위키·블로그가 공유한다. 기존 import 경로(`@/lib/wiki`)는 re-export로 유지해 호출부를 한 줄도 고치지 않았다 (`1097046`)
- **블로그 라우트 5종** [`pages/blog/`](pages/blog/) — 홈 / 카테고리 목록 / 포스트 상세 / 태그 인덱스 / 태그별 목록. 렌더링은 기존 `components/markdown.tsx`·`mermaid.tsx`를 그대로 재사용한다 (`2e7c6c3`, `cfe8d74`)
- **sitemap 자동 생성** [`scripts/generate-sitemap.mjs`](scripts/generate-sitemap.mjs) — `next build` 후 `out/`을 스캔해 만든다. 라우팅 로직을 두 번 구현하지 않아도 되고 실제로 생성된 페이지만 들어간다. `noindex`인 위키·로드맵·notion 경로는 제외. 수동 관리하던 `public/sitemap.xml` 삭제 (`9d1fd6f`, `a575b5c`)
- **발행본 31편** [`content/blog/`](content/blog/) — 검색 엔지니어링 6편(`0015a58`), RAG 25편(`ed8feb2`~`4259c84`). 도식 **103개** 보존, 내부 링크 96개(깨짐 0), 시리즈 7개
- **카테고리·태그 통제 어휘** [`content/blog/categories.ts`](content/blog/categories.ts), [`content/blog/tags.ts`](content/blog/tags.ts) — 143편 전수 조사로 카테고리 12개와 태그 81종을 확정하고 **코드에서 강제**한다. 어휘를 벗어나면 빌드가 실패해, 128편 배치 변환에서 `redis`/`redis-cache` 같은 동의어 분산이 생기지 않는다 (`404d220`)
- **단위 테스트 36개** [`tests/blog/`](tests/blog/) — Vitest 도입. 목차 생성·frontmatter 검증·파일 스캔·시리즈 네비게이션·빈 카테고리 필터 (`55b96cc` 외)
- **메인 사이트 연동** [`pages/index.tsx`](pages/index.tsx), [`data/portfolio.ts`](data/portfolio.ts) — 네비게이션에 "기술 노트", "글·링크" 섹션에 **대표글 제목을 `getStaticProps`로 노출**. 하드코딩하지 않으므로 대표글이 바뀌면 자동 반영된다 (`de844cd`, `79770c6`)

### 수정

- **CRLF 개행에서 목차가 통째로 비던 문제** [`lib/toc.ts`](lib/toc.ts) — `md.split("\n")`로 자르면 Windows 체크아웃 시 줄 끝에 `\r`이 남는데, **JS의 `.`은 `\r`을 line terminator로 보아 제외**하므로 `/^(##|###)\s+(.*)$/`의 `(.*)`가 `$`에 닿지 못해 헤딩이 하나도 매치되지 않았다. 브랜치를 옮기는 것만으로 재현된다. `split(/\r?\n/)`로 수정하고 로더가 본문을 LF로 정규화한다 — 그러지 않으면 같은 커밋에서도 빌드 플랫폼에 따라 산출물이 달라진다 (`b454017`)
- **시리즈 글의 이전/다음이 무관한 글을 가리키던 문제** [`lib/blog/loader.ts`](lib/blog/loader.ts) — `getAdjacentPosts`가 카테고리 내 날짜순 이웃을 반환하는데, 분할된 긴 글은 한 원본에서 나와 `date`가 전부 같아 정렬이 **제목 가나다순 타이브레이커**로 넘어갔다. 시리즈 소속 23편 중 **22편**이 어긋났다. `series`가 있으면 같은 시리즈 안에서 `seriesOrder` 순으로 잇고, 시리즈를 **닫힌 단위**로 둔다(1편 prev=null, 마지막 next=null). 1차에는 시리즈가 없어 이 경로가 한 번도 실행된 적이 없었다 (`da2366c`)
- **선택 필드의 `undefined` 키가 빌드를 깨뜨리던 문제** [`lib/blog/frontmatter.ts`](lib/blog/frontmatter.ts) — `validateFrontmatter`가 값 없는 선택 필드도 키로 남겨, Next.js의 `getStaticProps` JSON 직렬화가 실패했다(`Error serializing .post.series`). 조건부 스프레드로 키 자체를 만들지 않게 고치고 우회하던 헬퍼를 삭제. 우회책은 새 페이지마다 잊지 않고 호출해야 하는 규율이라 페이지가 늘면 반드시 빠뜨린다 (`25fb8a4`)
- **태그 슬러그 검증 부재** [`lib/blog/frontmatter.ts`](lib/blog/frontmatter.ts) — 형식(`^[a-z0-9-]+$`)과 통제 어휘(81종)를 이중으로 강제한다. 없으면 `Redis`와 `redis`가 별개 태그 페이지가 되고 `CI/CD`의 슬래시가 경로를 깨뜨린다. 위반한 값을 **전부** 오류 메시지에 담아 어느 태그가 문제인지 바로 찾을 수 있게 했다 (`1948cc5`, `404d220`)
- **글이 0편인 카테고리가 노출되던 문제** — 2차 준비로 카테고리 12개를 미리 등록했으나 실제 글은 2개에만 있었다. 빈 페이지가 색인되면 "콘텐츠 없음"으로 평가받는다. `getPublishedCategories()`로 목록에서 빼고 `getStaticPaths`에서도 제외해 **페이지 자체를 만들지 않는다**. sitemap 93 → 83 URL (`d543dac`)
- **카운트 텍스트의 라이트·다크 명도가 뒤바뀐 문제** [`pages/blog/`](pages/blog/) — `text-slate-400 dark:text-slate-500`은 흰 배경에서 연하고 어두운 배경에서 진해 양쪽 다 대비가 낮다. 두 값을 맞바꿔 Lighthouse `color-contrast` 실패를 해소 (`a53a815`)

### 문서

- **요구사항·작업계획서** [`docs/superpowers/`](docs/superpowers/) — 143편 실측 기반 요구사항(§1~§14)과 1차 실행계획. 2차 실행 사양(카테고리 12개·태그 81종·발행 제외 9편)과 `rag` 실행에서 확정된 규칙 정정을 포함한다 (`b0077fa`~)

### 변경

- **포지셔닝 문구를 개발총괄·CTO 지향으로 정비** [`pages/index.tsx`](pages/index.tsx), [`pages/en/index.tsx`](pages/en/index.tsx) (`9d5e165`)
  - 국·영문의 `<title>`·`og:title`·`twitter:title`·JSON-LD `jobTitle`·히어로 부제를 `Agile Developer & Tech Lead` → **`개발총괄·CTO 지향 20년차 백엔드·플랫폼 리더`**(영문 `Head of Engineering / CTO-track · 20-yr Backend & Platform Leader`)로 교체
  - LinkedIn Featured 카드가 `og:title`을 따라오므로 카드 문구도 배포 후 함께 갱신된다
- **소개 철학 섹션을 AI-네이티브 톤으로 재작성** [`pages/index.tsx`](pages/index.tsx) (`9d5e165`)
  - "Tech Lead로서의 철학" → **"개발 리더로서의 철학"** 으로 개제, 폐기 문구("동료가 6개월 뒤에 읽어도 이해되는 코드") 제거
  - "개발 실행은 이미 AI로 이동 / 좋은 코드 = AI가 정확히 읽고 안전하게 고칠 수 있는 구조"로 재작성, 리더의 일을 아키텍처 표준·코드 리뷰·보안 게이트·검증 파이프라인 구축으로 서술

## 2026-07-21

> 작업 브랜치 `feat/system-diagram-animation` — **main 미병합 · 미배포**. 시스템 구성도 섹션의 정지 PNG를 데이터 흐름이 애니메이션되는 인라인 SVG로 교체하는 작업. **다이어그램 10개 전부 완료**되어 회사 4곳(야나두 → SK브로드밴드 → CJ헬로비전 → 쌍용정보통신) 카드가 모두 화면에 노출된다. 계획서 Task 1~16 완료.

### 추가

- **흐름도 렌더링 파이프라인** [`components/flow-diagram/`](components/flow-diagram/) — 순수 데이터(`FlowSpec`)를 받아 SVG 다이어그램을 그리는 컴포넌트 묶음. 타입·기하 계산·스펙 검증기(`9661d33`), 뷰포트 감지 훅(`307b362`), SVG 프리미티브(노드 5종·화살촉 마커·계층 띠·범례, `0a16d1c`), 렌더러(`db0e674`). 자동 레이아웃 엔진 없이 좌표를 데이터에 직접 명시하는 방식이며 신규 npm 의존성 0개
- **CSS 흐름 애니메이션** [`styles/globals.css`](styles/globals.css) — 엣지 종류별 색 토큰(요청/데이터/외부/비동기, 라이트·다크 각각)과 `stroke-dashoffset`·`offset-path` 키프레임. `prefers-reduced-motion: reduce`에서 전면 정지, 뷰포트 밖 다이어그램은 `data-flow-animate`로 애니메이션 차단 (`e54e0b5`)
- **SKB 로그 기반 추천·검색 흐름도** [`data/diagrams/skb-flow-search.ts`](data/diagrams/skb-flow-search.ts) — 4계층 19노드 19엣지. 로그 수집(FileBeat→Logstash→Kafka→ES) / 분석(Python·Sanic) / 서빙 / 텍스트·NUGU 음성 검색 (`a1cefc9`)
- **나머지 흐름도 9개** [`data/diagrams/`](data/diagrams/) — 파일럿 이후 회사 4곳의 도식을 모두 채웠다. 원본 PNG가 있는 것은 구조를 그대로 재현하고, 화면 캡처뿐이거나 원본이 없는 것은 **원본에 문자로 실재하는 항목만** 노드로 올려 재구성했다
  - 야나두: AI 서비스(16노드, 신규 제작 `b083a79`), 전체 시스템(24노드 — 이 저장소 최대, 연결선 신규 작성 `1710c87`), 앱(11노드 `89536e2`)
  - SK브로드밴드: B tv N-Screen(13노드 `6544c1d`), 시스템 아키텍처(13노드 `4b01f64`), 서빙 API·영상 메타·이미지 플랫폼(22노드 `737b59e`)
  - CJ헬로비전: TVING N-Screen(11노드, 신규 제작 `feedc11`)
  - 쌍용정보통신: KT QOOK TV A-MOC · 가입자계 ISM(6+7노드, 자료 없이 기억 기반이라 **사용자 검수를 거쳐** 커밋 `0008e3d`)
- **야나두 · CJ헬로비전 · 쌍용정보통신 그룹** [`data/portfolio.ts`](data/portfolio.ts) — 회사 그룹 3개를 신설해 총 4개 그룹 10장 구성을 완성. 야나두 회사 라벨은 사이트 전체와 동일하게 `(주)야나두 a kakao company (구 카카오키즈)`로 표기
- **모바일 전용 2열 재배치** [`components/flow-diagram/stacked-layout.ts`](components/flow-diagram/stacked-layout.ts) — 좁은 화면에서 위상 정렬로 노드 순서를 정하고 계층별 2열로 접어 가로 스크롤을 없앤다. 비인접 엣지는 오른쪽 통로 4트랙으로 분산 (`0e16ac6`)
- **빌드 타임 스펙 검증** — 엣지가 없는 노드를 가리키거나 좌표가 viewBox를 벗어나면 `npm run build`가 실패한다. `DiagramItem.specId`가 실제 스펙으로 해석되는지도 함께 검사해, 오타로 카드가 조용히 사라지는 것을 막는다 (`04c2af8`, `a95fa12`)

### 변경

- **시스템 구성도 섹션 구조 개편** [`pages/index.tsx`](pages/index.tsx), [`data/portfolio.ts`](data/portfolio.ts), [`components/system-diagram-card.tsx`](components/system-diagram-card.tsx) — 3열 PNG 카드 그리드를 회사별 그룹 + 세로 스택으로 교체. 재직 기간은 `DiagramGroup.period` 단일 출처에서 가져오며, 확대 보기에 `[흐름도] / [원본 자료]` 전환 탭을 추가(원본 PNG가 없는 신규 도식은 탭 숨김). 경력 타임라인 카드(`Career.png`)는 상단 경력 섹션과 중복되어 제거 (`6f3579c`)
- **모바일 전환 기준을 컨테이너 폭 실측 기반으로 교정** — 전환 판단은 화면 폭이 아니라 카드 안쪽 컨테이너 폭(`화면 폭 − 80px`)에서 걸린다. 2열 임계값 290px, 전환 기준 `max(minWidth, viewBox.w × 0.7)`. 360·375·393·430px 실기기가 전부 2열로 들어오며, 768px 태블릿도 축소 대신 재배치된다 (`f380864`, `1c6ce07`)
- **쌍용정보통신 프로젝트 명칭을 경력 섹션과 통일** [`data/portfolio.ts`](data/portfolio.ts) — 계획 문서의 "KT IPTV A-MOC"·"통합보안관제 NMS" 대신 경력 카드의 확정 표기인 **"KT QOOK TV A-MOC 플랫폼"**·**"KT 가입자계 통합보안관리시스템(ISM)"** 을 사용한다. 같은 페이지 안에서 이름이 갈리면 별개 프로젝트로 오해된다 (`0008e3d`)

### 수정

- **다이얼로그를 열면 SVG DOM id가 중복**되던 문제 — 카드와 다이얼로그가 같은 스펙을 동시에 렌더해 `<title>`/`<desc>`/마커 id가 겹쳤다. `useId()`로 인스턴스별 접두어를 생성해 분리 (`04c2af8`)
- **2열 배치에서 바로 아래 칸 연결이 우회선으로 빠지던 문제** — 순번 차이만으로 판정하면 앞선 계층의 노드 수가 홀수일 때 다른 열을 같은 열로 오인한다. 실제 `x` 좌표 일치까지 확인하도록 수정, 우회 엣지 7→5개 (`27b6651`)
- **좁은 화면에서 엣지 라벨이 노드에 겹쳐 글자가 잘리던 문제** [`components/flow-diagram/stacked-layout.ts`](components/flow-diagram/stacked-layout.ts) — 모바일 재배치는 노드가 열 전체 폭을 차지해 라벨을 놓을 여백이 구조적으로 없다(`Collection` → `collectio`). 좁은 화면에서는 라벨을 그리지 않고, 방향은 화살촉이·종류는 색과 범례가 계속 전달하도록 정리 (`85e14b6`)
- 계층 y 범위가 겹칠 때 노드가 두 계층에 중복 배치되던 문제 (`17263fd`)
- 타원 노드에서 대각선 화살표가 도형에서 떨어져 보이던 문제(사각형 기준 앵커 계산), 긴 한글 라벨이 노드를 넘치던 문제, 노드마다 생기던 불필요한 탭 스톱 등 프리미티브 지적 5건 (`2707934`)

## 2026-07-08

### 추가

- **프로덕트 리더십 v2 시안 페이지** [`pages/product-lead-v2/index.tsx`](pages/product-lead-v2/index.tsx) (공개 라우트 `/product-lead-v2/`, 다른 세션에서 작성) — `/product-lead/`와 동일한 레이아웃·효과에 문구만 재구성한 A/B 시안. 핵심 차이: ① 히어로·핵심 요약을 **"차세대 CMS(NCMS) 재구축 발주 PM 완주"** 중심으로 재배치(재구축 경험을 앞세우고 TVING은 "그 도메인의 1세대"로 후치) ② "원조 구축" → **"1세대 구축/설계"** ③ "발주사 PM" → **"발주 PM"** 표기 ④ CJ 여정 임팩트를 "재구축 판단의 출발점"으로 연결 ⑤ SEO description에 "20~30인 조직 총괄" 명시 (`5421a9c`)

### 변경

- **프로덕트 리더십 진입 링크를 모두 `/product-lead-v2/`로 교체** — 히어로 보조 CTA·`#product` 섹션 CTA [`pages/index.tsx`](pages/index.tsx), sitemap 등록도 v1→v2 교체(`lastmod` 2026-07-08) [`public/sitemap.xml`](public/sitemap.xml). v1(`/product-lead/`)은 직접 URL로만 접근 가능하며 자기 canonical을 유지한 채 남아 있음 — 시안 확정 시 정리 필요 (`6524b1e`)

## 2026-07-07

### 추가

- 프로덕트 리더십 대표 여정에 **쌍용정보통신(통신연구소/뉴미디어기술팀, 2005.11 - 2012.06 · 6년 8개월)** 경력 추가 — KT ISM·KT QOOK TV A-MOC를 "20년 OTT·플랫폼 여정의 출발점"으로 서술해 페이지 곳곳의 "20년" 표현과 정합 확보 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`77558bb`)
- `/product-lead/`에 메인과 동일한 **배경 효과**(도트 패턴·그라데이션 워시·블롭 2개·SVG 스트라이프 `HeroStripeBackdrop`)와 핵심 요약 4카드 **동전 회전**(`CoinFlipDeck`, 4번째 카드 딜레이 0.54s를 [`styles/globals.css`](styles/globals.css)에 추가), h1 **손글씨 인사말**("안녕하세요, 허우용입니다" 리빌) 적용 — 기존 타이틀 "플랫폼 프로덕트 리더"는 부제로 이동 (`fa6f660`)
- 대표 여정 카드에 **역할 서술(`roleDetail`) 추가** — 안내 문구가 약속한 "맥락 → 역할 → 접근 → 임팩트" 4단 구조 완성. 직함(카드 제목)과 구분해 각 시기의 책임 범위를 한 문장으로 서술하고, 역할 줄로 옮긴 내용(전 직군 총괄, 발주사 PM)은 접근 문장에서 덜어내 중복 정리 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`07beac5`)

### 변경

- `/product-lead/`에서 **"처음"이라는 표현 제거** — "TVING CMS를 처음 구축한/만든" → "TVING CMS를 구축한/만든". 핵심 요약 CMS 카드·대표 여정 역할 서술·SEO description·히어로 문단 4곳 일괄 적용 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`ece6b85`)
- 핵심 요약 CMS 카드 문구 정비 — "통과"→"경험", 발주사 PM 표기 통일 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`732cbd9`)
- 소개의 "개발자로서의 철학"을 **"Tech Lead로서의 철학"** 으로 재작성 — 팀의 코드 표준 → 기획·UI/UX 제품 관점(신규 단락) → 위임·성장·AI의 3단락 구성. 기존 시그니처 메시지(6개월 뒤에도 읽히는 코드, AI 활용)는 리더 관점으로 승격해 유지 [`pages/index.tsx`](pages/index.tsx) (`ad35823`, `3ffdaab`)
- 프로덕트 리더십 대표 여정의 **팀명을 메인 경력 카드 기준으로 통일** — SKB `(B tv 백엔드 · 미디어클라우드)` → `SK Broadband (AI 서비스 개발스쿼드/미디어클라우드스쿼드)`, `CJ 헬로비전` → `CJ Hellovision` [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`ad35823`)

### 수정

- `/product-lead/` **모바일 반응형 정비** — 역량 매핑 표의 요구 역량 열 `whitespace-nowrap`을 `md:` 이상으로 한정해 모바일에서 근거 열이 짓눌리던 문제 해결, h1 손글씨 인사말에 `break-keep`을 적용해 좁은 화면에서 어절 단위 줄바꿈. 320/390/1280px 실화면 검증 완료 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`cab2203`)
- **모바일 타이포그래피 최적화**(iPhone 12 Pro/14 Pro Max 실화면 검증) — ① 전역 `word-break: keep-all` + `overflow-wrap: break-word`로 사이트 전체 한글 어절 단위 줄바꿈 [`styles/globals.css`](styles/globals.css) ② 메인 손글씨 h1 모바일 크기 60px→48px(`text-5xl sm:text-6xl md:text-8xl`)로 한 줄 유지 ③ 히어로 소개 문단의 강제 `<br/>`을 모바일에서 숨겨 자연스러운 문단 흐름(640px+ 기존 유지) ④ `/product-lead/` CTA 버튼 모바일 가운데 정렬 (`1343171`)
- 경력·대표 여정 카드의 **재직 년월 배지와 근속기간을 같은 줄에 배치**(세로 쌓임 제거) — 메인 4곳·프로덕트 리더십 4곳 [`pages/index.tsx`](pages/index.tsx), [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`f6af9c7`)

### 변경

- 대표 여정을 **최신 경력부터 역순 정렬**(야나두 → SKB → CJ헬로비전 → 쌍용정보통신)하고, 기간을 연 단위에서 **월 단위(`2022.02 - 재직중` 등) + 근속기간(연·개월)** 표기로 통일 — 메인 경력 카드와 동일 포맷·레이아웃 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`9a11609`)
- `/product-lead/`에 메인 페이지의 CSS 효과 재사용 적용 — 전 섹션 `SectionReveal` 스크롤 등장, 카드 hover 그림자, CTA 버튼 hover 리프트, 프로필 사진 동전 회전(`profile-coin-*`). 신규 CSS 추가 없음 (`9a11609`)

## 2026-07-06

### 추가

- 경력 카드에 **근속기간(연·개월)** 표시 — 날짜(뱃지)와 분리해 파란색으로 강조, 스캔 시 재직 기간이 바로 읽히도록 함 [`pages/index.tsx`](pages/index.tsx). 소개 "현재 포지션" 카드도 동일 표기로 통일 (`b370439`)
- 히어로 인사말을 **macOS "hello" 손글씨 스타일**로 변경 — Nanum Pen Script(Google Fonts, [`pages/_document.tsx`](pages/_document.tsx)) + 왼→오 획이 그려지는 손글씨 리빌 애니메이션 [`styles/globals.css`](styles/globals.css), `prefers-reduced-motion` 존중 (`b370439`)

### 변경

- 쌍용정보통신 재직 시작을 **정식 채용일 기준**으로 정정: `2005.05` → `2005.11 - 2012.06 (6년 8개월)` [`pages/index.tsx`](pages/index.tsx) (`b370439`)
- 프로덕트 리더십 페이지 결제·정산·구독 역량 문구를 총괄 기준 정식 문구로 교체(플레이스홀더 제거) [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`b370439`)
- SKB 경력을 **차세대 CMS(NCMS) 재구축 발주사 PM**(MSA 설계·검토·오픈 조율) 역할로 표현 정확화, CJ TVING 임팩트 `처음부터 구축`→`팀과 함께 구축`, 대표 여정 부제의 `정량 지표 보강 예정` 문구 제거 [`pages/index.tsx`](pages/index.tsx), [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) (`431f3c0`)

## 2026-07-05

### 추가

- 프로덕트 리더십 요약 페이지 [`pages/product-lead/index.tsx`](pages/product-lead/index.tsx) — 공개 라우트 `/product-lead/`. 핵심 요약 4·대표 여정 3(맥락→접근→임팩트)·플랫폼 프로덕트 리드 요구 역량 매핑 표. 정성 서술 기반이며 정량 지표는 실측치 확보 후 보강 예정 (`b9b88a6`)
- 홈 `#product` "프로덕트 리더십" 요약 섹션 + 히어로 보조 CTA(`플랫폼 프로덕트 리더로 보기`) + 내비 앵커 [`pages/index.tsx`](pages/index.tsx), [`data/portfolio.ts`](data/portfolio.ts) (`b9b88a6`)
- [`public/sitemap.xml`](public/sitemap.xml) — `/product-lead/` 등록(공개 색인) (`b9b88a6`)

### 변경

- 회사 표기 정비: `야나두` → **(주)야나두 a kakao company (구 카카오키즈)**, 직책을 **커머스개발실장**으로 [`pages/index.tsx`](pages/index.tsx)·[`pages/en/index.tsx`](pages/en/index.tsx)의 JSON-LD·메타 description·히어로·경력 카드에 반영. 서비스/제품명("야나두 AI 서비스", "야나두 앱")은 유지 (`b9b88a6`)

## 2026-06-02

### 추가

- 소개 카드 동전 회전: [`components/coin-flip-deck.tsx`](components/coin-flip-deck.tsx) — 스크롤 진입 시 현재 포지션/전문 분야/팀 규모 카드가 0.18s 간격으로 순차적으로 Y축 360° 한 바퀴 회전 후 감속 정지 (`prefers-reduced-motion: reduce` 시 비활성), [`styles/globals.css`](styles/globals.css) 키프레임 (`c94f3f6`)

### 변경

- [`pages/index.tsx`](pages/index.tsx) — 히어로 소개를 20년+ 엔지니어링 리더 관점으로 재작성(KT 추가, 온프레미스(IDC)/AWS 경험 명시), 개발자 철학을 코드 가독성 중심 메시지로 다듬고 AI 업무 효율 극대화 단락 추가, 메타 `description` 정비 (`7d05e2c`)
- [`pages/en/index.tsx`](pages/en/index.tsx) — 영문 페이지를 한글 버전과 동일한 톤으로 갱신(소개·Highlights·메타 description) (`2b08b35`)

## 2026-05-01

### 추가

- 영문 요약 페이지 [`pages/en/index.tsx`](pages/en/index.tsx), 네비에서 EN 링크
- SEO: [`components/site-head.tsx`](components/site-head.tsx) — canonical, Open Graph, Twitter Card, JSON-LD `Person`
- [`public/robots.txt`](public/robots.txt), [`public/sitemap.xml`](public/sitemap.xml)
- [`lib/site.ts`](lib/site.ts) — `getSiteOrigin()` / `absoluteUrl()`, 선택적 `NEXT_PUBLIC_SITE_URL`
- [`data/portfolio.ts`](data/portfolio.ts) — 네비, 시스템 다이어그램, 스킬, 글·링크 데이터
- [`components/portfolio-nav.tsx`](components/portfolio-nav.tsx) — 스킵 링크, 모바일 메뉴, 스크롤 시 네비 강조, 데스크톱 링크 밑줄 모션
- [`components/theme-toggle.tsx`](components/theme-toggle.tsx) — 라이트/다크 (`html.dark`)
- [`components/section-reveal.tsx`](components/section-reveal.tsx) — 섹션 스크롤 등장
- [`components/system-diagram-card.tsx`](components/system-diagram-card.tsx) — 시스템 구성 다이얼로그 공통화·접근성
- [`components/hero-stripe-backdrop.tsx`](components/hero-stripe-backdrop.tsx) — 히어로 SVG 줄무늬 흐름 배경
- [`pages/_document.tsx`](pages/_document.tsx) — `lang="ko"`
- [`docs/site-renewal-completion-report.md`](docs/site-renewal-completion-report.md) — 리뉴얼 완료·QA·보안 요약 보고
- ESLint: [`.eslintrc.json`](.eslintrc.json), `npm run lint` 스크립트 및 [`.github/workflows/deploy.yml`](.github/workflows/deploy.yml) CI 단계
- [`HANDOFF.md`](HANDOFF.md), [`CHANGELOG.md`](CHANGELOG.md) — 세션 인수인계 및 변경 이력

### 변경

- [`README.md`](README.md) — 문서 링크 표, 기술 스택·프로젝트 구조 보강, 로컬 `/en` 안내
- [`pages/index.tsx`](pages/index.tsx) — 위 컴포넌트·데이터 연동, 다크 모드 클래스, 연락 `mailto`/`tel`, 푸터 연도 동적화, 히어로 blob·스태거·줄무늬
- [`styles/globals.css`](styles/globals.css) — shadcn용 CSS 변수, 전역 `*` transition 제거, 히어로/섹션/네비 모션, 줄무늬 키프레임
- [`tailwind.config.js`](tailwind.config.js) — `darkMode: 'class'`, shadcn 색 토큰, `data/` content 경로
- [`pages/_app.tsx`](pages/_app.tsx) — `next/font` Inter
- Next.js **14.2.35** 및 `eslint-config-next` 정렬 ([`package.json`](package.json))

### 수정

- README에서 구식 `npm run export` 안내 제거(이전 세션), `out/`·`NEXT_PUBLIC_SITE_URL`·`lint` 반영

---

이전 기록은 Git 히스토리를 참고하세요. (초기 `CHANGELOG.md` 도입일 기준 이전 변경은 본 파일에 없을 수 있습니다.)
